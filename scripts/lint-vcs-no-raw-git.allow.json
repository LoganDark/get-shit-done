{
	"$schema_version": 2,
	"$migration_note": "Phase 8 Plan 1 D-03 + D-04: migrated from top-level files/globs to per-entry { path|glob, reason, owner }; expires field dropped (solo-dev context override of REQUIREMENTS-LINT-02).",
	"$comment_2_1_09": "Phase 2.1 plan 09 (D-22 closing) — removal-pressure precedent retained as documentation; expanded into per-entry reasons below. D-22 hard rule continues: never add entries; only remove.",
	"$comment_19_10": "Phase 19 plan 19-10 (MERGE-05, locked operator decision — supersedes D-22 for this merge): allowlist re-pointed at the adopted upstream tree. New src/*.cts + hook/CI/fixture entries ADDED below with per-entry reasons; every legacy sdk/** and get-shit-done/** entry RETAINED until 19-12 deletes the residual trees (ledger row: pending-19-12-residue-deletion in 19-MERGE-AUDIT.md). 19-12 Task 2 strips the legacy lines and re-runs the gate.",
	"$comment_19_12": "Phase 19 plan 19-12 Task 2: residue deletion executed — the 11 legacy sdk/** path/glob entries STRIPPED (the pending-19-12-residue-deletion ledger row is resolved). Reasons in surviving entries may still cite sdk/ paths as historical lineage; the lint matches on path/glob fields only.",
	"entries": [
		{ "path": "tests/vcs-cjs-smoke.test.cjs", "reason": "Test fixtures — CJS smoke test for the vcs adapter requires raw setup.", "owner": "@LoganDark" },
		{ "path": "tests/__tools__/capture-vcs-baselines.cjs", "reason": "Test fixtures — golden-parity baseline capture harness needs raw git/jj invocations.", "owner": "@LoganDark" },
		{ "path": "scripts/lint-vcs-no-raw-git.cjs", "reason": "Scan scripts — lint-self contains the pattern strings it scans for.", "owner": "@LoganDark" },
		{ "path": "scripts/check-skip-count.cjs", "reason": "Scan scripts — skip-count check reads from origin/main via raw git log.", "owner": "@LoganDark" },
		{ "path": "scripts/base64-scan.sh", "reason": "Scan scripts — shell-only pre-commit-style filter; cannot use the JS-side adapter without circularity.", "owner": "@LoganDark" },
		{ "path": "scripts/prompt-injection-scan.sh", "reason": "Scan scripts — shell-only pre-commit-style filter; cannot use the JS-side adapter without circularity.", "owner": "@LoganDark" },
		{ "path": "scripts/secret-scan.sh", "reason": "Scan scripts — shell-only pre-commit-style filter; cannot use the JS-side adapter without circularity.", "owner": "@LoganDark" },
		{ "glob": "tests/**/*.test.cjs", "reason": "Test fixtures — CJS test surface requires raw setup invocations.", "owner": "@LoganDark" },
		{ "glob": "tests/**/*.test.ts", "reason": "Test fixtures — TS test surface requires raw setup invocations.", "owner": "@LoganDark" },
		{ "glob": ".github/workflows/**", "reason": "Hooks/CI — CI workflows orchestrate the adapter, not consume it; raw `git` is the substrate.", "owner": "@LoganDark" },
		{ "glob": ".githooks/**", "reason": "Hooks/CI — pre-commit/pre-push shell hooks predate the adapter and serve as the substrate.", "owner": "@LoganDark" },
		{ "glob": "docs/**", "reason": "Documentation — markdown content references `git` as English text; never executed.", "owner": "@LoganDark" },
		{ "glob": ".planning/**", "reason": "Documentation — planning markdown references `git` as English text; never executed.", "owner": "@LoganDark" },
		{ "path": "hooks/gsd-graphify-update.sh", "reason": "Hooks/CI — shell hook substrate with an explicit dual leg (VCS-audit 2026-07-08): probes jj FIRST (jj workspace root; the colocated git HEAD is normally detached under jj, which used to defeat the branch gate), falls back to git; default-branch + head capture carry jj forms (jj bookmark list / jj log -r @-).", "owner": "@LoganDark" },
		{ "path": "tests/helpers/graphify.cjs", "reason": "Test fixtures — graphify test helper needs raw git to construct deterministic commit graphs.", "owner": "@LoganDark" },
		{ "path": "src/vcs/exec.cts", "reason": "19-10 re-point of sdk/src/vcs/exec.ts (ported 19-05) — VCS adapter internals; raw `git` invocation is the substrate the cross-backend surface wraps.", "owner": "@LoganDark" },
		{ "path": "src/vcs/backends/git.cts", "reason": "19-10 re-point of sdk/src/vcs/backends/git.ts (ported 19-05) — the git backend implementation; raw `git` is the substrate.", "owner": "@LoganDark" },
		{ "path": "src/vcs/git/parallel.cts", "reason": "19-10 re-point of sdk/src/vcs/git/parallel.ts (ported 19-05) — git-side parallel-dispatch substrate; raw `git worktree`/`merge`/`branch -D` are the substrate the cross-backend `workspace.parallel.*` surface wraps.", "owner": "@LoganDark" },
		{ "glob": "src/vcs/__tests__/**", "reason": "19-10 re-point of the sdk/src/vcs/__tests__/** glob — parameterized contract suite requires raw `git`/`jj` setup commands; directory lands with the 19-11/19-12 test revival.", "owner": "@LoganDark" },
		{ "path": "src/shell-command-projection.cts", "reason": "Substrate per 19-07 ledger (substrate:adapter-internal exec seam) — the upstream-side spawn substrate (execGit + SpawnResultOutput) mirroring the fork sdk/src/vcs/exec.ts precedent; execGit consumers (VCS-audit 2026-07-08 recount): worktree-safety.cts, worktree-base-ref.cts, git-base-branch.cts (base-branch ladder tiers), capability-source.cts (git-as-transport). src/vcs/ does NOT consume it — it has its own exec.cts seam.", "owner": "@LoganDark" },
		{ "path": "src/worktree-safety.cts", "reason": "Substrate per 19-07 ledger (substrate:git-backend worktree machinery) — pure `git worktree` porcelain/policy with no jj counterpart; jj path served by vcs.workspace.parallel.*.", "owner": "@LoganDark" },
		{ "path": "src/worktree-base-ref.cts", "reason": "Substrate per 19-07 ledger (substrate:git-only base-ref degrade, #683) — worktree HEAD-drift degrade is a git-worktree-specific failure mode; jj parallel dispatch goes through vcs.workspace.parallel.*.", "owner": "@LoganDark" },
		{ "path": "gsd-core/workflows/_runtime-launcher.snippet.sh", "reason": "19-09 ledgered (T-19-23/Pitfall 9): RUNTIME_ROOT three-stage resolution `git rev-parse --show-toplevel || jj workspace root || pwd` keeps a DELIBERATE raw git-first leg to preserve upstream behavior on git/colocated repos; the jj leg is the fork fix. Allowlisted rather than weakening the shell-mode scan pattern (19-10 plan instruction).", "owner": "@LoganDark" },
		{ "path": "hooks/gsd-worktree-path-guard.js", "reason": "19-09 ledgered substrate (substrate:git-worktree-isolation-guard-fail-open-on-jj) — read-only rev-parse probes; FAIL-OPEN on jj-native; tool-layer counterpart of workspace.assert-dispatched-cwd.", "owner": "@LoganDark" },
		{ "path": "hooks/gsd-workflow-guard.js", "reason": "Substrate (19-10 ledger row): upstream PreToolUse workflow guard's single read-only `git branch --show-current` probe; tolerates non-git via status!=0 degrade — same fail-open class as gsd-worktree-path-guard.js.", "owner": "@LoganDark" },
		{ "path": "scripts/changeset/github-release-notes.cjs", "reason": "Upstream release tooling (runGit) on git checkouts; MIGR-05 adapter migration deferred to 19-13 reconciliation per the 19-04 ledger row + deferred-items.md.", "owner": "@LoganDark" },
		{ "path": "scripts/ci-rebase-check.cjs", "reason": "Hooks/CI — CI-only rebase-ability probe (merge --no-ff / --abort) running on GitHub Actions git checkouts; same substrate class as the .github/workflows/** entry.", "owner": "@LoganDark" },
		{ "path": "scripts/ci-test-scope.cjs", "reason": "Hooks/CI — CI scope detection diffs base...head on an Actions git checkout; orchestrates CI, not the adapter product surface.", "owner": "@LoganDark" },
		{ "path": "scripts/lint-legacy-dir-name.cjs", "reason": "Scan scripts — enumerates tracked files via `git ls-files` with a filesystem-walk fallback; cannot route through the JS adapter without circularity (same class as the other scan-script entries).", "owner": "@LoganDark" },
		{ "path": "scripts/mutation-matrix.cjs", "reason": "Hooks/CI — incremental mutation scope diff (base...HEAD) on Actions git checkouts; upstream mutation tooling kept runnable locally per the 19-01 mutation.yml ledger row.", "owner": "@LoganDark" },
		{ "path": "scripts/setup-branch-protection.sh", "reason": "Repo-ops — upstream operator-run one-shot GitHub branch-protection setup; the git lines are operator instructions for a git-hosted checkout, never part of the runtime product surface.", "owner": "@LoganDark" },
		{ "path": "scripts/sync-manifest-versions.cjs", "reason": "Upstream release tooling — version-sync restages manifests via guarded `git add` during release prep on git checkouts (rev-parse gate degrades outside a work tree).", "owner": "@LoganDark" },
		{ "path": "tests/fixtures/index.cjs", "reason": "Test fixtures — upstream fixture factory bootstraps deterministic raw-git repos (createFixture git:true); non-.test helper not covered by the tests/**/*.test.* globs.", "owner": "@LoganDark" }
	]
}
