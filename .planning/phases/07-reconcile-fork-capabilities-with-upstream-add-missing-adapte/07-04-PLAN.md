---
phase: 07-reconcile-fork-capabilities-with-upstream-add-missing-adapte
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - scripts/changeset/github-release-notes.cjs
  - scripts/lint-vcs-no-raw-git.cjs
autonomous: true
requirements:
  - MIGR-05
tags:
  - changeset
  - cross-backend-migration
  - lint-allowlist
  - phase-7

must_haves:
  truths:
    - "github-release-notes.cjs no longer calls cp.execFileSync('git', ...) — runGit helper removed"
    - "validateGitRef uses vcs.refs.exists for the rev-parse --verify probe"
    - "changedFragmentPaths uses vcs.diff({rev: expr.range, nameOnly, paths: ['.changeset']}) for the diff --name-only A..B -- .changeset call"
    - "readFileAtRef uses vcs.refs.readBlob (VCS-15) for the show <ref>:<file> call"
    - "Inline vcs-lint allow-git-here annotation deleted (no more raw git in this file)"
    - "Stale jj-port JSDoc at lines 33-36 deleted (file is now cross-backend)"
    - "scripts/lint-vcs-no-raw-git.cjs runs clean — github-release-notes.cjs no longer requires allowlist entry"
    - "All existing github-release-notes parsing/IR/serialize logic preserved unchanged"
    - "D-17: github-release-notes.cjs migrates to cross-backend vcs.log/vcs.diff/vcs.refs.readBlob (NOT vcs.gitOnly.*); allowlist exception dropped"
    - "D-18: Migrate over delete to preserve upstream-mergeability — file stays in tree as cross-backend rather than being removed"
  artifacts:
    - path: "scripts/changeset/github-release-notes.cjs"
      provides: "Cross-backend version of release-notes rendering using VcsAdapter"
      contains: "createVcsAdapter, vcs.refs.exists, vcs.diff, vcs.refs.readBlob"
    - path: "scripts/lint-vcs-no-raw-git.cjs"
      provides: "Allowlist confirmed clean — no github-release-notes entry needed"
  key_links:
    - from: "scripts/changeset/github-release-notes.cjs (validateGitRef)"
      to: "vcs.refs.exists"
      via: "ref existence probe replaces git rev-parse --verify"
      pattern: "vcs.refs.exists"
    - from: "scripts/changeset/github-release-notes.cjs (changedFragmentPaths)"
      to: "vcs.diff + expr.range + expr.rev"
      via: "name-only diff over a range, filtered to .changeset paths"
      pattern: "vcs.diff.*expr.range"
    - from: "scripts/changeset/github-release-notes.cjs (readFileAtRef)"
      to: "vcs.refs.readBlob (VCS-15 from Plan 1)"
      via: "file content at revision; replaces git show ref:file"
      pattern: "vcs.refs.readBlob"
---

<objective>
Migrate `scripts/changeset/github-release-notes.cjs` from `cp.execFileSync('git', ...)` (line 37 `runGit` helper) to the cross-backend `VcsAdapter` surface, consuming exactly the verbs available after Plan 1: `vcs.refs.exists`, `vcs.diff({...})`, and the new `vcs.refs.readBlob` (VCS-15 fold-in). Per CONTEXT D-17/D-18, the migration preserves upstream-mergeability (file stays cross-backend, not deleted) while dropping the inline lint allowlist annotation.

Purpose: Close MIGR-05. This is the first production cross-backend consumer of `vcs.refs.readBlob` from Plan 1. The script's logic (changeset enumeration + markdown rendering) is unchanged — only the VCS access layer flips.

Output:
- `scripts/changeset/github-release-notes.cjs` with `runGit` helper removed; three call sites (validateGitRef, changedFragmentPaths, readFileAtRef) re-routed through `createVcsAdapter(repo, {}).{refs.exists, diff, refs.readBlob}`.
- Inline `// vcs-lint:allow-git-here` annotation at line 37 deleted.
- Stale `jj-port: dev-only changeset tooling…` JSDoc at lines 33-36 deleted.
- `scripts/lint-vcs-no-raw-git.cjs` runs clean against this file (confirms no other raw-git invocations linger in the changeset directory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-CONTEXT.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-RESEARCH.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-PATTERNS.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-01-SUMMARY.md

@scripts/changeset/github-release-notes.cjs
@scripts/lint-vcs-no-raw-git.cjs

<interfaces>
Adapter exports used by this script (extracted post-Plan-1):

```javascript
// From sdk/dist-cjs/vcs/index.js — cjs consumption pattern (matches Phase 2 plan 02-04 D-01 relative-path shape):
const { createVcsAdapter, expr } = require('../../sdk/dist-cjs/vcs/index.js');

// API:
// const vcs = createVcsAdapter(repoPath, {});  // backend auto-detect
// vcs.refs.exists(rev: RevisionExpr): boolean
// vcs.diff(opts: DiffOpts): DiffResult
// vcs.refs.readBlob(rev: RevisionExpr, path: string): string  // Phase 7 Plan 1 VCS-15
// expr.rev(name: string): RevisionExpr
// expr.range(a: RevisionExpr, b: RevisionExpr): RevisionExpr
```

```typescript
// DiffOpts (post-Plan-1):
interface DiffOpts {
  rev?: RevisionExpr;
  nameOnly?: boolean;
  paths?: string[];
  diffFilter?: 'added' | 'modified' | 'deleted' | 'renamed' | 'typechange';
}

interface DiffResult {
  nameOnly?: string[];
}
```

Existing call sites in github-release-notes.cjs to migrate:
- Line 56: `runGit(repo, ['rev-parse', '--verify', backtick-ref-caret-commit-backtick])` — used purely for existence-checking.
- Line 63: `runGit(repo, ['diff', '--name-only', backtick-from..to-backtick, '--', '.changeset'])` — file enumeration.
- Line 71: `runGit(repo, ['show', backtick-ref-colon-file-backtick])` — blob read at ref.

All three flow through `runGit` (line 32-42), which is the single helper to delete.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate github-release-notes.cjs to VcsAdapter and drop the runGit helper</name>
  <read_first>
    - scripts/changeset/github-release-notes.cjs (entire file, 203 lines — landing site)
    - scripts/lint-vcs-no-raw-git.cjs (allowlist mechanism; understand the inline-annotation pattern at lines ~11-14, 47-48)
    - .planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-PATTERNS.md sections on the changeset migration and allowlist drop
    - .planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-01-SUMMARY.md (confirm VCS-15 readBlob signature landed as planned)
    - sdk/dist-cjs/vcs/index.js (confirm createVcsAdapter + expr exports — should be built post-Plan-1)
    - Existing test file if any — search via `find . -path ./node_modules -prune -o -name '*release-notes*' -type f -print`
  </read_first>
  <behavior>
    - The `runGit` helper at lines 32-42 is removed entirely.
    - A new lazy-init helper provides the VcsAdapter for a given repo path. The first call constructs `createVcsAdapter(repo, {})`; subsequent calls reuse the cached adapter per repo. Lazy init avoids constructing an adapter at module import time (matches helpers.cjs lazy-getter pattern from Phase 1 plan 01-04 W-4).
    - `validateGitRef({ repo, ref, label })` at lines 44-58 replaces the rev-parse-verify probe with `vcs.refs.exists(expr.rev(ref))` and throws `Invalid git ref for <label>: <ref> (does not resolve)` if false. The string-shape validation (ref hygiene regex, no `..`, no `//`, etc.) is preserved unchanged.
    - `changedFragmentPaths({ repo, fromRef, toRef })` at lines 60-68 replaces the diff-name-only call with `vcs.diff({ rev: expr.range(expr.rev(from), expr.rev(to)), nameOnly: true, paths: ['.changeset'] })` and then re-applies the existing `/^\.changeset\/[^/]+\.md$/` regex filter on the result's `nameOnly` array.
    - `readFileAtRef({ repo, ref, file })` at lines 70-72 replaces the show-ref-file call with `vcs.refs.readBlob(expr.rev(ref), file)`.
    - The stale jj-port JSDoc at lines 33-36 (inside the runGit helper) is gone with the helper.
    - The inline vcs-lint allow-git-here annotation at line 37 is gone with the helper.
    - All other functions (`loadFragmentsFromRange`, `buildGithubReleaseNotesIr`, `classifyGroup`, `formatBullet`, `compareUrl`, `serializeGithubReleaseNotes`, `renderGithubReleaseNotes`, exports object) are unchanged.
    - The lint guard `scripts/lint-vcs-no-raw-git.cjs` runs clean across the whole repo after this edit.
  </behavior>
  <action>
    (1) Open the file and replace top-level imports + helper structure with a single coordinated edit.

    (a) Replace the `cp` import at line 3 AND the runGit helper at lines 32-42 AND the JSDoc above it.

    Replace the `const cp = require('node:child_process');` line with NOTHING (delete it — the new code does not use `cp`). Add a new require line after the existing `require('./parse.cjs')` import:
    ```
    const { createVcsAdapter, expr } = require('../../sdk/dist-cjs/vcs/index.js');
    ```

    Replace the existing `function runGit(repo, args) { ... }` block (lines 32-42) with the lazy adapter helper:
    ```
    // Phase 7 MIGR-05: migrated to cross-backend VcsAdapter per D-17 / D-18 — the script
    // stays cross-backend (preserves upstream-mergeability) rather than being deleted.
    // The adapter is constructed lazily per repo path so callers in test fixtures can
    // operate on multiple repos within a single Node process.
    const adapterCache = new Map();
    function getVcs(repo) {
      let vcs = adapterCache.get(repo);
      if (!vcs) {
        vcs = createVcsAdapter(repo, {});
        adapterCache.set(repo, vcs);
      }
      return vcs;
    }
    ```

    (b) Migrate `validateGitRef` (lines 44-58). Replace the existing function body. After the existing string-hygiene validation block (which is PRESERVED unchanged through the regex check), replace the `runGit(repo, ['rev-parse', '--verify', ...])` call with:
    ```
    const vcs = getVcs(repo);
    if (!vcs.refs.exists(expr.rev(ref))) {
      throw new Error('Invalid git ref for ' + label + ': ' + ref + ' (does not resolve)');
    }
    return ref;
    ```

    (c) Migrate `changedFragmentPaths` (lines 60-68). Replace the function body. The `from`/`to` validation calls are preserved. Replace the `runGit(...).split(...).filter(Boolean).filter(...)` chain with:
    ```
    const vcs = getVcs(repo);
    const diffResult = vcs.diff({
      rev: expr.range(expr.rev(from), expr.rev(to)),
      nameOnly: true,
      paths: ['.changeset'],
    });
    const names = Array.isArray(diffResult.nameOnly) ? diffResult.nameOnly : [];
    return names.filter((file) => /^\.changeset\/[^/]+\.md$/.test(file));
    ```

    (d) Migrate `readFileAtRef` (lines 70-72). Replace the function body with:
    ```
    const vcs = getVcs(repo);
    return vcs.refs.readBlob(expr.rev(ref), file);
    ```

    (e) Verify no other references to `runGit` or `cp.execFileSync` remain in the file:
    `grep -cE "runGit|cp\.execFileSync|cp\." scripts/changeset/github-release-notes.cjs`
    Expected: 0.

    (2) Confirm SDK is built (required because the new require statement resolves to dist-cjs):
    `pnpm -C sdk build:cjs`

    (3) Smoke-test the require shape:
    `node -e "const m = require('./scripts/changeset/github-release-notes.cjs'); console.log(Object.keys(m).sort().join(','));"`
    Expected output (sorted): `buildGithubReleaseNotesIr,changedFragmentPaths,classifyGroup,loadFragmentsFromRange,renderGithubReleaseNotes,serializeGithubReleaseNotes,validateGitRef`

    (4) Find and run any existing test:
    ```
    find tests scripts -name '*release-notes*' -type f -not -path './node_modules/*'
    ```
    If a test file exists, run `node --test <file>` (or `pnpm test <file>` for vitest). It must exit 0. If no test file exists, that's a pre-existing testing gap (acceptable — the migration is mechanical and the existing function shapes are preserved).

    (5) Run the lint guard:
    `node scripts/lint-vcs-no-raw-git.cjs`
    Must exit 0 across the whole repo.

    (6) Confirm no separate allowlist JSON file exists (per RESEARCH §"scripts/lint-vcs-no-raw-git.cjs (Plan 4 allowlist drop)"):
    `grep -rn "github-release-notes" scripts/lint-vcs-no-raw-git.cjs`
    Expected: no matches (the allowlist mechanism is inline-annotation only, and we deleted the annotation along with the runGit helper). If `scripts/lint-vcs-no-raw-git.cjs` does reference github-release-notes by name, delete that reference.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && pnpm -C sdk build:cjs >/dev/null 2>&1 && node -e "const m = require('./scripts/changeset/github-release-notes.cjs'); console.log(Object.keys(m).sort().join(','));" && echo "---module-ok---" && grep -cE "cp\.execFileSync|runGit|vcs-lint:allow-git-here" scripts/changeset/github-release-notes.cjs && echo "---raw-git-count-end---" && grep -cE "createVcsAdapter|vcs\.refs\.exists|vcs\.diff|vcs\.refs\.readBlob" scripts/changeset/github-release-notes.cjs && echo "---adapter-count-end---" && node scripts/lint-vcs-no-raw-git.cjs 2>&1 | tail -5; echo "---lint-end---"; grep -n "github-release-notes" scripts/lint-vcs-no-raw-git.cjs 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -C sdk build:cjs` exits 0
    - `node -e "require('./scripts/changeset/github-release-notes.cjs')"` exits 0 (module loads cleanly)
    - `grep -c "cp.execFileSync" scripts/changeset/github-release-notes.cjs` returns 0
    - `grep -c "runGit" scripts/changeset/github-release-notes.cjs` returns 0
    - `grep -c "vcs-lint:allow-git-here" scripts/changeset/github-release-notes.cjs` returns 0
    - `grep -c "createVcsAdapter" scripts/changeset/github-release-notes.cjs` returns >= 1
    - `grep -c "vcs.refs.exists" scripts/changeset/github-release-notes.cjs` returns >= 1
    - `grep -c "vcs.diff" scripts/changeset/github-release-notes.cjs` returns >= 1
    - `grep -c "vcs.refs.readBlob" scripts/changeset/github-release-notes.cjs` returns >= 1
    - `grep -c "expr.range" scripts/changeset/github-release-notes.cjs` returns >= 1
    - `grep -c "expr.rev" scripts/changeset/github-release-notes.cjs` returns >= 3 (validateGitRef + range bounds + readFileAtRef)
    - `node scripts/lint-vcs-no-raw-git.cjs` exits 0
    - `grep -c "github-release-notes" scripts/lint-vcs-no-raw-git.cjs` returns 0 (no allowlist entry remains)
    - All 7 exported functions still present in the module's Object.keys output: buildGithubReleaseNotesIr, changedFragmentPaths, classifyGroup, loadFragmentsFromRange, renderGithubReleaseNotes, serializeGithubReleaseNotes, validateGitRef
  </acceptance_criteria>
  <done>github-release-notes.cjs is cross-backend; runGit helper + stale JSDoc + inline lint annotation deleted; three call sites (validateGitRef, changedFragmentPaths, readFileAtRef) routed through VcsAdapter; first production consumer of VCS-15 readBlob landed; lint-vcs-no-raw-git stays at 0 violations; no per-file allowlist entry needed; MIGR-05 closed; upstream-mergeability preserved per D-18 (the file stays in tree as cross-backend rather than being deleted).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller-supplied `repo` path → createVcsAdapter | a path crosses into the adapter constructor; existing adapter validation applies |
| Caller-supplied `ref` → expr.rev → adapter argv | refnames cross into git/jj argv via the adapter's RevisionExpr translation |
| Caller-supplied `file` → readBlob → adapter argv | file paths cross into `git show ref:file` / `jj file show -- file` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.04-01 | Tampering | refname injection via validateGitRef's `ref` parameter | mitigate | Existing string-hygiene regex `/^[A-Za-z0-9._/-]+$/` + no `..` + no `//` + no leading `-` checks PRESERVED unchanged. Plan 1's adapter-side `validateRefname` provides defense-in-depth on bookmark/branch paths. |
| T-07.04-02 | Tampering | path injection via readFileAtRef's `file` parameter | accept | This is a dev-only release-notes script that runs against trusted changeset paths (`.changeset/*.md`). `readBlob` itself does no path canonicalization (it forwards `file` to `git show` / `jj file show`); both git and jj reject paths attempting to escape the repo (e.g., `../`). No new attack surface introduced; matches upstream behavior. |
| T-07.04-03 | Information disclosure | dev tool reads arbitrary repo blobs | accept | Same as the existing script's threat model — caller already has filesystem access; reading blobs at refs is the whole point. No privilege escalation. |
| T-07.04-SC | Tampering | sdk/dist-cjs/vcs/index.js consumed via relative require | accept | The require path `../../sdk/dist-cjs/vcs/index.js` resolves to in-tree compiled SDK code — no external package, no supply-chain risk introduced by Plan 4. Build provenance is the repo's pnpm + tsc pipeline (Phase 1 VCS-06). |
</threat_model>

<verification>
After Task 1 completes:

1. **Lint guard clean:** `node scripts/lint-vcs-no-raw-git.cjs` exits 0; no allowlist needed.
2. **Module loads cleanly:** the Node smoke check `require('./scripts/changeset/github-release-notes.cjs')` exits 0.
3. **No raw git left:** grep counts for `cp.execFileSync`, `runGit`, `vcs-lint:allow-git-here` all return 0.
4. **Adapter consumers present:** grep counts for `createVcsAdapter`, `vcs.refs.exists`, `vcs.diff`, `vcs.refs.readBlob` all return >= 1.
5. **First VCS-15 consumer landed:** confirms Plan 1 readBlob is wired into real production code.
6. **Existing exports preserved:** module's `Object.keys()` shows the same 7 functions as before.
</verification>

<success_criteria>
- `cp.execFileSync` and the `runGit` helper are gone from `scripts/changeset/github-release-notes.cjs`
- Three call sites (validateGitRef / changedFragmentPaths / readFileAtRef) routed through VcsAdapter
- VCS-15 `readBlob` has its first production consumer (closes the planner judgment-call loop from Plan 1)
- Inline `vcs-lint:allow-git-here` annotation deleted with the helper it gated
- Stale `jj-port: dev-only changeset tooling…` JSDoc deleted
- `node scripts/lint-vcs-no-raw-git.cjs` exits 0 across the whole repo
- `scripts/lint-vcs-no-raw-git.cjs` has zero references to `github-release-notes` (no allowlist entry persists)
- All 7 exports preserved; module API surface unchanged for downstream callers
- File stays in tree as cross-backend per D-18 (upstream-mergeability preserved)
- MIGR-05 closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-04-SUMMARY.md` documenting:
- Before/after line counts (the file shrinks because the runGit helper + JSDoc + inline annotation go away)
- Confirmation that VCS-15 readBlob landed correctly in Plan 1 and works end-to-end through the migrated readFileAtRef
- Lint guard status (0 violations expected)
- Whether an existing test for github-release-notes was found and exercised; if not, note the pre-existing testing gap (not a Plan 4 blocker — migration is mechanical)
- Hand-off note: github-release-notes.cjs is now the canonical example of "migrate dev tooling to adapter per D-17/D-18" for future similar fork-mergeability migrations.
</output>
