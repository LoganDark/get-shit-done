---
phase: 07-reconcile-fork-capabilities-with-upstream-add-missing-adapte
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/quick.md
autonomous: true
requirements:
  - PROMPT-04
tags:
  - workflows
  - markdown
  - dead-code-removal
  - phase-7

must_haves:
  truths:
    - "execute-phase.md no longer contains a raw-git else-branch fallback for wave-cleanup"
    - "quick.md no longer contains a raw-git else-branch fallback for wave-cleanup"
    - "Both files retain the gsd-sdk query call as a single unconditional shell line"
    - "TODO(jj-port) comment blocks pinned to the dead fallback bodies are removed"
    - "No per-runtime sibling files exist (Codex / Gemini / OpenCode) — install.js projection handles them"
    - "Shell syntax remains balanced (if/fi count after edit is unchanged or reduced symmetrically)"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "wave-cleanup invocation collapsed to unconditional gsd-sdk query line"
      pattern: "gsd-sdk query worktree\\.cleanup-wave --manifest"
    - path: "get-shit-done/workflows/quick.md"
      provides: "wave-cleanup invocation collapsed to unconditional gsd-sdk query line"
      pattern: "gsd-sdk query worktree\\.cleanup-wave --manifest"
  key_links:
    - from: "get-shit-done/workflows/execute-phase.md (cleanup section)"
      to: "gsd-sdk query worktree.cleanup-wave (Plan 2 executor)"
      via: "single shell line, no conditional fallback"
      pattern: "gsd-sdk query worktree\\.cleanup-wave --manifest \"\\$WAVE_WORKTREE_MANIFEST\""
    - from: "get-shit-done/workflows/quick.md (cleanup section)"
      to: "gsd-sdk query worktree.cleanup-wave (Plan 2 executor)"
      via: "single shell line, no conditional fallback"
      pattern: "gsd-sdk query worktree\\.cleanup-wave --manifest \"\\$QUICK_WORKTREE_MANIFEST\""
---

<objective>
Hard-delete the `if command -v gsd-sdk >/dev/null 2>&1; then ... else ... fi` blocks in `get-shit-done/workflows/execute-phase.md` (lines 774-891) and `get-shit-done/workflows/quick.md` (lines 787-911). Per CONTEXT D-15 (default-to-hard-delete) and Phase 5 PROMPT-01 (gsd-sdk is always installed), the `else` body is dead code now that Plan 2's executor returns real results.

Purpose: Close PROMPT-04. Reduce maintenance surface (~117-125 LOC per file). Preserves Phase 5 multi-runtime parity automatically because no per-runtime workflow .md siblings exist — `bin/install.js` transform pipeline regenerates Codex / Gemini / OpenCode variants at install time (RESEARCH Assumption A5).

Output: Each file's `if ... else ... fi` block collapses to a single unconditional shell line: `gsd-sdk query worktree.cleanup-wave --manifest "$<VAR>" || exit 1`. Net diff: ~1 line replaces ~117 deletions (execute-phase.md) / ~125 deletions (quick.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-CONTEXT.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-RESEARCH.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-PATTERNS.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-02-SUMMARY.md

@get-shit-done/workflows/execute-phase.md
@get-shit-done/workflows/quick.md

<!-- The boundary excerpt for execute-phase.md (lines 774-891) — preserved as-is in the if-arm; entire else body deleted -->
<!-- Same for quick.md (lines 787-911) -->
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hard-delete the else-branch fallback in execute-phase.md and quick.md</name>
  <read_first>
    - get-shit-done/workflows/execute-phase.md (read lines 760-900 — covers the orchestrator-CWD-pin block at 770-772, the if/else/fi block at 774-891, and the next section starting near 894)
    - get-shit-done/workflows/quick.md (read lines 780-915 — covers the same pattern at 787-911 plus surrounding context)
    - .planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-PATTERNS.md §"get-shit-done/workflows/execute-phase.md (hard-delete else body — Plan 3)" and §"get-shit-done/workflows/quick.md (hard-delete else body — Plan 3)" (delete-boundary excerpts)
    - .planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-RESEARCH.md §"Pitfall 8" (indentation/balanced-if-fi guidance)
  </read_first>
  <behavior>
    - In `execute-phase.md`, the block currently at lines 774-891 (`if command -v gsd-sdk >/dev/null 2>&1; then` through the closing `fi`) is replaced by a single shell line: `gsd-sdk query worktree.cleanup-wave --manifest "$WAVE_WORKTREE_MANIFEST" || exit 1`. The preceding orchestrator-CWD pin block (lines ~770-772) is preserved unchanged. The next section (cleanup-tail snippet at ~line 894+) is preserved unchanged.
    - In `quick.md`, the block currently at lines 787-911 is replaced by a single shell line: `gsd-sdk query worktree.cleanup-wave --manifest "$QUICK_WORKTREE_MANIFEST" || exit 1`. The preceding helper comment block at lines 784-786 is preserved. The next section after 911 is preserved unchanged.
    - The TODO(jj-port) comment blocks at execute-phase.md lines 784-789 and quick.md lines 796-801 are DELETED (they were pinned to the dead fallback).
    - The "On jj-port this routes to the stubbed wave-cleanup executor" comments at execute-phase.md lines 775-778 and quick.md lines 788-791 are DELETED (no longer accurate — the executor is real).
    - The fenced code block boundaries (` ``` `) are preserved.
    - No other content in either file is modified.
    - `if`/`fi` counts in each file are balanced after the edit (the deleted `if`/`fi` pair was a matched set, so net `if` count and net `fi` count both decrement by exactly 1).
  </behavior>
  <action>
    **Approach:** Use Edit tool, NOT a sed/awk pipe. Each file needs exactly ONE Edit call replacing a large `old_string` with a small `new_string`.

    **(1) execute-phase.md edit.**

    Open `get-shit-done/workflows/execute-phase.md`. Read lines 770-895 first to confirm the boundary.

    The `old_string` to replace begins at line 774 (`   if command -v gsd-sdk >/dev/null 2>&1; then`) and ends at line 891 (`   fi`). The exact starting context — including the leading 3-space indentation that's consistent with the surrounding markdown — must be matched.

    The replacement is a single shell line with the same indentation:

    ```
       gsd-sdk query worktree.cleanup-wave --manifest "$WAVE_WORKTREE_MANIFEST" || exit 1
    ```

    Use the Edit tool with:
    - `old_string`: the entire 118-line block starting with `   if command -v gsd-sdk >/dev/null 2>&1; then` (line 774) and ending with `   fi` (line 891). Include the TODO(jj-port) comment block at lines 784-789 and the "On jj-port this routes…" comment at lines 775-778 in the deletion — they're all inside the conditional or its dead body. Match the leading 3 spaces of indentation exactly.
    - `new_string`: `   gsd-sdk query worktree.cleanup-wave --manifest "$WAVE_WORKTREE_MANIFEST" || exit 1`

    After Edit, verify:
    - `grep -n "WAVE_WORKTREE_MANIFEST" get-shit-done/workflows/execute-phase.md` shows the new single line at the same approximate position as before.
    - `grep -n "command -v gsd-sdk" get-shit-done/workflows/execute-phase.md` returns no matches in the wave-cleanup section (other sections may legitimately still reference it).
    - `grep -c "TODO(jj-port)" get-shit-done/workflows/execute-phase.md` returns 0 (the only TODO(jj-port) in this file was in the dead block).
    - `grep -c "not_implemented_in_jj_port" get-shit-done/workflows/execute-phase.md` returns 0.
    - File still parses as valid Markdown — visually scan the surrounding context to confirm fence-block opening (` ```bash `) and closing (` ``` `) still balance.

    **(2) quick.md edit.**

    Open `get-shit-done/workflows/quick.md`. Read lines 780-915 to confirm boundary.

    The `old_string` to replace begins at line 787 (`   if command -v gsd-sdk >/dev/null 2>&1; then`) and ends at line 911 (`   fi`). Similar 3-space indentation pattern.

    Replacement:
    ```
       gsd-sdk query worktree.cleanup-wave --manifest "$QUICK_WORKTREE_MANIFEST" || exit 1
    ```

    Use the Edit tool with:
    - `old_string`: the entire ~125-line block.
    - `new_string`: the single shell line above.

    After Edit, verify same invariants:
    - `grep -n "QUICK_WORKTREE_MANIFEST" get-shit-done/workflows/quick.md` shows the new single line.
    - `grep -c "TODO(jj-port)" get-shit-done/workflows/quick.md` returns 0.
    - `grep -c "not_implemented_in_jj_port" get-shit-done/workflows/quick.md` returns 0.
    - No orphan `done < "$WT_PATHS_FILE"` / `fi` lines left behind (Pitfall 8).

    **(3) Smoke-check the unaffected references.**

    quick.md has another `command -v gsd-sdk` check at line 128 (unrelated to wave-cleanup — likely a "is gsd-sdk available?" gate at the top of the workflow). That MUST remain unchanged. Verify with `grep -n "command -v gsd-sdk" get-shit-done/workflows/quick.md` — there should be exactly ONE match remaining (at the early-init line), not zero.

    execute-phase.md may have similar unrelated `command -v gsd-sdk` checks elsewhere. Verify with `grep -n "command -v gsd-sdk" get-shit-done/workflows/execute-phase.md` — confirm any remaining match is OUTSIDE the wave-cleanup section.

    **(4) Multi-runtime parity check.**

    Per RESEARCH Assumption A5 + PATTERNS.md "Multi-runtime sync": there are no `workflows/codex/` or `workflows/gemini/` or `workflows/opencode/` subdirectories. Verify:
    ```bash
    find get-shit-done/workflows -mindepth 1 -maxdepth 2 -type d
    ```
    Output should ONLY include `execute-phase/`, `discuss-phase/`, `execute-phase/steps`, `discuss-phase/modes`, `discuss-phase/templates`. NO per-runtime subdirs. Phase 5 PROMPT-03's `bin/install.js` transform pipeline owns the multi-runtime projections — no additional file edits needed in this plan.

    **(5) No need to run shell to verify** — these are markdown files, not executable shell. The `bash -n` check would not catch unbalanced `if`/`fi` inside markdown fences. The visual inspection + grep counts are sufficient.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && echo "--- execute-phase.md after-state ---" && grep -nE "command -v gsd-sdk|gsd-sdk query worktree\.cleanup-wave|TODO\(jj-port\)|not_implemented_in_jj_port" get-shit-done/workflows/execute-phase.md && echo "--- quick.md after-state ---" && grep -nE "command -v gsd-sdk|gsd-sdk query worktree\.cleanup-wave|TODO\(jj-port\)|not_implemented_in_jj_port" get-shit-done/workflows/quick.md && echo "--- runtime-dirs check ---" && find get-shit-done/workflows -mindepth 1 -maxdepth 2 -type d</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gsd-sdk query worktree.cleanup-wave --manifest "\$WAVE_WORKTREE_MANIFEST"' get-shit-done/workflows/execute-phase.md` returns >= 1 (the unconditional call landed)
    - `grep -c 'gsd-sdk query worktree.cleanup-wave --manifest "\$QUICK_WORKTREE_MANIFEST"' get-shit-done/workflows/quick.md` returns >= 1
    - `grep -c 'TODO(jj-port)' get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -c 'TODO(jj-port)' get-shit-done/workflows/quick.md` returns 0
    - `grep -c 'not_implemented_in_jj_port' get-shit-done/workflows/execute-phase.md` returns 0
    - `grep -c 'not_implemented_in_jj_port' get-shit-done/workflows/quick.md` returns 0
    - `grep -c 'WT_PATHS_FILE' get-shit-done/workflows/execute-phase.md` returns 0 (this var was only in the dead fallback)
    - `grep -c 'WT_PATHS_FILE' get-shit-done/workflows/quick.md` returns 0
    - `wc -l get-shit-done/workflows/execute-phase.md` returns a count ~117 lines smaller than pre-edit
    - `wc -l get-shit-done/workflows/quick.md` returns a count ~125 lines smaller than pre-edit
    - `find get-shit-done/workflows -mindepth 1 -maxdepth 2 -type d` output contains ONLY pre-existing subdirectories: execute-phase, discuss-phase, execute-phase/steps, discuss-phase/modes, discuss-phase/templates (NO new codex/gemini/opencode subdirs)
    - `grep -c 'command -v gsd-sdk' get-shit-done/workflows/quick.md` returns exactly 1 (the early-init gate at line ~128, NOT in wave-cleanup section)
  </acceptance_criteria>
  <done>Both workflow .md files have the `if ... else ... fi` raw-git fallback hard-deleted; each retains a single unconditional `gsd-sdk query worktree.cleanup-wave --manifest "..." || exit 1` line; TODO(jj-port) comments and stub-routing comments are gone; no orphan shell tokens left behind; net line reduction ~117 (execute-phase) + ~125 (quick) = ~242 LOC of dead code retired; PROMPT-04 closed; multi-runtime parity preserved via existing install.js transform pipeline (no per-runtime files to touch).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow .md → user shell | the .md is read by Claude during execution and the bash snippets are executed; no new boundary introduced by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.03-01 | (none) | workflow .md edits | accept | No executable surface change. Markdown edits are dead-code removal; the surviving `gsd-sdk query` call already existed (was the if-arm) and is unchanged. No new attack surface introduced. |
| T-07.03-SC | (none) | dependencies | accept | No new packages installed; no new shell commands invoked at workflow execution time. |
</threat_model>

<verification>
After Task 1 completes:

1. **Both workflow files reflect the hard-delete:** verification command from Task 1 shows zero TODO(jj-port) / not_implemented_in_jj_port / WT_PATHS_FILE matches.
2. **Unconditional gsd-sdk call present in each file:** grep counts >= 1 in each.
3. **No per-runtime drift:** find command shows only pre-existing workflow subdirectories.
4. **End-to-end smoke (optional, off the critical path):** running `/gsd-execute-phase` and `/gsd-quick` against a real fixture would exercise the new collapsed path. Not required for Plan 3 closure because Plan 2's integration tests already prove the executor returns ok:true via the `gsd-sdk query` CLI.
</verification>

<success_criteria>
- `if command -v gsd-sdk ... else ... fi` blocks deleted from both workflow files
- Each file collapses to a single `gsd-sdk query worktree.cleanup-wave --manifest "$VAR" || exit 1` line (with appropriate variable name)
- TODO(jj-port) comment blocks and "stubbed wave-cleanup" comment lines removed
- Net line count reduction: ~117 lines (execute-phase.md), ~125 lines (quick.md)
- No per-runtime workflow .md sibling files exist or are needed (install.js transform pipeline handles projections per Phase 5 PROMPT-03)
- PROMPT-04 closed
</success_criteria>

<output>
After completion, create `.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-03-SUMMARY.md` documenting:
- Final line counts (pre/post diff for both files)
- Surviving unconditional gsd-sdk lines verbatim
- Confirmation that no per-runtime workflow files were touched (RESEARCH A5 holds)
- Hand-off note to Plan 5 (test-surface triage) — workflow files are now jj-clean, so any workflow-execution-driven test surface failure during Plan 5 triage cannot root to the dead fallback.
</output>
