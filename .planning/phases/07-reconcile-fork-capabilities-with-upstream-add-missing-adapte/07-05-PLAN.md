---
phase: 07-reconcile-fork-capabilities-with-upstream-add-missing-adapte
plan: 05
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - .planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-LEARNINGS.md
autonomous: true
requirements:
  - TEST-09
  - TEST-10
  - TEST-11
tags:
  - test-triage
  - strict-green
  - phase-close-gate
  - phase-7

must_haves:
  truths:
    - "All 4 installer-migration test files pass on both git and jj-colocated backends"
    - "All 4 shell-projection / bug-3413 / bug-3441 / bug-3442 test files pass on both backends"
    - "sdk/src/query-raw-output-projection.test.ts passes on both backends"
    - "Every delta between git-lane and jj-lane outcomes is documented in 07-LEARNINGS.md with a root-cause classification"
    - "If a failure roots to an adapter-verb gap beyond VCS-08..VCS-15, Phase 7.1 INSERTED is spawned per D-16 (Phase 7 does NOT expand scope)"
    - "Strict-green close-gate per D-15: phase does not close until both lanes are green on all 9 test surfaces"
    - "D-14: Full run of installer-migrations + shell-projection + query-raw-output-projection on BOTH git and jj-colocated backends — no triage-by-name; every delta documented"
  artifacts:
    - path: ".planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-LEARNINGS.md"
      provides: "Per-test triage record + root-cause classification + any 8th-verb escape signals"
      contains: "TEST-09\\|TEST-10\\|TEST-11\\|backend delta\\|root cause"
  key_links:
    - from: "Plan 5 test execution"
      to: "Phase 7 close gate (CONTEXT D-15)"
      via: "all 9 test surfaces must pass on both backends before phase closes"
      pattern: "strict-green"
---

<objective>
Run the 9 new upstream test surfaces (`installer-migration-{report,authoring,install-integration}.test.cjs` + `installer-migrations.test.cjs` + `shell-command-projection-dispatch.test.cjs` + `bug-3413-shell-command-projection.test.cjs` + `bug-3441-path-action-projection.test.cjs` + `bug-3442-shim-projection-drift-guard.test.cjs` + `sdk/src/query-raw-output-projection.test.ts`) against BOTH the `git` and `jj-colocated` backends. Per D-14: full run, no triage-by-name. Per D-15: strict-green — Phase 7 does NOT close until both lanes are green on all 9 surfaces. Per D-16: any failure rooting to an adapter-verb gap beyond VCS-08..VCS-15 SPAWNS `Phase 7.1 INSERTED` rather than expanding Phase 7 scope.

Purpose: Close TEST-09 / TEST-10 / TEST-11. This is the Phase 7 close gate.

Output: `07-LEARNINGS.md` documenting per-test outcomes on both backends, root-cause classification for any divergences, and (if applicable) a Phase 7.1 INSERTED proposal handed off to `/gsd-discuss-phase 7.1`.

**Expected outcome per RESEARCH:** 7 of 9 test files have zero VCS references; the remaining 2 (`shell-command-projection-dispatch.test.cjs` which tests `execGit` itself; `installer-migration-install-integration.test.cjs` which uses `spawnSync` for the install runner) MAY have backend-dependent behavior, but the researcher's empirical scan found only the `execGit` test as a definite git-only carve-out. The plan is designed to be cheap to complete if no adapter gaps surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-CONTEXT.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-RESEARCH.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-PATTERNS.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-02-SUMMARY.md
@.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-04-SUMMARY.md

@tests/installer-migration-report.test.cjs
@tests/installer-migration-authoring.test.cjs
@tests/installer-migration-install-integration.test.cjs
@tests/installer-migrations.test.cjs
@tests/shell-command-projection-dispatch.test.cjs
@tests/bug-3413-shell-command-projection.test.cjs
@tests/bug-3441-path-action-projection.test.cjs
@tests/bug-3442-shim-projection-drift-guard.test.cjs
@sdk/src/query-raw-output-projection.test.ts

<interfaces>
Test runner conventions for this repo:
- `tests/*.test.cjs` files use `node:test` (cjs). Run via `node --test <file>` OR `node scripts/run-tests.cjs <file>` (latter is the canonical runner).
- `sdk/src/**/*.test.ts` files use vitest. Run via `pnpm test <file>` or `pnpm -C sdk test <file>`.
- Backend selection via `GSD_TEST_BACKENDS=git` or `GSD_TEST_BACKENDS=jj-colocated` env var (per TEST-04).
- D-15 strict-green: both lanes must pass; no "document and defer" path.
- D-16 escape: if root cause is an adapter-verb gap NOT in VCS-08..VCS-15, Phase 7 does NOT add it — spawn Phase 7.1 INSERTED.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute the 9 test surfaces on both backends; capture results</name>
  <read_first>
    - tests/installer-migration-report.test.cjs (full file — confirm RESEARCH A3 zero-VCS-refs claim)
    - tests/installer-migration-authoring.test.cjs (full file)
    - tests/installer-migration-install-integration.test.cjs (full file — researcher flagged this uses spawnSync for install runner)
    - tests/installer-migrations.test.cjs (full file)
    - tests/shell-command-projection-dispatch.test.cjs (full file — known to test execGit helper itself; this is the legitimate git-only carve-out)
    - tests/bug-3413-shell-command-projection.test.cjs (full file)
    - tests/bug-3441-path-action-projection.test.cjs (full file)
    - tests/bug-3442-shim-projection-drift-guard.test.cjs (full file)
    - sdk/src/query-raw-output-projection.test.ts (full file)
    - scripts/run-tests.cjs (the canonical test runner — understand env-var honoring)
    - tests/helpers.cjs (vcsTest fixture, BACKENDS_AVAILABLE — confirm jj-colocated lane is wired)
  </read_first>
  <behavior>
    - Each of the 9 test files runs to completion on BOTH `GSD_TEST_BACKENDS=git` and `GSD_TEST_BACKENDS=jj-colocated` lanes.
    - Test results are recorded as (file, lane, pass/fail, failing-test-names) tuples.
    - For `shell-command-projection-dispatch.test.cjs`: if it test `execGit` itself, treat git-only as legitimate. Document the carve-out in 07-LEARNINGS.md — do NOT count this as a strict-green failure if its test bodies are explicitly testing git-only code paths (e.g., `execGit('/tmp/definitely-not-a-git-repo-8675309', ...)`).
    - For each (file, lane) failure pair, classify root cause into one of:
        - **Cosmetic/test-fixture issue** (no production code change needed; fix in Plan 5 if cheap, else file as Plan 5 follow-up task)
        - **Adapter usage gap in test setup** (test should use `vcs` fixture instead of raw git/jj — fix in Plan 5 if cheap)
        - **Adapter verb gap in production code** (we landed VCS-08..VCS-15; if the production code under test relies on a NEW verb not in this set → spawn Phase 7.1 INSERTED per D-16, do NOT expand Phase 7)
        - **Legitimate git-only carve-out** (e.g., execGit unit test) — document in 07-LEARNINGS.md and exempt from strict-green
        - **A3 colocated pre-commit gap (Phase 4 LEARNINGS Open Q1)** — per RESEARCH Pitfall 6, NOT Phase 7 scope; surface and defer to a future hooks-hardening phase
    - 07-LEARNINGS.md created with the triage record.
  </behavior>
  <action>
    **(1) Build prereqs:** Plan 1 + Plan 2 must have landed. Re-confirm:
    ```bash
    pnpm -C sdk build:cjs
    ```

    **(2) Execute the 4 installer-migration tests on git lane:**
    ```bash
    GSD_TEST_BACKENDS=git node --test \
      tests/installer-migration-report.test.cjs \
      tests/installer-migration-authoring.test.cjs \
      tests/installer-migration-install-integration.test.cjs \
      tests/installer-migrations.test.cjs
    ```
    Capture full output to `/tmp/phase7-test-installer-git.txt`.

    **(3) Execute the same 4 on jj-colocated lane:**
    ```bash
    GSD_TEST_BACKENDS=jj-colocated node --test \
      tests/installer-migration-report.test.cjs \
      tests/installer-migration-authoring.test.cjs \
      tests/installer-migration-install-integration.test.cjs \
      tests/installer-migrations.test.cjs
    ```
    Capture to `/tmp/phase7-test-installer-jj.txt`.

    **(4) Execute the 4 shell-projection / bug-* tests on both lanes:**
    ```bash
    GSD_TEST_BACKENDS=git node --test \
      tests/shell-command-projection-dispatch.test.cjs \
      tests/bug-3413-shell-command-projection.test.cjs \
      tests/bug-3441-path-action-projection.test.cjs \
      tests/bug-3442-shim-projection-drift-guard.test.cjs
    GSD_TEST_BACKENDS=jj-colocated node --test \
      tests/shell-command-projection-dispatch.test.cjs \
      tests/bug-3413-shell-command-projection.test.cjs \
      tests/bug-3441-path-action-projection.test.cjs \
      tests/bug-3442-shim-projection-drift-guard.test.cjs
    ```
    Capture to `/tmp/phase7-test-projection-git.txt` and `/tmp/phase7-test-projection-jj.txt`.

    **(5) Execute the SDK projection test on both lanes:**
    ```bash
    GSD_TEST_BACKENDS=git pnpm -C sdk test query-raw-output-projection.test.ts
    GSD_TEST_BACKENDS=jj-colocated pnpm -C sdk test query-raw-output-projection.test.ts
    ```
    Capture to `/tmp/phase7-test-sdk-projection-git.txt` and `/tmp/phase7-test-sdk-projection-jj.txt`.

    **(6) Build the triage matrix.** Parse each output file for pass/fail counts. Construct a table:

    | Test file | git pass/total | jj-colo pass/total | Delta? | Root cause | Disposition |
    |-----------|----------------|---------------------|--------|------------|-------------|
    | installer-migration-report | A/A | A/A | none | n/a | green |
    | installer-migration-authoring | … | … | … | … | … |
    | (etc.) |

    For each delta row, do a focused investigation: read the failing test body, identify whether the failure is fixture-level, production-code-level (Plan 1's verbs sufficient?), or A3-rooted.

    **(7) Resolve fixable cosmetic/fixture issues in-flight.** If any failures are 1-3 LOC test-fixture fixes (e.g., the test hardcodes `.git` and needs `.git || .jj`), make the fix and re-run. Stay within `tests/` scope — do not touch production code in this task.

    **(8) If a failure roots to an adapter-verb gap beyond VCS-08..VCS-15:** STOP this task immediately, draft a Phase 7.1 INSERTED proposal in 07-LEARNINGS.md (mini-CONTEXT.md describing the missing verb + the test that surfaced it), and hand off to `/gsd-discuss-phase 7.1`. Per CONTEXT D-16, Phase 7 does NOT expand. The remaining test surfaces wait for Phase 7.1 close.

    **(9) Write 07-LEARNINGS.md.** Template:
    ```markdown
    # Phase 7 LEARNINGS — first upstream sync (v1.1 milestone)

    **Date:** YYYY-MM-DD
    **Phase close:** {green / blocked on Phase 7.1 INSERTED / etc.}

    ## Test-surface triage (TEST-09, TEST-10, TEST-11)

    ### Triage matrix
    {the table from step 6}

    ### Per-delta root-cause analysis
    {for each delta row: 2-5 sentences describing what failed, where, why, and how it was resolved}

    ### Legitimate git-only carve-outs
    {e.g., shell-command-projection-dispatch.test.cjs tests execGit itself; document and exempt from strict-green}

    ### A3 deferrals (Phase 4 LEARNINGS Open Q1)
    {if any failure rooted to A3 colocated-pre-commit gap, surface here and defer per RESEARCH Pitfall 6}

    ## Phase 7 close-gate evidence

    - [ ] VCS-08..VCS-15 contract tests green on both backends (Plan 1 evidence)
    - [ ] WAVE-01 executor integration tests green on both backends (Plan 2 evidence)
    - [ ] PROMPT-04 workflow .md edits land (Plan 3 evidence)
    - [ ] MIGR-05 github-release-notes migration green (Plan 4 evidence)
    - [ ] TEST-09 installer-migration surfaces green on both backends (this plan)
    - [ ] TEST-10 shell-projection / bug-3413/3441/3442 surfaces green on both backends (this plan)
    - [ ] TEST-11 query-raw-output-projection green on both backends (this plan)
    - [ ] lint-vcs-no-raw-git stays at 0 violations
    - [ ] skip-count guard does not regress

    ## Phase 7.1 INSERTED proposal (if applicable)
    {only filled in if step 8 fires}

    ## Hand-off
    Phase 7 closes when all close-gate evidence above is ticked. ROADMAP.md Phase 7 entry updated to mark complete. REQUIREMENTS.md status table for VCS-08..VCS-15, WAVE-01, PROMPT-04, MIGR-05, TEST-09..11 flipped to Complete.
    ```

    **(10) Update REQUIREMENTS.md status table** to mark each landed requirement Complete (if all close-gate evidence holds):
    For each row in the v1.1 status table (VCS-08..VCS-15, WAVE-01, PROMPT-04, MIGR-05, TEST-09..11), change `Pending` to `Complete (Phase 7 plan 07-NN)` referencing the plan number that landed it.

    **(11) Update ROADMAP.md Phase 7 entry** to reflect closure:
    - Change `**Plans:** 0 plans` to `**Plans:** 5 plans`
    - Replace `- [ ] TBD (run /gsd-plan-phase 7 to break down)` with the actual plan list (07-01..07-05)
    - When the phase actually closes (after this plan finishes successfully), the closing line should be analogous to Phase 4's: `*Last updated: YYYY-MM-DD — Phase 7 plan execution complete (5/5). …summary of what landed…*`
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && (GSD_TEST_BACKENDS=git node --test tests/installer-migration-report.test.cjs tests/installer-migration-authoring.test.cjs tests/installer-migration-install-integration.test.cjs tests/installer-migrations.test.cjs 2>&1 | tail -5); echo "---installer-git-end---"; (GSD_TEST_BACKENDS=jj-colocated node --test tests/installer-migration-report.test.cjs tests/installer-migration-authoring.test.cjs tests/installer-migration-install-integration.test.cjs tests/installer-migrations.test.cjs 2>&1 | tail -5); echo "---installer-jj-end---"; (GSD_TEST_BACKENDS=git node --test tests/shell-command-projection-dispatch.test.cjs tests/bug-3413-shell-command-projection.test.cjs tests/bug-3441-path-action-projection.test.cjs tests/bug-3442-shim-projection-drift-guard.test.cjs 2>&1 | tail -5); echo "---projection-git-end---"; (GSD_TEST_BACKENDS=jj-colocated node --test tests/shell-command-projection-dispatch.test.cjs tests/bug-3413-shell-command-projection.test.cjs tests/bug-3441-path-action-projection.test.cjs tests/bug-3442-shim-projection-drift-guard.test.cjs 2>&1 | tail -5); echo "---projection-jj-end---"; (GSD_TEST_BACKENDS=git pnpm -C sdk test query-raw-output-projection.test.ts 2>&1 | tail -5); echo "---sdk-git-end---"; (GSD_TEST_BACKENDS=jj-colocated pnpm -C sdk test query-raw-output-projection.test.ts 2>&1 | tail -5); echo "---sdk-jj-end---"; ls .planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-LEARNINGS.md && echo "---learnings-exists---"; grep -c "Phase 7 close-gate evidence" .planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-LEARNINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 installer-migration test files exit 0 on the git lane (`GSD_TEST_BACKENDS=git node --test ...`)
    - All 4 installer-migration test files exit 0 on the jj-colocated lane (`GSD_TEST_BACKENDS=jj-colocated node --test ...`) — OR the failure is classified as legitimate git-only carve-out / A3-rooted in 07-LEARNINGS.md, OR a Phase 7.1 INSERTED proposal is in 07-LEARNINGS.md
    - All 4 shell-projection / bug-3413 / bug-3441 / bug-3442 test files exit 0 on the git lane
    - All 4 exit 0 on the jj-colocated lane (same caveat applies for legitimate carve-outs)
    - `sdk/src/query-raw-output-projection.test.ts` exits 0 on both lanes
    - `.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-LEARNINGS.md` exists
    - 07-LEARNINGS.md contains a "Triage matrix" section with one row per test file
    - 07-LEARNINGS.md contains a "Phase 7 close-gate evidence" checklist
    - `node scripts/lint-vcs-no-raw-git.cjs` exits 0
    - `node scripts/check-skip-count.cjs` exits 0
    - REQUIREMENTS.md status table updated: VCS-08..VCS-15, WAVE-01, PROMPT-04, MIGR-05, TEST-09..11 all marked Complete (or, if Phase 7.1 INSERTED fires, the affected IDs are marked appropriately)
    - ROADMAP.md Phase 7 entry updated with the 5 landed plans and closure notes
  </acceptance_criteria>
  <done>All 9 upstream test surfaces have been executed on both backends; results are documented in 07-LEARNINGS.md with per-row root-cause classification; strict-green close-gate (D-15) is met OR a Phase 7.1 INSERTED proposal is filed (D-16); REQUIREMENTS.md and ROADMAP.md reflect Phase 7 closure (or partial closure pending Phase 7.1); Phase 7 is ready to merge to main per the CI matrix's required-blocking gate from Phase 5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test execution → repo state | tests construct tmpdir fixtures; no production state mutated; vcsTest pattern isolates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.05-01 | (none) | test execution | accept | Plan 5 is verification only — no new code lands here. Tests run in isolated tmpdir fixtures per existing vcsTest pattern. No new attack surface introduced. |
| T-07.05-SC | (none) | dependencies | accept | No new packages installed; only test execution. |
</threat_model>

<verification>
After Task 1 completes:

1. **All 9 test surfaces have explicit pass-or-document outcomes on both backends.**
2. **07-LEARNINGS.md exists and contains the triage matrix + close-gate checklist.**
3. **REQUIREMENTS.md status table reflects Phase 7 outcomes.**
4. **ROADMAP.md Phase 7 entry reflects the 5 landed plans + closure notes.**
5. **No raw-git regression:** `node scripts/lint-vcs-no-raw-git.cjs` exits 0.
6. **No skip-count regression:** `node scripts/check-skip-count.cjs` exits 0.
</verification>

<success_criteria>
- All 4 installer-migration tests run on both backends; results documented
- All 4 shell-projection / bug-3413 / bug-3441 / bug-3442 tests run on both backends; results documented
- sdk/src/query-raw-output-projection.test.ts runs on both backends; results documented
- Strict-green per D-15 OR Phase 7.1 INSERTED proposal filed per D-16
- 07-LEARNINGS.md captures triage matrix + close-gate evidence + any Phase 7.1 proposal
- REQUIREMENTS.md status table for VCS-08..VCS-15, WAVE-01, PROMPT-04, MIGR-05, TEST-09..11 reflects landing outcomes
- ROADMAP.md Phase 7 entry updated (5/5 plans complete OR partial + Phase 7.1 INSERTED queued)
- lint-vcs-no-raw-git stays at 0 violations
- skip-count guard does not regress
- TEST-09, TEST-10, TEST-11 closed (or Phase-7.1-INSERTED-blocked, with proposal in hand)
</success_criteria>

<output>
After completion, create `.planning/phases/07-reconcile-fork-capabilities-with-upstream-add-missing-adapte/07-05-SUMMARY.md` and `07-LEARNINGS.md`:

**07-05-SUMMARY.md** — landing notes for THIS plan (test execution results, deltas resolved in-flight, hand-off to phase closure).

**07-LEARNINGS.md** — phase-wide learnings (triage matrix, close-gate evidence, any Phase 7.1 INSERTED proposal, hand-off to next milestone).

Phase 7 closes when both files land AND all close-gate checkboxes in 07-LEARNINGS.md are ticked. If Phase 7.1 INSERTED fires, the phase closes partially: deliverables 1-4 (verbs, executor, workflow .md, release-notes) land cleanly; deliverable 5 (test triage) splits into "what passed in Phase 7" + "what migrates to Phase 7.1" with the latter handed off to `/gsd-discuss-phase 7.1`.
</output>
