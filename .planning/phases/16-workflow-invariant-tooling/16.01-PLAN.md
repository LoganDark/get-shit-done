---
phase: 16-workflow-invariant-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lint-vcs-parallel-call-presence.cjs
  - scripts/lint-vcs-parallel-call-presence.allow.json
  - tests/scripts/lint-vcs-parallel-call-presence.test.cjs
  - tests/scripts/fixtures/lint-vcs-parallel-call-presence/paired/get-shit-done/workflows/paired.md
  - tests/scripts/fixtures/lint-vcs-parallel-call-presence/dispatch-only/get-shit-done/workflows/dispatch-only.md
  - tests/scripts/fixtures/lint-vcs-parallel-call-presence/fanin-only/get-shit-done/workflows/fanin-only.md
  - tests/scripts/fixtures/lint-vcs-parallel-call-presence/prose-only/get-shit-done/workflows/prose-only.md
  - tests/scripts/fixtures/lint-vcs-parallel-call-presence/allowlist/get-shit-done/workflows/opted-out.md
  - .github/workflows/parallel-e2e.yml
autonomous: true
requirements:
  - LINT-06
tags:
  - lint
  - workflow
  - ci

must_haves:
  truths:
    - "Running `node scripts/lint-vcs-parallel-call-presence.cjs` against the live tree exits 0 (current workflows already pair dispatch + fan-in correctly); allowlist ships with `entries: []` per D-08"
    - "A fixture file with `workspace.parallel.dispatch` literal in a bash/sh/zsh fence but no `workspace.parallel.fan-in` literal in any fence triggers exit 1 with a file:1 diagnostic on stderr — exercises D-01 (FILE-level pairing scope, NOT fence/section/proximity), D-02 (bidirectional), D-03 (SCAN_ROOTS narrowed to get-shit-done/workflows/), D-14 scenario (ii) (dispatch-only fence fails)"
    - "A fixture file with `workspace.parallel.fan-in` literal in a bash/sh/zsh fence but no `workspace.parallel.dispatch` literal in any fence triggers exit 1 with a file:1 diagnostic on stderr — exercises D-02 vice-versa direction and D-14 scenario (iii) (fan-in-only fence fails)"
    - "A fixture file that mentions `parallel` / `wave` ONLY in prose (no literal inside a bash/sh/zsh fence) exits 0 — Pitfall 7 false-positive guard, exercises D-14 scenario (iv)"
    - "An allowlist entry of shape `{path, reason, owner}` suppresses a known-opt-out file (entry shape rejected if `expires` field present per `feedback_solo_dev_no_expires`) — exercises D-14 scenario (v); D-09 forbids any inline `vcs-lint:allow-parallel-call-absent-here` escape annotation (YAGNI — defer until a real cancel-only consumer emerges)"
    - "Lint script docblock documents the bash/sh/zsh fence scope per D-10 (a) (verbatim FENCE_OPEN/FENCE_CLOSE reuse from audit-workflow-raw-git.cjs lines 48-49 per CF-06), the per-entry allowlist as the sole opt-out mechanism in v1.4 per D-10 (b) + D-09, and one-line cross-reference to .planning/research/PITFALLS.md §IP-2 per D-10 (c)"
    - "The lint step appears in `.github/workflows/parallel-e2e.yml` adjacent to the existing `audit-workflow-raw-git.cjs` step at line 125-127, inside the `parallel-e2e` matrix job (inherits jj-colocated blocking discipline via `parallel-e2e-gate`)"
  artifacts:
    - path: "scripts/lint-vcs-parallel-call-presence.cjs"
      provides: "default-deny CI lint enforcing FILE-level dispatch ⇔ fan-in literal pairing (D-01) inside bash/sh/zsh fences under get-shit-done/workflows/ (D-03); bidirectional per D-02"
      contains: "FENCE_OPEN / FENCE_CLOSE regex constants byte-identically duplicated from scripts/audit-workflow-raw-git.cjs lines 48 and 49 per CF-06"
    - path: "scripts/lint-vcs-parallel-call-presence.allow.json"
      provides: "schema-v2 per-entry allowlist (initial: entries: []) per D-08; documents IP-2 cancel-exclusion narrative stub per D-08"
      contains: "$comment_ip2_cancel_exclusion"
    - path: "tests/scripts/lint-vcs-parallel-call-presence.test.cjs"
      provides: "node:test fixture-based unit tests covering 5 D-14 scenarios (paired/dispatch-only/fanin-only/prose-only/allowlist)"
    - path: ".github/workflows/parallel-e2e.yml"
      provides: "new CI step `Lint — workflow call-presence (LINT-06)` adjacent to CI-06 audit step + 2 new paths-filter entries"
  key_links:
    - from: "scripts/lint-vcs-parallel-call-presence.cjs"
      to: "scripts/lib/allowlist-parser.cjs"
      via: "require('./lib/allowlist-parser.cjs')"
      pattern: "parseAllowlist"
    - from: "scripts/lint-vcs-parallel-call-presence.cjs"
      to: "scripts/audit-workflow-raw-git.cjs:48-49"
      via: "byte-identical FENCE_OPEN / FENCE_CLOSE regex duplication per CF-06 (NOT shared-lib extraction — REQUIREMENTS.md §Out of Scope L92 forbids)"
      pattern: "FENCE_OPEN.*=.*\\^\\\\s\\*"
    - from: ".github/workflows/parallel-e2e.yml"
      to: "scripts/lint-vcs-parallel-call-presence.cjs"
      via: "new step `run: node scripts/lint-vcs-parallel-call-presence.cjs` inserted after the existing audit step at line 127"
      pattern: "lint-vcs-parallel-call-presence"
---

<objective>
Ship a new CI lint `scripts/lint-vcs-parallel-call-presence.cjs` that enforces FILE-level pairing of `workspace.parallel.dispatch` ⇔ `workspace.parallel.fan-in` literal substrings inside bash/sh/zsh shell fences under `get-shit-done/workflows/`. Wire it into `.github/workflows/parallel-e2e.yml` adjacent to the existing `audit-workflow-raw-git.cjs` CI-06 step (NOT promoted to `npm pretest`). Ship a fixture-based unit test under `tests/scripts/` covering 5 D-14 scenarios — including the Pitfall 7 false-positive guard for prose-only mentions in non-dispatch workflows like `code-review.md`/`audit-fix.md`.

Purpose: Closes LINT-06 (the last v1.3 open workflow-lint deferral). Prevents future drift where a workflow declares parallel-dispatch in shell fences without calling the paired fan-in verb. The lint operates by SHELL FENCE — NOT by heading or prose mention (Pitfall 7) — and uses BYTE-IDENTICAL duplication of `audit-workflow-raw-git.cjs:48-49` `FENCE_OPEN` / `FENCE_CLOSE` regex per CF-06 (shared-lib extraction explicitly forbidden by REQUIREMENTS.md §Out of Scope L92).

Output: 4 new files + 1 modified CI workflow + 5 fixture .md files under `tests/scripts/fixtures/lint-vcs-parallel-call-presence/`. Zero new npm packages. Zero changes to any production workflow .md (lint passes against the live tree out-of-the-box — `execute-phase.md` and `quick.md` already pair both literals).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-workflow-invariant-tooling/16-CONTEXT.md
@.planning/phases/16-workflow-invariant-tooling/16-RESEARCH.md
@.planning/phases/16-workflow-invariant-tooling/16-PATTERNS.md
@.planning/phases/16-workflow-invariant-tooling/16-VALIDATION.md
@scripts/lint-vcs-no-raw-git.cjs
@scripts/audit-workflow-raw-git.cjs
@scripts/lib/allowlist-parser.cjs
@tests/lint-vcs-no-raw-git-fixture.test.cjs

<interfaces>
<!-- Locked contracts extracted from the canonical analog files. Executor uses
     these directly; no codebase exploration needed for shape questions. -->

From scripts/audit-workflow-raw-git.cjs:42-50 (CF-06 byte-identical duplication target):
```javascript
// Production lint's start-of-statement detection. Used here verbatim — keeps
// audit's matching rule byte-identical with what the production lint enforces.
const SHELL_GIT_RE = /(?:^|[ \t;&|(])git[ \t]+[a-zA-Z]/;

// Fenced block delimiters. Match opening fence with bash/sh/zsh language tag
// (case-insensitive) and any plain closing fence. Long-fence support (4+ backticks
// or tildes) for fences-of-fences in workflow examples.
const FENCE_OPEN = /^\s*(```+|~~~+)\s*(bash|sh|zsh)\b/i;
const FENCE_CLOSE = /^\s*(```+|~~~+)\s*$/;
const SCAN_ROOTS = ['get-shit-done/workflows', 'get-shit-done/references', 'agents'];
```

From scripts/audit-workflow-raw-git.cjs:98-130 (per-file fence walker — adapt to FILE-level boolean accumulation):
```javascript
function findMarkdown(dir, out) {
  let entries;
  try { entries = fs.readdirSync(dir, { withFileTypes: true }); } catch { return; }
  for (const entry of entries) {
    if (entry.isSymbolicLink()) continue;  // T-13-04 defense-in-depth
    const full = path.join(dir, entry.name);
    if (entry.isDirectory()) findMarkdown(full, out);
    else if (entry.isFile() && entry.name.endsWith('.md')) out.push(full);
  }
}

function scanFile(absPath, repoRoot) {
  const rel = path.relative(repoRoot, absPath).split(path.sep).join('/');
  const lines = fs.readFileSync(absPath, 'utf8').split('\n');
  const hits = [];
  let inFence = false;
  for (let i = 0; i < lines.length; i += 1) {
    const line = lines[i];
    if (!inFence && FENCE_OPEN.test(line)) { inFence = true; continue; }
    if (inFence && FENCE_CLOSE.test(line)) { inFence = false; continue; }
    if (!inFence) continue;
    if (/^\s*#/.test(line)) continue; // shell comment — skip
    if (SHELL_GIT_RE.test(line)) {
      hits.push({ line: i + 1, snippet: line.trim().slice(0, 200) });
    }
  }
  return { path: rel, count: hits.length, hits };
}
```

From scripts/lint-vcs-no-raw-git.cjs:1-50 (lint shape, argv, allowlist plumbing):
```javascript
#!/usr/bin/env node
'use strict';
const fs = require('fs');
const path = require('path');
const { parseAllowlist } = require('./lib/allowlist-parser.cjs');

function parseArgv(argv) {
  const out = { scanRoot: null };
  for (let i = 2; i < argv.length; i += 1) {
    if (argv[i] === '--scan-root' && argv[i + 1]) { out.scanRoot = argv[i + 1]; i += 1; }
  }
  return out;
}
const ARGV = parseArgv(process.argv);
const REPO_ROOT = path.resolve(__dirname, '..');
const SCAN_ROOT = ARGV.scanRoot ? path.resolve(ARGV.scanRoot) : REPO_ROOT;

const ALLOW = parseAllowlist(
  require('./lint-vcs-no-raw-git.allow.json'),
  'lint-vcs-no-raw-git',
);
const ALLOW_FILES = ALLOW.files;
const ALLOW_GLOB_REGEXES = ALLOW.globRegexes;
```

From scripts/lint-vcs-no-raw-git.cjs:146-164 (exit pattern):
```javascript
if (violations.length === 0) {
  console.log(`ok lint-vcs-no-raw-git: ${files.length} files scanned in ${SCAN_ROOT}, 0 violations`);
  process.exit(0);
}
process.stderr.write(`\nERROR lint-vcs-no-raw-git: ${violations.length} file(s) with raw git invocations\n\n`);
for (const v of violations) {
  // per-violation file:line emit on stderr
}
process.exit(1);
```

From scripts/lib/allowlist-parser.cjs (parser contract):
```javascript
// Returns: { files: Set<string>, globRegexes: RegExp[] }
// Per-entry schema: exactly one of {path, glob}, plus BOTH reason and owner as
// non-empty strings. expires field FORBIDDEN per feedback_solo_dev_no_expires.
function parseAllowlist(json, name) { /* schema v2 enforcement */ }
module.exports = { parseAllowlist };
```

From scripts/lint-vcs-no-raw-git.allow.json (schema-v2 shape):
```json
{
  "$schema_version": 2,
  "$migration_note": "...",
  "$comment_<id>": "...",
  "entries": [
    { "path": "<file>", "reason": "<text>", "owner": "@<handle>" },
    { "glob": "<pattern>", "reason": "<text>", "owner": "@<handle>" }
  ]
}
```

From tests/lint-vcs-no-raw-git-fixture.test.cjs:15-54 (Pattern B mkdtemp fixture skeleton):
```javascript
const { test } = require('node:test');
const assert = require('node:assert/strict');
const fs = require('node:fs');
const os = require('node:os');
const path = require('node:path');
const { spawnSync } = require('node:child_process');

const REPO_ROOT = path.resolve(__dirname, '..', '..');
const SCRIPT = path.join(REPO_ROOT, 'scripts', '<lint>.cjs');

test('<scenario name>', () => {
  const fixDir = fs.mkdtempSync(path.join(os.tmpdir(), '__<scope>-fixture-'));
  try {
    // ... materialize synthetic .md files under fixDir/get-shit-done/workflows/
    const r = spawnSync(process.execPath, [SCRIPT, '--scan-root', fixDir], { encoding: 'utf-8' });
    assert.equal(r.status, 0);  // or 1 for negative
  } finally {
    fs.rmSync(fixDir, { recursive: true, force: true });
  }
});
```

From .github/workflows/parallel-e2e.yml:36-42 + :121-127 (CI step shape + paths-filter):
```yaml
# paths-filter block (lines 36-42)
paths:
  - 'sdk/src/vcs/**'
  - 'sdk/src/query/workspace-parallel-*.ts'
  - 'get-shit-done/workflows/**'
  - 'scripts/audit-workflow-raw-git.cjs'
  - 'scripts/e2e-parallel-phase.sh'
  - '.github/workflows/parallel-e2e.yml'

# Existing audit step (lines 121-127) — new step inserts AFTER line 127
- name: Audit — raw git in workflow markdown (CI-06)
  shell: bash
  run: node scripts/audit-workflow-raw-git.cjs
```

Production literals already present (lint passes against live tree out-of-the-box):
- get-shit-done/workflows/execute-phase.md: `workspace.parallel.dispatch` at one fence; `workspace.parallel.fan-in` at another fence (DIFFERENT `##` sections — that's why D-01 is FILE-level, not SECTION-level)
- get-shit-done/workflows/quick.md: same shape

D-01 / D-02 / D-03 locks (from CONTEXT.md — NON-NEGOTIABLE):
- D-01: FILE-level pairing. NOT fence-scoped, NOT section-scoped, NOT proximity-scoped.
- D-02: Bidirectional — file with dispatch literal but no fan-in literal → exit 1; AND vice versa.
- D-03: SCAN_ROOTS = ['get-shit-done/workflows'] ONLY (narrower than audit's 3-root scope per Pitfall 7 false-positive guard).
- D-08: Initial allowlist `entries: []` with `$schema_version: 2` + `$comment_ip2_cancel_exclusion` narrative stub (NO speculative seeding).
- D-09: NO `vcs-lint:allow-parallel-call-absent-here` inline escape annotation reserved (pure YAGNI; reserved in name only by ROADMAP SC1).
- D-10: Lint docblock cites IP-2 via one-line cross-reference to `.planning/research/PITFALLS.md §IP-2` — not a verbose dump.
- D-14: 5 fixture scenarios (paired pass / dispatch-only fail / fan-in-only fail / prose-only pass / allowlist suppress).

CF-04 / CF-06 locks (NON-NEGOTIABLE):
- CF-04: Per-entry allowlist `{path|glob, reason, owner}` ONLY — NO `expires` field (per `feedback_solo_dev_no_expires`).
- CF-06: BYTE-IDENTICAL duplication of `FENCE_OPEN` / `FENCE_CLOSE` constants from `audit-workflow-raw-git.cjs:48-49` (NOT shared-lib extraction — REQUIREMENTS.md §Out of Scope L92 explicitly forbids "Do NOT extract a shared 'fence-aware walker'"). Add comment `// Byte-identical to scripts/audit-workflow-raw-git.cjs:48-49 (CF-06)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship the lint script + allowlist + 5 fixture files</name>
  <files>scripts/lint-vcs-parallel-call-presence.cjs, scripts/lint-vcs-parallel-call-presence.allow.json, tests/scripts/fixtures/lint-vcs-parallel-call-presence/paired/get-shit-done/workflows/paired.md, tests/scripts/fixtures/lint-vcs-parallel-call-presence/dispatch-only/get-shit-done/workflows/dispatch-only.md, tests/scripts/fixtures/lint-vcs-parallel-call-presence/fanin-only/get-shit-done/workflows/fanin-only.md, tests/scripts/fixtures/lint-vcs-parallel-call-presence/prose-only/get-shit-done/workflows/prose-only.md, tests/scripts/fixtures/lint-vcs-parallel-call-presence/allowlist/get-shit-done/workflows/opted-out.md</files>
  <read_first>
    - scripts/lint-vcs-no-raw-git.cjs (full file — analog for shape, argv, allowlist plumbing, exit code semantics, file:line diagnostic emit)
    - scripts/audit-workflow-raw-git.cjs (full file — fence walker, FENCE_OPEN/FENCE_CLOSE at :48-49 verbatim for CF-06 duplication, findMarkdown at :98-107 for T-13-04 symlink skip, scanFile at :114-130 for the inner loop)
    - scripts/lib/allowlist-parser.cjs (full file — parseAllowlist contract, schema-v2 enforcement, no-expires guard)
    - scripts/lint-vcs-no-raw-git.allow.json (full file — schema-v2 shape, $schema_version + $migration_note + $comment_* narrative pattern that D-08 mirrors)
    - .planning/phases/16-workflow-invariant-tooling/16-PATTERNS.md lines 30-211 (the "Pattern Assignments" sections for `scripts/lint-vcs-parallel-call-presence.cjs` + `.allow.json`)
    - get-shit-done/workflows/execute-phase.md (skim for production occurrence: search `workspace.parallel.dispatch` and `workspace.parallel.fan-in` literals — confirm they appear in DIFFERENT fences in DIFFERENT `##` sections; this is WHY D-01 is FILE-level)
  </read_first>
  <behavior>
    - Test 1 (paired-fences-pass): A fixture `get-shit-done/workflows/paired.md` containing `workspace.parallel.dispatch` in one bash fence AND `workspace.parallel.fan-in` in a SEPARATE bash fence → lint exit 0
    - Test 2 (dispatch-only-fail): A fixture `get-shit-done/workflows/dispatch-only.md` containing ONLY `workspace.parallel.dispatch` in a bash fence (no fan-in literal anywhere in any fence) → lint exit 1; stderr contains the relative path + ":1" + the word "fan-in" in the diagnostic
    - Test 3 (fanin-only-fail): A fixture `get-shit-done/workflows/fanin-only.md` containing ONLY `workspace.parallel.fan-in` in a bash fence (no dispatch literal anywhere in any fence) → lint exit 1; stderr contains the relative path + ":1" + the word "dispatch" in the diagnostic
    - Test 4 (prose-only-pass): A fixture `get-shit-done/workflows/prose-only.md` mentioning "parallel", "wave", and "Task(" only in prose paragraphs (NEVER inside bash/sh/zsh fences) → lint exit 0 (Pitfall 7 false-positive guard — the most load-bearing test)
    - Test 5 (allowlist-suppress): A fixture `get-shit-done/workflows/opted-out.md` containing dispatch literal but NOT fan-in literal, PLUS the production allowlist temporarily extended with `{path: "get-shit-done/workflows/opted-out.md", reason: "test fixture", owner: "@LoganDark"}` → lint exit 0. Implementation note: the allowlist is loaded from `scripts/lint-vcs-parallel-call-presence.allow.json` at the lint's `__dirname` — meaning the fixture must invoke the production script with a fixture-specific allowlist file. Two valid implementation options: (a) extend the production `.allow.json` ENTRIES with a fixture entry, run the test, then revert (risky — could pollute production allowlist if test crashes); (b) write the fixture markdown so its relative path matches an entry already-present in the production allowlist; (c) RECOMMENDED — exercise the allowlist code path by writing the fixture inside the production scan-root (NOT the mkdtemp), but the cleaner pattern is to test allowlist suppression by simply confirming the parsed allowlist exposes both the path-set and glob-regex paths (i.e., load `parseAllowlist` directly with a fixture JSON and assert it returns `{files: Set, globRegexes: []}` with the entry present — this proves the path-suppression mechanism works without requiring fixture+production-allowlist coupling). USE option (c) for the test — invoke `parseAllowlist` directly with a fixture JSON object and assert the returned `files` Set contains the fixture path, plus assert that `parseAllowlist({entries: [{path: "x", expires: "...", reason: "y", owner: "@z"}]}, "...")` THROWS per `feedback_solo_dev_no_expires`.
  </behavior>
  <action>
    Create `scripts/lint-vcs-parallel-call-presence.cjs` mirroring `scripts/lint-vcs-no-raw-git.cjs` shape (per D-01 / D-02 / D-03 / D-10, per CF-04 / CF-06, per PATTERNS §"Pattern Assignments / scripts/lint-vcs-parallel-call-presence.cjs"):

    1. Shebang `#!/usr/bin/env node` + `'use strict';`
    2. JSDoc docblock at lines 2-20: (a) one-line purpose ("FILE-level pairing lint for workspace.parallel.dispatch ⇔ workspace.parallel.fan-in literals in bash/sh/zsh fences under get-shit-done/workflows/"); (b) cross-reference `// IP-2 (cancel/fanIn-only exclusion): see .planning/research/PITFALLS.md §IP-2 — deferred per D-09, no inline escape annotation today.` (D-10 one-line form, not verbose dump); (c) "Scope: SHELL FENCE not prose mention (Pitfall 7 / CF-04)"; (d) "Fence regex: BYTE-IDENTICAL to scripts/audit-workflow-raw-git.cjs:48-49 (CF-06 — shared-lib extraction forbidden per REQUIREMENTS.md §Out of Scope L92)"
    3. Imports — mirror `lint-vcs-no-raw-git.cjs:21-23`: `const fs = require('fs'); const path = require('path'); const { parseAllowlist } = require('./lib/allowlist-parser.cjs');`
    4. `parseArgv(argv)` function — verbatim copy from `lint-vcs-no-raw-git.cjs:32-41` (single `--scan-root <path>` flag, no other flags)
    5. `REPO_ROOT` + `SCAN_ROOT` constants — verbatim from `lint-vcs-no-raw-git.cjs:35-41`
    6. `ALLOW` constant — `parseAllowlist(require('./lint-vcs-parallel-call-presence.allow.json'), 'lint-vcs-parallel-call-presence')`; expose `ALLOW_FILES = ALLOW.files` + `ALLOW_GLOB_REGEXES = ALLOW.globRegexes`
    7. Fence regex constants — BYTE-IDENTICAL duplication from `audit-workflow-raw-git.cjs:48-49`. Add the comment `// Byte-identical to scripts/audit-workflow-raw-git.cjs:48-49 (CF-06 — shared-lib extraction forbidden per REQUIREMENTS.md §Out of Scope L92).` AS THE LINE IMMEDIATELY ABOVE the two const declarations. The two consts are: `const FENCE_OPEN = /^\s*(\`\`\`+|~~~+)\s*(bash|sh|zsh)\b/i;` and `const FENCE_CLOSE = /^\s*(\`\`\`+|~~~+)\s*$/;`
    8. `SCAN_ROOTS` — `['get-shit-done/workflows']` ONLY per D-03 (narrower than audit's 3-root scope to avoid Pitfall 7 false-positives on `references/` + `agents/`)
    9. Pair literal constants per CF-04 + D-01 — `const PARALLEL_DISPATCH_RE = /workspace\.parallel\.dispatch/;` and `const PARALLEL_FAN_IN_RE = /workspace\.parallel\.fan-in/;` (bare literal substrings; the fence walker already scopes to bash content). Comment: `// Content-driven literal substring detection (CF-04). Hyphenated fan-in per execute-phase.md production literal.`
    10. `findMarkdown(dir, out)` walker — verbatim copy from `audit-workflow-raw-git.cjs:98-107` INCLUDING the `if (entry.isSymbolicLink()) continue;` T-13-04 defense (ASVS V12 — see threat_model T-16.01-01)
    11. `scanFile(absPath, scanRoot)` — adapt from `audit-workflow-raw-git.cjs:114-130` but accumulate per-file booleans `hasDispatch` / `hasFanIn` instead of per-line `hits[]`. Skip shell-comment lines (`/^\s*#/`) per the audit precedent. Return `{path: rel, hasDispatch, hasFanIn}`.
    12. Main loop: iterate `SCAN_ROOTS` joined to `SCAN_ROOT`, walk each, run `scanFile` on each .md. Filter allowlist hits via the `isAllowed(rel)` pattern from `lint-vcs-no-raw-git.cjs` (check `ALLOW_FILES.has(rel)` + `ALLOW_GLOB_REGEXES.some(re => re.test(rel))`). Compute `violation = (hasDispatch !== hasFanIn) && !isAllowed(rel)` (XOR — exactly ONE literal present indicates an unpaired file; D-02 bidirectional).
    13. Exit pattern — mirror `lint-vcs-no-raw-git.cjs:146-164`: on zero violations, `console.log('ok lint-vcs-parallel-call-presence: <N> files scanned in <SCAN_ROOT>, 0 violations'); process.exit(0);`. On violations, per-file stderr emit anchored at line 1 (per <specifics>): `process.stderr.write('  ' + v.file + ':1  missing paired call (has ' + (v.hasDispatch ? 'dispatch' : 'fan-in') + ' literal but no ' + (v.hasDispatch ? 'fan-in' : 'dispatch') + ' literal in any fence)\n');`. End with the same Fix-suggestion footer pattern from `lint-vcs-no-raw-git.cjs:158-160` swapped for the allowlist filename. `process.exit(1);`

    Create `scripts/lint-vcs-parallel-call-presence.allow.json` per D-08 — schema v2 with empty entries + IP-2 narrative stub:
    - `$schema_version: 2`
    - `$migration_note: "Phase 8 Plan 1 D-03 + D-04: per-entry { path|glob, reason, owner }; expires field dropped per solo-dev override."`
    - `$comment_ip2_cancel_exclusion: "This lint enforces dispatch <-> fan-in pairing presence; cancel and fanIn-only workflows are NOT in scope today — when a real consumer emerges, the lint scope rule is revisited then. See .planning/research/PITFALLS.md IP-2."`
    - `entries: []`
    NO speculative entries for `.planning/**` / `docs/**` defensive guards (CONTEXT D-08 — fence-scope-only scan is the structural defense; defending twice violates the per-entry schema's anti-hollowing intent).

    Create 5 fixture .md files under `tests/scripts/fixtures/lint-vcs-parallel-call-presence/` (Pattern B per D-14):
    - `paired/get-shit-done/workflows/paired.md` — contains two SEPARATE bash fences, one with `workspace.parallel.dispatch` call and one with `workspace.parallel.fan-in` call (mirrors empirical structure of `execute-phase.md`)
    - `dispatch-only/get-shit-done/workflows/dispatch-only.md` — ONE bash fence with `workspace.parallel.dispatch`, NO fan-in literal anywhere in any fence
    - `fanin-only/get-shit-done/workflows/fanin-only.md` — ONE bash fence with `workspace.parallel.fan-in`, NO dispatch literal anywhere in any fence
    - `prose-only/get-shit-done/workflows/prose-only.md` — uses words "parallel", "wave", "Task(" ONLY in markdown prose paragraphs (NEVER inside any code fence). Include one harmless `code` fence to verify the walker scans content but the words `parallel.dispatch` / `parallel.fan-in` never appear inside the fence. This fixture is the Pitfall 7 false-positive guard for `code-review.md`/`audit-fix.md`-style prose-only workflows.
    - `allowlist/get-shit-done/workflows/opted-out.md` — contains dispatch literal in a bash fence, NO fan-in literal (would normally fail). Used by the allowlist test in Task 2.

    Local sanity check (will be re-verified by tests in Task 2):
      `node scripts/lint-vcs-parallel-call-presence.cjs` against the live tree should output `ok lint-vcs-parallel-call-presence: <N> files scanned in <REPO_ROOT>, 0 violations` and exit 0 (because both `execute-phase.md` and `quick.md` already pair both literals).

    Anti-patterns to AVOID (CRITICAL per CONTEXT + PATTERNS §Anti-patterns to AVOID):
    - Do NOT use a heading-based or SAME-FENCE pairing scope (empirically falsified — Pitfall 7).
    - Do NOT extract `FENCE_OPEN`/`FENCE_CLOSE` into a shared `scripts/lib/fence-walker.cjs` — REQUIREMENTS.md §Out of Scope L92 forbids "Do NOT extract a shared 'fence-aware walker' — premature abstraction; two consumers do not justify a third file." Use byte-identical duplication WITH the CF-06 comment instead.
    - Do NOT add the inline escape annotation `vcs-lint:allow-parallel-call-absent-here` — D-09 defers entirely; ROADMAP SC1 reserves the name but the regex constant + recognition logic is NOT shipped.
    - Do NOT scan `references/` or `agents/` — D-03 limits scope to `get-shit-done/workflows/` only.
    - Do NOT add an `expires` field to the allowlist schema — `feedback_solo_dev_no_expires` + CF-04. `parseAllowlist` THROWS on expires (per the parser's own schema-v2 enforcement at `scripts/lib/allowlist-parser.cjs`).
    - Do NOT seed `.planning/**` or `docs/**` globs as defensive guards — CONTEXT D-08 + PATTERNS §Anti-patterns line 208.
  </action>
  <verify>
    <automated>node /Users/LoganDark/Documents/Projects/get-shit-done/scripts/lint-vcs-parallel-call-presence.cjs &amp;&amp; test -f /Users/LoganDark/Documents/Projects/get-shit-done/scripts/lint-vcs-parallel-call-presence.allow.json &amp;&amp; node -e 'const j=require("/Users/LoganDark/Documents/Projects/get-shit-done/scripts/lint-vcs-parallel-call-presence.allow.json"); if(j.$schema_version!==2) process.exit(1); if(!Array.isArray(j.entries)) process.exit(2); if(j.entries.length!==0) process.exit(3); if(!j.$comment_ip2_cancel_exclusion) process.exit(4);' &amp;&amp; ls /Users/LoganDark/Documents/Projects/get-shit-done/tests/scripts/fixtures/lint-vcs-parallel-call-presence/paired/get-shit-done/workflows/paired.md /Users/LoganDark/Documents/Projects/get-shit-done/tests/scripts/fixtures/lint-vcs-parallel-call-presence/dispatch-only/get-shit-done/workflows/dispatch-only.md /Users/LoganDark/Documents/Projects/get-shit-done/tests/scripts/fixtures/lint-vcs-parallel-call-presence/fanin-only/get-shit-done/workflows/fanin-only.md /Users/LoganDark/Documents/Projects/get-shit-done/tests/scripts/fixtures/lint-vcs-parallel-call-presence/prose-only/get-shit-done/workflows/prose-only.md /Users/LoganDark/Documents/Projects/get-shit-done/tests/scripts/fixtures/lint-vcs-parallel-call-presence/allowlist/get-shit-done/workflows/opted-out.md</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/lint-vcs-parallel-call-presence.cjs` exists, has shebang `#!/usr/bin/env node`, has docblock containing the substring `.planning/research/PITFALLS.md` (D-10 IP-2 cross-reference) AND the substring `CF-06` (fence-regex byte-identical comment)
    - File `scripts/lint-vcs-parallel-call-presence.cjs` contains the line `const FENCE_OPEN = /^\s*(\`\`\`+|~~~+)\s*(bash|sh|zsh)\b/i;` AND the line `const FENCE_CLOSE = /^\s*(\`\`\`+|~~~+)\s*$/;` BYTE-IDENTICAL to `scripts/audit-workflow-raw-git.cjs:48-49`
    - File `scripts/lint-vcs-parallel-call-presence.cjs` contains `const PARALLEL_DISPATCH_RE = /workspace\.parallel\.dispatch/;` AND `const PARALLEL_FAN_IN_RE = /workspace\.parallel\.fan-in/;` (bare literal substrings; the fence walker scopes to bash content)
    - File `scripts/lint-vcs-parallel-call-presence.cjs` SCAN_ROOTS literal value is `['get-shit-done/workflows']` (exactly one entry; NOT including `references/` or `agents/`)
    - File `scripts/lint-vcs-parallel-call-presence.cjs` imports parseAllowlist via `require('./lib/allowlist-parser.cjs')` (verbatim from `lint-vcs-no-raw-git.cjs:23`)
    - File `scripts/lint-vcs-parallel-call-presence.cjs` includes `if (entry.isSymbolicLink()) continue;` in the walker (T-13-04 / ASVS V12 defense — see threat_model T-16.01-01)
    - File `scripts/lint-vcs-parallel-call-presence.cjs` does NOT contain the string `vcs-lint:allow-parallel-call-absent-here` (D-09 — annotation not reserved in v1.4)
    - Running `node scripts/lint-vcs-parallel-call-presence.cjs` against the live tree (no args) exits 0 with stdout matching `^ok lint-vcs-parallel-call-presence:` (production workflows already pair both literals)
    - File `scripts/lint-vcs-parallel-call-presence.allow.json` parses as JSON; has `$schema_version: 2`; has `$comment_ip2_cancel_exclusion` field with non-empty string; has `entries` field as `[]` (zero entries — D-08)
    - File `scripts/lint-vcs-parallel-call-presence.allow.json` does NOT contain the literal `expires` (per `feedback_solo_dev_no_expires`)
    - 5 fixture .md files exist at the paths listed in `files_modified`
    - The `paired.md` fixture contains BOTH literal substrings `workspace.parallel.dispatch` AND `workspace.parallel.fan-in` (verifiable via grep)
    - The `prose-only.md` fixture contains the words "parallel" or "wave" or "Task(" in prose form but does NOT contain `workspace.parallel.dispatch` OR `workspace.parallel.fan-in` ANYWHERE in the file (verifiable via grep — must be ZERO matches for both literals)
  </acceptance_criteria>
  <done>The lint script exists with correct shape; passes against the live tree; allowlist file ships with empty entries + IP-2 narrative stub; 5 fixture files exist for the next task's tests to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship node:test fixture-based unit test covering 5 D-14 scenarios + wire CI step into parallel-e2e.yml</name>
  <files>tests/scripts/lint-vcs-parallel-call-presence.test.cjs, .github/workflows/parallel-e2e.yml</files>
  <read_first>
    - tests/lint-vcs-no-raw-git-fixture.test.cjs (full file — Pattern B mkdtemp + spawnSync + --scan-root structural template, 5+ scenario test bodies as analog for each of the 5 D-14 cases)
    - tests/scripts/audit-workflow-raw-git.test.cjs (full file — node:test convention for tests/scripts/, mkdtemp-per-test isolation, finally-wrapped rmSync cleanup)
    - scripts/lint-vcs-parallel-call-presence.cjs (the lint script just created in Task 1 — the SUT)
    - .github/workflows/parallel-e2e.yml (full file — to understand CI step placement and paths-filter structure; new step lands AFTER line 127 audit step)
    - scripts/lib/allowlist-parser.cjs (full file — parseAllowlist contract; Task uses this directly for the allowlist-suppress D-14 scenario)
    - tests/scripts/fixtures/lint-vcs-parallel-call-presence/ (the 5 fixture .md trees created in Task 1)
  </read_first>
  <behavior>
    - Test scenario 1 (D-14 #1 — paired): `node scripts/lint-vcs-parallel-call-presence.cjs --scan-root <fixtureRoot>/paired` → exit code 0; stderr empty; stdout starts with `ok lint-vcs-parallel-call-presence:`
    - Test scenario 2 (D-14 #2 — dispatch-only): `node scripts/lint-vcs-parallel-call-presence.cjs --scan-root <fixtureRoot>/dispatch-only` → exit code 1; stderr contains the path `get-shit-done/workflows/dispatch-only.md`, contains `:1`, AND contains the word `fan-in` (diagnostic explains the missing literal); stdout empty or "ERROR"-prefixed
    - Test scenario 3 (D-14 #3 — fan-in-only): same as scenario 2 but for `fanin-only` fixture and the missing-literal word is `dispatch`
    - Test scenario 4 (D-14 #4 — prose-only): `node scripts/lint-vcs-parallel-call-presence.cjs --scan-root <fixtureRoot>/prose-only` → exit code 0; stderr empty (the Pitfall 7 false-positive guard — prose mentions don't fire)
    - Test scenario 5 (D-14 #5 — allowlist suppress): invokes `parseAllowlist` directly with a fixture JSON `{$schema_version: 2, entries: [{path: 'get-shit-done/workflows/opted-out.md', reason: 'test fixture', owner: '@LoganDark'}]}` and asserts the returned `files` Set contains `'get-shit-done/workflows/opted-out.md'`. SEPARATELY assert that `parseAllowlist({$schema_version: 2, entries: [{path: 'x', expires: '2026-01-01', reason: 'y', owner: '@z'}]}, 'test')` THROWS (proves no-expires enforcement per `feedback_solo_dev_no_expires`).
    - CI Step Verify: After modifying `.github/workflows/parallel-e2e.yml`, automated grep proves the new step exists adjacent to the existing audit step
  </behavior>
  <action>
    Create `tests/scripts/lint-vcs-parallel-call-presence.test.cjs` per CONTEXT D-14 and PATTERNS §"tests/scripts/lint-vcs-parallel-call-presence.test.cjs":

    1. Imports — mirror `tests/lint-vcs-no-raw-git-fixture.test.cjs:15-23`:
       `const { test } = require('node:test'); const assert = require('node:assert/strict'); const fs = require('node:fs'); const os = require('node:os'); const path = require('node:path'); const { spawnSync } = require('node:child_process');`
    2. Constants — `REPO_ROOT = path.resolve(__dirname, '..', '..');`, `SCRIPT = path.join(REPO_ROOT, 'scripts', 'lint-vcs-parallel-call-presence.cjs');`, `FIXTURE_ROOT = path.join(__dirname, 'fixtures', 'lint-vcs-parallel-call-presence');`
    3. Five test functions (use direct fixture-path scan-root — NOT mkdtempSync for these since the fixtures are checked into git; mkdtempSync is for tests that materialize ephemeral content. Mirror the simpler `audit-workflow-raw-git.test.cjs` pattern that points scan-root at a fixture subdir):

       - `test('LINT-06 D-14 #1: paired dispatch + fan-in literals in separate fences → exit 0', () => {...})` — spawns `process.execPath, [SCRIPT, '--scan-root', path.join(FIXTURE_ROOT, 'paired')]`; asserts `r.status === 0`; asserts `r.stdout.startsWith('ok lint-vcs-parallel-call-presence:')`
       - `test('LINT-06 D-14 #2: dispatch-only fence → exit 1; diagnostic names fan-in as missing', () => {...})` — `--scan-root .../dispatch-only`; asserts `r.status === 1`; asserts `r.stderr.includes('dispatch-only.md:1')`; asserts `r.stderr.includes('fan-in')`
       - `test('LINT-06 D-14 #3: fan-in-only fence → exit 1; diagnostic names dispatch as missing', () => {...})` — symmetric to #2
       - `test('LINT-06 D-14 #4: prose-only mentions of parallel/wave → exit 0 (Pitfall 7 false-positive guard)', () => {...})` — `--scan-root .../prose-only`; asserts `r.status === 0`
       - `test('LINT-06 D-14 #5: allowlist entry suppresses opted-out file', () => {...})` — invokes `parseAllowlist` directly from the parser module: `const { parseAllowlist } = require(path.join(REPO_ROOT, 'scripts', 'lib', 'allowlist-parser.cjs'));`. First assertion: parse a fixture JSON containing the opted-out path; assert `result.files.has('get-shit-done/workflows/opted-out.md')` AND `result.files instanceof Set`. SECOND assertion: parse a fixture JSON containing `expires` field → assert it THROWS (per `feedback_solo_dev_no_expires` + CF-04).

    4. Each test that spawns the lint MUST wrap the spawnSync call with `{encoding: 'utf-8', timeout: 15000}` to prevent hung-process flakes.

    Modify `.github/workflows/parallel-e2e.yml` per CF-05 + PATTERNS §".github/workflows/parallel-e2e.yml":

    1. Add 2 paths-filter entries — INSERT INTO existing `paths:` block at line 36-42, BETWEEN the existing `'scripts/audit-workflow-raw-git.cjs'` line (line 40) and the existing `'scripts/e2e-parallel-phase.sh'` line (line 41):
       - `      - 'scripts/lint-vcs-parallel-call-presence.cjs'`
       - `      - 'scripts/lint-vcs-parallel-call-presence.allow.json'`
    2. Add new lint step — INSERT INTO the same job as the existing audit step, IMMEDIATELY AFTER the line `        run: node scripts/audit-workflow-raw-git.cjs` (line 127). The new step is exactly 3 YAML lines (preserve indentation from the audit step — typically 6-space-indented under `steps:`):
       ```
             # LINT-06 / Phase 16.01: enforce dispatch <-> fan-in pairing presence in
             # bash/sh/zsh fences under get-shit-done/workflows/. FILE-level scope per
             # D-01; default-deny + per-entry allowlist via scripts/lib/allowlist-parser.cjs.
             # CI-only (NOT promoted to npm pretest per CF-05).
             - name: Lint — workflow call-presence (LINT-06)
               shell: bash
               run: node scripts/lint-vcs-parallel-call-presence.cjs
       ```
       (Match the YAML indentation EXACTLY to the existing audit step's indentation — use the audit step's indent as the template; do not invent indentation.)
    3. Do NOT add a separate `parallel-e2e-gate-lint` job — the existing `parallel-e2e-gate` blocking job at `:136-161` already covers the new step automatically (CF-05).
    4. Do NOT modify any other section (jobs, matrix, env, on triggers, etc.).

    Anti-patterns to AVOID:
    - Do NOT invoke the lint without `--scan-root` in tests (would scan the production repo root + pollute the test with whatever's in `get-shit-done/workflows/` today).
    - Do NOT share fixture state across tests (use distinct fixture subdirectories per scenario — the fixture files were created in Task 1 with separate subtrees per scenario).
    - Do NOT use a custom matcher / `expect.extend` — `node:assert/strict` is the `tests/` convention (per PATTERNS line 271 + `tests/scripts/audit-workflow-raw-git.test.cjs:22`).
    - Do NOT combine the new CI step with the existing audit step into one `run:` block — CF-05 mandates "adjacent" not "combined" (separate steps give clearer failure attribution per RESEARCH §Alternatives Considered line 132).
    - Do NOT add the new step outside the `parallel-e2e` matrix job — it MUST run on the jj-colocated cell for the blocking guarantee to apply.
    - Do NOT promote to `npm pretest` — CF-05 + REQUIREMENTS.md §Out of Scope L93 forbid.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; node --test tests/scripts/lint-vcs-parallel-call-presence.test.cjs &amp;&amp; grep -q "lint-vcs-parallel-call-presence" .github/workflows/parallel-e2e.yml &amp;&amp; grep -c "scripts/lint-vcs-parallel-call-presence" .github/workflows/parallel-e2e.yml | grep -qE "^[3-9]|^[1-9][0-9]"</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/scripts/lint-vcs-parallel-call-presence.test.cjs` exists and uses `require('node:test')` + `require('node:assert/strict')` (NOT vitest — tests/ convention per PATTERNS line 271)
    - All 5 test functions exist, named per D-14 #1 through #5, identifiable via grep for `LINT-06 D-14 #1` through `LINT-06 D-14 #5` in the test source
    - `node --test tests/scripts/lint-vcs-parallel-call-presence.test.cjs` exits 0 with all 5 tests passing (no `# fail` lines, no `# skip` lines)
    - Test for D-14 #5 (allowlist) invokes `parseAllowlist` directly from `scripts/lib/allowlist-parser.cjs` AND includes an assertion that `parseAllowlist` THROWS when given an entry with `expires` (proves `feedback_solo_dev_no_expires` enforcement)
    - `.github/workflows/parallel-e2e.yml` contains the literal `Lint — workflow call-presence (LINT-06)` (the new step name; em-dash NOT hyphen, per CF-05 adjacency to audit step's same em-dash style)
    - `.github/workflows/parallel-e2e.yml` contains the literal `run: node scripts/lint-vcs-parallel-call-presence.cjs` (the new step body)
    - `.github/workflows/parallel-e2e.yml` paths-filter block (around line 36-44 after insertion) contains `scripts/lint-vcs-parallel-call-presence.cjs` AND `scripts/lint-vcs-parallel-call-presence.allow.json` (verifiable via `grep -c "lint-vcs-parallel-call-presence" .github/workflows/parallel-e2e.yml` returning 3 or more — script step name + script run path + 2 paths-filter entries; at least 3 hits proves all sites)
    - `.github/workflows/parallel-e2e.yml` does NOT contain a new top-level job named `parallel-e2e-gate-lint` or similar — the existing `parallel-e2e-gate` job is unchanged (verifiable via `grep -E "^[a-z-]+:$" .github/workflows/parallel-e2e.yml` returning the SAME job names as the pre-task file).
    - `.github/workflows/parallel-e2e.yml` does NOT have the lint step duplicated outside the existing `parallel-e2e` matrix job
    - `npm pretest` / `package.json` does NOT reference `lint-vcs-parallel-call-presence.cjs` (per CF-05)
    - Manual verification (checkpoint per VALIDATION.md §Manual-Only): operator inspects `.github/workflows/parallel-e2e.yml` to confirm new step is required-blocking via the existing `parallel-e2e-gate` job dependency chain — this is documented as `<human-check>` in the verify block since the YAML structure / required-status-check binding is a manual policy decision that GitHub branch-protection settings (outside repo) ultimately enforce.
  </acceptance_criteria>
  <done>5 D-14 test scenarios all pass; CI step + paths-filter entries land in `.github/workflows/parallel-e2e.yml`; new step is inside the existing `parallel-e2e` matrix job; no separate gate job added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → lint via CLI argv | `--scan-root <path>` argument is operator-supplied (or test-supplied). Untrusted path traversal risk if not canonicalized. |
| CI runner → repo filesystem | The lint walks `.md` files; symlink traversal could escape repo root if not guarded. |
| Workflow markdown → lint regex | The lint reads bash/sh/zsh fence content. Pathological inputs (extremely long lines, binary content disguised as `.md`) should not crash the lint. |
| Allowlist JSON → parser | `scripts/lint-vcs-parallel-call-presence.allow.json` is part of the repo (committed). No external input on this surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.01-01 | Tampering / Information Disclosure | `findMarkdown` walker in `scripts/lint-vcs-parallel-call-presence.cjs` | mitigate | Skip symbolic links via `entry.isSymbolicLink() && continue` — defense-in-depth byte-identical to `scripts/audit-workflow-raw-git.cjs:102` (T-13-04 precedent). Prevents a malicious symlink in the scan-root from causing the walker to descend outside the repo root. ASVS V12 (file resource handling). |
| T-16.01-02 | Tampering | `--scan-root` argv handling | mitigate | `path.resolve(ARGV.scanRoot)` canonicalizes the input (verbatim from `scripts/lint-vcs-no-raw-git.cjs:35`). Prevents relative-path tricks. The lint never writes files — only reads + emits diagnostics to stdout/stderr; lateral movement risk is bounded to "scan files in a directory the operator can already read." |
| T-16.01-03 | Denial of Service | `scanFile` regex against pathological line lengths | accept | Workflow markdown is human-authored under `get-shit-done/workflows/`; lines exceeding 100KB do not appear in the production tree. The regex `PARALLEL_DISPATCH_RE = /workspace\.parallel\.dispatch/` is a bare literal substring (no backtracking — no ReDoS surface). FENCE_OPEN/FENCE_CLOSE regex from CF-06 are anchored, also bounded. Mitigation cost (line-length cap) exceeds risk benefit for CI-only lint. |
| T-16.01-SC | Tampering (supply chain) | No new npm installs (zero new packages per RESEARCH §Package Legitimacy Audit) | N/A | No package install tasks in this plan. The Package Legitimacy Gate protocol does not apply. All dependencies are Node.js built-ins (`fs`, `path`) + existing repo modules (`scripts/lib/allowlist-parser.cjs`). |
</threat_model>

<verification>
End-of-plan acceptance:
1. `node scripts/lint-vcs-parallel-call-presence.cjs` against the live tree (no args) exits 0 with `ok` stdout prefix
2. `node --test tests/scripts/lint-vcs-parallel-call-presence.test.cjs` passes all 5 scenarios (0 fail, 0 skip)
3. `grep -c "lint-vcs-parallel-call-presence" .github/workflows/parallel-e2e.yml` returns ≥ 3 (step name + step run path + 2 paths-filter entries)
4. `pnpm test` (full suite from repo root) green — the new node:test test file is auto-discovered by the project's test runner
5. No new entry in `scripts/lint-vcs-no-raw-git.allow.json` (LINT-05 net diff stays at zero — the new lint scripts are raw-git-clean by construction)
6. `grep -c "expires" scripts/lint-vcs-parallel-call-presence.allow.json` returns 0 (no-expires enforcement per `feedback_solo_dev_no_expires`)
</verification>

<success_criteria>
ROADMAP SC1 (LINT-06 acceptance, verbatim):
- `scripts/lint-vcs-parallel-call-presence.cjs` exits 0 when scanning workflow markdown containing paired `workspace.parallel.dispatch` + `workspace.parallel.fan-in` literals in shell fences; exits 1 when fan-in literal is present but dispatch is missing within the same file (or vice versa)
- Content-driven detection via literal substring match in `bash`/`sh`/`zsh` fences (NOT heading-based tagging per Pitfall 7)
- Per-entry `{path|glob, reason, owner}` allowlist consumed via `scripts/lib/allowlist-parser.cjs` (no `expires` per `feedback_solo_dev_no_expires`)
- Fixture-based unit test under `tests/scripts/` covers Pitfall 7 false-positive cases

ROADMAP SC2:
- New CI step in `.github/workflows/parallel-e2e.yml` runs the lint adjacent to the existing `audit-workflow-raw-git.cjs` CI-06 step; required-blocking on jj-colocated (inherits via `parallel-e2e-gate`); NOT promoted to `npm pretest`
</success_criteria>

<output>
Create `.planning/phases/16-workflow-invariant-tooling/16-01-SUMMARY.md` after both tasks complete and verification is green.
</output>
