---
phase: 16-workflow-invariant-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/cleanup-subagent-workspaces.ts
  - sdk/src/query/command-static-catalog-domain.ts
  - sdk/src/query/command-manifest.non-family.ts
  - sdk/src/query/command-aliases.generated.ts
  - sdk/src/vcs/jj/parallel.ts
  - sdk/src/vcs/jj/workspace-cleanup.ts
  - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts
  - scripts/dogfood-restore.sh
  - tests/cli-cleanup-subagent-workspaces.test.cjs
  - tests/scripts/dogfood-restore-orphan-cleanup.test.cjs
autonomous: true
requirements:
  - CLEANUP-02
tags:
  - cleanup
  - parallel
  - vcs

must_haves:
  truths:
    - "After `vcs.workspace.parallel.fan-in` clean-path success branch on jj backend, no `.claude/jj-workspaces/phase-*-subagent-*` FS directories exist on disk for any workspace in the handle — D-15 cross-backend assertion (`existsSync(ws.path) === false` for every workspace in the handle) lives in `sdk/src/vcs/__tests__/` using vcs-fixture.ts Pattern B mkdtemp"
    - "Conflicted-branch behavior UNCHANGED — `performJjParallelFanIn` conflicted branch preserves workspaces on disk for human inspection per W3 (a) joint-assertion contract; inline comment at parallel.ts:394-410 documents the intentional cleanup-omission"
    - "Cross-backend symmetry — git-cell fanIn-success path already removes workspaces via `git worktree remove --force`; new regression test asserts `existsSync(ws.path) === false` post-clean-fanIn on git per D-15 git-cell scope (Phase 15 deferred-idea note: no orphan-dirs on git is the existing invariant)"
    - "New CLI bridge `gsd-sdk query cleanup-subagent-workspaces` accepts `--phase <N>` OR `--all-phases` (mutually exclusive) per D-04 (matches explicit-flag norm of workspace-parallel-{dispatch,fan-in,cancel}.ts); `--all-phases` enumerates `.claude/jj-workspaces/` via readdirSync filtered by /^phase-(\\d+)-subagent-\\d+$/ per D-05, calls the helper once per unique phase number, merges to a single `{ data: {abandoned, failedReaped} }` envelope per D-06; returns structured `{ok: false, reason: '<snake>'}` envelope on argv errors"
    - "Three-site CLI bridge registration is complete — verb resolves at runtime via `gsd-sdk query cleanup-subagent-workspaces` (registered at catalog + manifest + aliases.generated per CF-02)"
    - "`scripts/dogfood-restore.sh` includes an idempotent post-restore cleanup step (LAST step per D-11; trap-with-WARN per D-12; second diagnostic with counts per D-13); end-to-end test seeds two distinct phase numbers (e.g., phase-15-subagent-1 and phase-16-subagent-2) per D-16 to verify `--all-phases` enumeration covers cross-phase orphans after a synthetic `jj op restore` + orphan-survival fixture"
    - "Single owner per IP-5 — fanIn clean-path branch, dogfood-restore.sh (via CLI bridge), and the existing cancel verb ALL call `cleanupSubagentWorkspaces(...)` directly; no inline `rmSync` / `jj workspace forget` duplicate body in any of the 3 consumers; helper docblock at sdk/src/vcs/jj/workspace-cleanup.ts gains a one-line consumer-completion cross-reference to .planning/phases/16-.../16-CONTEXT.md per D-17"
  artifacts:
    - path: "sdk/src/query/cleanup-subagent-workspaces.ts"
      provides: "CLI bridge wrapping cleanupSubagentWorkspaces helper for bash consumers; supports --phase N (single-phase) and --all-phases (enumerate .claude/jj-workspaces/ and merge envelopes per D-06)"
      exports: ["cleanupSubagentWorkspacesQuery"]
    - path: "sdk/src/query/command-static-catalog-domain.ts"
      provides: "Site 1 of three-site bridge registration (catalog domain — import + map entry per CF-02)"
      contains: "cleanupSubagentWorkspacesQuery"
    - path: "sdk/src/query/command-manifest.non-family.ts"
      provides: "Site 2 of three-site bridge registration (manifest descriptor)"
      contains: "cleanup-subagent-workspaces"
    - path: "sdk/src/query/command-aliases.generated.ts"
      provides: "Site 3 of three-site bridge registration (alias table)"
      contains: "cleanup-subagent-workspaces"
    - path: "sdk/src/vcs/jj/parallel.ts"
      provides: "clean-path branch direct TS call to cleanupSubagentWorkspaces (D-07); inline AP-5 comment at conflicted branch (CF-03 / SC4)"
      contains: "CLEANUP-02"
    - path: "scripts/dogfood-restore.sh"
      provides: "post-restore orphan-workspace cleanup step (LAST step per D-11; trap with WARN per D-12; second diagnostic per D-13)"
      contains: "cleanup-subagent-workspaces"
    - path: "tests/cli-cleanup-subagent-workspaces.test.cjs"
      provides: "CLI bridge smoke test proving three-site verb resolution end-to-end"
    - path: "tests/scripts/dogfood-restore-orphan-cleanup.test.cjs"
      provides: "End-to-end bash integration test — synthetic jj op restore + 2-phase orphan-survival fixture; asserts dirs gone post-script"
  key_links:
    - from: "sdk/src/vcs/jj/parallel.ts clean-path else branch (after merged.push(mergeChangeId) at :460)"
      to: "sdk/src/vcs/jj/workspace-cleanup.ts (cleanupSubagentWorkspaces helper at :134)"
      via: "direct TS function call cleanupSubagentWorkspaces(mainRepoRoot, handle.phaseNumber, handle.workspaces)"
      pattern: "cleanupSubagentWorkspaces\\("
    - from: "sdk/src/query/cleanup-subagent-workspaces.ts"
      to: "sdk/src/vcs/jj/workspace-cleanup.ts"
      via: "import { cleanupSubagentWorkspaces } from '../vcs/jj/workspace-cleanup.js'"
      pattern: "import.*cleanupSubagentWorkspaces.*workspace-cleanup"
    - from: "scripts/dogfood-restore.sh last step"
      to: "sdk/src/query/cleanup-subagent-workspaces.ts (via gsd-sdk query CLI bridge)"
      via: "CLEANUP_JSON=$(gsd-sdk query cleanup-subagent-workspaces --all-phases ... || ...)"
      pattern: "cleanup-subagent-workspaces.*--all-phases"
---

<objective>
Close CLEANUP-02 (v14-orphan-jj-workspace-dirs) by wiring TWO new consumer call sites of the Phase 15.04-extracted `cleanupSubagentWorkspaces` helper at `sdk/src/vcs/jj/workspace-cleanup.ts:134`:

1. **TS-direct call** from the `performJjParallelFanIn` clean-path `else` branch (after `merged.push(mergeChangeId)` at `parallel.ts:460`). This reaps orphan `.claude/jj-workspaces/phase-*-subagent-*` directories on every successful clean fan-in. Conflicted-branch behavior is UNCHANGED per CF-03 / AP-5 / W3 (a) joint-assertion contract — workspaces stay on disk for human inspection; inline comment at `parallel.ts:394-410` documents the intentional cleanup-omission per ROADMAP SC4.

2. **CLI-bridge call** from `scripts/dogfood-restore.sh` — a new bridge `sdk/src/query/cleanup-subagent-workspaces.ts` (three-site registration per CF-02). Bridge supports both `--phase <N>` AND `--all-phases` per D-04 (mutually exclusive); `--all-phases` enumerates `.claude/jj-workspaces/` via `readdirSync`, filters by `/^phase-(\d+)-subagent-\d+$/`, calls the helper per phase, merges per-phase results to a single `{abandoned, failedReaped}` envelope per D-06. The dogfood-restore.sh step lands LAST per D-11 (after `tar -xf` + complete diagnostic), traps the bridge call with `|| WARN-echo + exit-0` per D-12, and adds a second diagnostic echo with counts per D-13.

Purpose: Closes the last v14-orphan-jj-workspace-dirs cleanup-contract gap. Single owner per IP-5 — all three consumers (cancel verb, fanIn clean-path, dogfood-restore.sh) call the SAME helper; no inline duplication of `jj workspace forget + rmSync` body anywhere. Conflicted branch UNCHANGED is the load-bearing W3 (a) contract — forensics inspection requires workspaces to persist on disk.

Output: 1 new TS file + 6 modified production files + 2 new test files + 1 helper JSDoc cross-reference (D-17). Zero new npm packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-workflow-invariant-tooling/16-CONTEXT.md
@.planning/phases/16-workflow-invariant-tooling/16-RESEARCH.md
@.planning/phases/16-workflow-invariant-tooling/16-PATTERNS.md
@.planning/phases/16-workflow-invariant-tooling/16-VALIDATION.md
@sdk/src/vcs/jj/workspace-cleanup.ts
@sdk/src/vcs/jj/parallel.ts
@sdk/src/query/workspace-parallel-cancel.ts
@sdk/src/query/command-static-catalog-domain.ts
@sdk/src/query/command-manifest.non-family.ts
@sdk/src/query/command-aliases.generated.ts
@scripts/dogfood-restore.sh
@tests/cli-workspace-parallel-cancel.test.cjs

<interfaces>
Locked contracts extracted from the canonical analog files. Executor uses these directly; no codebase exploration needed for shape questions.

From `sdk/src/vcs/jj/workspace-cleanup.ts:134-138` — LOCKED HELPER SIGNATURE per CF-01. Function `cleanupSubagentWorkspaces` takes `(mainRepoRoot: string, phaseNumber: number, workspaces?: readonly { name: string; path: string }[])` and returns `CleanupSubagentWorkspacesResult = { abandoned: readonly string[]; failedReaped: readonly string[] }`. Idempotent per D-03/D-06: re-call after a successful cleanup returns `{abandoned: [], failedReaped: []}`. Sidecar discipline — does NOT import from `backends/jj.ts`.

From `sdk/src/vcs/jj/parallel.ts:557-561` — intra-file self-precedent for the direct call shape (the existing `performJjParallelCancel` site). Destructures both return fields: `const { abandoned, failedReaped } = cleanupSubagentWorkspaces(mainRepoRoot, handle.phaseNumber, handle.workspaces);`

From `sdk/src/vcs/jj/parallel.ts:67` — import line already in place. Token to grep for: `cleanupSubagentWorkspaces` (1 import + 1 existing call site at :557 — Task 2 ADDS a second call site in the clean-path branch).

From `sdk/src/vcs/jj/parallel.ts:389-461` — the conditional whose ELSE branch needs the new call. Structure: `let incompleteQueued = 0;` `const merged: string[] = [];` `let surplusBookmarks: string[] = [];` `const failedReaped: string[] = [];` (line 392 — the variable that the new call merges into). Then `if (conflicted) { ... }` (lines 394-410 — the W3 (a) preservation branch; NO MODIFICATION except inline comment) `else { ... merged.push(mergeChangeId); }` (line 460 — INSERT THE NEW HELPER CALL IMMEDIATELY AFTER THIS LINE).

From `sdk/src/query/workspace-parallel-cancel.ts` — full 88-line analog template. The cancel bridge uses `--handle @<path>` / `@-` file-or-stdin pattern; the new bridge uses `--phase N` / `--all-phases` mutually exclusive — different argv shape. Token to mirror: `export const workspaceParallelCancelQuery: QueryHandler = async (args, projectDir) => { ... }`. Error envelope shape: `return { data: { ok: false, reason: 'handle_required' } };`. Final success return: `return { data: cancelResult };`. Inline argv walk pattern at lines 54-64 — no shared argv parser exists.

From `sdk/src/query/command-static-catalog-domain.ts:23, 76-79` — Site 1 of three-site registration. Line 23 import: `import { workspaceParallelCancelQuery } from './workspace-parallel-cancel.js';`. Lines 76-79 map entries: comment line `// Phase 15.04 (PARALLEL-07): cancel verb — synchronous teardown of...` followed by `['workspace.parallel.cancel', workspaceParallelCancelQuery],` and `['workspace parallel.cancel', workspaceParallelCancelQuery],` (TWO entries because dot+space alias for the dot-namespaced verb). For the new single-word-with-hyphens verb `cleanup-subagent-workspaces`, mirror the `worktree.cleanup-wave`-style single-entry precedent — ONE entry per CF-02 / PATTERNS line 673.

From `sdk/src/query/command-manifest.non-family.ts:60-63` — Site 2 of three-site registration. Existing block lists `workspace.assert-dispatched-cwd`, `workspace.parallel.dispatch`, `workspace.parallel.fan-in`, `workspace.parallel.cancel` (line 63). Entry shape: `{ canonical: '<name>', aliases: [<dot-space-alias>], mutation: <bool>, outputMode: 'json' }`.

From `sdk/src/query/command-aliases.generated.ts:1-3 + :156` — Site 3 of three-site registration. Header says "GENERATED FILE." Cancel entry at line 156: `{ canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true },`. Per Pitfall 6 + RESEARCH §Open Q1: planner first inspects lines 1-10 to find the source-of-generation comment; if a regenerator exists, runs it; otherwise hand-edits.

From `scripts/dogfood-restore.sh:29, 55-61` — current state. Line 29 sets `set -euo pipefail` (DO NOT CHANGE). Lines 55-61 are the existing tail: `echo "dogfood-restore: restoring op-id ${PRE_OP_ID}" >&2`, `jj op restore "$PRE_OP_ID"`, `echo "dogfood-restore: extracting ${TARBALL_PATH}" >&2`, `tar -xf "$TARBALL_PATH" -C .`, `echo "dogfood-restore: complete. Verify with: jj diff --summary && jj log -r '@-..@' --no-graph" >&2`. The new step appends AFTER this final echo per D-11.

From `tests/cli-workspace-parallel-cancel.test.cjs:31-47` — CLI smoke test analog. Imports `node:test`, `node:assert/strict`, `node:child_process` spawnSync, `node:path`. Defines `SDK_BIN = path.resolve(__dirname, '..', 'sdk', 'bin', 'gsd-sdk.js');`. Helper `runQuery(argv, { stdin })` wraps `spawnSync('node', [SDK_BIN, 'query', ...argv], { encoding: 'utf-8', timeout: 15000 })`. Tests prove three-site verb resolution via canonical-form positive test + bogus-verb negative control. The new bridge needs the same shape plus mutually-exclusive-flag tests.

From `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts:197-201` — the existing test assertion that needs FLIPPING. Currently: after clean fan-in, `expect(remainingWorkspaces.length).toBe(handle.workspaces.length);` (asserts workspaces still present — wrong invariant after CLEANUP-02 lands). After this plan: must flip to `expect(remainingWorkspaces.length).toBe(0);` (asserts workspaces reaped). The pre-existing comment block at :185-196 must be updated to reflect the new "wired" state (the "(cleanup wiring belongs to downstream Phase 11 plans)" parenthetical becomes obsolete). The STATE.md entry under "Phase 11 Plan 01" says "will flip back to 0 when 11.04/11.05 wires clean-workspace cleanup" — this plan is the "later" plan that finally flips it.

From `sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts:94, 120` — assertion shape precedent. Pre-cancel `expect(existsSync(ws.path)).toBe(true);` (line 94 — proves dispatch materialized). Post-cancel `expect(existsSync(ws.path)).toBe(false);` (line 120 — proves cleanup happened). This is the EXACT shape Task 3 mirrors for the post-clean-fanIn assertion. Test pre-conditions: the cleanup helper's `forget + rmSync` body has been exercised; same body is reused via the new fanIn-clean-path call site.

From `sdk/src/vcs/jj/octopus.ts:300` — the canonical workspace name format. Workspaces are named `phase-${phaseTag}-subagent-${idx}` where `phaseTag = String(phaseNumber).padStart(2, '0')`. The regex `/^phase-(\d+)-subagent-\d+$/` MUST match this format exactly (anchored — both start `^` AND end `$`) to prevent path-injection via crafted dir names per ASVS V12 / threat_model T-16.02-04.

CRITICAL DECISIONS — NON-NEGOTIABLE per CONTEXT.md:
- **CF-01:** Helper signature locked — 3-arg form with `workspaces?` override. Idempotent per D-03/D-06.
- **CF-02:** Three-site CLI bridge registration — missing ANY site = runtime "verb not found" (Pitfall 6 / Phase 11 plan 02 audit).
- **CF-03 / AP-5 / SC4:** Conflicted branch UNCHANGED. ANY `rmSync` or `cleanupSubagentWorkspaces` call inside `if (conflicted) {...}` block violates the W3 (a) joint-assertion contract.
- **D-04:** Bridge supports BOTH `--phase N` AND `--all-phases` (mutually exclusive). Mutual-exclusion check rejects `--phase 16 --all-phases` early.
- **D-05:** `--all-phases` enumerates `.claude/jj-workspaces/` via `readdirSync`, filters by `/^phase-(\d+)-subagent-\d+$/`.
- **D-06:** Single merged envelope across phases — `{abandoned: readonly string[], failedReaped: readonly string[]}`. Workspace names are phase-prefixed so concatenation is collision-free.
- **D-07:** fanIn clean-path uses TS-direct call (handle.workspaces in scope, cheaper). NOT via CLI bridge.
- **D-11/D-12/D-13:** dogfood-restore.sh cleanup step is LAST; trap-with-WARN + exit 0; second diagnostic with counts.
- **D-15:** Cross-backend test in `sdk/src/vcs/__tests__/` (vitest) — augments existing `cmd-parallel-jj.test.ts` + `cmd-parallel-git.test.ts` with new `it` blocks (NOT a new file).
- **D-16:** dogfood-restore integration test seeds TWO phase numbers to verify `--all-phases` cross-phase enumeration.
- **D-17:** Add one-line cross-reference to `16-CONTEXT.md` at the helper's docblock (consumer-completion record).
- **Pitfall 11 / IP-5:** Single owner — NEVER inline `rmSync` / `jj workspace forget`. ALWAYS call the helper.
- **VERIFIED via Bash (RESEARCH §Claude's Discretion):** `sdk/src/query/cli/argv.ts` does NOT exist; bridge must use inline argv walk per `workspace-parallel-cancel.ts:54-64` precedent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ship the CLI bridge + three-site registration + CLI smoke test</name>
  <files>sdk/src/query/cleanup-subagent-workspaces.ts, sdk/src/query/command-static-catalog-domain.ts, sdk/src/query/command-manifest.non-family.ts, sdk/src/query/command-aliases.generated.ts, tests/cli-cleanup-subagent-workspaces.test.cjs</files>
  <read_first>
    - sdk/src/query/workspace-parallel-cancel.ts (full 88-line file — analog template for the new bridge; copy argv-walk pattern, structured-error-envelope shape, three-site registration self-documentation block)
    - sdk/src/query/command-static-catalog-domain.ts lines 1-100 (the import block + DOMAIN_STATIC_CATALOG array; verify cancel verb at :78-79 and the worktree.cleanup-wave single-entry precedent at :67-68 to mirror)
    - sdk/src/query/command-manifest.non-family.ts lines 1-80 (manifest array; verify cancel verb at :63 to mirror)
    - sdk/src/query/command-aliases.generated.ts lines 1-10 + lines 150-165 (see GENERATED FILE header + cancel verb at :156; check whether hand-edit or regeneration is the operation)
    - sdk/src/vcs/jj/workspace-cleanup.ts (full 202-line file — locked helper docblock at :96-133 + body at :134-200; confirms workspaces?-fallback + idempotency contract)
    - sdk/src/query/utils.ts (or .js — check exports) to confirm `QueryHandler` type signature
    - tests/cli-workspace-parallel-cancel.test.cjs (full file — analog for the smoke test; canonical-form positive test + bogus-verb negative control + mutually-exclusive-flag test pattern)
    - sdk/src/vcs/jj/octopus.ts line 300 (canonical workspace name format `phase-${phaseTag}-subagent-${idx}` — confirms the regex used in --all-phases enumeration)
    - .planning/phases/16-workflow-invariant-tooling/16-PATTERNS.md lines 275-447 (Pattern Assignments for the CLI bridge + the three registration sites + the CLI smoke test)
  </read_first>
  <behavior>
    - Bridge behavior 1: `gsd-sdk query cleanup-subagent-workspaces` (no flags) → exit 0; stdout contains JSON `{ok: false, reason: 'phase_or_all_phases_required'}`
    - Bridge behavior 2: `gsd-sdk query cleanup-subagent-workspaces --phase 16 --all-phases` → exit 0; stdout contains JSON `{ok: false, reason: 'phase_and_all_phases_mutually_exclusive'}`
    - Bridge behavior 3: `gsd-sdk query cleanup-subagent-workspaces --phase abc` (non-numeric) → exit 0; stdout contains JSON `{ok: false, reason: 'invalid_phase_number'}` (ASVS V5 guard)
    - Bridge behavior 4: `gsd-sdk query cleanup-subagent-workspaces --phase 16` against a directory with NO `.claude/jj-workspaces/` → exit 0; stdout contains JSON `{abandoned: [], failedReaped: []}` (idempotent helper contract per D-03/D-06)
    - Bridge behavior 5: `gsd-sdk query cleanup-subagent-workspaces --all-phases` against a directory with NO `.claude/jj-workspaces/` → exit 0; stdout contains JSON `{abandoned: [], failedReaped: []}` (merged empty envelope per D-06)
    - Three-site registration: `gsd-sdk query cleanup-subagent-workspaces-XYZ` (bogus verb) → non-zero exit OR `unknown verb` substring on stderr (negative control proves verb-resolution lookup is active)
  </behavior>
  <action>
    Create `sdk/src/query/cleanup-subagent-workspaces.ts` mirroring `sdk/src/query/workspace-parallel-cancel.ts` shape per CF-02, D-04, D-05, D-06, and per PATTERNS §"sdk/src/query/cleanup-subagent-workspaces.ts".

    File structure:
    1. JSDoc docblock — purpose statement (Phase 16.02 / CLEANUP-02 — CLI bridge wrapping the locked Phase 15.04 helper at `sdk/src/vcs/jj/workspace-cleanup.ts:134` for bash consumers like `scripts/dogfood-restore.sh`); flag enumeration (--cwd, --phase N, --all-phases); explicit note "The TS-side fanIn clean-path branch calls the helper DIRECTLY (NOT via this bridge — bridge is for bash only)" per D-07; three-site registration call-out citing the catalog/manifest/aliases triplet by exact filename + line per CF-02 + Pitfall 6; reference to the smoke test path `tests/cli-cleanup-subagent-workspaces.test.cjs`.
    2. Imports — `import { readdirSync, existsSync } from 'node:fs';`, `import { join } from 'node:path';`, `import { cleanupSubagentWorkspaces } from '../vcs/jj/workspace-cleanup.js';`, `import type { QueryHandler } from './utils.js';`. NO `createVcsAdapter` import — helper is sidecar code called directly with `mainRepoRoot` arg; no adapter hop needed.
    3. Constant — `const WORKSPACE_NAME_RE = /^phase-(\d+)-subagent-\d+$/;` (anchored — matches the canonical format from `octopus.ts:300`; prevents path-injection via crafted dir names per ASVS V12 / threat_model T-16.02-04)
    4. Export `cleanupSubagentWorkspacesQuery: QueryHandler = async (args, projectDir) => {...}`:
       - Inline argv walk per cancel-bridge precedent (`workspace-parallel-cancel.ts:54-64`): three flags — `--cwd <path>` (default `projectDir`), `--phase <N>` (parse via `Number(args[++i])`), `--all-phases` (bare boolean toggle).
       - Mutual-exclusion + required-flag checks BEFORE any side effect — order: (a) `if (phase !== undefined && allPhases) return { data: { ok: false, reason: 'phase_and_all_phases_mutually_exclusive' } };` (b) `if (phase === undefined && !allPhases) return { data: { ok: false, reason: 'phase_or_all_phases_required' } };` (c) `if (phase !== undefined && (Number.isNaN(phase) || !Number.isInteger(phase) || phase < 0)) return { data: { ok: false, reason: 'invalid_phase_number' } };` (ASVS V5 — `--phase abc` parses as `NaN` per `Number('abc')`).
       - Phase-single dispatch: `if (phase !== undefined) { const result = cleanupSubagentWorkspaces(cwd, phase); return { data: result }; }`
       - `--all-phases` enumeration per D-05: `const workspacesDir = join(cwd, '.claude', 'jj-workspaces');` then `if (!existsSync(workspacesDir)) return { data: { abandoned: [], failedReaped: [] } };` (helper-equivalent idempotent no-op for missing dir) then `const entries = readdirSync(workspacesDir);` then `const phases = new Set<number>();` with `for (const e of entries) { const m = WORKSPACE_NAME_RE.exec(e); if (m) phases.add(Number(m[1])); }` then iterate phase set in ascending order (specifics §"Cross-phase merge in the bridge is order-preserving"): `const sortedPhases = [...phases].sort((a, b) => a - b);` then call `cleanupSubagentWorkspaces(cwd, p)` for each → merge per D-06: `const merged = { abandoned: [] as string[], failedReaped: [] as string[] };` with `for (const p of sortedPhases) { const r = cleanupSubagentWorkspaces(cwd, p); merged.abandoned.push(...r.abandoned); merged.failedReaped.push(...r.failedReaped); }`.
       - Return `{ data: merged }` (or for single-phase: `{ data: result }`). Both shapes are `{abandoned, failedReaped}` so consumers see uniform envelope per D-06.
       - Idempotency: re-invoking `--all-phases` after a successful cleanup returns `{abandoned: [], failedReaped: []}` from every per-phase helper call (helper's contract per D-03/D-06 of Phase 15); merged envelope is also all-empty.

    Register the new verb at all THREE sites per CF-02 / Pitfall 6 / PATTERNS §"sdk/src/query/command-*":

    Site 1 — `sdk/src/query/command-static-catalog-domain.ts`:
    - Add import after line 23 cancel-verb import line: `import { cleanupSubagentWorkspacesQuery } from './cleanup-subagent-workspaces.js';` Place adjacent to the cancel-verb import for visual locality.
    - Add ONE map entry to `DOMAIN_STATIC_CATALOG` adjacent to the cancel verb's entries at :78-79. Single-word-with-hyphens verb → ONE entry (NOT a dot+space alias pair like cancel's). Mirror the `worktree.cleanup-wave`-style single-entry precedent at `:67-68`. Entry: `['cleanup-subagent-workspaces', cleanupSubagentWorkspacesQuery],` with a comment line ABOVE: `// Phase 16.02 (CLEANUP-02): cleanup verb — orphan jj-workspace dir reap.`

    Site 2 — `sdk/src/query/command-manifest.non-family.ts`:
    - Add ONE manifest entry adjacent to the cancel entry at `:63`. Entry: `{ canonical: 'cleanup-subagent-workspaces', aliases: [], mutation: true,  outputMode: 'json' },` — `mutation: true` because the verb mutates filesystem state (`rmSync` workspace dirs); `outputMode: 'json'` because envelope is structured `{abandoned, failedReaped}`; `aliases: []` because single-word-with-hyphens (no dot/space alias surface).

    Site 3 — `sdk/src/query/command-aliases.generated.ts`:
    - First inspect lines 1-10 to find the source-of-generation comment per PATTERNS line 702 + Pitfall 6 / RESEARCH §Open Q1 recommendation. If a regenerator command exists (e.g., `pnpm run generate:aliases` or similar): run it; otherwise hand-edit.
    - Either way, file MUST end with this entry adjacent to the cancel entry at `:156`: `{ canonical: 'cleanup-subagent-workspaces', aliases: [], mutation: true },`

    Create `tests/cli-cleanup-subagent-workspaces.test.cjs` mirroring `tests/cli-workspace-parallel-cancel.test.cjs` shape:

    1. Imports + `runQuery` helper — VERBATIM from `tests/cli-workspace-parallel-cancel.test.cjs:31-47` (only the SDK_BIN path constant + the runQuery wrapper; do not import any cancel-specific helpers).
    2. Tests (mirror cancel test's 3-test structure + add mutually-exclusive-flag tests):
       - `test('CLI smoke: bridge without --phase or --all-phases returns {ok:false, reason:phase_or_all_phases_required}', () => {...})` — `runQuery(['cleanup-subagent-workspaces'])`; assert `r.status === 0`; parse stdout as JSON; assert `parsed.ok === false && parsed.reason === 'phase_or_all_phases_required'`.
       - `test('CLI smoke: --phase + --all-phases together returns {ok:false, reason:phase_and_all_phases_mutually_exclusive}', () => {...})` — `runQuery(['cleanup-subagent-workspaces', '--phase', '16', '--all-phases'])`; assert `parsed.reason === 'phase_and_all_phases_mutually_exclusive'`.
       - `test('CLI smoke: --phase non-numeric returns {ok:false, reason:invalid_phase_number}', () => {...})` — `runQuery(['cleanup-subagent-workspaces', '--phase', 'abc'])`; assert `parsed.reason === 'invalid_phase_number'`.
       - `test('CLI smoke: bogus verb cleanup-subagent-workspaces-XYZ returns unknown-verb error (negative control)', () => {...})` — mirror the cancel test's bogus-verb test; assert either non-zero `r.status` OR an `unknown verb` substring on `r.stderr`. Proves the three-site lookup is functional — if any registration site were skipped, the legitimate verb name would ALSO return unknown-verb and the positive tests would fail.
    3. Each test wraps `spawnSync` with `{encoding: 'utf-8', timeout: 15000}` per the cancel test's precedent.

    Anti-patterns to AVOID (CRITICAL per PATTERNS + CONTEXT):
    - Do NOT skip ANY of the three registration sites — Pitfall 6 / Phase 11 plan 02 documented this exact runtime failure mode.
    - Do NOT use `process.exit(1)` for error reporting — return structured `{ok:false, reason: '<snake>'}` envelope per PATTERNS line 383.
    - Do NOT call the helper inline (inlining `rmSync` + `jj workspace forget`) — always import from `'../vcs/jj/workspace-cleanup.js'` per Pitfall 11 single-owner mitigation.
    - Do NOT register under `workspace.parallel.*` namespace — the verb is NOT a `VcsAdapter` method; it's a phase-level cleanup helper bridged for bash. Mirror `worktree.cleanup-wave` (single-word with hyphens, no dot-namespace).
    - Do NOT exercise the bridge against a real `.claude/jj-workspaces/` tree in the smoke test — smoke is verb-resolution + argv-validation only. Behavior is tested by Task 3 (cross-backend vitest test) and Task 4 (dogfood-restore integration test).
    - Do NOT add a `--help` test — cancel test omits per RESEARCH N4 (gsd-sdk does not implement `--help`).
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && pnpm --filter ./sdk run build && node --test tests/cli-cleanup-subagent-workspaces.test.cjs && grep -q "cleanupSubagentWorkspacesQuery" sdk/src/query/command-static-catalog-domain.ts && grep -q "cleanup-subagent-workspaces" sdk/src/query/command-manifest.non-family.ts && grep -q "cleanup-subagent-workspaces" sdk/src/query/command-aliases.generated.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `sdk/src/query/cleanup-subagent-workspaces.ts` exists; exports `cleanupSubagentWorkspacesQuery: QueryHandler`; imports `cleanupSubagentWorkspaces` from `'../vcs/jj/workspace-cleanup.js'`
    - The bridge implementation contains `WORKSPACE_NAME_RE = /^phase-(\d+)-subagent-\d+$/` (anchored regex per ASVS V12 / T-16.02-04)
    - The bridge does NOT call `rmSync` directly anywhere (single-owner per Pitfall 11 — only the helper does the actual teardown)
    - The bridge's mutual-exclusion check returns `{ok: false, reason: 'phase_and_all_phases_mutually_exclusive'}` BEFORE any filesystem read (verifiable via test scenario 2 — no `.claude/jj-workspaces/` access needed for the error to fire)
    - `sdk/src/query/command-static-catalog-domain.ts` contains import line `import { cleanupSubagentWorkspacesQuery } from './cleanup-subagent-workspaces.js';` AND map entry `['cleanup-subagent-workspaces', cleanupSubagentWorkspacesQuery]` (verifiable via `grep -c cleanupSubagentWorkspacesQuery sdk/src/query/command-static-catalog-domain.ts` returning ≥ 2)
    - `sdk/src/query/command-manifest.non-family.ts` contains entry with `canonical: 'cleanup-subagent-workspaces'` (verifiable via grep)
    - `sdk/src/query/command-aliases.generated.ts` contains entry with `canonical: 'cleanup-subagent-workspaces'` (verifiable via grep)
    - `pnpm --filter ./sdk run build` exits 0 (TypeScript compilation succeeds for the new bridge)
    - `node --test tests/cli-cleanup-subagent-workspaces.test.cjs` passes all 4 test scenarios (no `# fail`, no `# skip`)
    - The CLI smoke test file uses `node:test` + `node:assert/strict` (NOT vitest — `tests/` convention)
    - Test for bogus-verb negative control confirms the lookup mechanism is functional (if it ALSO returns "unknown verb" for the legitimate verb name, the positive tests would fail — they don't, so registration is complete)
  </acceptance_criteria>
  <done>CLI bridge file exists; verb registered at all three sites; TypeScript compiles; CLI smoke test passes 4 scenarios proving argv validation + verb resolution + mutual-exclusion + invalid-phase guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire fanIn clean-path TS-direct call + inline AP-5 comment at conflicted branch + D-17 helper docblock cross-reference + flip the workspaces-reaped assertion in jj test + add D-15 git regression test</name>
  <files>sdk/src/vcs/jj/parallel.ts, sdk/src/vcs/jj/workspace-cleanup.ts, sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts, sdk/src/vcs/__tests__/cmd-parallel-git.test.ts</files>
  <read_first>
    - sdk/src/vcs/jj/parallel.ts (full 574-line file — focus on lines 289-510 for `performJjParallelFanIn` function; lines 67 for the existing import; lines 389-461 for the conditional whose ELSE branch needs the new call; lines 557-561 for the intra-file self-precedent of the direct call shape)
    - sdk/src/vcs/jj/workspace-cleanup.ts (full 202-line file — locked helper docblock at :96-133; add a one-line D-17 cross-reference at the docblock pointing to `.planning/phases/16-workflow-invariant-tooling/16-CONTEXT.md` as the "consumer-completion record")
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (full file — focus on lines 90-215 for the existing N=2/3/4 clean-path describe; lines 197-201 for the assertion that needs FLIPPING; lines 225+ for the conflicted-branch describe at :225 to add the inverse-assertion to)
    - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts (full file — find the clean-path describe; add a sibling `it` block asserting the git-cell `worktree remove --force` already produces post-clean-fanIn `existsSync(ws.path) === false`)
    - sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts (lines 94, 118-121 — the post-cancel assertion shape `expect(existsSync(ws.path)).toBe(false)` that Task mirrors)
    - .planning/phases/16-workflow-invariant-tooling/16-PATTERNS.md lines 487-781 (Pattern Assignments for `sdk/src/vcs/jj/parallel.ts` MODIFIED + the two test augment sections)
  </read_first>
  <behavior>
    - After modification, `performJjParallelFanIn` clean-path `else` branch calls `cleanupSubagentWorkspaces(mainRepoRoot, handle.phaseNumber, handle.workspaces)` after `merged.push(mergeChangeId)` at line 460
    - The `failedReaped` returned by the helper is merged into the function-local `failedReaped: string[]` array (variable rename `cleanupFailedReaped` to avoid shadowing; per PATTERNS line 530)
    - Conflicted branch (`if (conflicted) {...}` at lines 394-410) is functionally UNCHANGED — ONLY an inline comment is added documenting the intentional cleanup-omission per ROADMAP SC4 + CF-03 + AP-5
    - vitest test `cmd-parallel-jj.test.ts` clean-path `it` block at lines 126-212: assertion at line 201 FLIPS from `expect(remainingWorkspaces.length).toBe(handle.workspaces.length)` to `expect(remainingWorkspaces.length).toBe(0)`; the preceding comment block at :185-196 is updated to reflect "wired" state (the "(cleanup wiring belongs to downstream Phase 11 plans)" parenthetical removed). Add a SECOND assertion using the cancel-test shape: `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(false); }` (mirrors `cmd-parallel-cancel-jj.test.ts:118-121`)
    - vitest test `cmd-parallel-jj.test.ts` conflicted-branch describe at :225: add an inverse assertion proving workspaces are PRESERVED on the conflicted branch — `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(true); }` (CF-03 / AP-5 lock-in regression guard)
    - vitest test `cmd-parallel-git.test.ts`: add a sibling `it` block to the clean-path describe asserting `existsSync(ws.path) === false` post-clean-fanIn (git-cell regression guard — git's `worktree remove --force` already produces this invariant; the test prevents future regressions if anyone touches the git fanIn path)
    - The locked helper's docblock at `sdk/src/vcs/jj/workspace-cleanup.ts:96-133` gets ONE NEW LINE per D-17: a cross-reference pointing to `.planning/phases/16-workflow-invariant-tooling/16-CONTEXT.md` as the "consumer-completion record" (the three consumers are now all wired)
  </behavior>
  <action>
    Modify `sdk/src/vcs/jj/parallel.ts` — TWO surgical edits (clean-path direct call + conflicted-branch inline comment):

    Edit 1 (clean-path `else` branch — new direct call) at the location IMMEDIATELY after `merged.push(mergeChangeId);` on line 460. Insert the call site per CONTEXT D-07 + PATTERNS §"sdk/src/vcs/jj/parallel.ts" lines 487-530:
    - Insert a comment block: `// CLEANUP-02 / Phase 16.02 (D-07): tear down materialized subagent` `// workspaces on the clean path. Handle.workspaces is the authoritative` `// source-of-truth (Pitfall 4 mitigation per Phase 15 N1 — custom` `// workspacePath overrides MUST be honored). Direct TS-side call (NOT via` `// the CLI bridge — bridge is for bash consumers only per D-07).`
    - Insert the helper call destructured to a renamed local to avoid shadowing the outer `failedReaped`: `const { failedReaped: cleanupFailedReaped } = cleanupSubagentWorkspaces(mainRepoRoot, handle.phaseNumber, handle.workspaces);`
    - Insert the merge: `for (const name of cleanupFailedReaped) failedReaped.push(name);`
    - Note: do NOT destructure `abandoned` from the helper — the merged FanInResult envelope has no field for it (different shape from the CancelResult envelope at the cancel call site at :557-561; cancel returns abandoned + failedReaped + surplusBookmarks + surplusWorkspaces; fanIn returns failedReaped only).

    Edit 2 (conflicted-branch inline comment) — INSIDE the `if (conflicted) {...}` block at lines 394-410. Insert AT THE TOP of the block body (after the opening `{`, before `// W3 (a):` at line 395 OR adjacent to the existing W3 (a) comment). The new comment per CONTEXT CF-03 + PATTERNS line 532-544 + ROADMAP SC4: `// W3 (a) / CF-03 / AP-5 (Phase 16.02 lock-in): workspaces are PRESERVED` `// on disk here for human inspection of the conflicted state. NO call to` `// cleanupSubagentWorkspaces — the joint-assertion contract (ROADMAP SC4)` `// makes this divergence from the clean-path branch load-bearing. DO NOT` `// add reap here; the cleanup-omission is intentional and tested by the` `// inverse assertion in cmd-parallel-jj.test.ts conflicted describe.`
    - NO functional changes to the conflicted branch (NO `rmSync`, NO `cleanupSubagentWorkspaces` call, NO body modification). Comment-only edit.

    Modify `sdk/src/vcs/jj/workspace-cleanup.ts` — ONE surgical edit per D-17 at the docblock at lines 96-133:
    - Add ONE NEW LINE to the docblock (preserves existing JSDoc structure). The line goes at the end of the description block, before the `@param` lines start (around line 125 — find the natural place near the existing consumer enumeration). The new line: `* Consumer-completion record (Phase 16.02): see .planning/phases/16-workflow-invariant-tooling/16-CONTEXT.md §"Cleanup-Contract Documentation Site" — all 3 consumers (cancel verb, fanIn clean-path, dogfood-restore.sh) are wired post-Phase-16.`

    Modify `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` — TWO edits (clean-path assertion flip + conflicted-branch inverse assertion):

    Edit 1 (clean-path scenario at lines 126-212):
    - Find the assertion at lines 197-201: `const remainingWorkspaces = vcs.workspace.list().filter(...); expect(remainingWorkspaces.length).toBe(handle.workspaces.length);`
    - FLIP the expected value: `expect(remainingWorkspaces.length).toBe(0);` (workspaces are now reaped on clean fanIn per CLEANUP-02 D-07)
    - UPDATE the preceding comment block at lines 185-196: remove the parenthetical "(cleanup wiring belongs to downstream Phase 11 plans)" — replace with "(cleanup is wired by Phase 16.02 CLEANUP-02 — clean fanIn reaps all dispatched workspaces via the locked helper)."
    - ADD a second assertion using the cancel-test shape AFTER the flipped `expect(...).toBe(0)` line: `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(false); }` with a comment `// CLEANUP-02 D-15: every dispatched workspace dir is gone post-clean-fanIn (mirror cmd-parallel-cancel-jj.test.ts:118-121 shape).`

    Edit 2 (conflicted-branch describe at line 225+):
    - Inside the existing `describe.sequential.skipIf(!jjAvailable)(...workspace.parallel.fanIn — N=2 in-tree-conflict...)` `it` block (around lines 248-330), AFTER the existing `expect(result.conflicted).toBe(true)` + queue-entry checks, ADD a new assertion: `// CLEANUP-02 D-15 / CF-03 / AP-5 (W3 (a) joint-assertion lock-in):` `// conflicted-branch workspaces MUST persist on disk for human inspection.` `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(true); }`
    - Add a brief comment above the new for-loop explaining WHY this is the inverse polarity of the clean-path assertion.

    Modify `sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` — ONE edit (git regression test per D-15):
    - Identify the existing git-side clean-path describe (mirror the jj-side structure — for-loop over N=2/3/4 dispatching git worktree adapter; check for existing `vcs.workspace.parallel.fanIn` call pattern).
    - INSIDE the clean-path `it` block (or add a NEW `it` block at the same describe nesting level), AFTER the clean-fanIn result is returned, add the same shape assertion: `// CLEANUP-02 D-15 git-cell regression guard: git's existing` `// worktree remove --force already produces this invariant; this test prevents` `// future regressions if the git fanIn path is touched. Helper is jj-specific` `// per Phase 15 deferred-ideas — git's worktree-remove is the equivalent.` `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(false); }`
    - Do NOT call `cleanupSubagentWorkspaces` on the git side — the helper is jj-specific (located at `sdk/src/vcs/jj/workspace-cleanup.ts`; importing it from a git-side code path would violate the backend-isolation invariant). Git's existing `worktree remove --force` in `sdk/src/vcs/git/parallel.ts` is the equivalent (Phase 15 deferred-ideas: "Promote `cleanupSubagentWorkspaces` to a cross-backend helper — defer until git-side orphan-dirs become a problem").

    Anti-patterns to AVOID (CRITICAL):
    - Do NOT add ANY `rmSync` or `cleanupSubagentWorkspaces` call inside `if (conflicted) {...}` in `parallel.ts`. This violates the W3 (a) joint-assertion contract — workspaces MUST persist on disk for human inspection.
    - Do NOT inline a raw `rmSync` / `jj workspace forget` body anywhere — always call the helper (Pitfall 11 single-owner).
    - Do NOT call the helper via the CLI bridge from TypeScript — the bridge is for bash; direct call here is cheaper and avoids the language hop (D-07).
    - Do NOT change the helper's signature (CF-01 locked) — only the docblock gets the new D-17 cross-reference line.
    - Do NOT add a retry config or skip modifier on any new vitest `it`/`describe` block (per `cmd-parallel-cancel-jj.test.ts:5-10` D-15 structural rules).
    - Do NOT create a separate per-test mkdtemp in the augment tests (the existing describe already owns one via `beforeAll`).
    - Do NOT add the no-orphan assertion to the conflicted scenario without inverting polarity to `.toBe(true)` — Pitfall 2 (AP-5) means conflicted workspaces MUST persist on disk.
    - Do NOT call `cleanupSubagentWorkspaces` on the git side — the helper is jj-specific.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && pnpm --filter ./sdk run build && pnpm --filter ./sdk test -- src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts && grep -A 2 "if (conflicted)" sdk/src/vcs/jj/parallel.ts | grep -q "AP-5\|W3 (a)\|CF-03" && grep -A 2 "merged.push(mergeChangeId)" sdk/src/vcs/jj/parallel.ts | grep -q "cleanupSubagentWorkspaces" && grep -q "Consumer-completion record" sdk/src/vcs/jj/workspace-cleanup.ts</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/vcs/jj/parallel.ts` clean-path `else` branch (lines around :411-461) contains a call to `cleanupSubagentWorkspaces(mainRepoRoot, handle.phaseNumber, handle.workspaces)` AFTER `merged.push(mergeChangeId)` at :460 (verifiable: `grep -n "cleanupSubagentWorkspaces" sdk/src/vcs/jj/parallel.ts` returns ≥ 3 hits — 1 import at :67 + 1 existing cancel call at :557 + 1 new fanIn-clean call after :460)
    - `sdk/src/vcs/jj/parallel.ts` clean-path new call uses the renamed local `cleanupFailedReaped` to avoid shadowing the outer `failedReaped` variable (verifiable via grep `cleanupFailedReaped` returning ≥ 1 hit)
    - `sdk/src/vcs/jj/parallel.ts` conflicted branch at lines 394-410 contains the new inline comment referencing `CF-03`, `AP-5`, AND `W3 (a)` (verifiable: `grep -A 8 "if (conflicted)" sdk/src/vcs/jj/parallel.ts` shows comment block containing those three tokens)
    - `sdk/src/vcs/jj/parallel.ts` conflicted branch is functionally UNCHANGED (NO `rmSync` call, NO `cleanupSubagentWorkspaces` call — verifiable: extracting the conflicted-branch body and grepping for `cleanupSubagentWorkspaces` returns 0 hits)
    - `sdk/src/vcs/jj/workspace-cleanup.ts` docblock contains the literal `Consumer-completion record (Phase 16.02)` (D-17 cross-reference; verifiable via grep)
    - `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` line ~201 assertion is FLIPPED to `expect(remainingWorkspaces.length).toBe(0)` (NOT `.toBe(handle.workspaces.length)`)
    - `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` clean-path scenario contains the NEW assertion `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(false); }` (CLEANUP-02 D-15 — mirrors cancel-test shape)
    - `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` conflicted-branch describe contains the inverse assertion `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(true); }` (CF-03 / AP-5 lock-in regression guard)
    - `sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` clean-path describe contains an `it` block asserting `for (const ws of handle.workspaces) { expect(existsSync(ws.path)).toBe(false); }` post-clean-fanIn (git regression guard per D-15)
    - `sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` does NOT contain any call to `cleanupSubagentWorkspaces` (helper is jj-specific per Phase 15 deferred-ideas)
    - `pnpm --filter ./sdk run build` exits 0 (TypeScript compilation succeeds after the surgical edits)
    - `pnpm --filter ./sdk test -- src/vcs/__tests__/cmd-parallel-jj.test.ts` passes — including the flipped clean-path assertion AND the new conflicted-branch inverse assertion
    - `pnpm --filter ./sdk test -- src/vcs/__tests__/cmd-parallel-git.test.ts` passes — including the new git regression test
  </acceptance_criteria>
  <done>Clean-path fanIn reaps workspaces via the helper; conflicted branch preserves workspaces with inline AP-5 comment; jj test assertion flipped + 2 new shape assertions added; git regression test added; helper docblock cross-references Phase 16 CONTEXT.md.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire dogfood-restore.sh post-restore cleanup step + ship bash-driven integration test seeding 2 phase numbers</name>
  <files>scripts/dogfood-restore.sh, tests/scripts/dogfood-restore-orphan-cleanup.test.cjs</files>
  <read_first>
    - scripts/dogfood-restore.sh (full 61-line file — the existing `set -euo pipefail` discipline at :29; the existing tail at :55-61 where the new step appends per D-11)
    - scripts/dogfood-rehearse.sh line 131 (the restore invocation under `set -e`; D-12 WARN trap keeps this harness green when the SDK is mid-rebuild)
    - tests/scripts/audit-workflow-raw-git.test.cjs (full file — node:test convention for `tests/scripts/`; Pattern B mkdtemp pattern; bash-script-invoking integration test analog)
    - sdk/src/vcs/jj/workspace-cleanup.ts (re-read for the `--all-phases` enumeration regex; confirm `phase-${padded}-subagent-${idx}` canonical format)
    - sdk/src/query/cleanup-subagent-workspaces.ts (the bridge created in Task 1 — confirms how `gsd-sdk query cleanup-subagent-workspaces --all-phases` returns the merged JSON envelope)
    - .planning/phases/16-workflow-invariant-tooling/16-PATTERNS.md lines 450-596 (Pattern Assignments for `tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` + `scripts/dogfood-restore.sh`)
  </read_first>
  <behavior>
    - After modification, `scripts/dogfood-restore.sh` line count grows from 61 to ~70 (adds ~7-10 lines for the cleanup step + WARN trap + second diagnostic per D-11/D-12/D-13)
    - The new cleanup step lands as the LAST step in the script — AFTER the existing `tar -xf "$TARBALL_PATH" -C .` at :59 AND AFTER the existing "complete" diagnostic echo at :61
    - The cleanup step traps the bridge call with `|| { echo "WARN: orphan cleanup failed" >&2; echo '{"abandoned":[],"failedReaped":[]}'; }` per D-12 — non-zero exit from the bridge does NOT halt the script (set -euo pipefail at :29 stays in effect; the trap pattern is the escape hatch)
    - A second diagnostic echo fires AFTER the cleanup with format `dogfood-restore: orphan-workspace cleanup complete (abandoned=$N, failedReaped=$M)` per D-13 (counts via `jq -r '.abandoned | length'` / `'.failedReaped | length'` on the merged envelope)
    - Integration test `tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` seeds 2 distinct phase numbers (e.g., `phase-15-subagent-1`, `phase-16-subagent-2`) in `.claude/jj-workspaces/`, runs the production `scripts/dogfood-restore.sh` against a synthetic fixture, and asserts BOTH workspace dirs are gone after the script completes (proves `--all-phases` enumeration crosses phase boundaries per D-16)
    - Integration test exits 0 even if the cleanup-bridge call returns a WARN (D-12 — recovery hygiene is non-blocking; restore success is what matters)
  </behavior>
  <action>
    Modify `scripts/dogfood-restore.sh` per CONTEXT D-11/D-12/D-13 + PATTERNS §"scripts/dogfood-restore.sh":

    Append AT THE END (after the final `echo "dogfood-restore: complete. Verify with: ..." >&2` at line 61):

    Block to add (verbatim text, indentation matches the existing shell style — no leading tabs since the existing script uses 0-indent shell):

    ```
    # Phase 16.02 (CLEANUP-02 / D-11/D-12/D-13): post-restore orphan-workspace
    # cleanup. Idempotent — re-invoking on a clean tree returns
    # {abandoned:[], failedReaped:[]}. Trap with WARN so a cleanup-only miss
    # does NOT flag "restore failed" (op-restore + tar both succeeded).
    CLEANUP_JSON=$(gsd-sdk query cleanup-subagent-workspaces --all-phases 2>&1 \
      || { echo "WARN: orphan cleanup failed" >&2; echo '{"abandoned":[],"failedReaped":[]}'; })
    ABANDONED_COUNT=$(echo "$CLEANUP_JSON" | jq -r '.abandoned | length' 2>/dev/null || echo "?")
    FAILED_COUNT=$(echo "$CLEANUP_JSON" | jq -r '.failedReaped | length' 2>/dev/null || echo "?")
    echo "dogfood-restore: orphan-workspace cleanup complete (abandoned=${ABANDONED_COUNT}, failedReaped=${FAILED_COUNT})" >&2
    ```

    Key invariants preserved:
    - `set -euo pipefail` at :29 stays unchanged — the `|| {...}` trap on the cleanup invocation is the escape hatch that allows continuation past a non-zero exit from the bridge without breaking the `-e` discipline (D-12).
    - stderr-only diagnostics (`>&2`) — matches every existing `echo` in the script.
    - NO file writes into the colocated jj working tree (`feedback_avoid_jj_auto_tracked_output`) — output stays in shell variables + stderr; `jq` parses an in-memory variable, NOT a file.
    - The new step is the LAST step in the script — preserves Pitfall 2 ordering invariant (jj op restore → tar -xf with tar last; cleanup is appended downstream so it physically cannot perturb the tar-last invariant). Coexists cleanly with Plan 18.02 future WR-01 precondition (`[ -f .planning/STATE.md ]` before tar — cleanup is well downstream of any addition between op-restore and tar).

    Create `tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` per CONTEXT D-16 + PATTERNS §"tests/scripts/dogfood-restore-orphan-cleanup.test.cjs":

    1. Imports — `'use strict';` + `const test = require('node:test');` + `const assert = require('node:assert/strict');` + `const path = require('node:path');` + `const { mkdtempSync, mkdirSync, writeFileSync, rmSync, existsSync } = require('node:fs');` + `const { tmpdir } = require('node:os');` + `const { spawnSync, execSync } = require('node:child_process');`
    2. Helper `seedWorkspace(root, phaseNum, idx)`: creates a directory at `root/.claude/jj-workspaces/phase-${String(phaseNum).padStart(2,'0')}-subagent-${idx}/`, writes a marker file `marker.txt` containing the string `orphan`. Returns the absolute path to the created workspace dir.
    3. Helper `seedJjRepo(root)`: invokes `execSync('jj git init --colocate', { cwd: root })` to materialize a colocated jj repo (so the production `jj op restore` invocation inside dogfood-restore.sh succeeds against a real jj repo). Synthesize a placeholder `.planning/STATE.md` file (so Plan 18.02's future `[ -f .planning/STATE.md ]` precondition is not retroactively violated).
    4. Helper `seedFakeTarball(root)`: creates a minimal tarball at `${tmpdir}/dogfood-test-N.tar` containing an empty `.planning/` directory; returns the tarball absolute path. The script's `tar -xf` step will overlay this on top of the synthetic repo without disrupting the orphan-dir seeds (which live under `.claude/`, not `.planning/`).
    5. Helper `currentJjOpId(root)`: returns the output of `execSync('jj op log --no-graph -T "id ++ \\"\\n\\"" --limit 1', { cwd: root })` — the latest op-id to feed to dogfood-restore.sh as the restore target. Trim whitespace before returning.
    6. Test: `test('D-16: dogfood-restore.sh reaps orphan workspace dirs from 2 distinct phases via --all-phases', () => {...})` body:
       - `const fixDir = mkdtempSync(path.join(tmpdir(), '__dogfood-restore-cleanup-'));`
       - `try {` + `seedJjRepo(fixDir);` + `const ws15 = seedWorkspace(fixDir, 15, 1);` + `const ws16 = seedWorkspace(fixDir, 16, 2);` + `assert.ok(existsSync(ws15), 'seed: ws15 dir must exist before script run');` + `assert.ok(existsSync(ws16), 'seed: ws16 dir must exist before script run');` + `const opId = currentJjOpId(fixDir);` + `const tarball = seedFakeTarball(fixDir);` + `const restoreScript = path.resolve(__dirname, '..', '..', 'scripts', 'dogfood-restore.sh');` + `const r = spawnSync('bash', [restoreScript, opId, tarball], { cwd: fixDir, encoding: 'utf-8', timeout: 60000 });`
       - Assert `r.status === 0` (D-12 — exit 0 even if cleanup-bridge fails). Include `r.stderr` in the assertion message for diagnostic clarity.
       - Assert `!existsSync(ws15)` AND `!existsSync(ws16)` — BOTH workspace dirs reaped via `--all-phases` enumeration (proves D-05/D-06 cross-phase enumeration is correct).
       - Assert `r.stderr.includes('orphan-workspace cleanup complete')` — proves D-13 second diagnostic fires.
       - Assert `r.stderr.includes('dogfood-restore: complete')` — proves the existing "complete" echo fires BEFORE the new cleanup step (Pitfall 2 ordering invariant preserved).
       - `} finally { rmSync(fixDir, { recursive: true, force: true }); }` (idempotent cleanup; never leaves test artifacts in tmpdir per `feedback_avoid_jj_auto_tracked_output`)
    7. Skip-gate at top of file: `if (!commandExists('jj')) { test('skip: jj not installed', () => {}); return; }` — mirrors how other jj-dependent tests skip when the binary is unavailable (use `which jj` via `spawnSync` and check exit code; or scrape `process.env.PATH` for `jj` via `node:fs`). If skip is implemented via a `t.skip()` call inside the `test()` body, that's also acceptable. The test MUST NOT hard-fail on a system without jj installed.

    Anti-patterns to AVOID:
    - Do NOT insert the cleanup BETWEEN `jj op restore` and `tar -xf` in `dogfood-restore.sh` — violates Pitfall 2 ordering invariant (RESEARCH §Pitfall 5).
    - Do NOT remove the `|| {...}` trap — `set -euo pipefail` would halt the script on a non-zero exit from the bridge, misleadingly signaling "restore failed" (D-12 / RESEARCH §Pitfall 5).
    - Do NOT use `git` anywhere in this script — `feedback_avoid_jj_auto_tracked_output` + `project_no_raw_git`.
    - Do NOT write a JSON file under `.planning/` or `.claude/` in the test — output stays in-process per `feedback_avoid_jj_auto_tracked_output`.
    - Do NOT skip the second phase seeding — single-phase seeds would not exercise the cross-phase enumeration code path (D-05/D-06 — that's the WHOLE POINT of the test per D-16).
    - Do NOT write the test under `sdk/src/__tests__/` — bash-driven integration belongs under `tests/scripts/` (the `tests/` → `node:test` bifurcation per RESEARCH Standard Stack line 114).
    - Do NOT skip on `set -euo pipefail` failures — the test MUST assert the script's exit semantics (D-12: trap+WARN means exit 0 even on cleanup-fail).
    - Do NOT skip the jq-availability gate — if `jq` isn't installed, dogfood-restore.sh's `ABANDONED_COUNT=$(echo ... | jq ...)` fails over to `echo "?"` per the `|| echo "?"` fallback — the test's second-diagnostic assertion still passes because `"orphan-workspace cleanup complete (abandoned=?, failedReaped=?)"` contains `"orphan-workspace cleanup complete"`. The test does NOT require jq for correctness, only for nicer-looking diagnostics.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && bash -n scripts/dogfood-restore.sh && grep -q "cleanup-subagent-workspaces --all-phases" scripts/dogfood-restore.sh && grep -q "WARN: orphan cleanup failed" scripts/dogfood-restore.sh && grep -q "orphan-workspace cleanup complete" scripts/dogfood-restore.sh && node --test tests/scripts/dogfood-restore-orphan-cleanup.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/dogfood-restore.sh` exits 0 (syntactically valid bash after the append)
    - `scripts/dogfood-restore.sh` contains the literal `gsd-sdk query cleanup-subagent-workspaces --all-phases` (proves D-04 `--all-phases` mode is used; D-12 WARN trap is around the call)
    - `scripts/dogfood-restore.sh` contains the literal `WARN: orphan cleanup failed` (proves D-12 trap exists)
    - `scripts/dogfood-restore.sh` contains the literal `orphan-workspace cleanup complete` (proves D-13 second diagnostic exists)
    - `scripts/dogfood-restore.sh` does NOT contain `git ` followed by a verb (per `project_no_raw_git`; verifiable via `grep -nE '(^|[ \t;&|(])git[ \t]+[a-zA-Z]' scripts/dogfood-restore.sh` returning 0 hits)
    - The new cleanup block is positioned AFTER the existing `tar -xf` line (verifiable: `awk '/tar -xf/{tar=NR} /cleanup-subagent-workspaces/{cleanup=NR; if(cleanup > tar) print "OK"; else print "FAIL"}' scripts/dogfood-restore.sh` prints `OK`)
    - File `tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` exists; uses `node:test` framework (NOT vitest); contains the literal `seedWorkspace` helper and seeds at least 2 distinct phase numbers (`phase-15-` AND `phase-16-` per D-16; verifiable via grep)
    - `node --test tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` passes the D-16 test scenario (no `# fail`; `# skip` is allowed if jj is not installed)
    - The integration test asserts BOTH `!existsSync(ws15)` AND `!existsSync(ws16)` post-script (proves cross-phase enumeration; verifiable via grep on the test source for `!existsSync(ws15)` AND `!existsSync(ws16)`)
    - The integration test calls `rmSync(fixDir, ...)` inside a `finally` block (per `feedback_avoid_jj_auto_tracked_output` — test artifacts never persist in tmpdir or repo)
    - The integration test uses `spawnSync('bash', [restoreScript, opId, tarball], { cwd: fixDir, ... })` — invokes the production script directly with the script's positional args (NOT a re-implementation of the script body)
  </acceptance_criteria>
  <done>dogfood-restore.sh appends the post-restore cleanup step with WARN trap + second diagnostic; bash-driven integration test seeds 2 phase numbers and asserts both reaped; script exit semantics (D-12) preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash → CLI bridge via `gsd-sdk query` | `dogfood-restore.sh` invokes the bridge as a subprocess; bridge receives operator-controlled `--phase` / `--all-phases` flags. Untrusted input on `--phase` value if invoked outside the script. |
| CLI bridge → filesystem walker | `--all-phases` enumeration reads `.claude/jj-workspaces/` directory entries. Crafted directory names (e.g., `phase-99999999999-subagent-...`, `../../etc/passwd`, symlinks) could trigger pathological behavior. |
| CLI bridge → helper sidecar | Bridge invokes `cleanupSubagentWorkspaces` which itself runs `jj workspace forget` + `rmSync`. Path of the workspace dir is derived from `readdirSync` of `.claude/jj-workspaces/` — bounded but worth defending. |
| Test fixture → production script | Integration test invokes the production `scripts/dogfood-restore.sh` against a tmpdir fixture. Tmpdir path traversal risk if the fixture's tarball or op-id is crafted maliciously. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16.02-01 | Tampering (path injection) | `--all-phases` enumeration in `sdk/src/query/cleanup-subagent-workspaces.ts` | mitigate | Strict anchored regex `WORKSPACE_NAME_RE = /^phase-(\d+)-subagent-\d+$/` filters readdirSync output. Crafted directory names like `phase-99-subagent-../etc/passwd` fail the anchor (literal `/` not in the character class). Phase number extracted via `Number(m[1])` — only the canonical digit-group is parsed. ASVS V12 (file resource handling). |
| T-16.02-02 | Denial of Service | `--phase` NaN / negative / non-integer values | mitigate | Explicit guard: `if (Number.isNaN(phase) || !Number.isInteger(phase) || phase < 0) return { data: { ok: false, reason: 'invalid_phase_number' } };`. Mirrors the cancel-bridge's required-flag pattern + adds an ASVS V5 input-validation gate. Prevents `--phase abc` from triggering `String(NaN).padStart(2, '0')` → `'NaN'` → no-match regex → silent no-op (worse than fail-loud). |
| T-16.02-03 | Tampering | `cleanupSubagentWorkspaces` helper invocation with crafted workspace name from `--all-phases` | mitigate | Helper has its own anchored regex defense at `workspace-cleanup.ts:154` (`new RegExp(\`^phase-${phaseTag}-subagent-\\d+$\`)`). Bridge passes only the phase NUMBER (a parsed integer) to the helper, which constructs the regex internally — no path string crosses the bridge → helper boundary. Defense in depth. |
| T-16.02-04 | Tampering / Information Disclosure | Symlink within `.claude/jj-workspaces/` could escape repo root during rmSync | accept (with rationale) | The helper uses `rmSync(ws.path, { recursive: true, force: true })`. Node.js's `rmSync` follows symlinks by default for the LAST path component but does NOT follow symlinks WITHIN the recursive traversal (per Node.js docs and audit of `node_modules/.../rimraf-equivalent`). The threat scenario is bounded: an attacker who can already plant a symlink inside `.claude/jj-workspaces/` already has filesystem write access; the mitigation cost (gate every helper call with `lstatSync` symlink check) exceeds the realistic risk window for a jj-workspace dir owned by the operator's own dispatch loop. ASVS V12 risk accepted with documentation. |
| T-16.02-05 | Tampering (script injection) | `dogfood-restore.sh` CLEANUP_JSON command substitution evaluating shell metacharacters in JSON output | mitigate | The bridge returns JSON via stdout; `CLEANUP_JSON=$(gsd-sdk query ...)` captures it as a bash variable. The variable is then piped to `jq` for parsing — never `eval`'d or `bash -c`'d. JSON content (even crafted) cannot trigger shell injection through this code path. Verified: `jq -r '.abandoned | length'` returns a number; even malicious JSON content stays inside `jq`'s parser. Defense: stdin-pipe to jq + `2>/dev/null` swallow + `|| echo "?"` fallback. |
| T-16.02-06 | Denial of Service | dogfood-restore.sh hang if CLI bridge hangs indefinitely | accept (operator-driven recovery context) | The bridge is invoked from a recovery script — the operator is at the keyboard. If the bridge hangs (e.g., readdirSync on a stuck filesystem), the operator notices and Ctrl-C's. Adding a `timeout` wrapper would complicate the WARN-trap pattern (timeout exit codes vary by platform). Risk accepted; recovery context is human-supervised. |
| T-16.02-SC | Tampering (supply chain) | No new npm installs (zero new packages per RESEARCH §Package Legitimacy Audit) | N/A | No package install tasks in this plan. The Package Legitimacy Gate protocol does not apply. All dependencies are Node.js built-ins (`fs`, `path`), existing repo modules (`sdk/src/vcs/jj/workspace-cleanup.ts`), and CLI tools assumed available (`jj`, `jq`, `tar`). |
</threat_model>

<verification>
End-of-plan acceptance:
1. `pnpm --filter ./sdk run build` exits 0 after all surgical edits (TypeScript compiles)
2. `node --test tests/cli-cleanup-subagent-workspaces.test.cjs` passes all 4 scenarios (Task 1 verification)
3. `pnpm --filter ./sdk test -- src/vcs/__tests__/cmd-parallel-jj.test.ts` passes — including the flipped clean-path assertion + new conflicted-branch inverse assertion (Task 2 verification)
4. `pnpm --filter ./sdk test -- src/vcs/__tests__/cmd-parallel-git.test.ts` passes — including the new git regression test (Task 2 verification)
5. `bash -n scripts/dogfood-restore.sh` exits 0 (Task 3 — script is syntactically valid bash after the append)
6. `node --test tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` passes the D-16 test scenario (Task 3 verification)
7. `grep -c "cleanupSubagentWorkspaces" sdk/src/vcs/jj/parallel.ts` returns ≥ 3 (1 import + 1 existing cancel call + 1 NEW fanIn-clean call)
8. `grep -c "cleanup-subagent-workspaces" sdk/src/query/*.ts` returns ≥ 4 (1 new bridge file + 3 registration sites — catalog + manifest + aliases.generated)
9. `pnpm test` (full suite from repo root) green — no regressions in any other test
10. `grep -c "git " scripts/dogfood-restore.sh` returns 0 (no raw git in the modified script — `feedback_avoid_jj_auto_tracked_output`)
11. `grep -c "expires" sdk/src/query/cleanup-subagent-workspaces.ts` returns 0 (no allowlist semantics on the bridge — bridge has no allowlist surface)
12. Manual verification (per VALIDATION.md §Manual-Only): operator inspects `sdk/src/vcs/jj/parallel.ts` near `performJjParallelFanIn` conflicted-branch path — confirms inline comment documents "conflicted branch preserves workspaces for forensics per W3 (a) joint-assertion contract" (CF-03 / AP-5 documentation correctness — automatable via grep but reviewer confirms wording).
</verification>

<success_criteria>
ROADMAP SC3 (CLEANUP-02 success criteria, verbatim):
- After `vcs.workspace.parallel.fan-in` clean-path success branch on jj backend, no `.claude/jj-workspaces/phase-*-subagent-*` FS directories exist on disk
- Cross-backend test via `vcs-fixture.ts` Pattern B mkdtemp asserts post-fanIn `existsSync(ws.path) === false` for every workspace in the handle

ROADMAP SC4:
- Conflicted branch behavior UNCHANGED per AP-5 — `performJjParallelFanIn` conflicted branch preserves workspaces on disk for human inspection (W3 (a) joint-assertion contract); only the clean-path branch reaps; documented inline at the code site

ROADMAP SC5:
- `scripts/dogfood-restore.sh` includes an idempotent post-restore step that removes any surviving `.claude/jj-workspaces/phase-*-subagent-*` directories (consumes the same `cleanupSubagentWorkspaces` helper extracted by Phase 15.04); end-to-end test asserts the dirs are gone after a synthetic `jj op restore` + orphan-survival fixture

IP-5 Single-Owner Invariant:
- All three consumers (cancel verb, fanIn clean-path, dogfood-restore.sh) call `cleanupSubagentWorkspaces(...)` directly; no inline `rmSync` / `jj workspace forget` duplicate body anywhere
- Verifiable: grep across `sdk/src/`, `scripts/`, `tests/` for `rmSync(.*phase-.*subagent` returns ZERO hits (the helper is the only owner)

CF-02 Three-Site Registration:
- `cleanup-subagent-workspaces` verb resolves at runtime — registered at all three of `command-static-catalog-domain.ts`, `command-manifest.non-family.ts`, `command-aliases.generated.ts`
</success_criteria>

<output>
Create `.planning/phases/16-workflow-invariant-tooling/16-02-SUMMARY.md` after all 3 tasks complete and verification is green.
</output>
