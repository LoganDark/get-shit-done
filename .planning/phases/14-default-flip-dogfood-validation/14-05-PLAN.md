---
phase: 14-default-flip-dogfood-validation
plan: 05
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04]
files_modified:
  - scripts/dogfood-phase-14.sh
  - .planning/intel/v1.3-dogfood-metrics.md
  - .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md
autonomous: false
requirements: [DOGFOOD-01, DOGFOOD-02]
requirements_addressed: [DOGFOOD-01, DOGFOOD-02]
tags: [dogfood, metrics, pitfall-10, jj-cell, git-cell, recovery-anchor]
must_haves:
  truths:
    - "`scripts/dogfood-phase-14.sh` exists and runs to exit 0 against THIS repo with `parallelization: true` (Plan 01 flipped it)"
    - "Pre-snapshot dir exists at `mktemp -d -t gsd-dogfood-pre-XXXX` BEFORE jj-cell dispatch (with `pre.oplog` + `planning.tar` both present)"
    - "`gsd/phase-14-dogfood` bookmark created in `jj bookmark list` after dispatch, pointing to a non-main change_id (assertion: `jj log -r 'gsd/phase-14-dogfood..@' --no-graph` reports non-empty pre-fanIn)"
    - "Post fan-in: `jj log -r 'divergent()' --no-graph` empty (per ROADMAP SC3)"
    - "Post fan-in: all N agent bookmarks abandoned (`jj bookmark list | grep -c '^gsd/phase-14-agent-' == 0`)"
    - "Post green: `gsd/phase-14-dogfood` bookmark abandoned (`jj bookmark list | grep -c '^gsd/phase-14-dogfood$' == 0`)"
    - "`.planning/intel/v1.3-dogfood-metrics.md` exists and contains per-cell `dispatch_ms`, `fan_in_ms`, `conflict_count` for both jj-cell and git-cell, plus literal `pre_snapshot_path`, `tarball_sha256`, `pre_op_id` recovery anchor fields"
    - "git cell dispatched via the same internal SDK CLI verb invocations as the jj cell (NOT by calling `scripts/e2e-parallel-phase.sh` unchanged) — RESEARCH § G recommendation: fork the dispatch+fan-in invocations into the wrapper so both cells capture per-stage timing apples-to-apples"
    - "Both cells dispatch N=3 plans by default; the wrapper accepts an `N=2` env-var override for Phase 13 baseline comparison (per RESEARCH Open Q3 + Pitfall 4)"
    - "On failure mid-dogfood, the operator can invoke `bash scripts/dogfood-restore.sh <pre_op_id> <tarball_path>` using the values printed to stderr and recorded in the metrics file"
    - "Post-execute CONTEXT.md updated with the literal `mktemp` path of the actual run, the captured pre-op-id, and verbatim script invocation (per D-10 surface 1 of 2)"
  artifacts:
    - path: "scripts/dogfood-phase-14.sh"
      provides: "Jj-cell + git-cell dogfood orchestrator with timing capture, pre-snapshot, and metrics emission"
      contains: "gsd/phase-14-dogfood"
      min_lines: 200
    - path: ".planning/intel/v1.3-dogfood-metrics.md"
      provides: "Durable v1.4+ regression baseline + recovery anchor"
      contains: "Recovery Anchor"
      min_lines: 50
    - path: ".planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md"
      provides: "Post-execute recovery procedure prose appended with run-specific literals"
      contains: "scripts/dogfood-restore.sh"
  key_links:
    - from: "scripts/dogfood-phase-14.sh"
      to: "gsd-sdk query workspace.parallel.dispatch"
      via: "SDK CLI bridge invocation with --cwd $REPO_ROOT --phase 14 --main-bookmark gsd/phase-14-dogfood"
      pattern: "workspace\\.parallel\\.dispatch"
    - from: "scripts/dogfood-phase-14.sh"
      to: "gsd-sdk query workspace.parallel.fan-in"
      via: "SDK CLI bridge invocation with --handle @<file> --results @-"
      pattern: "workspace\\.parallel\\.fan-in"
    - from: ".planning/intel/v1.3-dogfood-metrics.md"
      to: "scripts/dogfood-restore.sh"
      via: "Recovery Anchor section embeds the verbatim invocation"
      pattern: "scripts/dogfood-restore\\.sh"
---

<objective>
Implement DOGFOOD-01 + DOGFOOD-02 (final phase of v1.3). Ship `scripts/dogfood-phase-14.sh` — the jj-cell + git-cell dogfood orchestrator — and produce `.planning/intel/v1.3-dogfood-metrics.md` as the durable v1.4+ baseline. Also append D-10 surface 1 (post-execute recovery prose with literal run values) to CONTEXT.md.

**Hybrid execution model (D-01)**:
- **jj cell (in-repo)**: creates `gsd/phase-14-dogfood` bookmark in THIS repo's colocated jj (NOT main), captures pre-snapshot, dispatches N runtime-synthetic plans via `gsd-sdk query workspace.parallel.dispatch` against the dogfood bookmark, runs the fan-in, asserts clean topology, abandons the dogfood bookmark on green.
- **git cell (mktemp throwaway)**: per RESEARCH Open Q5 recommendation, the wrapper forks the SDK CLI verb invocations (`workspace.parallel.dispatch` + `workspace.parallel.fan-in`) into its own bash code — does NOT call `scripts/e2e-parallel-phase.sh` unchanged — so both cells produce `dispatch_ms` + `fan_in_ms` separately for apples-to-apples comparison. The wrapper sets up the mktemp throwaway repo, seeds it, runs the dispatch sequence, and tears it down via `trap` on EXIT.

Both cells use `N=${N:-3}` default-3-with-override (per RESEARCH Open Q3 + Pitfall 4 — N=3 gives one extra data point per cell at marginal runtime cost, reducing baseline variance noise; `N=2 bash scripts/dogfood-phase-14.sh` is the Phase 13 comparison override).

**Pre-snapshot + recovery (D-09 + D-12 + D-10)**:
- Pre-snapshot lives at `mktemp -d -t gsd-dogfood-pre-XXXX` (sibling temp dir, NEVER inside this repo's WC per memory `feedback_avoid_jj_auto_tracked_output`).
- Two artifacts inside: `pre.oplog` (output of `jj op log -n 200`) and `planning.tar` (tarball of `.planning/`).
- Tarball SHA-256 computed via `sha256sum` (Linux) or `shasum -a 256` (macOS) with portable detection via `command -v sha256sum`.
- `pre.op_id` captured via `jj op log -n 1 --no-graph -T 'id ++ "\n"'`.
- These three values (pre_op_id, pre_snapshot_path, tarball_sha256) are written into the durable `.planning/intel/v1.3-dogfood-metrics.md` post-run as the v1.4+ recovery anchor — even after `$PRE` is GC'd by the OS.

**Metrics file (D-12)**:
- Path: `.planning/intel/v1.3-dogfood-metrics.md`.
- Two markdown tables (per-cell metrics for jj cell + git cell).
- One "Recovery Anchor" section with the three durable fields + verbatim `bash scripts/dogfood-restore.sh <pre_op_id> <tarball_path>` invocation.
- One "Rehearsal evidence" section pasting the Plan 04 rehearsal output.
- Format mirrors `.planning/intel/06-dogfood-log.md`'s front-matter + code-fence shape (but body is tabular, not forensic prose).

**Post-execute CONTEXT.md update (D-10 surface 1)**:
- Append a new section "Post-execute recovery procedure (run-specific)" at the end of `14-CONTEXT.md` containing the literal `mktemp` path, the captured pre-op-id, and the verbatim `bash scripts/dogfood-restore.sh <args>` invocation. This is the SC4-literal "documented in the dogfood phase's CONTEXT.md".

The plan is `autonomous: false` because Pitfall 10 + ROADMAP SC3-SC5 require **human verification** of the dogfood metrics + cleanup + recovery-anchor correctness BEFORE the phase closes.

Output: One new shell script + one new metrics markdown file + one CONTEXT.md append.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md
@.planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md
@.planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md
@.planning/phases/14-default-flip-dogfood-validation/14-01-SUMMARY.md
@.planning/phases/14-default-flip-dogfood-validation/14-02-SUMMARY.md
@.planning/phases/14-default-flip-dogfood-validation/14-03-SUMMARY.md
@.planning/phases/14-default-flip-dogfood-validation/14-04-SUMMARY.md
@.planning/intel/06-dogfood-log.md

<interfaces>
<!-- Patterns from e2e-parallel-phase.sh that this dogfood orchestrator borrows -->

From scripts/e2e-parallel-phase.sh:38-66 (usage block + env-var validation pattern; the dogfood wrapper mirrors the shape but with different env vars: GSD_SDK + N instead of GSD_E2E_BACKEND).

From scripts/e2e-parallel-phase.sh:101-105 (sibling-mktemp + trap cleanup — applied to git cell's throwaway repo; NOT applied to $PRE per D-09).

From scripts/e2e-parallel-phase.sh:178-190 (dispatch invocation skeleton):
- printf the plans JSON
- pipe to `$GSD_SDK query workspace.parallel.dispatch --cwd <dir> --phase <N> --main-bookmark <name> --plan @-`
- capture stdout as HANDLE_JSON
- check `jq -r '.ok // "true"'` is not "false"

From scripts/e2e-parallel-phase.sh:225-246 (per-workspace commit with cwd-pinned subshell + .planning marker for findProjectRoot per Pitfall 5).

From scripts/e2e-parallel-phase.sh:267-272 (fan-in handle-file workaround — mktemp + write + invoke + rm).

From scripts/e2e-parallel-phase.sh:286-295 (assert_eq helper).

From RESEARCH § Common Operation 3 (pre-snapshot capture — D-09):
- mktemp -d gsd-dogfood-pre-XXXX
- jj op log -n 1 --no-graph -T 'id ++ "\n"'
- jj op log -n 200 > pre.oplog
- tar -cf planning.tar .planning/
- portable sha256 detection (`command -v sha256sum`)

From RESEARCH § Common Operation 4 (timing capture — D-12):
- DISPATCH_START_MS=$(date +%s%3N) ... DISPATCH_END_MS=$(date +%s%3N) ... DISPATCH_MS=$((END - START))
- Same bracketing around fan-in

From RESEARCH § C "Bookmark create/abandon" (D-01 jj-cell bookmark lifecycle):
- jj bookmark create gsd/phase-14-dogfood -r @-
- jj bookmark forget gsd/phase-14-dogfood (NEVER `delete` — `delete` tries remote)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/dogfood-phase-14.sh orchestrator</name>
  <files>scripts/dogfood-phase-14.sh</files>
  <read_first>
    - scripts/e2e-parallel-phase.sh (full 357-line file — the closest analog; identical jq pipelines, dispatch/fan-in shapes, per-workspace commit pattern; the git-backend repo-init pattern is load-bearing)
    - scripts/dogfood-restore.sh (Plan 03 product — referenced for the recovery anchor's verbatim invocation)
    - scripts/dogfood-rehearse.sh (Plan 04 product — pattern reference for the timing/measurement style if needed)
    - sdk/src/query/workspace-parallel-dispatch.ts (the CLI bridge under exercise — confirms Plan 02 envelope ships before this script runs)
    - .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md § D-01 + § D-02 + § D-09 + § D-12
    - .planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md § C + § D + § Common Operations 3+4 + § G "Optional Phase 13 carry-forwards"
    - .planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md § "scripts/dogfood-phase-14.sh" (per-file mutation detail + deviation list)
    - .planning/intel/06-dogfood-log.md (Phase 6 sibling-clone dogfood precedent for the sibling-mktemp idiom + tool-version metadata convention)
    - scripts/lint-vcs-no-raw-git.cjs + scripts/lint-vcs-no-raw-git.allow.json (the lint regex this script must NOT trigger; raw `jj` is fine; raw `git` is forbidden; current allowlist net is +1 = 24 entries from Phase 10)
  </read_first>
  <action>
    Create `scripts/dogfood-phase-14.sh` with this overall structure (full implementation per RESEARCH § C + § D + § PATTERNS — approximately 200-250 lines):

    1. **Shebang + safety preamble** (matches `e2e-parallel-phase.sh:1` + `:34`):
       - `#!/usr/bin/env bash` (NOT zsh per RESEARCH § Project Constraints — this script reuses e2e-parallel-phase.sh patterns verbatim).
       - Header comment block (15-20 lines) stating: purpose, two execution cells, plan-count override mechanism, Pitfall 10 framing, pre-snapshot / recovery references, what to do on failure (call `bash scripts/dogfood-restore.sh <args>`).
       - `set -euo pipefail`.

    2. **Env-var defaults + project-root precondition**:
       - `GSD_SDK="${GSD_SDK:-gsd-sdk}"` matching `e2e-parallel-phase.sh:67`.
       - `N="${N:-3}"` (default 3, override via `N=2 bash scripts/dogfood-phase-14.sh` per RESEARCH Open Q3 + Pitfall 4).
       - Validate N is a positive integer ≥ 2: `case "$N" in '' | *[!0-9]* ) FATAL ;; esac` and `[ "$N" -lt 2 ] && FATAL "N must be >= 2"`.
       - Project-root precondition: `[ -f .planning/config.json ]` or FATAL with usage (script must be run from project root; mirrors Plan 04's check).
       - Record `REPO_ROOT="$PWD"`.

    3. **Usage block** (`scripts/e2e-parallel-phase.sh:38-56` pattern, adapted): a `usage()` function that prints the script's purpose, env-var contract (`GSD_SDK`, `N`), and example invocations. Invoked when wrong args or `--help` is given.

    4. **Build agent labels dynamically from N**: a for loop producing `phase-14-1 phase-14-2 ...` style labels (matches the `e2e-parallel-phase.sh:176` `AGENT_LABELS` pattern but parameterized).

    5. **Pre-snapshot block (D-09)** — RESEARCH Common Operation 3 verbatim:
       - `PRE=$(mktemp -d "${TMPDIR:-/tmp}/gsd-dogfood-pre-XXXX")` — sibling temp, NOT in WC.
       - **NO `trap rm -rf "$PRE"` on EXIT** — D-12 needs the path persisted for the recovery anchor. The directory survives the run intentionally.
       - `PRE_OP_ID=$(jj op log -n 1 --no-graph -T 'id ++ "\n"')` — full 64-hex op-id.
       - `jj op log -n 200 > "${PRE}/pre.oplog"`.
       - `tar -cf "${PRE}/planning.tar" .planning/`.
       - Portable SHA-256: `command -v sha256sum >/dev/null && TARBALL_SHA=$(sha256sum "${PRE}/planning.tar" | awk '{print $1}') || TARBALL_SHA=$(shasum -a 256 "${PRE}/planning.tar" | awk '{print $1}')`.
       - Echo all four values (PRE, PRE_OP_ID, tarball path, TARBALL_SHA) to stderr — these are the recovery-anchor seed values.

    6. **jj-cell dispatch sequence (D-01 jj cell)**:
       - Create bookmark: `jj bookmark create gsd/phase-14-dogfood -r @-`.
       - Echo the bookmark's resolved change_id to stderr (`jj log -r 'gsd/phase-14-dogfood' --no-graph -T 'change_id.short(8)' -n 1`).
       - Build `PLANS_JSON` from `AGENT_LABELS` using the `printf | jq -R . | jq -sc 'map({agentId: ., planId: .})'` pipeline (`e2e-parallel-phase.sh:178` shape).
       - **Time the dispatch invocation**: `DISPATCH_START_MS=$(date +%s%3N)` → `HANDLE_JSON=$(...)` → `DISPATCH_END_MS=$(date +%s%3N)` → `DISPATCH_MS_JJ=$((DISPATCH_END_MS - DISPATCH_START_MS))`.
       - Dispatch invocation: `printf '%s' "$PLANS_JSON" | $GSD_SDK query workspace.parallel.dispatch --cwd "$REPO_ROOT" --phase 14 --main-bookmark gsd/phase-14-dogfood --plan @-`.
       - Validate `HANDLE_OK=$(printf '%s' "$HANDLE_JSON" | jq -r '.ok // "true"')`; FATAL on `false` (the CONFIG-02 envelope from Plan 02 would fire here if Plan 01's flip hadn't shipped; treat it as a hard error).
       - Per-workspace commit loop with `.planning/` marker for `findProjectRoot` per Pitfall 5 + RESEARCH § C, mirrors `e2e-parallel-phase.sh:225-246` byte-identically except: commit message is `"feat(14-dogfood): jj-cell work ${WS_AGENT}"` and the file is `work-${WS_AGENT}.txt`.
       - Build `RESULTS_ACCUM` JSON from the Handle's workspaces (mirrors `e2e-parallel-phase.sh:262` shape: `[{agentId, exitCode: 0}, ...]` via jq).
       - **Time the fan-in invocation**: `FANIN_START_MS=$(date +%s%3N)` → `FAN_RESULT=$(...)` → `FANIN_END_MS=$(date +%s%3N)` → `FAN_IN_MS_JJ=$((FANIN_END_MS - FANIN_START_MS))`.
       - Fan-in invocation uses the `mktemp` handle-file workaround per `e2e-parallel-phase.sh:267-272`.
       - Compute `CONFLICT_COUNT_JJ=$(printf '%s' "$FAN_RESULT" | jq -r 'if .conflicted then 1 else 0 end')`.

    7. **jj-cell post-dispatch assertions** (per ROADMAP SC3 + dogfood acceptance criteria):
       - Assertion: `jj log -r 'divergent()' --no-graph --ignore-working-copy -T 'change_id ++ "\n"'` is EMPTY (SC3 / TEST-14 parity — mirrors `e2e-parallel-phase.sh:334`).
       - Assertion: `jj bookmark list` shows NO `^gsd/phase-14-agent-` entries (all N agent bookmarks abandoned by fan-in).
       - Use an `assert_eq` helper mirroring `e2e-parallel-phase.sh:286-295`.

    8. **jj-cell bookmark cleanup on green**:
       - `jj bookmark forget gsd/phase-14-dogfood` (per RESEARCH § C — `forget` is local-only; NEVER `delete`).
       - Final assertion: `jj bookmark list | grep -c '^gsd/phase-14-dogfood$'` equals 0 (bookmark gone).

    9. **git-cell dispatch sequence (D-01 git cell, RESEARCH Open Q5 — fork SDK CLI invocations into wrapper)**:
       - `GIT_REPO=$(mktemp -d "${TMPDIR:-/tmp}/gsd-dogfood-git-XXXX")`.
       - Trap cleanup ONLY for `$GIT_REPO` (the git-cell throwaway): `cleanup_git() { rm -rf "$GIT_REPO"; }; trap cleanup_git EXIT`. The PRE dir is NOT trap-cleaned.
       - Initialize a fresh git repo in `$GIT_REPO`. **Inspect `scripts/e2e-parallel-phase.sh` lines 80-150 for the EXACT git-backend repo-init pattern** — this is the load-bearing reference. The harness already solved this lint-cleanly per STATE.md L137 ("Phase 13 Plan 03 Rule-3 deviation: the plan d09_constraint wrongly claimed jj-git-init is no-raw-git-lint-clean — the lint regex matches the inner git substring; fixed by quoting the subcommand word"). For git-only backends, `e2e-parallel-phase.sh` uses `jj "git" init --colocate` quoted to dodge lint, then exports `GSD_VCS=git` to pin the adapter. Replicate that exact idiom. **DO NOT introduce a NEW raw `git ` invocation** — if a raw-git is unavoidable, the LINT-05 net diff goes to +2 and must be documented in the SUMMARY.

       After repo-init, mirror the same pre-flight + dispatch + commit + fan-in sequence as the jj cell, but against `$GIT_REPO`:
       - Set up `--cwd "$GIT_REPO"` for all `gsd-sdk query` invocations.
       - Set `MAIN_BOOKMARK="main"` for the git cell (it's a fresh repo; main is the natural bookmark).
       - Export `GSD_VCS=git` to pin the adapter to git (mirrors `e2e-parallel-phase.sh:81-84`).
       - Build `PLANS_JSON` the same way (N agent labels, e.g., `phase-14-git-1`, `phase-14-git-2`, ...).
       - Time dispatch + fan-in: `DISPATCH_MS_GIT` and `FAN_IN_MS_GIT`.
       - Per-workspace commit loop (mirrors jj-cell pattern; uses `gsd-sdk query commit` cwd-pinned subshells with `.planning/` marker).
       - Validate fan-in result; compute `CONFLICT_COUNT_GIT`.
       - **No "bookmark abandon on green" step for the git cell** — git's lifecycle is different; the throwaway repo is trap-removed on EXIT.

    10. **Metrics emission**:
        - Capture run metadata: `RUN_DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)`, `RUN_USER="${USER:-unknown}"`, `JJ_VERSION=$(jj --version 2>&1 | awk '{print $2}')`, `NODE_VERSION=$(node --version 2>&1)`, `GSD_SDK_COMMIT=$(jj log -r @ --no-graph -T 'change_id.short(12)' -n 1)`.
        - Write `.planning/intel/v1.3-dogfood-metrics.md` per Task 2's structure (this script can either inline the heredoc write or call out to a separate helper; recommend inline heredoc for simplicity — keeps the metrics-emission logic co-located with the data sources).

    11. **Final stderr summary** echoing the key values for at-a-glance review: `dispatch_ms` (jj+git), `fan_in_ms` (jj+git), `conflict_count` (jj+git), `pre_snapshot_path`, `tarball_sha256`, `pre_op_id`. Plus the verbatim recovery invocation: `bash scripts/dogfood-restore.sh "${PRE_OP_ID}" "${PRE}/planning.tar"`.

    12. **chmod +x scripts/dogfood-phase-14.sh** after writing.

    **Lint compliance**: `scripts/lint-vcs-no-raw-git.cjs` scans `.sh` files. Apply the `jj "git" init --colocate` quoting trick from `e2e-parallel-phase.sh:142` for any jj/git invocation that needs the literal "git" substring. For the git-cell init, replicate `e2e-parallel-phase.sh`'s exact pattern. If a NEW raw-git invocation is unavoidable, document the LINT-05 diff in the SUMMARY (LINT-05 net could go from +1 to +2; researcher believes this is avoidable).

    **Style**: tabs throughout; status to stderr; US English; no trailing commas (n/a).
  </action>
  <verify>
    <automated>node scripts/lint-vcs-no-raw-git.cjs && bash -n scripts/dogfood-phase-14.sh && bash scripts/dogfood-phase-14.sh</automated>
  </verify>
  <acceptance_criteria>
    - Source: file exists and is executable — `test -x scripts/dogfood-phase-14.sh` exits 0
    - Source: shebang — `grep -c '^#!/usr/bin/env bash$' scripts/dogfood-phase-14.sh` equals 1
    - Source: `grep -c 'set -euo pipefail' scripts/dogfood-phase-14.sh` equals 1
    - Source: bookmark name — `grep -c 'gsd/phase-14-dogfood' scripts/dogfood-phase-14.sh` is at least 3 (creation, dispatch arg, abandon)
    - Source: bookmark management uses `forget` not `delete` — `grep -c 'jj bookmark forget gsd/phase-14-dogfood' scripts/dogfood-phase-14.sh` is at least 1 and `grep -c 'jj bookmark delete gsd/phase-14-dogfood' scripts/dogfood-phase-14.sh` equals 0
    - Source: timing capture via `date +%s%3N` — `grep -c 'date +%s%3N' scripts/dogfood-phase-14.sh` is at least 4 (dispatch start/end × 2 cells)
    - Source: pre-snapshot mktemp — `grep -c 'gsd-dogfood-pre-XXXX' scripts/dogfood-phase-14.sh` is at least 1
    - Source: NO trap-rm of $PRE — bash -c 'grep -E "trap[^\n]*PRE" scripts/dogfood-phase-14.sh' produces no output (PRE is durable, not trap-cleaned)
    - Source: tarball SHA-256 with portable detection — `grep -c 'command -v sha256sum' scripts/dogfood-phase-14.sh` is at least 1 and `grep -c 'shasum -a 256' scripts/dogfood-phase-14.sh` is at least 1
    - Source: `findProjectRoot` marker per workspace — `grep -c 'mkdir -p .*\.planning' scripts/dogfood-phase-14.sh` is at least 2 (once per cell)
    - Source: SDK CLI verb invocations — `grep -c 'workspace.parallel.dispatch' scripts/dogfood-phase-14.sh` is at least 2 (once per cell); `grep -c 'workspace.parallel.fan-in' scripts/dogfood-phase-14.sh` is at least 2
    - Source: `N="${N:-3}"` default — `grep -c 'N="\${N:-3}"' scripts/dogfood-phase-14.sh` equals 1
    - Source: lint clean — `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (the LINT-05 net diff goes from +1 to either +1 or +2; the SUMMARY records the actual outcome and any new allowlist entry)
    - Source: bash syntax valid — `bash -n scripts/dogfood-phase-14.sh` exits 0
    - Behavior: pre-snapshot capture is BEFORE bookmark creation — assertion: line number of `jj op log` in the file is LESS THAN line number of `jj bookmark create gsd/phase-14-dogfood`
    - Behavior: bookmark create is BEFORE dispatch invocation — assertion: line number of `jj bookmark create gsd/phase-14-dogfood` is LESS THAN line number of `workspace.parallel.dispatch` (first occurrence)
    - Behavior: script runs to exit 0 against this repo with Plan 01's flip already applied — `bash scripts/dogfood-phase-14.sh; echo $?` last line is `0`
    - Behavior: post-run state — `jj bookmark list | grep -c '^gsd/phase-14-dogfood$'` equals 0 (bookmark abandoned on green)
    - Behavior: post-run state — `jj bookmark list | grep -c '^gsd/phase-14-agent-'` equals 0 (all agent bookmarks abandoned)
    - Behavior: post-run state — `jj log -r 'divergent()' --no-graph --ignore-working-copy` produces empty output (SC3)
    - Behavior: pre-snapshot dir exists after the run — `[ -d "$PRE" ]` would exit 0 (the PRE variable is captured in the metrics file path for verification; alternative grep: `grep -E '/tmp/gsd-dogfood-pre-' .planning/intel/v1.3-dogfood-metrics.md` returns at least one line — confirms the literal path got recorded)
    - Behavior: snapshot artifacts present — at the recorded `$PRE` path, both `pre.oplog` and `planning.tar` exist
    - Behavior: main bookmark untouched — `jj log -r main --no-graph -T 'change_id ++ "\n"' -n 1` shows the same change_id as pre-dogfood (Pitfall 10 blast-radius bounded; verification is human-in-the-loop in the checkpoint Task 4)
  </acceptance_criteria>
  <done>
    `scripts/dogfood-phase-14.sh` exists, runs end-to-end to exit 0, dispatches N=3 plans on both backends, captures per-stage timing, abandons all transient bookmarks on green, leaves main untouched, and emits the values needed for the metrics file. Lint clean (allowlist diff documented in SUMMARY).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write .planning/intel/v1.3-dogfood-metrics.md (D-12)</name>
  <files>.planning/intel/v1.3-dogfood-metrics.md</files>
  <read_first>
    - .planning/intel/06-dogfood-log.md (lines 1-11 + 40-67 + 248-250 — front-matter shape, code-fence convention, cleanup-policy disclosure pattern)
    - .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md § D-12 (metrics field list)
    - .planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md § D "Metrics file format recommendation" lines 1067-1124 (the literal markdown skeleton)
    - .planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md § ".planning/intel/v1.3-dogfood-metrics.md" (deviations from `06-dogfood-log.md`)
    - The stderr output from running `scripts/dogfood-phase-14.sh` in Task 1 — contains the literal values to embed
  </read_first>
  <action>
    Create `.planning/intel/v1.3-dogfood-metrics.md` with the structure RESEARCH § D specifies (lines 1067-1124). The file is a durable v1.4+ regression baseline, not a transient scanner sidecar — committing it to the working tree IS expected and acceptable (D-12 distinguishes from memory `feedback_avoid_jj_auto_tracked_output`).

    Note: Task 1's script (`scripts/dogfood-phase-14.sh`) may write this file inline via heredoc as part of its execution — if so, Task 2's job is to verify the file matches this structure (and fix any discrepancies manually). If Task 1's script does NOT inline-write the metrics file, Task 2 writes it directly from the script's stderr output.

    Structure:

    1. **Title**: `# v1.3 Dogfood Metrics — Phase 14`.

    2. **Front-matter pairs** (mirroring `06-dogfood-log.md:1-11`):
       - `**Date:**` (UTC, ISO format from Task 1's `$RUN_DATE`).
       - `**Operator:**` ($RUN_USER + Claude executor identifier).
       - `**This-repo state before dogfood:**` `parallelization=false (D-03 pre-flip)`.
       - `**This-repo state after dogfood:**` `parallelization=true (Plan 01 + Plan 05)`.
       - `**Tool versions:**` `jj <version>`, `Node <version>`, `gsd-sdk built from <change_id>`.

    3. **Per-cell metrics tables** (RESEARCH § D recommendation; one table per cell):

       Section `### jj cell (in-repo, bookmark gsd/phase-14-dogfood)`: a markdown table with rows for `N (plan count)`, `dispatch_ms`, `fan_in_ms`, `conflict_count`, `workspaces created`, `bookmark cleanup` (text: `clean (gsd/phase-14-dogfood abandoned)`), `jj log -r 'divergent()' --no-graph` result (text: `(empty)`).

       Section `### git cell (mktemp throwaway via dispatch+fan-in invocations forked from e2e-parallel-phase.sh per Open Q5)`: parallel table with rows for `N`, `dispatch_ms`, `fan_in_ms`, `conflict_count`, `workspaces created`, `merged.length` from the FanInResult.

       Fill all numeric cells with the actual values from Task 1's stderr output.

    4. **Recovery Anchor (durable per D-09 + D-12)**: a new section header `## Recovery Anchor (durable per D-09 / D-12)` containing:
       - `**Pre-op-id:**` followed by the actual full 64-hex op-id from Task 1's `$PRE_OP_ID`.
       - `**Pre-snapshot dir (sibling mktemp, transient):**` followed by the literal `$PRE` path.
       - `**Tarball:**` followed by the literal `$PRE/planning.tar` path.
       - `**Tarball SHA-256:**` followed by `$TARBALL_SHA` from Task 1.
       - `**Recovery procedure:**` followed by a code-fenced bash block with the verbatim invocation `bash scripts/dogfood-restore.sh '<pre-op-id>' '<tarball-path>'` — using the actual values, not placeholders.
       - A paragraph noting the OS GC implication: "If the `/tmp/...` directory has been GC'd by the OS, the tarball is irrecoverable from this anchor. The pre-op-id alone (without the tarball) restores jj repo state including the WC and bookmarks; the `.planning/` content reverts to whatever the WC had at the snapshot operation. The tarball is the additive recovery surface for `.planning/` content that may have been modified post-op-id by the dogfood itself."
       - A paragraph noting the unrelated-work caveat (per RESEARCH § Security Domain row "jj op restore reverting unrelated post-dogfood work"): "Operators must save unrelated post-dogfood work before invoking recovery — `jj op restore` reverts to the pre-op-id snapshot, discarding everything that happened after."

    5. **Rehearsal evidence**: a section pasting the literal Plan 04 rehearsal stderr output (the three PASS lines + final green message).

    6. **Out-of-band notes**: a section for any deviations from the planned procedure — e.g., if Plan 04's rehearsal needed `--what=repo` instead of the default, document it here; if the script needed an allowlist entry, document the LINT-05 diff here.

    Style: US English; consistent header levels (`##` for top-level sections, `###` for sub-sections within); no trailing whitespace; no trailing commas.
  </action>
  <verify>
    <automated>test -f .planning/intel/v1.3-dogfood-metrics.md && node -e "const t = require('fs').readFileSync('.planning/intel/v1.3-dogfood-metrics.md','utf8'); for (const s of ['Recovery Anchor','Pre-op-id','Tarball SHA-256','dispatch_ms','fan_in_ms','conflict_count','jj cell','git cell']) { if (!t.includes(s)) { console.error('MISSING:', s); process.exit(1); } }"</automated>
  </verify>
  <acceptance_criteria>
    - Source: file exists — `test -f .planning/intel/v1.3-dogfood-metrics.md` exits 0
    - Source: contains `# v1.3 Dogfood Metrics — Phase 14` — `grep -c '^# v1.3 Dogfood Metrics — Phase 14' .planning/intel/v1.3-dogfood-metrics.md` equals 1
    - Source: front-matter pairs — `grep -c '^\*\*Date:\*\*' .planning/intel/v1.3-dogfood-metrics.md` is at least 1
    - Source: per-cell sections — `grep -c '### jj cell' .planning/intel/v1.3-dogfood-metrics.md` equals 1 and `grep -c '### git cell' .planning/intel/v1.3-dogfood-metrics.md` equals 1
    - Source: required metric fields per cell — `grep -c 'dispatch_ms' .planning/intel/v1.3-dogfood-metrics.md` is at least 2 (once per cell) and `grep -c 'fan_in_ms' .planning/intel/v1.3-dogfood-metrics.md` is at least 2 and `grep -c 'conflict_count' .planning/intel/v1.3-dogfood-metrics.md` is at least 2
    - Source: Recovery Anchor section — `grep -c '## Recovery Anchor' .planning/intel/v1.3-dogfood-metrics.md` equals 1
    - Source: recovery anchor required fields — `grep -c 'Pre-op-id' .planning/intel/v1.3-dogfood-metrics.md` is at least 1 and `grep -c 'Tarball SHA-256' .planning/intel/v1.3-dogfood-metrics.md` is at least 1 and `grep -c 'Pre-snapshot dir' .planning/intel/v1.3-dogfood-metrics.md` is at least 1
    - Source: recovery procedure invocation — `grep -c 'scripts/dogfood-restore\.sh' .planning/intel/v1.3-dogfood-metrics.md` is at least 1
    - Source: rehearsal evidence section — `grep -c 'Rehearsal evidence' .planning/intel/v1.3-dogfood-metrics.md` is at least 1
    - Source: numeric values present — `grep -E '^\| dispatch_ms \| [0-9]+ \| ms \|' .planning/intel/v1.3-dogfood-metrics.md` returns at least 2 lines (one per cell — non-zero ms values mean Task 1 actually measured something, not just wrote zeros)
    - Behavior: tarball SHA-256 is a 64-hex string — `grep -oE '[a-f0-9]{64}' .planning/intel/v1.3-dogfood-metrics.md` returns at least 2 lines (the pre-op-id AND the tarball SHA — both 64-hex)
  </acceptance_criteria>
  <done>
    `.planning/intel/v1.3-dogfood-metrics.md` exists with the documented structure, real numeric values from Task 1's actual run, a complete Recovery Anchor section with the verbatim restore invocation, and rehearsal evidence pasted in.
  </done>
</task>

<task type="auto">
  <name>Task 3: Append post-execute recovery procedure to 14-CONTEXT.md (D-10 surface 1)</name>
  <files>.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md</files>
  <read_first>
    - .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md (current state — D-10 surface 1 promised in the CONTEXT.md prose to be appended POST-EXECUTE)
    - .planning/intel/v1.3-dogfood-metrics.md (Task 2 product — contains the literal run values to mirror)
    - The stderr output from running `scripts/dogfood-phase-14.sh` in Task 1
  </read_first>
  <action>
    Append a new section to `.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` documenting the actual run-specific recovery values. Per D-10 surface 1 ("Prose in this CONTEXT.md — the SC4-literal 'documented in the dogfood phase's CONTEXT.md' — appended post-execute with the literal `mktemp` path of the actual run, the captured pre-op-id, and the verbatim script invocation").

    The new section goes at the END of `14-CONTEXT.md`, AFTER the existing `*Phase: 14-default-flip-dogfood-validation*` / `*Context gathered: 2026-05-23*` footer lines, OR before the footer if the footer must remain last. Choose whichever keeps the file readable; current convention puts the footer last so prepend BEFORE the footer.

    New section structure:

    ```markdown
    ---

    ## Post-execute recovery procedure (run-specific)

    Filled in by Plan 05 (Task 3) on 2026-05-23 after the dogfood ran.

    **Pre-op-id:** `<actual op-id from $PRE_OP_ID>`
    **Pre-snapshot dir:** `<actual $PRE path>`
    **Tarball:** `<$PRE/planning.tar>`
    **Tarball SHA-256:** `<actual $TARBALL_SHA>`

    **Recovery invocation (run from project root):**

    ```bash
    bash scripts/dogfood-restore.sh '<pre-op-id>' '<$PRE/planning.tar>'
    ```

    The recovery script restores jj repo state via `jj op restore <pre-op-id>` (default `--what=repo,remote-tracking`) FIRST, then extracts the planning tarball via `tar -xf <tarball> -C .` LAST (Pitfall 2 ordering — restore first so the WC update doesn't clobber the authoritative .planning/ tarball content).

    If the `<$PRE path>` directory has been GC'd by the OS (typical TMPDIR cleanup interval), the tarball is irrecoverable from this anchor. The pre-op-id alone (without the tarball) restores jj repo state including the WC and bookmarks; the `.planning/` content reverts to whatever the WC had at the snapshot operation.

    Operators must save unrelated post-dogfood work before invoking recovery — `jj op restore` reverts to the pre-op-id snapshot, discarding everything that happened after.

    Rehearsal evidence (Plan 04) confirmed the recovery script works correctly against a `cp -a` clone of this repo before the real dogfood ran. See `.planning/intel/v1.3-dogfood-metrics.md` § "Rehearsal evidence" for the PASS lines.

    ---
    ```

    Fill in `<actual op-id from $PRE_OP_ID>`, `<actual $PRE path>`, `<$PRE/planning.tar>`, `<actual $TARBALL_SHA>` from Task 1's stderr output (the same values that go into `.planning/intel/v1.3-dogfood-metrics.md` § Recovery Anchor in Task 2). The two files MUST agree byte-for-byte on these values — they're the same durable recovery anchor recorded in two places per D-10 (CONTEXT.md prose + the metrics file).

    Do NOT modify any other section of `14-CONTEXT.md` — only the append at the end. Preserve the existing `<domain>`, `<decisions>`, `<canonical_refs>`, etc., sections verbatim.
  </action>
  <verify>
    <automated>grep -c 'Post-execute recovery procedure' .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md && grep -c 'scripts/dogfood-restore.sh' .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - Source: new section header — `grep -c '## Post-execute recovery procedure' .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` equals 1
    - Source: recovery invocation present — `grep -c 'scripts/dogfood-restore.sh' .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` is at least 1
    - Source: literal pre-op-id present — `grep -oE '[a-f0-9]{64}' .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` returns at least 2 lines (pre-op-id + tarball SHA, both 64-hex)
    - Source: pre-op-id matches metrics file — extracting the same 64-hex op-id from both files produces the same value (cross-reference: `node -e "const a=require('fs').readFileSync('.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md','utf8').match(/Pre-op-id.+?([a-f0-9]{64})/)[1]; const b=require('fs').readFileSync('.planning/intel/v1.3-dogfood-metrics.md','utf8').match(/Pre-op-id.+?([a-f0-9]{64})/)[1]; if (a!==b) { console.error('mismatch:',a,'vs',b); process.exit(1); }"` exits 0)
    - Source: existing CONTEXT.md sections unchanged — `grep -c '<decisions>' .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` still equals 1 and `grep -c '<canonical_refs>' .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` still equals 1 (the original tagged sections are intact)
  </acceptance_criteria>
  <done>
    `14-CONTEXT.md` carries the run-specific recovery anchor (D-10 surface 1). The values match `.planning/intel/v1.3-dogfood-metrics.md` § Recovery Anchor byte-for-byte.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify dogfood outcome + recovery anchor</name>
  <action>Pause for the operator to inspect the dogfood outcome from Tasks 1-3. The executor presents: (a) the per-cell metrics from `.planning/intel/v1.3-dogfood-metrics.md` (non-zero `dispatch_ms` / `fan_in_ms`, zero `conflict_count`), (b) the Recovery Anchor with the literal pre-op-id + tarball SHA-256 + sibling-mktemp path, (c) confirmation that `main` bookmark's change_id is unchanged before/after dogfood (Pitfall 10 blast-radius evidence), (d) confirmation that `gsd/phase-14-dogfood` and all `gsd/phase-14-agent-*` bookmarks are abandoned. Operator approves only if all four are clean; otherwise route to gap closure or invoke `bash scripts/dogfood-restore.sh <pre-op-id> <tarball-path>` to roll back.</action>
  <what-built>
    Tasks 1-3 above produced:
    - `scripts/dogfood-phase-14.sh` (executable, runs to exit 0, captures timing + pre-snapshot + metrics).
    - `.planning/intel/v1.3-dogfood-metrics.md` (durable v1.4+ baseline with per-cell tables + Recovery Anchor section).
    - `.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` (appended Post-execute recovery procedure section).
    The dogfood actually ran. The `gsd/phase-14-dogfood` bookmark was created, used for dispatch, and abandoned on green. Main bookmark untouched. Pre-snapshot anchor durably recorded for v1.4+ regression and emergency recovery.
  </what-built>
  <how-to-verify>
    1. **Open `.planning/intel/v1.3-dogfood-metrics.md`** and confirm:
       - The two per-cell tables (jj + git) have non-empty `dispatch_ms` and `fan_in_ms` cells with real millisecond numbers (not placeholders, not zero).
       - `conflict_count` is `0` for both cells (clean fan-in — happy-path baseline).
       - The Recovery Anchor section has the full 64-hex pre-op-id, the sibling-mktemp `$PRE` path, the tarball SHA-256, and a runnable `bash scripts/dogfood-restore.sh '<id>' '<path>'` invocation.
       - The Rehearsal evidence section has the three PASS lines from Plan 04.
    2. **Verify main bookmark untouched** — Pitfall 10 blast-radius check:
       - `jj log -r main --no-graph -T 'change_id ++ "\n"' -n 1` produces the same change_id as before the dogfood ran (capture before Plan 05 ran, compare after).
       - This is the strongest Pitfall 10 signal: if main changed, the dogfood polluted real history and something is structurally wrong.
    3. **Verify bookmark cleanup**:
       - `jj bookmark list | grep -c '^gsd/phase-14-dogfood$'` produces `0` (dogfood bookmark abandoned).
       - `jj bookmark list | grep -c '^gsd/phase-14-agent-'` produces `0` (all transient agent bookmarks abandoned).
    4. **Verify pre-snapshot anchor is still on disk and usable** (optional, time-limited):
       - The `$PRE` path recorded in the metrics file should still exist (mktemp dirs typically survive at least a few hours).
       - `[ -f "<$PRE>/planning.tar" ]` exits 0.
       - Tarball SHA-256 matches the recorded value: re-run `shasum -a 256 "<$PRE>/planning.tar"` and compare.
    5. **Verify the recovery anchor in CONTEXT.md matches the metrics file**:
       - The same 64-hex op-id appears in both `.planning/intel/v1.3-dogfood-metrics.md` and `.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md`.
       - Same path, same SHA.
    6. **Approval decision**:
       - If all of (1)-(5) check out, approve to close the phase. The dogfood succeeded; main is untouched; metrics are durable; recovery is anchored in two places (CONTEXT.md + metrics file).
       - If anything looks wrong — main bookmark moved, conflict_count is 1+ on clean dogfood, missing values in metrics, mismatched anchors — flag the issue and route to gap closure or restore via `bash scripts/dogfood-restore.sh <args>` (the values are still in stderr / metrics file).
  </how-to-verify>
  <resume-signal>Type "approved" if (1) dogfood ran clean (zero conflicts, all bookmarks abandoned), (2) main is untouched, (3) metrics file + CONTEXT.md anchor agree on the recovery values, (4) you're confident the v1.4+ baseline is well-grounded. Otherwise describe the issue and the orchestrator routes to gap closure or recovery.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project root → dogfood script | The script must be invoked from project root; the path-marker check is the boundary. |
| dogfood script → jj/jj-colocated WC | Pre-snapshot, bookmark create/dispatch/fan-in, bookmark abandon — all jj-level mutations against THIS repo. Operator-controlled invocation. |
| dogfood script → git mktemp throwaway | The git cell creates and destroys an isolated mktemp repo; no production-data exposure. |
| dogfood script → metrics file | The script (or Task 2) writes durable evidence into `.planning/intel/v1.3-dogfood-metrics.md`. The file is committed to the repo — durable artifact, not transient output. |
| metrics file ↔ CONTEXT.md prose | Two surfaces hold the same recovery anchor (Pre-op-id, tarball SHA, path). Cross-check via grep keeps them in sync. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-T-bookmark | Tampering (bookmark name injection) | hardcoded bookmark name `gsd/phase-14-dogfood` | mitigate | Bookmark name is a literal constant in the script; not derived from user input. Same posture as `e2e-parallel-phase.sh:161` ("threat T-13-10 — agent labels are constants"). Per RESEARCH § Security Domain row "Bookmark name injection". |
| T-14-T-shell-injection | Tampering (shell injection via env vars) | `GSD_SDK`, `N` env vars | mitigate | `N` is validated via `case` block (positive integer only). `GSD_SDK` is used as a quoted invocation token; no `eval` anywhere. Per RESEARCH § Security Domain row "Shell injection via env vars". |
| T-14-Disclosure-pre-snapshot | Information Disclosure (auto-snapshot of pre-snapshot artifacts) | sibling `$PRE` mktemp directory | mitigate | `$PRE` is created via `mktemp -d -t gsd-dogfood-pre-XXXX` OUTSIDE this repo's WC — NEVER inside. Memory `feedback_avoid_jj_auto_tracked_output` enforced. The literal path captured in the metrics file is a STRING reference only; the directory remains sibling-to-the-WC. Per RESEARCH § Security Domain row "Auto-snapshot leak via mktemp tarball under WC". |
| T-14-V6-crypto | Cryptography (V6) | tarball SHA-256 anchor | mitigate | SHA-256 via `sha256sum` (Linux) / `shasum -a 256` (macOS) — portable detection via `command -v`. NOT for cryptographic security (no signing); for content-addressed recovery anchoring (verify the tarball hasn't been corrupted before invoking restore). Per RESEARCH § Security Domain row "V6 Cryptography (minor)". |
| T-14-V12-tarball | Files and Resources (V12) | $PRE mktemp + planning.tar | mitigate | All mktemp uses random-suffix; tarball name is fixed inside the randomized directory. No predictable filename pattern. Per RESEARCH § Security Domain row "V12 Files and Resources". |
| T-14-Repud-pitfall-10 | Repudiation (dogfood pollutes main) | jj-cell dispatch against `gsd/phase-14-dogfood` bookmark | mitigate | Hardcoded `--main-bookmark gsd/phase-14-dogfood` argument; bookmark created BEFORE dispatch; assertion that `main` bookmark's change_id is unchanged post-run (human checkpoint Task 4 verifies). Per ROADMAP SC3 "isolated bookmark NOT main". Per RESEARCH § Security Domain row "Config-flip surprise" (broader category). |
| T-14-Repud-unrelated-work | Repudiation (recovery reverts unrelated post-dogfood work) | jj op restore semantics | mitigate (documented) | The metrics file's Recovery Anchor section + CONTEXT.md appended prose both call this out: "Operators must save unrelated post-dogfood work before invoking recovery." Per RESEARCH § Security Domain row "jj op restore reverting unrelated post-dogfood work". |
</threat_model>

<verification>
- `scripts/dogfood-phase-14.sh` runs to exit 0 in standard execution.
- `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (or +1 allowlist entry documented if unavoidable).
- `.planning/intel/v1.3-dogfood-metrics.md` exists and is structurally complete (cross-grep passes the long acceptance criteria).
- `.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md` carries the appended recovery anchor section.
- The pre-op-id appears identically in both metrics file and CONTEXT.md.
- `jj log -r main --no-graph` shows main unchanged.
- All transient bookmarks (`gsd/phase-14-dogfood` and `gsd/phase-14-agent-*`) are abandoned.
- `jj log -r 'divergent()' --no-graph` empty post fan-in.
- Human checkpoint approves.
</verification>

<success_criteria>
- DOGFOOD-01: 2-3 (default N=3) synthetic plans dispatched on isolated `gsd/phase-14-dogfood` bookmark via the new dispatcher; clean fan-in confirmed; agent bookmarks cleaned; both jj and git cells exercised.
- DOGFOOD-02: pre-snapshot captured via sibling mktemp; recovery primitive rehearsed (Plan 04) AND empirically exercised against this repo's anchor; metrics file durable with `dispatch_ms`, `fan_in_ms`, `conflict_count` per cell + Recovery Anchor section with pre-op-id + tarball SHA + pre-snapshot path.
- D-10 dual-surface complete: CONTEXT.md appended prose + `scripts/dogfood-restore.sh` runnable + both point to the same anchor.
- ROADMAP SC3 honored: `jj log -r 'divergent()' --no-graph` empty post fan-in.
- ROADMAP SC4 honored: pre-snapshot recovery procedure documented in CONTEXT.md.
- ROADMAP SC5 honored: metrics recorded to `.planning/intel/v1.3-dogfood-metrics.md`.
- Pitfall 10 blast-radius bounded: main bookmark untouched; dogfood bookmark abandoned; transient bookmarks abandoned; recovery anchor durable.
- Human checkpoint approves; phase ready to close.
</success_criteria>

<output>
Create `.planning/phases/14-default-flip-dogfood-validation/14-05-SUMMARY.md` when done documenting:
- The exact `dispatch_ms` + `fan_in_ms` + `conflict_count` values per cell from the run.
- Confirmation main bookmark change_id is unchanged before/after dogfood (Pitfall 10 evidence).
- Confirmation all transient bookmarks abandoned.
- The literal pre-op-id + $PRE path + tarball SHA-256 (also recorded in the metrics file and CONTEXT.md — three sources of truth must agree).
- Any LINT-05 allowlist diff (target: zero; max acceptable: +1 — document the entry and reason if used).
- Any deviations from the planned recovery script ordering (the rehearsal in Plan 04 should have validated the default `--what` choice; document if it didn't).
- The human-verify checkpoint resolution (approved / issues).
- Confirmation `.planning/intel/v1.3-dogfood-metrics.md` and `14-CONTEXT.md` post-execute section agree byte-for-byte on the recovery anchor values.
</output>
