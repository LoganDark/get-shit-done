---
phase: 14-default-flip-dogfood-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/workspace-parallel-dispatch.ts
  - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts
autonomous: true
requirements: [CONFIG-02]
requirements_addressed: [CONFIG-02]
tags: [cli-bridge, validation-envelope, loadConfig, contract-tests]
must_haves:
  truths:
    - "`gsd-sdk query workspace.parallel.dispatch` returns `{ok:false, reason:'parallelization_disabled', message:<non-empty>}` when the effective `.planning/config.json` has `parallelization: false`"
    - "The envelope does NOT fire when the `parallelization` key is missing (loadConfig defaults-merge returns `true`)"
    - "The envelope does NOT fire when `parallelization: true` is explicit"
    - "The strict-equal-`false` check ensures brownfield repos with the old nested-object shape (truthy object) do NOT trigger the refusal — only explicit boolean `false` does"
    - "The `message` field guides the user to two unblock paths: set `parallelization: true` OR remove the explicit `false` entry"
  artifacts:
    - path: "sdk/src/query/workspace-parallel-dispatch.ts"
      provides: "Fourth validation envelope (`parallelization_disabled`) alongside the existing three peer envelopes"
      contains: "reason: 'parallelization_disabled'"
    - path: "sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts"
      provides: "CONFIG-02 envelope contract test fixtures (D-03 mitigation — three `it` cases)"
      contains: "CONFIG-02 — parallelization_disabled"
    - path: "sdk/src/vcs/__tests__/cmd-parallel-git.test.ts"
      provides: "CONFIG-02 envelope contract test fixtures (D-03 mitigation — three `it` cases)"
      contains: "CONFIG-02 — parallelization_disabled"
  key_links:
    - from: "sdk/src/query/workspace-parallel-dispatch.ts"
      to: "sdk/src/config.ts loadConfig"
      via: "await loadConfig(cwd) call"
      pattern: "loadConfig\\(cwd\\)"
    - from: "sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts"
      to: "sdk/src/query/workspace-parallel-dispatch.ts"
      via: "dynamic import + handler invocation with FS-fixture projectDir"
      pattern: "workspaceParallelDispatchQuery"
---

<objective>
Implement CONFIG-02 (per D-06 + D-07 + D-08) by adding a fourth validation envelope to the CLI bridge `sdk/src/query/workspace-parallel-dispatch.ts` that refuses dispatch with `{ok:false, reason:'parallelization_disabled', message:<clear>}` when the effective `parallelization` config is explicitly `false`. Then implement the D-03 mitigation by adding contract test fixtures to `cmd-parallel-{jj,git}.test.ts` — these replace the in-the-wild "this repo's `parallelization: false`" invariant fixture (lost when Plan 01 flips this-repo to `true`) by moving it to test-fixture form.

Per D-06 the envelope lives ONLY at the CLI bridge — NOT in the adapter sidecars (`sdk/src/vcs/jj/parallel.ts`, `sdk/src/vcs/git/parallel.ts`), which are UPSTREAM-02-disciplined and forbidden from importing config-aware modules. Per D-07 the check uses `loadConfig`'s defaults-merge (missing-key → `true`). Per D-08 the envelope shape is `{ok, reason, message}` (not a thrown typed exception — the shell-harness `jq` consumer pattern needs the envelope).

Per RESEARCH finding #1: the SDK `loadConfig` does NOT perform the nested→flat normalization that CJS `core.cjs:480-485` does. The check MUST use strict equality `config.parallelization === false`, not loose-falsey `!config.parallelization`. A brownfield repo with the old nested object shape would loose-falsey-pass-through as truthy; strict-equal-`false` keeps the failure mode unambiguous.

Purpose: Closes the silent-no-op footgun for direct-SDK and shell-harness callers (the workflow gate at `execute-phase.md:133` already filters via the workflow config, but CLI-bridge callers bypass that gate). CONFIG-02 makes the refusal explicit and actionable.

Output: One modified CLI bridge source file + two modified contract test files (3 new `it` cases per file, 6 total new assertions).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md
@.planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md
@.planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md

<interfaces>
<!-- Existing peer envelopes the new one mirrors -->
<!-- Executor uses these as the literal pattern to extend -->

From sdk/src/query/workspace-parallel-dispatch.ts:42-71 (handler signature + three peer envelopes):
```typescript
export const workspaceParallelDispatchQuery: QueryHandler = async (args, projectDir) => {
  let cwd = projectDir;
  let phaseNumber: number | undefined;
  let mainBookmark = '';
  let planRaw: string | undefined;
  let maxConcurrency: number | undefined;

  for (let i = 0; i < args.length; i++) {
    if (args[i] === '--cwd' && args[i + 1]) {
      cwd = args[++i];
    } else if (args[i] === '--phase' && args[i + 1]) {
      phaseNumber = Number(args[++i]);
    } else if (args[i] === '--main-bookmark' && args[i + 1]) {
      mainBookmark = args[++i];
    } else if (args[i] === '--plan' && args[i + 1]) {
      planRaw = args[++i];
    } else if (args[i] === '--max-concurrency' && args[i + 1]) {
      maxConcurrency = Number(args[++i]);
    }
  }

  if (phaseNumber === undefined || Number.isNaN(phaseNumber)) {
    return { data: { ok: false, reason: 'phase_number_required' } };
  }
  if (!mainBookmark) {
    return { data: { ok: false, reason: 'main_bookmark_required' } };
  }
  if (planRaw === undefined) {
    return { data: { ok: false, reason: 'plan_required' } };
  }
  // ... NEW envelope inserts AFTER line 71 and BEFORE line 73 ...
  let plan: readonly { agentId: string; planId: string; workspacePath?: string }[];
  // ...
```

From sdk/src/query/config-gates.ts:8 + :26-28 (loadConfig invocation precedent):
```typescript
import { CONFIG_DEFAULTS, loadConfig } from '../config.js';

export const checkConfigGates: QueryHandler = async (args, projectDir) => {
  const config = await loadConfig(projectDir);
  // ...
};
```

From sdk/src/config.ts:71 + :92 (GSDConfig.parallelization signature):
```typescript
parallelization: boolean;        // line 71
parallelization: true,            // line 92 — default
```

From sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts:36-42 (existing top-of-file imports — the new test extends this):
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { mkdtempSync, mkdirSync, writeFileSync, rmSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { execSync } from 'node:child_process';
```

From sdk/src/vcs/__tests__/cmd-parallel-git.test.ts:55-61 (parallel sibling imports — same shape):
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import { mkdtempSync, mkdirSync, writeFileSync, rmSync, existsSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { execSync, spawnSync } from 'node:child_process';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CONFIG-02 envelope to CLI bridge (D-06 + D-07 + D-08)</name>
  <files>sdk/src/query/workspace-parallel-dispatch.ts</files>
  <read_first>
    - sdk/src/query/workspace-parallel-dispatch.ts (current 96-line file; existing three envelopes at lines 64/67/70)
    - sdk/src/query/config-gates.ts (loadConfig invocation precedent at lines 8 + 26-28)
    - sdk/src/config.ts:60-100 (the GSDConfig type and CONFIG_DEFAULTS — confirms `parallelization: boolean` field and default `true`)
    - sdk/src/config.ts:140-218 (loadConfig signature: async, returns `Promise<GSDConfig>` — already compatible with the async QueryHandler signature)
    - sdk/src/config.test.ts:162-176 (confirms SDK loader does NOT normalize nested-shape; pass-through is intentional)
    - .planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md § B "CONFIG-02 implementation surface" (insertion-point rationale) and § A "Critical caveat" (the strict-equal-false reason)
    - .planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md § "sdk/src/query/workspace-parallel-dispatch.ts" (mutation site detail; deviation list)
  </read_first>
  <behavior>
    - Behavior 1: When `.planning/config.json` has `parallelization: false`, handler returns `{data: {ok: false, reason: 'parallelization_disabled', message: <string>}}` — `message` contains the literal substring `parallelization: true` (guides user to set it explicitly).
    - Behavior 2: When `.planning/config.json` does NOT contain a `parallelization` key, handler does NOT return the parallelization_disabled envelope (loadConfig defaults-merge returns `true`).
    - Behavior 3: When `.planning/config.json` has `parallelization: true`, handler does NOT return the parallelization_disabled envelope.
    - Behavior 4: When `parallelization` is set to a non-boolean truthy value (e.g., the legacy nested object `{enabled: true, ...}`), handler does NOT return the parallelization_disabled envelope — strict-equal-`false` check protects against the loose-falsey trap (this is the brownfield safety net per RESEARCH finding #1).
    - Behavior 5: The new envelope fires AFTER the three existing input-validation envelopes (phase_number_required, main_bookmark_required, plan_required) — input validation is cheap; loadConfig is async I/O.
    - Behavior 6: The new envelope fires BEFORE `JSON.parse(planText)` — failing fast on disabled-config beats parsing a plan we won't dispatch.
  </behavior>
  <action>
    Add `import { loadConfig } from '../config.js';` to the import block at the top of the file (alongside the existing `import { readFileSync }` and `import { createVcsAdapter }` lines — the file currently imports `readFileSync` from `node:fs` at line 28 and `createVcsAdapter` from `../vcs/index.js` at line 29; the new import goes alongside these).

    Inside the `workspaceParallelDispatchQuery` handler body, insert a new envelope block AFTER the `plan_required` check (after the existing line `return { data: { ok: false, reason: 'plan_required' } };`) and BEFORE the `let plan: readonly ...` declaration line. The new block:

    1. `await`s `loadConfig(cwd)` (uses the resolved `cwd` from the argv loop, NOT `projectDir` — the handler honors `--cwd` overrides, so the loaded config must match the dispatch's cwd).
    2. Tests `config.parallelization === false` (strict equality — DO NOT use `!config.parallelization`; per RESEARCH finding #1 and § A "Critical caveat", the SDK `loadConfig` does NOT normalize nested-shape config, so the legacy nested object `{enabled: true, ...}` would loose-falsey-pass-through as truthy, breaking the brownfield safety net).
    3. On true, returns the envelope `{data: {ok: false, reason: 'parallelization_disabled', message: <string>}}`. The `message` field is the new addition relative to the existing three envelopes (D-08 requires it to guide users to two unblock paths). Concrete `message` value: `'Parallelization is disabled in .planning/config.json. Set ` + "`parallelization: true`" + `, or remove the explicit ` + "`false`" + ` entry to fall back to the default (true).'` — uses string concatenation to allow backtick characters inside the message without escaping issues, matches the indentation style of the existing envelopes (tabs).

    The envelope must be a peer return (`return { data: { ... } };`) of the existing three envelopes — identical TypeScript shape, just with the extra `message` key. NO try/catch wrapping (loadConfig has its own error handling); NO logging; NO throwing. The envelope follows the orchestrator-shell-friendly shape that downstream `jq` consumers expect (`jq -r '.ok // "true"'` then check for `false`).

    Do NOT add new files. Do NOT modify any other handler. Do NOT change the existing three envelopes. Preserve tab indentation (the file already uses 2-space — match the file's existing style; do not introduce tabs into a 2-space file).
  </action>
  <verify>
    <automated>cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts -t "parallelization_disabled"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "reason: 'parallelization_disabled'" sdk/src/query/workspace-parallel-dispatch.ts` equals 1
    - Source: `grep -c "import { loadConfig } from '../config.js';" sdk/src/query/workspace-parallel-dispatch.ts` equals 1
    - Source: `grep -c "config.parallelization === false" sdk/src/query/workspace-parallel-dispatch.ts` equals 1 (strict equality — NOT loose-falsey)
    - Source: `grep -c "!config.parallelization" sdk/src/query/workspace-parallel-dispatch.ts` equals 0 (negative assertion — the loose-falsey trap MUST NOT be used)
    - Source: `grep -c "await loadConfig(cwd)" sdk/src/query/workspace-parallel-dispatch.ts` equals 1 (uses `cwd`, not `projectDir`)
    - Source: `grep -c "message:" sdk/src/query/workspace-parallel-dispatch.ts` equals 1 (one new envelope with a message field; the other three peer envelopes do not carry messages)
    - Source: `grep -n "phase_number_required\|main_bookmark_required\|plan_required\|parallelization_disabled" sdk/src/query/workspace-parallel-dispatch.ts` shows the four envelopes in this order (the parallelization check is the FOURTH, placed AFTER the input checks)
    - Behavior: TSC type-check passes — `cd sdk && pnpm tsc --noEmit` exits 0
    - Behavior: Task 2 fixture tests pass (see Task 2 acceptance_criteria below — co-validates this task's implementation)
  </acceptance_criteria>
  <done>
    The CLI bridge has the fourth validation envelope (`parallelization_disabled`) with a guiding `message` field. The strict-equal-`false` check protects against the legacy nested-shape loose-falsey trap. TSC clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CONFIG-02 envelope contract tests (D-03 mitigation) to cmd-parallel-{jj,git}.test.ts</name>
  <files>
    sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
    sdk/src/vcs/__tests__/cmd-parallel-git.test.ts
  </files>
  <read_first>
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (the current test file structure; especially the top-of-file imports at lines 36-42 and the `for (const N of [2, 3, 4])` loop at line 96; the new describe block goes at end-of-file as a sibling of the existing top-level `describe.sequential.skipIf` blocks)
    - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts (parallel-sibling structure; imports at lines 55-61; same top-level describe layout)
    - sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts (the closest existing precedent for testing `workspaceParallelDispatchQuery` directly with a tmp `projectDir`; uses `vi.mock` for adapter recording — the new D-03 fixtures use FS instead, which is simpler because the envelope returns BEFORE the adapter call so no mock is needed)
    - .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md § D-03 (the mitigation rationale: in-the-wild invariant moves to test-fixture)
    - .planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md § Common Operation 2 (the exact fixture pattern)
    - .planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md § "sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts" (the per-file mutation detail + deviation notes)
  </read_first>
  <behavior>
    - Test 1 (per file): With `.planning/config.json` containing `{"parallelization": false}` in a fresh mkdtemp projectDir, calling `workspaceParallelDispatchQuery(['--phase','14','--main-bookmark','main','--plan','[{...}]'], tmpDir)` returns `{data: {ok: false, reason: 'parallelization_disabled', message: <string matching /parallelization: true/>}}`.
    - Test 2 (per file): With `.planning/config.json` NOT containing a `parallelization` key (e.g., `{"workflow": {}}`), the same call does NOT return the `parallelization_disabled` envelope — `data.reason` is undefined or different (the call proceeds past the parallelization check). Behavior assertion: `data.reason !== 'parallelization_disabled'` (the call may surface a different envelope or proceed to adapter dispatch; the assertion is only that THIS specific envelope did NOT fire).
    - Test 3 (per file): With `.planning/config.json` containing `{"parallelization": true}` explicitly, the same call does NOT return the `parallelization_disabled` envelope. Same assertion shape as Test 2.
  </behavior>
  <action>
    In BOTH `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` and `sdk/src/vcs/__tests__/cmd-parallel-git.test.ts`:

    1. **Extend the top-of-file import block** to add `mkdtemp, writeFile, mkdir` from `'node:fs/promises'` (the existing imports already use sync flavors from `node:fs`; the new fixture uses the promise flavors for cleaner async test code — alternatively reuse the existing sync flavors wrapped in `await Promise.resolve(...)` if the executor prefers consistency with the surrounding code).

    2. **Append a new top-level `describe('CONFIG-02 — parallelization_disabled', () => { ... })` block at the end of the file**, AFTER all existing `describe.sequential.skipIf` blocks. This new block is NOT inside the `for (const N of [2,3,4])` loop and NOT inside any other describe. It does NOT require `jjAvailable` / `gitAvailable` gating because the envelope returns BEFORE `createVcsAdapter()` is called — the test exercises ONLY the CLI bridge's envelope code path, which is backend-agnostic.

    3. **Inside the new describe, add THREE `it()` blocks** (per CONTEXT D-03 mitigation and RESEARCH § F Phase Requirements → Test Map lines 729-731 — three desirable cases):

       - it 1 — `it('returns {ok:false, reason:parallelization_disabled} when .planning/config.json has explicit false', async () => { ... })`: creates a fresh `mkdtemp` with prefix `gsd-cfg02-jj-` (in the jj test file) or `gsd-cfg02-git-` (in the git test file) — random suffix per Pattern B; writes `.planning/config.json` containing `JSON.stringify({ parallelization: false })`; dynamically imports `workspaceParallelDispatchQuery` from `'../../query/workspace-parallel-dispatch.js'`; calls it with argv `['--phase','14','--main-bookmark','main','--plan', JSON.stringify([{agentId:'a',planId:'p'}])]` and projectDir=tmpDir; asserts `data.ok === false`, `data.reason === 'parallelization_disabled'`, `typeof data.message === 'string'`, and `data.message` matches the regex `/parallelization: true/` (guides users to the unblock path).

       - it 2 — `it('does NOT fire envelope when .planning/config.json omits parallelization key', async () => { ... })`: same fixture setup but writes `JSON.stringify({ workflow: {} })` (no parallelization key — loadConfig's defaults-merge returns `true`); asserts `data.reason !== 'parallelization_disabled'` (the only behavior under test for this case; the call may fail later with a different envelope or proceed to adapter dispatch — that's fine).

       - it 3 — `it('does NOT fire envelope when .planning/config.json has explicit true', async () => { ... })`: same fixture setup but writes `JSON.stringify({ parallelization: true })`; same assertion as it 2.

    4. **Use `mkdtemp` from `node:fs/promises`** with a backend-distinct prefix (jj file uses `gsd-cfg02-jj-`, git file uses `gsd-cfg02-git-`) — random suffix from `Math.random().toString(36).slice(2, 10)` for Pattern B collision-resistance, matching the existing `setupJjRepo` / setup-helper convention in each file.

    5. **Do NOT add `jj git init --colocate`** in the new describe — the envelope returns BEFORE the adapter is created, so the tmp dir does NOT need to be a real jj/git repo. Just a `.planning/config.json` file inside a `.planning/` directory.

    6. **NO `vi.mock` setup** — the envelope returns before `createVcsAdapter`, so the real adapter is never reached; mocking is unnecessary.

    7. **Cleanup**: optional `rmSync` in an `afterAll` per describe — or just let the OS reap mktemp dirs (the existing tests in the same file do not aggressively clean up mktemp dirs created in the parallel scenarios; choose the convention that matches what's already in the file for consistency).

    8. **Tab indentation**: BOTH test files currently use tab indentation; preserve it.

    The byte-identical structure across the two files (with only the prefix label `jj` vs `git` differing) is intentional — CONTEXT D-03 says "the vitest contract tests `cmd-parallel-{jj,git}.test.ts` ... get one new fixture case each." The two files mirror each other.
  </action>
  <verify>
    <automated>cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts -t "CONFIG-02"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c "CONFIG-02 — parallelization_disabled" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` equals 1
    - Source: `grep -c "CONFIG-02 — parallelization_disabled" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` equals 1
    - Source: `grep -c "parallelization: false" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` is at least 1 (test 1 writes this in its fixture)
    - Source: `grep -c "parallelization: false" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` is at least 1
    - Source: each file has THREE `it(` calls inside the new `CONFIG-02 — parallelization_disabled` describe block — verify via: `awk '/CONFIG-02 — parallelization_disabled/,/^})/{print}' sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts | grep -c "it(" ` equals 3 (similar for git file)
    - Test command: `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts -t "CONFIG-02"` exits 0 with 6 passing tests (3 per file)
    - Test command: `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts -t "parallelization_disabled"` reports at least 1 passing test in this file
    - Test command: `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-git.test.ts -t "parallelization_disabled"` reports at least 1 passing test in this file
    - Behavior: The skip-count baseline is unchanged — `node scripts/check-skip-count.cjs` exits 0 (TEST-16 invariant: no new skips)
    - Source: prefixes match — `grep -c "gsd-cfg02-jj-" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` is at least 1 and `grep -c "gsd-cfg02-git-" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` is at least 1 (per-file backend-distinct mktemp prefix)
  </acceptance_criteria>
  <done>
    Both contract test files contain a new top-level `describe('CONFIG-02 — parallelization_disabled')` block with three `it()` cases. All six tests pass against the Task 1 envelope implementation. The D-03 in-the-wild invariant ("explicit false is preserved") is now codified at the test-fixture level.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → handler | `--cwd` / `--phase` / `--main-bookmark` / `--plan` / `--max-concurrency` flags from a shell consumer. |
| disk config → loadConfig | `.planning/config.json` is read by `loadConfig(cwd)`. Trust boundary is JSON parse + the strict-equal-`false` check. |
| test fixture → SDK handler | The new contract tests write `.planning/config.json` to a mktemp dir and call the handler directly with `projectDir=tmpDir`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-V5-cfg | Input Validation (V5) | `config.parallelization` boolean coercion in the new envelope | mitigate | Use strict equality (`config.parallelization === false`), NOT loose-falsey (`!config.parallelization`). Per RESEARCH finding #1, the SDK `loadConfig` does NOT normalize nested-shape; a brownfield repo with `{enabled: true, ...}` would loose-falsey-pass-through as truthy, but strict-equal-`false` keeps the failure mode unambiguous. Acceptance criteria above asserts the strict-equality check explicitly. Per RESEARCH § Security Domain row "V5 Input Validation". |
| T-14-V7-err | Error Handling (V7) | `message` field content | mitigate | The `message` field guides the user to two unblock paths (set `true` explicitly, or remove the explicit `false` entry). Does NOT leak filesystem paths, secrets, or sensitive config beyond the `parallelization` key. Per RESEARCH § Security Domain row "V7 Error Handling". |
| T-14-V12-tmp | Files and Resources (V12) | mkdtemp test fixtures (`gsd-cfg02-{jj,git}-`) | mitigate | All test fixtures use `mkdtemp` with random suffix (Pattern B); no predictable filename. Per RESEARCH § Security Domain row "V12 Files and Resources" + TEST-16 Pattern B. |
</threat_model>

<verification>
- `cd sdk && pnpm tsc --noEmit` passes (type-check clean).
- `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` exits 0 (full file passes, including the new CONFIG-02 describes).
- `cd sdk && pnpm test:unit` exits 0 (full unit suite — no regressions).
- `node scripts/check-skip-count.cjs` exits 0 (TEST-16 baseline preserved — no new skips).
- The strict-equal-`false` check is enforced in source (grep gates above).
- The `message` field is non-empty and matches the regex `/parallelization: true/` (guides users to the unblock path).
</verification>

<success_criteria>
- CONFIG-02 implemented at the CLI bridge per D-06 + D-07 + D-08.
- D-03 mitigation tests in place: the in-the-wild "explicit false is preserved" invariant moves from this-repo's `.planning/config.json` to contract-test fixtures.
- Strict-equal-`false` check per RESEARCH finding #1 prevents legacy nested-shape loose-falsey misfire.
- TSC clean; full unit suite green; no skip-count regression.
</success_criteria>

<output>
Create `.planning/phases/14-default-flip-dogfood-validation/14-02-SUMMARY.md` when done documenting:
- Exact insertion point of the new envelope in `workspace-parallel-dispatch.ts` (line range diff)
- The exact `message` string shipped (so downstream docs / metrics-file can quote it)
- Test counts: 3 per file × 2 files = 6 new passing tests
- Confirmation `pnpm test:unit` is green
- Confirmation strict-equal-`false` check is present (not loose-falsey)
</output>
