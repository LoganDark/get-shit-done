---
phase: 14-default-flip-dogfood-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/templates/config.json
  - .planning/config.json
autonomous: true
requirements: [CONFIG-01]
requirements_addressed: [CONFIG-01]
tags: [config, parallelization, brownfield-flip]
must_haves:
  truths:
    - "Install template `get-shit-done/templates/config.json` declares `parallelization: true` as a flat boolean (not nested)"
    - "This repo's `.planning/config.json` declares `parallelization: true` (was `false`)"
    - "The `feat-3167` template-parsing test still passes (asserts only on `template.ship.pr_body_sections`)"
    - "Greenfield repos initialized post-flip get `parallelization: true` from the template; existing repos with explicit `false` keep `false` via `loadConfig` defaults-merge (no migration command needed)"
    - "D-05: No Phase-6-style greenfield/brownfield boundary added for parallelization; `loadConfig`'s defaults-merge of `CONFIG_DEFAULTS.parallelization = true` already provides the required semantic — greenfield repos get `true` by default, existing repos with explicit `false` keep `false`, and no migration command is introduced"
  artifacts:
    - path: "get-shit-done/templates/config.json"
      provides: "Install template with flat-boolean `parallelization: true`"
      contains: '"parallelization": true,'
    - path: ".planning/config.json"
      provides: "This repo's runtime config flipped to `parallelization: true`"
      contains: '"parallelization": true,'
  key_links:
    - from: "get-shit-done/templates/config.json"
      to: "tests/feat-3167-ship-pr-body-sections.test.cjs:135"
      via: "JSON.parse + deepEqual on template.ship.pr_body_sections"
      pattern: "template\\.ship\\.pr_body_sections"
    - from: ".planning/config.json"
      to: "sdk/src/config.ts loadConfig"
      via: "defaults-merge read at every gsd-sdk query invocation"
      pattern: "parallelization"
---

<objective>
Flip the default for the `parallelization` config knob from `false` to `true` at two sites:
1. **Install template** (`get-shit-done/templates/config.json` lines 31-38) — flatten the nested upstream-stub block `{enabled, plan_level, task_level, skip_checkpoints, max_concurrent_agents, min_plans_for_parallel}` to the flat boolean `true` that matches the SDK schema (`sdk/src/config.ts:71` and `core.cjs:311`).
2. **This-repo runtime** (`.planning/config.json:4`) — change the `false` to `true` so the Phase 14 dogfood (Plan 05) can actually dispatch against THIS repo's colocated jj.

This implements CONFIG-01 (per D-04 + D-03). The mitigation test fixture replacing the in-the-wild "explicit `false` is preserved" invariant lives in Plan 02 (cmd-parallel-{jj,git}.test.ts new fixture cases).

Purpose: Defaults flip is the user-observable success signal of v1.3. Greenfield repos get parallelization on by default; existing repos with explicit `false` keep `false` via loadConfig defaults-merge (D-05 — no Phase-6-style sticky-config boundary needed). This repo's brownfield flip is a permanent change accepting the trade-off that the in-the-wild invariant moves to the test fixture (mitigation in Plan 02).

Output: Two modified config files. No new files, no new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md
@.planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md
@.planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md

<interfaces>
<!-- The SDK schema is the contract that the template flatten matches. -->
<!-- Executor uses these to verify the post-flatten shape. -->

From sdk/src/config.ts:71 (parallelization field declaration):
```typescript
parallelization: boolean;
```

From sdk/src/config.ts:92 (default value):
```typescript
parallelization: true,
```

From get-shit-done/bin/lib/core.cjs:311 (CJS default parity):
```javascript
parallelization: true,
```

From get-shit-done/bin/lib/core.cjs:480-485 (CJS-side shape normalization — exists for grandfathered nested-shape brownfield repos, NOT needed for the SDK loader):
```javascript
const parallelization = (() => {
  const val = get('parallelization');
  if (typeof val === 'boolean') return val;
  if (typeof val === 'object' && val !== null && 'enabled' in val) return val.enabled;
  return defaults.parallelization;
})();
```

From tests/feat-3167-ship-pr-body-sections.test.cjs:135-136 (the only programmatic consumer of the template):
```javascript
const template = JSON.parse(readRepoFile('get-shit-done/templates/config.json'));
assert.deepEqual(template.ship.pr_body_sections, []);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten template parallelization block (D-04)</name>
  <files>get-shit-done/templates/config.json</files>
  <read_first>
    - get-shit-done/templates/config.json (current nested-block shape at lines 31-38)
    - sdk/src/config.ts:71 (target schema: `parallelization: boolean`)
    - get-shit-done/bin/lib/core.cjs:311 (CJS default value parity)
    - tests/feat-3167-ship-pr-body-sections.test.cjs (the only test that JSON.parses this file — confirms it asserts only on template.ship.pr_body_sections, untouched at line 24)
    - .planning/phases/14-default-flip-dogfood-validation/14-PATTERNS.md § "get-shit-done/templates/config.json" (mutation site detail)
  </read_first>
  <action>
    Replace the nested `parallelization` value (currently lines 31-38, a 6-key object: `enabled`, `plan_level`, `task_level`, `skip_checkpoints`, `max_concurrent_agents`, `min_plans_for_parallel`) with the flat boolean literal `true`. The replacement keeps the key `"parallelization"` at the same indentation as its neighbors (`"planning"` above and `"gates"` below); only the VALUE collapses from the multi-line object to the bare token `true`. Preserve the trailing comma after `true` (since `"gates"` follows). All other top-level keys remain untouched. Per D-04 rationale, the SDK schema is `parallelization: boolean` (sdk/src/config.ts:71); the nested-block shape was inherited from upstream on 2026-01-12 and is INERT at init because `core.cjs:480-485` normalizes both shapes — flattening makes the template match the schema and the CJS default (`core.cjs:311`). No trailing-comma additions, no key re-ordering, no whitespace changes outside the replaced lines. This implements D-05 implicitly (no greenfield/brownfield boundary — `loadConfig` defaults-merge is the only mechanism).
  </action>
  <verify>
    <automated>node --test tests/feat-3167-ship-pr-body-sections.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '"parallelization": true,' get-shit-done/templates/config.json` equals 1
    - Source: `grep -c '"enabled": true,' get-shit-done/templates/config.json` equals 0 (the nested-block `enabled` key is gone)
    - Source: `grep -c 'max_concurrent_agents' get-shit-done/templates/config.json` equals 0 (no nested fields survive)
    - Source: `grep -c '"pr_body_sections"' get-shit-done/templates/config.json` equals 1 (the `ship.pr_body_sections` array still exists — untouched by D-04)
    - Test command: `node --test tests/feat-3167-ship-pr-body-sections.test.cjs` exits 0
    - Behavior: `node -e "const fs=require('fs'); const t=JSON.parse(fs.readFileSync('get-shit-done/templates/config.json','utf8')); if (t.parallelization !== true) process.exit(1); if (!Array.isArray(t.ship.pr_body_sections)) process.exit(2);"` exits 0
    - Source: `node -e "JSON.parse(require('fs').readFileSync('get-shit-done/templates/config.json','utf8'))"` exits 0 (valid JSON; no trailing-comma syntax errors)
  </acceptance_criteria>
  <done>
    The install template declares `"parallelization": true,` as a flat boolean. The feat-3167 test (the only programmatic consumer) still passes. JSON validity preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip this-repo parallelization to true (D-03)</name>
  <files>.planning/config.json</files>
  <read_first>
    - .planning/config.json (current state: `"parallelization": false,` at line 4)
    - .planning/phases/14-default-flip-dogfood-validation/14-CONTEXT.md § D-03 (the brownfield-flip rationale; the mitigation test plan)
    - .planning/phases/14-default-flip-dogfood-validation/14-RESEARCH.md § A "Runtime State Inventory" lines 360-372 (confirms no cache/daemon/installed-artifact needs reconciliation — flip is a single-token file edit)
  </read_first>
  <action>
    Change `.planning/config.json` line 4 from `"parallelization": false,` to `"parallelization": true,`. Single-token edit; all other keys in the file remain untouched (`vcs.adapter`, `workflow.*`, `commit_docs`, etc.). Per D-03, this is THE pre-step the Phase 14 dogfood (Plan 05) needs in order to actually dispatch — without it, the CONFIG-02 envelope from Plan 02 will refuse with `{ok:false, reason:'parallelization_disabled'}`. The trade-off (losing the in-the-wild "explicit `false` is preserved" CONFIG-01 invariant fixture) is mitigated by the contract-test fixtures Plan 02 adds to `cmd-parallel-{jj,git}.test.ts`. The flip is permanent (no rollback intent). This is a brownfield-flip of an existing repo — `loadConfig`'s defaults-merge reads the file fresh on every `gsd-sdk query` invocation, so no cache/daemon reconciliation is required.
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('.planning/config.json','utf8')); if (c.parallelization !== true) { console.error('FAIL: parallelization is', c.parallelization); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '"parallelization": true,' .planning/config.json` equals 1
    - Source: `grep -c '"parallelization": false,' .planning/config.json` equals 0
    - Behavior: `node -e "console.log(JSON.parse(require('fs').readFileSync('.planning/config.json','utf8')).parallelization)"` prints `true`
    - Source: `node -e "JSON.parse(require('fs').readFileSync('.planning/config.json','utf8'))"` exits 0 (still valid JSON)
    - Source: All other top-level keys preserved — `node -e "const k=Object.keys(JSON.parse(require('fs').readFileSync('.planning/config.json','utf8'))).sort(); process.stdout.write(k.join(','))"` produces the same key set as the pre-edit file modulo no key changes (manual check via git diff: only line 4 changes)
  </acceptance_criteria>
  <done>
    This repo's runtime config declares `"parallelization": true`. `loadConfig` will return `true` on the next SDK invocation. The CONFIG-02 pre-flight envelope (Plan 02) will no longer refuse dispatch against this repo.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk-config → SDK loader | `.planning/config.json` + `get-shit-done/templates/config.json` are read by `loadConfig` (SDK) and `buildNewProjectConfig` (CJS install path). Trust boundary is the JSON parser. |
| install template → new-project config | `cmdConfigNewProject` copies the template into a new project's `.planning/config.json` at `gsd init` time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-V5-cfg | Input Validation (V5) | JSON shape of `.planning/config.json` + template | mitigate | Both edits preserve valid JSON; the only changed token is the value of `parallelization`. The `feat-3167` test exercises the template's JSON parse + `.ship.pr_body_sections` invariant. RESEARCH § Security Domain row "V5 Input Validation" confirms boolean-coercion is via `=== false` strict equality (Plan 02 envelope), eliminating string-truthy ambiguity. |
| T-14-V12-cfg | Files and Resources (V12) | static config files in repo | accept | These are checked-in source files; no temp-file races. Atomic file write (Write tool) is sufficient. |
| T-14-Repud | Repudiation (config-flip surprise) | greenfield projects post-flip get parallelization on by default | accept | This is the INTENT of v1.3 per ROADMAP SC1. The CONFIG-02 envelope (Plan 02) gives operators a clear refusal path; PROJECT.md / REQUIREMENTS.md document the new default. RESEARCH § Security Domain row covers this. |
</threat_model>

<verification>
- Both files remain valid JSON after the edits (`node -e "JSON.parse(...)"` exits 0).
- The `feat-3167` template-parsing test stays green: `node --test tests/feat-3167-ship-pr-body-sections.test.cjs`.
- `node -e "console.log(JSON.parse(require('fs').readFileSync('.planning/config.json','utf8')).parallelization)"` prints `true`.
- `node -e "console.log(JSON.parse(require('fs').readFileSync('get-shit-done/templates/config.json','utf8')).parallelization)"` prints `true` (boolean, not object).
- No new files. No new dependencies.
</verification>

<success_criteria>
- CONFIG-01 (D-04 + D-03 portion) implemented: install template + this-repo runtime both declare `parallelization: true` as a flat boolean.
- `feat-3167` test green (the only programmatic template consumer).
- Both config files parse as valid JSON.
- D-05 honored: no Phase-6-style sticky-config boundary added; `loadConfig` defaults-merge is the sole mechanism.
</success_criteria>

<output>
Create `.planning/phases/14-default-flip-dogfood-validation/14-01-SUMMARY.md` when done documenting:
- Diff summary for both files (single-token / single-block changes)
- Confirmation `feat-3167` test passes
- Confirmation both files are valid JSON
- D-05 non-action note (no greenfield/brownfield boundary added)
</output>
</content>
</invoke>