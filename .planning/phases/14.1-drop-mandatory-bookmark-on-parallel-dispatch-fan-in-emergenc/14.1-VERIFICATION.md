---
phase: 14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc
verified: 2026-05-24T21:51:26Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 14.1: Drop mandatory bookmark on parallel dispatch + fan-in (emergency) Verification Report

**Phase Goal:** Remove the required `mainBookmark: string` field from `ParallelDispatchOpts` / `ParallelDispatchHandle` across both backends, replacing it with optional `mainBookmarks?: readonly string[]` (default `[]` → no advance). Fan-in still merges into `@` (jj) / current HEAD (git — detached or branch). Only the *advance* step becomes opt-in. Drop the workflow's over-broad `current-branch` FATAL preflight so bookmark-less jj working copies and detached-HEAD git working copies are both first-class.
**Verified:** 2026-05-24T21:51:26Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `ParallelDispatchOpts.mainBookmark` and `ParallelDispatchHandle.mainBookmark` removed; `mainBookmarks?: readonly string[]` is the only field (CF-01 hard rename) | VERIFIED | `sdk/src/vcs/types.ts:478` (`mainBookmarks?: readonly string[]` on ParallelDispatchOpts) + `:515` (same on ParallelDispatchHandle); grep finds no `mainBookmark:` in either interface |
| 2 | Dispatch+fan-in succeeds on bookmark-less jj `@` with mainBookmarks omitted/[]; zero `jj bookmark set` invocations | VERIFIED | `cmd-parallel-jj.test.ts:475-547` SC5 scenario 1 passes; setupJjRepo() bookmark-less by construction; test asserts merge lands at `@` and zero bookmark set invocations |
| 3 | Dispatch+fan-in succeeds on detached-HEAD git with mainBookmarks omitted/[]; zero `git update-ref` invocations; merge lands into HEAD itself | VERIFIED | `cmd-parallel-git.test.ts:737-822` SC5 scenario 1 passes; `setupGitRepoDetached()` helper at :727-735 runs `git checkout --detach HEAD` |
| 4 | Non-empty `mainBookmarks` list with one invalid name throws BEFORE any side effect (CF-02 all-or-nothing pre-validation) | VERIFIED | jj test:629-699 scenario 3 (`bad ref` jj rejection) + git test:894-958 scenario 3 (`bad..ref` git-validateRefname rejection); both verify pre-/post-state of valid names is identical |
| 5 | `current-branch` FATAL preflight REMOVED from BOTH execute-phase.md AND quick.md (D-01 symmetric) | VERIFIED | grep `FATAL: orchestrator is on detached HEAD` and `main_bookmark_required` return 0 hits in both files; `if [ -z "$EXPECTED_BRANCH" ] || [ "$EXPECTED_BRANCH" = "HEAD" ]` block absent in both; `--main-bookmark` flag removed from dispatch invocations (execute-phase.md:559-561 + quick.md:681-683) |
| 6 | `tests/quick-md-parallel-dispatch.test.cjs` CR-03 polarity FLIPPED — assertions read "preflight ABSENT" in both files | VERIFIED | `assert.doesNotMatch` at lines 95, 96, 100, 101 against the same EXPECTED_BRANCH preflight regex pair; byte-identical FATAL-message symmetry guard dropped (only comment trace remains); 9/9 node-test pass |
| 7 | `WorkspaceMergeOpts.mainBookmark` (types.ts:226) UNTOUCHED (D-03 STRICT OOS); MERGE-08 deferred-item REQ-ID FILED in REQUIREMENTS.md + STATE.md | VERIFIED | `types.ts:226` still reads `mainBookmark: string;  // D-03 atomic main-advance: REQUIRED`; REQUIREMENTS.md:39 contains MERGE-08 entry with full rationale; REQUIREMENTS.md:105 traceability row `| MERGE-08 | (deferred — v1.5+) | Deferred |`; STATE.md:231 Deferred Items table row present |
| 8 | ROADMAP.md Phase 14.1 SC1 `:226` typo struck; SC4 (line 252) + Touches (line 258) amended to add quick.md | VERIFIED | ROADMAP.md:249 reads `types.ts:467,496` (no `:226`); :252 mentions both `workflows/execute-phase.md AND workflows/quick.md`; :258 Touches list contains `workflows/quick.md`, `workspace-parallel-dispatch.ts`, `tests/quick-md-parallel-dispatch.test.cjs` |
| 9 | CLI flag shape STAYS singular `--main-bookmark`; bridge accumulates into `mainBookmarks: string[]` (D-02 repeated-singular accumulator) | VERIFIED | `workspace-parallel-dispatch.ts:61` `const mainBookmarks: string[] = []`; :70-71 `--main-bookmark` parser pushes into array; idiom precedent cited in JSDoc :18-21 (gh / git -c) |
| 10 | CLI envelope reason `main_bookmark_required` DELETED (D-02); every other reason token unchanged | VERIFIED | `workspace-parallel-dispatch.ts:82-87` only carries `phase_number_required` and `plan_required`; no `main_bookmark_required` exists anywhere in the bridge body; comment at :82-84 documents the deletion |
| 11 | TSC closes green in sdk/ after all consumer edits (CF-01 audit trail — no missed sites) | VERIFIED | `cd sdk && pnpm tsc --noEmit` exits 0 (verified by re-running during verification) |
| 12 | Lint allowlist net diff is +0 (CF-06) | VERIFIED | `scripts/lint-vcs-no-raw-git.cjs` exits 0 — 1092 files scanned, 0 violations (matches SUMMARY claim) |

**Score:** 12/12 truths verified

### Roadmap Success Criteria Coverage (ROADMAP.md Phase 14.1 SC1-SC6)

| SC | Description | Status | Evidence |
|----|-------------|--------|----------|
| SC1 | `ParallelDispatchOpts.mainBookmark` + `ParallelDispatchHandle.mainBookmark` removed → optional `mainBookmarks?: readonly string[]`; SDK type-checks cleanly; WorkspaceMergeOpts.mainBookmark OOS | VERIFIED | Truth 1 + Truth 7 + Truth 11 evidence stack |
| SC2 | jj fan-in skips `jj bookmark set` when list empty/omitted; non-empty → all-or-nothing pre-validation BEFORE any advance | VERIFIED | `jj/parallel.ts:428-449` two-pass loop (lines 432-434 Pass 1 validate, lines 437-448 Pass 2 advance); CF-02 verified by test scenarios 1 (skip when empty) + 3 (all-or-nothing throw on invalid) |
| SC3 | git fan-in works on detached HEAD without error; `git merge --no-ff` already detached-HEAD-safe; non-empty list advances via `git update-ref refs/heads/<name> HEAD` | VERIFIED | `git/parallel.ts:464-501` STEP 1.5 post-merge per-name update-ref loop; guards on `mainBookmarks.length > 0 && !conflicted`; SC5 scenario 1 (cmd-parallel-git.test.ts:753) asserts merge lands into detached HEAD with no branch advances |
| SC4 | Both workflow files drop `current-branch` FATAL preflight in lockstep; dispatch invocations omit `--main-bookmark` | VERIFIED | Truth 5 evidence; both workflow files updated symmetrically per D-01 (per Phase 11-08 CR-03 precedent) |
| SC5 | Cross-backend tests cover (a) empty list + bookmark-less jj/detached-HEAD git, (b) non-empty advance, (c) all-or-nothing rejection | VERIFIED | 6 new SC5 scenarios shipped — 3 per backend; all pass (jj:13/13 + git:14/14 = 27/27 vitest); jj uses `bad ref` (space) and git uses `bad..ref` (consecutive dots) per the documented validator-class mismatch |
| SC6 | PARALLEL-07 cancel verb (Phase 15.04) does NOT inherit bookmark requirement; updated if necessary | VERIFIED (with WARNING) | ROADMAP.md:254 codifies the requirement; Phase 15.04 PLAN (15-04-PLAN.md) describes cancel signature as `cancel(handle): CancelResult` with no bookmark coupling — verb itself does not inherit. **Caveat:** the plan's test scenarios at line 404 still use the OLD `mainBookmark: 'main'` shape in dispatch setup; this will surface when 15.04 executes. See WARNING note below. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `sdk/src/vcs/types.ts` | ParallelDispatchOpts.mainBookmarks? + ParallelDispatchHandle.mainBookmarks? | VERIFIED | Lines 478 + 515 have `mainBookmarks?: readonly string[]` with full JSDoc explaining empty/omitted/non-empty semantics |
| `sdk/src/vcs/jj/parallel.ts` | All-or-nothing two-pass validate-then-advance loop in fan-in clean path | VERIFIED | Lines 428-449 implement the two-pass loop; validateMainBookmark helper at :120 kept and reused; Object.freeze CF-05 wrap at :251 |
| `sdk/src/vcs/git/parallel.ts` | Post-merge per-name update-ref loop guarded on non-empty + !conflicted | VERIFIED | Lines 464-501 (STEP 1.5); imports validateRefname at :98; manifest writer drop applied at :233-237 (other rows kept for parity); Object.freeze at :260 |
| `sdk/src/query/workspace-parallel-dispatch.ts` | Repeated-singular --main-bookmark accumulator + dropped envelope path | VERIFIED | Lines 59-77 parser body; main_bookmark_required path deleted at :82-87; JSDoc rewritten at :2-39 to describe new semantic |
| `get-shit-done/workflows/execute-phase.md` | Dispatch block WITHOUT current-branch FATAL preflight; dispatch invocation WITHOUT --main-bookmark | VERIFIED | Lines 527-562 contain the new dispatch block; FATAL block absent; --main-bookmark absent from dispatch invocation; EXPECTED_BRANCH= assignment kept for downstream drift guard |
| `get-shit-done/workflows/quick.md` | Same symmetric edit | VERIFIED | Lines 665-689 contain the new dispatch block; same drops applied symmetrically |
| `tests/quick-md-parallel-dispatch.test.cjs` | Polarity-flipped CR-03 lint asserting preflight ABSENT in both files | VERIFIED | Lines 93-103 describe block uses `assert.doesNotMatch` × 4 (2 in quick.md test, 2 in execute-phase.md test); 9/9 node-test pass |
| `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` | 3 new describe blocks per SC5 (jj backend) | VERIFIED | Lines 461-700 contain 3 SC5 describe blocks; 6 mechanical-preserve migrations at :134,255,356; 13/13 vitest pass; uses `bad ref` (jj-validator-rejects-space) in scenario 3 |
| `sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` | 3 new describe blocks per SC5 (git backend) | VERIFIED | Lines 716-958 contain 3 SC5 describe blocks; setupGitRepoDetached() helper at :727; 6 mechanical-preserve migrations at :152,235,321,409,554,672; 14/14 vitest pass; uses `bad..ref` (git-validateRefname-rejects-double-dot) in scenario 3 |
| `.planning/REQUIREMENTS.md` | MERGE-08 deferred-item entry | VERIFIED | Line 39 contains full MERGE-08 entry with rationale; line 105 traceability row `| MERGE-08 | (deferred — v1.5+) | Deferred |` |
| `.planning/STATE.md` | Deferred Items table row for MERGE-08 | VERIFIED | Line 231 contains the row with paired-with-PARALLEL-08 rationale |
| `.planning/ROADMAP.md` | Phase 14.1 SC1 amendment + SC4/Touches amendments adding quick.md | VERIFIED | Line 249 reads `:467,496` (no `:226` typo); line 252 mentions BOTH `workflows/execute-phase.md AND workflows/quick.md`; line 258 Touches list contains both new files |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `sdk/src/query/workspace-parallel-dispatch.ts` | `sdk/src/vcs/types.ts (ParallelDispatchOpts.mainBookmarks)` | `dispatch({mainBookmarks, ...})` call | WIRED | Line 118-123 dispatch call passes `mainBookmarks` accumulated from argv |
| `sdk/src/vcs/jj/parallel.ts (fan-in)` | `handle.mainBookmarks` | destructure + two-pass validate+advance loop | WIRED | Line 428 `const mainBookmarks = handle.mainBookmarks ?? [];` then conditional non-empty branch at :429-449 |
| `sdk/src/vcs/git/parallel.ts (fan-in)` | `handle.mainBookmarks` | post-merge update-ref loop | WIRED | Line 486 `const mainBookmarks = handle.mainBookmarks ?? [];` then conditional !conflicted+non-empty branch at :487-501 invoking `git update-ref refs/heads/<name> HEAD` |
| `tests/quick-md-parallel-dispatch.test.cjs` | `get-shit-done/workflows/{execute-phase,quick}.md` | static-text lint asserting preflight ABSENT | WIRED | Lines 95, 96, 100, 101 use `assert.doesNotMatch` against the EXPECTED_BRANCH preflight regex AND the FATAL message regex |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `jj/parallel.ts (Handle)` | `mainBookmarks` field | `Object.freeze([...(mainBookmarks ?? [])])` from `opts.mainBookmarks` | YES — destructured from caller at :176, propagated via Handle to fan-in at :428 | FLOWING |
| `git/parallel.ts (Handle)` | `mainBookmarks` field | `Object.freeze([...(mainBookmarks ?? [])])` from `opts.mainBookmarks` | YES — destructured from caller at :150, propagated via Handle to fan-in at :486 | FLOWING |
| `workspace-parallel-dispatch.ts` | `mainBookmarks` array | repeated `args[++i]` accumulator at :71 | YES — passes through to `vcs.workspace.parallel.dispatch()` call at :118-123 | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TSC closes green in sdk/ | `cd sdk && pnpm tsc --noEmit; echo EXIT=$?` | `EXIT=0` | PASS |
| node-test 9/9 on CR-03 lint | `node --test tests/quick-md-parallel-dispatch.test.cjs` | 9 pass / 0 fail | PASS |
| vitest 13/13 on cmd-parallel-jj | `pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts` | 13 pass / 0 fail | PASS |
| vitest 14/14 on cmd-parallel-git | `pnpm vitest run src/vcs/__tests__/cmd-parallel-git.test.ts` | 14 pass / 0 fail | PASS |
| vitest on 3 forced-touch test files (run individually-acceptable) | `pnpm vitest run cmd-parallel-max-concurrency-{cli,adapter}.test.ts cmd-workspace-assert-dispatched-cwd.test.ts` | 10 pass / 0 fail when run individually | PASS |
| jj-workspace.test.ts (workspace.merge consumer; D-03 OOS) | `pnpm vitest run src/vcs/__tests__/jj-workspace.test.ts` | 24 pass / 0 fail | PASS |
| Legacy CJS shim tests (bug-3384 + wave-cleanup-executor) | `node --test tests/wave-cleanup-executor.test.cjs tests/bug-3384-worktree-cleanup-manifest.test.cjs` | 10 pass / 0 fail | PASS |
| lint-vcs-no-raw-git (1092 files / 0 violations claim) | `node scripts/lint-vcs-no-raw-git.cjs` | `ok lint-vcs-no-raw-git: 1092 files scanned, 0 violations` | PASS |
| lint-vcs-no-commit-id | `node scripts/lint-vcs-no-commit-id.cjs` | `0 violations` | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PARALLEL-08 | 14.1-01-PLAN.md | Parallel-dispatch bookmark optionality + detached-HEAD support | SATISFIED | REQUIREMENTS.md:38 marked `[x]` complete; traceability table at :104 shows `| PARALLEL-08 | Phase 14.1 (plan 14.1-01) | Complete |`; all 6 SC verified above |
| MERGE-08 (paired deferred) | — (deferred, filed in this phase) | WorkspaceMergeOpts.mainBookmark deferred-by-design (D-03 STRICT OOS) | SATISFIED | REQUIREMENTS.md:39 entry + traceability row at :105 `| MERGE-08 | (deferred — v1.5+) | Deferred |`; STATE.md:231 Deferred Items table row |

No orphan requirements — every PARALLEL-08 SC mapped to verified evidence.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `get-shit-done/bin/lib/worktree-safety.cjs` | 409, 484 | Silent dead `mainBookmark` writes in `reconstructHandleFromLegacyPlan` | Info | Per CONTEXT Option A — `.cjs` runtime (no TSC), zero live runtime consumers; documented in SUMMARY as intentional residue. Not a regression. |
| `.planning/phases/15-adapter-surface-extensions-rename/15-04-PLAN.md` | 151, 404 | Stale `mainBookmark: 'main'` references in future-phase plan documentation | Info | Documentation drift in NOT-YET-EXECUTED Phase 15.04 plan. Will surface as TSC red when Phase 15.04 executor runs and writes the corresponding test code. See WARNING note below. |

No critical or warning-level anti-patterns introduced by Phase 14.1 changes. No TBD/FIXME/XXX debt markers in any of the 14 files modified by this phase.

### Code Review Findings (informational — REVIEW.md)

Per the verification focus instructions, the code review surfaced **0 critical, 4 warnings, 5 info** items — these are advisory follow-ups, NOT must-haves. Recorded here for traceability:

- **WR-01** (`jj/parallel.ts:428-434`): Validation moved into fan-in runs AFTER the N-parent `jj new` side effect. The same defect mirrors on git side at `git/parallel.ts:486-490` (runs after per-branch merge loop). Test scenarios pin "no partial advance of bookmarks" but NOT "no merge change created at @". Class: correctness; resolution path: shift validation to top of fan-in before workspace.list().
- **WR-02** (`git/parallel.ts:386-422` vs `jj/parallel.ts:335-350`): Cross-backend asymmetry — git `merge --no-ff` implicitly advances HEAD-tracked branch as side effect, jj N-parent `jj new` does not. Class: cross-backend contract; resolution path: detach HEAD before per-branch merge loop OR document the leak in scenario 1 test.
- **WR-03** (`jj/parallel.ts:389,408`): Dead self-assignment `surplusBookmarks = []` reassigns to the already-empty initialization. Class: quality; resolution: convert `let` to `const`, remove reassignment.
- **WR-04** (`cmd-parallel-max-concurrency-cli.test.ts:43-56`): No direct test that `--main-bookmark` CLI flags arrive on `ParallelDispatchOpts.mainBookmarks`. Class: test-coverage gap; resolution: extend mock-adapter recording to capture `mainBookmarks`.
- **IN-01 .. IN-05**: 5 info-tier items covering stale comments, validator-prefix carry-over, pre-existing EXPECTED_BRANCH no-op (Pitfall 6 documented), test-scenario-3 merge-rollback probes gap.

These advisory items are out of scope for Phase 14.1 closure per the verification focus and may be folded into Phase 18 CR-followups (see STATE.md `v14-review-followups` todo).

### Warnings (verifier-surfaced)

**Forward-looking — Phase 15.04 plan stale references (SC6 caveat).** The Phase 15.04 plan at `.planning/phases/15-adapter-surface-extensions-rename/15-04-PLAN.md` contains two references to the OLD `mainBookmark` shape that Phase 14.1 retired:

- Line 151: Type-extract reference reading `mainBookmark: string;` (pseudo-code documentation of the OLD `ParallelDispatchHandle` shape)
- Line 404: Test scenario `vcs.workspace.parallel.dispatch({..., mainBookmark:'main'})` in cancel-clean-abandon-N=2 scenario

The SUMMARY claims (line 184-185) that "Phase 15.04 plan needs NO amendment." This is correct in that the **cancel verb itself** does not inherit the bookmark requirement (the verb signature `cancel(handle): CancelResult` is bookmark-free per Phase 15 D-01). However, **the plan's test scenario at line 404 will fail TSC** when Phase 15.04 executes — `mainBookmark` is no longer a property on `ParallelDispatchOpts`. The Phase 15.04 executor will need to translate `mainBookmark: 'main'` → `mainBookmarks: ['main']` (or simply omit) when authoring the test code.

**Risk:** LOW — this is forward-looking plan documentation drift. The TSC failure will surface immediately when 15.04 is executed; the fix is mechanical (1-2 character substitution). Does NOT affect Phase 14.1 codebase correctness.

**Recommendation:** Either patch 15-04-PLAN.md preemptively (1 line edit each at :151 and :404 to use `mainBookmarks?: readonly string[]` / `mainBookmarks: ['main']`), or defer to the 15.04 executor (per SUMMARY's stance). No action required for Phase 14.1 closure.

### Anti-Pattern Notes (informational)

- **Intermittent test flake under concurrent load:** `cmd-workspace-assert-dispatched-cwd.test.ts` `beforeAll` hook intermittently times out when all 5 forced-touch test files run in the same vitest worker pool. Same class as `v14-jj-reap-test-flake` (TEST-17, scheduled Phase 18). The test passes cleanly when run alone (5 pass / 0 fail, 6.5s). Phase 14.1 SUMMARY documents this in "Issues Encountered" as out-of-scope per Pitfall 9 (per-test fix only). NOT a Phase 14.1 regression.
- **Pre-existing unrelated lint failure:** `tests/lint-vcs-no-raw-git-fixture.test.cjs` fails `lint-no-source-grep.cjs` with one violation (assert.match/doesNotMatch on .stdout/.stderr). This file is NOT in Phase 14.1's scope and predates this phase. Documented here only because the verifier ran the lint as part of broad health-check; flagged for whoever owns the no-source-grep cleanup.

### Gaps Summary

**No gaps.** All 12 must-haves verified directly against the codebase. All 6 ROADMAP Success Criteria SC1-SC6 hold. All 14 files modified per the plan exist with the expected content. TSC closes green. Vitest 27/27 + node-test 9/9 + lint 1092/0 all match the SUMMARY's claims independently. PARALLEL-08 traced to Complete in REQUIREMENTS.md traceability table; MERGE-08 filed as Deferred in both REQUIREMENTS.md and STATE.md per the D-03 STRICT OOS decision.

The two informational warnings (15.04 plan stale references, pre-existing test flake under concurrent load) do NOT block Phase 14.1 closure — they are forward-looking concerns that will surface during their respective downstream phases (15.04 execution + 18 TEST-17 fix).

The 4 code-review WR-* warnings are advisory follow-ups per the verification focus instructions, NOT must-haves, and may be folded into Phase 18 CR-followups.

---

*Verified: 2026-05-24T21:51:26Z*
*Verifier: Claude (gsd-verifier)*
