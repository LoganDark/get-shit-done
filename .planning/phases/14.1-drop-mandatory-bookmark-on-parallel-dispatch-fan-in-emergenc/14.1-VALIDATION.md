---
phase: 14.1
slug: drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-24
---

# Phase 14.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (sdk/) + node:test (tests/) |
| **Config file** | sdk/vitest.config.ts; tests/ uses node --test |
| **Quick run command** | `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` |
| **Full suite command** | `cd sdk && pnpm vitest run && cd .. && node --test tests/quick-md-parallel-dispatch.test.cjs` |
| **Estimated runtime** | ~30s quick (2 files); ~3-5 min full sdk vitest |

---

## Sampling Rate

- **After every task commit:** Run `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` (the 2 roadmap-listed test files; ~30s)
- **After every plan wave:** Run full suite (only one wave; this is the final-wave check)
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds per-task

---

## Per-Task Verification Map

> Tasks are placeholders here — the planner fills in concrete `{N}-01-NN` IDs once PLAN.md is written. Verification surface is fixed by PARALLEL-08 SC1-6 + CONTEXT.md D-01..D-04.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 14.1-01-T-types | 01 | 1 | PARALLEL-08 SC1 | — | `mainBookmark` field removed from `ParallelDispatchOpts` and `ParallelDispatchHandle`; `mainBookmarks?: readonly string[]` present | unit (tsc) | `cd sdk && pnpm tsc --noEmit` | ✅ | ⬜ pending |
| 14.1-01-T-jj-fanin | 01 | 1 | PARALLEL-08 SC2 | — | jj fan-in skips `bookmark set` when `mainBookmarks` empty; all-or-nothing pre-validation when non-empty | unit | `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts` | ✅ | ⬜ pending |
| 14.1-01-T-git-fanin | 01 | 1 | PARALLEL-08 SC3 | — | git fan-in works on detached HEAD; per-name `update-ref refs/heads/<name> HEAD` after merge when non-empty | unit | `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-git.test.ts` | ✅ | ⬜ pending |
| 14.1-01-T-cli-bridge | 01 | 1 | PARALLEL-08 SC1 (CLI) | — | `--main-bookmark` repeats accumulate into `mainBookmarks`; envelope `main_bookmark_required` reason path deleted | unit | `cd sdk && pnpm vitest run src/query/__tests__/workspace-parallel-dispatch.test.ts` (if exists) + `cd sdk && pnpm tsc --noEmit` | ✅ | ⬜ pending |
| 14.1-01-T-workflows | 01 | 1 | PARALLEL-08 SC4 + D-01 | — | `current-branch` FATAL preflight removed from BOTH `execute-phase.md` and `quick.md`; dispatch invocations drop `--main-bookmark "$EXPECTED_BRANCH"` arg | lint | `node --test tests/quick-md-parallel-dispatch.test.cjs` (CR-03 lint inverted: assert preflight ABSENT in both) | ✅ | ⬜ pending |
| 14.1-01-T-tests-new | 01 | 1 | PARALLEL-08 SC5 | — | 3 new scenarios per backend in `cmd-parallel-{jj,git}.test.ts`: (a) empty-list + bookmark-less/detached HEAD; (b) valid non-empty advances each name; (c) all-or-nothing rejection with no partial moves | unit | `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` | ✅ | ⬜ pending |
| 14.1-01-T-tests-forced | 01 | 1 | PARALLEL-08 SC1 (mechanical) | — | 6 forced-touch sites in 3 test files migrated `mainBookmark: 'main'` → `mainBookmarks: ['main']` site-for-site (mechanical preserve per D-04) | unit (tsc) | `cd sdk && pnpm tsc --noEmit` (TSC failures gate this); `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts` | ✅ | ⬜ pending |
| 14.1-01-T-roadmap-req | 01 | 1 | PARALLEL-08 SC6 + D-03 | — | REQUIREMENTS.md MERGE-08 entry filed; STATE.md Deferred Items row added; ROADMAP.md SC1 ":226" typo struck + line 252 amended (+ quick.md) + line 258 Touches list amended (+ quick.md + tests/quick-md-parallel-dispatch.test.cjs); Phase 15.04 plan untouched (CF-04 confirms compatibility) | doc | `grep -l 'MERGE-08' .planning/REQUIREMENTS.md && grep -L ':226' .planning/ROADMAP.md \| head -1` (presence + absence checks) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> Final task IDs derive from the planner's wave/task layout. The verification surface here is the contract — the planner may bundle tasks differently (e.g., merge T-types + T-jj-fanin into one atomic commit if mid-pass tsc failures are intolerable) but every requirement above MUST land on at least one task.

---

## Wave 0 Requirements

- [x] `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` — already exists; new describe block + scenario migration
- [x] `sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` — already exists; new describe block + scenario migration
- [x] `tests/quick-md-parallel-dispatch.test.cjs` — already exists; CR-03 lint inversion
- [x] vitest + node:test — frameworks already installed

*Existing infrastructure covers all phase requirements. No Wave 0 stubs needed; tests added directly in the implementation tasks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Real `--auto` chain on bookmark-less `@` no longer fails at dispatch preflight | PARALLEL-08 (regression source) | End-to-end behavior on a real workflow run; integration territory beyond unit tests | After commit, run `/gsd:plan-phase 14.1 --auto` on a checkout where `jj bookmark list` returns empty; verify dispatch proceeds to plan execution without "main bookmark missing" or "no current branch" error |
| Detached-HEAD git real-world dispatch+fan-in | PARALLEL-08 SC3 | Same as above for the git backend | In a colocated jj repo: `git checkout --detach HEAD`, run a parallel-dispatch workflow, verify merge lands and HEAD remains detached |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (N/A — all infrastructure exists)
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
