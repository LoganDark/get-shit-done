---
phase: 14.1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/vcs/types.ts
  - sdk/src/vcs/jj/parallel.ts
  - sdk/src/vcs/git/parallel.ts
  - sdk/src/query/workspace-parallel-dispatch.ts
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/quick.md
  - tests/quick-md-parallel-dispatch.test.cjs
  - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts
  - sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [PARALLEL-08]

must_haves:
  truths:
    - "ParallelDispatchOpts.mainBookmark and ParallelDispatchHandle.mainBookmark are removed; mainBookmarks?: readonly string[] is the only field (CF-01 hard rename, no alias)"
    - "Dispatch+fan-in succeeds on bookmark-less jj @ (no jj bookmark create main in setup) with mainBookmarks omitted/[]; zero jj bookmark set invocations observable"
    - "Dispatch+fan-in succeeds on detached-HEAD git (git checkout --detach HEAD) with mainBookmarks omitted/[]; zero git update-ref invocations observable; merge lands into HEAD itself"
    - "Non-empty mainBookmarks list with one invalid name throws BEFORE any bookmark set / update-ref runs; no partial advance observable (CF-02 all-or-nothing pre-validation)"
    - "Workflow current-branch FATAL preflight is REMOVED from BOTH get-shit-done/workflows/execute-phase.md AND get-shit-done/workflows/quick.md in lockstep (D-01 symmetric)"
    - "tests/quick-md-parallel-dispatch.test.cjs CR-03 block polarity is FLIPPED — assertions read 'preflight ABSENT' in both files (D-01)"
    - "WorkspaceMergeOpts.mainBookmark (types.ts:226) is UNTOUCHED (D-03 STRICT OUT OF SCOPE); MERGE-08 deferred-item REQ-ID is FILED in REQUIREMENTS.md + STATE.md Deferred Items table"
    - "ROADMAP.md Phase 14.1 SC1 ':226' citation STRUCK; line 252 amended to add quick.md; line 258 Touches list amended to add quick.md + tests/quick-md-parallel-dispatch.test.cjs"
    - "CLI flag shape STAYS singular --main-bookmark; bridge accumulates repeats into mainBookmarks: string[] (D-02 repeated-singular accumulator)"
    - "CLI envelope reason 'main_bookmark_required' is DELETED (D-02); every other reason token unchanged"
    - "TSC closes green in sdk/ after all consumer edits land (CF-01 audit trail — no missed sites)"
    - "Lint allowlist net diff is +0 (CF-06)"

  artifacts:
    - path: "sdk/src/vcs/types.ts"
      provides: "ParallelDispatchOpts.mainBookmarks?: readonly string[] + ParallelDispatchHandle.mainBookmarks?: readonly string[]"
      contains: "mainBookmarks?: readonly string[]"
    - path: "sdk/src/vcs/jj/parallel.ts"
      provides: "All-or-nothing two-pass validate-then-advance loop in fan-in clean path"
      contains: "mainBookmarks"
    - path: "sdk/src/vcs/git/parallel.ts"
      provides: "Post-merge per-name update-ref loop guarded on non-empty mainBookmarks + !conflicted"
      contains: "update-ref"
    - path: "sdk/src/query/workspace-parallel-dispatch.ts"
      provides: "Repeated-singular --main-bookmark accumulator + dropped main_bookmark_required envelope path"
      contains: "mainBookmarks.push"
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Dispatch block top WITHOUT current-branch FATAL preflight; dispatch invocation WITHOUT --main-bookmark arg"
      contains: "workspace.parallel.dispatch"
    - path: "get-shit-done/workflows/quick.md"
      provides: "Dispatch block top WITHOUT current-branch FATAL preflight; dispatch invocation WITHOUT --main-bookmark arg"
      contains: "workspace.parallel.dispatch"
    - path: "tests/quick-md-parallel-dispatch.test.cjs"
      provides: "Polarity-flipped CR-03 lint asserting preflight ABSENT in BOTH workflow files"
      contains: "doesNotMatch"
    - path: "sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts"
      provides: "3 new describe blocks per SC5: empty-list / non-empty advance / all-or-nothing validation (jj backend)"
      contains: "mainBookmarks"
    - path: "sdk/src/vcs/__tests__/cmd-parallel-git.test.ts"
      provides: "3 new describe blocks per SC5: empty-list+detached-HEAD / non-empty advance / all-or-nothing validation (git backend)"
      contains: "checkout --detach"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MERGE-08 deferred-item entry under §Deferred-Item Harvest (Parallel Verb)"
      contains: "MERGE-08"
    - path: ".planning/STATE.md"
      provides: "Deferred Items table row for MERGE-08"
      contains: "MERGE-08"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 14.1 SC1 amendment (struck ':226'); line 252 + 258 amendments adding quick.md"
      contains: "quick.md"

  key_links:
    - from: "sdk/src/query/workspace-parallel-dispatch.ts"
      to: "sdk/src/vcs/types.ts (ParallelDispatchOpts.mainBookmarks)"
      via: "dispatch({mainBookmarks, ...}) call"
      pattern: "mainBookmarks,?\\s*$"
    - from: "sdk/src/vcs/jj/parallel.ts (fan-in)"
      to: "handle.mainBookmarks"
      via: "destructure + two-pass validate+advance loop"
      pattern: "handle\\.mainBookmarks"
    - from: "sdk/src/vcs/git/parallel.ts (fan-in)"
      to: "handle.mainBookmarks"
      via: "post-merge update-ref loop"
      pattern: "update-ref.*refs/heads"
    - from: "tests/quick-md-parallel-dispatch.test.cjs"
      to: "get-shit-done/workflows/{execute-phase,quick}.md"
      via: "static-text lint asserting preflight ABSENT"
      pattern: "doesNotMatch.*EXPECTED_BRANCH"
---

<objective>
Drop the required `mainBookmark: string` field on `ParallelDispatchOpts` / `ParallelDispatchHandle` and replace it with optional `mainBookmarks?: readonly string[]` (default `[]` → no advance). Make bookmark-less jj `@` and detached-HEAD git first-class working states for parallel dispatch + fan-in. Hard rename per CF-01 — no deprecation alias; TSC failures are the audit trail. Symmetric workflow preflight removal per D-01. Strict scope per D-03 — `WorkspaceMergeOpts.mainBookmark` stays untouched; new MERGE-08 deferred-item REQ-ID files the asymmetry as deliberate. Single plan, single wave per CF-08.

Purpose: Close PARALLEL-08 emergency gap. The bookmark requirement baked in by Phase 11 (PARALLEL-01 "D-03 atomic main-advance: REQUIRED") blocks two legitimate working-copy states — bookmark-less jj `@` (normal jj WIP) and detached HEAD git — that the underlying primitives (`jj new` parents, `git merge --no-ff`) already support natively.

Output: 15 files modified across SDK types, both backend bodies, CLI bridge, both workflow files, polarity-flipped lint, two roadmap-listed test files with new SC5 scenarios + 3 dedicated forced-touch test files migrated, and three planning docs amended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-CONTEXT.md
@.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-RESEARCH.md
@.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-PATTERNS.md
@.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-VALIDATION.md

<interfaces>
<!-- Type contracts. Source of truth: sdk/src/vcs/types.ts. Executor uses these directly — no codebase exploration needed for shape. -->

Current state of `ParallelDispatchOpts` (sdk/src/vcs/types.ts:458-469):

```typescript
export interface ParallelDispatchOpts {
  plan: readonly { agentId: string; planId: string; workspacePath?: string }[];
  phaseNumber: number;
  mainBookmark: string;             // <-- DROPS; replaced by mainBookmarks?
  maxConcurrency?: number;
}
```

Current state of `ParallelDispatchHandle` (sdk/src/vcs/types.ts:493-518 — abridged):

```typescript
export interface ParallelDispatchHandle {
  phaseRoot: string;
  phaseNumber: number;
  mainBookmark: string;             // <-- DROPS; replaced by mainBookmarks?
  manifest: string;
  workspaces: readonly Readonly<{
    name: string;
    path: string;
    baseRev: string;
    agentId: string;
    baselineOpId?: string;
  }>[];
}
```

Target state (both interfaces): `mainBookmark: string;` line becomes
`mainBookmarks?: readonly string[];` — JSDoc mirrors the sibling style at line 226
(`mainBookmark: string;  // D-03 atomic ...`), updated to describe the new semantic:
empty/omitted → skip advance step; non-empty → all-or-nothing pre-validation +
sequential advance.

Existing helper to KEEP (sdk/src/vcs/jj/parallel.ts:120-130, called per-name in
the new all-or-nothing loop):

```typescript
function validateMainBookmark(name: string): void {
  if (name.length === 0 || name.startsWith('-') || !/^[A-Za-z0-9._/-]+$/.test(name)) {
    throw new Error(
      `parallel: main bookmark name "${name}" is not a valid jj bookmark refname`,
    );
  }
}
```

Existing helper to REUSE on git side (sdk/src/vcs/refs-validator.ts: `validateRefname`).

Existing exec primitive (sdk/src/vcs/exec.ts): `vcsExec(cwd, 'jj' | 'git', argv): { exitCode, stdout, stderr }` — synchronous; no async ever per Phase 9 D-01.

`OUT OF SCOPE per D-03` — `WorkspaceMergeOpts.mainBookmark` at types.ts:226 stays unchanged. Detection: callers of `vcs.workspace.merge(...)` or any reference to `WorkspaceMergeOpts` — DO NOT TOUCH. This includes `tests/vcs-adapter-contract.test.cjs:164,171` which targets the merge verb (Pitfall 8).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TSC-atomic field rename across SDK + CLI bridge</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/jj/parallel.ts, sdk/src/vcs/git/parallel.ts, sdk/src/query/workspace-parallel-dispatch.ts, sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts, sdk/src/vcs/__tests__/cmd-parallel-git.test.ts, sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts, sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts, sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts</files>

  <read_first>
    <!-- Source-of-truth files BEFORE editing. -->
    - sdk/src/vcs/types.ts (current ParallelDispatchOpts at lines 458-469; ParallelDispatchHandle at lines 493-518; OOS WorkspaceMergeOpts at 222-228 — DO NOT touch)
    - sdk/src/vcs/jj/parallel.ts (validateMainBookmark helper at 120-130 — KEEP; destructure at 176; pre-validate call at 188; Handle field at 247; fan-in clean-path block at 405-419)
    - sdk/src/vcs/git/parallel.ts (destructure at 149; manifest-row at 240; Handle field at 252; merge loop ending at line 454 — insertion site for new update-ref loop)
    - sdk/src/query/workspace-parallel-dispatch.ts (parser var at 46; assignment at 55-56; envelope path at 67-69; dispatch call at 105)
    - sdk/src/vcs/refs-validator.ts (validateRefname for git-side per-name validation)
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (3 SDK opts sites at lines 134, 255, 356 — MECHANICAL PRESERVE per D-04, NO new describes here; 3 CLI argv sites at lines 492, 531, 560 STAY unchanged per D-02)
    - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts (6 SDK opts sites at lines 152, 235, 321, 409, 554, 672 — MECHANICAL PRESERVE; 3 CLI argv sites at lines 747, 786, 815 STAY unchanged per D-02)
    - sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts (1 site at line 48 — MECHANICAL PRESERVE)
    - sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts (3 sites at lines 110, 150, 156 — MECHANICAL PRESERVE)
    - sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts (2 sites at lines 148, 316 — MECHANICAL PRESERVE)
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-PATTERNS.md (sections "sdk/src/vcs/types.ts", "sdk/src/vcs/jj/parallel.ts", "sdk/src/vcs/git/parallel.ts", "sdk/src/query/workspace-parallel-dispatch.ts")
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-RESEARCH.md "Pattern 1: Two-pass validate-then-mutate (all-or-nothing)" and "Common Pitfalls" §§ Pitfall 1 (line-drift), Pitfall 4 (D-02 four-line bridge edit), Pitfall 9 (TSC audit trail)
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-CONTEXT.md decisions CF-01 / CF-02 / CF-03 / CF-05 / D-02 / D-03
  </read_first>

  <behavior>
    - Test: `cd sdk && pnpm tsc --noEmit` exits 0 after all consumer edits land.
    - Behavior: dispatch with no mainBookmarks arg yields a frozen Handle whose `mainBookmarks` field is either omitted or `Object.freeze([])`.
    - Behavior: jj fan-in clean path with empty Handle.mainBookmarks runs ZERO `jj bookmark set` calls; merge lands at `@` via the existing N-parent `jj new` step.
    - Behavior: jj fan-in with `mainBookmarks: ['valid-1','bad..ref','valid-2']` throws BEFORE any `jj bookmark set` runs; pre-throw and post-throw output of `jj log -r 'present(valid-1)' --no-graph` equals empty in both samples.
    - Behavior: git fan-in clean path with empty Handle.mainBookmarks runs ZERO `git update-ref` calls; merge lands into current HEAD via the existing `merge --no-ff` step.
    - Behavior: git fan-in with `mainBookmarks: ['valid-1','bad..ref','valid-2']` throws BEFORE any `git update-ref` runs.
    - Behavior: `gsd-sdk query workspace.parallel.dispatch --phase N --plan ...` (no `--main-bookmark` flag) returns `{ok:true, ...}` (the old `main_bookmark_required` envelope path is GONE).
    - Behavior: repeated `--main-bookmark foo --main-bookmark bar` accumulates into `mainBookmarks: ['foo','bar']` in argv order, no de-dup.
  </behavior>

  <action>
    Perform the four-file atomic TSC-closing edit. Per CF-01 (hard rename, no alias) the type rename and EVERY in-tree consumer change land in one commit so TSC stays green; missed consumers manifest as TSC red — they are the audit trail per Pitfall 9.

    **A) sdk/src/vcs/types.ts (TYPE rename — 2 sites):**

    1. Locate the `ParallelDispatchOpts` interface (currently lines 458-469). Rename the `mainBookmark: string;` field to `mainBookmarks?: readonly string[];`. JSDoc the field with: empty/omitted means skip the advance step entirely (CF-02/CF-03); non-empty triggers all-or-nothing pre-validation then sequential advance.
    2. Locate the `ParallelDispatchHandle` interface (currently lines 493-518). Rename its `mainBookmark: string;` field to `mainBookmarks?: readonly string[];` with parallel JSDoc.
    3. **DO NOT touch** the `WorkspaceMergeOpts.mainBookmark` field at line 226. That field is locked OUT OF SCOPE per D-03 — it has zero non-test production callers and a separate MERGE-08 deferred-item REQ-ID files the asymmetry (Task 3 below). Roadmap SC1's `:226` citation is a typo (also struck in Task 3).

    **B) sdk/src/vcs/jj/parallel.ts (jj backend body):**

    1. Line ~176 destructure: change `mainBookmark` to `mainBookmarks` in `const { mainRepoRoot, vcs, plan, phaseNumber, mainBookmark } = opts;`.
    2. Line ~188 pre-validate call: DROP the single up-front `validateMainBookmark(mainBookmark);` call — per-name validation moves to the fan-in pre-validation loop below. The `validateMainBookmark` helper at line 120 is KEPT (Discretion item — same signature, repurposed; do NOT delete and re-add).
    3. Line ~247 Handle field: change `mainBookmark,` to `mainBookmarks: Object.freeze([...(opts.mainBookmarks ?? [])]) as readonly string[],` — preserves the Phase 9 D-05 frozen-pure-JSON invariant (CF-05).
    4. Lines ~405-419 (the fan-in clean-path block per RESEARCH Pitfall 1, the actual closing brace may sit at line 419 — re-grep before line-range directives): REPLACE the single-bookmark advance pattern with the two-pass all-or-nothing loop. Mirror the W1 pattern at lines 178-188:
       - Read `const mainBookmarks = handle.mainBookmarks ?? [];`
       - Guard the entire advance step on `mainBookmarks.length > 0`.
       - Pass 1: `for (const name of mainBookmarks) validateMainBookmark(name);` (throws on first invalid — zero side effects yet).
       - Pass 2: `for (const name of mainBookmarks) { const setArgs = [...jjArgvFlags(mainRepoRoot), 'bookmark','set', name, '-r','@']; const setRes = vcsExec(mainRepoRoot, 'jj', setArgs); if (setRes.exitCode !== 0) throw new Error('parallel.fanIn: main-bookmark advance (${name}) failed: ${setRes.stderr || setRes.stdout}');}` — keep argv composition identical to the existing block's per-line layout.
       - Add a JSDoc-style code comment noting: throw on first failure leaves partial state if name K fails after names 1..K-1 advanced; atomic rollback via `jj op restore` is deferred (Discretion item; mid-iteration `jj bookmark set` failure surface).

    **C) sdk/src/vcs/git/parallel.ts (git backend body):**

    1. Line ~149 destructure: change `mainBookmark` to `mainBookmarks` (same as jj).
    2. Line ~240 manifest writer: DROP the `main_bookmark: mainBookmark,` row from the `manifestBody.worktrees.map(...)` object literal. Per CONTEXT "Manifest writer cleanup" recommendation: no live reader on either backend (the jj-side already emits `manifest: ''`; the workflow-side reader was retired in Phase 11 P05); other manifest rows (`worktree_path`, `branch`, `expected_base`) STAY for parity.
    3. Line ~252 Handle field: change `mainBookmark,` to `mainBookmarks: Object.freeze([...(opts.mainBookmarks ?? [])]) as readonly string[],` (same as jj, CF-05 frozen-pure-JSON).
    4. AFTER the merge loop completes successfully and BEFORE STEP 2 (crashed-agent classifier — typically just after line 454, re-grep for the `for (const ws of handle.workspaces)` close-brace): ADD the per-name `update-ref` loop, guarded on `mainBookmarks.length > 0 && !conflicted`. Use `validateRefname` (imported from `refs-validator.ts`) for pass-1 validation (git-side uses the stricter validator per RESEARCH `## Don't Hand-Roll`). Pass-2 invokes `vcsExec(mainRepoRoot, 'git', ['update-ref', `refs/heads/${name}`, 'HEAD'])`; non-zero exit throws with parallel error message `parallel.fanIn: update-ref refs/heads/${name} failed: ${refRes.stderr || refRes.stdout}`. Lint-clean: this file is already on the `lint-vcs-no-raw-git.allow.json` allowlist (entry #3) per CF-06; no allowlist diff (+0).

    **D) sdk/src/query/workspace-parallel-dispatch.ts (CLI bridge — D-02 four-line edit per RESEARCH Pitfall 4):**

    1. Line ~46: change `let mainBookmark = '';` to `const mainBookmarks: string[] = [];` (const because pushing into mutable array; D-02 LOCKED on repeated-singular accumulator).
    2. Lines ~55-56: inside the `else if (args[i] === '--main-bookmark' && args[i + 1])` branch, change the body from `mainBookmark = args[++i];` to `mainBookmarks.push(args[++i]);` — no de-dup; preserve argv order.
    3. Lines ~67-69: DELETE the entire `if (!mainBookmark) { return { data: { ok: false, reason: 'main_bookmark_required' } }; }` block. Empty list is now legal — bookmark-less first-class per CF-02/CF-03/D-02. Every OTHER envelope reason token (`phase_number_required`, `plan_required`, `plan_parse_failed`, etc.) STAYS unchanged.
    4. Line ~105: change `mainBookmark,` to `mainBookmarks,` in the `vcs.workspace.parallel.dispatch({...})` call.
    5. Update the JSDoc header comment at the top of the file: where the file currently documents `--main-bookmark <name>` as `required: main bookmark name`, change to describe the repeated-singular semantic — zero flags → empty list → no advance; repeats accumulate into argv-order list.

    **Verification mid-edit (production-side only — applies after Sections A-D, before Section E):** Use `grep -nE 'mainBookmark[^s]|mainBookmark$' sdk/src/ -r` and check that NO surviving hits exist on the four production-side edited files (`types.ts`, `jj/parallel.ts`, `git/parallel.ts`, `workspace-parallel-dispatch.ts`). Hits on `WorkspaceMergeOpts.mainBookmark` (types.ts:226), the `validateMainBookmark` helper name, and error-message strings like `"main-bookmark advance"` are EXPECTED (helper name preserved per Discretion; error strings are user-facing wording). Hits in the test files under `sdk/src/vcs/__tests__/cmd-parallel*` and `sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd` are ALSO expected until Section E migrates them in the SAME commit.

    **E) Mechanical preserve at the 5 test files (forced-touch sites — D-04 site-for-site `mainBookmark: 'main',` → `mainBookmarks: ['main'],`; NO new describes here, NO `[]` default expansion; the new SC5 scenarios stay in Task 3):**

    The whole point of this fold is that the TSC audit trail can only close green when EVERY consumer site (production + test scaffolding) is migrated in the SAME commit as the type rename. Leaving the test sites for Task 3 would make Task 1's `pnpm tsc --noEmit` verify red by construction — every test file that references `mainBookmark: 'main'` would trip a "Property 'mainBookmark' does not exist on type 'ParallelDispatchOpts'" error. D-04 (mechanical preserve at forced-touch sites — no behavioral changes; the sites carry through identically to keep the surrounding scenarios green) makes this a pure rename, not a behavior change.

    1. `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` — change the 3 SDK opts sites at lines ~134, ~255, ~356 from `mainBookmark: 'main',` to `mainBookmarks: ['main'],`. The 3 CLI argv sites at lines ~492, ~531, ~560 (`['--phase', '14', '--main-bookmark', 'main', '--plan', plan]`) STAY UNCHANGED — D-02 preserves the singular `--main-bookmark` CLI token.
    2. `sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` — change the 6 SDK opts sites at lines ~152, ~235, ~321, ~409, ~554, ~672 from `mainBookmark: 'main',` to `mainBookmarks: ['main'],`. The 3 CLI argv sites at lines ~747, ~786, ~815 STAY UNCHANGED.
    3. `sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts` — change the single site at line ~48 from `mainBookmark: 'main',` to `mainBookmarks: ['main'],`.
    4. `sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts` — change each of the 3 sites at lines ~110, ~150, ~156 from `mainBookmark: 'main',` to `mainBookmarks: ['main'],`.
    5. `sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts` — change each of the 2 sites at lines ~148, ~316 from `mainBookmark: 'main',` to `mainBookmarks: ['main'],`.

    Behavioral guarantee: ALL existing scenarios in these 5 files must stay green against the migrated SDK. The migration is pure scaffolding text — the test bodies, assertions, fixture setup, and `setupJjRepo`/`setupGitRepo` helpers are NOT touched. The only behavioral signal from this section is that the existing tests STILL pass.

    **OOS reaffirmation (Pitfalls 8 + 10):** DO NOT touch `tests/vcs-adapter-contract.test.cjs:164,171` (targets `WorkspaceMergeOpts.mainBookmark` — D-03 strict OOS), `tests/wave-cleanup-executor.test.cjs`, `tests/bug-3384-worktree-cleanup-manifest.test.cjs` (legacy snake_case manifest tests — outside scope), or `get-shit-done/bin/lib/worktree-safety.cjs:404-421` (legacy CJS shim — Option A per CONTEXT recommendation; the shim's `mainBookmark` write becomes silent dead code on the `.cjs` runtime with zero behavioral effect).

    **Commit all nine files as ONE atomic commit** so TSC closes the audit trail cleanly AND the migrated test files pass green in the same revision. Use `gsd-sdk query commit "refactor(14.1): rename ParallelDispatch{Opts,Handle}.mainBookmark -> mainBookmarks?: readonly string[] + migrate 15 forced-touch test sites (D-04 mechanical preserve)" --files sdk/src/vcs/types.ts sdk/src/vcs/jj/parallel.ts sdk/src/vcs/git/parallel.ts sdk/src/query/workspace-parallel-dispatch.ts sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts sdk/src/vcs/__tests__/cmd-parallel-git.test.ts sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts`.
  </action>

  <verify>
    <automated>cd sdk && pnpm tsc --noEmit && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -nE 'mainBookmark[^s]|mainBookmark$' sdk/src/vcs/types.ts | grep -v 'WorkspaceMergeOpts' | grep -v ':226:'` returns EMPTY (the only surviving hit on `mainBookmark` token in types.ts is the OOS WorkspaceMergeOpts field).
    - Source assertion: `grep -nE 'mainBookmark[^s]|mainBookmark$' sdk/src/vcs/jj/parallel.ts | grep -v 'function validateMainBookmark' | grep -v 'main-bookmark advance'` returns EMPTY (helper name + error-message string survive).
    - Source assertion: `grep -nE 'mainBookmark[^s]|mainBookmark$' sdk/src/vcs/git/parallel.ts` returns EMPTY (the new error string uses `update-ref refs/heads/${name}`, NOT `mainBookmark`).
    - Source assertion: `grep -nE 'mainBookmark[^s]|mainBookmark$' sdk/src/query/workspace-parallel-dispatch.ts` returns EMPTY; `grep -n 'mainBookmarks' sdk/src/query/workspace-parallel-dispatch.ts | wc -l` returns >= 3 (declaration + push + dispatch call).
    - Source assertion: `grep -n 'mainBookmarks?: readonly string\[\]' sdk/src/vcs/types.ts | wc -l` returns 2 (Opts + Handle).
    - Source assertion: `grep -n 'Object.freeze(\[\.\.\.(opts.mainBookmarks' sdk/src/vcs/jj/parallel.ts sdk/src/vcs/git/parallel.ts | wc -l` returns 2 (one frozen-list wrap per backend Handle).
    - Source assertion: `grep -n 'main_bookmark_required' sdk/src/query/workspace-parallel-dispatch.ts` returns EMPTY (envelope path DELETED).
    - Source assertion: `grep -nE 'for \(const name of mainBookmarks\)' sdk/src/vcs/jj/parallel.ts | wc -l` returns 2 (validate pass + advance pass in jj fan-in).
    - Source assertion: `grep -nE 'for \(const name of mainBookmarks\)' sdk/src/vcs/git/parallel.ts | wc -l` returns 2 (validate pass + update-ref pass in git fan-in).
    - Source assertion: `grep -n "main_bookmark: " sdk/src/vcs/git/parallel.ts` returns EMPTY (manifest row DROPPED per "Manifest writer cleanup").
    - Test command: `cd sdk && pnpm tsc --noEmit` exits 0 (CF-01 audit trail closed).
    - Source assertion: `grep -cE "mainBookmark: 'main'" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts sdk/src/vcs/__tests__/cmd-parallel-git.test.ts sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts` returns 0 (all 15 forced-touch sites migrated; CLI argv sites under `--main-bookmark` remain — they do not match the literal `mainBookmark: 'main'` form).
    - Source assertion: `grep -cE "mainBookmarks:\s*\['main'\]" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts sdk/src/vcs/__tests__/cmd-parallel-git.test.ts sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts sdk/src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts sdk/src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts` returns 15 (3 jj + 6 git + 1 max-cli + 3 max-adapter + 2 cwd).
    - Source assertion: `grep -c -- '--main-bookmark' sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` returns 6 (3 CLI argv sites per file — STAYS per D-02).
    - Test command: `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts` exits 0; ALL existing scenarios stay green against the migrated SDK (mechanical preserve guarantee — no behavioral changes).
    - CLI assertion: `node sdk/dist/bin/gsd-sdk.js query workspace.parallel.dispatch --help 2>&1 || echo "$?"` (or equivalent help probe) does NOT emit `main_bookmark_required` as a reason token; the help text references `--main-bookmark` as repeatable.
    - Lint assertion: `node scripts/lint-vcs-no-raw-git.cjs` exits 0; `lint-vcs-no-raw-git.allow.json` diff in this commit is empty (CF-06 net +0).
  </acceptance_criteria>

  <done>
    TSC green on sdk/. Both Opts and Handle interfaces carry only `mainBookmarks?: readonly string[]`. The NINE-file edit (4 SDK production files + 5 test files carrying 15 forced-touch sites) is committed as ONE atomic commit. jj fan-in body has the two-pass validate-then-advance loop guarded on non-empty. git fan-in body has the post-merge per-name update-ref loop guarded on non-empty + !conflicted. CLI bridge accumulates repeated `--main-bookmark` into `mainBookmarks`. `main_bookmark_required` envelope path is gone. All 5 migrated test files pass vitest green (mechanical preserve — no behavioral changes; SC5 new describes still pending in Task 3). WorkspaceMergeOpts.mainBookmark at types.ts:226 is UNCHANGED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Symmetric workflow preflight removal + polarity-flipped CR-03 lint</name>
  <files>get-shit-done/workflows/execute-phase.md, get-shit-done/workflows/quick.md, tests/quick-md-parallel-dispatch.test.cjs</files>

  <read_first>
    - get-shit-done/workflows/execute-phase.md (preflight block ~lines 528-535: EXPECTED_BRANCH+FATAL exit 1; dispatch invocation ~lines 556-561 carrying `--main-bookmark "$EXPECTED_BRANCH"`; stall-surveillance probe at ~line 719; fan-in branch-drift guard at ~lines 764-766 — UNCHANGED per Pitfall 6 + Pitfall 7)
    - get-shit-done/workflows/quick.md (symmetric preflight block ~lines 666-673; symmetric dispatch invocation ~line 680; fan-in branch-drift guard ~line 766 — UNCHANGED)
    - tests/quick-md-parallel-dispatch.test.cjs (CR-03 describe block at lines 87-104; comment block at lines 14-17 — Phase 14.1 rationale rewrite)
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-PATTERNS.md sections "get-shit-done/workflows/execute-phase.md", "get-shit-done/workflows/quick.md", "tests/quick-md-parallel-dispatch.test.cjs"
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-RESEARCH.md §§ Pitfall 6 (branch-drift guard safe under empty), Pitfall 7 (stall-surveillance probe degrades gracefully) — both confirm NO additional defensive edits needed
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-CONTEXT.md decisions CF-07 (assert_clean_wc orthogonal), D-01 (workflow scope SYMMETRIC)
  </read_first>

  <behavior>
    - Behavior: After the edit, running `/gsd:plan-phase` on a checkout where `jj bookmark list` returns empty does NOT abort at the dispatch preflight (the "main bookmark missing" / "no current branch" FATAL message is gone from BOTH workflows).
    - Behavior: After the edit, `git checkout --detach HEAD` followed by a parallel workflow does NOT abort — the dispatch invocation carries zero `--main-bookmark` flags, so the SDK Handle has empty `mainBookmarks` and fan-in skips bookmark/ref advance.
    - Behavior: The fan-in branch-drift guard at line ~766 in both files STILL short-circuits when `EXPECTED_BRANCH` is unset (`[ -z "${EXPECTED_BRANCH:-}" ] || ...` already safe per Pitfall 6) — no change required there.
    - Test: `node --test tests/quick-md-parallel-dispatch.test.cjs` passes with the polarity-flipped CR-03 block asserting preflight ABSENT in both files.
  </behavior>

  <action>
    Symmetric three-file edit. Per D-01 the workflow change is in lockstep across `execute-phase.md` and `quick.md`; the CR-03 lint flips polarity in the same commit so the lint remains a regression net against future re-introduction.

    **A) get-shit-done/workflows/execute-phase.md:**

    1. Lines ~531-535: DELETE the `if [ -z "$EXPECTED_BRANCH" ] || [ "$EXPECTED_BRANCH" = "HEAD" ]; then ... exit 1; fi` block in its entirety (the FATAL + RECOVERY echo lines + the `exit 1`). KEEP line ~530 (`EXPECTED_BRANCH=$(gsd-sdk query current-branch --cwd . --pick branch)`) — downstream guard at line ~766 still consumes this variable (gracefully degrades to no-op when empty per Pitfall 6).
    2. Line ~558: in the dispatch invocation, DROP the `--main-bookmark "$EXPECTED_BRANCH"` flag-and-value pair. The line currently reads `gsd-sdk query workspace.parallel.dispatch --phase "${PHASE_NUMBER}" --main-bookmark "$EXPECTED_BRANCH" --plan @-`; the new shape is `gsd-sdk query workspace.parallel.dispatch --phase "${PHASE_NUMBER}" --plan @-` (zero `--main-bookmark` flags → empty `mainBookmarks` → no advance, per D-02).
    3. Lines ~764-766 (fan-in branch-drift guard): NO CHANGE per Pitfall 6 — the existing `[ -z "${EXPECTED_BRANCH:-}" ] || ...` prefix already short-circuits the whole `||` chain to success when EXPECTED_BRANCH is empty.
    4. Line ~719 (stall-surveillance probe consuming `--range "${EXPECTED_BRANCH}"`): NO CHANGE per Pitfall 7 — degrades to "no commits since dispatch" silently via the existing `2>/dev/null` suppression; acceptable graceful degradation.

    **B) get-shit-done/workflows/quick.md (symmetric edit):**

    1. Lines ~669-673: DELETE the byte-identical preflight block. KEEP line ~668 (`EXPECTED_BRANCH=$(gsd-sdk query current-branch --cwd . --pick branch)`).
    2. Line ~680: DROP `--main-bookmark "$EXPECTED_BRANCH"` from the dispatch invocation (same flag-and-value pair removal as execute-phase.md).
    3. Line ~766 fan-in branch-drift guard: NO CHANGE — same safe-under-empty short-circuit.

    **C) tests/quick-md-parallel-dispatch.test.cjs (CR-03 polarity flip):**

    1. Lines ~14-17 (comment block describing CR-03 rationale): REWRITE to describe the polarity-flip rationale. The new comment should explain that PARALLEL-08 (Phase 14.1) dropped the preflight in BOTH `execute-phase.md` and `quick.md`; the CR-03 lint now guards against accidental REINTRODUCTION of the preflight. Cite PARALLEL-08 / Phase 14.1 D-01 inline.
    2. Lines ~87-94 (the two `assert.match` calls inside the describe block): flip both to `assert.doesNotMatch` against the same regex (`/\[ -z "\$EXPECTED_BRANCH" \] \|\| \[ "\$EXPECTED_BRANCH" = "HEAD" \]/`). Update the inline `assert` description strings from "must pre-check EXPECTED_BRANCH for empty/HEAD before dispatch" to "must NOT pre-check EXPECTED_BRANCH (preflight removed per PARALLEL-08 — bookmark-less / detached working copies are first-class)". Also flip the `assert.match(.., /FATAL: orchestrator is on detached HEAD/, ...)` assertions to `assert.doesNotMatch(...)` — the FATAL message string is gone from both files.
    3. Lines ~98-104 (the `both files use byte-identical FATAL message (symmetry guard)` test): DROP this test entirely (RESEARCH §"Symmetric dual-file workflow edit" recommends DROP — no message string remains to compare; option to keep as `assert.ok(qm == null && em == null)` is planner-discretion-rejected here for clarity).
    4. Update the surrounding `test.describe(...)` block title from `'CR-03 EXPECTED_BRANCH empty/HEAD pre-check'` to `'CR-03 EXPECTED_BRANCH preflight ABSENT (PARALLEL-08 polarity flip)'`.

    **Commit the three-file change as one atomic commit:** Use `gsd-sdk query commit "refactor(14.1): drop current-branch FATAL preflight from execute-phase.md + quick.md; flip CR-03 lint polarity" --files get-shit-done/workflows/execute-phase.md get-shit-done/workflows/quick.md tests/quick-md-parallel-dispatch.test.cjs`.
  </action>

  <verify>
    <automated>node --test tests/quick-md-parallel-dispatch.test.cjs 2>&1 | grep -vE '^(#|$)' | grep -E '^(ok|not ok|fail)' | (! grep -q 'not ok' && ! grep -q 'fail' && echo OK)</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -cE '^\s*if \[ -z "\$EXPECTED_BRANCH" \]' get-shit-done/workflows/execute-phase.md` returns 0 (preflight removed).
    - Source assertion: `grep -cE '^\s*if \[ -z "\$EXPECTED_BRANCH" \]' get-shit-done/workflows/quick.md` returns 0 (preflight removed).
    - Source assertion: `grep -cE 'FATAL: orchestrator is on detached HEAD or branch query returned empty' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/quick.md` returns 0 (FATAL message gone from BOTH files).
    - Source assertion: `grep -c -- '--main-bookmark "\$EXPECTED_BRANCH"' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/quick.md` returns 0 (no dispatch invocation carries `--main-bookmark` arg).
    - Source assertion: `grep -n 'EXPECTED_BRANCH=\$(gsd-sdk query current-branch' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/quick.md | wc -l` returns 2 (variable definition KEPT in both files — fan-in branch-drift guard still consumes it).
    - Source assertion: `grep -nE 'workspace\.parallel\.dispatch' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/quick.md | wc -l` returns at least 2 (one dispatch invocation per file survives, just without `--main-bookmark`).
    - Source assertion: `grep -n 'doesNotMatch' tests/quick-md-parallel-dispatch.test.cjs | wc -l` returns >= 4 (two `doesNotMatch` per workflow file = at least 4; the byte-identical symmetry test is dropped).
    - Source assertion: `grep -n 'PARALLEL-08\|Phase 14\.1' tests/quick-md-parallel-dispatch.test.cjs | wc -l` returns >= 1 (rationale updated in comment block).
    - Test command: `node --test tests/quick-md-parallel-dispatch.test.cjs` exits 0 with all sub-tests passing (the flipped CR-03 block asserts preflight ABSENT in both files; previously-existing CR-02 tests stay green).
  </acceptance_criteria>

  <done>
    Both workflow files (`execute-phase.md` and `quick.md`) have the dispatch-block-top `current-branch` FATAL preflight removed. Both dispatch invocations no longer carry `--main-bookmark "$EXPECTED_BRANCH"`. The CR-03 lint at `tests/quick-md-parallel-dispatch.test.cjs:87-94` is polarity-flipped: it asserts the preflight is ABSENT in both files. The byte-identical-symmetry test is dropped. `node --test tests/quick-md-parallel-dispatch.test.cjs` exits green. The fan-in branch-drift guard at line ~766 in both files is UNCHANGED (already safe under empty per Pitfall 6).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC5 scenarios (new describes on cmd-parallel-jj/git) + planning doc amendments</name>
  <files>sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts, sdk/src/vcs/__tests__/cmd-parallel-git.test.ts, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>

  <read_first>
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (existing setupJjRepo at lines 64-87 — ALREADY bookmark-less per Pitfall 5, REUSE AS-IS; existing `for (const N of [2,3,4])` describe at 97-215; SDK opts sites at 134/255/356 ALREADY migrated by Task 1; CLI argv sites at 492/531/560 unchanged. This task adds 3 NEW describe blocks at the file bottom — does NOT touch any existing scenarios)
    - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts (existing setupGitRepo at lines 85-104; SDK opts sites at 152/235/321/409/554/672 ALREADY migrated by Task 1; CLI argv sites at 747/786/815 unchanged. This task adds 3 NEW describe blocks at the file bottom — does NOT touch any existing scenarios)
    - .planning/REQUIREMENTS.md §"Deferred-Item Harvest (Parallel Verb)" (existing PARALLEL-07 + PARALLEL-08 entries — APPEND MERGE-08 in same section style)
    - .planning/STATE.md §"Deferred Items" table (Architecture / Hooks / Lint / API rows — APPEND MERGE-08 row)
    - .planning/ROADMAP.md §"Phase 14.1" (Goal text; Success Criteria 1 with ":226" typo at line ~249; line 252 workflow-touches sentence; line 258 Touches list)
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-PATTERNS.md sections "sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts", "sdk/src/vcs/__tests__/cmd-parallel-git.test.ts", "tests/quick-md-parallel-dispatch.test.cjs" (FYI for context — already touched in Task 2), ".planning/REQUIREMENTS.md", ".planning/ROADMAP.md", ".planning/STATE.md"
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-RESEARCH.md §§ Pitfall 3 (forced-touch count drift between context.md count of 6 and actual 3 SDK + 3 CLI), Pitfall 5 (REUSE setupJjRepo as-is — already bookmark-less), Pitfall 8 (don't touch tests/vcs-adapter-contract.test.cjs — WorkspaceMergeOpts is OOS per D-03), Pitfall 10 (don't touch legacy snake_case wave-cleanup tests)
    - .planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-CONTEXT.md D-01 (ROADMAP amendments: add quick.md to lines 252 + 258), D-03 (REQUIREMENTS MERGE-08 + STATE Deferred Items row + ROADMAP strike ":226"), D-04 (test-file migration MECHANICAL PRESERVE at forced-touch sites — no `[]` default expansion in those files)
  </read_first>

  <behavior>
    - Test: `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` passes with the existing scenarios (migrated by Task 1) STAY green AND the 3 new SC5 describe blocks per backend pass green.
    - Test (jj scenario 1): dispatch with no `mainBookmarks` arg on a bookmark-less setupJjRepo; fan-in; probe `jj log -r 'present(main)' --no-graph -T 'change_id ++ "\n"'` returns empty (no `main` bookmark created).
    - Test (jj scenario 3): dispatch with `mainBookmarks: ['valid-name', 'bad..ref', 'another-valid']`; fan-in throws (matches `/bad\.\.ref|invalid/`); pre-throw and post-throw probes of `jj log -r 'present(valid-name)' --no-graph` BOTH return empty (no partial advance).
    - Test (git scenario 1): dispatch with no `mainBookmarks` arg on detached HEAD (`git checkout --detach HEAD` after seed commit); fan-in; `git rev-parse --abbrev-ref HEAD` returns `'HEAD'` (still detached); `git rev-parse refs/heads/main 2>/dev/null` returns the SEED commit SHA (or non-zero exit if main was never created), NOT the merge commit.
    - Behavior: `.planning/REQUIREMENTS.md` contains a MERGE-08 entry; `.planning/STATE.md` Deferred Items table has a MERGE-08 row; `.planning/ROADMAP.md` Phase 14.1 SC1 no longer cites `:226` and line ~252 + line ~258 reference `quick.md`.
  </behavior>

  <action>
    Two sub-edits in one commit: (1) new SC5 scenarios in `cmd-parallel-{jj,git}.test.ts` (3 describes per backend; existing scenarios + mechanical preserve sites already migrated by Task 1), (2) `.planning/REQUIREMENTS.md` MERGE-08 entry + `.planning/STATE.md` Deferred Items row + `.planning/ROADMAP.md` Phase 14.1 amendments.

    **Why no Section A/B mechanical preserve here:** Task 1 already migrated all 15 forced-touch sites (3 in cmd-parallel-jj + 6 in cmd-parallel-git + 1 in max-concurrency-cli + 3 in max-concurrency-adapter + 2 in assert-dispatched-cwd) in the same commit as the SDK type rename so TSC closes green. Per D-04 those edits are pure mechanical scaffolding (site-for-site `mainBookmark: 'main',` → `mainBookmarks: ['main'],`); they carry no behavioral semantic and must not be re-touched here. This task only ADDS new describe blocks at the file bottom of cmd-parallel-jj/git.

    **A) New SC5 scenarios on `cmd-parallel-jj.test.ts` (3 new describe blocks at file bottom, mirroring the existing `describe.sequential.skipIf(!jjAvailable)(...)` style with per-describe `setupJjRepo` + `beforeAll`/`afterAll` cleanup, per Discretion item for grep affordance):**

    Add THREE new top-level describe blocks at the file bottom (after the existing W3 (a) joint-assertion describe at line ~397 and before the existing CONFIG-02 describe at line ~476 — slot them BEFORE the CONFIG-02 block):

    1. `describe.sequential.skipIf(!jjAvailable)('workspace.parallel — bookmark-less / empty mainBookmarks (PARALLEL-08 SC5 scenario 1)', () => { ... })`:
       - Setup: REUSE `setupJjRepo()` AS-IS — already bookmark-less per Pitfall 5 (no `jj bookmark create main` in setup).
       - `it('empty mainBookmarks: fan-in skips bookmark set, merge lands at @', { timeout: 30000 }, () => { ... })`:
         - Construct plan with N=2 agents.
         - Call `vcs.workspace.parallel.dispatch({ plan, phaseNumber: 9 /* matches the .planning/phases/09-test dir created in setup */ })` — OMIT the `mainBookmarks` field entirely (testing the `undefined` → empty path).
         - Simulate clean work (mirror existing tests at lines 134-178 — squash a marker file in each workspace).
         - Call `vcs.workspace.parallel.fanIn(handle, handle.workspaces.map(w => ({ agentId: w.agentId, exitCode: 0 })))`.
         - `expect(result.conflicted).toBe(false)`.
         - `expect(result.merged.length).toBe(1)`.
         - Probe: `execSync("jj log -r 'present(main)' -T 'change_id ++ \"\\n\"' --no-graph", { cwd: dir, encoding: 'utf-8' }).trim()` — assert it equals `''` (no `main` bookmark exists post-fan-in).

    2. `describe.sequential.skipIf(!jjAvailable)('workspace.parallel — non-empty mainBookmarks advance (PARALLEL-08 SC5 scenario 2)', () => { ... })`:
       - Setup: REUSE `setupJjRepo()`.
       - `it('mainBookmarks: [name1, name2] advances each name to merge head', { timeout: 30000 }, () => { ... })`:
         - Construct plan with N=2 agents; dispatch with `mainBookmarks: ['release', 'integration']` (TWO bookmarks per Pitfall 5 — these will be CREATE-or-UPDATE on jj since the seed has no existing bookmarks).
         - Simulate clean work and fan-in.
         - Probe each name: `execSync("jj log -r '<name>' -T 'change_id ++ \"\\n\"' --no-graph", { cwd: dir })` — assert both `release` and `integration` resolve to the same change_id (the merge head at `@`).

    3. `describe.sequential.skipIf(!jjAvailable)('workspace.parallel — all-or-nothing pre-validation (PARALLEL-08 SC5 scenario 3)', () => { ... })`:
       - Setup: REUSE `setupJjRepo()`.
       - `it('non-empty mainBookmarks with one invalid name throws BEFORE any bookmark set; no partial advance', { timeout: 30000 }, () => { ... })`:
         - Dispatch with `mainBookmarks: ['valid-name', 'bad..ref', 'another-valid']` (the `bad..ref` triggers `validateMainBookmark`'s `/^[A-Za-z0-9._/-]+$/` rejection of the consecutive-dots pattern — verify by reading validateMainBookmark's regex at jj/parallel.ts:127).
         - Snapshot pre-state: `execSync("jj log -r 'present(valid-name)' --no-graph", { cwd: dir }).trim()` returns `''`; same for `another-valid`.
         - `expect(() => vcs.workspace.parallel.fanIn(handle, results)).toThrow(/parallel: main bookmark name "bad\.\.ref"/)` (the validateMainBookmark error format).
         - Snapshot post-throw: same two probes return `''` — no partial advance occurred.

    **B) New SC5 scenarios on `cmd-parallel-git.test.ts` (3 new describe blocks at file bottom, parallel structure):**

    Add a small helper adjacent to `setupGitRepo` (and a parallel `setupGitRepoDetached`):

    ```typescript
    function setupGitRepoDetached(): string {
        const dir = setupGitRepo();
        execSync('git checkout --detach HEAD', { cwd: dir, stdio: 'pipe' });
        return dir;
    }
    ```

    Slot the new describe blocks BEFORE the existing CONFIG-02 describe (mirroring jj-side placement):

    1. `describe.sequential.skipIf(!gitAvailable)('workspace.parallel — detached HEAD / empty mainBookmarks (PARALLEL-08 SC5 scenario 1)', () => { ... })`:
       - Setup: USE `setupGitRepoDetached()`.
       - `it('empty mainBookmarks + detached HEAD: fan-in merges into HEAD, no branch advances', { timeout: 30000 }, () => { ... })`:
         - Dispatch with N=2, OMIT `mainBookmarks`.
         - Simulate clean work, fan-in.
         - `expect(result.conflicted).toBe(false)`.
         - Probe: `execSync('git rev-parse --abbrev-ref HEAD', { cwd: dir, encoding: 'utf-8' }).trim()` — assert it equals `'HEAD'` (still detached).
         - Probe: `execSync('git rev-parse HEAD', { cwd: dir, encoding: 'utf-8' }).trim()` — assert it equals the post-merge SHA (the merge landed into the detached HEAD).
         - Probe: `execSync('git rev-parse refs/heads/main', { cwd: dir, encoding: 'utf-8' }).trim()` — assert it does NOT equal the post-merge SHA (the original `main` from `git init -b main` is not advanced — it still points at the seed commit).

    2. `describe.sequential.skipIf(!gitAvailable)('workspace.parallel — non-empty mainBookmarks advance (PARALLEL-08 SC5 scenario 2)', () => { ... })`:
       - Setup: USE `setupGitRepo()` (NOT detached — we want to verify each named ref advances).
       - `it('mainBookmarks: [name1, name2] advances each via update-ref refs/heads/<name>', { timeout: 30000 }, () => { ... })`:
         - Dispatch with `mainBookmarks: ['release', 'integration']`.
         - Fan-in.
         - Probe each: `execSync('git rev-parse refs/heads/release', { cwd: dir, encoding: 'utf-8' }).trim()` and same for `integration` — assert both equal the post-merge HEAD SHA.

    3. `describe.sequential.skipIf(!gitAvailable)('workspace.parallel — all-or-nothing pre-validation (PARALLEL-08 SC5 scenario 3)', () => { ... })`:
       - Setup: USE `setupGitRepo()`.
       - `it('non-empty mainBookmarks with one invalid name throws BEFORE any update-ref; no partial advance', { timeout: 30000 }, () => { ... })`:
         - Dispatch with `mainBookmarks: ['valid-name', 'bad..ref', 'another-valid']`.
         - Snapshot pre-state: `execSync('git rev-parse refs/heads/valid-name 2>&1 || true', { cwd: dir })` returns a non-zero / unknown-ref error message; same for `another-valid`.
         - `expect(() => vcs.workspace.parallel.fanIn(handle, results)).toThrow()` (validateRefname's error format on `bad..ref` — the `..` triggers rejection).
         - Snapshot post-throw: same two probes still return unknown-ref errors — no partial advance.

    **C) `.planning/REQUIREMENTS.md` MERGE-08 entry (per D-03 — appended to §"Deferred-Item Harvest (Parallel Verb)" after PARALLEL-08):**

    Append a new bullet immediately after the PARALLEL-08 entry (currently the last bullet in that section):

    ```markdown
    - [ ] **MERGE-08**: `WorkspaceMergeOpts.mainBookmark` at `sdk/src/vcs/types.ts:226` retains the required-bookmark shape PARALLEL-08 dropped from `ParallelDispatchOpts`/`ParallelDispatchHandle`. Deferred-by-design: `vcs.workspace.merge` has zero non-test production callers post-Phase 11 P03 migration; the only consumers are `sdk/src/vcs/__tests__/jj-workspace.test.ts:406,436` (passing valid bookmark names) and the git-side `currentBranch !== opts.mainBookmark` guard at `backends/git.ts:680-687`. Defer revision until a real caller emerges that needs the same bookmark-less / detached-HEAD freedom Phase 14.1 granted parallel-dispatch. Same gap class as PARALLEL-08; documented as deliberate strict-scope omission per Phase 14.1 D-03.
    ```

    Append a row to the §"Traceability" table:

    ```markdown
    | MERGE-08 | (deferred — v1.5+) | Deferred |
    ```

    Update the §"Traceability" coverage summary footer to reflect the new entry (deferred items don't count toward "mapped to phases").

    **D) `.planning/STATE.md` Deferred Items table row append (per D-03):**

    Append a row to the `## Deferred Items` table (after the existing "Reviews" row for Phase 14 review followups):

    ```markdown
    | API | MERGE-08 `WorkspaceMergeOpts.mainBookmark` revision (paired with PARALLEL-08 strict scope decision; zero non-test production callers) | Filed v1.4 Phase 14.1 (deferred until a real caller emerges; see REQUIREMENTS.md for full rationale) | v1.4 Phase 14.1 |
    ```

    **E) `.planning/ROADMAP.md` Phase 14.1 amendments (per D-01 + D-03):**

    1. Phase 14.1 SC1 (currently `...Public type at \`sdk/src/vcs/types.ts:226,467,496\` is updated atomically.`): STRIKE the `:226` citation. The corrected line reads `...Public type at \`sdk/src/vcs/types.ts:467,496\` is updated atomically.` (drop the comma-separated `:226`).
    2. Phase 14.1 SC4 (line ~252, currently `\`workflows/execute-phase.md\`: the \`current-branch\` FATAL preflight ...`): AMEND to read `\`workflows/execute-phase.md\` AND \`workflows/quick.md\`: the \`current-branch\` FATAL preflight at the dispatch-block top is removed from BOTH files in lockstep (per Phase 14.1 D-01 symmetric scope).`
    3. Phase 14.1 Plans/Touches list (line ~258, currently lists 6 files): AMEND the Touches list to add `get-shit-done/workflows/quick.md` and `tests/quick-md-parallel-dispatch.test.cjs`. The final touches list (in the plan bullet) should now read: `Touches \`sdk/src/vcs/types.ts\`, \`sdk/src/vcs/jj/parallel.ts\`, \`sdk/src/vcs/git/parallel.ts\`, \`get-shit-done/workflows/execute-phase.md\`, \`get-shit-done/workflows/quick.md\`, \`sdk/src/query/workspace-parallel-dispatch.ts\`, \`sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts\`, \`sdk/src/vcs/__tests__/cmd-parallel-git.test.ts\`, \`tests/quick-md-parallel-dispatch.test.cjs\`.` (Plus the 3 forced-touch test files implicit per D-04.)

    **Commit all five files as one atomic commit:** Use `gsd-sdk query commit "test(14.1): add 3 SC5 scenarios per backend (cmd-parallel-jj/git); file MERGE-08 deferral + ROADMAP amendments" --files sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts sdk/src/vcs/__tests__/cmd-parallel-git.test.ts .planning/REQUIREMENTS.md .planning/STATE.md .planning/ROADMAP.md`.

    **OOS reaffirmations (Pitfall 8 + Pitfall 10):** Already enforced in Task 1's Section E (the migrated set explicitly excludes `tests/vcs-adapter-contract.test.cjs:164,171`, `tests/wave-cleanup-executor.test.cjs`, `tests/bug-3384-worktree-cleanup-manifest.test.cjs`, and `get-shit-done/bin/lib/worktree-safety.cjs:404-421`). Task 3 does NOT touch those files either — the new describes go ONLY into `cmd-parallel-jj.test.ts` and `cmd-parallel-git.test.ts` at the file bottom.
  </action>

  <verify>
    <automated>cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `grep -cE "^\s*mainBookmarks:\s*\['main'\]," sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` returns 3 (the 3 SDK opts sites migrated by Task 1 — Task 3 does NOT alter this count; verifies Task 1's mechanical preserve carried through).
    - Source assertion: `grep -cE "^\s*mainBookmarks:\s*\['main'\]," sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` returns 6 (the 6 SDK opts sites migrated by Task 1).
    - Source assertion: `grep -c -- '--main-bookmark' sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` returns 6 (3 CLI argv sites per file — STAYS per D-02).
    - Source assertion: `grep -c 'PARALLEL-08 SC5 scenario' sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` returns 6 (3 new describe blocks per backend).
    - Source assertion: `grep -c 'function setupGitRepoDetached' sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` returns 1 (new helper added).
    - Source assertion: `grep -c 'MERGE-08' .planning/REQUIREMENTS.md` returns 2 or more (one in the deferred-item bullet, one in the traceability table).
    - Source assertion: `grep -c 'MERGE-08' .planning/STATE.md` returns 1 or more (one row in the Deferred Items table).
    - Source assertion: `grep -cE 'types\.ts:226' .planning/ROADMAP.md` returns 0 (the ":226" typo is struck from SC1).
    - Source assertion: `grep -c 'quick.md' .planning/ROADMAP.md` returns 2 or more (workflow-touches sentence + Touches list in plan bullet).
    - Test command: `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` exits 0; all 6 new SC5 describes pass green AND the existing scenarios (already migrated by Task 1) stay green.
    - CLI assertion: `grep -cE 'WorkspaceMergeOpts|workspace\.merge' tests/vcs-adapter-contract.test.cjs sdk/src/vcs/__tests__/jj-workspace.test.ts | grep -v ':0$' | wc -l` returns 2 or more (the pre-existing OOS sites are untouched — Pitfall 8 honored, D-03 strict scope).
    - CLI assertion: `jj diff --name-only -r @ -- tests/wave-cleanup-executor.test.cjs tests/bug-3384-worktree-cleanup-manifest.test.cjs get-shit-done/bin/lib/worktree-safety.cjs | wc -l` returns 0 (Pitfall 10 + Option A — these files untouched in this commit).
  </acceptance_criteria>

  <done>
    6 new SC5 describes ship green (3 per backend: empty-list / non-empty advance / all-or-nothing) on `cmd-parallel-jj.test.ts` + `cmd-parallel-git.test.ts`. Existing scenarios in those files (already migrated by Task 1's mechanical preserve) STAY GREEN. The 6 CLI argv sites are UNCHANGED per D-02. `.planning/REQUIREMENTS.md` has MERGE-08 bullet + traceability row. `.planning/STATE.md` Deferred Items table has MERGE-08 row. `.planning/ROADMAP.md` Phase 14.1 SC1 has the `:226` typo struck, line ~252 amended to add `quick.md`, line ~258 Touches list amended. No edits to `tests/vcs-adapter-contract.test.cjs`, `tests/wave-cleanup-executor.test.cjs`, `tests/bug-3384-worktree-cleanup-manifest.test.cjs`, or `worktree-safety.cjs` (Pitfalls 8 + 10 + Option A honored).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → SDK call | User-supplied (`--main-bookmark <name>`) strings cross into `vcs.workspace.parallel.dispatch({ mainBookmarks: [...] })` |
| SDK call → jj/git spawn argv | `mainBookmarks: string[]` entries flow into `jj bookmark set <name>` and `git update-ref refs/heads/<name> HEAD` argv arrays |
| Handle JSON round-trip | `gsd-sdk query workspace.parallel.dispatch` serializes the Handle to JSON; downstream `fan-in` deserializes it — `mainBookmarks: readonly string[]` must survive `Object.freeze` + JSON round-trip cleanly (CF-05) |
| Workflow markdown shell | `EXPECTED_BRANCH` shell variable in `execute-phase.md` / `quick.md` previously gated dispatch; after this phase, no gate on bookmark-less / detached states |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14.1-T1 | Tampering | `sdk/src/vcs/jj/parallel.ts` + `sdk/src/vcs/git/parallel.ts` two-pass validate-then-mutate | accept | TOCTOU between Pass 1 (`validateMainBookmark`/`validateRefname` over the list) and Pass 2 (`jj bookmark set` / `git update-ref` over the list): a concurrent process could race a `jj bookmark set` (or git ref write) that invalidates a previously-valid name. **Severity: LOW (informational; no security impact, only correctness).** jj's command-level locking already prevents partial-write torn state inside a single operation; git's `update-ref` is atomic per-ref. Document in JSDoc on the new fan-in loops as best-effort. (Per `<security_threat_model>` T1.) |
| T-14.1-T2 | Tampering | `vcsExec(repo, 'jj', ['bookmark','set', <name>, '-r','@'])` + `vcsExec(repo, 'git', ['update-ref', \`refs/heads/${name}\`, 'HEAD'])` | mitigate | Bookmark/ref name injection via shell metacharacters. `validateMainBookmark` enforces `/^[A-Za-z0-9._/-]+$/` + non-empty + no leading `-`; `validateRefname` is stricter (rejects `..`, `@{`, `.lock`, etc.). Existing `vcsExec` substrate uses argv arrays (no shell interpretation per `sdk/src/vcs/exec.ts:19` spawnSync). Defense-in-depth: SC5 scenario 3 explicitly tests rejection of `'bad..ref'` BEFORE any side effect, proving validation gates the spawn call. **Severity: LOW (existing validation already covers).** |
| T-14.1-T3 | Tampering | git fan-in `git update-ref refs/heads/<name> HEAD` with concurrent ref writer | accept | A concurrent process could write the same ref between Pass 1 validation and Pass 2 update-ref; `update-ref` is the atomic primitive — concurrent writes resolve via git's ref-transaction; no torn state. **Severity: LOW (informational; no torn state possible).** |
| T-14.1-T4 | Spoofing | Removal of `current-branch` FATAL preflight could mask a real bug | accept | The preflight was over-broad — per RESEARCH and CONTEXT.md, it only caught "bookmark-less `@`" and "detached HEAD," both of which are now first-class. The phase goal is explicitly to drop this preflight; the SC5 scenarios cover the previously-blocked legitimate states. No HIGH-severity threat surfaces; this is the explicit goal of the phase. **Severity: LOW.** |
| T-14.1-SC | Tampering | npm/pip/cargo installs | n/a | Phase touches existing files only; no new dependencies installed. RESEARCH §"Installation" confirms zero new packages. Package legitimacy gate does not apply. |
</threat_model>

<verification>
## Phase-Level Verification (Wave 1 / single-wave)

After ALL three tasks land, run:

1. **TSC closes:** `cd sdk && pnpm tsc --noEmit` exits 0 — CF-01 audit trail closed; no surviving consumer references the old `mainBookmark` field on `ParallelDispatchOpts` / `ParallelDispatchHandle`.

2. **vitest passes on roadmap-listed + forced-touch files:** `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts src/vcs/__tests__/cmd-parallel-max-concurrency-adapter.test.ts src/vcs/__tests__/cmd-workspace-assert-dispatched-cwd.test.ts` exits 0 with all existing scenarios green AND the 6 new SC5 describe blocks (3 per backend) green.

3. **Lint passes (polarity-flipped CR-03 + raw-git lint):** `node --test tests/quick-md-parallel-dispatch.test.cjs && node scripts/lint-vcs-no-raw-git.cjs` both exit 0. The CR-03 block asserts preflight ABSENT in both workflow files; raw-git lint allowlist net diff is +0 per CF-06.

4. **Doc presence/absence sweep:** `grep -n 'MERGE-08' .planning/REQUIREMENTS.md .planning/STATE.md && grep -L ':226' .planning/ROADMAP.md && grep -c 'quick.md' .planning/ROADMAP.md` — first command returns hits in both files, second confirms `:226` ABSENT from ROADMAP.md, third returns >= 2.

5. **OOS reaffirmation:** `grep -c 'WorkspaceMergeOpts' sdk/src/vcs/types.ts` returns at least 1 (the type still exists); `grep -nE 'mainBookmark:\s*string' sdk/src/vcs/types.ts | grep -v 'WorkspaceMergeOpts' | wc -l` returns 0 (only the WorkspaceMergeOpts.mainBookmark field survives — D-03 strict OOS); visual diff of `types.ts:222-228` against pre-phase state shows ZERO modifications inside `WorkspaceMergeOpts`.

6. **Full sdk vitest sweep:** `cd sdk && pnpm vitest run` exits 0 (no regression on adjacent tests).

7. **Manual checkpoint behavior:** (See `<manual-only-verifications>` in 14.1-VALIDATION.md — operator runs a real `--auto` chain or detached-HEAD git dispatch end-to-end to confirm production behavior matches unit-test expectations. Optional gate; the automated suite already covers the contract per SC1-6.)
</verification>

<success_criteria>
**Phase 14.1 is complete when all 6 success criteria from ROADMAP.md Phase 14.1 are TRUE:**

1. ✓ `ParallelDispatchOpts.mainBookmark` and `ParallelDispatchHandle.mainBookmark` removed; replaced by optional `mainBookmarks?: readonly string[]` (default `[]`). SDK type-checks cleanly with no caller referencing the old field. Public type at `sdk/src/vcs/types.ts:467,496` updated atomically (the SC1 `:226` typo is struck per D-03).

2. ✓ jj fan-in (`sdk/src/vcs/jj/parallel.ts`): when `mainBookmarks` is empty/omitted, the `jj bookmark set <name> -r @` step is skipped — fan-in completes with a merge into `@` and zero bookmark mutation. When non-empty, every name validated via `validateMainBookmark` BEFORE any advance side effect; iteration is all-or-nothing.

3. ✓ git fan-in (`sdk/src/vcs/git/parallel.ts`): works on detached HEAD without error. Merge still lands via `git merge --no-ff` into current HEAD. No "no current branch" / "main bookmark missing" path exists. When `mainBookmarks` is non-empty, each name advances via `git update-ref refs/heads/<name> HEAD` after successful merge.

4. ✓ `workflows/execute-phase.md` AND `workflows/quick.md` (D-01 SYMMETRIC scope): the `current-branch` FATAL preflight at the dispatch-block top is removed from BOTH files in lockstep. The dispatch CLI invocations omit `--main-bookmark`. Bookmark-less jj `@` and detached-HEAD git working copies dispatch successfully.

5. ✓ New cross-backend tests in `sdk/src/vcs/__tests__/cmd-parallel-{jj,git}.test.ts` cover: (a) dispatch+fan-in with empty `mainBookmarks` on bookmark-less jj `@` AND on detached-HEAD git; (b) dispatch+fan-in with valid non-empty list — each name advances; (c) non-empty list containing one invalid name — all-or-nothing rejection BEFORE any side effect (no partial bookmark moves).

6. ✓ PARALLEL-07's new `vcs.workspace.parallel.cancel` verb (Phase 15.04) does NOT inherit the bookmark requirement — cancel operates on `@` and the workspace SET only (per CF-04 from Phase 15 CONTEXT.md D-01; the Phase 15.04 plan needs no amendment because the `CancelResult` shape is already bookmark-free).

**Additional invariants per CONTEXT.md decisions:**

- D-03 strict scope: `WorkspaceMergeOpts.mainBookmark` at `sdk/src/vcs/types.ts:226` UNCHANGED. MERGE-08 deferred-item REQ-ID filed in REQUIREMENTS.md + STATE.md Deferred Items table.
- D-01 symmetric: ROADMAP.md Phase 14.1 line 252 amended (workflow-touches sentence adds `quick.md`); line 258 Touches list amended (adds `quick.md` + `tests/quick-md-parallel-dispatch.test.cjs`).
- CF-06 lint: `lint-vcs-no-raw-git.allow.json` net diff is +0.
- CF-01 hard rename: no deprecation alias anywhere; TSC closes green (audit trail).
- CF-05 frozen-pure-JSON: `mainBookmarks: Object.freeze([...]) as readonly string[]` in both backend Handle construction sites.
</success_criteria>

<output>
Create `.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-01-SUMMARY.md` when done. The summary should record:

- Atomic-commit log: three commits (Task 1 NINE-file SDK rename + forced-touch test migration, Task 2 three-file workflow + lint flip, Task 3 five-file SC5 scenarios + planning amendments).
- TSC closure observation: any consumer sites discovered during the rename that were NOT in the canonical_refs list (Pitfall 9 audit-trail learnings).
- Pitfall verifications: explicitly note Pitfalls 5, 6, 7, 8, 10 honored (no setupJjRepo edit; no branch-drift guard edit; no stall-surveillance probe edit; no `vcs-adapter-contract.test.cjs` touch; no legacy wave-cleanup test touches).
- MERGE-08 entry placement (file + line) for downstream MILESTONES.md update at v1.4 close.
- Confirmation that the legacy CJS shim at `worktree-safety.cjs:404-421` was LEFT ALONE per Option A (any grep hits on `mainBookmark` in `.cjs` files are expected residue; document the silent-dead-code outcome).
</output>
