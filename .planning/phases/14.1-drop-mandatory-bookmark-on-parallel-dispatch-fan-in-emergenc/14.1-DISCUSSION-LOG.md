# Phase 14.1: Drop mandatory bookmark on parallel dispatch + fan-in (emergency) - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-24
**Phase:** 14.1-Drop mandatory bookmark on parallel dispatch + fan-in (emergency)
**Areas discussed:** Workflow touch scope, CLI flag shape, WorkspaceMergeOpts (types.ts:226) scope, Test-file migration strategy
**Mode:** advisor (USER-PROFILE.md present; vendor_philosophy=pragmatic → calibration tier = standard; NON_TECHNICAL_OWNER = false)

---

## Workflow touch scope

Advisor research surfaced that `tests/quick-md-parallel-dispatch.test.cjs:87-94` (Phase 11-08 CR-03 fix) asserts the `EXPECTED_BRANCH` preflight is present in BOTH `execute-phase.md` AND `quick.md`. Option A would leave that lint asserting a stale invariant.

| Option | Description | Selected |
|--------|-------------|----------|
| Symmetric scope | execute-phase.md + quick.md + tests/quick-md-parallel-dispatch.test.cjs CR-03 lint update | ✓ |
| Literal-roadmap | execute-phase.md only — leaves quick.md FATAL preflight + stale CR-03 lint | |
| Split phase | Ship execute-phase.md in 14.1, defer quick.md to 14.2 | |

**User's choice:** Symmetric scope.
**Notes:** Phase 11-08 precedent already established symmetric workflow correction for this exact pattern. Roadmap Touches list and SC4 text need amendment during plan-checker (add `quick.md` + `tests/quick-md-parallel-dispatch.test.cjs`).

---

## CLI flag shape for mainBookmarks

Advisor research confirmed `workspace.parallel.fan-in` CLI bridge has no `--main-bookmark` flag (Handle JSON carries the list). Dispatch is the only multiplicity surface on the CLI side. Refnames forbid both whitespace and commas — no future delimiter clash for any option.

| Option | Description | Selected |
|--------|-------------|----------|
| Repeated singular `--main-bookmark <name>` | Token unchanged; accumulate via `mainBookmarks.push()`. No envelope churn beyond dropping `main_bookmark_required`. | ✓ |
| Comma-separated `--main-bookmarks name1,name2` | Token rename; new parser; new parse_failed reason. | |
| JSON array `--main-bookmarks '[...]'` | Reuses `--plan @-/@file/inline` triple-form precedent. Quoting friction in zsh. | |
| Drop the flag; advance becomes a separate verb | Architecturally clean but invents a new verb beyond PARALLEL-08. | |

**User's choice:** Repeated singular `--main-bookmark`.
**Notes:** Idiom match for `gh --label`, `git -c`, `ssh -o`. One-line parser edit. Empty default = bookmark-less first-class without any flag.

---

## WorkspaceMergeOpts.mainBookmark (types.ts:226) scope

Advisor research confirmed `vcs.workspace.merge` has zero non-test production callers — `executeWorktreeWaveCleanupPlan` migrated to `vcs.workspace.parallel.fanIn` in Phase 11 P03. Only 2 test sites use it. Pitfall-2 risk collapses to "latent dead-weight in an unused verb."

| Option | Description | Selected |
|--------|-------------|----------|
| Strict scope + OOS note in plan | Revise lines 467 + 496 only; SC1 ":226" struck as typo. | |
| Symmetric scope (include 226) | Revise all 3 sites + git `currentBranch !==` guard. | |
| Strict + REQUIREMENTS amendment | Strict scope plus a new MERGE-XX deferred-item REQ-ID in REQUIREMENTS.md documenting the asymmetry as deliberate. | ✓ |

**User's choice:** Strict + REQUIREMENTS amendment.
**Notes:** Phase scope stays tight (emergency, single plan); explicit deferred-item filing documents the asymmetry as deliberate (not silent omission). New REQ-ID proposed as MERGE-08, mirrors PARALLEL-08 numbering. Roadmap SC1 ":226" citation is a typo to strike during plan-checker.

---

## Test-file migration strategy

Advisor research confirmed all 6 forced-touch sites (across 3 files, after G3 excluded `jj-workspace.test.ts`) are pure scaffolding — none assert anything about bookmark advance. New `[]` default coverage is already delivered by the 2 roadmap-listed test files via mandated scenarios.

| Option | Description | Selected |
|--------|-------------|----------|
| Mechanical preserve | `mainBookmarks: ['main']` site-for-site; minimum-diff per Pitfall 9. | ✓ |
| Simplify-to-default | Drop the field entirely; tests exercise new `[]` behavior. | |
| Defer to follow-up | Mechanical preserve now; sweep in a future cleanup phase. | |

**User's choice:** Mechanical preserve.
**Notes:** Conflating rename + behavior-change audits is the risk Option A avoids; future-grep clarity favors literal `mainBookmarks: ['main']` as "scaffolding marker." `jj-workspace.test.ts` excluded by G3 decision (strict scope on `WorkspaceMergeOpts`).

---

## Claude's Discretion

The user delegated the following to planner judgment (captured in CONTEXT.md):

- Exact JSDoc wording on renamed `mainBookmarks?` fields.
- Whether to keep or delete `validateMainBookmark` helper (recommendation: keep).
- Test fixture choice for empty-list scenarios (Pattern B `mkdtemp` is the v1.3 default).
- Whether new scenarios live in new `describe` blocks vs slot inline (recommendation: new describe for grep affordance).
- Mid-iteration `jj bookmark set` failure semantics (recommendation: throw-on-first-failure with JSDoc note; atomic rollback deferred).
- Manifest writer cleanup on git side (recommendation: drop the `main_bookmark` field entirely; jj-side already retired).
- `fan-in branch-drift guard` (lines 765-766 in both workflows) collapse semantics when `EXPECTED_BRANCH` unset (recommendation: no-op; planner confirms).

## Deferred Ideas

- `WorkspaceMergeOpts.mainBookmark` revision → new MERGE-08 REQ-ID in REQUIREMENTS.md (this phase's commit).
- Mid-iteration `jj bookmark set` failure atomic rollback — deferred unless a real bug surfaces.
- `baselineOpId?` field cleanup on `ParallelDispatchHandle.workspaces[]` — orthogonal to PARALLEL-08; no current caller; deferred indefinitely.
- `vcs.workspace.parallel.advance-bookmarks` separate verb (CLI Option D) — architecturally clean, explicit OOS for v1.4 emergency phase.
