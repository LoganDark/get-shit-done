---
phase: 11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd
plan: 06
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - get-shit-done/workflows/quick.md
autonomous: true
requirements: [PROMPT-07]
tags: [workflow, quick, raw-git-delete, parallel-dispatch-rewire]

must_haves:
  truths:
    - "quick.md lines 660-810 (~150 LOC raw-git block) are DELETED"
    - "Replacement is one `gsd-sdk query workspace.parallel.dispatch` call + Agent() invocation + one `gsd-sdk query workspace.parallel.fan-in` call"
    - "`QUICK_WORKTREE_MANIFEST` mktemp variable is eliminated (D-01 — analogous to D-01 in execute-phase.md)"
    - "Quick mode dispatches N=1 (single executor) — the parallel verb works trivially at N=1"
    - "Per Pitfall 3: gating variable is `$USE_WORKTREES` (project-level), NOT `$USE_WORKTREES_FOR_PLAN`"
    - "`<worktree_branch_check>` block at lines 681-709 is DELETED (orchestrator-side templating gone; agent-side replacement lives in `gsd-executor.md` per Plan 11.4)"
    - "`worktree.cleanup-wave --manifest` call at lines 776-789 is replaced by `workspace.parallel.fan-in`"
    - "Any `<execution_context>` `@~/.claude/get-shit-done/references/worktree-path-safety.md` reference is REPLACED with `dispatch-cwd-safety.md` (per Plan 11.4 rename)"
  artifacts:
    - path: get-shit-done/workflows/quick.md
      provides: "Quick workflow with parallel-dispatch routed through cross-backend SDK verbs"
      contains: "gsd-sdk query workspace.parallel.dispatch"
      contains: "gsd-sdk query workspace.parallel.fan-in"
  key_links:
    - from: get-shit-done/workflows/quick.md
      to: "vcs.workspace.parallel.dispatch (via gsd-sdk query)"
      via: "HANDLE_JSON=$(gsd-sdk query workspace.parallel.dispatch ...)"
      pattern: "workspace\\.parallel\\.dispatch"
    - from: get-shit-done/workflows/quick.md
      to: "vcs.workspace.parallel.fanIn (via gsd-sdk query)"
      via: "FAN_RESULT=$(echo \"$RESULTS_JSON\" | gsd-sdk query workspace.parallel.fan-in ...)"
      pattern: "workspace\\.parallel\\.fan-in"
---

<objective>
Apply the same rewire shape as Plan 11.5 (`execute-phase.md`) to `get-shit-done/workflows/quick.md` per PROMPT-07 + D-09. Quick mode is the single-task variant of the workflow — it typically dispatches N=1 (single executor for a quick task). The parallel verb works at N=1 trivially: dispatch returns a Handle with one workspace; the orchestrator runs one Agent(); fan-in merges one workspace. Per D-01 the `QUICK_WORKTREE_MANIFEST` mktemp variable is eliminated. Per Pitfall 3 (called out in the patterns map) the gating variable in quick.md is `$USE_WORKTREES` (project-level) NOT `$USE_WORKTREES_FOR_PLAN` (per-plan, used in execute-phase.md) — do not blindly copy the execute-phase.md replacement; flip the gating variable.

Purpose: Close the workflow-markdown consumer migration. After this plan ships, every consumer of `vcs.workspace.parallel.*` runs through `gsd-sdk query` — the deletions are the architectural evidence of PROMPT-06/07/08/09 collectively.

Output: A shorter quick.md whose parallel section is a thin shell wrapper over the SDK verbs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-CONTEXT.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-PATTERNS.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-02-SUMMARY.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-05-SUMMARY.md

<interfaces>
<!-- Same SDK verb shapes as Plan 11.5 — see Plan 11.5's interfaces block for the replacement sketch. -->

<!-- Quick.md-specific gating variable difference (Pitfall 3):                                          -->
<!-- Wrong: if [ "${USE_WORKTREES_FOR_PLAN:-true}" != "false" ]; then ...                               -->
<!-- Right: if [ "${USE_WORKTREES:-true}" != "false" ]; then ...                                        -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete QUICK_WORKTREE_MANIFEST + worktree_branch_check + cleanup-tail; replace dispatch block with SDK-verb shell wrapper; preserve USE_WORKTREES gating</name>
  <files>get-shit-done/workflows/quick.md</files>
  <read_first>
    - get-shit-done/workflows/quick.md (entire lines 660-810 region; lines 667-669 mktemp variable; lines 681-709 worktree_branch_check delete target; lines 776-789 worktree.cleanup-wave call replace; any `<execution_context>` loads referencing worktree-path-safety.md)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-PATTERNS.md (`quick.md:660-810 (MODIFY — PROMPT-07)` section — adaptation per Pitfall 3 + MUST DELETE list)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-CONTEXT.md D-01 (no manifest file) + D-09 (mirrors execute-phase.md shape)
    - get-shit-done/workflows/execute-phase.md (Plan 11.5 result — the canonical replacement shape to mirror; but DIFFER on gating variable)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-05-SUMMARY.md (settled conventions from execute-phase.md migration)
  </read_first>
  <action>
    Five sub-actions inside `get-shit-done/workflows/quick.md`. Apply them in this order.

    (1) DELETE `QUICK_WORKTREE_MANIFEST=$(mktemp ...)` initialization at lines ~667-669 in full. Per D-01 the Handle JSON lives in a shell variable only.

    (2) DELETE the `<worktree_branch_check>` block at lines ~681-709 in full. Per Plan 11.4 + D-04 the four worktree-aware guards collapsed into `gsd-executor.md` — orchestrator-side templating is gone.

    (3) REPLACE the `<execution_context>` load directive (if present in this file) — if `@~/.claude/get-shit-done/references/worktree-path-safety.md` appears, change to `@~/.claude/get-shit-done/references/dispatch-cwd-safety.md`. Per Plan 11.4 the file was renamed. If no such load directive exists in quick.md, skip this step (record in SUMMARY).

    (4) REPLACE the dispatch block with the same SDK-verb shell wrapper shape as Plan 11.5. Adaptations specific to quick.md per Pitfall 3:
    - The outer gating conditional is `if [ "${USE_WORKTREES:-true}" != "false" ]; then ...` (project-level variable). Do NOT use `USE_WORKTREES_FOR_PLAN`.
    - Build the plan JSON for the single quick-task (N=1 typical). The `--phase` flag value here is the quick-task's phase identifier (or a quick-task-specific tag like `quick`); the `--main-bookmark` value follows the existing quick.md convention.
    - Call `HANDLE_JSON=$(gsd-sdk query workspace.parallel.dispatch --phase "{phase_tag}" --main-bookmark "{main_bookmark}" --plan @-)` with the plan JSON via stdin.
    - Iterate `echo "$HANDLE_JSON" | jq -c '.workspaces[]'` — for N=1 this is one iteration; the parallel verb works trivially.
    - Spawn the single Agent() per the existing quick.md Agent()-dispatch idiom (whether sequential pattern, single-shot, or other — preserve quick.md's existing dispatch style; it does NOT need the `.git/config.lock`-race sequential warning because N=1 has no race surface, but if the existing prose carries a sequential-dispatch warning, preserve it for consistency).

    (5) REPLACE the fan-in call at lines ~776-789. Replace `gsd-sdk query worktree.cleanup-wave --manifest "$QUICK_WORKTREE_MANIFEST"` with the same `FAN_RESULT=$(echo "$RESULTS_JSON" | gsd-sdk query workspace.parallel.fan-in --handle ... --results @-)` pattern from Plan 11.5. The disambiguation for `--handle` / `--results` follows Plan 11.2's settled flag semantics (recorded in 11-02-SUMMARY.md).

    DELETE any quick.md-side cleanup-tail snippet analogous to execute-phase.md's `[ -n "${QUICK_WORKTREE_MANIFEST:-}" ] && ...` raw-git block.

    PRESERVE UNCHANGED:
    - All non-parallel-dispatch sections of quick.md (the quick-task framing, task description, success-criteria sections, etc.).
    - Any Agent()-dispatch instructions for the N=1 single-executor case that are not about raw-git worktree management.
    - All `<execution_context>` loads OTHER than the worktree-path-safety.md → dispatch-cwd-safety.md swap (if applicable).
    - Markdown structure, section headings, surrounding prose.

    Net LOC delta target: ≈ −120 to −150 lines (150 deleted minus ~30 added for replacement shell block).

    If quick.md is structured such that the parallel-dispatch block is only conditionally rendered (e.g. behind `USE_WORKTREES`), preserve the conditional rendering pattern but swap the dispatch contents inside the conditional.
  </action>
  <verify>
    <automated>grep -c "QUICK_WORKTREE_MANIFEST" get-shit-done/workflows/quick.md ; grep -c "workspace.parallel.dispatch" get-shit-done/workflows/quick.md ; grep -c "workspace.parallel.fan-in" get-shit-done/workflows/quick.md ; grep -c "worktree.cleanup-wave" get-shit-done/workflows/quick.md ; grep -c "USE_WORKTREES_FOR_PLAN" get-shit-done/workflows/quick.md ; grep -c "USE_WORKTREES" get-shit-done/workflows/quick.md ; grep -c "worktree-path-safety.md" get-shit-done/workflows/quick.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "QUICK_WORKTREE_MANIFEST" get-shit-done/workflows/quick.md` returns 0 (manifest mktemp eliminated).
    - `grep -c "workspace.parallel.dispatch" get-shit-done/workflows/quick.md` returns >= 1.
    - `grep -c "workspace.parallel.fan-in" get-shit-done/workflows/quick.md` returns >= 1.
    - `grep -c "worktree.cleanup-wave" get-shit-done/workflows/quick.md` returns 0.
    - `grep -c "<worktree_branch_check>" get-shit-done/workflows/quick.md` returns 0.
    - `grep -c "USE_WORKTREES_FOR_PLAN" get-shit-done/workflows/quick.md` returns 0 (Pitfall 3 — wrong variable was never adopted in quick.md; verify).
    - `grep -cE "\\\$USE_WORKTREES\\b|\\\$\\{USE_WORKTREES" get-shit-done/workflows/quick.md` returns >= 1 (project-level gating preserved).
    - `grep -c "worktree-path-safety.md" get-shit-done/workflows/quick.md` returns 0 (reference replaced OR was absent — record which in SUMMARY).
    - `git diff --stat get-shit-done/workflows/quick.md` shows net-negative LOC of at least 100 lines removed.
    - `grep -nE "^\\s*git (worktree|merge|branch -D|update-ref)" get-shit-done/workflows/quick.md | grep -v "^#"` returns 0 lines inside shell-fence blocks (raw-git deleted from the parallel-dispatch region).
  </acceptance_criteria>
  <done>
    `QUICK_WORKTREE_MANIFEST` eliminated; parallel-dispatch block rewired to SDK verbs (with N=1 trivially honored); `<worktree_branch_check>` deleted; reference replaced or absent; `USE_WORKTREES` gating preserved per Pitfall 3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow markdown → orchestrator shell | Same as Plan 11.5 — the workflow is a prompt; orchestrator runs the shell blocks. No external untrusted input. |
| Orchestrator shell → gsd-sdk subprocess | Same SDK CLI handlers (Plan 11.2) handle JSON parsing + adapter dispatch. |
| Orchestrator shell → single Agent() | Quick mode N=1 — single workspace cwd extracted from Handle; passed to Agent(). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.06-01 | N/A | quick.md raw-git delete + SDK-verb wrap | N/A | Workflow markdown rewrite — same threat profile as Plan 11.5. No new attack surface; net security improvement (deleted raw-git block had its own surface). |
| T-11.06-02 | Tampering | Handle JSON in `$HANDLE_JSON` shell variable | accept | Same as Plan 11.5 — SDK-produced + orchestrator-consumed; both trusted. |
| T-11.06-03 | Documentation drift | `<execution_context>` reference rename (if present) | mitigate | If the rename applies, the regression test in Plan 11.4 (`tests/bug-3097-3099-executor-worktree-path-safety.test.cjs`) primarily checks execute-phase.md; if quick.md is also asserted by a similar test, that test would need extending — surface in SUMMARY if encountered. |
</threat_model>

<verification>
- `wc -l get-shit-done/workflows/quick.md` shows net-negative LOC vs baseline (target: -100 lines or more).
- All grep counts in the acceptance criteria pass.
- `scripts/lint-vcs-no-commit-id.cjs` continues to pass.
- `scripts/lint-vcs-no-raw-git.cjs` continues to pass on this file.
- All tests that previously passed continue to pass: `node --test tests/wave-cleanup-executor.test.cjs tests/bug-3384-worktree-cleanup-manifest.test.cjs tests/bug-3097-3099-executor-worktree-path-safety.test.cjs` exits 0.
- No raw `git worktree`, `git merge`, `git branch -D`, `git update-ref` calls inside shell-fence blocks in the parallel-dispatch region.
- If quick.md has a structural test in `tests/` (grep `tests/.*quick.*`), it still passes.
</verification>

<success_criteria>
- ~150 LOC of raw-git block deleted from quick.md (PROMPT-07 architectural evidence).
- Replacement shell wrapper invokes `workspace.parallel.dispatch` + `workspace.parallel.fan-in` only.
- N=1 trivial case works through the parallel verb.
- `$USE_WORKTREES` (project-level) gating preserved — `$USE_WORKTREES_FOR_PLAN` does NOT leak in.
- `<worktree_branch_check>` deleted; `worktree.cleanup-wave` call replaced.
</success_criteria>

<output>
Create `.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-06-SUMMARY.md` when done. Record:
- LOC delta on `quick.md` (before / after / net).
- Whether quick.md previously referenced `worktree-path-safety.md` in `<execution_context>` (recorded yes/no).
- Confirmation that `USE_WORKTREES_FOR_PLAN` never appears in quick.md (Pitfall 3 honored).
- Any structural difference vs Plan 11.5's execute-phase.md rewrite (e.g. quick.md's Agent()-dispatch idiom is single-shot vs sequential-loop).
- Whether any quick.md-specific test needed updating.
</output>
