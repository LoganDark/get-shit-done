---
phase: 11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/vcs/jj/parallel.ts
  - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
autonomous: true
requirements: [PARALLEL-06]
tags: [vcs, jj, parallel, bookmark-retire, cross-phase-amendment]

must_haves:
  truths:
    - "jj/parallel.ts no longer creates gsd/phase-NN-subagent-IDX bookmarks during dispatch"
    - "jj/parallel.ts no longer batch-deletes those bookmarks during fanIn"
    - "cmd-parallel-jj.test.ts asserts the workspace SET was reaped via vcs.workspace.list(), not via bookmark presence"
    - "TEST-14 `divergent()` assertion is preserved untouched (topology invariant intact)"
    - "FanInResult.surplusBookmarks field continues to exist on the result shape but is trivially `[]` on the clean branch (sweep code retired; default value stays)"
    - "Git side `worktree-agent-*` branches are NOT touched (load-bearing backend asymmetry)"
  artifacts:
    - path: sdk/src/vcs/jj/parallel.ts
      provides: "Bookmark-free composition layer; octopus structure references subagent heads as parents directly"
      contains: "createPhaseStructure + createSubagentSlot loop preserved; eager bookmark create loop deleted; batched bookmark delete deleted; surplus-bookmark sweep deleted"
    - path: sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
      provides: "Contract tests flipped to workspace.list-based assertions"
      contains: "vcs.workspace.list().filter prefix on /phase-NN-subagent-/"
  key_links:
    - from: sdk/src/vcs/jj/parallel.ts
      to: "octopus.createPhaseStructure parent references"
      via: "N-parent jj new at lines 356-371"
      pattern: "jj new -r @ -r"
    - from: sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
      to: "vcs.workspace.list()"
      via: "remainingWorkspaces.length === 0 assertion"
      pattern: "workspace.list\\(\\)\\.filter"
---

<objective>
Retire the vestigial jj-side bookmark plumbing in `sdk/src/vcs/jj/parallel.ts` per D-02 (cross-phase amendment) and flip the matching Phase 9 contract-test assertions to use `vcs.workspace.list()` directly. The octopus structure created by `createPhaseStructure` + per-slot `createSubagentSlot` already references subagent heads as parents of the merge — bookmarks were redundant scaffolding. On jj this scaffolding now goes; on git the `worktree-agent-*` branches stay (load-bearing backend asymmetry — git worktrees can't track anonymous heads ergonomically).

Purpose: Land FIRST in the Phase 11 sequence so the Phase 9 test-shape cascade is visible in commit history (CONTEXT.md Claude's Discretion item 4 + sequencing rationale). Establishes the "discover workspaces via vcs.workspace.list()" idiom that all subsequent Phase 11 plans build on.

Output: A bookmark-free `jj/parallel.ts` body + updated contract tests; downstream plans 11.2..11.6 inherit the workspace-list discovery pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-CONTEXT.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-PATTERNS.md
@.planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md

<interfaces>
<!-- From sdk/src/vcs/jj/parallel.ts — load-bearing existing structure (DO NOT remove): -->
<!-- - performJjParallelDispatch(opts): ParallelDispatchHandle — orchestrator entrypoint -->
<!-- - createPhaseStructure + createSubagentSlot loop at lines 200-222 (octopus topology, preserved) -->
<!-- - currentHeads re-resolution at lines 311-344 (Rule 1 fix, preserved) -->
<!-- - N-parent `jj new` at lines 356-371 (the WHY bookmarks are unnecessary, preserved) -->
<!-- - performJjParallelFanIn(handle, results): FanInResult — fan-in entrypoint -->

<!-- From sdk/src/vcs/types.ts — FanInResult shape (UNCHANGED): -->
<!-- { merged, conflicted, conflictedPaths, incompleteQueued, failedReaped, surplusBookmarks } -->
<!-- surplusBookmarks field stays on the type contract; default `[]` on the clean branch after sweep code retires. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Retire jj-side bookmark create + delete + surplus sweep blocks per D-02</name>
  <files>sdk/src/vcs/jj/parallel.ts</files>
  <read_first>
    - sdk/src/vcs/jj/parallel.ts (entire file — lines 1-50 header, lines 200-244 dispatch body, lines 308-543 fanIn body, lines 489-493 prefix-naming precedent)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-PATTERNS.md (`sdk/src/vcs/jj/parallel.ts (MODIFY — D-02)` section — explicit line ranges and preserve-or-delete map)
    - .planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md (D-04..D-09 carry-forward shape contracts)
  </read_first>
  <action>
    Three deletions in `sdk/src/vcs/jj/parallel.ts`, no additions:

    (1) Delete the eager bookmark-create loop at lines 224-244 in full, including the leading comment block at lines 224-227. The loop iterates `slots`, builds `bookmarkName = \`gsd/phase-\${phaseTag}-subagent-\${slot.idx}\``, calls `validateAgentBookmarkName`, and runs `jj bookmark create -r <slot.headChange> -- <bookmarkName>` via `vcsExec`. Per D-02 this is wasted work — the octopus structure already references each slot's head as a parent of the phase-merge via `createSubagentSlot`.

    (2) Delete the batched bookmark-delete block at lines 447-465 in `performJjParallelFanIn`'s clean branch in full. The block constructs `agentBookmarkNames[]` from `handle.workspaces.length`, calls `validateAgentBookmarkName` for each, then runs one `jj bookmark delete -- <name1> <name2> ...` via `vcsExec` and throws on non-zero exit.

    (3) Delete the post-delete surplus sweep at lines 471-494 in full (the `jj bookmark list` invocation, the prefix filter, and the `surplusBookmarks.push(...)` accumulator). KEEP the initialization `surplusBookmarks: string[] = []` (it stays `[]` by construction on the clean branch; the type-contract field on `FanInResult` is unchanged).

    MUST PRESERVE (do not touch — load-bearing):
    - `octopus.createPhaseStructure` + `createSubagentSlot` loop at lines 200-222
    - Manifest writer at lines 246-265 (SDK-internal observability per Phase 9 Assumption A6)
    - Pre-merge `currentHeads` re-resolution at lines 311-344 (Rule 1 fix from plan 09.05)
    - N-parent `jj new` at lines 356-371 (this IS the canonical reference for parents — the reason bookmarks are vestigial)

    If the `validateAgentBookmarkName` import becomes unused after deletions (3 call sites all removed), delete the import line as well. If it remains used elsewhere in the file, leave the import.

    After deletions, run `tsc --noEmit` against the SDK; the file must compile clean and no other site in `sdk/src/` must reference the deleted bookmark plumbing. If callers exist outside `jj/parallel.ts` that referenced any of these specific code paths, surface them in the SUMMARY — none are expected per the Phase 11 patterns map.
  </action>
  <verify>
    <automated>cd sdk && pnpm run build 2>&1 | grep -E "(error|TS)" | head -20 ; grep -nE "(bookmark create|bookmark delete|gsd/phase-.*-subagent-)" src/vcs/jj/parallel.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "jj bookmark create" sdk/src/vcs/jj/parallel.ts | grep -v '^#'` returns nothing (no surviving bookmark-create call).
    - `grep -n "jj bookmark delete" sdk/src/vcs/jj/parallel.ts | grep -v '^#'` returns nothing.
    - `grep -nE "gsd/phase-\\\$\\{phaseTag\\}-subagent-" sdk/src/vcs/jj/parallel.ts | grep -v '^#'` returns nothing in either the dispatch or fanIn body (the prefix string literal disappears with the deletions).
    - `grep -n "createPhaseStructure\|createSubagentSlot" sdk/src/vcs/jj/parallel.ts | wc -l` returns >= 2 (preserved octopus structure call sites still present).
    - `grep -n "jj new -r @ -r\|N-parent" sdk/src/vcs/jj/parallel.ts | wc -l` returns >= 1 (the load-bearing N-parent merge code path is still present).
    - `cd sdk && pnpm exec tsc --noEmit` exits 0 (file compiles clean).
    - `git diff --stat sdk/src/vcs/jj/parallel.ts` shows net-negative LOC (deletions only).
  </acceptance_criteria>
  <done>
    Bookmark-create loop, batched bookmark-delete block, and surplus-bookmark sweep all removed from `sdk/src/vcs/jj/parallel.ts`. Octopus structure intact. SDK builds clean. `FanInResult.surplusBookmarks` continues to be `[]` on the clean branch by construction.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flip cmd-parallel-jj.test.ts bookmark-presence assertions to workspace-list filter</name>
  <files>sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (lines 1-50 header + lines 120-200 N=2/3/4 clean-branch test body + lines 280-310 conflict-branch body + every `surplusBookmarks` mention)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-PATTERNS.md (`sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (MODIFY — D-02 flip)` section)
    - sdk/src/vcs/jj/parallel.ts (Task 1 result, for the new workspace-list shape the assertion proves)
  </read_first>
  <action>
    Replace every `surplusBookmarks` length assertion (existing precedent at lines 178 and 294, plus any other `expect(result.surplusBookmarks` occurrence) with an assertion on the post-fanIn workspace SET. The assertion proves that the workspaces created during dispatch were reaped post-fanIn — same INTENT as the original "bookmarks are cleaned up" assertion, expressed against the actual source of truth (workspaces).

    Replacement assertion shape (paste/adapt per test scenario, scoped to each N's `phaseTag`):
    use `vcs.workspace.list()` → filter by `path.includes('/phase-' + phaseTag + '-subagent-')` → assert filtered length is 0 on the clean branch and equals the number of un-merged workspaces on the conflict branch. Read the prefix-construction precedent at the (now-deleted) `jj/parallel.ts:489-493` pattern; phaseTag is the zero-padded form already in the test scope.

    KEEP UNTOUCHED:
    - TEST-14 `jj log -r 'divergent()' --no-graph` empty assertion (the octopus-topology invariant — completely independent of bookmark state).
    - All N=2/3/4 dispatch scenario titles + setup (the topology test surface is intact).
    - Conflict-branch behavior assertions on `result.conflicted`, `result.conflictedPaths`, `result.incompleteQueued`, `IncompleteWorkEntry.reason === 'merge-in-tree-conflict'`.
    - Crash-branch behavior assertions on `result.failedReaped`, `IncompleteWorkEntry.reason === 'crashed-with-uncommitted-work'`.

    Optional cosmetic: update any test title text that says "surplusBookmarks empty" to "workspaces reaped" or similar; INTENT-preserving rewording is acceptable. Behavioral semantics are the load-bearing piece.

    If the test file imports `validateAgentBookmarkName` or any other helper that became dead in Task 1, drop the import.
  </action>
  <verify>
    <automated>cd sdk && pnpm exec vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "surplusBookmarks\\.length" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts | grep -v '^#'` returns 0 lines (assertions flipped away).
    - `grep -nE "workspace\\.list\\(\\)\\.filter" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts | wc -l` returns >= 2 (at least the clean and conflict branches use the new shape; each `N ∈ {2,3,4}` may have its own).
    - `grep -nE "divergent\\(\\)" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts | wc -l` returns >= 1 (TEST-14 assertion preserved).
    - `cd sdk && pnpm exec vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts` reports all scenarios green (N=2/3/4 dispatch + clean fanIn + in-tree-conflict + crashed-agent + topology) with zero skips.
    - The `merge-in-tree-conflict` reason assertion and `crashed-with-uncommitted-work` reason assertion remain green (no enum surface change).
    - No new `retry: N` config and no `describe.skip` added (TEST-16 invariant preserved).
  </acceptance_criteria>
  <done>
    `cmd-parallel-jj.test.ts` asserts workspace reaping via `vcs.workspace.list()` rather than via bookmark presence. TEST-14 divergent() assertion untouched. Full test suite green on jj fixture.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan is a pure deletion + test-assertion flip on existing SDK code; no new attack surface, no new input parsing, no new IPC. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.01-01 | N/A | sdk/src/vcs/jj/parallel.ts | N/A | Internal SDK adapter migration — deletion of vestigial bookmark scaffolding. No new attack surface; no user input; no privilege boundary crossed. Surviving primitives (`vcsExec`, `expr.rev`, `octopus.createPhaseStructure`) are already covered by Phase 9 threat models. |
| T-11.01-02 | Tampering | FanInResult.surplusBookmarks field | accept | Field stays on type contract but value is trivially `[]` on clean branch by construction. No tampering surface — value is producer-derived, not user-derived. |
</threat_model>

<verification>
- Full SDK build (`pnpm run build` from `sdk/`) exits 0.
- `pnpm exec vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts` reports green for all N=2/3/4 scenarios + clean + conflict + crash + topology branches.
- `git diff --stat sdk/src/vcs/jj/parallel.ts` shows net-negative LOC.
- `scripts/lint-vcs-no-commit-id.cjs` continues to pass on the changed files (no commit_id leakage in either file).
- `scripts/lint-vcs-no-raw-git.cjs` continues to pass (jj/parallel.ts has no raw-git invocations and the test file is in `__tests__/`).
</verification>

<success_criteria>
- Phase 9 jj-side dispatch + fan-in functionally equivalent post-deletion: dispatch creates N workspaces with distinct change_ids; fan-in produces one N-parent merge; `divergent()` revset stays empty.
- Bookmark plumbing fully retired on jj — zero `gsd/phase-NN-subagent-IDX` literal references survive in `sdk/src/vcs/jj/parallel.ts`.
- Contract test surface flipped to source-of-truth assertions (workspaces, not bookmarks).
- Test-suite green count for `cmd-parallel-jj.test.ts` unchanged vs Phase 9 baseline (no skip regressions, no retry: N).
</success_criteria>

<output>
Create `.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-01-SUMMARY.md` when done. Record:
- Exact line ranges deleted from `sdk/src/vcs/jj/parallel.ts` (3 ranges).
- Number of assertions flipped in `cmd-parallel-jj.test.ts` and the new assertion shape used.
- Confirmation that TEST-14 `divergent()` assertion is byte-identical to Phase 9 baseline.
- Any unexpected callers/references encountered (none expected per patterns map).
</output>
