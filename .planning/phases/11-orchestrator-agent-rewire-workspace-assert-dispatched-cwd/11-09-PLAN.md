---
phase: 11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd
plan: 09
type: execute
wave: 4
depends_on: [11-02, 11-03]
gap_closure: true
files_modified:
  - sdk/src/vcs/jj/parallel.ts
  - get-shit-done/bin/lib/worktree-safety.cjs
  - get-shit-done/bin/gsd-tools.cjs
  - tests/jj-parallel-no-manifest-write.test.cjs
autonomous: true
requirements: [PROMPT-06, PROMPT-07]
tags: [cleanup, manifest-retire, jj, worktree-safety, gap-closure, no-orchestrator-sidecar-state]

must_haves:
  truths:
    - "VERIFICATION.md WR-01 WARNING closed: `sdk/src/vcs/jj/parallel.ts:232-245` no longer `mkdtempSync + writeFileSync`s the WAVE_WORKTREE_MANIFEST sidecar on every dispatch; `handle.manifest` is set to the empty string `''` to mirror `bin/lib/worktree-safety.cjs::reconstructHandleFromLegacyPlan` (line 412) and honor D-01's `no_orchestrator_sidecar_state` invariant"
    - "VERIFICATION.md WR-01 carry-over closed: the docstring at `sdk/src/vcs/jj/parallel.ts:23` (and the function-level comment at lines 150-152) no longer describe the manifest write as part of the contract"
    - "VERIFICATION.md WR-02 WARNING closed: `bin/lib/worktree-safety.cjs:489` `ok` derivation explicitly accounts for `incompleteQueued > 0` so callers cannot mark ROADMAP complete while retries are queued. The new derivation is `ok: fanIn.conflicted === false && (fanIn.failedReaped || []).length === 0 && (fanIn.incompleteQueued || 0) === 0`"
    - "VERIFICATION.md IN-02 INFO closed: `bin/gsd-tools.cjs:368-376` `TOP_LEVEL_USAGE` help text gains a `workspace.*` line listing the three new verbs (`workspace.assert-dispatched-cwd`, `workspace.parallel.dispatch`, `workspace.parallel.fan-in`)"
    - "VERIFICATION.md WR-04 INFO closed: `bin/gsd-tools.cjs:985-993` `worktree` case is either removed entirely OR routes ALL subcommands (not just `cleanup-wave`) through the deprecation-error path with a uniform message"
    - "Regression test `tests/jj-parallel-no-manifest-write.test.cjs` asserts: (a) the jj/parallel.ts source contains no `mkdtempSync.*gsd-wave-manifest` token (source-level guard against re-introduction); (b) running `performJjParallelDispatch` on a jj fixture produces a Handle whose `.manifest === ''`; (c) no new directory matching `gsd-wave-manifest-*` is created under `tmpdir()` during dispatch"
    - "WR-05 INFO (derivePhaseRoot returns non-existent path) is OUT OF SCOPE for this plan — flagged in CONTEXT as a forward-compat trap; not a blocker for any REQ-ID; record as deferred backlog item in the SUMMARY"
    - "IN-01 INFO (baselineOpId: undefined drops on JSON.stringify roundtrip in reconstructHandleFromLegacyPlan) is OUT OF SCOPE — same rationale: forward-compat trap with no current consumer; deferred"
  artifacts:
    - path: sdk/src/vcs/jj/parallel.ts
      provides: "Jj parallel dispatch with no orphaned sidecar write"
      contains: "manifest: ''"
    - path: get-shit-done/bin/lib/worktree-safety.cjs
      provides: "Cleanup helper whose ok-flag honors incompleteQueued"
      contains: "incompleteQueued || 0) === 0"
    - path: get-shit-done/bin/gsd-tools.cjs
      provides: "Top-level CLI help that documents the workspace.* surface"
      contains: "workspace.assert-dispatched-cwd"
    - path: tests/jj-parallel-no-manifest-write.test.cjs
      provides: "Regression test pinning the no-sidecar-write contract"
      contains: "gsd-wave-manifest"
  key_links:
    - from: sdk/src/vcs/jj/parallel.ts (cleaned dispatch)
      to: bin/lib/worktree-safety.cjs::reconstructHandleFromLegacyPlan
      via: "Both code paths now set handle.manifest = '' — parity verified"
      pattern: "manifest:\\s*''"
    - from: get-shit-done/bin/lib/worktree-safety.cjs (corrected ok derivation)
      to: FanInResult.incompleteQueued field
      via: "ok derivation gates on incompleteQueued in addition to conflicted + failedReaped"
      pattern: "incompleteQueued"
    - from: tests/jj-parallel-no-manifest-write.test.cjs
      to: sdk/src/vcs/jj/parallel.ts source content + tmpdir scan
      via: "Source-level grep + filesystem assertion that no manifest dir was created"
      pattern: "manifest.*''|gsd-wave-manifest"
---

<objective>
Close the trailing cleanup gaps left by VERIFICATION.md after the blockers in Plans 11-07 and 11-08 close:

- **WR-01 (WARNING):** `sdk/src/vcs/jj/parallel.ts:226-245` still `mkdtempSync(... 'gsd-wave-manifest-')` + `writeFileSync`s a WAVE_WORKTREE_MANIFEST sidecar on EVERY dispatch and stores the path on `handle.manifest`. Phase 11 D-01 declared this retired and `reconstructHandleFromLegacyPlan` correctly sets `manifest: ''`. The retirement claim and the implementation diverge — every jj dispatch leaks one tmpdir. Verifier's recommendation: remove the write entirely.

- **WR-02 (WARNING):** `bin/lib/worktree-safety.cjs:489` derives `ok` as `fanIn.conflicted === false && (fanIn.failedReaped || []).length === 0`. This excludes `incompleteQueued > 0` — a fan-in result that queued retries returns `ok: true` with a non-empty `pending[]`. Downstream consumers reading just `.ok` may mark a ROADMAP plan complete while retries are queued.

- **IN-02 (INFO):** `bin/gsd-tools.cjs:368-376` `TOP_LEVEL_USAGE` does not list the new `workspace.*` surface. `--help` users have no discoverability for the three new verbs.

- **WR-04 (INFO):** `bin/gsd-tools.cjs:985-993` `worktree` case is only deprecation-guarded for `cleanup-wave`; other subcommands silently fall through.

Also re-states D-01 architectural invariant: only persistent state is workspaces / bookmarks / HEADs. The jj/parallel.ts write directly violates this — closing it is the architectural completion proof of the Phase 11 collapse goal.

**OUT OF SCOPE (explicitly deferred to backlog with SUMMARY rationale):**
- **WR-05 INFO** (`derivePhaseRoot` returns non-existent path; callers must `mkdir -p`) — forward-compat trap; no current consumer hits ENOENT in production.
- **IN-01 INFO** (`reconstructHandleFromLegacyPlan` `baselineOpId: undefined` drops on JSON.stringify roundtrip) — forward-compat trap; no current consumer reads `baselineOpId`.

Both are explicit defers per `<planner_authority_limits>` (no current consumer = no blocker for any REQ-ID).

Purpose: Architectural cleanup. None of the REQ-IDs flip status as a direct consequence of THIS plan alone (the milestone blockers are addressed by Plans 11-07 + 11-08); but this plan removes the visible architectural drift between the stated D-01 invariant and the implementation.

Output: A jj/parallel.ts dispatch that writes nothing to disk except VCS state; a worktree-safety.cjs whose `ok` derivation matches its `pending[]` semantics; a `gsd-tools.cjs --help` that lists the workspace verbs; and a regression test pinning the no-write invariant against future drift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-CONTEXT.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-02-SUMMARY.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-03-SUMMARY.md

<interfaces>
<!-- Code excerpts the executor will modify or refer to. -->

From sdk/src/vcs/jj/parallel.ts:226-265 (the orphaned manifest writer — WR-01 source):

  // WAVE_WORKTREE_MANIFEST writer (VCS-19). New fields: plan_id, backend.
  // Allocated under a fresh `mkdtemp` to avoid collisions with concurrent
  // orchestrator processes (and with the workflow-markdown writer that
  // collapses in Phase 11). Every agentId already passed `validateAgentId`
  // above, so the `worktree-agent-${agentId}` branch literal is guaranteed
  // to satisfy the worktree-safety.cjs:334 regex.
  const manifestDir = mkdtempSync(join(tmpdir(), 'gsd-wave-manifest-'));
  const manifestPath = join(manifestDir, 'wave-worktree-manifest.json');
  const manifestBody = { /* ... */ };
  writeFileSync(manifestPath, JSON.stringify(manifestBody, null, 2), 'utf-8');

  return Object.freeze({
    phaseRoot,
    phaseNumber,
    mainBookmark,
    manifest: manifestPath,                    // <-- becomes '' after fix
    workspaces: Object.freeze(/* ... */),
  }) satisfies ParallelDispatchHandle;

From bin/lib/worktree-safety.cjs:404-421 (the parity target — sets manifest: ''):

  function reconstructHandleFromLegacyPlan(plan) {
    const entries = Array.isArray(plan?.entries) ? plan.entries : [];
    return Object.freeze({
      phaseRoot: plan.repoRoot,
      phaseNumber: plan.phaseNumber ?? 0,
      mainBookmark: entries[0]?.main_bookmark ?? '',
      manifest: '',                            // <-- parity target
      workspaces: Object.freeze(/* ... */),
    });
  }

From bin/lib/worktree-safety.cjs:485-493 (the WR-02 ok-derivation):

  const processedFromHandle = handle.workspaces.map((ws, i) => { /* ... */ });
  return {
    ok: fanIn.conflicted === false && (fanIn.failedReaped || []).length === 0,  // <-- excludes incompleteQueued
    action: plan.action,
    entries: processedFromHandle,
    pending,
  };

For bin/gsd-tools.cjs: use Grep to locate `TOP_LEVEL_USAGE` (around line 368) and `case 'worktree'` (around line 985); the exact body shapes vary — read the surrounding ~20 lines before editing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire WAVE_WORKTREE_MANIFEST write in jj/parallel.ts (WR-01)</name>
  <files>sdk/src/vcs/jj/parallel.ts</files>
  <read_first>
    Read in order:
    - sdk/src/vcs/jj/parallel.ts FULL file (or at minimum lines 1-30 for the top-of-file docstring, lines 150-270 for the dispatch body, and lines 295-360 for any fan-in code that may reference the manifest field — verify fan-in does NOT read handle.manifest from disk after the write retires)
    - get-shit-done/bin/lib/worktree-safety.cjs lines 404-421 (the `reconstructHandleFromLegacyPlan` parity target — set `manifest: ''` exactly the same way)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-CONTEXT.md D-01 (the architectural invariant being restored: "only persistent state is workspaces/bookmarks/HEADs themselves")
    - sdk/src/vcs/types.ts (Grep for `ParallelDispatchHandle` — confirm the `manifest: string` field shape accepts empty string; the type does not require the file to exist)
  </read_first>
  <action>
    In `sdk/src/vcs/jj/parallel.ts`, perform three surgical edits:

    Edit 1 — Delete the manifest writer block. Remove the entire block at lines 226-245 (the `// WAVE_WORKTREE_MANIFEST writer (VCS-19). ...` comment + the `mkdtempSync` + `writeFileSync` calls). Remove `manifestDir`, `manifestPath`, and `manifestBody` variable bindings — all dead after the write retires.

    Edit 2 — Remove the now-unreferenced imports at the top of the file: `mkdtempSync`, `writeFileSync` from `node:fs`; `tmpdir` from `node:os`. Keep `join` if it is still referenced by `derivePhaseRoot` at lines 139-147 (it IS used there — `join(mainRepoRoot, '.planning', 'phases')` — so keep `join`). Use Grep on the file to confirm each removed import is no longer referenced anywhere in the file before deleting.

    Edit 3 — Set `manifest: ''` on the returned Handle. In the `return Object.freeze({...})` block at lines 250-269 (line numbers shift after Edit 1's deletion), change `manifest: manifestPath` to `manifest: ''`. Add an inline comment on the same line: `manifest: '', // D-01: no orchestrator-managed sidecar state (parity with bin/lib/worktree-safety.cjs:reconstructHandleFromLegacyPlan)`.

    Edit 4 — Update the top-of-file docstring at line 23 area AND the function-level docstring at lines 150-152. In each, delete any sentence describing the manifest write as part of the contract (e.g. "write the WAVE_WORKTREE_MANIFEST", "manifest path on the handle is the on-disk JSON sidecar"). Replace with a single explicit note: "Phase 11 D-01: no manifest sidecar on disk; `handle.manifest` is the empty string. The Handle is the orchestrator's full state."

    Do NOT touch the fan-in body at lines 289+ — it does NOT read `handle.manifest` from disk per 11-03-SUMMARY (cleanup reconstructs from in-memory state).

    Do NOT touch `derivePhaseRoot` at lines 139-147 — WR-05 is explicitly out of scope.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && grep -L 'mkdtempSync' sdk/src/vcs/jj/parallel.ts && grep -q "manifest: ''" sdk/src/vcs/jj/parallel.ts && cd sdk && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md WR-01 WARNING closed: source-assertable — running `grep -c mkdtempSync sdk/src/vcs/jj/parallel.ts` returns 0 AND `grep -c gsd-wave-manifest sdk/src/vcs/jj/parallel.ts` returns 0 AND `grep -c "manifest: ''" sdk/src/vcs/jj/parallel.ts` is ≥ 1.
    - Source-assertable: dead imports gone — `grep -cE 'mkdtempSync|writeFileSync' sdk/src/vcs/jj/parallel.ts` is 0 in import lines.
    - Test-command-assertable: `cd sdk && pnpm tsc --noEmit` passes.
    - Test-command-assertable: `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts` is green (no regression — fan-in does not depend on the disk manifest).
    - Source-assertable: stale docstring claim removed — `grep -c 'write the WAVE_WORKTREE_MANIFEST' sdk/src/vcs/jj/parallel.ts` is 0.
  </acceptance_criteria>
  <done>jj/parallel.ts no longer writes to disk on dispatch. handle.manifest is ''. Docstring honest. tsc + existing jj parallel tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix worktree-safety.cjs ok derivation to honor incompleteQueued (WR-02)</name>
  <files>get-shit-done/bin/lib/worktree-safety.cjs</files>
  <read_first>
    Read in order:
    - get-shit-done/bin/lib/worktree-safety.cjs lines 444-498 (the `executeWorktreeWaveCleanupPlan` body — specifically line 489 is the broken `ok` derivation; lines 474-476 are the `incompleteQueued` to `pending[]` mapping; lines 477-487 are the per-entry `ok` calculation that has the same blind spot)
    - tests/wave-cleanup-executor.test.cjs lines 28-37 (the `ok:true, reason:'empty_plan'` contract — the fix must not break the empty-plan case where `pending` is empty and `ok:true` is correct because the early-return at line 446-454 is untouched)
    - tests/bug-3384-worktree-cleanup-manifest.test.cjs (regression test for the cleanup path; check whether it asserts `r.ok` in any branch — if yes, confirm the new derivation still satisfies it)
  </read_first>
  <action>
    In `get-shit-done/bin/lib/worktree-safety.cjs` around line 489, modify the `ok` field of the returned object so the derivation explicitly accounts for `incompleteQueued`:

    Replace the existing outer `ok` derivation:
    `ok: fanIn.conflicted === false && (fanIn.failedReaped || []).length === 0,`
    with:
    `ok: fanIn.conflicted === false && (fanIn.failedReaped || []).length === 0 && (fanIn.incompleteQueued || 0) === 0,`

    Also update the per-entry `ok` calculation in the `processedFromHandle` `.map` at line ~485:
    Replace `ok: !fanIn.conflicted && (fanIn.failedReaped || []).length === 0,` with `ok: !fanIn.conflicted && (fanIn.failedReaped || []).length === 0 && (fanIn.incompleteQueued || 0) === 0,`.

    Do NOT touch the empty-plan early-return at lines 446-454 — when there are no entries, `ok: true` is correct (nothing to merge, nothing to queue).

    Do NOT touch the outer catch's `{ ok: false, ... }` return at line 496 — adapter-thrown errors already correctly produce `ok: false`.

    Add a one-line comment directly above the outer `ok` line explaining the change: `// WR-02 fix: incompleteQueued > 0 means retries are queued; callers must not mark ROADMAP complete while queue is non-empty.`
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && OCC=$(grep -c 'incompleteQueued || 0) === 0' get-shit-done/bin/lib/worktree-safety.cjs); test "$OCC" -ge 2 && node --test tests/wave-cleanup-executor.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md WR-02 WARNING closed: source-assertable — `grep -c 'incompleteQueued || 0) === 0' get-shit-done/bin/lib/worktree-safety.cjs` is ≥ 2 (one for outer return, one for per-entry processedFromHandle map).
    - Source-assertable: `grep -c 'WR-02 fix' get-shit-done/bin/lib/worktree-safety.cjs` is ≥ 1 (the explanatory comment is present).
    - Test-command-assertable: `node --test tests/wave-cleanup-executor.test.cjs` passes (empty-plan + existing assertions still green; the new gate does not regress them because the existing tests do not produce `incompleteQueued > 0`).
    - Test-command-assertable: `node --test tests/bug-3384-worktree-cleanup-manifest.test.cjs` passes (cleanup manifest regression untouched by the ok-derivation change).
  </acceptance_criteria>
  <done>worktree-safety.cjs ok-flag now matches its pending[] semantics. Downstream callers cannot mark plans complete while retries are queued.</done>
</task>

<task type="auto">
  <name>Task 3: Document workspace.* verbs in gsd-tools.cjs TOP_LEVEL_USAGE + close worktree deprecation gap (IN-02 + WR-04)</name>
  <files>get-shit-done/bin/gsd-tools.cjs</files>
  <read_first>
    Read in order:
    - get-shit-done/bin/gsd-tools.cjs — use Grep with `TOP_LEVEL_USAGE` to find the help-text constant (around line 368-376 per VERIFICATION.md), then Read that range plus ~30 lines around it
    - get-shit-done/bin/gsd-tools.cjs — use Grep with `case 'worktree'` to find the dispatcher case (around line 985-993 per VERIFICATION.md), then Read that range plus ~30 lines around it
    - sdk/src/query/command-static-catalog-domain.ts lines 60-75 (the canonical handler dispatch — the canonical names `workspace.assert-dispatched-cwd`, `workspace.parallel.dispatch`, `workspace.parallel.fan-in` live here; use these literals in the help text)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md anti-patterns table (IN-02 and WR-04 entries)
  </read_first>
  <action>
    Two surgical edits to `get-shit-done/bin/gsd-tools.cjs`:

    Edit 1 — Extend TOP_LEVEL_USAGE (IN-02 closure). Locate the existing help text near line 368-376. It is a template literal listing available subcommands grouped by domain. Find the most natural insertion point (likely after an existing `worktree` or `head-ref` line, OR as a new grouped section). Add three lines (or one line summarizing the three) listing the new verbs. Suggested format mirroring existing entries:

    `  workspace.assert-dispatched-cwd   - assert cwd is a dispatched (non-primary) workspace`
    `  workspace.parallel.dispatch       - dispatch N parallel subagent workspaces (returns Handle JSON)`
    `  workspace.parallel.fan-in         - merge dispatched workspaces back; returns FanInResult JSON`

    Match the indentation and column alignment of existing lines. If the file uses tabs (per project CLAUDE.md), use tabs; if spaces are used in this specific file (Read to confirm), match.

    Edit 2 — Close worktree-case deprecation gap (WR-04 closure). Locate the `case 'worktree':` block (around line 985-993). The current shape (per VERIFICATION.md) only deprecation-guards the `cleanup-wave` subcommand. The remediation choice:

    Option A — Remove the `worktree` case entirely. If grep confirms no live caller invokes `gsd-tools.cjs worktree` for anything other than `cleanup-wave` (run `grep -rn "gsd-tools.*worktree" get-shit-done/ scripts/ tests/ docs/` from the repo root and inspect output), DELETE the entire `case 'worktree':` block. The dispatcher's default `case` already prints an "unknown command" error which is the correct UX.

    Option B — Extend the deprecation guard to cover all subcommands uniformly. If Option A's grep audit shows live callers exist beyond `cleanup-wave`, replace the existing per-subcommand branch with a single fall-through that emits the deprecation error for ANY `worktree` subcommand: `console.error('ERROR: gsd-tools worktree.* subcommands are deprecated. Use \`gsd-sdk query workspace.parallel.*\` (workspace.assert-dispatched-cwd, workspace.parallel.dispatch, workspace.parallel.fan-in) instead.'); process.exit(1);`.

    Whichever option is chosen, record the rationale in the SUMMARY.md (which one was used + the grep evidence).

    Do NOT touch any other case in the gsd-tools.cjs dispatcher — only the `worktree` case + the TOP_LEVEL_USAGE constant.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && grep -q 'workspace.assert-dispatched-cwd' get-shit-done/bin/gsd-tools.cjs && grep -q 'workspace.parallel.dispatch' get-shit-done/bin/gsd-tools.cjs && grep -q 'workspace.parallel.fan-in' get-shit-done/bin/gsd-tools.cjs && node get-shit-done/bin/gsd-tools.cjs --help 2>&1 | grep -q 'workspace'</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md IN-02 INFO closed: source-assertable — `grep -c 'workspace.assert-dispatched-cwd' get-shit-done/bin/gsd-tools.cjs` is ≥ 1 AND `grep -c 'workspace.parallel.dispatch' get-shit-done/bin/gsd-tools.cjs` is ≥ 1 AND `grep -c 'workspace.parallel.fan-in' get-shit-done/bin/gsd-tools.cjs` is ≥ 1.
    - Behavioral-assertable: `node get-shit-done/bin/gsd-tools.cjs --help` (or whatever the help invocation is — verify by Read of the file) prints output containing `workspace.assert-dispatched-cwd` literally. If the help command path is different, document it in the SUMMARY.
    - VERIFICATION.md WR-04 INFO closed: either source-assertable Option A (`grep -c "case 'worktree'" get-shit-done/bin/gsd-tools.cjs` is 0) OR source-assertable Option B (`grep -c "deprecated" get-shit-done/bin/gsd-tools.cjs` increased by ≥ 1 in the worktree case body, and ALL worktree subcommands route through the deprecation error — verify by reading the case body).
  </acceptance_criteria>
  <done>--help users discover the workspace.* surface. worktree subcommands are uniformly deprecation-guarded (or removed). Architectural drift between current CLI surface and Phase 11 deliverables is gone.</done>
</task>

<task type="auto">
  <name>Task 4: Regression test pinning the no-manifest-write invariant (WR-01 closure proof)</name>
  <files>tests/jj-parallel-no-manifest-write.test.cjs</files>
  <read_first>
    Read in order:
    - tests/bug-3384-worktree-cleanup-manifest.test.cjs (FULL file — the canonical Phase 11 manifest-related regression test pattern using `fs.readFileSync` + `assert.match` / `assert.doesNotMatch`; mirror its structure)
    - tests/bug-3097-3099-executor-worktree-path-safety.test.cjs (second pattern reference)
    - sdk/src/vcs/jj/parallel.ts post-Task-1 state (the source-level guard target — confirm `mkdtempSync` and `gsd-wave-manifest` are gone)
  </read_first>
  <action>
    Create `tests/jj-parallel-no-manifest-write.test.cjs`. The test has TWO layers — a source-level guard (always runs) and a filesystem-level guard (only runs when a live jj fixture is available; skip otherwise):

    ```javascript
    const test = require('node:test');
    const assert = require('node:assert');
    const fs = require('node:fs');
    const path = require('node:path');
    const os = require('node:os');

    const repoRoot = path.resolve(__dirname, '..');
    const PARALLEL_TS = fs.readFileSync(
      path.join(repoRoot, 'sdk/src/vcs/jj/parallel.ts'),
      'utf-8'
    );

    test.describe('WR-01 source-level guard (always runs)', () => {
      test.test('jj/parallel.ts source contains no manifest-write tokens', () => {
        assert.doesNotMatch(
          PARALLEL_TS,
          /mkdtempSync\s*\([^)]*gsd-wave-manifest/,
          'WR-01: sdk/src/vcs/jj/parallel.ts must not mkdtempSync a gsd-wave-manifest dir (D-01 invariant: no orchestrator-managed sidecar state)'
        );
        assert.doesNotMatch(
          PARALLEL_TS,
          /writeFileSync\s*\([^)]*wave-worktree-manifest/,
          'WR-01: sdk/src/vcs/jj/parallel.ts must not writeFileSync the wave-worktree-manifest.json sidecar'
        );
      });

      test.test('jj/parallel.ts returns manifest: "" (parity with reconstructHandleFromLegacyPlan)', () => {
        assert.match(
          PARALLEL_TS,
          /manifest:\s*''/,
          'WR-01: handle.manifest must be the empty string (mirrors bin/lib/worktree-safety.cjs:reconstructHandleFromLegacyPlan)'
        );
      });

      test.test('jj/parallel.ts docstring no longer claims the manifest write is part of the contract', () => {
        assert.doesNotMatch(
          PARALLEL_TS,
          /write the WAVE_WORKTREE_MANIFEST/,
          'WR-01: docstring claim that the manifest is written must be removed'
        );
      });
    });

    test.describe('WR-01 filesystem-level guard (skipped if jj fixture unavailable)', { skip: true }, () => {
      // Implementer note: if a jj-fixture helper exists in the test suite (e.g. the
      // pattern used by cmd-parallel-jj.test.ts), upgrade this describe block to
      // run a live dispatch + scan /tmp for new gsd-wave-manifest-* dirs. For now
      // the source-level guard is sufficient to pin the contract; the live test
      // is opportunistic.
      test.test('live dispatch produces no gsd-wave-manifest-* dir under tmpdir', () => {
        const before = fs.readdirSync(os.tmpdir()).filter((d) => d.startsWith('gsd-wave-manifest-'));
        // ... live dispatch via createVcsAdapter(repoRoot).workspace.parallel.dispatch({...}) ...
        const after = fs.readdirSync(os.tmpdir()).filter((d) => d.startsWith('gsd-wave-manifest-'));
        assert.deepStrictEqual(after, before, 'WR-01: live jj dispatch must not create a gsd-wave-manifest-* tmpdir');
      });
    });
    ```

    Adapt the require / describe pattern to whichever framework the other `.test.cjs` files use (read `tests/bug-3384-worktree-cleanup-manifest.test.cjs` first — it's the closest precedent). If it uses `node:test` describe/test, mirror it; if it uses a different runner (e.g. mocha), adapt.

    The source-level guard is the load-bearing assertion — it's always runnable and would catch any future re-introduction of the manifest write. The filesystem-level live-fixture guard is annotated `skip: true` and left as an opportunistic-upgrade hook for whoever next touches jj-fixture testing.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && node --test tests/jj-parallel-no-manifest-write.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Test-command-assertable: `node --test tests/jj-parallel-no-manifest-write.test.cjs` passes (all source-level guards green; filesystem-level guard skips cleanly).
    - Source-assertable: the test file exists at the listed path.
    - Source-assertable: `grep -c 'doesNotMatch' tests/jj-parallel-no-manifest-write.test.cjs` is ≥ 2 (both `mkdtempSync.*gsd-wave-manifest` and `writeFileSync.*wave-worktree-manifest` anti-patterns are pinned).
    - Source-assertable: `grep -c 'manifest:.*'\'\'' tests/jj-parallel-no-manifest-write.test.cjs` is ≥ 1 (the positive assertion is present).
    - WR-01 is protected against silent regression — any future edit that re-introduces the manifest write fails this test.
  </acceptance_criteria>
  <done>Regression test landed. The no-sidecar-state invariant is now a pinned source-level contract; D-01 cannot silently drift again.</done>
</task>

</tasks>

<verification>
  Overall phase checks for this gap-closure plan:
  - `cd sdk && pnpm tsc --noEmit` green (Task 1 compiles).
  - `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-jj.test.ts` green (no jj regression).
  - `node --test tests/wave-cleanup-executor.test.cjs` green (Task 2's ok-derivation change does not regress existing tests).
  - `node --test tests/bug-3384-worktree-cleanup-manifest.test.cjs` green (existing cleanup-path regression survives).
  - `node --test tests/jj-parallel-no-manifest-write.test.cjs` green (Task 4 regression test passes).
  - `grep -c mkdtempSync sdk/src/vcs/jj/parallel.ts` is 0 (WR-01 closed).
  - `grep -c 'incompleteQueued || 0) === 0' get-shit-done/bin/lib/worktree-safety.cjs` is ≥ 2 (WR-02 closed).
  - `grep -c 'workspace.assert-dispatched-cwd' get-shit-done/bin/gsd-tools.cjs` is ≥ 1 (IN-02 closed).
  - WR-04 closure verified by reading the post-edit `case 'worktree'` block per the Option A/B branch taken.
</verification>

<success_criteria>
  - All Phase 11 anti-patterns listed in VERIFICATION.md `Anti-Patterns Found` table at severity Warning or higher are addressed (WR-01, WR-02) or explicitly deferred with rationale (WR-05).
  - All severity-Info items in the same table addressed (IN-02, WR-04) or explicitly deferred (IN-01).
  - The D-01 invariant ("only persistent state is workspaces/bookmarks/HEADs themselves; no orchestrator-managed sidecar files") is restored — implementation matches stated framing.
  - SUMMARY records the deferred items (WR-05, IN-01) so backlog tracking is explicit.
</success_criteria>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sdk → filesystem | Task 1 removes a `mkdtempSync` + `writeFileSync` pair. After the fix, the dispatch path performs zero filesystem writes outside VCS-managed paths (and even those are wrapped by the adapter). |
| CLI dispatcher → help output | Task 3 adds three new help lines but does not parse new arguments — no new attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.09-01 | Information Disclosure | The previous manifest write leaked workspace paths into world-readable `/tmp` | mitigate | Task 1 removes the write entirely; no leak path remains. |
| T-11.09-02 | Tampering | A second process could craft a fake `gsd-wave-manifest-*` dir matching the previous naming convention | mitigate | After Task 1, no code reads the dir; even if a fake exists it is ignored. Task 4 source-level guard prevents re-introduction. |
| T-11.09-03 | DoS | Previously, repeated dispatches leaked tmpdirs (no cleanup) | mitigate | Task 1 removes the source of the leak. Existing tmpdirs from before this fix remain on disk and are harmless; if user wants to reclaim space they can run `rm -rf /tmp/gsd-wave-manifest-*` manually (note this in SUMMARY recovery instructions). |
| T-11.09-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — verification trivially holds. |

</threat_model>

<output>
Create `.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-09-SUMMARY.md` when done. SUMMARY must:
- Confirm WR-01, WR-02, IN-02, WR-04 closures with grep evidence.
- Record the Option A vs Option B choice for Task 3 Edit 2 (worktree case removal vs uniform deprecation) and the grep audit evidence behind the decision.
- Explicitly record WR-05 and IN-01 as DEFERRED with rationale (no current consumer; forward-compat traps).
- Note that this plan does NOT flip any REQ-ID status on its own — it is architectural completion of the Phase 11 invariants. The REQ-ID flips happen via 11-07 (VCS-20, PROMPT-08) and 11-08 (PROMPT-07, PARALLEL-06).
- Mention the one-time `/tmp/gsd-wave-manifest-*` cleanup hint for users who ran Phase 11 with the leaking implementation.
</output>
