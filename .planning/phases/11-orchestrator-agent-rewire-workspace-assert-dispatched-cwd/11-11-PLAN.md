---
phase: 11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd
plan: 11
type: execute
wave: 5
depends_on: [11-04, 11-07]
gap_closure: true
files_modified:
  - sdk/src/query/workspace-assert-dispatched-cwd.ts
  - agents/gsd-executor.md
  - tests/cmd-workspace-assert-dispatched-cwd.test.ts
  - tests/agent-prompts-no-raw-git.test.cjs
autonomous: true
requirements: [PROMPT-08, VCS-20]
tags: [gap-closure, blocker, no-raw-git, agent-prompt, regression-test, run-2]

must_haves:
  truths:
    - "VERIFICATION.md SC-2 / REVIEW.md CR-03 BLOCKER closed: `agents/gsd-executor.md:431` no longer contains `git rev-parse --show-toplevel`. The FATAL recovery diagnostic dump (lines 424-432) instead reads the resolved primary workspace fs path from a NEW field `primaryWorkspacePath` on the existing `DISPATCH_CHECK` JSON payload — no separate shell-out, no raw git invocation in the agent prompt, no jj-fallback dead-code branch on colocated checkouts."
    - "Fix path selected (per VERIFICATION.md `missing:` options at line 45): option (c) — `workspace.assert-dispatched-cwd` extends its output envelope to include `primaryWorkspacePath` (the resolved fs path of `entries[0]`, the primary workspace, regardless of match outcome). This is the cleanest seam per the verifier: (1) the verb already iterates `vcs.workspace.list()` and has a `resolveJjWorkspacePath()` helper from Plan 11-07 CR-01 closure, (2) option (a) `workspace.list --pick 'workspaces[0].path'` is impossible — `workspace.list` is NOT a CLI verb (only `workspace.assert-dispatched-cwd` and `workspace.parallel.*` are exposed via gsd-tools top-level help), (3) option (b) would require adding a whole new `repo-root` verb for a single-line diagnostic. Option (c) reuses an existing entry point, keeps the agent prompt to ONE verb call, and centralizes the name→path resolution on the SDK side where backend-opacity already lives."
    - "Output envelope shape change (additive, non-breaking): the verb now returns `{ok, workspaceName, workspacePath, isPrimary, primaryWorkspacePath}` on both backends. `primaryWorkspacePath` is the resolved fs path of `entries[0]` (using `resolveJjWorkspacePath` on jj, `safeRealpath(entries[0].path)` on git). When `entries[0]` cannot be resolved (e.g., empty workspace list), `primaryWorkspacePath` is `null`. The field is ALWAYS present in the envelope so consumers see a stable shape — same `null`-not-`undefined` discipline as `workspaceName`/`workspacePath` in the failure case (existing verb pattern at lines 116-128)."
    - "VERIFICATION.md `missing:` class-wide regression-test gap closed: NEW test file `tests/agent-prompts-no-raw-git.test.cjs` greps `agents/gsd-executor.md` for any standalone `git ` invocation (word-boundary `\\bgit\\s+(rev-parse|status|log|diff|...)` pattern) and fails on any match. Scope is `agents/gsd-executor.md` initially (the file where the violation occurred); the test design includes a list of additional agent files (`agents/*.md`) that can be cheaply extended as more agents land. The reviewer (REVIEW.md CR-03) explicitly named `\\bgit \\b` as the pattern — we use a slightly tighter `\\bgit\\s+[a-z-]+` to avoid false positives on `digit`, `github`, `git tag` in comments-about-git, etc."
    - "VERIFICATION.md SC-2 SC-1b caveat retired: the structural collapse in gsd-executor.md (lines 419-436 — single DISPATCH_CHECK invocation) remains intact; only the inner diagnostic-probe line changes. The 'no backend-kind branching in agent prompts' invariant from PROMPT-08 is now strictly enforced at file level by the new class-wide grep test."
    - "Requirement PROMPT-08 flips BLOCKED → SATISFIED. The Plan 11-07 CR-04 closure intent (dump payload + PWD + repo root on FATAL for triage) is preserved — operators still see the resolved primary workspace fs path on every FATAL — but now via an SDK-mediated value rather than a raw git invocation. REQUIREMENTS.md:122 (PROMPT-08 'Complete') becomes consistent with verifier truth."
    - "Parity-test scope: `tests/cmd-workspace-assert-dispatched-cwd.test.ts` extends from {ok, workspaceName, workspacePath, isPrimary} parity to ALSO assert `primaryWorkspacePath` parity across git and jj fixtures. The test must pin: (a) `primaryWorkspacePath` is non-null on both backends for a normal workspace list, (b) the value equals `realpath(entries[0].path)` semantically on git and equals `jj workspace root --name <default-name>` semantically on jj, (c) the field is present (even if null) in the failure case (empty workspace list, unmatched cwd) — same `null`-not-`undefined` discipline."
    - "Out-of-scope (deferred to a future plan or phase): REVIEW.md WR-01 (dispatch-cwd-safety.md protected-ref doc overstatement), WR-02 (N+1 jj subprocess pattern — adjacent file but separate-defect; the verifier flagged it as a Phase 14 watch-item, NOT a Phase 11 blocker), WR-03 (cmd-workspace-assert-dispatched-cwd.test.ts backend-opacity-as-invariant pin — info-level), WR-04 (cleanFanIn predicate duplicated literal in worktree-safety.cjs — unrelated file), WR-05 (tests/wave-cleanup-executor.test.cjs missing combined failure mode — unrelated file), IN-02 (dispatch-cwd-safety.md recovery section orchestrator-retry guidance), IN-03 (manifest-retirement comment duplication). None are goal-blocking for PROMPT-08."
  artifacts:
    - path: sdk/src/query/workspace-assert-dispatched-cwd.ts
      provides: "Verb envelope extends to include resolved primary workspace fs path (backend-opaque, single source of truth for the agent's diagnostic dump)"
      contains: "primaryWorkspacePath"
    - path: agents/gsd-executor.md
      provides: "FATAL recovery diagnostic dump now reads primaryWorkspacePath from the existing DISPATCH_CHECK jq payload — zero raw git invocations anywhere in the file"
      contains: "primaryWorkspacePath"
    - path: tests/cmd-workspace-assert-dispatched-cwd.test.ts
      provides: "Parity test extends to pin primaryWorkspacePath presence + value across git and jj backends"
      contains: "primaryWorkspacePath"
    - path: tests/agent-prompts-no-raw-git.test.cjs
      provides: "Class-wide regression net: greps agent prompt files for raw git invocations and fails on any match (catches future re-introductions of the same class of defect)"
      contains: "agents/gsd-executor.md"
  key_links:
    - from: agents/gsd-executor.md
      to: sdk/src/query/workspace-assert-dispatched-cwd.ts
      via: "REPO_ROOT=$(echo \"$DISPATCH_CHECK\" | jq -r '.primaryWorkspacePath // \"<unresolvable>\"')"
      pattern: "primaryWorkspacePath // \"<unresolvable>\""
    - from: tests/agent-prompts-no-raw-git.test.cjs
      to: agents/gsd-executor.md
      via: "fs.readFileSync + assert.doesNotMatch(..., /\\bgit\\s+(rev-parse|...)/)"
      pattern: "doesNotMatch.*\\\\bgit"
---

<objective>
Close run-1 BLOCKER cluster B: `agents/gsd-executor.md:431` runs `git rev-parse --show-toplevel 2>/dev/null || jj workspace root 2>/dev/null || echo "<unresolvable>"` in the dispatched-cwd FATAL recovery diagnostic dump (added by Plan 11-07 CR-04 closure). This is a direct violation of the project rule `project_no_raw_git` recorded in CLAUDE.md and MEMORY: "VCS adapter must cover read AND write; lint guard is whole-repo default-deny on `git`, not just mutating verbs; even `git status` perturbs colocated jj state." On the project's own colocated git+jj checkout (`vcs.adapter: jj`), the git branch ALWAYS fires first, succeeds, and may report a misleading toplevel when the agent is in a non-default jj workspace whose fs path sits outside the git toplevel — making the fallback to jj dead code on every colocated invocation.

Purpose: flip requirement PROMPT-08 from BLOCKED → SATISFIED by (1) extending `workspace.assert-dispatched-cwd` to carry the resolved primary workspace fs path in its envelope (verifier-named fix option (c) — the cleanest seam because the verb already does name→path resolution on jj), (2) rewriting the agent's FATAL recovery diagnostic to read `primaryWorkspacePath` from the existing `$DISPATCH_CHECK` jq payload, and (3) adding a class-wide regression test that fails on any standalone `git ` invocation in `agents/gsd-executor.md` so future re-introductions of this defect class are caught at commit time.

Output: an additive `primaryWorkspacePath` field on the verb's envelope (backend-opaque on both backends), a raw-git-free agent prompt, and a regression test that pins the deny-list intent at file level.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-CONTEXT.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-REVIEW.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-07-SUMMARY.md
@sdk/src/query/workspace-assert-dispatched-cwd.ts
@agents/gsd-executor.md
@tests/cmd-workspace-assert-dispatched-cwd.test.ts
@./CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From sdk/src/query/workspace-assert-dispatched-cwd.ts — current envelope (lines 116-146):
- failure shape: `{ok:false, workspaceName:null, workspacePath:null, isPrimary:false}`
- success/match shape: `{ok: !isPrimary, workspaceName: matched.path, workspacePath: matchedPath ?? matched.path, isPrimary}`
- existing helpers:
  - `safeRealpath(p: string): string | null` (lines 36-42) — try realpathSync, swallow ENOENT.
  - `resolveJjWorkspacePath(repoCwd: string, name: string): string | null` (lines 51-68) — shells out to `jj workspace root --name <NAME>` via `vcsExec`; returns null on non-zero exit. Plan 11-07 CR-01 closure.
- existing iteration: lines 101-114 loop over `entries` calling `resolveJjWorkspacePath` per entry. Entries[0] is the primary by convention (lines 86-88 docstring).

From agents/gsd-executor.md:418-436 — current FATAL recovery (Plan 11-07 CR-04 closure):
- single SDK call: `DISPATCH_CHECK=$(gsd-sdk query workspace.assert-dispatched-cwd --cwd .)`
- jq extracts: `OK`, `IS_PRIMARY`, `WS_NAME`
- diagnostic dump on FATAL: payload, $PWD, REPO_ROOT (current raw-git line at :431)
- exit 1 with FATAL + RECOVERY messages

From tests/cmd-workspace-assert-dispatched-cwd.test.ts (Plan 11-07 created) — parity test scaffolding exists:
- fixtures construct minimal git and jj workspace lists.
- assertions pin {ok, workspaceName, workspacePath, isPrimary} across both backends.
- adding `primaryWorkspacePath` requires extending each fixture's expected output object plus one or two new test cases for the failure-with-still-resolved-primary scenario.

From CLAUDE.md + MEMORY entry `project_no_raw_git`:
- project rule: "No raw git anywhere in jj-port. VCS adapter must cover read AND write; lint guard is whole-repo default-deny on `git`, not just mutating verbs."
- the existing repo-wide lint (`scripts/lint-no-raw-git.sh` or equivalent) operates at the scripts/ level; the new file-targeted test extends the same intent to AGENT PROMPT FILES specifically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend workspace.assert-dispatched-cwd to emit primaryWorkspacePath in its envelope</name>
  <files>sdk/src/query/workspace-assert-dispatched-cwd.ts, tests/cmd-workspace-assert-dispatched-cwd.test.ts</files>
  <read_first>
    - sdk/src/query/workspace-assert-dispatched-cwd.ts (the file being modified — entire file, focus on lines 70-146 for the handler body, lines 51-68 for the existing jj resolver, and lines 36-42 for safeRealpath)
    - tests/cmd-workspace-assert-dispatched-cwd.test.ts (the parity test file — needs new assertions for primaryWorkspacePath)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md (`gaps:` block for the no-raw-git truth — verifier's `missing:` option (c) at line 45 is what this task implements)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-REVIEW.md (CR-03 section)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-07-SUMMARY.md (Plan 11-07 closed CR-01; the jj resolver this task reuses was added there)
  </read_first>
  <action>
    Edit `sdk/src/query/workspace-assert-dispatched-cwd.ts`:

    1. In the handler body, AFTER the existing `entries` retrieval (line 81) and BEFORE the cwd-match loop (line 101), compute `primaryWorkspacePath` by resolving `entries[0]` (if present):
       - If `entries.length === 0`, `primaryWorkspacePath = null`.
       - If `vcs.kind === 'jj'`, `primaryWorkspacePath = resolveJjWorkspacePath(cwd, entries[0].path)` (returns string-or-null on resolve failure).
       - Else (git), `primaryWorkspacePath = safeRealpath(entries[0].path)` (returns string-or-null on ENOENT).
       - The variable is `const primaryWorkspacePath: string | null = ...`.

    2. Update BOTH return-shapes to include the new field:
       - Failure branch (lines 116-128): add `primaryWorkspacePath` to the returned `data` object.
       - Success branch (lines 138-146): add `primaryWorkspacePath` to the returned `data` object.
       - Field placement: at the END of the object, after `isPrimary`. This is additive and non-breaking — existing consumers reading `{ok, workspaceName, workspacePath, isPrimary}` ignore the new field; the new consumer in gsd-executor.md (Task 2) opts in via jq.

    3. Update the file-level docstring (lines 1-28) to document the new field in the envelope contract: "Returns the flat predicate D-03 contract: `{ ok, workspaceName, workspacePath, isPrimary, primaryWorkspacePath }`. `primaryWorkspacePath` is the resolved fs path of `entries[0]` (the primary workspace), backend-opaque, always present (null on resolution failure or empty list). Plan 11-11 adds this field to retire the raw-`git rev-parse` probe from `agents/gsd-executor.md` per project rule `project_no_raw_git`."

    Edit `tests/cmd-workspace-assert-dispatched-cwd.test.ts`:

    4. Extend EACH existing test case's expected-output object literal with `primaryWorkspacePath: <expected-value>`. The expected values:
       - git fixture: `primaryWorkspacePath` equals `realpathSync(fixture.workspaces[0].path)`.
       - jj fixture: `primaryWorkspacePath` equals the resolved fs path of the default workspace (the test fixture sets this up via its existing `vcsExec` mock; mirror what the per-entry resolver returns in the existing iteration test).
       - When the workspace list is intentionally empty (if such a fixture exists), `primaryWorkspacePath` equals `null`.

    5. Add ONE new test case `'primaryWorkspacePath is resolved even when cwd does not match any workspace (failure case)'`: cwd is set to a path outside the workspace list so `matchedIndex === -1`, the failure-branch return runs, but `primaryWorkspacePath` is STILL the resolved fs path of `entries[0]`. This pins the contract that the new field is independent of the match outcome — the agent prompt's FATAL recovery needs the primary path EXACTLY when the verb returns ok:false.

    Do NOT change the existing function signatures, the cwd-match loop body, or the `vcsExec` shell-out. Do NOT introduce new VCS adapter methods.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done && pnpm --filter ./sdk exec tsx --test tests/cmd-workspace-assert-dispatched-cwd.test.ts 2>&1 | tee /tmp/p11-11-t1.log ; tail -30 /tmp/p11-11-t1.log ; grep -E "^# fail [1-9]" /tmp/p11-11-t1.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -c 'primaryWorkspacePath' sdk/src/query/workspace-assert-dispatched-cwd.ts` returns >= 3 (one const declaration + two return-shape inclusions; docstring may add one more).
    - source assertion: `grep -nE 'const primaryWorkspacePath' sdk/src/query/workspace-assert-dispatched-cwd.ts` returns exactly 1 match.
    - source assertion: BOTH return statements (around lines 120-128 and 138-146) include `primaryWorkspacePath` — `grep -B1 -A8 'return {' sdk/src/query/workspace-assert-dispatched-cwd.ts | grep -c primaryWorkspacePath` returns >= 2.
    - source assertion: the docstring update is present — `grep -F 'primaryWorkspacePath' sdk/src/query/workspace-assert-dispatched-cwd.ts | head -1 | grep -c "Plan 11-11"` returns >= 0 (informational — exact docstring wording at the executor's discretion, but the rationale citation should appear).
    - source assertion (regression-safety): existing helpers untouched — `grep -c "function safeRealpath\\|function resolveJjWorkspacePath" sdk/src/query/workspace-assert-dispatched-cwd.ts` returns 2 (unchanged from pre-edit).
    - source assertion (regression-safety): the cwd-match loop body is unchanged — `grep -A 14 "for (let i = 0; i < entries.length" sdk/src/query/workspace-assert-dispatched-cwd.ts` output equals the pre-edit baseline (the new computation lives BEFORE the loop, not inside it).
    - behavior assertion: `tests/cmd-workspace-assert-dispatched-cwd.test.ts` exits 0; new `primaryWorkspacePath`-pin test cases all pass.
    - parity assertion: for the git+jj fixture pair with semantically-equivalent input, the test asserts the verb returns the SAME `primaryWorkspacePath` value (modulo realpath normalization) — pinning the backend-opaque envelope shape per the verifier's "no backend-kind branching at consumer layer" intent.
  </acceptance_criteria>
  <done>
    `workspace.assert-dispatched-cwd` envelope additively includes `primaryWorkspacePath` on both backends. The field is the resolved fs path of `entries[0]` (primary workspace), always present (null on resolution failure or empty list), independent of cwd-match outcome. Parity test pins the new field across git and jj fixtures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace raw `git rev-parse` in gsd-executor.md:431 with jq read of primaryWorkspacePath from existing DISPATCH_CHECK payload</name>
  <files>agents/gsd-executor.md</files>
  <read_first>
    - agents/gsd-executor.md (the file being modified — focus on lines 410-440, the dispatched-cwd FATAL recovery block)
    - sdk/src/query/workspace-assert-dispatched-cwd.ts (post Task 1 — verb now emits primaryWorkspacePath in both success and failure shapes)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md (`gaps:` second truth — the project-rule violation with full citation)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-REVIEW.md (CR-03 section + the fix sketch at lines 192-196)
    - ./CLAUDE.md (project rules — anchor for the no-raw-git invariant)
    - $HOME/.claude/projects/-Users-LoganDark-Documents-Projects-get-shit-done/memory/MEMORY.md (project_no_raw_git entry — confirm the rule's exact wording)
  </read_first>
  <action>
    Edit `agents/gsd-executor.md` line 431. The current line is:

    `  REPO_ROOT=$(git rev-parse --show-toplevel 2>/dev/null || jj workspace root 2>/dev/null || echo "<unresolvable>")`

    Replace it with a single jq read from the existing `$DISPATCH_CHECK` payload (already captured at line 419):

    `  REPO_ROOT=$(echo "$DISPATCH_CHECK" | jq -r '.primaryWorkspacePath // "<unresolvable>"')`

    Update the surrounding comment block (lines 424-428) — adjust the wording to reflect that REPO_ROOT now comes from the SDK verb's envelope, not a shell probe. Add a parenthetical reference to Plan 11-11 / `project_no_raw_git`. Keep the diagnostic-purpose intent (operator field-triage) intact.

    Do NOT touch:
    - The DISPATCH_CHECK invocation at line 419.
    - The OK/IS_PRIMARY/WS_NAME jq extractions at lines 420, 422, 423.
    - The FATAL / RECOVERY echo statements at lines 433-434.
    - The exit 1 at line 435.
    - The closing fi at line 436.

    Do NOT introduce a new variable beyond REPO_ROOT (the existing name is preserved so any operator runbook references still resolve). Do NOT add a `--cwd` arg to the jq invocation — the verb call at line 419 already passed `--cwd .` and the payload carries the primary path resolved against that cwd.
  </action>
  <verify>
    <automated>grep -nE "\\bgit\\b" agents/gsd-executor.md | grep -v "git commit" | grep -v "@~/.claude" ; FOUND=$(grep -cE "git rev-parse|git status|git log|git diff|git checkout|git worktree|git config" agents/gsd-executor.md) ; [ "$FOUND" -eq 0 ] || { echo "FAIL: raw git invocations still present" ; exit 1 ; } ; grep -F 'primaryWorkspacePath // "<unresolvable>"' agents/gsd-executor.md || { echo "FAIL: primaryWorkspacePath jq read missing" ; exit 1 ; } ; exit 0</automated>
  </verify>
  <acceptance_criteria>
    - source assertion (negative — the BLOCKER's defining grep): `grep -F 'git rev-parse' agents/gsd-executor.md` returns ZERO hits.
    - source assertion (positive): `grep -F 'primaryWorkspacePath // "<unresolvable>"' agents/gsd-executor.md` returns one hit on or adjacent to line 431.
    - source assertion (class-wide negative): `grep -E "\\bgit (rev-parse|status|log|diff|checkout|worktree|config|add|commit|push|pull|fetch|branch|merge|rebase|reset|stash)" agents/gsd-executor.md | grep -v "^#" | grep -v "\\bgit commit\\b is the established"` returns ZERO uncommented occurrences. (Note: `git commit` MAY remain in narrative docstring text referencing the established conceptual operation — the SDK's commit verb still routes through jj per `project_no_raw_git` carve-out at runtime. The lint regex below scopes to invocation-shape, not noun-shape.)
    - source assertion (regression-safety): DISPATCH_CHECK invocation at line ~419 unchanged — `grep -F 'DISPATCH_CHECK=$(gsd-sdk query workspace.assert-dispatched-cwd --cwd .)' agents/gsd-executor.md` returns one hit.
    - source assertion (regression-safety): OK / IS_PRIMARY / WS_NAME jq extractions unchanged — `grep -cE '^\\s*(OK|IS_PRIMARY|WS_NAME)=\\$\\(echo' agents/gsd-executor.md` returns 3.
    - source assertion (regression-safety): FATAL + RECOVERY echo lines unchanged — `grep -cE '"FATAL: cwd is not a dispatched|"RECOVERY: cd into the workspace path"' agents/gsd-executor.md` returns 2.
    - behavior assertion: Task 3's new class-wide regression test passes (`node --test tests/agent-prompts-no-raw-git.test.cjs` exits 0 after Task 3 lands).
  </acceptance_criteria>
  <done>
    `agents/gsd-executor.md` contains zero raw `git` invocations. The FATAL recovery diagnostic dump now sources REPO_ROOT from the SDK verb's envelope via a single jq read — same diagnostic content for operators, zero project-rule violation. The structural collapse from Plan 11-07 SC-1b remains intact (still a single SDK call, still backend-opaque).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create tests/agent-prompts-no-raw-git.test.cjs — class-wide regression net against re-introduction of raw git in agent prompts</name>
  <files>tests/agent-prompts-no-raw-git.test.cjs</files>
  <read_first>
    - agents/gsd-executor.md (post Task 2 — the file the new test grep-asserts against)
    - tests/quick-md-parallel-dispatch.test.cjs (reference shape — read-only source product files via fs.readFileSync, assert with node:test + node:assert; same conventions, same `allow-test-rule` comment header)
    - tests/jj-parallel-no-manifest-write.test.cjs (alternate reference — similar source-grep test pattern; review for the right negative-assertion style)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md (`missing:` item line 46 — verifier explicitly named this regression test)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-REVIEW.md (CR-03 section — reviewer's `\\bgit \\b` pattern suggestion)
    - $HOME/.claude/projects/-Users-LoganDark-Documents-Projects-get-shit-done/memory/MEMORY.md (project_no_raw_git entry)
  </read_first>
  <action>
    Create new test file `tests/agent-prompts-no-raw-git.test.cjs`. Use the existing `tests/quick-md-parallel-dispatch.test.cjs` as the structural template:

    - `'use strict';` + `allow-test-rule` comment header explaining this test reads agent prompt markdown files for grep-based no-raw-git enforcement (not a source-grep on SDK code).
    - File-level docstring explaining the rationale: cites `CLAUDE.md`, `project_no_raw_git` MEMORY entry, REVIEW.md CR-03, Plan 11-11.
    - `require('node:test')`, `require('node:assert')`, `fs.readFileSync` per the existing pattern.

    Define `AGENT_FILES` array as a const list:
    - `'agents/gsd-executor.md'` — the file where the violation occurred; primary scope.
    - The test should be EASY to extend with additional agent files (e.g., `'agents/gsd-verifier.md'` or `'agents/gsd-checker.md'` if they grow raw-git surface in the future). Add a comment block above the array describing how to add files.

    Define `GIT_INVOCATION_RE` — a word-boundary-anchored regex matching standalone `git <verb>` invocations. Per the threat model T-11-11-03, use a tight pattern that lists the known git verbs explicitly rather than `git\\s+\\w+` (which catches `github`, `gitignore`, etc. in narrative text):
    - Pattern: `/\\bgit\\s+(rev-parse|status|log|diff|checkout|worktree|config|add|push|pull|fetch|branch|merge|rebase|reset|stash|tag|init|clone|remote|describe|show|ls-files|cat-file|update-ref)\\b/`
    - Inline comment on the pattern noting the choice: precise verb-list catches the documented violation class without false-positive on `github`, `digit`, `gitlab`, `git commit` in narrative noun-shape, etc.
    - Note: `git commit` is INTENTIONALLY EXCLUDED from this pattern. The project's SDK commit verb still routes through jj at runtime; the agent prompt may legitimately use the noun "git commit" to describe the conceptual operation (e.g., "Run `git commit` normally" in execute-phase.md). If `git commit` ever leaks into a literal invocation here, the broader project-wide lint catches it; this file-scoped test focuses on the read-side surface where the BLOCKER actually fired.

    Define `ALLOWED_TOKENS_RE` — a regex of legitimate `git` mentions that the negative assertion should TOLERATE. Apply by stripping these patterns from the file body BEFORE the grep:
    - `git commit` (noun-shape narrative).
    - `.git/hooks` / `.git/config.lock` (path-shape narrative).
    - `git-style` / `git-equivalent` (adjective-shape narrative).
    - any line inside a markdown code-block-fence that is annotated as `# example only` or similar — keep the regex tight enough that exempting these is unnecessary in practice; if the test ever fires a false positive, the fix is to make the source line less ambiguous, not to relax the test.

    Tests:

    (1) `'agents/gsd-executor.md contains no raw git invocations'`:
        - Read the file.
        - Strip ALLOWED_TOKENS_RE (replace matches with empty string).
        - Assert `!GIT_INVOCATION_RE.test(stripped)`. Failure message names the verb that matched and cites `project_no_raw_git`.

    (2) `'agents/gsd-executor.md still uses the SDK verb for repo-root resolution (positive pin)'`:
        - `assert.match(EXEC, /primaryWorkspacePath \\/\\/ "<unresolvable>"/, 'Plan 11-11 fix must be present: REPO_ROOT sourced via jq from DISPATCH_CHECK.primaryWorkspacePath')`.
        - Pins the POSITIVE state alongside the negative — protects against a future edit that removes the raw git AND the jq read together (leaving REPO_ROOT undefined).

    (3) (Optional, recommended) `'all AGENT_FILES pass the no-raw-git check'`:
        - Iterate the AGENT_FILES list, apply the same strip + assert to each. With only `gsd-executor.md` in the list initially, this is functionally equivalent to test (1); the structure makes future extension a one-line add.

    Do NOT depend on any SDK or gsd-tools binary — pure node:test + node:assert + fs + path. Do NOT make this an async test. Do NOT add timeouts (the grep is microsecond-scale).
  </action>
  <verify>
    <automated>node --test tests/agent-prompts-no-raw-git.test.cjs 2>&1 | tee /tmp/p11-11-t3.log ; grep -E "^# (tests|pass|fail)" /tmp/p11-11-t3.log ; grep -E "^# fail [1-9]" /tmp/p11-11-t3.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - file existence: `test -f tests/agent-prompts-no-raw-git.test.cjs && echo OK` prints OK.
    - source assertion (file header): `head -3 tests/agent-prompts-no-raw-git.test.cjs | grep -F 'allow-test-rule'` returns one hit.
    - source assertion (rationale citation): `grep -cE 'project_no_raw_git|CLAUDE.md' tests/agent-prompts-no-raw-git.test.cjs` returns >= 1.
    - source assertion (regex pattern): `grep -F '\\bgit\\s+' tests/agent-prompts-no-raw-git.test.cjs` returns >= 1.
    - source assertion (scope list): `grep -F "'agents/gsd-executor.md'" tests/agent-prompts-no-raw-git.test.cjs` returns >= 1.
    - source assertion (positive pin): `grep -F 'primaryWorkspacePath' tests/agent-prompts-no-raw-git.test.cjs` returns >= 1.
    - behavior assertion: `node --test tests/agent-prompts-no-raw-git.test.cjs` exits 0 with at least 2 passing tests; zero failures.
    - load-bearing assertion: temporarily edit `agents/gsd-executor.md` to re-introduce `git rev-parse --show-toplevel` on a scratch line, re-run the test, confirm it FAILS with a message citing `project_no_raw_git`. Revert the edit. The test must be load-bearing — a no-op smoke check is unacceptable.
    - regression-safety: project-level test suite (`node --test tests/quick-md-parallel-dispatch.test.cjs tests/jj-parallel-no-manifest-write.test.cjs tests/cmd-workspace-assert-dispatched-cwd.test.ts tests/agent-prompts-no-raw-git.test.cjs`) all green.
  </acceptance_criteria>
  <done>
    `tests/agent-prompts-no-raw-git.test.cjs` exists, scopes to `agents/gsd-executor.md` initially, uses a word-boundary-anchored verb-list regex, asserts the negative (no raw git invocations) AND the positive (SDK-mediated REPO_ROOT still present). Test exits 0 in the green state and FAILS with a clear message when the BLOCKER is re-introduced. Extension to additional agent files is a one-line array push.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent prompt → SDK verb | `$DISPATCH_CHECK` JSON crosses here via stdout-of-`gsd-sdk query`; jq parses the envelope |
| primaryWorkspacePath consumer → fs probe | resolved fs path is echoed to stderr on FATAL — diagnostic only, no further action taken on the value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11-01 | Information disclosure | primaryWorkspacePath in envelope (success branch too) | accept | The field is the absolute fs path of the primary workspace, which is already discoverable via `vcs.workspace.list()` (already exposed implicitly via existing `workspaceName`/`workspacePath` in success case) and via running the orchestrator on its own checkout (the workspace root is the orchestrator's $PWD by definition). Adding the field to the success envelope does not widen the disclosure surface. |
| T-11-11-02 | Tampering | jq read of `.primaryWorkspacePath` with `// "<unresolvable>"` fallback | mitigate | The fallback ensures REPO_ROOT is always set to a printable string even if the SDK verb's envelope is malformed (e.g., a future verb regression drops the field). The fallback value `<unresolvable>` is intentionally non-path-shaped so downstream consumers can't be tricked into treating it as a real path. |
| T-11-11-03 | Tampering | regression-test regex over agent prompt files | mitigate | The verb-list-based regex is anchored on `\\bgit\\s+(rev-parse\|status\|...)\\b` rather than the looser `\\bgit \\b` the reviewer suggested. Reason: the looser pattern would catch `git commit` in narrative text (which the project allows as a noun referring to the conceptual operation — the SDK commit verb still routes through jj at runtime) and force the test author to whitelist legitimate uses. The tighter verb-list pattern catches the documented violation class (read-side raw git: rev-parse, status, log, etc.) without false-positives on legitimate noun-shape mentions. Documented inline as a design choice with a path for tightening if the violation surface ever shifts to write-side. |
| T-11-11-04 | Defense in depth | additive envelope field on SDK verb | mitigate | The new `primaryWorkspacePath` field is additive; existing consumers (workspace-parallel-dispatch.ts and any other code reading the verb's output today) ignore it via JS object-spread defaults. No breaking change. Parity test pins the field's presence + value across backends to prevent a future jj-side refactor from silently dropping it. |

Block-on `high` — none of the above are `high`. ASVS L1 gate passes. The defense-in-depth improvement (T-11-11-04) is the entire point of the plan.
</threat_model>

<verification>
- BLOCKER closed at the agent-prompt layer:
  - `grep -F 'git rev-parse' agents/gsd-executor.md` returns 0.
  - `grep -F 'primaryWorkspacePath // "<unresolvable>"' agents/gsd-executor.md` returns 1.
- New SDK envelope field present:
  - `grep -c 'primaryWorkspacePath' sdk/src/query/workspace-assert-dispatched-cwd.ts` returns >= 3.
- Parity test extension green:
  - `cd /Users/LoganDark/Documents/Projects/get-shit-done && pnpm --filter ./sdk exec tsx --test tests/cmd-workspace-assert-dispatched-cwd.test.ts` exits 0.
- Class-wide regression test green AND load-bearing:
  - `node --test tests/agent-prompts-no-raw-git.test.cjs` exits 0 with >= 2 passing tests.
  - Temporary re-introduction of `git rev-parse --show-toplevel` in agents/gsd-executor.md causes the test to FAIL with a message citing `project_no_raw_git` (verified by executor as part of Task 3 acceptance).
- Plan 11-07 SC-1b structural collapse preserved:
  - DISPATCH_CHECK still a single `gsd-sdk query workspace.assert-dispatched-cwd --cwd .` call at agents/gsd-executor.md:419.
  - The 71-line guard cluster from pre-Phase-11 is NOT re-introduced.
- Project-level cross-test sanity: `node --test tests/quick-md-parallel-dispatch.test.cjs tests/jj-parallel-no-manifest-write.test.cjs tests/agent-prompts-no-raw-git.test.cjs` all green; SDK test suite for the modified verb green.
</verification>

<success_criteria>
- Requirement PROMPT-08 flips BLOCKED → SATISFIED per re-verification. The "subagent prompts never inspect backend kind — no raw `git`, no `vcs.kind` branching" truth in 11-VERIFICATION.md flips failed → verified.
- `agents/gsd-executor.md` is raw-git-free at the read-side surface. Project rule `project_no_raw_git` is now enforced at file level by `tests/agent-prompts-no-raw-git.test.cjs`, extending the existing repo-wide deny intent to AGENT PROMPT FILES specifically.
- Verifier-named fix option (c) is implemented cleanly: one new field on the existing SDK verb, one jq read in the agent prompt, one parity-test extension, one new class-wide regression test. No new verbs, no new VCS adapter methods, no breaking-change envelope shape.
- `.planning/REQUIREMENTS.md` line 122 (PROMPT-08 "Complete") is now consistent with verifier truth.
- No collateral changes — workspace-parallel-dispatch.ts untouched, workspace-parallel-fan-in.ts untouched, dispatch-cwd-safety.md untouched (its WR-01 doc-overstatement is out-of-scope per `must_haves` deferred list).
</success_criteria>

<output>
Create `.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-11-SUMMARY.md` documenting:
- The verbose verifier-option trade-off: why option (c) over (a) [no such CLI verb exists] and (b) [overkill: new verb for a single diagnostic].
- The envelope-shape diff (before/after JSON), one-line per backend (git/jj) showing the new field flows correctly.
- The agent-prompt diff (one-line replacement), with pre/post grep proof.
- The class-wide regression test's load-bearing verification (test FAILS when raw git re-introduced; reverts cleanly).
- Explicit note that PROMPT-08 is now functional and REQUIREMENTS.md:122 is consistent with verifier truth.
- The deferred-to-future list (WR-01, WR-02, WR-03, WR-04, WR-05, IN-02, IN-03) with a one-line justification per item that none are goal-blocking for PROMPT-08.
- A pointer to Plan 11-10 SUMMARY (which closes the sibling BLOCKER cluster A in the same wave) so the next verifier pass reads both run-2 closures together.
</output>
