---
phase: 11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd
plan: 10
type: execute
wave: 5
depends_on: [11-05, 11-08]
gap_closure: true
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - tests/quick-md-parallel-dispatch.test.cjs
autonomous: true
requirements: [PROMPT-06, PARALLEL-06]
tags: [gap-closure, blocker, execute-phase, dispatch, regression-test, run-2, revision-1]

must_haves:
  truths:
    - "VERIFICATION.md SC-2a / REVIEW.md CR-01 BLOCKER closed: `get-shit-done/workflows/execute-phase.md` no longer references undefined `$WAVE_WORKTREE_PLANS_JSON` at the dispatch site. The variable is now CONSTRUCTED from the existing `WAVE_WORKTREE_PLANS` plan-id accumulator (populated by `execute-phase/steps/per-plan-worktree-gate.md:94`) via a `jq -R . | jq -sc 'map({agentId: ., planId: .})'` pipeline. Shape matches the workspace-parallel-dispatch.ts:73-85 contract `{agentId, planId, workspacePath?}`."
    - "VERIFICATION.md SC-2a / REVIEW.md CR-02 BLOCKER closed: `--phase \"{phase_number}\"` (workflow placeholder syntax that does NOT expand inside bash code blocks) is replaced with `--phase \"${PHASE_NUMBER}\"` — the established bash-variable convention already used at execute-phase.md:312, :806, :1059, :1098. The dispatch verb's `Number(args[++i])` parse now succeeds and the verb no longer returns `{ok:false, reason:'phase_number_required'}` on every invocation."
    - "VERIFICATION.md SC-2a regression-net gap closed: `tests/quick-md-parallel-dispatch.test.cjs`'s `EXEC` carry-over describe-block extends from one HANDLE_OK-symmetry assertion (lines 48-53) to ALSO pin the plan-shape (flat array, no `{plans:[...]}` wrapper; contains `agentId.*planId` field tokens) and the numeric-phase shape (no literal `--phase \"{phase_number}\"` placeholder; presence of `--phase \"\\$\\{PHASE_NUMBER\\}\"` bash-variable form). These are precisely the carry-overs that would have caught both BLOCKERs at commit time per VERIFICATION.md `missing` items and REVIEW.md IN-01."
    - "Requirement PROMPT-06 flips BLOCKED → SATISFIED. The structural collapse from Plan 11-05 plus the run-1 guards from Plan 11-08 plus this run-2 dispatch-line fix together make the execute-phase.md parallel-dispatch path end-to-end functional (no `{ok:false}` on every invocation), pinned by regression tests so future edits cannot silently regress."
    - "REQUIREMENTS.md drift fix: `.planning/REQUIREMENTS.md` line 120 (PROMPT-06 marked 'Complete' while structurally blocked per verifier) is left as 'Complete' AFTER this plan lands because the gap closes here — no revert needed. The plan SUMMARY must include a short note confirming the table is now consistent with verifier truth."
    - "Out-of-scope (do NOT touch): the dispatch-line bash already includes the EXPECTED_BRANCH empty/HEAD pre-check (Plan 11-08 CR-03 closure, lines 531-535) and the HANDLE_OK FATAL guard (Plan 11-08 CR-02 carry-over, lines 540-541). These remain unchanged. Quick.md side is correct per Plan 11-08 and is NOT touched."
    - "Out-of-scope (deferred to a future plan or phase): REVIEW.md WR-01 (dispatch-cwd-safety.md protected-ref doc overstatement), WR-02 (N+1 jj subprocess in workspace-assert-dispatched-cwd.ts), WR-04 (cleanFanIn predicate duplicated literal in worktree-safety.cjs), WR-05 (tests/wave-cleanup-executor.test.cjs missing combined failure mode), IN-02 (dispatch-cwd-safety.md recovery section), IN-03 (manifest-retirement comment duplication). None are goal-blocking for PROMPT-06."
    - "Revision-1 (checker iter 1): WARNING 2 — Task 2's load-bearing-test acceptance criterion no longer offers `git stash` as a revert mechanism. Offering raw `git stash` directly contradicts `project_no_raw_git` (the rule plan 11-11 is enforcing). The revised criterion specifies `jj abandon` exclusively (or `jj restore <path>` for file-precision revert)."
    - "Revision-1 (checker iter 1): WARNING 4 — Task 1's `printf '%s\\n' $WAVE_WORKTREE_PLANS | jq -R .` construction relies on INTENTIONAL unquoted-variable word-splitting. Plan IDs are filename-derived (the per-plan-worktree-gate.md:94 accumulator pushes them via `PLAN_ID=$(basename \"$plan_path\" -PLAN.md)` shape — see steps/per-plan-worktree-gate.md for exact provenance) so the attack surface is empty by construction. The action body now documents this with an inline comment and the threat model (T-11-10-04) pins the filename-derived invariant explicitly."
  artifacts:
    - path: get-shit-done/workflows/execute-phase.md
      provides: "Functional parallel-dispatch invocation: constructs WAVE_WORKTREE_PLANS_JSON from the plan-id accumulator AND passes --phase \"${PHASE_NUMBER}\" (bash variable, not the unexpanded {phase_number} placeholder)"
      contains: "WAVE_WORKTREE_PLANS_JSON=$(printf"
    - path: get-shit-done/workflows/execute-phase.md
      provides: "Same file, second invariant: numeric phase via bash variable"
      contains: "--phase \"${PHASE_NUMBER}\""
    - path: tests/quick-md-parallel-dispatch.test.cjs
      provides: "Regression net symmetric across QUICK and EXEC for all three CR-02 sub-invariants (plan-shape, numeric --phase, HANDLE_OK guard)"
      contains: "WAVE_WORKTREE_PLANS_JSON"
  key_links:
    - from: get-shit-done/workflows/execute-phase.md
      to: sdk/src/query/workspace-parallel-dispatch.ts
      via: "gsd-sdk query workspace.parallel.dispatch --phase \"${PHASE_NUMBER}\" --main-bookmark \"$EXPECTED_BRANCH\" --plan @-"
      pattern: "workspace.parallel.dispatch.*--phase \"\\$\\{PHASE_NUMBER\\}\""
    - from: tests/quick-md-parallel-dispatch.test.cjs
      to: get-shit-done/workflows/execute-phase.md
      via: "fs.readFileSync of EXEC + assert.match/assert.doesNotMatch on plan-shape and --phase shape"
      pattern: "WAVE_WORKTREE_PLANS_JSON|--phase \"\\\\\\$\\\\\\{PHASE_NUMBER"
---

<objective>
Close run-1 BLOCKER cluster A: the parallel-dispatch invocation in `get-shit-done/workflows/execute-phase.md` (the `bash` fenced block around lines 527-541) is structurally non-functional on two independent dimensions — (1) `$WAVE_WORKTREE_PLANS_JSON` is referenced (line 536) but never constructed anywhere in the workflow tree, and (2) `--phase "{phase_number}"` (line 538) is the workflow's orchestrator-substitution placeholder which is NOT expanded inside a bash code block, so the verb sees the 14-character literal string. Either alone is fatal; both fire simultaneously on every wave dispatch. The Plan 11-08 HANDLE_OK FATAL guard catches the resulting `{ok:false}` so users see a clean error message — but the parallel-execution path is unconditionally broken.

Purpose: flip requirement PROMPT-06 from BLOCKED → SATISFIED by landing the two one-line bash fixes (jq pipeline + bash-variable form) AND extending `tests/quick-md-parallel-dispatch.test.cjs`'s existing `EXEC` carry-over describe-block with the plan-shape + numeric-phase assertions that would have caught both BLOCKERs at commit time. Pinning the carry-overs in the same plan that fixes the defect is the whole point of run-2: closing run-1's drift without leaving the regression net narrower than the defect class.

Output: a functional `workspace.parallel.dispatch` invocation in execute-phase.md (mirrors the proven-correct shape in quick.md:675-683) and a symmetric regression test that pins both the QUICK and EXEC sides against future re-introduction.

Revision-1 scope: this plan was revised once after a plan-checker iter 1 pass. Two warnings landed: WARNING 2 (Task 2 acceptance offered `git stash` — direct project_no_raw_git contradiction) and WARNING 4 (Task 1's word-splitting on `$WAVE_WORKTREE_PLANS` was load-bearing but undocumented). Both fixes are surgical edits inside this plan; the dispatch-line fixes themselves are unchanged from run-2.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-CONTEXT.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-REVIEW.md
@.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-08-SUMMARY.md
@get-shit-done/workflows/execute-phase.md
@get-shit-done/workflows/quick.md
@get-shit-done/workflows/execute-phase/steps/per-plan-worktree-gate.md
@sdk/src/query/workspace-parallel-dispatch.ts
@tests/quick-md-parallel-dispatch.test.cjs
@./CLAUDE.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From sdk/src/query/workspace-parallel-dispatch.ts — verb contract:
- `--phase <number>` is parsed via `Number(args[++i])`; rejects NaN with `{ok:false, reason:'phase_number_required'}` (lines 58-65).
- `--plan @-` reads stdin; parses as JSON; rejects non-array with `{ok:false, reason:'plan_json_parse_failed'}` (lines 73-85).
- Each plan entry shape: `{agentId: string, planId: string, workspacePath?: string}` (line 73).

From get-shit-done/workflows/quick.md:675-683 — REFERENCE SHAPE (correct, do not touch):
- `QUICK_PLAN_JSON=$(jq -nc --arg aid "${quick_id}" --arg pid "${quick_id}" '[{agentId:$aid,planId:$pid}]')`
- `--phase 0 --main-bookmark "$EXPECTED_BRANCH" --plan @-`
- HANDLE_OK guard: `HANDLE_OK=$(echo "$HANDLE_JSON" | jq -r '.ok // "true"'); [ "$HANDLE_OK" = "false" ] && { echo "FATAL:..." >&2; exit 1; }`

From get-shit-done/workflows/execute-phase/steps/per-plan-worktree-gate.md:94 — accumulator source:
- `WAVE_WORKTREE_PLANS` is space-separated plan IDs accumulated as the per-plan gate decides USE_WORKTREES_FOR_PLAN. Already non-empty-checked at execute-phase.md:768 and :817. Plan IDs are filename-derived (e.g. `11-10`, `11-11`) — the attack surface for unquoted word-splitting on this variable is empty by construction.

From get-shit-done/workflows/execute-phase.md:312 — established bash variable convention:
- `${PHASE_NUMBER}` is the canonical bash-variable form already used at :312, :806, :1059, :1098. NOT `{phase_number}` (workflow-substitution placeholder, only fires in agent prompt literals).

From tests/quick-md-parallel-dispatch.test.cjs:48-53 — existing EXEC carry-over (incomplete):
- Already pins the HANDLE_OK FATAL guard symmetry on EXEC.
- MISSING: plan-shape (no `{plans:[...]}` wrapper, contains `agentId.*planId`) and numeric-phase shape (no literal `{phase_number}`, presence of bash variable form).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build WAVE_WORKTREE_PLANS_JSON and switch --phase to bash variable in execute-phase.md dispatch block</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <read_first>
    - get-shit-done/workflows/execute-phase.md (the file being modified — focus on lines 521-545 dispatch block and lines 308-315 for the ${PHASE_NUMBER} convention)
    - get-shit-done/workflows/quick.md (the proven-correct sibling shape at lines 660-686 — do NOT modify, read-only reference)
    - get-shit-done/workflows/execute-phase/steps/per-plan-worktree-gate.md (line 94 shows how WAVE_WORKTREE_PLANS is populated as a plan-id accumulator — confirm plan IDs are filename-derived for the word-split safety argument below)
    - sdk/src/query/workspace-parallel-dispatch.ts (lines 47, 58-65, 73-85 — verb's parse contract)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md (`gaps:` block for SC-2a — both BLOCKERs documented with exact line citations)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-REVIEW.md (CR-01 and CR-02 sections)
  </read_first>
  <action>
    Edit `get-shit-done/workflows/execute-phase.md` inside the existing `bash` fenced block at lines 527-542. TWO independent edits, neither touches the surrounding EXPECTED_BRANCH pre-check (lines 531-535) or the HANDLE_OK guard (lines 540-541).

    Edit 1 — insert a `WAVE_WORKTREE_PLANS_JSON` construction BEFORE the `HANDLE_JSON=$(...)` line (currently line 536). The pipeline reads from the existing `WAVE_WORKTREE_PLANS` plan-id accumulator and emits a flat array of `{agentId, planId}` objects matching workspace-parallel-dispatch.ts:73 contract. Use the form:

    ```
    # WAVE_WORKTREE_PLANS_JSON: build the dispatch plan-array from the per-plan-worktree-gate.md:94
    # accumulator. Plan IDs are filename-derived (matches glob 11-NN) — `$WAVE_WORKTREE_PLANS` is
    # intentionally unquoted here so word-splitting feeds each plan-id as a separate jq -R . input.
    # Adding quotes would put the entire space-joined list into a single jq line. The attack surface
    # for shell-meta injection is empty by construction; see Plan 11-10 D-04 / T-11-10-04.
    WAVE_WORKTREE_PLANS_JSON=$(printf '%s\n' $WAVE_WORKTREE_PLANS | jq -R . | jq -sc 'map({agentId: ., planId: .})')
    ```

    The double-jq pipeline (`jq -R .` for raw-input quoting, then `jq -sc 'map(...)'` for slurp+compact) is the secure form per T-11-10-01 — it never naively concatenates plan IDs into a JSON literal. Match agentId === planId (the per-plan agent identity is the plan ID in execute-phase mode, mirroring quick mode's `quick_id` pattern). Place this block immediately before the `HANDLE_JSON=$(printf '%s' "$WAVE_WORKTREE_PLANS_JSON" \` line.

    Edit 2 — change `--phase "{phase_number}"` on what is currently line 538 to `--phase "${PHASE_NUMBER}"`. The `${PHASE_NUMBER}` bash variable is already established at :312 (`state.begin-phase --phase "${PHASE_NUMBER}"`), :806, :1059, :1098. Do NOT introduce a new variable.

    The post-edit block must read approximately:

    EXPECTED_BASE=...
    DISPATCH_TS=...
    EXPECTED_BRANCH=...
    if [ -z "$EXPECTED_BRANCH" ] || [ "$EXPECTED_BRANCH" = "HEAD" ]; then (EXISTING FATAL — DO NOT TOUCH)
    # NEW: construct plan-array JSON from the WAVE_WORKTREE_PLANS plan-id accumulator (Plan 11-10 / PROMPT-06 closure)
    # ...intentional-word-split comment from Edit 1...
    WAVE_WORKTREE_PLANS_JSON=$(printf '%s\n' $WAVE_WORKTREE_PLANS | jq -R . | jq -sc 'map({agentId: ., planId: .})')
    HANDLE_JSON=$(printf '%s' "$WAVE_WORKTREE_PLANS_JSON" | gsd-sdk query workspace.parallel.dispatch --phase "${PHASE_NUMBER}" --main-bookmark "$EXPECTED_BRANCH" --plan @-)
    [ -z "$HANDLE_JSON" ] && { ... } (EXISTING — DO NOT TOUCH)
    HANDLE_OK=... (EXISTING — DO NOT TOUCH)

    Do NOT change any other line in the file. Do NOT touch quick.md. Do NOT modify the per-plan-worktree-gate.md.
  </action>
  <verify>
    <automated>node --test tests/quick-md-parallel-dispatch.test.cjs 2>&1 | tee /tmp/p11-10-t1.log | grep -E '^(ok|not ok|# (tests|pass|fail))' ; grep -E "^# fail [1-9]" /tmp/p11-10-t1.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -nE 'WAVE_WORKTREE_PLANS_JSON=\\$\\(printf' get-shit-done/workflows/execute-phase.md` returns at least one match (the new construction line).
    - source assertion: `grep -c 'WAVE_WORKTREE_PLANS_JSON' get-shit-done/workflows/execute-phase.md` returns 2 (the construction site + the `printf '%s' "$WAVE_WORKTREE_PLANS_JSON"` use site).
    - source assertion: `grep -c 'jq -R \\. | jq -sc' get-shit-done/workflows/execute-phase.md` returns 1.
    - source assertion: `grep -F '--phase "${PHASE_NUMBER}"' get-shit-done/workflows/execute-phase.md` returns one hit.
    - source assertion (negative): `grep -F -- '--phase "{phase_number}"' get-shit-done/workflows/execute-phase.md` returns ZERO hits (the literal placeholder is gone from the bash block; uses of `{phase_number}` in agent prompt literals at :565, :573, :695 are preserved).
    - source assertion (word-split rationale comment present): `grep -F 'intentionally unquoted' get-shit-done/workflows/execute-phase.md` OR `grep -F 'word-split' get-shit-done/workflows/execute-phase.md` returns at least one hit on a line adjacent to the new construction. The comment makes WARNING 4's load-bearing invariant visible to future readers.
    - source assertion (regression-safety): EXPECTED_BRANCH pre-check at the start of the block is unchanged — `grep -nE '\\[ -z "\\$EXPECTED_BRANCH" \\] \\|\\| \\[ "\\$EXPECTED_BRANCH" = "HEAD" \\]' get-shit-done/workflows/execute-phase.md` still returns the existing hit.
    - source assertion (regression-safety): HANDLE_OK guard preserved — `grep -F 'HANDLE_OK=$(echo "$HANDLE_JSON" | jq -r' get-shit-done/workflows/execute-phase.md` still returns one hit.
    - quick.md untouched — `jj diff --stat get-shit-done/workflows/quick.md` shows zero modifications (project uses jj per CLAUDE.md / MEMORY entry `user_vcs_jj`).
    - behavior assertion: `node --test tests/quick-md-parallel-dispatch.test.cjs` exits 0 (existing 7 tests still pass; Task 2 will add 3-4 more).
  </acceptance_criteria>
  <done>
    execute-phase.md dispatch block constructs WAVE_WORKTREE_PLANS_JSON via the jq pipeline (with an inline comment explaining the intentional word-split on `$WAVE_WORKTREE_PLANS`) before invoking the verb, and passes `--phase "${PHASE_NUMBER}"` (bash variable, not the literal placeholder). The two BLOCKER root causes are mechanically closed. All existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/quick-md-parallel-dispatch.test.cjs EXEC carry-over to pin plan-shape and numeric-phase invariants</name>
  <files>tests/quick-md-parallel-dispatch.test.cjs</files>
  <read_first>
    - tests/quick-md-parallel-dispatch.test.cjs (the file being modified — entire file, focus on lines 31-53 for QUICK plan-shape/numeric-phase patterns and 48-53 for the EXEC carry-over describe-block)
    - get-shit-done/workflows/execute-phase.md (post Task 1 — the file the new assertions will grep against)
    - get-shit-done/workflows/quick.md (the reference shape the EXEC carry-overs mirror)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-VERIFICATION.md (`gaps:` block `missing:` items lines 33-36 — verifier-supplied test assertions to land)
    - .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-REVIEW.md (IN-01 section — exact carry-over the reviewer prescribed)
  </read_first>
  <action>
    Extend the existing `test.describe('CR-02 execute-phase.md carry-over', ...)` block at tests/quick-md-parallel-dispatch.test.cjs:48-53. Do NOT remove or rewrite the existing HANDLE_OK symmetry test at :49-52 — only ADD sibling `test.test(...)` blocks alongside it.

    Add three new `test.test(...)` cases inside the existing describe block:

    (1) Plan-shape carry-over — pin EXEC contains a WAVE_WORKTREE_PLANS_JSON construction with the jq-array shape and uses the {agentId, planId} field names:
        - `assert.match(EXEC, /WAVE_WORKTREE_PLANS_JSON=\\$\\(/, 'CR-02 carry-over: execute-phase.md must CONSTRUCT WAVE_WORKTREE_PLANS_JSON, not just reference it (Plan 11-10 / PROMPT-06)')`
        - `assert.match(EXEC, /jq -sc '\\s*map\\(\\{agentId:.*planId:.*\\}\\)/, 'CR-02 carry-over: execute-phase.md plan array must contain {agentId,planId} fields matching workspace-parallel-dispatch.ts:73 contract')`
        - `assert.doesNotMatch(EXEC, /\\{plans:\\[/, 'CR-02 carry-over: execute-phase.md must NOT wrap plan as {plans:[...]} (dispatch verb expects flat array)')`

    (2) Numeric --phase carry-over — pin EXEC uses bash variable form and not the literal placeholder:
        - `assert.match(EXEC, /--phase "\\$\\{PHASE_NUMBER\\}"/, 'CR-02 carry-over: execute-phase.md must pass --phase via bash variable \${PHASE_NUMBER} (numeric at runtime)')`
        - `assert.doesNotMatch(EXEC, /--phase "\\{phase_number\\}"/, 'CR-02 carry-over: literal --phase "{phase_number}" is the workflow placeholder, NOT expanded in bash blocks (verb returns phase_number_required)')`

    (3) Construction-uses-source-accumulator pin — pin the construction reads from `WAVE_WORKTREE_PLANS` (the per-plan gate's accumulator), so a future refactor that drops the source variable cannot silently leave the construction reading from an undefined accumulator:
        - `assert.match(EXEC, /WAVE_WORKTREE_PLANS_JSON=\\$\\(printf '%s\\\\n' \\$WAVE_WORKTREE_PLANS \\|/, 'CR-02 carry-over: WAVE_WORKTREE_PLANS_JSON must be built from the WAVE_WORKTREE_PLANS plan-id accumulator (per-plan-worktree-gate.md:94)')`

    File-level docstring update — extend the top comment block (lines 4-20) with a brief note describing the run-2 carry-overs and citing Plan 11-10. Keep the existing CR-02/CR-03 rationale intact; add a "Run-2 (Plan 11-10): ..." paragraph after the existing CR-02 paragraph.

    Do NOT touch the existing CR-03 describe-block (lines 55-73) or the CR-02 quick.md describe-block (lines 31-46). Do NOT add an unrelated EXEC test outside the existing carry-over describe.
  </action>
  <verify>
    <automated>node --test tests/quick-md-parallel-dispatch.test.cjs 2>&1 | tee /tmp/p11-10-t2.log | grep -E '^# (tests|pass|fail)' ; PASS_COUNT=$(grep -c '^ok ' /tmp/p11-10-t2.log) ; [ "$PASS_COUNT" -ge 10 ] || { echo "FAIL: expected >=10 ok lines (7 existing + >=3 new), got $PASS_COUNT" ; exit 1 ; } ; grep -E "^# fail [1-9]" /tmp/p11-10-t2.log && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -cE "test\\.test\\(" tests/quick-md-parallel-dispatch.test.cjs` returns >= 10 (was 7; +3 new minimum).
    - source assertion: `grep -cF 'WAVE_WORKTREE_PLANS_JSON=\\$\\(' tests/quick-md-parallel-dispatch.test.cjs` returns >= 1 (new construction-pin assertion).
    - source assertion: `grep -cF -- '--phase "\\$\\{PHASE_NUMBER\\}"' tests/quick-md-parallel-dispatch.test.cjs` returns >= 1.
    - source assertion: `grep -cF -- '--phase "\\{phase_number\\}"' tests/quick-md-parallel-dispatch.test.cjs` returns >= 1 (the negative-assertion pattern).
    - source assertion: existing CR-02 quick.md describe block at line ~31-46 is BYTE-IDENTICAL pre/post — `grep -A 16 "CR-02 quick.md dispatch shape" tests/quick-md-parallel-dispatch.test.cjs` output unchanged from pre-edit (visually verifiable; the diff hunk should add lines inside the EXEC carry-over describe ONLY).
    - source assertion: existing CR-03 describe block at line ~55-73 is BYTE-IDENTICAL pre/post.
    - behavior assertion: `node --test tests/quick-md-parallel-dispatch.test.cjs` exits 0 with >= 10 passing tests (7 existing + >= 3 new). NO failing assertions.
    - behavior assertion (load-bearing check): revert Task 1's edit temporarily (`jj abandon` the working-copy change to execute-phase.md ONLY — keep test edits) OR use `jj restore get-shit-done/workflows/execute-phase.md` for file-precision revert. Re-run the test, and confirm at least 2 of the 3 new tests FAIL. This proves the new assertions are load-bearing and not vacuously green. After confirming, restore Task 1's edit via `jj squash`/`jj edit` of the prior change or by re-applying the diff. **Do NOT use `git stash` — that directly contradicts the no-raw-git rule plan 11-11 is enforcing in the sibling closure.**
    - regression-safety: original 7 tests still pass — `grep "^ok [0-9]" /tmp/p11-10-t2.log | wc -l` returns >= 10 and `grep -c "^not ok" /tmp/p11-10-t2.log` returns 0.
  </acceptance_criteria>
  <done>
    `tests/quick-md-parallel-dispatch.test.cjs` EXEC carry-over describe-block has the three plan-shape + numeric-phase + accumulator-source pins. The test file exits 0 with all (>= 10) tests green AND fails the appropriate subset (>= 2 of the 3 new assertions) when Task 1's edit is temporarily reverted via `jj abandon`/`jj restore` (not `git stash`). Future edits to execute-phase.md cannot silently re-introduce either BLOCKER without breaking CI.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator shell → SDK CLI verb | bash-substituted JSON crosses here via stdin (printf '%s' | gsd-sdk query ... --plan @-) |
| WAVE_WORKTREE_PLANS accumulator → JSON literal | plan IDs flow from the per-plan-worktree-gate.md accumulator into the dispatch verb's plan input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-01 | Tampering | jq pipeline that builds WAVE_WORKTREE_PLANS_JSON from plan IDs | mitigate | Use `printf '%s\n' $WAVE_WORKTREE_PLANS | jq -R . | jq -sc 'map(...)'` — the `jq -R .` raw-input mode treats each line as a literal string and quotes it correctly for JSON. Naive `"[\"$plan_id\",...]"` concatenation would allow shell-meta or JSON-meta injection if a plan ID ever contained `"` or `\`. Plan IDs are produced by `gsd-sdk query phase-plan-index` from filesystem paths (filenames like `11-10`), so the injection surface is already low, but the jq form is the defensive default. |
| T-11-10-02 | Information disclosure | dispatch verb error payload bubbling through HANDLE_OK guard FATAL | accept | The FATAL message at execute-phase.md:541 echoes `$HANDLE_JSON` to stderr, which on failure contains `{ok:false, reason:'plan_json_parse_failed', error:'...'}`. The error text is from JSON.parse and may include a snippet of the malformed input — accepted because the input is an orchestrator-built JSON literal from local plan IDs, not user-supplied data. |
| T-11-10-03 | Tampering | regression-test grep pattern false-positives | mitigate | The new `--phase "{phase_number}"` negative-assertion uses a literal-bracket-escaped pattern in a `assert.doesNotMatch` over the EXEC string — the markdown content does contain `{phase_number}` in agent prompt literals at execute-phase.md:565, 573, 695. The test pattern must anchor on `--phase "{phase_number}"` (with the `--phase ` prefix) so the negative assertion is precise and doesn't false-fire on legitimate placeholder uses elsewhere in the file. |
| T-11-10-04 | Tampering (revision-1 WARNING 4) | Intentional word-splitting on `$WAVE_WORKTREE_PLANS` upstream of the jq pipeline | mitigate | The construction `printf '%s\n' $WAVE_WORKTREE_PLANS | jq -R . | ...` deliberately does NOT quote `$WAVE_WORKTREE_PLANS`. The intent is to feed each space-separated plan-id as a separate jq input line. Quoting the variable would collapse the entire list onto one line and `jq -R . | jq -sc 'map(...)'` would emit a 1-element array. This is load-bearing AND safe by construction: plan IDs are filename-derived (the per-plan-worktree-gate.md:94 accumulator pushes IDs that match the glob `[0-9][0-9](\\.[0-9]+)?-[0-9][0-9]`), so the shell-meta-injection surface is empty. T-11-10-01's `jq -R .` mitigation already handles the JSON-side quoting; T-11-10-04 documents the SHELL-side invariant. Mitigation: the inline comment above the construction in execute-phase.md (per Edit 1 of Task 1) explicitly notes "intentionally unquoted" and cites this threat ID. Future contributors who see the bare `$WAVE_WORKTREE_PLANS` get the rationale without needing to read the threat model. |

Block-on `high` — none of the above are `high`. ASVS L1 gate passes.
</threat_model>

<verification>
- Both BLOCKER root causes mechanically closed in execute-phase.md:
  - `grep -F 'WAVE_WORKTREE_PLANS_JSON=$(printf' get-shit-done/workflows/execute-phase.md` returns 1.
  - `grep -F '--phase "${PHASE_NUMBER}"' get-shit-done/workflows/execute-phase.md` returns 1.
  - `grep -F -- '--phase "{phase_number}"' get-shit-done/workflows/execute-phase.md` returns 0.
- Word-split rationale comment landed: `grep -F 'intentionally unquoted' get-shit-done/workflows/execute-phase.md || grep -F 'word-split' get-shit-done/workflows/execute-phase.md` returns at least one hit (WARNING 4 closure).
- Regression-net symmetric across QUICK and EXEC:
  - `node --test tests/quick-md-parallel-dispatch.test.cjs` exits 0; >= 10 tests pass; >= 3 new EXEC-carry-over assertions exist alongside the existing HANDLE_OK pin.
- quick.md unchanged: `jj diff --stat get-shit-done/workflows/quick.md` shows zero deltas.
- Whole-file regression check (project-wide test suite): `cd /Users/LoganDark/Documents/Projects/get-shit-done && node --test tests/quick-md-parallel-dispatch.test.cjs tests/cmd-workspace-assert-dispatched-cwd.test.ts tests/wave-cleanup-executor.test.cjs tests/jj-parallel-no-manifest-write.test.cjs` exits 0 (sibling Phase 11 regression tests unaffected).
- WARNING 2 closure: Task 2's load-bearing-test acceptance criterion explicitly forbids `git stash` and prescribes `jj abandon`/`jj restore` exclusively. `grep -F 'git stash' .planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-10-PLAN.md` returns ONLY in the negative-prohibition context (i.e., "Do NOT use git stash"), never as a positive suggestion.
</verification>

<success_criteria>
- Requirement PROMPT-06 flips BLOCKED → SATISFIED per re-verification. SC-2a in 11-VERIFICATION.md flips FAILED → VERIFIED on next verifier pass.
- Both BLOCKER root causes (undefined `$WAVE_WORKTREE_PLANS_JSON`, literal `{phase_number}` placeholder) are mechanically closed.
- The regression test extensions catch future re-introductions of either BLOCKER at commit time. The test net is now symmetric across the QUICK and EXEC sides for the three CR-02 sub-invariants (plan-shape, numeric --phase, HANDLE_OK guard).
- `.planning/REQUIREMENTS.md` line 120 (PROMPT-06 "Complete") is now consistent with verifier truth; the SUMMARY documents this consistency.
- No collateral changes — quick.md untouched, per-plan-worktree-gate.md untouched, SDK verbs untouched.
- Revision-1 cleanups: WARNING 2 (`git stash` removed from acceptance criterion) + WARNING 4 (word-split rationale documented in action body + T-11-10-04 in threat model) closed.
</success_criteria>

<output>
Create `.planning/phases/11-orchestrator-agent-rewire-workspace-assert-dispatched-cwd/11-10-SUMMARY.md` documenting:
- The two one-line bash fixes (jq pipeline + bash-variable form), with pre/post grep proof.
- The three new EXEC carry-over assertions, with the test-revert-and-fail verification step output (proving the new tests are load-bearing) — note the revert was done via `jj abandon`/`jj restore`, NOT `git stash`.
- The word-split rationale comment landed inline in execute-phase.md (WARNING 4 closure).
- Explicit note that PROMPT-06 is now structurally functional and REQUIREMENTS.md:120 is consistent with verifier truth.
- The deferred-to-future list (WR-01, WR-02, WR-04, WR-05, IN-02, IN-03) with a one-line justification per item that none are goal-blocking for PROMPT-06.
</output>
</content>
</invoke>