---
phase: 13-ci-parallel-path-lane-lint-close-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md
autonomous: true
requirements:
  - LINT-04
  - CI-06
tags:
  - cascade-amendments
  - documentation

must_haves:
  truths:
    - "ROADMAP.md Phase 13 SC2 describes the audit as a baseline-regression guard (audit reports no raw-git ADDED beyond the recorded 127-hit baseline; on first green run current == baseline == pass), not a zero-hits assertion"
    - "ROADMAP.md Phase 13 SC3 uses 'baseline / no-regression invariant' wording, not 'zero-hits invariant'"
    - "CONTEXT.md D-08 and the CONTEXT.md <domain> Phase Boundary text describe the audit as a regression guard; the close-commit evidence is a quoted 'baseline 127, 0 new' summary, not a 'zero-hits' summary"
    - "No stale 'zero hits' / 'zero-hits' / 'reports zero' / 'proving zero' claim about the audit survives anywhere in ROADMAP.md, CONTEXT.md, or REQUIREMENTS.md"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 SC2 + SC3 re-baselined to regression-guard wording"
      contains: "127"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LINT-04 + CI-06 wording reconciled with the regression-guard framing (if zero-hits wording appears)"
      contains: "baseline"
    - path: ".planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md"
      provides: "D-08 + <domain> Phase Boundary re-worded to regression-guard framing"
      contains: "baseline"
  key_links:
    - from: ".planning/ROADMAP.md Phase 13 §Success Criteria"
      to: "plans 13-02 (audit) and 13-04 (CI lane)"
      via: "executors read SC2/SC3 for verification framing"
      pattern: "baseline"
    - from: ".planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md D-08"
      to: "v1.3 milestone close commit message"
      via: "close-commit evidence wording is sourced from D-08"
      pattern: "baseline 127"
---

<objective>
Cascade-amendment doc edits resolving RESEARCH.md Open Q1. ROADMAP SC2 and
CONTEXT.md D-08 currently state the audit "on first green run reports zero
raw-git hits" in `.md` shell-fence blocks. That is FALSE against the live tree:
a fence-aware scan (re-confirmed 2026-05-22) found 127 raw-git invocations
across 30 files. The user resolved Open Q1 by RE-BASELINING SC2 as a
regression guard, not by narrowing the audit scope and not by a 30-file
cleanup. This plan amends every now-incorrect "zero hits / zero-hits" claim to
the regression-guard framing consistently across ROADMAP.md, CONTEXT.md, and
REQUIREMENTS.md.

This runs in Wave 1 so the downstream audit-script plan (13-02) and CI-lane
plan (13-04) read corrected SC2/SC3 wording when their executors verify
against the success criteria. Precedent: Phase 10 plan 10-01 and Phase 12 plan
12-01 — same cascade-amendment-first structure.

Purpose: ROADMAP.md and REQUIREMENTS.md are the executor's source of truth for
"what must be TRUE" at phase completion; CONTEXT.md D-08 is the source of the
v1.3 milestone close-commit evidence wording. All three currently encode a
zero-hits assertion that cannot pass. Re-baseline them so the audit script is
built as a regression guard (audit fails only when NEW raw-git is added beyond
the recorded 127-hit baseline).

Output: Line-level string amendments — ROADMAP.md Phase 13 SC2 + SC3,
CONTEXT.md D-08 + the `<domain>` Phase Boundary item 2, and any LINT-04 / CI-06
zero-hits wording in REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md
@.planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-RESEARCH.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-01-PLAN.md

<amendment_facts>
<!-- The verified facts driving every amendment. Sourced from RESEARCH.md Open Q1
     (re-confirmed by the orchestrator 2026-05-22 with a fence-aware scan) and
     from the phase planning context phase-scope-decision. -->

VERIFIED BASELINE: a fence-aware scan of `.md` shell-fence blocks (the
bash / sh / zsh labelled fences) under get-shit-done/workflows/,
get-shit-done/references/, and agents/ found 127 raw-git invocations across
30 files. v1.3's PROMPT-06..09 only removed raw-git from the parallel-dispatch
path; the other 127 hits were never in scope.

USER DECISION (Open Q1 resolved): RE-BASELINE SC2 as a regression guard. The
audit carries a FROZEN baseline of the current 127-hit state and fails only
when NEW raw-git is ADDED beyond that baseline. On the first green run the
current state equals the baseline, so the audit passes. The user did NOT
choose a narrowed-scope zero-assertion and did NOT choose a 30-file cleanup.

REGRESSION-GUARD WORDING (use this consistently — it is the target framing
for every amendment; adapt phrasing to fit each sentence, keep the meaning
exact):
  "the audit reports no raw-git ADDED beyond the recorded 127-hit baseline;
   on the first green run the current state equals the baseline, so the audit
   passes"

KNOWN ZERO-HITS OCCURRENCES TO AMEND (located 2026-05-22 — the executor must
ALSO re-scan to catch any the planner missed):
  - ROADMAP.md line 179  — Phase 13 SC2: "reports zero raw-git hits in `*.md`
    shell-fence blocks ..."
  - ROADMAP.md line 180  — Phase 13 SC3: "fails if the zero-hits invariant
    breaks"
  - CONTEXT.md line 16   — <domain> Phase Boundary item 2: "a stdout-only
    scanner proving zero raw / git invocations ..."
  - CONTEXT.md line 21   — <domain> Phase Boundary item 3: "zero-hits invariant
    breaks"
  - CONTEXT.md line 75   — D-02 lane assertion 6: "exits 0 (CI-06 — zero
    raw-git ..."
  - CONTEXT.md lines 158-159 — D-08: "zero raw-git hits in `.md` shell-fence
    blocks = milestone-completeness proof" and "a quoted zero-hits audit
    summary"
  - REQUIREMENTS.md line 45 — LINT-04: "first green run with zero hits =
    milestone complete"
  - REQUIREMENTS.md line 60 — CI-06: "fails if zero-hits invariant breaks"

CLOSE-COMMIT EVIDENCE RE-FRAME (D-08): the v1.3 milestone close-commit
evidence becomes a quoted audit summary recording "baseline 127, 0 new" — NOT
a "zero-hits" summary.
</amendment_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-baseline ROADMAP.md Phase 13 SC2 + SC3 to regression-guard wording</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (locate "### Phase 13: CI parallel-path lane + lint close-gate", then its "**Success Criteria**" subsection — SC2 at line 179, SC3 at line 180)
    - .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-RESEARCH.md (§"Open Questions" Q1 — the 127-hit finding and the re-baseline resolution path 2)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-01-PLAN.md (cascade-amendment plan structure precedent — straight string replacement, no interpretation)
  </read_first>
  <action>
    Two string replacements inside the Phase 13 "**Success Criteria**" list.

    (1) SC2 — the current text reads (line 179):
    "`scripts/audit-workflow-raw-git.cjs` ships and on first green run reports
    zero raw-git hits in `*.md` shell-fence blocks under
    `get-shit-done/workflows/`, `get-shit-done/references/`, and `agents/`;
    documented as one-shot (NOT added to CI pretest)."

    Replace it so SC2 states the audit is a baseline-regression guard. The
    amended SC2 MUST: (a) name the audit script
    `scripts/audit-workflow-raw-git.cjs`; (b) state it carries a frozen
    baseline of the current 127-hit state across the three scan roots
    (`get-shit-done/workflows/`, `get-shit-done/references/`, `agents/`);
    (c) state it exits non-zero only when the current scan EXCEEDS the
    baseline (NEW raw-git added to workflow markdown), and exits 0 when within
    baseline; (d) state that on the first green run current == baseline ==
    pass; (e) keep the "documented as one-shot (NOT added to CI pretest)"
    clause verbatim. The amended SC2 MUST contain the literal substring `127`
    and the word `baseline`. The amended SC2 MUST NOT contain the word `zero`.

    (2) SC3 — the current text reads (line 180):
    "`parallel-e2e` lane runs LINT-04 audit and fails if the zero-hits
    invariant breaks (acts as milestone-completeness regression guard)."

    Replace the phrase "zero-hits invariant" with "baseline / no-regression
    invariant". Keep the rest of SC3 verbatim ("`parallel-e2e` lane runs
    LINT-04 audit and fails if the …; acts as milestone-completeness
    regression guard"). The amended SC3 MUST NOT contain the word `zero`.

    Preserve the SC numbering (SC1, SC4, SC5 untouched), the surrounding
    bullet/list structure, and every other phase section in the file. Do not
    modify the Phase 13 Goal, Depends on, Requirements, or Plans subsections.
  </action>
  <verify>
    <automated>grep -nA8 '^### Phase 13:' .planning/ROADMAP.md | grep -v '^#' | grep -ci 'zero' | grep -qx 0 || (echo "FAIL: 'zero' still appears in the Phase 13 section"; exit 1)</automated>
    <automated>grep -A12 '^### Phase 13:' .planning/ROADMAP.md | grep -q '127' || (echo "FAIL: amended Phase 13 SC2 does not mention the 127 baseline"; exit 1)</automated>
    <automated>grep -A12 '^### Phase 13:' .planning/ROADMAP.md | grep -q 'baseline / no-regression invariant' || (echo "FAIL: SC3 not re-baselined"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - In the Phase 13 section, `grep -nA12 '^### Phase 13:' .planning/ROADMAP.md | grep -iv '^[0-9]*[:-]#' | grep -ci 'zero'` returns 0 (no "zero" wording survives in Phase 13 SC text)
    - The amended SC2 line contains the literal `127` and the word `baseline`
    - The amended SC2 line still contains the clause `documented as one-shot` and `NOT added to CI pretest`
    - The amended SC3 line contains the exact phrase `baseline / no-regression invariant` and still contains `milestone-completeness regression guard`
    - `grep -n '^### Phase 13:' .planning/ROADMAP.md` shows the Phase 13 header within ±3 lines of line 171 (no structural drift)
    - SC1, SC4, SC5 under Phase 13 are byte-identical to their pre-edit text (no collateral edits)
  </acceptance_criteria>
  <done>Phase 13 SC2 reads as a baseline-regression guard naming the 127-hit baseline; SC3 uses "baseline / no-regression invariant"; no "zero" wording survives in the Phase 13 success criteria.</done>
</task>

<task type="auto">
  <name>Task 2: Re-frame CONTEXT.md D-08 + the &lt;domain&gt; Phase Boundary, and any REQUIREMENTS.md zero-hits wording</name>
  <files>.planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md (the `<domain>` Phase Boundary block at lines 6-34 — items 2 and 3 — and decision D-08 at lines 156-165)
    - .planning/REQUIREMENTS.md (the LINT-04 bullet at line 45 and the CI-06 bullet at line 60 under "### Lint close-gate (LINT)" and "### CI parallel-path lane (CI)")
    - .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-RESEARCH.md (§"Open Questions" Q1 — confirms the re-baseline framing)
  </read_first>
  <action>
    String replacements across two files. The target framing is the
    regression-guard wording in the `<amendment_facts>` block above.

    (1) CONTEXT.md `<domain>` Phase Boundary item 2 (line 16) currently calls
    the script "a stdout-only scanner proving zero raw `git` invocations
    inside `.md` shell-fence blocks ...". Re-word so it describes a stdout-only
    scanner that acts as a baseline-regression guard — it carries a frozen
    127-hit baseline and reports raw-git ADDED beyond that baseline. Keep the
    "stdout-only" property, the three scan roots (`get-shit-done/workflows/`,
    `get-shit-done/references/`, `agents/`), and the existing list of the
    bash / sh / zsh shell-fence labels.

    (2) CONTEXT.md `<domain>` Phase Boundary item 3 (line 21) currently says
    "fails if the zero-hits invariant breaks". Replace "zero-hits invariant"
    with "baseline / no-regression invariant".

    (3) CONTEXT.md D-02 lane assertion 6 (line 75) currently says
    "`node scripts/audit-workflow-raw-git.cjs` exits 0 (CI-06 — zero raw-git
    hits; a non-zero exit fails the lane)". Re-word the parenthetical: the
    audit exits 0 when the current scan is within the 127-hit baseline (no
    raw-git added); a non-zero exit (raw-git regression beyond baseline) fails
    the lane. Keep the rest of the assertion.

    (4) CONTEXT.md D-08 (lines 156-165) currently states the close-gate
    evidence is "(1) the first green `parallel-e2e` audit step — zero raw-git
    hits in `.md` shell-fence blocks = milestone-completeness proof — and
    (2) a quoted zero-hits audit summary in the v1.3 milestone close commit
    message." Re-word both clauses: (1) the first green `parallel-e2e` audit
    step — the current scan is within the 127-hit baseline (no raw-git added)
    = milestone-completeness regression-guard proof; (2) a quoted audit
    summary recording "baseline 127, 0 new" in the v1.3 milestone close commit
    message. Keep the rest of D-08 (the "no committed audit `.md` artifact"
    statement, the departure-from-12-HOOK-IDEMPOTENCY-AUDIT.md note).

    (5) REQUIREMENTS.md LINT-04 (line 45) currently says "v1.3 close-gate
    evidence: first green run with zero hits = milestone complete." Replace
    with: "v1.3 close-gate evidence: first green run within the recorded
    127-hit baseline (no raw-git added) = milestone complete." Keep the rest
    of the LINT-04 bullet (the scan-roots description, "Emits both `.md` +
    JSON sidecar", "Documented as one-shot; not added to CI pretest"). Do not
    touch the bullet's `- [ ]` checkbox state.

    (6) REQUIREMENTS.md CI-06 (line 60) currently says "fails if zero-hits
    invariant breaks". Replace "zero-hits invariant" with "baseline /
    no-regression invariant". Keep the rest of the CI-06 bullet and its
    `- [ ]` checkbox state.

    Do not modify any other decision, requirement, success criterion, or
    section in either file. Do not touch the REQUIREMENTS.md Traceability
    table. Do not change LINT-05 (its "+0 or +1" wording is correct and
    out of scope here).
  </action>
  <verify>
    <automated>grep -rniE 'zero[ -]?hits|reports zero|proving zero|with zero|zero raw' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md && (echo "FAIL: stale zero-hits wording survives"; exit 1) || echo "PASS: no stale zero-hits wording"</automated>
    <automated>grep -q 'baseline 127' .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md || (echo "FAIL: D-08 close-commit evidence not re-framed to 'baseline 127, 0 new'"; exit 1)</automated>
    <automated>grep -c 'baseline / no-regression invariant' .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md .planning/REQUIREMENTS.md | grep -vq ':0' || (echo "FAIL: 'baseline / no-regression invariant' missing from CONTEXT.md or REQUIREMENTS.md"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rniE 'zero[ -]?hits|reports zero|proving zero|with zero|zero raw' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md` returns no matches (exit 1 / empty)
    - CONTEXT.md D-08 contains the literal string `baseline 127, 0 new`
    - CONTEXT.md `<domain>` Phase Boundary item 3 contains `baseline / no-regression invariant`
    - REQUIREMENTS.md CI-06 bullet contains `baseline / no-regression invariant`
    - REQUIREMENTS.md LINT-04 bullet contains `127-hit baseline` and still contains `not added to CI pretest`
    - The REQUIREMENTS.md LINT-04 bullet still starts with `- [ ] **LINT-04**:` and CI-06 with `- [ ] **CI-06**:` (checkbox state preserved)
    - The REQUIREMENTS.md "## Traceability" table is unchanged (`grep '| LINT-04 | Phase 13 |' .planning/REQUIREMENTS.md` still matches)
    - LINT-05's `- [ ] **LINT-05**:` bullet is byte-identical to its pre-edit text
  </acceptance_criteria>
  <done>CONTEXT.md D-08 + `<domain>` Phase Boundary and REQUIREMENTS.md LINT-04 + CI-06 all describe the audit as a baseline-regression guard with the 127-hit baseline; the close-commit evidence is the "baseline 127, 0 new" summary; no stale zero-hits wording survives in any of the three files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits planning-documentation Markdown only. No code, no runtime, no untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `13-CONTEXT.md` — an incorrect amendment leaves a stale/contradictory success criterion that misdirects downstream executors | low | mitigate | Every amendment is a verbatim string replacement against an exact pre-edit string named in the action; the acceptance criteria assert (a) no stale "zero" wording remains and (b) the new regression-guard wording is present; the cascade-amendment-first wave ordering guarantees 13-02/13-04 read the corrected text |
| T-13-02 | Repudiation | v1.3 milestone close-commit evidence wording (sourced from D-08) | low | accept | D-08 is the durable VCS-permanent evidence record; re-framing it to "baseline 127, 0 new" makes the evidence claim accurate. No further mitigation needed — the close commit is itself the audit trail. |

No `high`-severity threats in this plan's scope (documentation-only change).
</threat_model>

<verification>
- `grep -rniE 'zero[ -]?hits|reports zero|proving zero|with zero|zero raw' .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-CONTEXT.md` returns nothing — no stale zero-hits claim about the audit survives anywhere
- ROADMAP.md Phase 13 SC2 names the `127` baseline and the word `baseline`; SC3 uses `baseline / no-regression invariant`
- CONTEXT.md D-08 close-commit evidence reads "baseline 127, 0 new"
- REQUIREMENTS.md LINT-04 + CI-06 reconciled to the regression-guard framing
- No collateral edits — diff against pre-amendment state shows only the named string replacements in the three files
</verification>

<success_criteria>
- ROADMAP.md Phase 13 SC2 + SC3 re-baselined to regression-guard wording per the user's Open Q1 resolution
- CONTEXT.md D-08 + `<domain>` Phase Boundary re-worded; close-commit evidence is the "baseline 127, 0 new" summary
- REQUIREMENTS.md LINT-04 + CI-06 zero-hits wording reconciled
- Documentation is internally consistent: the audit script plan (13-02) and the CI lane plan (13-04) will read SC2/SC3 wording that matches what the audit is actually built to do (a baseline-regression guard)
- No stale "zero hits" claim about the audit survives in any planning file
</success_criteria>

<output>
Create `.planning/phases/13-ci-parallel-path-lane-lint-close-gate/13-01-SUMMARY.md` when done. Summary captures: every string replaced (old → new), which files were touched, and explicit confirmation that a re-scan found no surviving "zero hits / zero-hits / reports zero / proving zero" wording about the audit in ROADMAP.md, REQUIREMENTS.md, or 13-CONTEXT.md.
</output>
