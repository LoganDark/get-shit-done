---
phase: 18-tactical-cleanup-test-flake-re-scoped
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcs/__tests__/jj-reap.test.ts
autonomous: true
requirements: [TEST-17]
must_haves:
  truths:
    - "TEST-17 has a recorded verdict: (a) flake reproduced → narrow per-test fix landed, or (b) not reproducible in 3+ consecutive full-suite runs → closed as resolved-by-restructure with the runs recorded as evidence"
    - "Root vitest.config.ts is byte-identical to its pre-plan state (UNTOUCHED)"
    - "scripts/check-skip-count.cjs is green (baseline 22 — no new skip patterns, no retry config)"
  artifacts:
    - path: "src/vcs/__tests__/jj-reap.test.ts"
      provides: "inclusion-filter test stable under full-suite load (modified ONLY if verdict (a); otherwise untouched)"
      contains: "inclusion-filter"
  key_links:
    - from: "src/vcs/__tests__/jj-reap.test.ts"
      to: "vitest.config.ts unit project"
      via: "inherited 30_000ms testTimeout + maxWorkers 2 (19-11) — the changed conditions the re-verify gate tests against"
      pattern: "inclusion-filter"
---

<objective>
Close TEST-17 via re-verify-then-fix: the `jj-reap.test.ts > workspace.reap > inclusion-filter` test timed out at 5000ms under the OLD `sdk/vitest.config.ts` full parallelism, but 19-11 doubly removed the flake's preconditions — the suite now runs under root `vitest.config.ts` with `maxWorkers: 2` AND a 30_000ms unit-project testTimeout (the 5s default no longer exists anywhere in the unit project). Verdict (b) resolved-by-restructure is the likely outcome (19-13 phase gate ran 612/612 green), but it must be PROVEN by 3+ recorded full-suite runs before anything is touched.

Reproduce BEFORE touching anything. If reproduced: per-test narrow fix only (Pitfall 9 envelope: diff ≤5 LOC, ≤1 file, root `vitest.config.ts` UNTOUCHED, no `retry: N`, no skip modifiers, no broader vitest reorg — `project_test_perf_pain_vitest` sweep stays out of scope).

Purpose: a deterministic verdict on the last open v1.4 flake, with evidence.
Output: either an unmodified tree + evidence transcript (verdict b), or a ≤5 LOC single-file fix (verdict a). Closes todo `v14-jj-reap-test-flake`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-RESEARCH.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-VALIDATION.md
@.planning/todos/pending/v14-jj-reap-test-flake.md
@src/vcs/__tests__/jj-reap.test.ts
@vitest.config.ts
</context>

## Artifacts this phase plan produces

No new files; possibly ZERO file edits (verdict b leaves the tree untouched). Symbols conditionally created:

- Verdict (a) only: an options-object timeout on the inclusion-filter test signature in `src/vcs/__tests__/jj-reap.test.ts` — `it('inclusion-filter: ...', { timeout: 60000 }, () => {...})` (suite precedent form, 15+ occurrences)
- Both verdicts: the evidence transcript (3+ run results: pass/fail + duration each) recorded in `18-03-SUMMARY.md` — stdout transcript summary only, NEVER files written into the working tree (`feedback_avoid_jj_auto_tracked_output`)

## Stale-literal note (locked deviation)

The REQUIREMENTS' fix-shape literal `it(..., 15_000)` predates 19-11 and is now a timeout REDUCTION (unit-project default is already 30_000). If verdict (a) fires, honoring the literal's INTENT (extend headroom) requires `{ timeout: 60000 }` (options-object form per PATTERNS.md — the trailing-number form is deprecated; a 60s precedent exists at `cmd-parallel-git.test.ts:429`). Record the deviation in the SUMMARY.

<tasks>

<task type="auto">
  <name>Task 1: Re-verification gate — 3+ recorded full-suite runs BEFORE touching anything</name>
  <files>(none — read/run only)</files>
  <read_first>
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-RESEARCH.md (§TEST-17 — config analysis, reproduction commands, fix-shape findings)
    - src/vcs/__tests__/jj-reap.test.ts (structure: describe.skipIf(!jjAvailable) wrapping 5 sequential its sharing a beforeAll fixture; inclusion-filter at L79-94; header comment L25-27 bans retry config and skip modifiers per D-15/Pitfall 9)
    - vitest.config.ts (READ ONLY — confirm maxWorkers: 2 and unit testTimeout: 30_000 still hold; this file is LOCKED)
    - .planning/todos/pending/v14-jj-reap-test-flake.md (original repro behavior: timeout in 3-file run, 636ms in isolation)
  </read_first>
  <action>
    Run the re-verification gate. Do NOT edit any file in this task.

    1. Run `GSD_TEST_BACKENDS=git,jj npx vitest run` (the 19-13 phase-gate form, ~110s, 612 tests) at least 3 consecutive times. For each run record: exit code, total pass/fail/skip counts, total duration, and specifically whether `jj-reap.test.ts > workspace.reap … > inclusion-filter` passed and its reported duration.
    2. Additionally run the original Phase 14 regression-gate 3-file shape once for fidelity to the original repro: `npx vitest run src/vcs/__tests__/jj-reap.test.ts src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` — record the same data.
    3. Optionally run the isolation control: `npx vitest run --project unit src/vcs/__tests__/jj-reap.test.ts -t 'inclusion-filter'` (original todo recorded 636ms in isolation).
    4. Decide the verdict: (b) NOT reproducible — every run green including inclusion-filter → proceed to Task 2 closure path; (a) REPRODUCED — any run shows the inclusion-filter timeout → proceed to Task 2 fix path.
    5. Keep all evidence as stdout transcript data for the SUMMARY. Do NOT write transcript files into the working tree.

    NOTE: if plan 18-02 runs concurrently in another workspace, that is irrelevant here — this plan's runs execute in its own workspace tree; the verdict is about THIS tree's behavior under the current locked config.
  </action>
  <verify>
    <automated>GSD_TEST_BACKENDS=git,jj npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - At least 3 consecutive `GSD_TEST_BACKENDS=git,jj npx vitest run` executions completed, each with recorded exit code, counts, duration, and inclusion-filter result
    - The 3-file regression-gate shape run once with the same data recorded
    - A single explicit verdict declared: (a) reproduced or (b) not reproducible — with the evidence table drafted for the SUMMARY
    - Zero file modifications in this task (`gsd_run query diff --name-only` shows no source/test/config changes attributable to this task)
  </acceptance_criteria>
  <done>Verdict (a) or (b) is decided on recorded evidence from 3+ full-suite runs; tree untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the verdict — narrow fix (a) or resolved-by-restructure closure (b)</name>
  <files>src/vcs/__tests__/jj-reap.test.ts</files>
  <read_first>
    - src/vcs/__tests__/jj-reap.test.ts (inclusion-filter signature at L79; file uses TABS; header comment L25-27 constraints)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md (§jj-reap.test.ts — the options-object timeout precedent `{ timeout: 30000 }` at cmd-parallel-jj.test.ts:141 and the 60s example at cmd-parallel-git.test.ts:429)
    - scripts/check-skip-count.cjs (what counts as a skip pattern — `describe.skipIf` is NOT counted; exempt annotation form)
  </read_first>
  <action>
    Verdict (b) — NOT reproducible (expected): make ZERO edits to any file. Close TEST-17 as resolved-by-restructure. The SUMMARY records: the 3+ run evidence table (exit codes, counts, durations, inclusion-filter timings), the config-change rationale (19-11 removed the 5s default twice over: maxWorkers 2 + unit testTimeout 30_000), and the explicit closure statement for todo `v14-jj-reap-test-flake`. Run `node scripts/check-skip-count.cjs` to confirm baseline 22 unchanged.

    Verdict (a) — reproduced: apply the narrowest fix honoring the Pitfall 9 envelope. Preferred shape: change ONLY the inclusion-filter test signature at L79 to the options-object form `it('inclusion-filter: ignores workspaces NOT matching phaseNamePrefix', { timeout: 60000 }, () => {` — 60_000 not the REQUIREMENTS' stale 15_000 literal (which would REDUCE the effective timeout below the 30_000 project default; record the deviation). Constraints (hard): diff ≤5 LOC; ≤1 file (`src/vcs/__tests__/jj-reap.test.ts` only); root `vitest.config.ts` byte-identical; no `retry: N`; no `.skip`/`.todo`/`x`-prefix patterns (header comment L25-27 + check-skip-count baseline 22). If a 30s timeout fired, that indicates a real hang rather than load latency — still stay inside the envelope: record the finding in the SUMMARY, apply the least-invasive option ({ concurrent: false }-style opt-out only as the documented fallback), and flag the root-cause question for a future phase rather than expanding scope. After the fix: re-run the full suite 3+ times to confirm the flake does not re-surface (todo acceptance), and run `node scripts/check-skip-count.cjs`.

    Either verdict: confirm `vitest.config.ts` is untouched via `gsd_run query diff --name-only` (must not list it).
  </action>
  <verify>
    <automated>node scripts/check-skip-count.cjs && npx vitest run --project unit src/vcs/__tests__/jj-reap.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Verdict (b): zero file edits; SUMMARY contains the 3+ run evidence table and the resolved-by-restructure closure statement
    - Verdict (a): diff ≤5 LOC, confined to src/vcs/__tests__/jj-reap.test.ts; options-object `{ timeout: ... }` form used (not the deprecated trailing number, not the stale 15_000 literal); 3+ post-fix full-suite runs green
    - Both verdicts: `gsd_run query diff --name-only` does NOT list vitest.config.ts at any point
    - Both verdicts: `node scripts/check-skip-count.cjs` green (baseline 22; no retry config, no skip modifiers added)
    - Both verdicts: `npx vitest run --project unit src/vcs/__tests__/jj-reap.test.ts` exits 0
  </acceptance_criteria>
  <done>TEST-17 closed with a recorded verdict and evidence; Pitfall 9 envelope respected; config and skip-count baselines intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | test-hygiene-only plan: runs the existing suite and conditionally edits one test signature; no untrusted input crosses any boundary, no production code touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | DoS (test-infra) | masking a real hang by inflating a timeout | accept | bounded: per Pitfall 9 a 30s+ timeout firing under maxWorkers:2 is recorded as a real-hang finding in the SUMMARY rather than silently absorbed; envelope forbids retry config that would hide nondeterminism |

No package installs in this plan (Package Legitimacy Gate: not applicable per 18-RESEARCH.md). No production attack surface — register kept for ASVS L1 completeness.
</threat_model>

<verification>
- 3+ recorded `GSD_TEST_BACKENDS=git,jj npx vitest run` executions (evidence in SUMMARY)
- `node scripts/check-skip-count.cjs` green (baseline 22)
- `vitest.config.ts` untouched (absent from `gsd_run query diff --name-only`)
- Verdict (a) only: diff ≤5 LOC, 1 file
</verification>

<success_criteria>
ROADMAP SC5 satisfied by exactly one verdict: (a) flake reproduced under the current config → per-test narrow fix (≤5 LOC, ≤1 file, config UNTOUCHED, skip-count green), or (b) not reproducible in 3+ full-suite runs → closed as resolved-by-restructure with the runs recorded as evidence in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-03-SUMMARY.md` when done. Include: the verdict, the full run-evidence table (exit codes, counts, durations, inclusion-filter timings per run), the 15_000-literal deviation note (if verdict a), and the `v14-jj-reap-test-flake` closure statement.
</output>
