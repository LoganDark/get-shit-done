---
phase: 18-tactical-cleanup-test-flake-re-scoped
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/vcs-command-router.cts
  - src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts
  - scripts/dogfood-restore.sh
  - src/vcs/__tests__/cmd-parallel-jj.test.ts
  - src/vcs/__tests__/cmd-parallel-git.test.ts
autonomous: true
requirements: [CLEANUP-03, CLEANUP-04, CLEANUP-05, CLEANUP-06, CLEANUP-07]
must_haves:
  truths:
    - "A non-array parsed plan returns {ok:false, reason:'plan_not_array'} BEFORE createVcsAdapter is called"
    - "NaN --max-concurrency returns {ok:false, reason:'max_concurrency_invalid'}; an absent flag still forwards undefined (D-07 contract preserved)"
    - "dogfood-restore.sh run outside a project root exits 1 with ERROR on stderr before the tarball-existence check, jj op restore, or tar -xf"
    - "The tar-overlay additive asymmetry is documented as intended (option b)"
    - "CONFIG-02 test runs leave zero gsd-cfg02-* dirs in tmp"
  artifacts:
    - path: "src/vcs-command-router.cts"
      provides: "Array.isArray + Number.isNaN fail-closed guards in the workspace.parallel.dispatch handler"
      contains: "plan_not_array"
    - path: "src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts"
      provides: "contract tests pinning both new envelopes"
      contains: "max_concurrency_invalid"
    - path: "scripts/dogfood-restore.sh"
      provides: "project-root assertion (after the positional parse, before the tarball check and both mutations) + overlay-asymmetry comment block"
      contains: ".planning/STATE.md"
  key_links:
    - from: "src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts"
      to: "src/vcs-command-router.cts"
      via: "VCS_VERB_TABLE['workspace.parallel.dispatch'] dynamic import (vite resolves .cjs specifier to .cts source)"
      pattern: "workspace\\.parallel\\.dispatch"
    - from: "src/vcs-command-router.cts guards"
      to: "createVcsAdapter"
      via: "early return BEFORE adapter creation (recordedDispatchOpts.length === 0 proves it)"
      pattern: "plan_not_array|max_concurrency_invalid"
---

<objective>
Close the Phase 14 code-review warnings WR-01..05 (CLEANUP-03..07) at their post-Phase-19 relocated paths. Per-WR commits per v1.4 Pitfall 10, in locked order: prod-code guards FIRST (CLEANUP-05 `Array.isArray` → `plan_not_array` envelope; CLEANUP-06 `Number.isNaN(maxConcurrency)` → `max_concurrency_invalid` envelope — both in `src/vcs-command-router.cts`, both returning BEFORE `createVcsAdapter`), then script fixes (CLEANUP-03 project-root assertion; CLEANUP-04 overlay decision = option (b) documented asymmetry per RESEARCH recommendation), then test fixes (CLEANUP-07 describe-scoped `let tmpDir` hoist + `afterEach` rm in both cmd-parallel test files).

The 5 Phase 14 info findings are NOT in scope: default is skip per the RESEARCH Pitfall 6 scope fence — opportunistic-only is never forced, even though info items 1-2 (arg-parser lower bound, silent unknown-flag handling) concern the dispatch arg loop adjacent to Task 1's edits; the fence wins and they stay skipped. The todo's "14-REVIEW.md closure note" criterion is path-unsatisfiable: `.planning/phases/14-default-flip-dogfood-validation/14-REVIEW.md` no longer exists in the tree (archived at milestone close) — this plan's SUMMARY is the superseding closure record, consistent with "REQUIREMENTS.md re-scope wins on paths".

Purpose: fail-closed CLI input validation (ASVS V5) + recovery-script safety + test hygiene.
Output: 5 per-WR commits across the 5 files above. Closes todo `v14-review-followups` WR-01..05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-RESEARCH.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-VALIDATION.md
@.planning/todos/pending/v14-review-followups.md
@src/vcs-command-router.cts
@scripts/dogfood-restore.sh
</context>

## Artifacts this phase plan produces

No new files. All edits are to existing files. New symbols created:

- New envelope reason strings in `src/vcs-command-router.cts`: `plan_not_array` (CLEANUP-05, locked by REQ text) and `max_concurrency_invalid` (CLEANUP-06, locked here per RESEARCH Open Q1 — snake_case like peers `phase_number_required` / `plan_json_parse_failed`; the contract tests pin it)
- New contract test names in `src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts`: a sibling describe (e.g. `describe('CLEANUP-05/06 — dispatch input guards')`) with `it`s covering plan inputs `'{"not":"an array"}'`, `'"str"'`, `'42'`, `'null'` and `--max-concurrency` values `NaN` / `banana`
- New assertion line in `scripts/dogfood-restore.sh`: `[ -f .planning/STATE.md ] || { echo "ERROR: dogfood-restore.sh must run from project root" >&2; exit 1; }` (WR-01 literal, inserted immediately after the L47-48 positional parse, BEFORE the L65-68 tarball check) — modified script behavior: wrong-cwd runs now abort at the root assertion pre-mutation (not at the tarball FATAL)
- New comment block in `scripts/dogfood-restore.sh` documenting the additive tar-overlay asymmetry as intended (CLEANUP-04 option b)
- `afterEach` tmpDir cleanup + describe-scoped `let tmpDir` in the CONFIG-02 describes of `src/vcs/__tests__/cmd-parallel-{jj,git}.test.ts`

## Commit structure (per-WR per v1.4 Pitfall 10)

5 commits in this order, each with its own verification: (1) `fix(18-02): WR-03/CLEANUP-05 plan_not_array guard + contract tests`, (2) `fix(18-02): WR-04/CLEANUP-06 max-concurrency NaN guard + contract tests`, (3) `fix(18-02): WR-01/CLEANUP-03 dogfood-restore project-root assertion`, (4) `docs(18-02): WR-02/CLEANUP-04 document tar-overlay asymmetry as intended`, (5) `test(18-02): WR-05/CLEANUP-07 CONFIG-02 afterEach tmpDir cleanup`. Guards and their pin tests land in the SAME per-WR commit (guard-addition, NOT TDD-RED).

<tasks>

<task type="auto">
  <name>Task 1: Router input guards (CLEANUP-05 + CLEANUP-06) with contract tests — commits 1 and 2</name>
  <files>src/vcs-command-router.cts, src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts</files>
  <read_first>
    - src/vcs-command-router.cts (the workspace.parallel.dispatch handler — pre-edit anchors: arg loop L1131-1143 with `maxConcurrency = Number(args[++i])` at L1141; phase guard at L1145-1147; plan_json_parse_failed try/catch at L1177-1189; createVcsAdapter call at L1191; re-locate by the `plan_json_parse_failed` literal if lines drifted)
    - src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts (the whole file — loader at L35-44, recordedDispatchOpts + vi.mock block at L48-71, D-07 absent-flag test at L132-145)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md (§vcs-command-router + §cmd-parallel-max-concurrency-cli — exact guard placement and the new-test envelope-assertion shape)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-RESEARCH.md (§CLEANUP-05/06)
  </read_first>
  <action>
    Router file style: 2-space indent, single quotes, semicolons (match the file, NOT the global tab rule). Test file uses TABS. Order: CLEANUP-05 commit first, then CLEANUP-06 commit (per the roadmap's listed order within "prod-code guards first").

    CLEANUP-05 (commit 1): in the `workspace.parallel.dispatch` handler, immediately after the `plan_json_parse_failed` try/catch (post-L1189) and BEFORE the `createVcsAdapter(cwd)` call, add a guard returning the fail-closed envelope with reason `plan_not_array` when `!Array.isArray(plan)` (data-shape peer to `phase_number_required`: `{ data: { ok: false, reason: ... } }`). In the same commit, add contract `it`s to a new sibling describe in cmd-parallel-max-concurrency-cli.test.ts reusing the EXISTING module-level vi.mock recorder (do NOT duplicate the vi.mock block): for each plan input `'{"not":"an array"}'`, `'"str"'`, `'42'`, `'null'` — reset `recordedDispatchOpts.length = 0`, load the verb via the existing `loadWorkspaceParallelDispatchVerb()` loader, call with `['--phase', '18', '--plan', <input>]` and projectDir `'/tmp/irrelevant-cwd'` (works: handler's config-read catch at ~L1161-1163 treats missing config as dispatch-allowed), then assert `data.ok === false`, `data.reason === 'plan_not_array'`, and `recordedDispatchOpts.length === 0` (guard fires BEFORE the adapter).

    CLEANUP-06 (commit 2): after the arg loop, adjacent to the L1145 phase guard, add a guard returning reason `max_concurrency_invalid` when `maxConcurrency !== undefined && Number.isNaN(maxConcurrency)`. The `!== undefined` leg is load-bearing: it preserves the D-07 absent-flag-forwards-undefined contract pinned by the existing test at L132-145 — that test MUST stay green. Reason-string decision: `max_concurrency_invalid` (RESEARCH Open Q1 recommendation; WR-04's "same guard as --phase" cannot reuse `phase_number_required` because --max-concurrency is an optional flag, not a required field — record this decision in the SUMMARY). Same commit: contract `it`s for `['--max-concurrency', 'NaN']` and `['--max-concurrency', 'banana']` (plus a valid `--phase` and a valid array `--plan` so only the concurrency guard can fire), asserting `data.ok === false`, `data.reason === 'max_concurrency_invalid'`, `recordedDispatchOpts.length === 0`.

    After both commits: run `pnpm run build:lib` to refresh the emitted `gsd-core/bin/lib/vcs-command-router.cjs` artifact (Pitfall 4 / 19-06 stale-dist precedent — vitest resolves the .cts source and needs no build, but the CLI/node:test surface requires the rebuilt artifact).
  </action>
  <verify>
    <automated>npx vitest run --project unit src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts && pnpm run build:lib && node scripts/lint-vcs-no-raw-git.cjs && node scripts/lint-vcs-no-commit-id.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --project unit src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts` exits 0 with the new `it`s passing AND the pre-existing D-07 absent-flag test still green
    - `grep -c "plan_not_array" src/vcs-command-router.cts` ≥ 1 and `grep -c "max_concurrency_invalid" src/vcs-command-router.cts` ≥ 1, both BEFORE the `createVcsAdapter(cwd)` line in the dispatch handler
    - All 4 non-array plan shapes (object, string, number, null) and both NaN inputs produce `{ok:false}` envelopes with `recordedDispatchOpts.length === 0`
    - `pnpm run build:lib` exits 0 (emitted artifact refreshed)
    - `node scripts/lint-vcs-no-raw-git.cjs` and `node scripts/lint-vcs-no-commit-id.cjs` exit 0
  </acceptance_criteria>
  <done>Both guards return fail-closed envelopes before adapter creation; envelopes pinned by contract tests in the existing vi.mock harness; D-07 contract preserved; emitted lib rebuilt. Two per-WR commits landed.</done>
</task>

<task type="auto">
  <name>Task 2: dogfood-restore.sh hardening (CLEANUP-03 + CLEANUP-04) — commits 3 and 4</name>
  <files>scripts/dogfood-restore.sh</files>
  <read_first>
    - scripts/dogfood-restore.sh (all 121 lines — pre-edit anchors: L25 comment-only precondition, L29 `set -euo pipefail`, L47-48 positional parse (the CLEANUP-03 insertion anchor), L65-68 tarball FATAL block, L71 `jj op restore`, L74 `tar -xf "$TARBALL_PATH" -C .`, L13-17 "Ordering rationale" comment-block precedent)
    - tests/scripts/dogfood-restore-orphan-cleanup.test.cjs (seedJjRepo already writes `.planning/STATE.md` into the fixture explicitly for this WR-01 precondition — both existing tests must pass unmodified)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md (§dogfood-restore.sh)
  </read_first>
  <action>
    Keep `#!/usr/bin/env bash` and TAB indentation (existing script — the zsh rule applies to new scripts only).

    CLEANUP-03 (commit 3): insert the WR-01 literal assertion immediately after the positional parse (`PRE_OP_ID`/`TARBALL_PATH`, L47-48) and BEFORE the tarball-existence check (L65-68) — still pre-mutation, so ONE assertion guards the tarball check AND both mutations: `[ -f .planning/STATE.md ] || { echo "ERROR: dogfood-restore.sh must run from project root" >&2; exit 1; }` — tab-indented, `set -e`-safe via the `||` group; the WR-01 one-liner is the spec text, prefer it over the if-block FATAL style. Placement deviates from RESEARCH §CLEANUP-03's post-tarball-check anchor (plan-checker fix): if the assertion landed after the tarball check, a wrong-cwd run with dummy args would exit 1 at `FATAL: tarball not found` pre- AND post-fix, making the wrong-cwd verify tautological — the root assertion must be what fires. Also update the L25 comment (`# Pre-condition: must be run from the project root.`) to note the check is now enforced.

    CLEANUP-04 (commit 4): option (b) documented asymmetry — RESEARCH recommendation, accepted at plan time (REQ explicitly allows either; (a) clean overlay would add a destructive `rm -rf .planning` to a recovery primitive, the worst place for new risk; Phase 14 P05 validated restore-then-untar empirically in production). Add a multi-line `#` comment block adjacent to the `tar -xf` line (style precedent: the file's own L13-17 "Ordering rationale" block) stating: `jj op restore` already rewinds tracked `.planning` state; the tar overlay is an additive top-up; files created after the snapshot surviving the restore is ACCEPTED, intended behavior (validated by Phase 14 P05's production run). Zero behavior change in this commit.

    The script must stay git-free (jj + tar only) — `lint-vcs-no-raw-git` scans `.sh`.
  </action>
  <verify>
    <automated>REPO="$PWD"; OUT=$(cd /tmp && bash "$REPO/scripts/dogfood-restore.sh" x y 2>&1); RC=$?; test "$RC" -eq 1 && printf '%s\n' "$OUT" | grep -q 'ERROR: dogfood-restore.sh must run from project root' && cd "$REPO" && node --test tests/scripts/dogfood-restore-orphan-cleanup.test.cjs && node scripts/lint-vcs-no-raw-git.cjs</automated>
  </verify>
  <acceptance_criteria>
    - With `REPO="$PWD"` captured before `cd`: `cd /tmp && bash "$REPO/scripts/dogfood-restore.sh" x y` exits 1 with `ERROR: dogfood-restore.sh must run from project root` on stderr — the root assertion fires, NOT `FATAL: tarball not found` (the assertion now precedes the tarball check); no jj op restore, no tar extraction attempted
    - `grep -c '\.planning/STATE\.md' scripts/dogfood-restore.sh` ≥ 1, positioned after the positional parse and before the tarball-existence check (hence before `jj op restore` and `tar -xf`)
    - `node --test tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` exits 0 (both existing tests green, unmodified — fixture already seeds .planning/STATE.md)
    - The comment block adjacent to `tar -xf` states the additive-overlay asymmetry is intended and cites jj op restore as the actual rollback (manual-review criterion per 18-VALIDATION.md)
    - `node scripts/lint-vcs-no-raw-git.cjs` exits 0
  </acceptance_criteria>
  <done>Wrong-cwd runs abort pre-mutation with ERROR; overlay semantics documented as intended; existing node:test green unmodified. Two per-WR commits landed.</done>
</task>

<task type="auto">
  <name>Task 3: CONFIG-02 tmpDir leak cleanup in both cmd-parallel test files (CLEANUP-07) — commit 5</name>
  <files>src/vcs/__tests__/cmd-parallel-jj.test.ts, src/vcs/__tests__/cmd-parallel-git.test.ts</files>
  <read_first>
    - src/vcs/__tests__/cmd-parallel-jj.test.ts (CONFIG-02 describe at ~L778-866; imports at L36-38; mkdtemp const sites at L780/L809/L842)
    - src/vcs/__tests__/cmd-parallel-git.test.ts (byte-parallel CONFIG-02 describe at ~L1007-1095, prefix `gsd-cfg02-git-`; mkdtemp sites at L1009/L1038/L1071)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md (§CONFIG-02 describes — the exact required edit shape incl. the let hoist)
  </read_first>
  <action>
    Per file (both files, TABS). The acceptance literal `afterEach(() => rm(tmpDir, {recursive: true, force: true}))` mechanically requires hoisting `tmpDir` from per-it `const` to describe scope (RESEARCH: required enabling edit, NOT scope creep):

    1. Imports: add `afterEach` to the vitest import line; add `rm` to the `node:fs/promises` import line (the async `rm` is the acceptance literal — do not substitute the already-imported sync `rmSync`).
    2. Inside the `describe('CONFIG-02 — parallelization_disabled', ...)` block ONLY (other describes already have their own beforeAll/afterAll rmSync cleanup — do not touch them): hoist `let tmpDir: string;` to describe scope; change each of the 3 `const tmpDir = await mkdtemp(...)` sites to `tmpDir = await mkdtemp(...)` (jj file: L780/L809/L842; git file: L1009/L1038/L1071).
    3. Add `afterEach(async () => { await rm(tmpDir, { recursive: true, force: true }); });` at the top of the describe. Safe because vitest runs same-file `it`s serially.

    Verification protocol (SC4 "test-run-then-inspect-tmp"): pre-clean any old leaks (`rm -rf "${TMPDIR:-/tmp}"/gsd-cfg02-*` — these are OUTSIDE the working tree, safe), run the targeted CONFIG-02 tests, then assert zero `gsd-cfg02-*` dirs remain. Single commit (commit 5).
  </action>
  <verify>
    <automated>rm -rf "${TMPDIR:-/tmp}"/gsd-cfg02-* 2>/dev/null; npx vitest run --project unit src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts -t 'CONFIG-02' && test "$(ls -d "${TMPDIR:-/tmp}"/gsd-cfg02-* 2>/dev/null | wc -l)" -eq 0</automated>
    <note>TMPDIR isolation contract: this pre-clean + zero-leak inspection must not run while another suite run shares the default TMPDIR — guaranteed for intra-phase wave-1 dispatch because 18-03 runs all of its suite invocations under a private `TMPDIR=$(mktemp -d)`.</note>
  </verify>
  <acceptance_criteria>
    - Targeted CONFIG-02 run exits 0 (all 6 tests across both files green)
    - After the run, `ls -d "${TMPDIR:-/tmp}"/gsd-cfg02-* 2>/dev/null | wc -l` returns 0 (6 leaked dirs per run eliminated)
    - Both files' CONFIG-02 describes contain `afterEach` with `rm(tmpDir, { recursive: true, force: true })` and a describe-scoped `let tmpDir: string;`
    - Full-file runs of both test files exit 0 (no collateral damage to the other describes)
    - `node scripts/check-skip-count.cjs` green (no new skip patterns; baseline 22)
  </acceptance_criteria>
  <done>Zero gsd-cfg02-* tmp dirs survive a CONFIG-02 run; both files fully green; one per-WR commit landed. SUMMARY records the WR-01..05 closure (superseding the archived 14-REVIEW.md closure-note criterion) and confirms the 5 info findings were left untouched per scope fence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv/JSON → dispatch handler | untrusted `--plan` JSON and `--max-concurrency` argv cross into the router before any adapter exists |
| operator shell → recovery script | `dogfood-restore.sh` performs `jj op restore` + `tar -xf -C .` relative to whatever cwd it is run from |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering/DoS | workspace.parallel.dispatch handler (non-array plan passes through unchecked) | mitigate | CLEANUP-05 `Array.isArray(plan)` guard, fail-closed `plan_not_array` envelope returned BEFORE `createVcsAdapter`; pinned by contract tests (ASVS V5 input validation) |
| T-18-02 | DoS | `--max-concurrency` parsing (NaN cap silently accepted into scheduling) | mitigate | CLEANUP-06 `Number.isNaN` guard, `max_concurrency_invalid` envelope; absent-flag undefined contract preserved; pinned by contract tests |
| T-18-03 | Tampering | `tar -xf -C .` into wrong cwd (path-relative extraction outside project root) | mitigate | CLEANUP-03 `[ -f .planning/STATE.md ]` assertion before BOTH mutations (`jj op restore` and `tar -xf`) |
| T-18-06 | Tampering | additive tar overlay surviving post-snapshot files | accept | CLEANUP-04 option (b): `jj op restore` is the actual rollback of tracked state; asymmetry documented as intended (Phase 14 P05 production-validated); a destructive clean overlay in a recovery primitive is higher risk than the accepted residue |

No package installs in this plan (Package Legitimacy Gate: not applicable per 18-RESEARCH.md).
</threat_model>

<verification>
- `npx vitest run --project unit src/vcs/__tests__/cmd-parallel-max-concurrency-cli.test.ts src/vcs/__tests__/cmd-parallel-jj.test.ts src/vcs/__tests__/cmd-parallel-git.test.ts` exit 0
- `node --test tests/scripts/dogfood-restore-orphan-cleanup.test.cjs` exit 0
- With `REPO="$PWD"` captured pre-cd: `cd /tmp && bash "$REPO/scripts/dogfood-restore.sh" x y` → exit 1, `ERROR: dogfood-restore.sh must run from project root` on stderr
- `pnpm run build:lib` exit 0 after router edits
- All four lint/audit gates exit 0; `node scripts/check-skip-count.cjs` green (baseline 22)
</verification>

<success_criteria>
ROADMAP SC2/SC3/SC4 satisfied: both router guards live with pinned contract-test envelopes (guards return before `createVcsAdapter`); dogfood-restore.sh aborts pre-mutation outside a project root and documents the overlay asymmetry; CONFIG-02 runs leak zero tmp dirs. 5 per-WR commits in the locked order (05, 06, 03, 04, 07).
</success_criteria>

<output>
Create `.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-02-SUMMARY.md` when done. Include: the `max_concurrency_invalid` reason-string decision record, the WR-01..05 closure table (superseding the archived 14-REVIEW.md closure-note criterion), and the info-findings-untouched confirmation.
</output>
