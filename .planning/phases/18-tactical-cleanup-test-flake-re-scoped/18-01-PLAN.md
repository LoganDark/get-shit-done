---
phase: 18-tactical-cleanup-test-flake-re-scoped
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd-core/workflows/transition.md
autonomous: true
requirements: [CLEANUP-01]
must_haves:
  truths:
    - "Each mutating transition step (phase.complete, PROJECT.md evolution, STATE.md sweep) is followed immediately by a gsd_run query commit fence"
    - "Both config-set workflow._auto_chain_active sites (Routes B1 L552 / B L606) carry tolerant commit-adjacency (|| true)"
    - "A dirty working copy halts the transition with FATAL before any terminal banner prints (verified in a throwaway fixture repo)"
    - "audit-workflow-raw-git frozen baseline stays green (transition.md stays at exactly 1 hit — no second launcher embed)"
  artifacts:
    - path: "gsd-core/workflows/transition.md"
      provides: "commit-adjacency after every mutating step + assert_clean_wc final gate"
      contains: '<step name="assert_clean_wc">'
  key_links:
    - from: "gsd-core/workflows/transition.md assert_clean_wc step"
      to: "gsd-tools diff verb"
      via: "gsd_run query diff --name-only fence"
      pattern: "gsd_run query diff --name-only"
    - from: "gsd-core/workflows/transition.md update_roadmap_and_state"
      to: "gsd-tools commit verb"
      via: "commit fence immediately after phase.complete"
      pattern: "gsd_run query commit"
---

<objective>
Close CLEANUP-01: graft the commit-adjacency + `assert_clean_wc` final-gate pattern (surviving in `gsd-core/workflows/execute-phase.md:1676` and `plan-phase.md`) into upstream's rewritten `gsd-core/workflows/transition.md`, which currently has ZERO commit steps and ZERO gates despite seven mutating steps — the exact false-clean-WC pattern that bit Phase 14.

HIGHEST PRIORITY in this phase per v1.4 Pitfall 2. Re-scope delta: this is not a reorder — upstream's 694-line rewrite has no commit steps at all, so the fix ADDS the missing `gsd_run query commit` fences after each mutating step, plus the two `config-set workflow._auto_chain_active` sites inside `offer_next_phase` (found by RESEARCH: `config-set` writes `.planning/config.json` without committing, so a gate placed before `offer_next_phase` would pass and the banner would still print over a dirty WC on real `true → false` flips).

Purpose: a transition can never declare "Phase {X} marked complete" / milestone-complete over an uncommitted working copy.
Output: modified `gsd-core/workflows/transition.md` only. Closes todo `v14-transition-md-update-gap`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-RESEARCH.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md
@.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-VALIDATION.md
@.planning/todos/pending/v14-transition-md-update-gap.md
@gsd-core/workflows/transition.md
@gsd-core/workflows/execute-phase.md
</context>

## Artifacts this phase plan produces

No new files. All edits are to the existing `gsd-core/workflows/transition.md`. New symbols created:

- New workflow step: `<step name="assert_clean_wc">` in `transition.md` (between `update_session_continuity_after_transition` and `offer_next_phase`)
- 5 new `gsd_run query commit` fence lines in `transition.md` with messages:
  - `docs(phase-${completed_phase}): complete phase via transition`
  - `docs(phase-${completed_phase}): evolve PROJECT.md after transition`
  - `docs(phase-${completed_phase}): update STATE.md after transition`
  - `chore: clear auto-advance chain flag` (×2, tolerant `|| true`, Routes B1 and B)
- New FATAL string: `FATAL: working copy is dirty before transition completion.`
- The SC1 fixture gate script is EPHEMERAL (run-and-discard under mktemp, never written into the working tree per `feedback_avoid_jj_auto_tracked_output`)

## Stale-criterion note (path re-scope wins)

The source todo's grep criterion `grep -c "gsd-sdk query diff --name-only" ./get-shit-done/workflows/transition.md` predates the Phase 19 restructure (gsd-sdk retired per ADR-0174; `get-shit-done/` → `gsd-core/`). Per the re-scope audit, the modern equivalent is `grep -c "gsd_run query diff --name-only" gsd-core/workflows/transition.md` ≥ 1 (ROADMAP SC1 form). Record this supersession in the SUMMARY.

<tasks>

<task type="auto">
  <name>Task 1: Graft commit-adjacency after every mutating step in transition.md</name>
  <files>gsd-core/workflows/transition.md</files>
  <read_first>
    - gsd-core/workflows/transition.md (the full file — verify the RESEARCH line anchors L166-167, L188-273, L379-397, L552, L606 still hold; if drifted, relocate by step name, not line number)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md (§transition.md — exact commit messages, --files lists, tolerant-commit form, load-bearing-order prose shape)
    - gsd-core/workflows/execute-phase.md (lines ~1569-1680 — the surviving commit-adjacency pattern: phase.complete at 1572-1577, close_phase_todos tolerant commit at 1645, PROJECT.md commit at 1670)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-RESEARCH.md (§CLEANUP-01 — mutating-step inventory table, config-set commit gap finding)
  </read_first>
  <action>
    Add 5 commit fences to gsd-core/workflows/transition.md, mirroring the execute-phase.md commit-adjacency pattern. ALL new fences use bare `gsd_run` — the launcher is already embedded at transition.md:166 and later fences (L304, L413, L432, L552, L606) already use bare `gsd_run`. NEVER add a second launcher embed (it would add a raw `git rev-parse` hit and regress the audit-workflow-raw-git frozen baseline of transition.md = 1). Bash fences in this file use TABS — match byte-style.

    1. In step `update_roadmap_and_state`: immediately after the `TRANSITION=$(gsd_run query phase.complete "${current_phase}")` line (~L167), add a commit fence line: `gsd_run query commit "docs(phase-${completed_phase}): complete phase via transition" --files .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md`. Follow with the load-bearing-order prose (PATTERNS.md shape): the commit MUST run immediately after `phase.complete` because the mutating verb writes to disk but does NOT commit; do not defer the commit past the result-parsing prose.

    2. At the end of step `evolve_project` (L188-273): add `gsd_run query commit "docs(phase-${completed_phase}): evolve PROJECT.md after transition" --files .planning/PROJECT.md` (precedent: execute-phase.md:1670).

    3. At the end of step `update_session_continuity_after_transition` (closes ~L397): add ONE sweep commit covering the four consecutive STATE.md-only steps (update_current_position_after_transition, update_project_reference, review_accumulated_context, update_session_continuity_after_transition) plus the graduation backlog written earlier by graduation_scan: `gsd_run query commit "docs(phase-${completed_phase}): update STATE.md after transition" --files .planning/STATE.md`. Add a one-line prose note that this single commit deliberately sweeps the whole STATE.md cluster (RESEARCH A1: the requirement's "STATE.md update" is one logical mutating step).

    4. Immediately after BOTH `gsd_run query config-set workflow._auto_chain_active false` call sites (Route B1 ~L552 and Route B ~L606), add the tolerant commit: `gsd_run query commit "chore: clear auto-advance chain flag" --files .planning/config.json || true`. The `|| true` mirrors execute-phase.md:1645 — when the flag was already `false` the rewrite is byte-identical, jj sees no change, and the commit is a harmless no-op. This closes the config-set commit gap: `gsd-tools query config-set` writes `.planning/config.json` via platformWriteSync WITHOUT committing (gsd-core/bin/lib/config.cjs:379-426), and these two sites run AFTER the Task 2 gate.

    Do NOT add `workspace.parallel.*` literals anywhere (would create a parallel-call-presence pairing obligation). Do NOT touch any other workflow file.
  </action>
  <verify>
    <automated>grep -v '^#' gsd-core/workflows/transition.md | grep -c 'gsd_run query commit' | grep -qx '5' && node scripts/audit-workflow-raw-git.cjs && node scripts/lint-vcs-parallel-call-presence.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gsd_run query commit' gsd-core/workflows/transition.md` returns exactly 5
    - `grep -c 'chore: clear auto-advance chain flag' gsd-core/workflows/transition.md` returns 2 (both config-set sites covered)
    - `grep -c 'git rev-parse --show-toplevel' gsd-core/workflows/transition.md` returns exactly 1 (no second launcher embed)
    - `node scripts/audit-workflow-raw-git.cjs` exits 0 (frozen baseline intact, transition.md = 1)
    - `node scripts/lint-vcs-parallel-call-presence.cjs` exits 0 (no new pairing obligation)
  </acceptance_criteria>
  <done>All 5 mutation surfaces in transition.md (phase.complete, PROJECT.md evolution, STATE.md cluster sweep, 2× config-set) have an immediate adjacent `gsd_run query commit` fence; audit baseline unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Insert assert_clean_wc final gate + fixture-verify it halts on dirty WC</name>
  <files>gsd-core/workflows/transition.md</files>
  <read_first>
    - gsd-core/workflows/transition.md (post-Task-1 state; locate the `</step>` closing `update_session_continuity_after_transition` and `<step name="offer_next_phase">` — PATTERNS.md pins these at L397/L399 pre-edit)
    - gsd-core/workflows/execute-phase.md (lines 1676-1719 — the `<step name="assert_clean_wc">` source: gate fence, categorized PLANNING_DIRTY/OTHER_DIRTY split, remediation list, "Why unconditional" + "Do not bypass" prose)
    - gsd-core/workflows/plan-phase.md (its assert_clean_wc step ~L1764-1804 — the precedent for re-deriving the preamble case list per workflow)
    - .planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-PATTERNS.md (§transition.md — verbatim gate fence text with the 3 reworded strings, preamble shape, fixture-repo seeding shape)
  </read_first>
  <action>
    1. Insert a new `<step name="assert_clean_wc">` into gsd-core/workflows/transition.md between the `</step>` that closes `update_session_continuity_after_transition` and `<step name="offer_next_phase">` (pre-edit anchors: L397/L399). Copy the gate fence from execute-phase.md:1686-1708 byte-style (TABS inside the fence), keeping `DIRTY=$(gsd_run query diff --name-only 2>/dev/null | jq -r '.nameOnly // [] | join("\n")')`, the PLANNING_DIRTY/OTHER_DIRTY grep categorization, diagnostics, remediation list, and `exit 1`. Reword exactly 3 strings for transition: `FATAL: working copy is dirty before transition completion.`; `Phase completion requires` → `Transition completion requires`; `re-run phase execution` → `re-run the transition`. Bare `gsd_run` — no launcher embed.

    2. Write the step preamble per the plan-phase.md re-wording precedent: enumerate THIS workflow's mutating steps and their now-present commits (phase.complete committed after L167; PROJECT.md evolution committed at end of evolve_project; STATE.md cluster swept by one commit after update_session_continuity_after_transition), and note explicitly that the only post-gate writes are the Route B1/B config-sets which carry their own tolerant commits — this documents the deliberate deviation from a literal "immediately before each of the 5 banner variants" placement (RESEARCH Open Q2 resolution: single gate step + committed config-sets beats 5 duplicated per-route gates). Also copy the closing "Why unconditional and not just .planning/" and "Do not bypass" prose blocks from execute-phase.md:1711-1718, reworded for transition.

    3. Fixture-verify SC1 ("synthetic uncommitted file halts the flow") — EPHEMERAL, never in this repo's working tree (jj auto-snapshot hazard): create a mktemp dir; `jj git init --colocate` + `jj config set --repo user.email/user.name` (seedJjRepo shape from tests/scripts/dogfood-restore-orphan-cleanup.test.cjs); extract the new gate fence body into a temp script under the mktemp dir with `gsd_run() { node <ABS_REPO>/gsd-core/bin/gsd-tools.cjs "$@"; }` defined at the top; (a) create an uncommitted file in the fixture, run the script with cwd=fixture → MUST exit 1 with `FATAL` on stderr; (b) commit the file in the fixture (raw jj is fine inside the throwaway fixture — matches the seedJjRepo test precedent), re-run → MUST exit 0. Delete the mktemp dir. Record both transcripts (exit codes + FATAL line) in the SUMMARY — stdout evidence only, no files in the tree.

    4. Document the global-install caveat in the SUMMARY (and plan completion output): `~/.claude/gsd-core/workflows/transition.md` carries the pre-fix text until the operator runs `node bin/install.js --claude --global` from this clone — NEVER the upstream npx installer (fork rule `feedback_fork_install_path`).
  </action>
  <verify>
    <automated>grep -c '<step name="assert_clean_wc">' gsd-core/workflows/transition.md | grep -qx '1' && grep -q 'gsd_run query diff --name-only' gsd-core/workflows/transition.md && grep -q 'FATAL: working copy is dirty before transition completion' gsd-core/workflows/transition.md && node scripts/audit-workflow-raw-git.cjs && node scripts/lint-vcs-no-raw-git.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'gsd_run query diff --name-only' gsd-core/workflows/transition.md` ≥ 1 (ROADMAP SC1 grep, superseding the todo's stale gsd-sdk form)
    - `grep -c '<step name="assert_clean_wc">' gsd-core/workflows/transition.md` returns exactly 1, located before `<step name="offer_next_phase">`
    - Fixture run (a): dirty WC → gate script exits 1, stderr contains `FATAL: working copy is dirty before transition completion.`
    - Fixture run (b): clean WC → gate script exits 0
    - `node scripts/audit-workflow-raw-git.cjs` exits 0 AND `grep -c 'git rev-parse --show-toplevel' gsd-core/workflows/transition.md` returns 1 (baseline intact)
    - `node scripts/lint-vcs-no-raw-git.cjs` exits 0
    - SUMMARY documents the global-install caveat (`node bin/install.js --claude --global`) and the fixture transcripts
  </acceptance_criteria>
  <done>transition.md halts with FATAL before any terminal banner when the WC is dirty (fixture-proven both polarities); all lint/audit gates green; operator reinstall caveat recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow → VCS history | the orchestrator declares terminal state ("Phase {X} marked complete") based on assumed-committed planning mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Repudiation | transition.md terminal banners | mitigate | this plan: commit-adjacency after every mutating step + unconditional assert_clean_wc gate before offer_next_phase — the banner can no longer assert a committed state that does not exist (Phase 14 false-clean-WC recurrence site) |
| T-18-05 | Tampering | transition.md gate fence (audit baseline) | mitigate | bare `gsd_run` only; no second launcher embed; audit-workflow-raw-git frozen baseline (transition.md = 1) asserted in both tasks |

No package installs in this plan (Package Legitimacy Gate: not applicable per 18-RESEARCH.md).
</threat_model>

<verification>
- `grep -c 'gsd_run query commit' gsd-core/workflows/transition.md` = 5; `grep -c '<step name="assert_clean_wc">'` = 1
- `node scripts/audit-workflow-raw-git.cjs` exit 0; `node scripts/lint-vcs-no-raw-git.cjs` exit 0; `node scripts/lint-vcs-parallel-call-presence.cjs` exit 0
- Ephemeral fixture: dirty → exit 1 + FATAL; clean → exit 0
</verification>

<success_criteria>
ROADMAP SC1 satisfied: every mutating transition.md step has an immediate `gsd_run query commit`; `assert_clean_wc` gate lands before the terminal banners and demonstrably halts on a synthetic uncommitted file in a fixture repo; the global-install caveat is documented; all four lint/audit gates stay green at baseline.
</success_criteria>

<output>
Create `.planning/phases/18-tactical-cleanup-test-flake-re-scoped/18-01-SUMMARY.md` when done. Include: fixture-verification transcripts (both polarities), the stale-grep-criterion supersession note, and the operator global-install caveat.
</output>
