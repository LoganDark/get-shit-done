---
phase: 03.1-make-tests-run-faster
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - .planning/intel/vitest-integration-baseline.md
  - .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Final post-fix median-of-3 measurement recorded as `## final` section in `.planning/intel/vitest-integration-baseline.md` (D-08 protocol used for symmetry with baseline)"
    - "3× back-to-back full integration run verified all pass — `success: true` in all three runs (D-05c flakiness gate explicit-final-run form)"
    - "Test-count parity asserted between `## baseline` and `## final` (D-05a)"
    - "Final ratio (`baseline median / final median`) computed and recorded in both the markdown AND the phase SUMMARY (D-03)"
    - "Phase SUMMARY at `.planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md` exists with sections: baseline, levers attempted, final ratio, deferred levers"
    - "ROADMAP.md Phase 03.1 goal line updated from the current substantive-but-prospective goal to a final-state goal that embeds the measured ratio + the actual lever outcome"
    - "No `.skip()`, `.only()`, `.todo()` introduced in any integration file across all 4 plans (D-05b cumulative check)"
    - "No changes to scripts/run-tests.cjs, tests/helpers.cjs, .github/workflows/test.yml, cjs `tests/*.test.cjs`, vitest unit project, or backend matrix (D-07 cumulative check)"
    - "No adapter code passes `--ignore-working-copy` anywhere in the harness chain (Phase 3 D-05)"
  artifacts:
    - path: ".planning/intel/vitest-integration-baseline.md"
      provides: "Final state of the baseline data file with `## final` section appended"
      contains: "## final"
    - path: ".planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md"
      provides: "Phase summary — what shipped, what was tried, final ratio"
      contains: "Final ratio:"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 03.1 goal line updated with final ratio"
      contains: "Phase 03.1"
  key_links:
    - from: ".planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md"
      to: ".planning/intel/vitest-integration-baseline.md"
      via: "ratio computation citing the baseline + final tables"
      pattern: "vitest-integration-baseline\\.md"
    - from: ".planning/ROADMAP.md Phase 03.1"
      to: ".planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md"
      via: "goal line cites final ratio sourced from SUMMARY"
      pattern: "Phase 03.1"
---

<objective>
Close Phase 03.1: run the final 3× back-to-back verification, compute the pre/post ratio, write the phase SUMMARY, and update ROADMAP.md.

Purpose: D-05c demands the final flakiness gate be 3× back-to-back independent runs (NOT in-process `--retry`). D-03 demands the final ratio be the recorded output of the phase. ROADMAP.md currently has a substantive-but-prospective goal for Phase 03.1 (set by the initial planner; describes WHAT the phase will do but says "Final ratio is recorded in this line after Plan 04 lands") — this plan replaces it with the final-state goal that embeds the measured ratio + lever outcome.

Output: `## final` section appended to the baseline markdown, `03.1-SUMMARY.md` written, ROADMAP.md updated. No code or config changes — this plan only measures, records, and reports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-make-tests-run-faster/03.1-CONTEXT.md
@.planning/phases/03.1-make-tests-run-faster/03.1-RESEARCH.md
@.planning/phases/03.1-make-tests-run-faster/03.1-PATTERNS.md
@.planning/phases/03.1-make-tests-run-faster/03.1-01-SUMMARY.md
@.planning/phases/03.1-make-tests-run-faster/03.1-02-SUMMARY.md
@.planning/phases/03.1-make-tests-run-faster/03.1-03-SUMMARY.md
@.planning/intel/vitest-integration-baseline.md
@sdk/vitest.config.ts
@sdk/scripts/profile-integration.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final median-of-3 measurement (the `## final` section)</name>
  <files>.planning/intel/vitest-integration-baseline.md</files>
  <read_first>
    - sdk/scripts/profile-integration.mjs (the harness)
    - .planning/intel/vitest-integration-baseline.md (has `## baseline` + lever sections from Plans 02/03)
    - sdk/vitest.config.ts (the final config state after Plans 02/03's keep-or-revert decisions)
    - .planning/phases/03.1-make-tests-run-faster/03.1-CONTEXT.md (D-05c: 3× back-to-back independent runs; D-08 baseline protocol applies symmetrically)
  </read_first>
  <action>
    Run the harness one more time to capture the final post-all-levers state.

    ```bash
    cd /Users/LoganDark/Documents/Projects/get-shit-done
    node sdk/scripts/profile-integration.mjs --label final --append
    ```

    The harness internally does the median-of-3 with the flakiness gate (D-05c is satisfied by the harness aborting on `success: false` in any of the 3 runs). The same 3 runs that produce the median also serve as the "3× back-to-back independent runs all pass" D-05c verification — there is no separate run needed.

    **Verify in the appended `## final` section:**
    1. `numTotalTests` equals the value in `## baseline` (D-05a literal match on the integer)
    2. `numPendingTests` is `<=` the value in `## baseline` (skip count must not increase)
    3. All 7 integration test files appear in the per-file table
    4. The `Median total wall-clock (ms)` cell has a non-zero value

    If any of those four fail, DO NOT proceed to Task 2 — the phase cannot close in this state. Surface to the user.

    **Do NOT pass `--ignore-working-copy` to anything.** Do NOT introduce any new flags. The harness invocation is verbatim what Plan 01 used.
  </action>
  <verify>
    <automated>
      cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; \
      grep -q "^## final" .planning/intel/vitest-integration-baseline.md &amp;&amp; \
      [ "$(grep -c "^## " .planning/intel/vitest-integration-baseline.md)" -ge "3" ] &amp;&amp; \
      grep -q "init-e2e.integration.test.ts" .planning/intel/vitest-integration-baseline.md &amp;&amp; \
      grep -q "golden.integration.test.ts" .planning/intel/vitest-integration-baseline.md &amp;&amp; \
      grep -q "sub-repos-root.integration.test.ts" .planning/intel/vitest-integration-baseline.md &amp;&amp; \
      node -e "const fs=require('fs');const dir='sdk/scripts/.tmp/profile-integration';for(const i of [1,2,3]){const p=dir+'/run-'+i+'.json';const j=JSON.parse(fs.readFileSync(p,'utf-8'));if(j.success!==true){console.error('run '+i+' had success!==true');process.exit(1)}}console.log('3/3 final runs reported success: true')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/vitest-integration-baseline.md` contains a `## final` section heading (verified by grep)
    - File has at least 3 `## ` headings (baseline + at-least-one-lever-section + final; verified by `grep -c "^## " >= 3`)
    - All 7 integration test filenames are present in the file (verified by grep of representative names)
    - Harness reported `ok profile-integration:` on stdout (3-of-3 runs succeeded)
    - `numTotalTests` in `## final` equals `numTotalTests` in `## baseline` (verified by human inspection of the two headline-numbers tables; numeric equality required)
  </acceptance_criteria>
  <done>
    Final measurement landed. Ratio computation in Task 2 has both endpoints (baseline + final).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 03.1-SUMMARY.md with final ratio + lever record</name>
  <files>.planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md</files>
  <read_first>
    - .planning/intel/vitest-integration-baseline.md (read all sections: baseline, lever sections, final)
    - .planning/phases/03.1-make-tests-run-faster/03.1-01-SUMMARY.md (Plan 01 outcomes)
    - .planning/phases/03.1-make-tests-run-faster/03.1-02-SUMMARY.md (L1 decision)
    - .planning/phases/03.1-make-tests-run-faster/03.1-03-SUMMARY.md (L2 decision)
    - .planning/phases/03.1-make-tests-run-faster/03.1-CONTEXT.md (D-03 ratio recording requirement; deferred ideas list)
    - .planning/phases/03.1-make-tests-run-faster/03.1-RESEARCH.md (L3, L4, L5 verdicts — these go in the "Considered, Not Applied" section)
  </read_first>
  <action>
    Create `.planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md` with the phase outcome. Use this exact structure:

    ```markdown
    # Phase 03.1 Summary — make tests run faster

    **Completed:** {today's date in YYYY-MM-DD}
    **Goal achieved:** Largest semantics-preserving wall-clock reduction on `sdk/` vitest integration project (7 files); ratio recorded against pre-fix baseline.

    ## Final Ratio

    **Pre-fix baseline median total:** {N} ms (from `.planning/intel/vitest-integration-baseline.md` `## baseline`)
    **Post-fix final median total:** {M} ms (from `## final`)
    **Final ratio:** {N/M}× faster (D-03)

    Per-file biggest winners (largest improvement ratios):
    1. {file} — baseline {x} ms → final {y} ms ({x/y}×)
    2. {file} — baseline {x} ms → final {y} ms ({x/y}×)

    Per-file unchanged or regressions (if any):
    - {file} — {comment}

    ## Semantics Preserved (D-05 checks)

    - D-05a test-count parity: baseline `numTotalTests` = {X} = final `numTotalTests` ✓
    - D-05a skip-count: baseline `numPendingTests` = {Y} ≥ final `numPendingTests` = {Y'} ✓
    - D-05b assertion fidelity: 0 new `.skip` / `.only` / `.todo` in any integration test file across all 4 plans ✓
    - D-05c flakiness: 3-of-3 final runs all reported `success: true` ✓
    - D-07 out-of-scope: `scripts/run-tests.cjs`, `tests/helpers.cjs`, `.github/workflows/test.yml`, cjs `tests/*.test.cjs` suite, vitest unit project, backend matrix — all unmodified ✓

    ## Levers Applied (Kept)

    {one bullet per kept lever, with measured ratio against its prior state — pull from `.planning/intel/vitest-integration-baseline.md` decision blocks}

    - **L1 `pool: 'threads'`** — {KEPT|REVERTED}; {ratio vs baseline}; rationale: {one line}
    - **L2 `isolate: false`** — {KEPT|REVERTED|ABORTED-PRE-FLIP}; {ratio if measured, or audit-finding summary if aborted}; rationale: {one line}

    ## Levers Considered, Not Applied

    Per RESEARCH §"Standard Stack" + lever audits:

    - **L3 — Per-test fixture refactor:** N/A. The 7 integration files do not use the `vcsTest` / `makeBackendFixture` pattern (RESEARCH grep verified). Only describe-level heavy work is `mkdtemp` + `git init` + `InitRunner.run()` in `init-e2e` / `lifecycle-e2e`, which is bound by Claude CLI latency, not by fixture pattern. Not refactorable without dropping the E2E nature of the tests.
    - **L4 — In-process gsd-tools dispatch:** Bounded headroom. `GSDTools` defaults `preferNativeQuery: true` (`sdk/src/gsd-tools.ts:68`), so 4 of 5 non-golden files already route through `createRegistry().dispatch()`. The remaining subprocess spawns are inside `captureGsdToolsOutput` (golden + read-only-parity files only, both of which MUST keep spawning — they ARE the SDK-vs-subprocess parity assertion). Refactoring `gsd-tools.cjs` for re-entrancy (touches `gsd-tools.cjs` + `core.cjs` + every command that calls `error()`) — cost estimated >> payoff per RESEARCH §L4 Audit. Not applied this phase.
    - **L5 — `test.concurrent` / `describe.concurrent`:** Low likelihood. Most integration files have 1-3 `it` per `describe`. The exception (`golden.integration.test.ts` with ~50 `it`s) is L4-ineligible (subprocess parity) and concurrent CJS spawns against the same `REPO_ROOT` would race on mutation commands. Bounded win; not applied.

    These levers remain available for a future perf phase if a different bottleneck dominates.

    ## Out-of-Scope Items (Deferred — per CONTEXT D-07)

    Items the user explicitly carried out of Phase 03.1:

    - `tests/*.test.cjs` cjs node:test suite spawn-cost work (455 files; the val.town spawn-cost analysis remains a valid future-phase trigger)
    - `sdk/` vitest unit project speedup (144 files; harness from Plan 01 is reusable)
    - Backend matrix cost (Phase 4 prep — fixture-template `cp -r` trick is a candidate)
    - CI runner tuning / GitHub Actions cost optimization (local-only baseline this phase)
    - `pretest: pnpm run build:sdk` incremental skip (rebuild cost is a fixed slice of every `pnpm test` invocation but D-07 deferred it)

    ## Decisions Referenced

    - D-01: target = sdk/ vitest integration project only ✓
    - D-02: profile + fix in same phase ✓
    - D-03: largest improvement without breaking semantics (no fixed ratio target) ✓
    - D-04: local M-series baseline only ✓
    - D-05: semantics gates all satisfied ✓
    - D-06: lever menu used (L1/L2 applied per evidence; L3/L4/L5 listed not-applied with rationale) ✓
    - D-07: explicit non-goals respected ✓
    - D-08: median-of-3 protocol for both baseline and final ✓
    - D-09: every lever flip tied to baseline observation; reverts on no-improvement applied ✓

    ## Artifacts

    - `sdk/scripts/profile-integration.mjs` — re-runnable harness; reusable for unit-project perf work
    - `.planning/intel/vitest-integration-baseline.md` — full measurement record (baseline, per-lever, final)
    - `sdk/vitest.config.ts` — config in final state (knob diff: {summary of net diff vs pre-Plan-02 state})

    ## Reusable for Future Phases

    The harness shape — median-of-3 via `--reporter=json`, markdown emitter, diagnostic-exit on flakiness — is project-pattern material. A future "make unit tests run faster" phase can:
    1. Reuse `sdk/scripts/profile-integration.mjs` with `--project unit` substitution (could be made a CLI flag)
    2. Reuse the `.planning/intel/vitest-*-baseline.md` shape
    3. Reuse the per-lever decision-record protocol (D-09 evidence-tied flips)
    ```

    Fill in all `{...}` placeholders with actual numbers from `.planning/intel/vitest-integration-baseline.md`. Numbers must be sourced from the markdown's headline-numbers tables, not invented.
  </action>
  <verify>
    <automated>
      cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; \
      test -f .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "^# Phase 03.1 Summary" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "Final ratio:" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "^## Semantics Preserved" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "^## Levers Applied" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "^## Levers Considered, Not Applied" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "D-05a test-count parity" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "D-05b assertion fidelity" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "D-05c flakiness" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "D-07 out-of-scope" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md &amp;&amp; \
      grep -q "L4 — In-process gsd-tools dispatch" .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md` exists
    - Title is `# Phase 03.1 Summary — make tests run faster` (verified by grep)
    - Required sections all present: `## Final Ratio`, `## Semantics Preserved (D-05 checks)`, `## Levers Applied (Kept)`, `## Levers Considered, Not Applied`, `## Out-of-Scope Items (Deferred — per CONTEXT D-07)`, `## Decisions Referenced`, `## Artifacts`, `## Reusable for Future Phases` (verified by 8 separate greps)
    - Each D-05 check (a/b/c) explicitly listed with ✓ or with a documented exception (verified by grep of `D-05a`, `D-05b`, `D-05c`)
    - All 3 not-applied levers (L3, L4, L5) explicitly documented (verified by grep of each)
    - `Final ratio:` line is present with a numeric ratio (e.g., `1.4×` or `2.3×`) — verified by grep
    - No `.skip(` / `.only(` / `.todo(` introduced anywhere in integration test files across all 4 plans (final D-05b cumulative check; verified by `grep -rE '\.(skip|only|todo)\(' sdk/src/*.integration.test.ts sdk/src/**/*.integration.test.ts | wc -l` returning the same count as at start of Phase 03.1)
  </acceptance_criteria>
  <done>
    Phase SUMMARY is committable. ROADMAP.md update in Task 3 can cite the ratio from this file.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md Phase 03.1 goal line + commit phase close</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (current state — Phase 03.1 goal is `[Urgent work - to be planned]`)
    - .planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md (the ratio + lever-record source-of-truth)
  </read_first>
  <action>
    Replace the Phase 03.1 placeholder in `.planning/ROADMAP.md` with a one-liner citing the actual ratio. Then commit the whole phase-close as a single jj squash.

    **Step 1 — Update ROADMAP.md.** First, re-read the current `.planning/ROADMAP.md` to see the live state of the Phase 03.1 block — do NOT rely on the snapshot below if it has drifted. As of the last revision of this plan, the Phase 03.1 block (lines ~103-114) looks like:
    ```markdown
    ### Phase 03.1: make tests run faster (INSERTED)

    **Goal:** Profile the `sdk/` vitest integration project (7 files; `sdk/src/**/*.integration.test.ts`), then apply the largest wall-clock speedup achievable without breaking semantics. Baseline + fix ship in the same phase per CONTEXT D-02. Success = ratio recorded against local M-series median-of-3 baseline; D-05 semantics gates (test-count parity, no .skip/.only/.todo added, 3x back-to-back all-pass) must all hold. Final ratio is recorded in this line after Plan 04 lands.
    **Requirements**: None formally mapped (TEST-06 spirit applies — skip count must not increase, asserted manually from --reporter=json numPendingTests per CONTEXT D-05a)
    **Depends on:** Phase 3
    **Plans:** 4 plans

    Plans:
    - [ ] 03.1-01-PLAN.md — Baseline harness (sdk/scripts/profile-integration.mjs) + pre-fix median-of-3 baseline data committed to .planning/intel/vitest-integration-baseline.md
    - [ ] 03.1-02-PLAN.md — L1 lever attempt: pool: 'threads' on integration project (with measured keep-or-revert decision per D-09)
    - [ ] 03.1-03-PLAN.md — L2 lever attempt: isolate: false on integration project (with pre-flip transitive-import audit + shuffle-order Pitfall 3 check + keep-or-revert per D-09)
    - [ ] 03.1-04-PLAN.md — Final verification: 3x median-of-3, compute pre/post ratio, write 03.1-SUMMARY.md, update this ROADMAP line with the ratio
    ```

    **Transformation:** replace the prospective goal line (which describes what the phase will do + the placeholder "Final ratio is recorded in this line after Plan 04 lands") with a final-state goal that embeds the actual measured ratio and lever outcome. Also flip every `- [ ]` plan-row to `- [x]`. Also update `**Plans:** 4 plans` → `**Plans:** 4 plans (complete)`. Using actual numbers from `03.1-SUMMARY.md`:
    ```markdown
    ### Phase 03.1: make tests run faster (INSERTED)

    **Goal:** Profiled the `sdk/` vitest integration project (7 files) and applied largest semantics-preserving wall-clock reduction. Final ratio: {N/M}× ({N} ms baseline → {M} ms final, median-of-3, local M-series). Kept levers: {summary of L1/L2 KEPT vs REVERTED}. Deferred: L3/L4/L5 with rationale (see 03.1-SUMMARY.md); cjs `tests/` suite + vitest unit project speedup deferred to future phases per D-07. All D-05 semantics gates (test-count parity, no `.skip`/`.only`/`.todo` introduced, 3x back-to-back `success: true`) held throughout.
    **Requirements**: None formally mapped (TEST-06 spirit applies — skip count must not increase, asserted manually from --reporter=json numPendingTests per CONTEXT D-05a)
    **Depends on:** Phase 3
    **Plans:** 4 plans (complete)

    Plans:
    - [x] 03.1-01-PLAN.md — Baseline harness (sdk/scripts/profile-integration.mjs) + pre-fix median-of-3 baseline data committed to .planning/intel/vitest-integration-baseline.md
    - [x] 03.1-02-PLAN.md — L1 lever attempt: pool: 'threads' on integration project ({KEPT|REVERTED} per D-09)
    - [x] 03.1-03-PLAN.md — L2 lever attempt: isolate: false on integration project ({KEPT|REVERTED|ABORTED-PRE-FLIP} per D-09)
    - [x] 03.1-04-PLAN.md — Final verification: 3x median-of-3, computed pre/post ratio, wrote 03.1-SUMMARY.md, updated this ROADMAP line with the ratio
    ```

    Also update the bottom-of-file progress table to mark Phase 03.1 complete. Find the table around lines 143-148:
    ```markdown
    | Phase | Plans Complete | Status | Completed |
    |-------|----------------|--------|-----------|
    | 1. Adapter Foundation + Git Backend | 5/5 | Complete | 2026-05-09 |
    | 2. Bulk Call-Site Migration (Still Git-Only) | 12/12 | Plans Complete (ready for phase-level verifier) |  |
    | 3. jj Backend Core — Squash, Refs, Conflict | 7/7 | Complete | 2026-05-12 |
    | 4. Workspaces + Octopus Structure + Hooks | 0/TBD | Not started | - |
    | 5. Command Translations + Brownfield Validation + CI Hardening | 0/TBD | Not started | - |
    ```

    Insert a new row for Phase 03.1 in numeric order (between Phase 3 and Phase 4):
    ```markdown
    | 03.1. make tests run faster | 4/4 | Complete | {today's date YYYY-MM-DD} |
    ```

    Also update the trailing `*Last updated:* ...` line to include Phase 03.1's outcome. Add a new line at the very bottom of the file:
    ```markdown
    *Last updated: {today's date} — Phase 03.1 plan execution complete (4/4). Vitest integration project median wall-clock reduced {N/M}× (from {N}ms baseline to {M}ms final, median-of-3 on local M-series). Levers landed: {summary}. Harness `sdk/scripts/profile-integration.mjs` reusable for future unit-project perf work. Test-count parity + skip-count + flakiness gates all green (D-05a/b/c).*
    ```
    (Do NOT remove existing `*Last updated: …*` trailer lines — they form a chronological footer per Phase 3's pattern. Just add the new one below them.)

    **Step 2 — Commit the whole phase close via jj.**
    Files in this commit: `.planning/intel/vitest-integration-baseline.md` (Task 1 appended `## final`), `.planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md` (Task 2 created), `.planning/ROADMAP.md` (this task updated). Commit message:
    ```
    feat(03.1): close phase — vitest integration suite {N/M}× faster

    Plan 03.1-04: final median-of-3 measurement, phase SUMMARY, ROADMAP.md
    Phase 03.1 goal-line replacement. Levers landed: {summary}. All D-05
    semantics gates green; D-07 out-of-scope files unmodified.
    ```
    Use `jj squash -B @ -k -m '<message>'` or whatever project-standard verb applies. Do NOT pass `--ignore-working-copy`.

    **Step 3 — Final invariant verification (informational; commit goes through regardless of these as long as the prior plans' commits are intact):**
    Run a single sanity grep from repo root to confirm no D-07 violations slipped in:
    ```bash
    # Confirm no untracked changes to D-07 files exist in working tree:
    jj diff -r @ --name-only 2>/dev/null | grep -E '^(scripts/run-tests\.cjs|tests/helpers\.cjs|\.github/workflows/test\.yml|tests/.*\.test\.cjs)$' &amp;&amp; echo "D-07 VIOLATION: out-of-scope file modified in this commit" || echo "D-07 ok"
    ```
    (This is informational — the actual gating happened at each prior plan's commit boundary. If this surfaces a violation now it means a prior plan slipped past its own gate, which is a verifier-phase issue.)
  </action>
  <verify>
    <automated>
      cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; \
      grep -q "Phase 03.1: make tests run faster" .planning/ROADMAP.md &amp;&amp; \
      grep -q "Final ratio:" .planning/ROADMAP.md &amp;&amp; \
      ! grep -q "Final ratio is recorded in this line after Plan 04 lands" .planning/ROADMAP.md &amp;&amp; \
      grep -q "\*\*Plans:\*\* 4 plans (complete)" .planning/ROADMAP.md &amp;&amp; \
      grep -q -- "- \[x\] 03.1-01-PLAN.md" .planning/ROADMAP.md &amp;&amp; \
      grep -q -- "- \[x\] 03.1-02-PLAN.md" .planning/ROADMAP.md &amp;&amp; \
      grep -q -- "- \[x\] 03.1-03-PLAN.md" .planning/ROADMAP.md &amp;&amp; \
      grep -q -- "- \[x\] 03.1-04-PLAN.md" .planning/ROADMAP.md &amp;&amp; \
      grep -q "| 03.1. make tests run faster | 4/4 | Complete" .planning/ROADMAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` no longer contains the placeholder phrase `Final ratio is recorded in this line after Plan 04 lands` (verified by negative grep — confirms the prospective goal line was replaced with the final-state form)
    - The Phase 03.1 section in ROADMAP.md mentions `Final ratio:` with a numeric value (verified by grep)
    - All 4 plan filenames (`03.1-01-PLAN.md` through `03.1-04-PLAN.md`) are listed under Phase 03.1 with `- [x]` (verified by 4 separate greps)
    - Progress table contains the row `| 03.1. make tests run faster | 4/4 | Complete | YYYY-MM-DD |` (verified by literal grep)
    - Phases 1, 2, 3, 4, 5 rows are still present in the progress table (verified by greps of representative substrings)
    - Commit uses jj (no raw git per CLAUDE.md)
    - Working-tree diff at the moment of commit touches ONLY `.planning/intel/vitest-integration-baseline.md`, `.planning/phases/03.1-make-tests-run-faster/03.1-SUMMARY.md`, and `.planning/ROADMAP.md` — no scope-creep files (verified by `jj diff -r @ --name-only` inspection)
  </acceptance_criteria>
  <done>
    Phase 03.1 is closed in ROADMAP.md. Final ratio is the user-visible artifact in the project planning tree.
  </done>
</task>

</tasks>

<verification>
- `.planning/intel/vitest-integration-baseline.md` has at least `## baseline`, lever section(s) from Plans 02/03, and `## final`
- D-05 a/b/c all asserted in the SUMMARY with checkmarks (or documented exceptions)
- ROADMAP.md goal line replaced with the actual ratio; Phase 03.1 marked Complete in the progress table
- No D-07 out-of-scope files touched across the 4 plans (cumulative)
- No `--ignore-working-copy` anywhere (Phase 3 D-05 cumulative)
- Test-count parity: `numTotalTests` baseline = final; `numPendingTests` baseline ≥ final
</verification>

<success_criteria>
- The user can run `git log` (or `jj log`) and see the phase's full arc: baseline land → L1 attempt → L2 attempt → phase close with ratio
- A future planner planning a unit-project perf phase can: read `03.1-SUMMARY.md`, copy `sdk/scripts/profile-integration.mjs`, parameterize `--project`, and reuse the entire phase shape
- ROADMAP.md is self-explanatory: the Phase 03.1 line tells you what shipped and by how much, without needing to read the SUMMARY
</success_criteria>

<output>
After completion, this plan IS the phase summary (`03.1-SUMMARY.md` was the deliverable). No additional `03.1-04-SUMMARY.md` needed — the phase-level summary supersedes it. The phase is ready for the phase-level verifier (`/gsd-verify-phase 03.1` or equivalent).
</output>
