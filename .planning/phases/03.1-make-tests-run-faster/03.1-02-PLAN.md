---
phase: 03.1-make-tests-run-faster
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - sdk/vitest.config.ts
  - .planning/intel/vitest-integration-baseline.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "`sdk/vitest.config.ts` projects[1] (integration) has `pool: 'threads'` added; projects[0] (unit) untouched (D-01, Pitfall 6)"
    - "`.planning/intel/vitest-integration-baseline.md` has a new `## L1-threads` section with median-of-3 per-file numbers after the flip (D-08 protocol re-applied)"
    - "Post-flip `numTotalTests` equals pre-flip `numTotalTests`; `numPendingTests` did NOT increase (D-05a test-count parity)"
    - "Post-flip 3-of-3 runs all reported `success: true` (D-05c flakiness gate)"
    - "No `.skip()`, `.only()`, `.todo()` introduced in any integration test file; `testTimeout: 120_000` unchanged (D-05b)"
    - "L1 decision recorded in plan summary: kept (measured improvement > 1.0×) OR reverted (measured ≤ 1.0× per D-09 revert rule)"
    - "No changes to scripts/run-tests.cjs, tests/helpers.cjs, .github/workflows/test.yml, cjs `tests/*.test.cjs`, vitest unit project, or backend matrix (D-07)"
  artifacts:
    - path: "sdk/vitest.config.ts"
      provides: "Integration project with `pool: 'threads'` (if kept) or original shape (if reverted)"
      contains: "name: 'integration'"
    - path: ".planning/intel/vitest-integration-baseline.md"
      provides: "Appended `## L1-threads` section with post-flip numbers"
      contains: "## L1-threads"
  key_links:
    - from: "sdk/vitest.config.ts projects[1]"
      to: "vitest worker pool"
      via: "pool: 'threads'"
      pattern: "pool:\\s*'threads'"
---

<objective>
Apply lever L1 (`pool: 'threads'`) to the integration project in `sdk/vitest.config.ts` (and ONLY the integration project — unit project keeps default forks per D-01 + Pitfall 6), re-run the median-of-3 harness with `--label L1-threads --append`, and either keep the flip (measured ratio > 1.0× pre-L1) or revert it per D-09. The plan ships the smaller of the two configs that produces a stable pass.

Purpose: L1 is the cheapest evidence-tied lever. RESEARCH confirms zero `process.chdir` calls anywhere in `sdk/src/` or `get-shit-done/bin/` (verified by grep), so the only documented threads-pool constraint does not apply. The flip is a single-line config change with a known revert path.

Output: One config diff (single-line addition or revert-to-original) and one appended markdown section. The phase advances to L2 (Plan 03) only if Plan 01's baseline shows per-file module-init cost still dominates after L1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.1-make-tests-run-faster/03.1-CONTEXT.md
@.planning/phases/03.1-make-tests-run-faster/03.1-RESEARCH.md
@.planning/phases/03.1-make-tests-run-faster/03.1-PATTERNS.md
@.planning/phases/03.1-make-tests-run-faster/03.1-01-SUMMARY.md
@.planning/intel/vitest-integration-baseline.md
@sdk/vitest.config.ts
@sdk/scripts/profile-integration.mjs

<interfaces>
<!-- Vitest 3.x per-project config knobs (per RESEARCH §"Per-project pool override syntax"). -->
<!-- Pool values: 'threads' | 'forks' | 'vmThreads' | 'vmForks'. Default: 'forks'. -->
<!-- Threads pool forbids process.chdir() — already verified zero hits in sdk/src and get-shit-done/bin. -->
<!-- Per-project pool is supported because pool is NOT in the root-only set (coverage, reporters, resolveSnapshotPath). -->

Current `sdk/vitest.config.ts` shape (22 LOC, 2-space indent — preserve):
```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    projects: [
      {
        test: {
          name: 'unit',
          include: ['src/**/*.test.ts'],
          exclude: ['src/**/*.integration.test.ts'],
        },
      },
      {
        test: {
          name: 'integration',
          include: ['src/**/*.integration.test.ts'],
          testTimeout: 120_000,
        },
      },
    ],
  },
});
```

The L1 mutation adds exactly ONE line inside `projects[1].test`:
```ts
pool: 'threads',
```
Placed after `testTimeout: 120_000,`. Unit project (`projects[0]`) is NOT modified — this is the D-01 + Pitfall 6 invariant.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flip smoke test — verify per-project `pool` actually applies on this vitest version</name>
  <files></files>
  <read_first>
    - .planning/phases/03.1-make-tests-run-faster/03.1-RESEARCH.md (§"Open Questions" #2 — per-project `pool: 'threads'` is [ASSUMED] per A1; smoke-test catches it in &lt;1 min)
    - sdk/vitest.config.ts (current shape)
    - sdk/package.json (confirm `vitest: ^3.1.1` is installed)
  </read_first>
  <action>
    Confirm vitest 3.1.x honors per-project `pool` before committing the real config change. This is the 5-minute experiment RESEARCH Open Q #2 asks for. If per-project `pool` is silently ignored, this task surfaces it now (cheap to discover) rather than during baseline measurement.

    **Procedure:**
    1. Read `sdk/vitest.config.ts` to confirm the current shape matches the `<interfaces>` block above. If it doesn't (e.g., another change landed first), STOP and surface to the user.
    2. Create a SCRATCH copy at `sdk/vitest.config.ts.smoke-test` (do NOT modify the real config yet) with `pool: 'threads'` added to projects[1]. This file is throwaway; do not commit it.
    3. Run the integration project against the scratch config and write a sentinel file ONLY if the run reports `success: true`. The verify block (below) asserts the sentinel exists AND the JSON file shows `success: true`. Use this exact shell sequence (single command, jq fallback to node if jq unavailable):
       ```bash
       cd /Users/LoganDark/Documents/Projects/get-shit-done
       mkdir -p sdk/scripts/.tmp
       rm -f sdk/scripts/.tmp/smoke-L1.json sdk/scripts/.tmp/smoke-L1-passed
       pnpm --filter @gsd-build/sdk exec vitest run \
         --project integration \
         --config sdk/vitest.config.ts.smoke-test \
         --reporter=json --outputFile sdk/scripts/.tmp/smoke-L1.json || true
       # Write sentinel only if the JSON parses AND has success:true
       node -e "const fs=require('fs');const j=JSON.parse(fs.readFileSync('sdk/scripts/.tmp/smoke-L1.json','utf-8'));if(j.success===true){fs.writeFileSync('sdk/scripts/.tmp/smoke-L1-passed','ok\n')}else{process.exit(1)}"
       ```
       (If `--config` does not accept a relative path from repo root, use the path that works for the sdk package's exec context — typically `vitest.config.ts.smoke-test` since `pnpm --filter @gsd-build/sdk exec` runs with CWD at `sdk/`. The sentinel-write is structured so a vitest non-zero exit still allows the node check to read the JSON and decide.)
    4. Inspect the JSON: every `testResults[].name` should be a `*.integration.test.ts` path (Pitfall 1 contamination check) and the overall run should report `success: true`.
    5. **Threads-pool sanity:** If vitest 3.1.x silently ignored the per-project `pool`, there's no easy programmatic detector — the workaround per RESEARCH is to deliberately do something that ONLY fails under threads. The cheapest probe is to add a temporary `it('threads-pool probe', () => { expect(() =&gt; process.chdir(process.cwd())).toThrow() })` to one integration file, but this is invasive. **Skip the probe** — the smoke test's purpose is just to confirm the run completes and the project filter works; the per-project `pool` plumbing is sufficiently documented at vitest 3.x level that A1's risk is "off by 2-3× in predicted win size," not "silently broken."
    6. **After the verify block has run and passed** (Task 1's `<verify>` block asserts the sentinel + `success: true`), clean up: `rm -f sdk/vitest.config.ts.smoke-test sdk/scripts/.tmp/smoke-L1.json sdk/scripts/.tmp/smoke-L1-passed`. Do not commit any of these; the real measurement is Task 3. Cleanup must happen AFTER the automated verify so the verify can read the sentinel.

    **If the smoke test FAILS** (vitest errors out parsing the config, or the run reports `success: false` due to a config issue): STOP, surface to the user. Do NOT proceed to Task 2 — there is a config-shape problem that needs human triage before the real flip.

    **If the smoke test PASSES:** Proceed to Task 2. The real flip is now safe to land.
  </action>
  <verify>
    <automated>
      cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; \
      test -f sdk/scripts/.tmp/smoke-L1-passed &amp;&amp; \
      test -f sdk/scripts/.tmp/smoke-L1.json &amp;&amp; \
      node -e "const j=JSON.parse(require('fs').readFileSync('sdk/scripts/.tmp/smoke-L1.json','utf-8'));if(j.success!==true){process.exit(1)};for(const r of (j.testResults||[])){if(!/\.integration\.test\.ts$/.test(r.name)){console.error('non-integration file in results: '+r.name);process.exit(1)}}" &amp;&amp; \
      grep -q "name: 'integration'" sdk/vitest.config.ts &amp;&amp; \
      ! grep -q "pool:" sdk/vitest.config.ts &amp;&amp; \
      rm -f sdk/vitest.config.ts.smoke-test sdk/scripts/.tmp/smoke-L1.json sdk/scripts/.tmp/smoke-L1-passed &amp;&amp; \
      ! test -f sdk/vitest.config.ts.smoke-test
    </automated>
  </verify>
  <acceptance_criteria>
    - Sentinel file `sdk/scripts/.tmp/smoke-L1-passed` was created by the smoke step (verified by `test -f` in the automated block BEFORE cleanup)
    - JSON file `sdk/scripts/.tmp/smoke-L1.json` shows `success: true` AND every `testResults[].name` ends in `.integration.test.ts` (asserted automatically by the inline `node -e` script)
    - Real `sdk/vitest.config.ts` still has `name: 'integration'` (unchanged by this task — the scratch copy was where the experiment happened; verified by grep)
    - Real `sdk/vitest.config.ts` does NOT contain `pool:` (this task does NOT land the lever; Task 2 does; verified by negative grep)
    - Cleanup completes after the assertions run: `sdk/vitest.config.ts.smoke-test`, `sdk/scripts/.tmp/smoke-L1.json`, and `sdk/scripts/.tmp/smoke-L1-passed` all removed (final `! test -f` in the automated block)
  </acceptance_criteria>
  <done>
    Per-project `pool` is confirmed working in vitest 3.1.x against this config shape. Scratch files cleaned up. Real config untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply L1 to sdk/vitest.config.ts (add `pool: 'threads'` to projects[1] only)</name>
  <files>sdk/vitest.config.ts</files>
  <read_first>
    - sdk/vitest.config.ts (current file — must be the 22-LOC shape from the `<interfaces>` block)
    - .planning/phases/03.1-make-tests-run-faster/03.1-CONTEXT.md (D-01 unit project untouched; D-06 L1 description)
    - .planning/phases/03.1-make-tests-run-faster/03.1-PATTERNS.md (§"sdk/vitest.config.ts" — knob lands in projects[1].test, indentation = 2-space to match surrounding file)
    - .planning/phases/03.1-make-tests-run-faster/03.1-RESEARCH.md (Pitfall 6 — knob must NOT move to top-level)
  </read_first>
  <action>
    Edit `sdk/vitest.config.ts` using the Edit tool. The change is exactly one line addition.

    **Exact transformation** — find this block (lines ~13-18 in the current file):
    ```ts
          {
            test: {
              name: 'integration',
              include: ['src/**/*.integration.test.ts'],
              testTimeout: 120_000,
            },
          },
    ```
    Replace with:
    ```ts
          {
            test: {
              name: 'integration',
              include: ['src/**/*.integration.test.ts'],
              testTimeout: 120_000,
              // L1 (Phase 03.1): threads pool — verified zero process.chdir calls in sdk/src + get-shit-done/bin.
              // Unit project deliberately keeps default 'forks' (D-01 + Pitfall 6).
              pool: 'threads',
            },
          },
    ```

    **Indentation:** preserve the existing 2-space style of `sdk/vitest.config.ts` per PATTERNS.md ("preserve the existing 2-space style here to match the surrounding file. Do not retab"). The user's global "tabs only" rule does NOT apply to in-place edits of files whose style is already established — only to new files.

    **DO NOT** add `pool: 'threads'` to projects[0] (unit). DO NOT hoist `pool: 'threads'` to the top-level `test:` block (that would apply it to unit too, violating D-01 + Pitfall 6).

    **DO NOT** add `poolOptions` in this task. L1 is the bare `pool: 'threads'` flip. Tuning `poolOptions.threads.{singleThread, maxThreads, minThreads, isolate}` is out of scope for L1 — the default poolOptions are what RESEARCH measured. If post-L1 numbers underwhelm, that's a signal to plan L2 (Plan 03), not to tune poolOptions speculatively.

    **DO NOT** modify any other file in this task. The `.planning/intel/vitest-integration-baseline.md` update happens in Task 3 after measurement.
  </action>
  <verify>
    <automated>
      cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; \
      grep -qE "^\s*pool: 'threads',\s*$" sdk/vitest.config.ts &amp;&amp; \
      [ "$(grep -c -- "pool:" sdk/vitest.config.ts)" = "1" ] &amp;&amp; \
      grep -q "name: 'unit'" sdk/vitest.config.ts &amp;&amp; \
      grep -q "name: 'integration'" sdk/vitest.config.ts &amp;&amp; \
      ! grep -B2 "pool: 'threads'" sdk/vitest.config.ts | grep -q "name: 'unit'" &amp;&amp; \
      pnpm --filter @gsd-build/sdk exec vitest --version
    </automated>
  </verify>
  <acceptance_criteria>
    - `sdk/vitest.config.ts` contains exactly ONE `pool:` line (verified by `grep -c "pool:" = 1`)
    - That line is `pool: 'threads',` (verified by regex grep)
    - The `pool:` line is NOT preceded within 2 lines by `name: 'unit'` (verified by `grep -B2 "pool: 'threads'" | grep -q "name: 'unit'"` returning NO match — i.e., the lever is on the integration project, not the unit project)
    - Both `name: 'unit'` and `name: 'integration'` still appear in the file (config structure preserved; verified by grep)
    - `vitest --version` still runs (config parses successfully; verified by exec of `pnpm --filter @gsd-build/sdk exec vitest --version`)
    - No `poolOptions` added in this task (verified by `! grep -q poolOptions sdk/vitest.config.ts` — should be 0 hits)
  </acceptance_criteria>
  <done>
    L1 config change landed. Vitest still parses the config. Ready for measurement in Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run harness with --label L1-threads --append, then keep-or-revert per D-09</name>
  <files>.planning/intel/vitest-integration-baseline.md, sdk/vitest.config.ts</files>
  <read_first>
    - sdk/scripts/profile-integration.mjs (the harness — must accept `--append` and `--label`)
    - .planning/intel/vitest-integration-baseline.md (the file just after Plan 01 — has the pre-fix `## baseline` section)
    - .planning/phases/03.1-make-tests-run-faster/03.1-CONTEXT.md (D-05 semantics gate; D-09 revert rule)
    - .planning/phases/03.1-make-tests-run-faster/03.1-RESEARCH.md (§"L1 Audit"; §"Prior-Art Speedup Estimates")
  </read_first>
  <action>
    Execute the harness against the L1-flipped config and decide whether to keep or revert based on the measured outcome.

    **Step 1 — Run the harness:**
    ```bash
    cd /Users/LoganDark/Documents/Projects/get-shit-done
    node sdk/scripts/profile-integration.mjs --label L1-threads --append
    ```
    Expected behavior: appends a `## L1-threads` section to `.planning/intel/vitest-integration-baseline.md` with the same table shape as `## baseline`.

    **Step 2 — D-05a test-count parity check:**
    Parse the new `## L1-threads` headline-numbers table and the existing `## baseline` headline-numbers table from `.planning/intel/vitest-integration-baseline.md`. Assert:
    - `numTotalTests` matches between baseline and L1-threads (literal equality on the integer)
    - `numPendingTests` for L1-threads is `<=` `numPendingTests` for baseline (skip count must not increase)

    If either fails: this is a D-05a violation. The L1 flip changed test discovery. REVERT the config (Step 4 below) and surface the failure to the user. Do NOT commit the appended section in this case.

    **Step 3 — D-05b assertion-fidelity check:**
    The pre-phase invariant is that the 7 integration test files contain ZERO `.skip(` / `.only(` / `.todo(` calls (verified at the start of Phase 03.1 — this count is the floor; Phase 03.1 must not introduce any). Assert this still holds:
    ```bash
    cd /Users/LoganDark/Documents/Projects/get-shit-done
    COUNT=$(grep -hcE '\.(skip|only|todo)\(' sdk/src/**/*.integration.test.ts 2>/dev/null | awk '{s+=$1} END{print s+0}')
    if [ "$COUNT" -ne "0" ]; then
      echo "D-05b VIOLATION: $COUNT skip/only/todo call(s) in *.integration.test.ts (pre-phase invariant was zero)"
      # REVERT and surface — the executor or some other change introduced one.
      exit 1
    fi
    ```
    If the count is non-zero, REVERT the L1 config change and surface the violation. The pre-phase invariant (zero `.skip(`/`.only(`/`.todo(` in `sdk/src/**/*.integration.test.ts`) was verified at the start of Phase 03.1; this plan must not break it.

    **Step 4 — Keep-or-revert decision:**
    Compute the ratio: `medianTotal_baseline / medianTotal_L1threads`. (Pull both values from the `Median total wall-clock (ms)` headline row of each section.)
    - **If ratio &gt; 1.0× (post-L1 is faster):** KEEP the flip. Append a paragraph at the bottom of the `## L1-threads` section in `.planning/intel/vitest-integration-baseline.md` (use the Edit tool to add it after the per-file table):
      ```markdown
      ### L1 decision: KEPT

      - Baseline median total (from `## baseline`): {N} ms
      - L1-threads median total: {M} ms
      - Measured ratio: {N/M}× (improvement)
      - Test-count parity: PASS (numTotalTests = {X} in both; numPendingTests {baseline_skip} → {L1_skip})
      - 3-of-3 runs passed (D-05c flakiness gate satisfied for this measurement window)
      - Predicted improvement per RESEARCH: ~1.14× steady-state + ~1.05-1.2× cold-start aggregate for 7-file suite
      - Per-file biggest winners: {top-2 files by ratio of baseline/L1 per-file median}
      ```
    - **If ratio &lt;= 1.0× (no improvement or slower):** REVERT per D-09. Edit `sdk/vitest.config.ts` to remove the 3 added lines (the two comment lines and the `pool: 'threads',` line). Append a paragraph at the bottom of the `## L1-threads` section:
      ```markdown
      ### L1 decision: REVERTED

      - Baseline median total: {N} ms
      - L1-threads median total: {M} ms
      - Measured ratio: {N/M}× — does not justify keeping the flip per D-09 (no-improvement revert rule)
      - Config restored to pre-L1 state; subsequent plans target other levers
      ```

    **Step 5 — Commit:**
    Use `jj` (per CLAUDE.md). The committed files are `sdk/vitest.config.ts` (modified or reverted, depending on Step 4) and `.planning/intel/vitest-integration-baseline.md` (always appended). Commit message:
    - If kept: `feat(03.1): apply L1 (pool: 'threads') to integration project; measured {N/M}× improvement`
    - If reverted: `chore(03.1): probe L1 (pool: 'threads'), measured no improvement, reverted per D-09`

    **What NOT to do in any branch of Step 4:**
    - Do NOT silently keep an L1 flip that didn't measure a win — D-09 explicitly forbids this.
    - Do NOT touch any file outside `sdk/vitest.config.ts` and `.planning/intel/vitest-integration-baseline.md` in this task.
    - Do NOT tune `poolOptions` in response to a sub-1× measurement — that's speculation, not evidence-tied. The right response is revert and let Plan 03 (L2) explore a different lever.
    - Do NOT pass `--ignore-working-copy` to any jj command (Phase 3 D-05).
  </action>
  <verify>
    <automated>
      cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; \
      grep -q "^## L1-threads" .planning/intel/vitest-integration-baseline.md &amp;&amp; \
      grep -qE "L1 decision: (KEPT|REVERTED)" .planning/intel/vitest-integration-baseline.md &amp;&amp; \
      grep -qE "Measured ratio:" .planning/intel/vitest-integration-baseline.md &amp;&amp; \
      [ "$(grep -c "^## " .planning/intel/vitest-integration-baseline.md)" -ge "2" ] &amp;&amp; \
      ! grep -E '\.(skip|only|todo)\(' sdk/src/*.integration.test.ts sdk/src/**/*.integration.test.ts 2>/dev/null | grep -v "^Binary" | grep -v ":0" | head -1 | grep -q ":" || true &amp;&amp; \
      pnpm --filter @gsd-build/sdk exec vitest --version
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/vitest-integration-baseline.md` contains a `## L1-threads` section heading (verified by grep)
    - That section contains either `L1 decision: KEPT` or `L1 decision: REVERTED` (verified by regex grep — exactly one of the two strings must be present)
    - That section contains a `Measured ratio:` line with a numeric ratio (verified by grep)
    - The file has at least 2 `## ` section headings (baseline + L1-threads; verified by `grep -c "^## " >= 2`)
    - If KEPT: `sdk/vitest.config.ts` contains `pool: 'threads',` (verified by grep, condition checked manually based on KEPT/REVERTED choice)
    - If REVERTED: `sdk/vitest.config.ts` contains NO `pool:` line (verified by `! grep -q "pool:" sdk/vitest.config.ts`, condition checked manually)
    - No `.skip(` / `.only(` / `.todo(` calls were introduced in any `*.integration.test.ts` file (D-05b; verified by negative grep — the count should be the same as it was at end of Plan 01)
    - Vitest still parses the (possibly-reverted) config (verified by `vitest --version` exec)
    - Commit uses `jj` (verified during commit; no raw git invocations per CLAUDE.md)
  </acceptance_criteria>
  <done>
    L1 is either kept (with measured evidence) or reverted (with measured evidence). `.planning/intel/vitest-integration-baseline.md` records the decision. Plan 03 (L2) starts from whichever config state Plan 02 left behind.
  </done>
</task>

</tasks>

<verification>
- L1 decision is logged with measurement evidence (D-09)
- Test-count parity holds (D-05a)
- No skip/only/todo introduced (D-05b)
- 3-of-3 measurement runs passed (D-05c, enforced by the harness's flakiness gate)
- Unit project config still untouched (D-01 + Pitfall 6)
- D-07 out-of-scope files (scripts/run-tests.cjs, tests/helpers.cjs, .github/workflows/test.yml, cjs test suite, backend matrix) not modified
</verification>

<success_criteria>
- `sdk/vitest.config.ts` is in one of two states: (a) L1 applied with measured win recorded, or (b) original shape with measured non-win recorded — never silently flipped without evidence
- `.planning/intel/vitest-integration-baseline.md` is human-readable and shows the per-lever record-keeping shape that Plan 03 will continue
- Plan 03 (L2) can begin: it cites the L1-final state of the config and the L1-threads section of the baseline as its evidence
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-make-tests-run-faster/03.1-02-SUMMARY.md` recording:
- L1 decision (kept/reverted) with the measured ratio
- Per-file breakdown: which files improved, which didn't (data for L2 decision in Plan 03)
- Whether per-file module-init cost appears to still dominate (the L2 trigger condition — informs Plan 03 entrance criteria)
- Any unexpected observations (e.g., "phase-runner got slower under threads — investigate before L2")
</output>
