---
phase: 03.1-make-tests-run-faster
plan: 02
subsystem: testing
tags: [vitest, pool, threads, skipped]
provides:
  - "(no code change — plan skipped after baseline reframing)"
key-files:
  created: []
  modified: []
key-decisions:
  - "Skip plan execution per user decision 2026-05-13. Lever L1 (pool: 'threads') was sized for a 16-min integration suite; the post-Plan-01 baseline is 7.4 s and the run-to-run variance window swallows any expected L1 win. Executing as-written would have produced 'measured ratio within ±5% of 1.0, revert per D-09' — known motion for known outcome."
patterns-established: []
requirements-completed: []
duration: 0 (not executed)
completed: 2026-05-13
status: skipped-by-design
---

# Plan 03.1-02: L1 pool='threads' — SKIPPED

**Not executed. Post-Plan-01 baseline (7.4 s integration suite) is already inside the user's "acceptable performance" envelope; lever flip would have been measurement noise.**

## Performance

- **Duration:** 0
- **Started:** —
- **Completed:** —
- **Tasks:** 0/3 (skipped before execution)
- **Files modified:** 0

## Why skipped

Plan 01 landed two changes that fundamentally reshaped the integration suite:
1. Gated 3 slow Claude-CLI tests behind `GSD_ENABLE_E2E=1` — removed ~95% of pre-Plan-01 wall-clock.
2. Ported 5 SDK↔gsd-tools.cjs parity drifts — independent correctness fix but also unblocked the harness.

Post-Plan-01 baseline (median per file, 3 runs):
- `golden.integration.test.ts`: 4109 ms
- `read-only-parity.integration.test.ts`: 3259 ms
- everything else: <100 ms each (most are sub-15 ms)
- **Total: 7394 ms**

Plan 02's gate was "keep the flip if measured ratio > 1.0× pre-L1" (per D-09). At 7.4 s, run-to-run variance is typically ±100–500 ms — a true L1 saving of ~200 ms (the realistic high-end on a 2-non-trivial-file suite) would not distinguish from noise. Plan 04's "compute pre/post ratio" gate would correspondingly report no shippable change. Net expected outcome of executing as-written: revert, write up "no win," advance.

User decision 2026-05-13 (after running `pnpm vitest` directly in `sdk/`): "it seems to be of acceptable performance now. We can do 2." (Option 2 = "skip 02 and 03, write phase SUMMARY explaining what happened.")

## What this means for downstream plans

- `sdk/vitest.config.ts` remains at its 22-LOC seed shape (no `pool: 'threads'` override).
- `.planning/intel/vitest-integration-baseline.md` retains only the `## baseline` section; no `## L1-threads --append` block was added.

## What's Next

Plan 03 is also skipped for the same reason. Plan 04 closes the phase. See `03.1-SUMMARY.md`.
