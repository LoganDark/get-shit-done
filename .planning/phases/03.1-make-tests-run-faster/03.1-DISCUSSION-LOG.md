# Phase 03.1: make tests run faster - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-12
**Phase:** 03.1-make-tests-run-faster
**Areas discussed:** Pain target, Mechanism, Backend matrix cost, CI vs local dev (initial selection); reframed mid-discussion to: Vitest scope, Parallelism aggression, Success criterion shape (post-correction); then: Phase shape, Ratio target, Measurement scope (final lock)

---

## Initial gray-area selection

User selected all four candidate areas: Pain target, Mechanism, Backend matrix cost, CI vs local dev. Four parallel `gsd-advisor-researcher` agents produced comparison tables. The Pain target table was presented first.

**Pain target — first presentation (rejected; reframed):**

| Option | Description | Selected |
|--------|-------------|----------|
| A — Profile-first, defer commitment | Instrument run-tests.cjs + helpers.cjs, commit baseline, scope picked later | |
| B — `tests/*.test.cjs` first, ratio target ≤ 1.2× | Attack 88+ spawning files via in-process dispatch | |
| C — `sdk/*.test.ts` first, hard-seconds budget | Leave cjs alone; budget vitest unit ≤ 15s local / ≤ 30s CI | |
| D — Both, ratio + qualitative gate | Combine B's in-process dispatch + C's pretest tightening | |

**User's choice:** Rejected the framing. Clarified: "vitest is the pain point; it takes foreeever. i want to try more parallelism if at all possible."

**Notes:** Both my framing and all four advisor researchers had anchored on the cjs `tests/` 455-file surface and its subprocess-spawn cost. The user redirected to the smaller-file but slower-felt `sdk/` vitest suite. Memory entry written so future sessions don't re-anchor on the cjs suite. Backend matrix cost and CI vs local dev were dropped from the discussion — the vitest integration project doesn't use the backend matrix (verified by grep: no `vcsTest` / `makeBackendFixture` / `BACKENDS_AVAILABLE` references in integration test files), and CI is informational under the local-only measurement choice (D-04).

---

## Vitest scope (reframed Pain target)

| Option | Description | Selected |
|--------|-------------|----------|
| unit only | Speed up `vitest run --project unit` (144 files) | |
| both, unit-first | Unit speedup first, then integration | |
| both, equal priority | Treat as one suite | |
| integration only | Unit is fine; integration is the real pain (7 files, testTimeout: 120s) | ✓ |

**User's choice:** integration only
**Notes:** Locks D-01 in CONTEXT.md. Unit is deferred even though an informal 15+ minute unit baseline run (killed before completion) suggests unit is also slow — user knows what hurts them and named integration explicitly.

---

## Parallelism aggression

| Option | Description | Selected |
|--------|-------------|----------|
| L1+L2 (pool=threads + isolate=false) | Highest expected payoff, medium pollution risk, reversible per-knob | |
| L1 only (pool=threads, isolate stays true) | Lower risk, smaller payoff | |
| L3 (per-test fixture refactor + concurrent within file) | Bigger scope, biggest payoff if completed | |
| Profile-first, then decide | Capture vitest --reporter=verbose baseline; target only the long tail | ✓ |

**User's choice:** Profile-first
**Notes:** Drives D-02 (profile + fix in the same phase) and D-09 (each lever flip must be tied to a baseline observation). The earlier "more parallelism if at all possible" intent is still honored — profile-first means we measure before flipping knobs, not that we won't flip them.

---

## Success criterion shape

| Option | Description | Selected |
|--------|-------------|----------|
| Hard wall-clock budget | Concrete target (e.g. ≤ X seconds on local M-series) | |
| Ratio against current baseline | E.g. ≥ 2× faster than current main on the same machine | |
| Qualitative — runnable on save | No hard number; "fast enough that I'll actually run vitest while editing" | |

**User's choice (first round):** Ratio against current baseline
**User's choice (refined):** Largest improvement achievable without breaking semantics (added as fourth option in the next question batch)
**Notes:** D-03 captures the refined shape. The ratio frame survives but the threshold is "largest achievable" rather than a fixed number.

---

## Phase shape (final batch)

| Option | Description | Selected |
|--------|-------------|----------|
| Profile + fix in the same phase | Plan 1: baseline. Plan 2+: data-driven fix, verify ratio met. | ✓ |
| Profile only — fix in a follow-up | 03.1 ends after baseline + recommendation. New phase to apply fixes. | |
| Skip profile, just fix | Override earlier pick: pool=threads + isolate=false + measure delta | |

**User's choice:** Profile + fix in the same phase
**Notes:** Locks D-02.

---

## Ratio target

| Option | Description | Selected |
|--------|-------------|----------|
| ≥ 2× | Integration suite ≤ 50% of baseline wall-clock | |
| ≥ 3× | Aggressive; likely requires isolate=false + L4 subprocess-elimination | |
| ≥ 1.5× | Conservative floor | |
| Largest improvement achievable without breaking semantics | No fixed ratio; stop when next knob fails tests or risk exceeds payoff | ✓ |

**User's choice:** Largest improvement achievable without breaking semantics
**Notes:** Locks D-03. The "without breaking semantics" half is operationalized in D-05 as test-count parity + assertion fidelity + 3× back-to-back flakiness gate.

---

## Measurement scope

| Option | Description | Selected |
|--------|-------------|----------|
| Local M-series only | User's machine is the daily friction; CI is informational | ✓ |
| Local + CI both | Catches CPU-count cliffs (local 16 vs CI 2-4 vCPU) | |
| CI only | Optimize for gating environment, not dev loop | |

**User's choice:** Local M-series only
**Notes:** Locks D-04. CI is informational only; ratio truth lives on the local box.

---

## Claude's Discretion

- Profiling reporter shape (built-in `--reporter=verbose` vs custom reporter vs `time` wrapper)
- Order in which the planner tries levers L1–L5 (must be evidence-tied per D-09 but the sequence is open)
- Per-project vs hoisted layout of new knobs in `sdk/vitest.config.ts`
- Where to record the final post-fix ratio (phase SUMMARY mandatory; ROADMAP.md append optional)
- Whether to expose vitest perf knobs via env (e.g., `VITEST_POOL=threads`) as an escape hatch

## Deferred Ideas

- `tests/*.test.cjs` cjs spawn-cost work (455 files via node:test; the original anchor that was deflected)
- Vitest unit project speedup (144 files; killed 15-min baseline suggests it's also slow but user named integration as the daily friction)
- Backend matrix cost / Phase 4 workspace test cost (fixture-template trick from advisor research is a future-phase candidate)
- CI runner tuning / GitHub Actions cost
- `pretest: pnpm run build:sdk` incremental skip (fingerprint-based)
