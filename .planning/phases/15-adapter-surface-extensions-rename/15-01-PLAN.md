---
phase: 15-adapter-surface-extensions-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/vcs/types.ts
  - sdk/src/vcs/backends.ts
  - sdk/src/vcs/backends/git.ts
  - sdk/src/vcs/backends/jj.ts
  - sdk/src/query/progress.ts
  - sdk/src/vcs/__tests__/git-backend.test.ts
  - sdk/src/vcs/__tests__/jj-skeleton.test.ts
  - sdk/src/vcs/__tests__/baseline-parity.test.ts
  - sdk/src/vcs/__tests__/jj-refs.test.ts
  - sdk/src/vcs/__tests__/backends.test.ts
  - get-shit-done/bin/lib/commands.cjs
  - tests/__tools__/capture-vcs-baselines.cjs
  - scripts/audit-root-commits-rename.cjs
  - tests/scripts/audit-root-commits-rename.test.cjs
  - .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: true
requirements: [NAMING-01]
requirements_addressed: [NAMING-01]
tags: [rename, audit, capability-matrix, sdk, vcs]

must_haves:
  truths:
    - "After 15.01 commits, per-extension `grep -c '\\brootCommits\\b'` (excluding carve-outs `node_modules`, `.git`, `.jj`, `dist-cjs`, `.archive-pre-v1.4`, `v1.2-research`) exits 0 across `*.ts`, `*.cjs`, `*.js` (NAMING-01 SC1)."
    - "Capability-matrix string literal at `sdk/src/vcs/backends.ts:79` reads `'refs.rootRevisions'`, NOT `'refs.rootCommits'` (Pitfall 3 / v1.2 retro CR-01 / threat T-15.01-01 mitigated)."
    - "`BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions']` is `['git', 'jj-colocated']`; `BACKENDS_AVAILABLE_FOR_VERB['refs.rootCommits']` is `undefined` (anti-assertion in `backends.test.ts`)."
    - "`dist-cjs/` rebuilt post-rename; compiled `.d.ts` references `rootRevisions` and zero `rootCommits` (threat T-15.01-02 mitigated)."
    - "Audit JSON written to `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` (slug-matched per A5; resolves Open Question 4)."
    - "Audit JSON contains `generatedAt`, `totalCount`, `byExtension` (grouped), `specialCases` (includes the `backends.ts:79` capability-matrix entry per D-11), `carveOuts`, `idempotencyHash` (MD5 over sorted tuples per D-12)."
    - "Audit script is stdout-only (no `fs.writeFileSync` / `fs.writeFile` calls); the plan task redirects stdout into the JSON sidecar path (D-13 + `feedback_avoid_jj_auto_tracked_output`)."
    - "Audit JSON commits FIRST, hard rename commits SECOND in the immediate next operation; `jj log` shows the two squash points adjacent with no intervening commits (resolves Open Question 3 — two adjacent commits)."
    - "`.planning/` historical-prose carve-out applied: PROJECT.md and STATE.md ACTIVE sections updated to `rootRevisions`; everything under `.planning/research/`, `.planning/intel/`, `.planning/milestones/`, `.planning/seeds/`, and the phase-15 CONTEXT/DISCUSSION-LOG/PATTERNS/RESEARCH/VALIDATION markdown left as historical-prose (resolves Open Question 1)."
  artifacts:
    - path: "scripts/audit-root-commits-rename.cjs"
      provides: "One-shot Node walker emitting grouped-by-extension JSON to stdout with idempotencyHash; module.exports walker for unit-test consumption."
      contains: "Object.freeze SKIP_DIRS, PATTERN = /\\brootCommits\\b/, createHash('md5'), module.exports = { walk, PATTERN, main }, process.stdout.write only (no fs.write)"
    - path: ".planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json"
      provides: "Frozen audit artifact recording the closed pre-rename call-site set."
      contains: "generatedAt, totalCount, byExtension, specialCases[{file:'sdk/src/vcs/backends.ts',line:79,kind:'capability-matrix-string-literal'}], carveOuts, idempotencyHash"
    - path: "tests/scripts/audit-root-commits-rename.test.cjs"
      provides: "node:test unit cases for the audit walker (ext coverage, SKIP_DIRS exclusion, carve-out exclusion, idempotencyHash determinism, specialCases extraction)."
    - path: "sdk/src/vcs/types.ts"
      provides: "VcsRefs interface with `rootRevisions(opts: { rev?: RevisionExpr }): string[]` replacing `rootCommits` at the original location."
    - path: "sdk/src/vcs/backends.ts"
      provides: "Capability matrix entry `'refs.rootRevisions': Object.freeze(['git', 'jj-colocated'] as const)` at the same line position (~:79); no `'refs.rootCommits'` key remains."
    - path: "sdk/src/vcs/__tests__/backends.test.ts"
      provides: "Presence assertion for `refs.rootRevisions` + anti-assertion for `refs.rootCommits` (Pitfall 3 guard)."
  key_links:
    - from: "scripts/audit-root-commits-rename.cjs"
      to: ".planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json"
      via: "shell stdout redirect (`> path`)"
      pattern: "node scripts/audit-root-commits-rename.cjs >"
    - from: "sdk/src/vcs/backends.ts"
      to: "sdk/src/vcs/types.ts"
      via: "string-keyed capability matrix referring to the interface method name"
      pattern: "'refs.rootRevisions':"
    - from: "get-shit-done/bin/lib/commands.cjs"
      to: "sdk/src/vcs/backends/{git,jj}.ts"
      via: "statsVcs.refs.rootRevisions runtime call (Pitfall 2 CJS-side site)"
      pattern: "statsVcs\\.refs\\.rootRevisions"
---

<objective>
Hard-rename `rootCommits` → `rootRevisions` across the cross-backend `VcsAdapter` surface and every production caller (`*.ts` + `*.cjs`), gated by a pre-rename JSON sidecar audit. The audit captures the closed call-site set (22 production hits across 9 files; 1 special case at `backends.ts:79`); the rename then drives the per-extension `grep -c '\brootCommits\b'` count to zero. Adjacent two-commit shape (audit JSON first, rename second) prevents drift between audit-gen and rename-apply per Pitfall 7 / D-12.

Purpose: Close v1.2 NAMING-01 deferral — the surface name now matches the semantics. `rootCommits` is misleading on jj (returns change_ids, not commit_ids); `rootRevisions` is alphabet-agnostic and cross-backend correct.

Output: 22 production-code rename diffs + capability-matrix flip + audit JSON + audit script + unit tests + rebuilt `dist-cjs/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md
@.planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md
@.planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-VALIDATION.md
@sdk/src/vcs/types.ts
@sdk/src/vcs/backends.ts
@sdk/src/vcs/backends/git.ts
@sdk/src/vcs/backends/jj.ts
@sdk/src/query/progress.ts
@get-shit-done/bin/lib/commands.cjs
@scripts/audit-id-namespace.cjs
@scripts/audit-workflow-raw-git.cjs
@tests/scripts/audit-id-namespace.test.cjs
@.planning/intel/id-namespace-audit.json

<interfaces>
<!-- Verified call-site inventory (22 production hits, 9 files) -->
<!-- Executor uses these directly — no codebase exploration needed. -->

From sdk/src/vcs/types.ts:336-367 (VcsRefs interface, line 363 is the rename target):
  rootCommits(opts: { rev?: RevisionExpr }): string[];

From sdk/src/vcs/backends.ts:79 (SPECIAL CASE — string literal not caught by TS compiler):
  'refs.rootCommits': Object.freeze(['git', 'jj-colocated'] as const),

From sdk/src/vcs/backends/git.ts:526 (function declaration):
  const rootCommits = (opts: { rev?: RevisionExpr }): string[] => {

From sdk/src/vcs/backends/git.ts:564 (Object.freeze shorthand):
  rootCommits,

From sdk/src/vcs/backends/jj.ts:964 (property literal):
  rootCommits: ({ rev }: { rev?: RevisionExpr }): string[] => {

From sdk/src/query/progress.ts:288,293 (TS caller, comment + call):
  // countCommits/rootCommits read from the configured backend.
  const roots = vcs.refs.rootCommits({ rev: vcs.refs.head });

From sdk/src/vcs/__tests__/git-backend.test.ts:414,419 (test describe + call):
  describe('createGitAdapter — refs.rootCommits (02-03 Task 1)', () => {
    const roots = vcs.refs.rootCommits({ rev: vcs.refs.head });

From sdk/src/vcs/__tests__/jj-skeleton.test.ts:147,148 (test label + call):
  it('refs.rootCommits() does not throw VcsNotImplementedError (wired in plan 03-03)', () => {
    expect(() => vcs.refs.rootCommits({})).not.toThrow(VcsNotImplementedError);

From sdk/src/vcs/__tests__/baseline-parity.test.ts:234,238 (comment + call):
  // Plan 02-06 Task 3: vcs.refs.rootCommits({rev}) wraps
  const roots = vcs.refs.rootCommits({ rev: vcs.refs.head });

From sdk/src/vcs/__tests__/jj-refs.test.ts:162,215,216,217,220 (5 occurrences across comments/labels/calls)

From get-shit-done/bin/lib/commands.cjs:998,1005 (CJS — Pitfall 2 silent-failure site):
  // countCommits / rootCommits / log({rev: expr.rev(<runtime-sha>)}) —
  const roots = statsVcs.refs.rootCommits({ rev: statsVcs.refs.head });

From tests/__tools__/capture-vcs-baselines.cjs:414 (comment only):
  // Adapter equivalent: vcs.refs.rootCommits({rev: vcs.refs.head}).

From scripts/audit-id-namespace.cjs:1-50 (audit precedent — paste-ready skeleton for new audit script):
  // shebang + 'use strict' + node:fs/node:path + SKIP_DIRS Object.freeze + walker + module.exports

From tests/scripts/audit-id-namespace.test.cjs:1-60 (test precedent — paste-ready skeleton):
  // 'use strict' + node:test + node:assert/strict + tmpdir fixture + module require

Audit JSON schema (from 15-CONTEXT.md D-09 + 15-RESEARCH.md §Pre-Rename Audit Generator):
{
  generatedAt: ISO string,
  totalCount: number,
  byExtension: { ts: [{file, line, snippet}], cjs: [...], md: [...], json: [...] },
  specialCases: [{ file:'sdk/src/vcs/backends.ts', line:79, kind:'capability-matrix-string-literal', snippet }],
  carveOuts: [{path, reason}],
  idempotencyHash: MD5 hex string
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit script + unit tests + audit JSON sidecar (Wave 0 + audit emission)</name>
  <files>scripts/audit-root-commits-rename.cjs, tests/scripts/audit-root-commits-rename.test.cjs, .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json</files>
  <read_first>
    - scripts/audit-id-namespace.cjs (audit-script structural template: shebang, 'use strict', node:fs/node:path imports, SKIP_DIRS Object.freeze, walk function, module.exports of pure functions, require.main guard)
    - scripts/audit-workflow-raw-git.cjs (stdout-only audit precedent: docblock cites the "audit writes NOTHING to disk" constraint verbatim; reuse the phrasing)
    - tests/scripts/audit-id-namespace.test.cjs (node:test fixture pattern: const test = require('node:test'); const assert = require('node:assert/strict'); mkdtempSync + try/finally + assert.equal)
    - .planning/intel/id-namespace-audit.json (JSON sidecar precedent; the grouped-by-classifier shape we mirror with grouped-by-extension)
    - .planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md (D-09 audit JSON schema, D-10 verification gate, D-11 specialCases requirement, D-12 idempotencyHash, D-13 stdout-only constraint)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (§Pre-Rename Audit Generator lines 818-905 — paste-ready skeleton; §rootCommits Call-Site Inventory lines 310-380 — verifies the 22-hit closed set)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (§Pattern S5 stdout-only audit scripts; §`scripts/audit-root-commits-rename.cjs` divergences list — do NOT add verdict enum, do NOT add BASELINE block, do NOT process.exit non-zero)
  </read_first>
  <behavior>
    - Walker: walks repo with SKIP_DIRS Set containing 'node_modules', '.git', '.jj', 'dist-cjs', '.archive-pre-v1.4', 'v1.2-research'. Scans extensions `.ts`, `.cjs`, `.js`, `.md`, `.json`. Records hits as `{file, line, snippet, ext}`.
    - Pattern: `PATTERN = /\brootCommits\b/` (Object.freeze the const, exported via module.exports for unit tests).
    - Grouping: `byExtension` is a plain object keyed by extension (without leading dot), values are arrays of `{file, line, snippet}` sorted by file then line.
    - specialCases: filtered subset of hits where `file === 'sdk/src/vcs/backends.ts' && line === 79`; entry kind `'capability-matrix-string-literal'` per D-11.
    - carveOuts: hardcoded two-entry array (`.planning/research/.archive-pre-v1.4/` + `.planning/milestones/v1.2-research/`) with `reason` strings per D-09.
    - idempotencyHash: MD5 hex over `JSON.stringify({tuples: hits.map(h => ({file:h.file, line:h.line})), counts: Object.fromEntries(Object.entries(byExtension).map(([k,v]) => [k, v.length]))})` — sorted tuples for determinism (D-12).
    - Output: `process.stdout.write(JSON.stringify(output, null, 2) + '\n')` — stdout-only, NO `fs.writeFileSync` / `fs.writeFile` / `fs.appendFile` calls anywhere in the file (Pitfall 6 / D-13 / `feedback_avoid_jj_auto_tracked_output`).
    - Exit code: always 0 (the JSON is the artifact; gate-passing is the plan task's job per 15-PATTERNS.md divergence note).
    - Test 1 (walker.ext-coverage): synthesize tmpdir with one file per ext (`.ts/.cjs/.js/.md/.json`) each containing `rootCommits` literal; assert each appears in `byExtension[ext]`.
    - Test 2 (SKIP_DIRS): write `node_modules/x/y.ts` containing the token; assert NOT in output.
    - Test 3 (carve-outs): write `.planning/research/.archive-pre-v1.4/x.md` containing the token; assert NOT in output.
    - Test 4 (idempotencyHash determinism): run audit twice against identical tmpdir tree; assert hashes equal.
    - Test 5 (specialCases): write synthetic `sdk/src/vcs/backends.ts` with `'refs.rootCommits'` on line 79; assert `specialCases.length === 1` with `kind === 'capability-matrix-string-literal'`.
    - Test 6 (stdout-only invariant): grep the script content for `fs.writeFile` or `fs.writeFileSync` or `fs.appendFile`; assert 0 occurrences.
  </behavior>
  <action>
    Create `scripts/audit-root-commits-rename.cjs` as a one-shot Node walker mirroring `scripts/audit-id-namespace.cjs` structural template per Pattern S5 in 15-PATTERNS.md. Imports: `node:fs`, `node:path`, `node:crypto`. Constants: `SKIP_DIRS` (Object.freeze Set with the six entries listed in behavior), `PATTERN = /\brootCommits\b/`, `SCAN_EXTS` Set with `.ts`, `.cjs`, `.js`, `.md`, `.json`. Walker function: takes `(dir, hits)`, uses `fs.readdirSync(dir, { withFileTypes: true })`, recurses into directories not in SKIP_DIRS, reads files with matching ext, splits on `\n`, pushes `{file, line, snippet, ext}` when PATTERN matches. `main()`: walks `.` (repo root), sorts hits by file+line, groups into `byExtension`, computes `specialCases` for backends.ts:79, computes idempotencyHash via `createHash('md5').update(JSON.stringify({tuples, counts})).digest('hex')`, assembles output object with the exact key order `generatedAt`, `totalCount`, `byExtension`, `specialCases`, `carveOuts`, `idempotencyHash`, writes via `process.stdout.write(JSON.stringify(output, null, 2) + '\n')`. End of file: `if (require.main === module) main(); module.exports = { walk, PATTERN, main };`. Do NOT add `process.exit(...)` with non-zero argument. Do NOT call `fs.writeFile*` anywhere. Do NOT add a verdict enum.

    Create `tests/scripts/audit-root-commits-rename.test.cjs` mirroring `tests/scripts/audit-id-namespace.test.cjs` shape per Pattern from 15-PATTERNS.md. Requires `node:test`, `node:assert/strict`, `node:fs`, `node:path`, `node:os`. Imports walker + PATTERN via `require('../../scripts/audit-root-commits-rename.cjs')`. Implements the 6 test cases listed in `<behavior>` above; each test uses `fs.mkdtempSync(path.join(os.tmpdir(), 'rename-audit-test-'))` + try/finally for cleanup. For Test 5, synthesize the file at the path `sdk/src/vcs/backends.ts` inside the tmpdir with exactly 79 leading lines (or use the actual line number returned by the walker when `'refs.rootCommits'` lives at any line) — verify the test asserts the kind classification rather than hardcoding the line number. For Test 6, read the script file with `fs.readFileSync` and assert `/fs\.(write|append)/.test(content) === false`.

    Generate `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` by invoking the script with stdout redirection: `node scripts/audit-root-commits-rename.cjs > .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` (the plan task is responsible for the redirection, NOT the script per D-13).

    After the audit JSON is on disk, commit this task's three files (script + test + JSON) as ONE squash via `gsd-sdk query commit` with message `"chore($PHASE): audit rootCommits call sites"` (resolves Open Question 3 — audit lands as its own commit immediately preceding the rename commit in Task 2).
  </action>
  <verify>
    <automated>node --test tests/scripts/audit-root-commits-rename.test.cjs &amp;&amp; node -e "const a = JSON.parse(require('fs').readFileSync('.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json', 'utf8')); for (const k of ['generatedAt','totalCount','byExtension','specialCases','carveOuts','idempotencyHash']) if (!(k in a)) { console.error('missing '+k); process.exit(1); } if (!a.specialCases.some(s =&gt; s.file === 'sdk/src/vcs/backends.ts' &amp;&amp; s.line === 79 &amp;&amp; s.kind === 'capability-matrix-string-literal')) { console.error('specialCases missing backends.ts:79 entry'); process.exit(1); } if (a.totalCount &lt; 20) { console.error('totalCount lower than expected production-hit count (~22)'); process.exit(1); } console.log('OK '+a.totalCount+' hits, hash='+a.idempotencyHash);"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-root-commits-rename.cjs` exists; `grep -E 'fs\.(write|append)' scripts/audit-root-commits-rename.cjs` returns 0 lines (Pitfall 6 / D-13 guard)
    - `scripts/audit-root-commits-rename.cjs` exports `walk`, `PATTERN`, `main` via `module.exports` (Test 1 import depends on this)
    - `node --test tests/scripts/audit-root-commits-rename.test.cjs` exits 0 with all 6 cases green
    - `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` exists with the six required top-level keys (`generatedAt`, `totalCount`, `byExtension`, `specialCases`, `carveOuts`, `idempotencyHash`)
    - `specialCases` contains exactly one entry where `file === 'sdk/src/vcs/backends.ts'`, `line === 79`, `kind === 'capability-matrix-string-literal'`
    - `totalCount` is >= 20 (loose lower bound; verified production-hit count is 22 per 15-RESEARCH.md A3)
    - Audit JSON commit lands as a standalone squash with description containing the literal substring `audit rootCommits` (separate from the rename commit in Task 2 — resolves Open Question 3)
  </acceptance_criteria>
  <done>Audit script, unit tests (`node --test` green), and audit JSON sidecar all committed; specialCases surfaces the backends.ts:79 entry explicitly; stdout-only invariant verifiable via grep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hard rename rootCommits → rootRevisions across 22 production sites + capability matrix + dist-cjs rebuild</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/backends.ts, sdk/src/vcs/backends/git.ts, sdk/src/vcs/backends/jj.ts, sdk/src/query/progress.ts, sdk/src/vcs/__tests__/git-backend.test.ts, sdk/src/vcs/__tests__/jj-skeleton.test.ts, sdk/src/vcs/__tests__/baseline-parity.test.ts, sdk/src/vcs/__tests__/jj-refs.test.ts, sdk/src/vcs/__tests__/backends.test.ts, get-shit-done/bin/lib/commands.cjs, tests/__tools__/capture-vcs-baselines.cjs, .planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json (the audit emitted in Task 1 — IS the closed work-list; iterate `byExtension` for the file+line set)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (§`sdk/src/vcs/types.ts` MODIFIED block lines 530-624 — VcsRefs current shape; §"Production rootCommits callers" table at the end with file:line:role:pitfall-guard mapping)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (§rootCommits Call-Site Inventory verifies 22 production hits; §Runtime State Inventory A1 requires `dist-cjs/` rebuild post-rename; §Pitfall 1 + Pitfall 2 explain the capability-matrix string-literal flip and CJS-side rename respectively)
    - sdk/src/vcs/types.ts (the interface declaration site — line 363)
    - sdk/src/vcs/backends.ts (the capability matrix — line 79 string literal; verify ~25 entries exist post-edit per backends.test.ts current size + 1 rename)
    - sdk/src/vcs/backends/git.ts (lines 526 and 564 per audit JSON)
    - sdk/src/vcs/backends/jj.ts (line 964 per audit JSON)
    - get-shit-done/bin/lib/commands.cjs (lines 998, 1005 — CJS Pitfall 2 site; no TS protection)
  </read_first>
  <behavior>
    - Test 1 (grep-zero gate, structural): `grep -c '\brootCommits\b' --include='*.ts'` (and `*.cjs` and `*.js`) excluding SKIP_DIRS exits 0 lines after the rename commit.
    - Test 2 (capability-matrix flip): `BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions']` equals `['git', 'jj-colocated']`; `BACKENDS_AVAILABLE_FOR_VERB['refs.rootCommits']` is `undefined` (anti-assertion in `backends.test.ts`).
    - Test 3 (rebuilt dist-cjs): after `pnpm --filter sdk build`, `grep -c 'rootRevisions' sdk/dist-cjs/vcs/types.d.ts >= 1` and `grep -c 'rootCommits' sdk/dist-cjs/vcs/types.d.ts === 0`.
    - Test 4 (CJS caller works at runtime): the commands.cjs:1005 call site (now `statsVcs.refs.rootRevisions`) resolves to the renamed method via the compiled `dist-cjs/`; existing tests covering `computeRoadmapStats` (or equivalent) pass green.
    - Test 5 (idempotency-hash adjacency): `jj log --no-graph -T 'description.first_line() ++ "\n"'` shows the audit commit immediately preceding the rename commit (no intervening commits — D-12 + Pitfall 7).
    - Behavior preserved: jj backend's `rootRevisions: ({rev})` keeps the exact body shape (uses `jj log -r 'root() & ::{target}'` template per backends/jj.ts:964-980); git backend's `rootRevisions = (opts)` keeps `git rev-list --max-parents=0 <target>` semantics per backends/git.ts:526-531.
  </behavior>
  <action>
    Iterate `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` `byExtension.ts` and `byExtension.cjs` entries; for each `{file, line, snippet}` apply the literal substring substitution `rootCommits` → `rootRevisions` (per `feedback_vitest_extend_over_free_fn` precedent: hard rename, no alias per CF-02). Identifier transformation rules:
      - `rootCommits` (interface method, function name, Object.freeze shorthand) → `rootRevisions`
      - `.rootCommits(` (call expression) → `.rootRevisions(`
      - `'refs.rootCommits'` (string literal — Pitfall 1 / D-11 special case at backends.ts:79) → `'refs.rootRevisions'`
      - `refs.rootCommits` (text inside comments / describe labels / test labels) → `refs.rootRevisions`

    For `sdk/src/vcs/types.ts:363`: the line `rootCommits(opts: { rev?: RevisionExpr }): string[];` becomes `rootRevisions(opts: { rev?: RevisionExpr }): string[];`. No JSDoc edit needed — original line lacked JSDoc.

    For `sdk/src/vcs/backends.ts:79`: flip the string-literal key on the `Object.freeze(['git', 'jj-colocated'] as const)` entry. This is the Pitfall 1 / threat T-15.01-01 mitigation — TS compiler does NOT catch this; the audit's `specialCases[0]` is the explicit reminder.

    For `sdk/src/vcs/backends/git.ts:526` rename the local const declaration; for `:564` rename the Object.freeze property shorthand. Both lines now reference `rootRevisions`.

    For `sdk/src/vcs/backends/jj.ts:964`: rename the property literal key on the refs Object.freeze body. Body of the closure is unchanged (function still calls `jj log -r 'root() & ::{target}'`).

    For `sdk/src/query/progress.ts:288,293`: rename comment text AND the call expression (TS — compiler catches drift between the two but rename both for prose consistency).

    For the 5 test files (`git-backend.test.ts`, `jj-skeleton.test.ts`, `baseline-parity.test.ts`, `jj-refs.test.ts`): rename describe labels, it labels, comments, divider comments, and all call-expression sites per audit.

    For `get-shit-done/bin/lib/commands.cjs:998,1005`: rename the comment AND the call expression. **Pitfall 2 site — Tests MUST cover this path** (this CJS site is what failed in v1.2 retro CR-01 because TS compiler does not extend to CJS callers).

    For `tests/__tools__/capture-vcs-baselines.cjs:414`: rename the comment text only (it's a comment-only hit).

    For `sdk/src/vcs/__tests__/backends.test.ts`: add a NEW presence assertion `expect(BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions']).toEqual(['git', 'jj-colocated']);` and a NEW anti-assertion `expect(BACKENDS_AVAILABLE_FOR_VERB['refs.rootCommits']).toBeUndefined();` adjacent to the existing capability-matrix presence assertions (see 15-PATTERNS.md §`backends.test.ts` lines 951-984). Also update the keys-count expectation if present (15-PATTERNS.md notes `+0 net` for the rename — same key count post-rename).

    Update `.planning/PROJECT.md` and `.planning/STATE.md` ACTIVE sections only — search for the string `rootCommits` in each and replace within "Current Position" / "Current Milestone" / "v1.4 Requirements" surfaces ONLY. Leave any historical-prose mentions (in "Accumulated Context > Decisions" or older entries) untouched per Open Question 1 default policy. Do NOT touch any file under `.planning/research/`, `.planning/intel/`, `.planning/milestones/`, `.planning/seeds/`, or the phase-15 `15-{CONTEXT,DISCUSSION-LOG,PATTERNS,RESEARCH,VALIDATION}.md` files (these are historical-prose carve-outs).

    Run `pnpm --filter sdk build` to regenerate `sdk/dist-cjs/*.js` and `*.d.ts` with the new name (A1 in 15-RESEARCH.md Runtime State Inventory — without this, CJS consumers binding to compiled output still resolve to `rootCommits`, causing runtime `TypeError`).

    Squash this task's edits + the dist-cjs rebuild output into a single commit via `gsd-sdk query commit` with message `"refactor($PHASE): rename rootCommits → rootRevisions"`. This commit MUST land immediately after the Task 1 audit commit with NO intervening commits (D-12 adjacency rule / Pitfall 7).

    Post-rename verification gate: re-run `node scripts/audit-root-commits-rename.cjs` and confirm `byExtension.ts.length + byExtension.cjs.length + byExtension.js.length === 0` AND `idempotencyHash` matches the committed JSON's hash UNCHANGED from before the rename (the renaming changes which lines contain the pattern, but if we exclude SKIP_DIRS correctly and zero out the production-code entries, the audit-as-grep returns no production hits).
  </action>
  <verify>
    <automated>for ext in ts cjs js; do count=$(grep -rn '\brootCommits\b' --include="*.$ext" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.jj --exclude-dir=dist-cjs --exclude-dir=.archive-pre-v1.4 --exclude-dir=v1.2-research . | wc -l); [[ $count -eq 0 ]] || { echo "FAIL: $ext still has $count hits"; exit 1; }; done &amp;&amp; pnpm --filter sdk build &amp;&amp; grep -c 'rootRevisions' sdk/dist-cjs/vcs/types.d.ts &amp;&amp; ! grep -q 'rootCommits' sdk/dist-cjs/vcs/types.d.ts &amp;&amp; pnpm --filter sdk test -- --run --no-coverage -t "BACKENDS_AVAILABLE_FOR_VERB"</automated>
  </verify>
  <acceptance_criteria>
    - `for ext in ts cjs js; do grep -rn '\brootCommits\b' --include="*.$ext" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.jj --exclude-dir=dist-cjs --exclude-dir=.archive-pre-v1.4 --exclude-dir=v1.2-research . ; done` returns ZERO lines total (NAMING-01 SC1)
    - `grep -c 'rootRevisions' sdk/dist-cjs/vcs/types.d.ts` >= 1 (T-15.01-02 mitigation: dist-cjs rebuilt with new name)
    - `grep -c 'rootCommits' sdk/dist-cjs/vcs/types.d.ts` equals 0 (no stale compiled references)
    - `pnpm --filter sdk test -- --run -t "BACKENDS_AVAILABLE_FOR_VERB"` exits 0 with the new `refs.rootRevisions` presence assertion green AND the `refs.rootCommits === undefined` anti-assertion green
    - `pnpm --filter sdk test -- --run --no-coverage` exits 0 (full SDK suite green — covers git-backend, jj-skeleton, baseline-parity, jj-refs renames)
    - `sdk/src/vcs/backends.ts:79` (or equivalent line if reflows) reads `'refs.rootRevisions': Object.freeze(...)` — verified via `grep -n "'refs.rootRevisions':" sdk/src/vcs/backends.ts` exits 0
    - `grep -n "'refs.rootCommits':" sdk/src/vcs/backends.ts` returns ZERO lines
    - `jj log -r 'description("audit rootCommits") | description("rename rootCommits")' --no-graph -T 'description.first_line() ++ "\n"'` returns exactly 2 lines (audit + rename) with rename listed directly after audit (D-12 adjacency)
    - `.planning/PROJECT.md` Current Milestone section references `rootRevisions` (not `rootCommits`); `.planning/research/`, `.planning/intel/`, `.planning/milestones/`, `.planning/seeds/`, and phase-15 markdown files unchanged from pre-rename state (carve-out preserved)
  </acceptance_criteria>
  <done>Per-extension grep-zero gate passes for `.ts`/`.cjs`/`.js`; backends.test.ts presence + anti-assertion green; dist-cjs rebuilt and grep-clean for old name; full SDK suite green; audit-and-rename commits adjacent in jj log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TypeScript compiler vs string-literal keys | The capability matrix at `sdk/src/vcs/backends.ts:79` uses string keys; TS compiler does NOT catch missed renames here (Pitfall 3 / v1.2 retro CR-01 precedent). |
| TS source vs compiled CJS | `get-shit-done/bin/lib/commands.cjs` is CommonJS; resolution against the SDK goes through `sdk/dist-cjs/` compiled output, NOT against the `.ts` source. Stale `dist-cjs/` causes runtime `TypeError: undefined is not a function`. |
| Audit script process vs colocated-jj working tree | An audit script that writes its own output triggers auto-snapshot mid-operation per `feedback_avoid_jj_auto_tracked_output`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.01-01 | Tampering | `sdk/src/vcs/backends.ts:79` capability-matrix string literal | mitigate | Audit JSON `specialCases` field explicitly surfaces this site (D-11); `backends.test.ts` includes anti-assertion `expect(BACKENDS_AVAILABLE_FOR_VERB['refs.rootCommits']).toBeUndefined()`; structural-grep gate `grep -n "'refs.rootRevisions':" sdk/src/vcs/backends.ts` exit 0. |
| T-15.01-02 | Tampering | `sdk/dist-cjs/` compiled output post-rename | mitigate | Plan task 2 includes explicit `pnpm --filter sdk build` step; acceptance criterion `grep -c 'rootRevisions' sdk/dist-cjs/vcs/types.d.ts >= 1` AND `grep -c 'rootCommits' sdk/dist-cjs/vcs/types.d.ts === 0`. |
| T-15.01-03 | Tampering | Audit-and-rename commit adjacency | mitigate | D-12 idempotencyHash + structural acceptance criterion `jj log -r 'description("audit") | description("rename")'` returns exactly 2 adjacent commits; Pitfall 7 prevention. |
| T-15.01-04 | Tampering | Audit script writes into colocated-jj working tree | mitigate | Acceptance criterion `grep -E 'fs\.(write|append)' scripts/audit-root-commits-rename.cjs` returns 0 lines; plan task does the redirection, not the script (D-13 / `feedback_avoid_jj_auto_tracked_output`). |
| T-15.01-SC | Tampering | npm/pip/cargo installs in this plan | accept | Phase 15 introduces ZERO new packages (RESEARCH §Package Legitimacy Audit); no install verbs in plan; gate trivially satisfied. |
</threat_model>

<verification>
**Plan-level gates (all must be green before plan commit lands):**

1. `node --test tests/scripts/audit-root-commits-rename.test.cjs` exits 0 (6 cases green)
2. `for ext in ts cjs js; do grep -rn '\brootCommits\b' --include="*.$ext" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.jj --exclude-dir=dist-cjs --exclude-dir=.archive-pre-v1.4 --exclude-dir=v1.2-research . ; done` returns ZERO lines total
3. `pnpm --filter sdk test -- --run --no-coverage` exits 0 (full SDK suite green — includes the new `refs.rootRevisions` presence + `refs.rootCommits` anti-assertion in backends.test.ts plus all 4 renamed test files)
4. `pnpm --filter sdk build && grep -c 'rootRevisions' sdk/dist-cjs/vcs/types.d.ts` >= 1 AND `grep -q 'rootCommits' sdk/dist-cjs/vcs/types.d.ts && echo FAIL || echo OK` prints OK
5. `jj log -r 'description("audit rootCommits") | description("rename rootCommits")' --no-graph` shows exactly 2 commits with the rename immediately after the audit
6. `grep -E 'fs\.(write|append)' scripts/audit-root-commits-rename.cjs` returns 0 lines (Pitfall 6 / threat T-15.01-04)
7. `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` parses as valid JSON with the six required top-level keys present
</verification>

<success_criteria>
**Measurable completion (Phase 15 SC1 / NAMING-01 / Open Question resolutions 1, 3, 4):**

- All 22 production-code `rootCommits` sites enumerated by the audit are renamed to `rootRevisions`
- The Pitfall 3 / threat T-15.01-01 `backends.ts:79` capability-matrix string literal is flipped (verified by `grep -n` exit 0 on the new key and exit non-zero on the old key)
- The Pitfall 2 / threat T-15.01-02 CJS-side rename (`commands.cjs:1005`) is applied AND `dist-cjs/` rebuilt so runtime resolution works
- The Open Question 1 default policy (`.planning/` historical-prose carve-out except active PROJECT.md/STATE.md sections) is implemented and documented in the SUMMARY
- The Open Question 3 resolution (two adjacent commits — audit first, rename second) is verifiable via jj log
- The Open Question 4 resolution (audit JSON at slug-matched path `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json`) is the committed location
- Full SDK suite green; `dist-cjs/` rebuilt; no stale `rootCommits` references anywhere outside the carve-outs
</success_criteria>

<output>
Create `.planning/phases/15-adapter-surface-extensions-rename/15-01-SUMMARY.md` when done. Capture:
- Audit JSON summary (totalCount, idempotencyHash, specialCases)
- Open Question resolutions (1, 3, 4)
- Any Pitfall 1/2/3 sites that surfaced unexpectedly during execution
- The `pnpm --filter sdk build` output excerpt confirming dist-cjs rebuilt
- jj log excerpt showing audit + rename adjacency
</output>
</content>
</invoke>