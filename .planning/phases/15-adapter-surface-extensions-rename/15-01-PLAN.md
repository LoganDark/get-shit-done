---
phase: 15-adapter-surface-extensions-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/vcs/types.ts
  - sdk/src/vcs/backends.ts
  - sdk/src/vcs/backends/jj.ts
  - sdk/src/vcs/backends/git.ts
  - sdk/src/query/progress.ts
  - get-shit-done/bin/lib/commands.cjs
  - tests/__tools__/capture-vcs-baselines.cjs
  - sdk/src/vcs/__tests__/git-backend.test.ts
  - sdk/src/vcs/__tests__/jj-skeleton.test.ts
  - sdk/src/vcs/__tests__/jj-refs.test.ts
  - sdk/src/vcs/__tests__/baseline-parity.test.ts
  - sdk/src/vcs/__tests__/backends.test.ts
  - scripts/audit-root-commits-rename.cjs
  - tests/scripts/audit-root-commits-rename.test.cjs
  - .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json
autonomous: true
requirements:
  - NAMING-01
requirements_addressed:
  - NAMING-01
tags:
  - rename
  - vcs-adapter
  - audit
  - hard-rename
user_setup: []

must_haves:
  truths:
    - "Pre-rename one-shot audit script `scripts/audit-root-commits-rename.cjs` exists and emits the D-09 grouped-by-extension JSON shape (`generatedAt`, `totalCount`, `byExtension`, `specialCases`, `carveOuts`, `idempotencyHash`) to stdout only (no in-tree file writes per `feedback_avoid_jj_auto_tracked_output`)."
    - "Audit JSON sidecar exists at `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` (slug-matched path per RESEARCH A5; deviates from CONTEXT D-09's `.planning/phases/15/...` numeric-dir form — recorded in <deviations>) and is written via shell redirection from the audit script, not by the script itself."
    - "Audit JSON `totalCount` reflects the LIVE pre-rename hit count at execute time. CONTEXT D-09 cites `totalCount: 26` as a historical sample; PATTERNS N3 notes the live tree shows 28+ code-only hits as of 2026-05-24. The audit JSON is the source of truth — any drift from 26 is expected and recorded in the sidecar, not back-amended to CONTEXT."
    - "Audit JSON `specialCases` array contains an explicit entry for `sdk/src/vcs/backends.ts:79` with `kind: 'capability-matrix-string-literal'` (Pitfall 3 / Pitfall 1 / v1.2 retro CR-01 — TSC does NOT catch string-key object access)."
    - "Audit JSON `carveOuts` array names both `.planning/research/.archive-pre-v1.4/` and `.planning/milestones/v1.2-research/` as historical-prose carve-outs per ROADMAP Phase 15 SC1."
    - "After the rename commit lands, the per-extension grep gate exits 0 across `*.ts`, `*.cjs`, `*.js`, `*.md`, `*.json` (the 5 extensions per ROADMAP SC1) excluding the CONTEXT D-13 carve-outs (`node_modules`, `.git`, `.jj`, `dist-cjs`, `.archive-pre-v1.4`, `v1.2-research`, `.planning/research`, `.planning/intel`, `.planning/milestones`, `.planning/seeds`, the in-flight `.planning/phases/15-adapter-surface-extensions-rename/` directory itself) — no live `\\brootCommits\\b` references remain in in-scope code or active planning surfaces. For the in-scope `.md`/`.json` files, see truths #15-19 below for per-file disposition (rename vs historical-prose carve-out)."
    - "TypeScript interface `VcsRefs` declares `rootRevisions(opts: { rev?: RevisionExpr }): string[];` at the prior `rootCommits` slot (`sdk/src/vcs/types.ts:363` pre-rename). Body returns SAME shape as the old `rootCommits` — `string[]` of root change_ids (jj) / root commit_ids (git)."
    - "Capability matrix key `BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions']` exists and equals `Object.freeze(['git', 'jj-colocated'] as const)` at `sdk/src/vcs/backends.ts:79`. The prior `'refs.rootCommits'` string-literal key is GONE — TSC does not catch this; the new `backends.test.ts` regression test does."
    - "Both backend bodies are renamed in-place: `sdk/src/vcs/backends/jj.ts:964` `rootCommits:` → `rootRevisions:` (body unchanged); `sdk/src/vcs/backends/git.ts:526` `const rootCommits =` → `const rootRevisions =` AND short-hand reference at `:564` `rootCommits,` → `rootRevisions,`."
    - "All production callers in `.cjs` / `.js` flip in the SAME commit as the TS interface: `get-shit-done/bin/lib/commands.cjs` (v1.2 retro CR-01 surface), `sdk/src/query/progress.ts`, `tests/__tools__/capture-vcs-baselines.cjs`. No deprecation alias per CF-02."
    - "All SDK test files flip in the SAME commit: `sdk/src/vcs/__tests__/git-backend.test.ts`, `jj-skeleton.test.ts`, `jj-refs.test.ts`, `baseline-parity.test.ts`."
    - "Pre-existing test coverage of `rootRevisions` (renamed-in-place) still passes (`pnpm --filter sdk test -- --run -t \"rootRevisions\"` returns green). The rename is behavior-preserving — no semantic change to the function bodies."
    - "Audit-then-rename commit adjacency holds: the audit JSON sidecar is committed in a commit IMMEDIATELY preceding the rename commit (D-12 idempotencyHash invariant — no third commit between audit-generation and rename)."
    - "`pnpm --filter sdk build` regenerates `sdk/dist-cjs/vcs/types.d.ts` with `rootRevisions` declared and zero `rootCommits` references."
    - "**Per-file disposition for in-scope `.md`/`.json` files (per CONTEXT D-13 amendment):** The in-scope active-doc files that contain `rootCommits` references (verified via grep at planning time, 2026-05-24) are classified as historical-prose-carve-out (NOT renamed) because every occurrence is descriptive prose narrating the rename work itself, not a live API reference. Each receives a `specialCases[*]` entry in the D-09 audit JSON with `kind: 'historical-prose-carve-out'` and a per-file `reason` string (see truths #16-19). Live API references would be renamed; descriptive prose about the rename stays."
    - "**`.planning/PROJECT.md`** (3 occurrences — :22, :110, :195): historical-prose-carve-out. Reason: all occurrences are bullet-list prose narrating which deferred items v1.4 pulled in (`'rootCommits' → 'rootRevisions' cosmetic rename`) — describing the rename work itself. No live API consumer. specialCases entry: `{file: '.planning/PROJECT.md', kind: 'historical-prose-carve-out', reason: 'milestone narrative naming the rename target — describes the work, does not consume the API'}`."
    - "**`.planning/STATE.md`** (4 occurrences — :177, :186, :222, :248): historical-prose-carve-out. Reason: all occurrences are blocker/status/operator-next-step prose describing the rename work (Pitfall 3 anchor at :177, orthogonality note at :186, deferred-table row at :222, operator-next-steps at :248). No live API consumer. specialCases entry: `{file: '.planning/STATE.md', kind: 'historical-prose-carve-out', reason: 'status-tracking prose naming the rename target — describes the work, does not consume the API'}`."
    - "**`.planning/ROADMAP.md`** (5 occurrences — :65, :262, :267, :275, :370): historical-prose-carve-out. Reason: all occurrences are roadmap prose describing Phase 15 / SC1 / the 15-01 plan / closeout notes — they NAME the rename target as the work to be done. The SC1 grep gate at :267 cites the literal pattern `\brootCommits\b` inside backticks; that backticked pattern IS the gate definition, not a live API consumer. No rename. specialCases entry: `{file: '.planning/ROADMAP.md', kind: 'historical-prose-carve-out', reason: 'roadmap phase narrative + SC1 gate-definition backtick literal — describes the work and defines the verification gate'}`."
    - "**`.planning/REQUIREMENTS.md`** (2 occurrences — :43, :94): historical-prose-carve-out. Reason: NAMING-01 requirement bullet at :43 is the literal spec of the rename (`rootCommits → rootRevisions cosmetic rename across SDK adapter surface...`); :94 is the Deferred-Item Harvest cross-reference (`NAMING-01 covers 'rootCommits' only; 'countCommits' is semantically different concept...`). Both are spec prose naming the target, NOT live API consumers. specialCases entry: `{file: '.planning/REQUIREMENTS.md', kind: 'historical-prose-carve-out', reason: 'NAMING-01 spec text + cross-reference prose naming the rename target — defines and bounds the work'}`."
    - "**`.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-01-SUMMARY.md`** (1 occurrence — :186): historical-prose-carve-out. Reason: closed-phase summary note `Phase 15.01 (NAMING-01 rootCommits→rootRevisions): Orthogonal — different rename surface` — historical narrative explicitly naming Phase 15.01 as orthogonal to 14.1's work. No live API consumer; modifying a closed-phase summary would falsify the historical record. specialCases entry: `{file: '.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-01-SUMMARY.md', kind: 'historical-prose-carve-out', reason: 'closed-phase summary historical narrative naming Phase 15.01 — modifying it would falsify the historical record'}`."
  artifacts:
    - path: "scripts/audit-root-commits-rename.cjs"
      provides: "One-shot stdout-only rename audit emitter"
      contains: "PATTERN = '\\\\brootCommits\\\\b'"
    - path: ".planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json"
      provides: "Pre-rename audit JSON sidecar (D-09 schema)"
      contains: '"specialCases"'
    - path: "tests/scripts/audit-root-commits-rename.test.cjs"
      provides: "node:test cases for the audit emitter's shape invariants"
      contains: "totalCount"
    - path: "sdk/src/vcs/types.ts"
      provides: "VcsRefs.rootRevisions method declaration (renamed in-place)"
      contains: "rootRevisions("
    - path: "sdk/src/vcs/backends.ts"
      provides: "Capability matrix entry 'refs.rootRevisions' (string-literal flipped per Pitfall 3)"
      contains: "'refs.rootRevisions'"
    - path: "sdk/src/vcs/__tests__/backends.test.ts"
      provides: "BACKENDS_AVAILABLE_FOR_VERB regression test (presence + anti-presence)"
      contains: "BACKENDS_AVAILABLE_FOR_VERB"
  key_links:
    - from: "scripts/audit-root-commits-rename.cjs"
      to: ".planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json"
      via: "shell redirection in the Plan 15.01 audit-emission task"
      pattern: "node scripts/audit-root-commits-rename.cjs > .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json"
    - from: "sdk/src/vcs/types.ts (VcsRefs.rootRevisions declaration)"
      to: "sdk/src/vcs/backends/{git,jj}.ts (renamed bodies)"
      via: "satisfies-style structural typing on the refs Object.freeze"
      pattern: "rootRevisions:|const rootRevisions ="
    - from: "sdk/src/vcs/backends.ts capability matrix"
      to: "Runtime verb resolution"
      via: "string-key lookup BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions']"
      pattern: "'refs.rootRevisions':"
---

<objective>
Plan 15.01 (NAMING-01) ships the hard rename `rootCommits` → `rootRevisions` across the cross-backend VcsAdapter surface, production CJS callers, SDK tests, and the capability matrix string literal. The rename is gated by a one-shot pre-rename JSON sidecar audit per Pitfall 3 / v1.2 retro CR-01 prevention — TSC catches the `.ts` rename only; the `.cjs` callers, the capability-matrix string-literal at `sdk/src/vcs/backends.ts:79`, and the test bodies are caught by the per-extension `grep -c` gate and the new `backends.test.ts` regression test.

Purpose: clears `rootCommits` from the namespace so 15.02-04 land cleanly on a `types.ts` baseline with no in-flight renames. Ships FIRST per CF-01 (smallest diff, smallest blast radius, clears the namespace for the additive 3-verb extensions).

Output:
- New one-shot audit script `scripts/audit-root-commits-rename.cjs` (discarded post-milestone)
- New audit JSON sidecar at `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` (D-09 grouped-by-extension schema)
- New node:test cases for the audit emitter at `tests/scripts/audit-root-commits-rename.test.cjs`
- TS interface rename in `sdk/src/vcs/types.ts:363` (single slot, body shape unchanged)
- Capability-matrix string-literal flip at `sdk/src/vcs/backends.ts:79`
- Backend body renames at `backends/jj.ts:964` (1 site) and `backends/git.ts:526,564` (2 sites — def + spread short-hand)
- Production-caller renames in `commands.cjs`, `progress.ts`, `capture-vcs-baselines.cjs`
- SDK test renames in 4 files (`git-backend.test.ts`, `jj-skeleton.test.ts`, `jj-refs.test.ts`, `baseline-parity.test.ts`)
- New regression test in `sdk/src/vcs/__tests__/backends.test.ts` asserting the capability-matrix key flipped (presence + anti-presence)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md
@.planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md
@.planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-VALIDATION.md
@scripts/audit-id-namespace.cjs
@.planning/intel/id-namespace-audit.json

<interfaces>
<!-- Key declarations and call sites the executor needs. Read in <read_first> blocks for live state. -->

VcsRefs interface (current state — sdk/src/vcs/types.ts:336-367):
- Line 337-338: `readonly head: RevisionExpr;` / `readonly parent: RevisionExpr;` (style precedent)
- Line 363: `rootCommits(opts: { rev?: RevisionExpr }): string[];` — RENAME TARGET

Capability matrix (current state — sdk/src/vcs/backends.ts:79):
- `'refs.rootCommits': Object.freeze(['git', 'jj-colocated'] as const),` — STRING-LITERAL RENAME TARGET (Pitfall 3)

jj backend body (sdk/src/vcs/backends/jj.ts:964-980):
- `rootCommits: ({ rev }: { rev?: RevisionExpr }): string[] => { ... }` — RENAME `:` key in-place

git backend body (sdk/src/vcs/backends/git.ts:526,564):
- Line 526: `const rootCommits = (opts: { rev?: RevisionExpr }): string[] => { ... }` — RENAME const
- Line 564: `rootCommits,` (spread short-hand in `refs = Object.freeze({...})`) — RENAME reference

Production callers (CJS / TS — TSC does NOT catch these unless they import from .d.ts):
- `get-shit-done/bin/lib/commands.cjs` — v1.2 retro CR-01 site
- `sdk/src/query/progress.ts`
- `tests/__tools__/capture-vcs-baselines.cjs`

SDK tests (vitest):
- `sdk/src/vcs/__tests__/git-backend.test.ts`
- `sdk/src/vcs/__tests__/jj-skeleton.test.ts`
- `sdk/src/vcs/__tests__/jj-refs.test.ts`
- `sdk/src/vcs/__tests__/baseline-parity.test.ts`

Audit precedent (scripts/audit-id-namespace.cjs:1-66):
- `'use strict';` header
- `const SKIP_DIRS = new Set([...])` + `const SCAN_EXT = /\\.(cjs|js|mjs|ts|md)$/`
- `function findFiles(dir, results) { ... }` walker pattern
- Stdout-only emit + caller redirects (D-13)
</interfaces>
</context>

<deviations>
**D-09 path form:** CONTEXT D-09 literal cites `.planning/phases/15/rootCommits-rename-audit.json` (numeric phase dir). The phase directory under v1.4 convention is the slug-matched `.planning/phases/15-adapter-surface-extensions-rename/` (per RESEARCH §A5 anchor + actual on-disk path verified pre-plan-write). This plan uses the slug-matched form so the audit lives inside the phase directory rather than creating a sibling polluting `.planning/phases/`. Per memory `feedback_phase_filename_padding` the padded form is honored throughout v1.4. The sidecar provenance is preserved; only the path string differs from the CONTEXT literal.

**N3 audit drift handling:** CONTEXT D-09 cites `totalCount: 26` as the historical sample baseline. PATTERNS N3 confirms the live tree shows 28+ code-only hits as of 2026-05-24. This plan does NOT pre-amend CONTEXT to a new fixed number; the audit JSON `totalCount` field IS the source of truth at execute time. Any drift from 26 is expected, recorded in the JSON sidecar, and used to size the per-extension grep gate. The grep gate's pass criterion is `count_post == 0`, NOT `count_pre == 26`.
</deviations>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script → stdout → JSON sidecar | Script output redirected by shell into a file in `.planning/`; never written directly by the script (Pitfall 8 mitigation) |
| capability matrix string key → runtime verb resolution | String-keyed lookup in `BACKENDS_AVAILABLE_FOR_VERB`; TSC does NOT validate the literal (v1.2 retro CR-01) |
| TS interface → CJS callers (`commands.cjs`, `progress.ts`) | TS-side rename does NOT compile-check CJS or untyped imports; per-extension grep gate is the safety net |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.01-01 | Tampering | `sdk/src/vcs/backends.ts:79` capability matrix string literal | mitigate | Audit JSON `specialCases` field surfaces the line explicitly; new `backends.test.ts` regression test asserts presence of `'refs.rootRevisions'` AND anti-asserts absence of `'refs.rootCommits'` |
| T-15.01-02 | Tampering | CJS callers (`commands.cjs:1005` v1.2 retro CR-01 site) | mitigate | Audit JSON `byExtension.cjs` array enumerates every CJS reference; per-extension grep gate `grep -c '\\brootCommits\\b' --include='*.cjs'` must exit 0 (zero matches) post-rename |
| T-15.01-03 | Tampering | Audit JSON sidecar tracked by jj auto-snapshot | mitigate | Audit script emits stdout ONLY per `feedback_avoid_jj_auto_tracked_output`; sidecar written by shell redirection in the plan task; sidecar committed in a separate adjacent commit (D-12 idempotencyHash invariant) |
| T-15.01-04 | Tampering | New `rootCommits` references added between audit and rename | mitigate | D-12 `idempotencyHash` (MD5 over sorted `{file, line}` tuples + per-extension counts) — re-running audit during rename commit prep would change the hash if new refs landed in the interim |
| T-15.01-05 | DoS | Audit script grep over the full repo | accept | `--exclude-dir` arguments scope the walk to in-tree code; runtime well under 5s on this repo size (verified by `audit-id-namespace.cjs` Phase 8 precedent) |
| T-15.01-06 | Information disclosure | `dist-cjs/` rebuild emits stale `rootCommits` symbols if not regenerated | mitigate | Plan task runs `pnpm --filter sdk build` after rename commit; grep gate covers `sdk/dist-cjs/vcs/types.d.ts` for `rootRevisions` presence |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Build pre-rename audit script + node:test cases + emit and commit JSON sidecar</name>
  <files>scripts/audit-root-commits-rename.cjs, tests/scripts/audit-root-commits-rename.test.cjs, .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json</files>
  <read_first>
    - scripts/audit-id-namespace.cjs (Phase 8 v1.2 audit precedent — copy header style, `'use strict'`, walker shape, EXTENSIONS-and-EXCLUDES arrays, stdout-only emit pattern)
    - .planning/intel/id-namespace-audit.json (v1.2 schema precedent — header field naming for `generatedAt`/`scanned_at`, sorted-key serialization)
    - .planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md (D-09 grouped-by-extension schema literal at lines 53-72; D-10..D-13 audit gates; carveOuts paths)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (Example 8 audit script shape at lines 1139-1214; Pitfall 8 jj auto-tracked-output mitigation at lines 609-623)
  </read_first>
  <action>
    Create `scripts/audit-root-commits-rename.cjs` per Pattern S6 (Hard-Rename Atomic-Commit + Pre-Audit Gate) + D-13:
    - Header per RESEARCH Example 8 (lines 1141-1147): file purpose, stdout-only contract per `feedback_avoid_jj_auto_tracked_output`, single-purpose one-shot (discarded post-milestone).
    - `'use strict';` then `const { createHash } = require('node:crypto');` + `const { execSync } = require('node:child_process');`.
    - `const PATTERN = '\\brootCommits\\b';` (word-boundary anchored; matches CONTEXT D-10 grep gate).
    - `const EXTENSIONS = ['ts', 'cjs', 'js', 'md', 'json'];` (per D-09 schema `byExtension` keys).
    - `const EXCLUDES = ['node_modules', '.git', '.jj', 'dist-cjs/', '.planning/research/.archive-pre-v1.4/', '.planning/milestones/v1.2-research/'];` (per ROADMAP Phase 15 SC1 carve-out list).
    - Walker uses `execSync('grep -rn ... .', {encoding: 'utf-8'})` (RESEARCH Example 8 simplest form acceptable here; the audit-id-namespace.cjs walker is a richer alternative but adds no value for this 28-hit one-shot) — wrap in try/catch because `grep` exits 1 on zero matches (post-rename success case).
    - Parse hits into `{file, line, snippet}` rows (split each grep line on `:`).
    - Group into `byExtension` map keyed by EXTENSIONS values.
    - Build `specialCases` array. Two kinds of entries: (1) the existing Pitfall 3 / D-11 capability-matrix-string-literal entry — filter hits where `file === './sdk/src/vcs/backends.ts' && line === 79`; annotate with `kind: 'capability-matrix-string-literal'`. (2) The B2-amendment historical-prose carve-outs — for each of the 5 in-scope `.md`/`.json` active-doc files (`.planning/PROJECT.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-01-SUMMARY.md`) whose hits the audit walker discovers, append `{file, kind: 'historical-prose-carve-out', reason: <per-file reason per plan truths #15-19>}` entries. The audit consumer (grep gate at Task 2) treats `specialCases` entries as expected-and-acceptable so the post-rename gate exits 0 across all 5 extensions even though those 5 files retain their descriptive prose.
    - Build `carveOuts` array verbatim per CONTEXT D-09 lines 66-69.
    - Compute `idempotencyHash`: `JSON.stringify({files: hits.map(h => h.file+':'+h.line).sort(), counts: <byExtension counts map>})` → `createHash('md5').update(hashInput).digest('hex')` (D-12).
    - Emit `{generatedAt, totalCount, byExtension, specialCases, carveOuts, idempotencyHash}` via `process.stdout.write(JSON.stringify(out, null, 2) + '\\n')` with sorted-keys serialization (per CONTEXT Discretion).
    - NEVER call `fs.writeFile`/`fs.writeFileSync` from inside the script body — the script writes only to stdout (Pitfall 8 enforcement).

    Create `tests/scripts/audit-root-commits-rename.test.cjs` per VALIDATION line 75 (node:test domain):
    - Use `node:test` framework (`const { test } = require('node:test'); const assert = require('node:assert/strict');`).
    - Spawn the audit script via `execSync('node scripts/audit-root-commits-rename.cjs', {encoding: 'utf-8'})`.
    - Parse stdout as JSON via `JSON.parse(stdout)`.
    - Assert presence of each top-level field: `generatedAt`, `totalCount`, `byExtension`, `specialCases`, `carveOuts`, `idempotencyHash`.
    - Assert `byExtension` is an object with keys `ts`/`cjs`/`js`/`md`/`json` (each value is an array).
    - Assert `carveOuts` contains entries naming `.planning/research/.archive-pre-v1.4/` and `.planning/milestones/v1.2-research/`.
    - Assert `idempotencyHash` matches `/^[0-9a-f]{32}$/` (MD5 hex).
    - Assert `specialCases` is an array; per RESEARCH §Pattern S6 + CONTEXT D-11 the entry for `sdk/src/vcs/backends.ts:79` MUST appear when run pre-rename. (Post-rename run for the post-rename test phase should show empty specialCases — that assertion lives in Task 2 verification, not here.)
    - Assert round-trip `JSON.parse(JSON.stringify(parsed))` is structurally equal to `parsed`.

    Emit the audit JSON sidecar via shell redirection:
    `node scripts/audit-root-commits-rename.cjs > .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json`

    Commit the audit script + tests + JSON sidecar in a SINGLE commit titled `chore(15-01): pre-rename rootCommits audit + sidecar` (D-12 audit/rename adjacency invariant: this commit lands IMMEDIATELY before the rename commit in Task 2).
  </action>
  <verify>
    <automated>node --test tests/scripts/audit-root-commits-rename.test.cjs &amp;&amp; test -f .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json &amp;&amp; node -e "const a=JSON.parse(require('fs').readFileSync('.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json','utf8')); for(const k of ['generatedAt','totalCount','byExtension','specialCases','carveOuts','idempotencyHash']) if(!(k in a)) throw new Error('missing '+k); if(!a.specialCases.some(c=>c.file.endsWith('backends.ts')&amp;&amp;c.line===79)) throw new Error('specialCases missing backends.ts:79'); if(!/^[0-9a-f]{32}$/.test(a.idempotencyHash)) throw new Error('idempotencyHash not MD5 hex'); console.log('audit JSON shape OK; totalCount='+a.totalCount);"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/audit-root-commits-rename.cjs` exists with `'use strict';` header and contains the literal substring `'\\\\brootCommits\\\\b'` (the PATTERN constant).
    - `scripts/audit-root-commits-rename.cjs` contains zero references to `fs.writeFile` / `fs.writeFileSync` / `fs.appendFile` (stdout-only contract; Pitfall 8 enforcement) — verifiable by `grep -E 'fs\\.(write|append)File' scripts/audit-root-commits-rename.cjs` returning zero lines.
    - `tests/scripts/audit-root-commits-rename.test.cjs` exists and uses `node:test`; running `node --test tests/scripts/audit-root-commits-rename.test.cjs` exits 0.
    - `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` exists and parses as valid JSON.
    - Parsed JSON contains all 6 top-level fields: `generatedAt`, `totalCount`, `byExtension`, `specialCases`, `carveOuts`, `idempotencyHash`.
    - `specialCases` contains an entry where `file` ends with `backends.ts` and `line === 79` and `kind === 'capability-matrix-string-literal'`.
    - `carveOuts` contains entries naming both `.planning/research/.archive-pre-v1.4/` and `.planning/milestones/v1.2-research/`.
    - `idempotencyHash` matches `/^[0-9a-f]{32}$/`.
    - `totalCount` is a positive integer (live tree shows 28+ code-only hits pre-rename per PATTERNS N3 — exact number recorded in the JSON).
    - Working copy is clean after the commit (`assert_clean_wc` per `feedback_workflow_assert_clean_wc`).
  </acceptance_criteria>
  <done>Audit script + node:test cases + audit JSON sidecar shipped in one commit. Sidecar `totalCount` reflects live pre-rename hit count. `specialCases` surfaces backends.ts:79 explicitly. `idempotencyHash` MD5 present. Ready for Task 2 rename commit (adjacent).</done>
</task>

<task type="auto">
  <name>Task 2: Hard-rename rootCommits → rootRevisions across TS interface, both backends, capability matrix, CJS callers, and SDK tests in a single atomic commit</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/backends.ts, sdk/src/vcs/backends/jj.ts, sdk/src/vcs/backends/git.ts, sdk/src/query/progress.ts, get-shit-done/bin/lib/commands.cjs, tests/__tools__/capture-vcs-baselines.cjs, sdk/src/vcs/__tests__/git-backend.test.ts, sdk/src/vcs/__tests__/jj-skeleton.test.ts, sdk/src/vcs/__tests__/jj-refs.test.ts, sdk/src/vcs/__tests__/baseline-parity.test.ts</files>
  <read_first>
    - sdk/src/vcs/types.ts (read line 363 to confirm `rootCommits` declaration is on a single line in VcsRefs)
    - sdk/src/vcs/backends.ts (read lines 75-82 to confirm `'refs.rootCommits'` literal at line 79)
    - sdk/src/vcs/backends/jj.ts (read lines 964-980 to confirm `rootCommits:` key + body)
    - sdk/src/vcs/backends/git.ts (read lines 526-531 for `const rootCommits = ...` AND lines 554-568 for the spread short-hand at line 564 `rootCommits,`)
    - sdk/src/query/progress.ts (grep `rootCommits` first to scope the in-file references)
    - get-shit-done/bin/lib/commands.cjs (grep `rootCommits` — v1.2 retro CR-01 site)
    - tests/__tools__/capture-vcs-baselines.cjs (grep `rootCommits`)
    - sdk/src/vcs/__tests__/git-backend.test.ts, jj-skeleton.test.ts, jj-refs.test.ts, baseline-parity.test.ts (grep `rootCommits` in each)
    - .planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json (the audit emitted in Task 1 — use the `byExtension` arrays as the authoritative file/line target list)
  </read_first>
  <action>
    Per Pattern 1 (Hard rename without deprecation alias; CF-02) + Pattern S6: ONE atomic commit touches every site enumerated by Task 1's audit JSON. No deprecation alias. No two-commit phasing of TS-then-CJS. The audit JSON's `byExtension` arrays are the authoritative target list — every entry must be flipped in this commit.

    Concrete renames (use the audit JSON as the master list; the following are the known load-bearing sites):

    1. `sdk/src/vcs/types.ts:363`: `rootCommits(opts: { rev?: RevisionExpr }): string[];` → `rootRevisions(opts: { rev?: RevisionExpr }): string[];` (single-line interface method rename; body shape unchanged).

    2. `sdk/src/vcs/backends.ts:79`: `'refs.rootCommits': Object.freeze(['git', 'jj-colocated'] as const),` → `'refs.rootRevisions': Object.freeze(['git', 'jj-colocated'] as const),` — STRING-LITERAL flip per Pitfall 3 / v1.2 retro CR-01 (TSC does NOT catch this; the new regression test in Task 3 does).

    3. `sdk/src/vcs/backends/jj.ts:964`: `rootCommits: ({ rev }: { rev?: RevisionExpr }): string[] => {` → `rootRevisions: ({ rev }: { rev?: RevisionExpr }): string[] => {` (object-literal key rename; body lines 965-980 unchanged).

    4. `sdk/src/vcs/backends/git.ts:526`: `const rootCommits = (opts: { rev?: RevisionExpr }): string[] => {` → `const rootRevisions = (opts: { rev?: RevisionExpr }): string[] => {`. AND at line 564 inside `const refs = Object.freeze({...})`: `rootCommits,` → `rootRevisions,` (short-hand spread reference — TSC catches this one since it's a `.ts` import).

    5. `get-shit-done/bin/lib/commands.cjs`: every `\\brootCommits\\b` occurrence flipped to `rootRevisions`. v1.2 retro CR-01 was a `commands.cjs` reference miss; this is the canonical surface that proves the audit gate works. Use the audit JSON's `byExtension.cjs` array as the target list.

    6. `sdk/src/query/progress.ts`: every `\\brootCommits\\b` occurrence flipped to `rootRevisions`.

    7. `tests/__tools__/capture-vcs-baselines.cjs`: every `\\brootCommits\\b` occurrence flipped to `rootRevisions`.

    8. `sdk/src/vcs/__tests__/git-backend.test.ts`, `jj-skeleton.test.ts`, `jj-refs.test.ts`, `baseline-parity.test.ts`: every `\\brootCommits\\b` occurrence flipped to `rootRevisions` (test bodies + `it(...)` strings). Test files are TS so TSC catches body-level references; but the `it('rootCommits returns ...')` description string is invisible to TSC — the grep gate catches it.

    Per the carve-outs in CONTEXT D-09 lines 66-69: DO NOT touch any file under `.planning/research/.archive-pre-v1.4/` or `.planning/milestones/v1.2-research/`. These are historical-prose carve-outs preserved verbatim.

    After all renames:
    - Run `cd sdk && pnpm tsc --noEmit` — must exit 0.
    - Run `cd sdk && pnpm vitest run src/vcs/__tests__/git-backend.test.ts src/vcs/__tests__/jj-refs.test.ts src/vcs/__tests__/jj-skeleton.test.ts src/vcs/__tests__/baseline-parity.test.ts --reporter=dot` — must exit 0 (rename is behavior-preserving).
    - Run the per-extension grep gate (D-10):
      `for ext in ts cjs js md json; do count=$(grep -rln "\\brootCommits\\b" --include="*.$ext" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.jj --exclude-dir=dist-cjs --exclude-dir=".archive-pre-v1.4" --exclude-dir="v1.2-research" --exclude-dir=research --exclude-dir=intel --exclude-dir=milestones --exclude-dir=seeds --exclude-dir="15-adapter-surface-extensions-rename" . | wc -l | tr -d ' '); test "$count" = "0" || { echo "FAIL: $ext has $count remaining"; exit 1; }; done`

      **In-scope `.md` / `.json` carve-out reconciliation:** the gate above excludes `.planning/research/`, `.planning/intel/`, `.planning/milestones/`, `.planning/seeds/`, and the in-flight phase directory itself per CONTEXT D-13. The remaining in-scope active-doc files that still match (verified at planning time, 2026-05-24) are: `.planning/PROJECT.md` (3 hits), `.planning/STATE.md` (4 hits), `.planning/ROADMAP.md` (5 hits), `.planning/REQUIREMENTS.md` (2 hits), and `.planning/phases/14.1-drop-mandatory-bookmark-on-parallel-dispatch-fan-in-emergenc/14.1-01-SUMMARY.md` (1 hit). Each is classified as `historical-prose-carve-out` per plan truths #15-19 — they describe the rename work or define the SC1 gate itself, NOT live API consumers. The executor MUST: (a) at audit-emission time (Task 1), pass `--exclude-dir=research --exclude-dir=intel --exclude-dir=milestones --exclude-dir=seeds --exclude-dir="15-adapter-surface-extensions-rename"` to the grep walker so the gate scope matches D-13; (b) for each in-scope `.md`/`.json` file that still appears in the audit JSON, append a `specialCases[*]` entry of the form `{file: <path>, kind: "historical-prose-carve-out", reason: <per-file reason per plan truths #15-19>}` to the audit sidecar; (c) NOT modify any of the 5 carve-out files in this plan — they remain verbatim; (d) re-run the gate post-rename — once the `*.ts`/`*.cjs`/`*.js` renames land and the carve-out files retain their historical-prose references (excluded explicitly by the `specialCases` entries), the gate exits 0 across all 5 extensions because the grep walker excludes the in-flight phase dir and the 5 historical-prose files are explicitly logged as `specialCases` (the audit consumer treats `specialCases` as "expected and acceptable" rather than "must-fix"). If the executor discovers a NEW live-API reference (not in the truth #15-19 list) during execution, that reference IS in-scope for rename — surface it in the audit JSON `byExtension` array AND rename it; do NOT silently add it to `specialCases`.
    - Commit as a single atomic commit titled `refactor(15-01): hard-rename rootCommits → rootRevisions across SDK adapter surface + CJS callers + tests` — this commit MUST be the next commit after Task 1's audit-sidecar commit (D-12 adjacency invariant; no third commit between).

    Then run `pnpm --filter sdk build` (regenerates `sdk/dist-cjs/`) — verify `grep -c rootRevisions sdk/dist-cjs/vcs/types.d.ts` reports a non-zero count AND `grep -c rootCommits sdk/dist-cjs/vcs/types.d.ts` reports 0. Commit the regenerated `dist-cjs/` if it differs from the prior state, titled `build(15-01): regenerate dist-cjs after rootRevisions rename`.
  </action>
  <verify>
    <automated>cd sdk &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm vitest run src/vcs/__tests__/git-backend.test.ts src/vcs/__tests__/jj-refs.test.ts src/vcs/__tests__/jj-skeleton.test.ts src/vcs/__tests__/baseline-parity.test.ts --reporter=dot &amp;&amp; cd .. &amp;&amp; for ext in ts cjs js md json; do count=$(grep -rln "\brootCommits\b" --include="*.$ext" --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.jj --exclude-dir=dist-cjs --exclude-dir=".archive-pre-v1.4" --exclude-dir="v1.2-research" --exclude-dir=research --exclude-dir=intel --exclude-dir=milestones --exclude-dir=seeds --exclude-dir="15-adapter-surface-extensions-rename" . 2>/dev/null | wc -l | tr -d ' '); test "$count" = "0" || { echo "FAIL: $ext has $count remaining"; exit 1; }; done; echo "5-extension grep gate PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\\brootRevisions\\b' sdk/src/vcs/types.ts` returns >= 1 (interface declaration present).
    - `grep -c '\\brootCommits\\b' sdk/src/vcs/types.ts` returns 0.
    - `grep -c "'refs.rootRevisions'" sdk/src/vcs/backends.ts` returns 1.
    - `grep -c "'refs.rootCommits'" sdk/src/vcs/backends.ts` returns 0.
    - `grep -c '\\brootRevisions\\b' sdk/src/vcs/backends/jj.ts` returns >= 1.
    - `grep -c '\\brootCommits\\b' sdk/src/vcs/backends/jj.ts` returns 0.
    - `grep -c '\\brootRevisions\\b' sdk/src/vcs/backends/git.ts` returns >= 2 (const def + spread short-hand).
    - `grep -c '\\brootCommits\\b' sdk/src/vcs/backends/git.ts` returns 0.
    - `grep -c '\\brootCommits\\b' get-shit-done/bin/lib/commands.cjs` returns 0 (v1.2 retro CR-01 surface fixed).
    - `cd sdk && pnpm tsc --noEmit` exits 0.
    - `cd sdk && pnpm vitest run src/vcs/__tests__/git-backend.test.ts src/vcs/__tests__/jj-refs.test.ts src/vcs/__tests__/jj-skeleton.test.ts src/vcs/__tests__/baseline-parity.test.ts --reporter=dot` exits 0.
    - Per-extension grep gate exits 0 across `*.ts`/`*.cjs`/`*.js` (excluding carve-outs) — D-10 contract.
    - The rename commit is the IMMEDIATE next commit after Task 1's audit-sidecar commit (D-12 adjacency invariant) — verifiable via `jj log -n 5` showing `audit` then `hard-rename` adjacency.
    - `sdk/dist-cjs/vcs/types.d.ts` contains `rootRevisions` and zero `rootCommits` references after `pnpm --filter sdk build`.
  </acceptance_criteria>
  <done>Hard rename atomic-commit landed adjacent to audit sidecar. TSC green, focused vitest green, per-extension grep gate green. `dist-cjs/` regenerated. CJS surface (commands.cjs v1.2 retro CR-01 site) flipped. Capability-matrix string-literal flipped at backends.ts:79.</done>
</task>

<task type="auto">
  <name>Task 3: Add capability-matrix regression test for the rootRevisions/rootCommits flip</name>
  <files>sdk/src/vcs/__tests__/backends.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/backends.test.ts (open to see existing test shape, imports, describe blocks; if no file exists yet, the test will be a new file)
    - sdk/src/vcs/backends.ts (lines 42-138 BACKENDS_AVAILABLE_FOR_VERB export shape)
  </read_first>
  <action>
    Add (or create) a regression-test block in `sdk/src/vcs/__tests__/backends.test.ts` per VALIDATION line 77 + must_haves line "Capability matrix key `BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions']` exists":

    If `backends.test.ts` does NOT exist, create it with the standard vitest header:
    `import { describe, it, expect } from 'vitest';`
    `import { BACKENDS_AVAILABLE_FOR_VERB } from '../backends.js';`

    Add a `describe('BACKENDS_AVAILABLE_FOR_VERB capability matrix — Phase 15.01 rootCommits → rootRevisions flip (Pitfall 3 / v1.2 retro CR-01)', () => {...})` block containing:

    - `it('exposes refs.rootRevisions for both backends', () => { ... })` — asserts `BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions']` is defined AND `Array.from(BACKENDS_AVAILABLE_FOR_VERB['refs.rootRevisions'])` equals `['git', 'jj-colocated']` (exact array equality via `expect(...).toEqual([...])`).

    - `it('removes the prior refs.rootCommits string-literal key (anti-assertion)', () => { ... })` — asserts `BACKENDS_AVAILABLE_FOR_VERB['refs.rootCommits']` is `undefined` (`expect(BACKENDS_AVAILABLE_FOR_VERB['refs.rootCommits']).toBeUndefined()`). This is the load-bearing anti-assertion: TSC does NOT catch a string-literal key reintroduction (a future contributor could re-add the old key); this test does.

    Commit as `test(15-01): regression test for rootRevisions capability-matrix flip` (single commit; SDK suite stays green throughout). The audit-then-rename-then-test commit sequence is the v1.2 NAMING precedent for hard-rename + regression-guard pattern.
  </action>
  <verify>
    <automated>cd sdk &amp;&amp; pnpm vitest run src/vcs/__tests__/backends.test.ts -t "Phase 15.01 rootCommits → rootRevisions flip" --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/vcs/__tests__/backends.test.ts` exists and imports `BACKENDS_AVAILABLE_FOR_VERB` from `'../backends.js'`.
    - File contains a `describe(...)` block whose first argument substring includes `Phase 15.01` AND `rootCommits` AND `rootRevisions`.
    - The describe block contains at least 2 `it(...)` blocks: one asserting `refs.rootRevisions` presence/value, one asserting `refs.rootCommits` absence (anti-assertion).
    - The presence assertion uses `expect(...).toEqual(['git', 'jj-colocated'])` (or strict-equivalent shape comparison) — not just `.toBeDefined()`.
    - The anti-assertion uses `expect(BACKENDS_AVAILABLE_FOR_VERB['refs.rootCommits']).toBeUndefined()`.
    - `cd sdk && pnpm vitest run src/vcs/__tests__/backends.test.ts -t "Phase 15.01 rootCommits → rootRevisions flip" --reporter=dot` exits 0.
    - Working copy is clean after the commit.
  </acceptance_criteria>
  <done>Regression test landed. The capability-matrix string-literal flip is now guarded against re-introduction. Plan 15.01 ready for fan-in.</done>
</task>

</tasks>

<verification>
- TypeScript build (`cd sdk && pnpm tsc --noEmit`) is green after Task 2.
- Focused SDK suite (`cd sdk && pnpm vitest run src/vcs/__tests__/git-backend.test.ts src/vcs/__tests__/jj-refs.test.ts src/vcs/__tests__/jj-skeleton.test.ts src/vcs/__tests__/baseline-parity.test.ts src/vcs/__tests__/backends.test.ts --reporter=dot`) is green after Task 3.
- Per-extension grep gate (D-10) returns 0 across `*.ts`/`*.cjs`/`*.js`/`*.md`/`*.json` (5 extensions per ROADMAP SC1; excluding carve-outs per CONTEXT D-13 — the 5 historical-prose-carve-out files are explicitly logged in audit JSON `specialCases` so the gate ignores them).
- Audit JSON sidecar at `.planning/phases/15-adapter-surface-extensions-rename/rootCommits-rename-audit.json` exists with all 6 schema fields populated and `specialCases` surfacing `backends.ts:79`.
- `dist-cjs/` regenerated; `sdk/dist-cjs/vcs/types.d.ts` references `rootRevisions` and contains zero `rootCommits` references.
- Three commits in adjacency order: `chore(15-01): pre-rename rootCommits audit + sidecar` → `refactor(15-01): hard-rename rootCommits → rootRevisions ...` → `test(15-01): regression test for rootRevisions capability-matrix flip`. (Optional 4th: `build(15-01): regenerate dist-cjs after rootRevisions rename`.) D-12 audit/rename adjacency invariant holds.
- Working copy clean throughout (no uncommitted `.planning/` files; `assert_clean_wc` per `feedback_workflow_assert_clean_wc`).
</verification>

<success_criteria>
- ROADMAP Phase 15 SC1 satisfied: `grep -rn '\\brootCommits\\b'` across `*.ts`/`*.cjs`/`*.js`/`*.md`/`*.json` (excluding CONTEXT D-13 carve-outs: `node_modules`, `.git`, `.jj`, `dist-cjs`, `.archive-pre-v1.4`, `v1.2-research`, `.planning/research`, `.planning/intel`, `.planning/milestones`, `.planning/seeds`, and the in-flight `.planning/phases/15-adapter-surface-extensions-rename/` directory) returns 0 hits after the rename. The 5 in-scope `.md`/`.json` historical-prose-carve-out files (PROJECT.md, STATE.md, ROADMAP.md, REQUIREMENTS.md, 14.1-01-SUMMARY.md) are logged in the audit JSON `specialCases` array with `kind: "historical-prose-carve-out"` and a per-file reason; the gate treats them as expected-and-acceptable rather than must-fix.
- Pre-rename JSON sidecar exists at the slug-matched path (deviation from CONTEXT D-09 numeric-dir form recorded in `<deviations>`).
- `byExtension` schema groups hits per CONTEXT D-09; `specialCases` surfaces `backends.ts:79`; `idempotencyHash` MD5 present.
- Capability-matrix string-literal flipped at `sdk/src/vcs/backends.ts:79` per Pitfall 3 (v1.2 retro CR-01 prevention).
- Hard rename atomic-commit per Pattern 1 / CF-02 — no deprecation alias.
- Plan 15.02 (idAlphabet) and 15.03 (matchPrefix) inherit a clean `rootRevisions`-only namespace in `types.ts` / `backends.ts` / both backend files (no rebase against in-flight rename).
- New regression test in `backends.test.ts` guards against future re-introduction of the `'refs.rootCommits'` string literal.
</success_criteria>

<output>
Create `.planning/phases/15-adapter-surface-extensions-rename/15-01-SUMMARY.md` when done. Include:
- Three commit IDs (audit-sidecar / hard-rename / regression-test) in adjacency order
- The audit JSON `totalCount` (live figure; PATTERNS N3 expects 28+)
- Grep-gate output (per-extension counts post-rename — all 0)
- Confirmation that `dist-cjs/` regenerated cleanly
- Any deviations from CONTEXT D-09 path form recorded (slug-matched vs numeric-dir)
</output>
</content>
</invoke>