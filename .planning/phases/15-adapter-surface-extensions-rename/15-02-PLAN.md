---
phase: 15-adapter-surface-extensions-rename
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - sdk/src/vcs/types.ts
  - sdk/src/vcs/backends.ts
  - sdk/src/vcs/backends/git.ts
  - sdk/src/vcs/backends/jj.ts
  - sdk/src/vcs/__tests__/adapter-contract.test.ts
  - sdk/src/vcs/__tests__/backends.test.ts
autonomous: true
requirements: [VCS-21]
requirements_addressed: [VCS-21]
tags: [adapter-surface, refs, alphabet, idAlphabet, sdk, vcs]

must_haves:
  truths:
    - "On the git adapter, `vcs.refs.idAlphabet === '0-9a-f'` (CF-03 opaque `readonly string`)."
    - "On the jj adapter, `vcs.refs.idAlphabet === 'k-z'` (CF-03 opaque `readonly string`)."
    - "`vcs.refs.idAlphabet` is a readonly string property (NOT a method); shape matches existing `head` / `parent` adjacent readonly properties."
    - "`BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']` is `['git', 'jj-colocated']` (A4 — required for the `test.skipIf(!ready('refs.idAlphabet'))` gate in the cross-backend test harness; resolves Open Question 4 from RESEARCH)."
    - "The structured `{kind, chars, minLen, maxLen}` alternative is REJECTED — adapter returns opaque string only (CF-03 / `project_unified_revision_model` invariant preserved)."
    - "Empirical alphabet provenance is recorded — git hex matches `expr.ts:35`/`format-migration/rewrite.ts:53`'s `[0-9a-f]` regex; jj k-z matches `jj-id-alphabet-probe.test.ts:49-75`'s probe and `format-migration/rewrite.ts:63`'s `[k-z]` regex."
  artifacts:
    - path: "sdk/src/vcs/types.ts"
      provides: "`VcsRefs` interface extended with `readonly idAlphabet: string;` adjacent to `readonly parent`."
      contains: "readonly idAlphabet: string;"
    - path: "sdk/src/vcs/backends.ts"
      provides: "Capability matrix entry `'refs.idAlphabet': Object.freeze(['git', 'jj-colocated'] as const)` adjacent to existing refs entries."
      contains: "'refs.idAlphabet':"
    - path: "sdk/src/vcs/backends/git.ts"
      provides: "`idAlphabet: '0-9a-f'` added to the `refs` Object.freeze block at ~:554."
      contains: "idAlphabet: '0-9a-f'"
    - path: "sdk/src/vcs/backends/jj.ts"
      provides: "`idAlphabet: 'k-z'` added to the `refs` Object.freeze block at ~:776."
      contains: "idAlphabet: 'k-z'"
    - path: "sdk/src/vcs/__tests__/adapter-contract.test.ts"
      provides: "Cross-backend test asserting `vcs.refs.idAlphabet` matches the backend's known alphabet."
    - path: "sdk/src/vcs/__tests__/backends.test.ts"
      provides: "Presence assertion `expect(BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']).toEqual(['git', 'jj-colocated'])`."
  key_links:
    - from: "sdk/src/vcs/__tests__/adapter-contract.test.ts"
      to: "sdk/src/vcs/backends/{git,jj}.ts"
      via: "createVcsAdapter -> vcs.refs.idAlphabet readonly property access"
      pattern: "vcs\\.refs\\.idAlphabet"
    - from: "sdk/src/vcs/backends.ts"
      to: "sdk/src/vcs/types.ts"
      via: "string-keyed capability matrix mirroring the new readonly property"
      pattern: "'refs.idAlphabet':"
---

<objective>
Add `vcs.refs.idAlphabet: string` to the cross-backend `VcsAdapter` surface — opaque char-class regex body (`'0-9a-f'` on git, `'k-z'` on jj per CF-03). Single readonly property addition on both backends; cross-backend contract test asserts backend-specific value via `test.skipIf(!ready('refs.idAlphabet'))` gate.

Purpose: Replaces the three ad-hoc duplications of the alphabet at `expr.ts:35`, `format-migration/rewrite.ts:53,63`, `parse/jj-id.ts` (the optional consumer refactor is DEFERRED to Phase 17 per CF-03 — Phase 15 ships only the canonical source).

Output: 6 file diffs (types.ts interface declaration + backends.ts matrix entry + both backend impls + adapter-contract test + backends.test presence assertion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md
@.planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md
@.planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-VALIDATION.md
@.planning/phases/15-adapter-surface-extensions-rename/15-01-SUMMARY.md
@sdk/src/vcs/types.ts
@sdk/src/vcs/backends.ts
@sdk/src/vcs/backends/git.ts
@sdk/src/vcs/backends/jj.ts
@sdk/src/vcs/__tests__/adapter-contract.test.ts
@sdk/src/vcs/__tests__/backends.test.ts
@sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts
@sdk/src/vcs/format-migration/rewrite.ts
@sdk/src/vcs/expr.ts

<interfaces>
<!-- Verified insertion points -->

From sdk/src/vcs/types.ts:336-367 (VcsRefs interface — post-15.01 state):
  export interface VcsRefs {
    readonly head: RevisionExpr;
    readonly parent: RevisionExpr;
    // 15.02 inserts here: `readonly idAlphabet: string;`
    bookmarks: VcsBookmarks;
    ...
    rootRevisions(opts: { rev?: RevisionExpr }): string[];    // post-15.01 rename
    ...
  }

From sdk/src/vcs/backends/git.ts:554-568 (refs Object.freeze block — post-15.01):
  const refs = Object.freeze({
    head: expr.head(),
    parent: expr.parent(),
    // 15.02 inserts here: `idAlphabet: '0-9a-f',`
    bookmarks,
    ...
    rootRevisions,    // post-15.01 (was rootCommits)
    ...
  });

From sdk/src/vcs/backends/jj.ts:776-980 (refs Object.freeze block — post-15.01):
  const refs: VcsRefs = Object.freeze({
    head: expr.head(),
    parent: expr.parent(),
    // 15.02 inserts here: `idAlphabet: 'k-z',`
    bookmarks,
    ...
    rootRevisions: ({rev}) => { ... },    // post-15.01
    ...
  });

From sdk/src/vcs/backends.ts:76-138 (capability matrix block):
  'refs.currentBookmarks': Object.freeze(['git', 'jj-colocated'] as const),
  'refs.resolveShort': Object.freeze(['git', 'jj-colocated'] as const),
  'refs.countCommits': Object.freeze(['git', 'jj-colocated'] as const),
  'refs.rootRevisions': Object.freeze(['git', 'jj-colocated'] as const),    // post-15.01 (was 'refs.rootCommits')
  // 15.02 inserts here: `'refs.idAlphabet': Object.freeze(['git', 'jj-colocated'] as const),`
  'refs.exists': Object.freeze(['git', 'jj-colocated'] as const),
  ...

From sdk/src/vcs/__tests__/adapter-contract.test.ts (cross-backend test harness shape):
  describe.for(selectedBackends())('VcsAdapter contract — backend=%s', (kind) => {
    const { test, setupHooks } = makeBackendFixture(kind);
    setupHooks();
    const ready = (verb: string): boolean => verbReady(verb, kind);

    test.skipIf(!ready('refs.idAlphabet'))('vcs.refs.idAlphabet returns backend-specific alphabet', ({ vcs }) => {
      const a = vcs.refs.idAlphabet;
      if (vcs.kind === 'git') expect(a).toBe('0-9a-f');
      else expect(a).toBe('k-z');
    });
  });

From sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts:49-75 (empirical alphabet provenance for jj):
  // probes jj-generated change_ids; confirmed jj 0.41 emits [k-z] lowercase only.

From sdk/src/vcs/format-migration/rewrite.ts:53,63 (existing alphabet regex literals — canonical sources):
  const GIT_SHA_RE = /[0-9a-f]{7,40}/;        // hex
  const JJ_CID_RE = /[k-z]{8,12}/;            // k-z lowercase only

From sdk/src/vcs/expr.ts:35,41 (SHA-or-changeId permissive regex):
  const SHA_OR_CHANGE_ID_RE = /^[0-9a-fA-F]{4,40}$|^[k-z]{4,40}$/;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `idAlphabet` readonly string to VcsRefs interface, both backends, capability matrix, and cross-backend test</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/backends.ts, sdk/src/vcs/backends/git.ts, sdk/src/vcs/backends/jj.ts, sdk/src/vcs/__tests__/adapter-contract.test.ts, sdk/src/vcs/__tests__/backends.test.ts</files>
  <read_first>
    - sdk/src/vcs/types.ts (lines 336-367 — VcsRefs interface with `readonly head`/`readonly parent` adjacent insertion point at line 339)
    - sdk/src/vcs/backends.ts (lines 70-138 — capability matrix; identify the insertion line adjacent to `'refs.rootRevisions':` post-15.01)
    - sdk/src/vcs/backends/git.ts (lines 554-568 — refs Object.freeze block; insertion adjacent to `parent: expr.parent()` at ~:556)
    - sdk/src/vcs/backends/jj.ts (lines 776-980 — refs Object.freeze block; insertion adjacent to `parent: expr.parent()` at ~:778)
    - sdk/src/vcs/__tests__/adapter-contract.test.ts (cross-backend test pattern — read the existing `describe.for(selectedBackends())` block and its `test.skipIf(!ready('verb'))` shape; new `idAlphabet` test follows this exact form)
    - sdk/src/vcs/__tests__/backends.test.ts (presence-assertion pattern — read the existing `expect(BACKENDS_AVAILABLE_FOR_VERB['refs.bookmarks.list']).toEqual([...])` shape to mirror)
    - sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts (empirical jj alphabet evidence — confirms `'k-z'` value)
    - sdk/src/vcs/format-migration/rewrite.ts (lines 45-65 — existing GIT_SHA_RE + JJ_CID_RE alphabet regex literals; the new `idAlphabet` is the canonical source these may consume in Phase 17)
    - .planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md (CF-03 opaque string decision; rejected structured form)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (§`sdk/src/vcs/types.ts` MODIFIED block; §`sdk/src/vcs/__tests__/adapter-contract.test.ts` MODIFIED block; §`backends.test.ts` MODIFIED block)
  </read_first>
  <behavior>
    - Test 1 (git alphabet): on git adapter created via `createVcsAdapter(tmpDir)` over a `git init`'d tmpdir, `vcs.refs.idAlphabet === '0-9a-f'`.
    - Test 2 (jj alphabet): on jj adapter created via `createVcsAdapter(tmpDir)` over a `jj git init --colocate`'d tmpdir, `vcs.refs.idAlphabet === 'k-z'`.
    - Test 3 (readonly property — not a method): TypeScript compiler accepts `vcs.refs.idAlphabet` as a string, NOT as a function. `typeof vcs.refs.idAlphabet === 'string'` in runtime, never a function (CF-03 / `project_unified_revision_model`).
    - Test 4 (capability matrix presence): `BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']` deep-equals `['git', 'jj-colocated']`.
    - Test 5 (verbReady gate works): `test.skipIf(!ready('refs.idAlphabet'))` does NOT skip on either backend (because both lanes admit the verb).
    - The value strings `'0-9a-f'` and `'k-z'` are stored as literal property values — NOT computed from regex.source slicing. JSDoc on the new `idAlphabet` declaration in types.ts cites the empirical provenance (jj-id-alphabet-probe.test.ts for jj; expr.ts/format-migration/rewrite.ts for git).
  </behavior>
  <action>
    In `sdk/src/vcs/types.ts`, locate the VcsRefs interface (begins around line 336). Add a new property declaration `readonly idAlphabet: string;` immediately after `readonly parent: RevisionExpr;` (line ~338) and before `bookmarks: VcsBookmarks;` (line ~339). Add a JSDoc block above the declaration framing the value as "Opaque character class regex body identifying the backend's revision-id alphabet. Git: '0-9a-f' (hex, case-insensitive matching per `core.abbrev`); jj: 'k-z' (lowercase only, per the change-id prefix index)." and noting "Phase 15 (VCS-21, CF-03): opaque `readonly string`; the structured `{kind, chars, minLen, maxLen}` alternative was rejected to preserve the unified-revision-model invariant (`project_unified_revision_model`)." Reference empirical sources by file (`sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts` for jj; `sdk/src/vcs/format-migration/rewrite.ts:53,63` for both).

    In `sdk/src/vcs/backends.ts`, locate the capability matrix block (lines ~70-138). Find the line containing `'refs.rootRevisions': Object.freeze(['git', 'jj-colocated'] as const),` (post-15.01 rename target). Add a new entry on the line immediately following: `'refs.idAlphabet': Object.freeze(['git', 'jj-colocated'] as const),`. This satisfies A4 — without this entry, the `test.skipIf(!ready('refs.idAlphabet'))` gate in `adapter-contract.test.ts` would skip every new test.

    In `sdk/src/vcs/backends/git.ts`, locate the refs Object.freeze block (lines ~554-568). Add the property entry `idAlphabet: '0-9a-f',` immediately after `parent: expr.parent(),` and before `bookmarks,` — adjacent to the readonly-property block at the top of the freeze object. This is a literal string assignment, NOT computed from a regex.

    In `sdk/src/vcs/backends/jj.ts`, locate the refs Object.freeze block (lines ~776-780). Add the property entry `idAlphabet: 'k-z',` immediately after `parent: expr.parent(),` and before `bookmarks,`. Same literal-string shape as git side.

    In `sdk/src/vcs/__tests__/adapter-contract.test.ts`, insert a new `test.skipIf(!ready('refs.idAlphabet'))` block inside the existing `describe.for(selectedBackends())(...)` body. The test reads `const a = vcs.refs.idAlphabet;` then branches on `vcs.kind`: if `'git'` expect `'0-9a-f'`, else expect `'k-z'`. Match the existing pattern shape verbatim — refer to the read_first file for the exact `test.skipIf` + arrow-function destructure (`({ vcs }) => { ... }`) form already used by sibling tests.

    In `sdk/src/vcs/__tests__/backends.test.ts`, add an `expect(BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']).toEqual(['git', 'jj-colocated']);` assertion adjacent to the existing capability-matrix presence assertions (e.g., next to the new `'refs.rootRevisions'` assertion added in plan 15.01). If a keys-count assertion exists in this file, bump the expected count by +1 (because this plan adds exactly one new matrix key).

    Commit via `gsd-sdk query commit` with message `"feat($PHASE): add vcs.refs.idAlphabet readonly property (VCS-21)"`.
  </action>
  <verify>
    <automated>pnpm --filter sdk test -- --run --no-coverage -t "vcs.refs.idAlphabet" &amp;&amp; pnpm --filter sdk test -- --run --no-coverage -t "BACKENDS_AVAILABLE_FOR_VERB" &amp;&amp; pnpm --filter sdk build &amp;&amp; grep -c 'idAlphabet' sdk/dist-cjs/vcs/types.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter sdk test -- --run -t "vcs.refs.idAlphabet"` exits 0 with both git ('0-9a-f') and jj ('k-z') assertions green
    - `pnpm --filter sdk test -- --run -t "BACKENDS_AVAILABLE_FOR_VERB"` exits 0 with the new `refs.idAlphabet` presence assertion green
    - `grep -n "readonly idAlphabet: string" sdk/src/vcs/types.ts` returns at least 1 line (interface declaration present)
    - `grep -n "'refs.idAlphabet':" sdk/src/vcs/backends.ts` returns exactly 1 line
    - `grep -n "idAlphabet: '0-9a-f'" sdk/src/vcs/backends/git.ts` returns exactly 1 line
    - `grep -n "idAlphabet: 'k-z'" sdk/src/vcs/backends/jj.ts` returns exactly 1 line
    - `pnpm --filter sdk test -- --run --no-coverage` (full SDK suite) exits 0
    - `grep -c 'idAlphabet' sdk/dist-cjs/vcs/types.d.ts` >= 1 (compiled `.d.ts` carries the new property)
    - In tsc strict mode, attempting `vcs.refs.idAlphabet = 'foo'` would fail at compile time (readonly enforcement — verified implicitly by the `readonly` modifier; do NOT add a runtime test that mutates and catches, since `Object.freeze` would throw)
  </acceptance_criteria>
  <done>idAlphabet ships on both backends as opaque readonly string; cross-backend test green on both lanes; capability matrix presence verified; dist-cjs rebuilt and carries the new property.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter readonly-property contract | The `idAlphabet` value is a literal string; consumers may regex against it (e.g., `new RegExp('^[' + alphabet + ']+$')`). The opaque-string promise means consumers don't switch on backend.kind (preserves `project_unified_revision_model`). |
| Capability-matrix presence | The `BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']` entry gates the `test.skipIf(!ready('refs.idAlphabet'))` flow in adapter-contract.test.ts — missing entry = silent test skip. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.02-01 | Tampering | `BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']` capability-matrix entry | mitigate | Acceptance criterion `grep -n "'refs.idAlphabet':" sdk/src/vcs/backends.ts` exit 0 + presence assertion in backends.test.ts (A4 mitigation — without the entry, `ready('refs.idAlphabet')` returns false and the cross-backend test silently skips). |
| T-15.02-02 | Tampering | Drift between literal string value and empirical alphabet | mitigate | The empirical jj `[k-z]` alphabet is recorded in `jj-id-alphabet-probe.test.ts:49-75` (jj 0.41 confirmed); git `[0-9a-f]` is enforced by `format-migration/rewrite.ts:53` GIT_SHA_RE. JSDoc on `idAlphabet` cites both sources so future changes (e.g., jj alphabet widening) prompt a sync. |
| T-15.02-SC | Tampering | npm/pip/cargo installs in this plan | accept | Phase 15 introduces ZERO new packages (RESEARCH §Package Legitimacy Audit); no install verbs in plan; gate trivially satisfied. |
</threat_model>

<verification>
**Plan-level gates:**

1. `pnpm --filter sdk test -- --run --no-coverage` exits 0 (full SDK suite — covers the new cross-backend test + presence assertion)
2. `grep -c 'idAlphabet' sdk/dist-cjs/vcs/types.d.ts >= 1` after `pnpm --filter sdk build`
3. The plan 15.01 SUMMARY exists at `.planning/phases/15-adapter-surface-extensions-rename/15-01-SUMMARY.md` (depends_on prerequisite — plan 15.02 should never start before 15.01 commits)
</verification>

<success_criteria>
**Measurable completion (Phase 15 SC2 / VCS-21):**

- `vcs.refs.idAlphabet === '0-9a-f'` on git adapter (cross-backend test green)
- `vcs.refs.idAlphabet === 'k-z'` on jj adapter (cross-backend test green)
- The property is a readonly string (NOT a function, NOT a structured object — CF-03 / `project_unified_revision_model` invariant preserved)
- Capability matrix entry is present (`BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet'] === ['git', 'jj-colocated']`)
- Full SDK suite green; dist-cjs rebuilt with new property
- Existing alphabet regex consumers (`expr.ts:35`, `format-migration/rewrite.ts:53,63`) are LEFT IN PLACE per CF-03 — optional refactor deferred to Phase 17 docs-drift batch
</success_criteria>

<output>
Create `.planning/phases/15-adapter-surface-extensions-rename/15-02-SUMMARY.md` when done. Capture:
- Exact insertion line numbers (post-15.01 reflow may shift lines)
- JSDoc text adopted on the new declaration
- Output of `pnpm --filter sdk test -- --run -t "vcs.refs.idAlphabet"` (test pass count + duration)
- Acknowledgment that the Phase 17 consumer-refactor for `expr.ts`/`format-migration/rewrite.ts` was DEFERRED per CF-03
</output>
</content>
</invoke>