---
phase: 15-adapter-surface-extensions-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01-adapter-surface-extensions-rename
files_modified:
  - sdk/src/vcs/types.ts
  - sdk/src/vcs/backends.ts
  - sdk/src/vcs/backends/jj.ts
  - sdk/src/vcs/backends/git.ts
  - sdk/src/vcs/__tests__/adapter-contract.test.ts
  - sdk/src/vcs/__tests__/backends.test.ts
autonomous: true
requirements:
  - VCS-21
requirements_addressed:
  - VCS-21
tags:
  - vcs-adapter
  - introspection
  - alphabet
  - refs-namespace
user_setup: []

must_haves:
  truths:
    - "TypeScript interface `VcsRefs` declares a new `readonly idAlphabet: string;` property mirroring the style of the existing `readonly head: RevisionExpr;` / `readonly parent: RevisionExpr;` slots at `sdk/src/vcs/types.ts:337-338`."
    - "JSDoc on `idAlphabet` frames the value as an opaque character-class regex body substring per CF-03 — consumers compose `new RegExp('^[' + vcs.refs.idAlphabet + ']+$')`. Does NOT promise structured enum/length/kind metadata (the rejected FEATURES.md alternative)."
    - "jj backend body at `sdk/src/vcs/backends/jj.ts` returns the literal string `'k-z'` from `vcs.refs.idAlphabet` (per CF-03 + RESEARCH §Example 2)."
    - "git backend body at `sdk/src/vcs/backends/git.ts` returns the literal string `'0-9a-f'` from `vcs.refs.idAlphabet` (per CF-03 + RESEARCH §Example 2)."
    - "Capability-matrix key `BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']` exists at `sdk/src/vcs/backends.ts` and equals `Object.freeze(['git', 'jj-colocated'] as const)`. The string-key add is invisible to TSC; backends.test.ts regression-asserts presence."
    - "Cross-backend adapter contract test in `sdk/src/vcs/__tests__/adapter-contract.test.ts` asserts `vcs.refs.idAlphabet === '0-9a-f'` for git and `=== 'k-z'` for jj using the existing `describe.for(selectedBackends())` shape at lines 28-46."
    - "Backends regression test in `sdk/src/vcs/__tests__/backends.test.ts` asserts the new `'refs.idAlphabet'` capability-matrix key is defined and equals `['git', 'jj-colocated']` (Pitfall 1 / Pitfall 3 prevention — TSC does not catch string-key omissions)."
    - "No existing alphabet regex consumers (`expr.ts:41`, `format-migration/rewrite.ts:53,63`) are touched in this plan — refactor to consume the new `idAlphabet` is explicitly deferred (Phase 17 docs-drift or later per CONTEXT Deferred Ideas)."
    - "`pnpm tsc --noEmit` is green after the type addition; `pnpm vitest run src/vcs/__tests__/adapter-contract.test.ts src/vcs/__tests__/backends.test.ts` is green after Task 3."
  artifacts:
    - path: "sdk/src/vcs/types.ts"
      provides: "VcsRefs.idAlphabet property declaration (readonly string)"
      contains: "readonly idAlphabet:"
    - path: "sdk/src/vcs/backends/jj.ts"
      provides: "jj backend idAlphabet wire-in returning 'k-z'"
      contains: "idAlphabet: 'k-z'"
    - path: "sdk/src/vcs/backends/git.ts"
      provides: "git backend idAlphabet wire-in returning '0-9a-f'"
      contains: "idAlphabet: '0-9a-f'"
    - path: "sdk/src/vcs/backends.ts"
      provides: "Capability matrix entry 'refs.idAlphabet'"
      contains: "'refs.idAlphabet'"
    - path: "sdk/src/vcs/__tests__/adapter-contract.test.ts"
      provides: "Cross-backend adapter assertion that idAlphabet returns the expected per-backend literal"
      contains: "vcs.refs.idAlphabet"
  key_links:
    - from: "sdk/src/vcs/types.ts (VcsRefs.idAlphabet)"
      to: "sdk/src/vcs/backends/{git,jj}.ts (per-backend literal returns)"
      via: "satisfies-style structural typing on the refs Object.freeze"
      pattern: "idAlphabet:\\s+'(0-9a-f|k-z)'"
    - from: "sdk/src/vcs/backends.ts capability matrix entry"
      to: "Runtime verb resolution"
      via: "string-key lookup BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']"
      pattern: "'refs.idAlphabet':"
    - from: "sdk/src/vcs/__tests__/adapter-contract.test.ts cross-backend assertion"
      to: "Both backend bodies"
      via: "vitest describe.for over backend kinds"
      pattern: "describe\\.for\\(selectedBackends\\(\\)\\)"
---

<objective>
Plan 15.02 (VCS-21) adds the public `vcs.refs.idAlphabet` introspection property on both backends. Three-line additive change on `VcsRefs` interface + one literal-string wire-in per backend + one capability-matrix row + one cross-backend adapter-contract assertion. Pure metadata (no exec call, no compute) — backends return a per-backend constant `'0-9a-f'` (git hex commit_id) / `'k-z'` (jj reverse-base32 change_id) for consumers to compose into regex patterns (`new RegExp('^[' + vcs.refs.idAlphabet + ']+$')`).

Purpose: replaces the three ad-hoc duplications in `expr.ts:41`, `format-migration/rewrite.ts:53,63`, `parse/jj-id.ts` with a single canonical source per backend (cross-backend unified per `project_unified_revision_model`). Consumed by Plan 15.03's `matchPrefix` body — `matchPrefix` reads `this.idAlphabet` to drive the alphabet-disjointness gate. Plan 15.02 ships the metadata; Plan 15.03 consumes it.

Output:
- New `readonly idAlphabet: string;` slot in `VcsRefs` interface at `sdk/src/vcs/types.ts:337-338`-style position (per RESEARCH §Pattern 2 — mirror `readonly head` styling)
- New `idAlphabet: 'k-z',` line in jj `refs = Object.freeze({...})` block at `sdk/src/vcs/backends/jj.ts:776+` (refs freeze block opens at line 776 per PATTERNS live anchor)
- New `idAlphabet: '0-9a-f',` line in git `refs = Object.freeze({...})` block at `sdk/src/vcs/backends/git.ts:554+`
- New `'refs.idAlphabet': Object.freeze(['git', 'jj-colocated'] as const),` row in `BACKENDS_AVAILABLE_FOR_VERB` at `sdk/src/vcs/backends.ts`
- New cross-backend assertion in `sdk/src/vcs/__tests__/adapter-contract.test.ts` inside the existing `describe.for(selectedBackends())` block
- New backends.test.ts assertion guarding the capability-matrix key presence

Sequential after 15.01 (file-overlap on `types.ts` + `backends.ts` + both backend files). Inherits a clean `rootRevisions`-only namespace from 15.01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md
@.planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md
@.planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-VALIDATION.md
@.planning/phases/15-adapter-surface-extensions-rename/15-01-SUMMARY.md
@sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts

<interfaces>
<!-- Live-source line anchors verified against the post-15.01 working tree. -->

VcsRefs interface (sdk/src/vcs/types.ts:336-367 post-15.01 rename):
- Line 337-338: `readonly head: RevisionExpr;` / `readonly parent: RevisionExpr;` — style precedent for new readonly idAlphabet
- Insertion point: after line 338 (before `bookmarks: VcsBookmarks;` at line 339), so the readonly properties cluster cleanly

jj backend refs freeze block (sdk/src/vcs/backends/jj.ts:776-980):
- Line 776: `const refs: VcsRefs = Object.freeze({`
- Lines 777-779: `head: expr.head(),` / `parent: expr.parent(),` / `bookmarks,`
- Insertion point: after line 779 (sibling of `parent: expr.parent()`)

git backend refs freeze block (sdk/src/vcs/backends/git.ts:554-568):
- Line 554: `const refs = Object.freeze({`
- Line 555-557: `head: expr.head(),` / `parent: expr.parent(),` / `bookmarks,`
- Insertion point: after line 557

Capability matrix (sdk/src/vcs/backends.ts:76-82):
- Existing cluster of `'refs.*'` keys; insertion point alongside `'refs.head'`-equivalent grouping (the existing `'refs.*'` cluster around lines 76-82 is fine)

adapter-contract.test.ts cross-backend block (existing, lines 28-46):
- `describe.for(selectedBackends())('VcsAdapter contract — backend=%s', (kind) => { ... })`
- `const ready = (verb: string): boolean => verbReady(verb, kind);` (skipIf guard)

jj-id-alphabet-probe.test.ts (existing — empirical disjointness probe):
- The 'k-z' alphabet is the verified jj change_id surface (probe asserts disjointness with `[0-9a-f]`)

RESEARCH Example 2 (15-RESEARCH.md:664-702) — verbatim wire-in shapes:
- `idAlphabet: 'k-z',` (jj)
- `idAlphabet: '0-9a-f',` (git)
- Both inserted near the top of the refs Object.freeze block, sibling of `head`/`parent`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `idAlphabet` string consumer → regex composition | Consumers compose `new RegExp('^[' + vcs.refs.idAlphabet + ']+$')`; string content must remain a valid `[...]` body |
| Capability-matrix string-literal key add → runtime verb resolution | New `'refs.idAlphabet'` key invisible to TSC; backends.test.ts regression-asserts presence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.02-01 | Tampering | `'refs.idAlphabet'` capability-matrix string-literal key omission | mitigate | backends.test.ts regression test asserts `BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']` is defined AND equals `['git', 'jj-colocated']`; mirrors Plan 15.01's anti-assertion pattern |
| T-15.02-02 | Tampering | Per-backend literal string drift (e.g. `'0-9A-F'` mixed-case, `'kz'` missing dash, `'a-z'` widened) | mitigate | adapter-contract.test.ts asserts exact `===` equality with the per-backend literal (`'0-9a-f'` / `'k-z'`); empirical jj-id-alphabet-probe.test.ts independently validates the k-z disjointness assumption against live jj output |
| T-15.02-03 | Information disclosure | `idAlphabet` widened to structured `{kind,chars,minLen,maxLen}` (FEATURES.md alternative) | mitigate | JSDoc explicitly frames the value as opaque-string-only per CF-03; structured-widening is OOS per CONTEXT Deferred Ideas |
| T-15.02-04 | Tampering | Existing alphabet regex consumers (`expr.ts:41`, `format-migration/rewrite.ts:53,63`) drifted from new canonical source | accept | Refactor explicitly deferred per CONTEXT Deferred Ideas (Phase 17 docs-drift or later); current consumers remain canonical until refactor |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add idAlphabet declaration to VcsRefs interface + capability-matrix entry + JSDoc</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/backends.ts</files>
  <read_first>
    - sdk/src/vcs/types.ts (lines 336-367 to confirm VcsRefs layout post-15.01 rename and the `readonly head: RevisionExpr;` styling precedent at line 337)
    - sdk/src/vcs/backends.ts (lines 75-82 to confirm the `'refs.*'` cluster post-15.01 rename — line 79 now shows `'refs.rootRevisions'`)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (Example 2 wire-in shapes at lines 664-702 + JSDoc template at lines 671-677)
  </read_first>
  <action>
    Insert a new `readonly idAlphabet: string;` property in the `VcsRefs` interface at `sdk/src/vcs/types.ts` immediately after `readonly parent: RevisionExpr;` (the existing readonly property at line 338). Add a JSDoc block above the new line per RESEARCH Example 2 (lines 671-677):

    `/**`
    ` * 15.02 (VCS-21): per-backend canonical id alphabet substring.`
    ` * Git: '0-9a-f' (hex commit_id). Jj: 'k-z' (reverse-base32 change_id).`
    ` * Consumers compose into regex patterns:`
    ` *   new RegExp('^[' + vcs.refs.idAlphabet + ']+$')`
    ` * Replaces three ad-hoc duplications in expr.ts:41, format-migration/rewrite.ts:53,63.`
    ` * Opaque-string per CF-03 — structured {kind,chars,minLen,maxLen} alternative rejected (YAGNI).`
    ` */`
    `readonly idAlphabet: string;`

    Then in `sdk/src/vcs/backends.ts` add a new capability-matrix row inside `BACKENDS_AVAILABLE_FOR_VERB` (the `Object.freeze({...})` block at lines 42-138). Insert near the `'refs.*'` cluster (around lines 76-82 post-15.01 rename) — for example after `'refs.rootRevisions'`:

    `'refs.idAlphabet': Object.freeze(['git', 'jj-colocated'] as const),`

    Run `cd sdk && pnpm tsc --noEmit` — must exit 1 with errors flagging missing `idAlphabet` in `backends/jj.ts` and `backends/git.ts` (the `satisfies VcsRefs` structural-typing gate). This is the EXPECTED FAILURE — it confirms the interface is wired into both backend bodies and the next task's wire-in is required.

    Commit as `feat(15-02): declare VcsRefs.idAlphabet contract + capability-matrix row` — the commit deliberately ships in a red TSC state until Task 2 wires the backend bodies. (Alternative: combine with Task 2 into a single TSC-green commit. Either path acceptable; the task ordering below assumes the combined-commit path for cleaner history but the planner notes the staged-red path as a valid alternative.)

    NOTE: If the planner prefers combined-commit ordering, defer the commit-and-tsc-noemit step until after Task 2 wires both backends; the two tasks together land as one green commit.
  </action>
  <verify>
    <automated>grep -c "readonly idAlphabet:" sdk/src/vcs/types.ts &amp;&amp; grep -c "'refs.idAlphabet'" sdk/src/vcs/backends.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'readonly idAlphabet:' sdk/src/vcs/types.ts` returns >= 1 (declaration present).
    - The JSDoc comment block immediately preceding the declaration mentions: `15.02`, `VCS-21`, `0-9a-f`, `k-z`, AND the regex-composition example string `new RegExp` — verifiable by grep on the file region.
    - `grep -c "'refs.idAlphabet'" sdk/src/vcs/backends.ts` returns 1 (single capability-matrix row).
    - The capability-matrix row pair (`['git', 'jj-colocated']`) is `Object.freeze`d (same shape as siblings) — verifiable by grep `"'refs.idAlphabet': Object.freeze\\(\\['git', 'jj-colocated'\\] as const\\),"`.
    - The declaration JSDoc does NOT mention `kind`/`minLen`/`maxLen`/`enum` (the rejected FEATURES.md structured-widening alternative; CF-03 enforcement). Verifiable by grep `-E 'minLen|maxLen' sdk/src/vcs/types.ts` returning zero lines within ~10 lines of `readonly idAlphabet:`.
    - If the planner ships this as a separate commit (red-TSC strategy), `cd sdk && pnpm tsc --noEmit` exits non-zero with errors flagging missing `idAlphabet` on both backends. If combined with Task 2 into one commit, TSC is green at commit time.
  </acceptance_criteria>
  <done>Interface declaration + capability-matrix entry shipped. Either staged-red or combined-green commit strategy acceptable; both backend wire-ins land in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Wire idAlphabet literals into both backend refs Object.freeze blocks</name>
  <files>sdk/src/vcs/backends/jj.ts, sdk/src/vcs/backends/git.ts</files>
  <read_first>
    - sdk/src/vcs/backends/jj.ts (lines 776-790 to confirm the refs Object.freeze opens at line 776 with `head`/`parent`/`bookmarks` as first siblings — insertion point is after line 779 `bookmarks,`)
    - sdk/src/vcs/backends/git.ts (lines 554-568 to confirm the refs Object.freeze opens at line 554 with the same shape — insertion point is after line 557 `bookmarks,`)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (Example 2 wire-in shapes at lines 682-702 + Pattern 2 styling note at lines 297-322)
  </read_first>
  <action>
    Add a single new line to the jj backend `refs = Object.freeze({...})` block in `sdk/src/vcs/backends/jj.ts` near the top of the freeze block (sibling of `head`/`parent`):

    `idAlphabet: 'k-z',`

    Add a single new line to the git backend `refs = Object.freeze({...})` block in `sdk/src/vcs/backends/git.ts`:

    `idAlphabet: '0-9a-f',`

    Both insertions are direct property assignments with the literal string per CF-03 — no helper function, no compute, no exec call. The values are RESEARCH-verified against the jj prefix index ('k-z' reverse-base32) and git core.abbrev / rev-parse alphabet ('0-9a-f' hex).

    Run `cd sdk && pnpm tsc --noEmit` — must exit 0. The new property satisfies the `VcsRefs` interface declared in Task 1; the structural typing closes.

    Commit as `feat(15-02): wire idAlphabet 'k-z' (jj) / '0-9a-f' (git) into refs Object.freeze blocks` OR combine with Task 1 into a single green commit titled `feat(15-02): add vcs.refs.idAlphabet introspection (VCS-21)`.
  </action>
  <verify>
    <automated>cd sdk &amp;&amp; pnpm tsc --noEmit &amp;&amp; cd .. &amp;&amp; grep -c "idAlphabet: 'k-z'" sdk/src/vcs/backends/jj.ts &amp;&amp; grep -c "idAlphabet: '0-9a-f'" sdk/src/vcs/backends/git.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "idAlphabet: 'k-z'" sdk/src/vcs/backends/jj.ts` returns exactly 1.
    - `grep -c "idAlphabet: '0-9a-f'" sdk/src/vcs/backends/git.ts` returns exactly 1.
    - The jj insertion sits inside the `refs = Object.freeze({...})` block (between line 776 `Object.freeze({` and the closing `})` of the refs block) — verifiable by checking the `idAlphabet` line number falls between those bookends via `grep -n`.
    - The git insertion sits inside the corresponding `refs = Object.freeze({...})` block at lines 554-568+ — same bookend verification.
    - `cd sdk && pnpm tsc --noEmit` exits 0 (structural typing closes; backends now satisfy the VcsRefs contract from Task 1).
    - Neither backend introduces helper functions, closures, or compute around `idAlphabet` — it is a bare literal string per CF-03. Verifiable by checking the line does NOT match `idAlphabet: \\(` (no function-call pattern) and does NOT match `idAlphabet: get|=>`.
  </acceptance_criteria>
  <done>Both backend wire-ins landed. TSC green. The VcsRefs contract closure for idAlphabet is complete.</done>
</task>

<task type="auto">
  <name>Task 3: Add cross-backend adapter-contract assertion + backends.test.ts regression guard for the capability-matrix entry</name>
  <files>sdk/src/vcs/__tests__/adapter-contract.test.ts, sdk/src/vcs/__tests__/backends.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/adapter-contract.test.ts (lines 28-46 to confirm the existing `describe.for(selectedBackends())` block shape + the `ready` skipIf helper at line 32)
    - sdk/src/vcs/__tests__/backends.test.ts (the file shipped in Plan 15.01 Task 3 — read it to confirm describe block conventions before adding a new describe)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (cross-backend describe shape verbatim at lines 694-708)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (15.02 cross-backend test shape at lines 710-715)
  </read_first>
  <action>
    Add to `sdk/src/vcs/__tests__/adapter-contract.test.ts` inside the existing `describe.for(selectedBackends())('VcsAdapter contract — backend=%s', (kind) => { ... })` block (around lines 28-46). Add the new test after the existing `test('vcs.kind matches backend kind', ...)`:

    `test.skipIf(!ready('refs.idAlphabet'))('vcs.refs.idAlphabet returns expected per-backend literal', ({ vcs }) => {`
    `  if (kind === 'git') expect(vcs.refs.idAlphabet).toBe('0-9a-f');`
    `  else expect(vcs.refs.idAlphabet).toBe('k-z');`
    `});`

    Note: per PATTERNS line 712, the `ready('refs.idAlphabet')` guard reads the capability matrix; once the matrix entry from Task 1 lands, the skipIf becomes a no-op and the assertion runs on both backends.

    Add to `sdk/src/vcs/__tests__/backends.test.ts` a new describe block for the 15.02 entry (sibling of the 15.01 `Phase 15.01 rootCommits → rootRevisions flip` block):

    `describe('BACKENDS_AVAILABLE_FOR_VERB capability matrix — Phase 15.02 refs.idAlphabet (VCS-21)', () => {`
    `  it('exposes refs.idAlphabet for both backends', () => {`
    `    expect(BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']).toEqual(['git', 'jj-colocated']);`
    `  });`
    `});`

    Run `cd sdk && pnpm vitest run src/vcs/__tests__/adapter-contract.test.ts src/vcs/__tests__/backends.test.ts -t "idAlphabet" --reporter=dot` — must exit 0.

    Commit as `test(15-02): cross-backend idAlphabet contract + capability-matrix regression`.
  </action>
  <verify>
    <automated>cd sdk &amp;&amp; pnpm vitest run src/vcs/__tests__/adapter-contract.test.ts src/vcs/__tests__/backends.test.ts -t "idAlphabet" --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/vcs/__tests__/adapter-contract.test.ts` contains at least one `test(...)` or `it(...)` block whose description includes `idAlphabet` AND whose body asserts both `'0-9a-f'` (git) and `'k-z'` (jj) literal strings.
    - The new test sits INSIDE the existing `describe.for(selectedBackends())` block (so it inherits the per-backend kind-binding) — verifiable by checking the test appears between the open of `describe.for(selectedBackends())` (around line 28) and its closing `})` (around line 46+ post-addition).
    - The new test uses `test.skipIf(!ready('refs.idAlphabet'))` (or equivalent skipIf guard) so it cleanly no-ops if the capability matrix entry is ever absent.
    - `sdk/src/vcs/__tests__/backends.test.ts` contains a new `describe(...)` block whose first argument substring includes `Phase 15.02` AND `idAlphabet` AND `VCS-21`.
    - That describe block contains an `it(...)` whose body asserts `BACKENDS_AVAILABLE_FOR_VERB['refs.idAlphabet']` is `toEqual(['git', 'jj-colocated'])`.
    - `cd sdk && pnpm vitest run src/vcs/__tests__/adapter-contract.test.ts src/vcs/__tests__/backends.test.ts -t "idAlphabet" --reporter=dot` exits 0.
    - Both backends pass the cross-backend assertion (neither is silently `.skip`ped due to a misconfigured skipIf).
  </acceptance_criteria>
  <done>Cross-backend adapter assertion + capability-matrix regression guard shipped. VCS-21 contract is observable from the SDK test surface on both backends. Plan 15.03 (matchPrefix) can now consume `this.idAlphabet` confidently.</done>
</task>

</tasks>

<verification>
- `cd sdk && pnpm tsc --noEmit` is green after Task 2.
- `cd sdk && pnpm vitest run src/vcs/__tests__/adapter-contract.test.ts src/vcs/__tests__/backends.test.ts -t "idAlphabet" --reporter=dot` is green after Task 3.
- Phase-15.01 regression tests still pass (no regression in rootRevisions surface): `cd sdk && pnpm vitest run src/vcs/__tests__/backends.test.ts --reporter=dot` is green.
- `grep -c "readonly idAlphabet:" sdk/src/vcs/types.ts` returns >= 1; both backend bodies return the expected literal.
- Capability-matrix string-literal key `'refs.idAlphabet'` present in `backends.ts` (TSC does not catch this; backends.test.ts regression-asserts).
- Working copy is clean after the final commit (`assert_clean_wc`).
</verification>

<success_criteria>
- ROADMAP Phase 15 SC2 satisfied: `vcs.refs.idAlphabet` returns `'0-9a-f'` on git adapter, `'k-z'` on jj adapter (opaque `readonly string`); cross-backend test asserts both.
- The new property is an opaque-string per CF-03 — no structured-enum/length metadata.
- Three ad-hoc duplications in `expr.ts:41`, `format-migration/rewrite.ts:53,63` remain UNTOUCHED (Phase 17 docs-drift refactor per CONTEXT Deferred Ideas) — Plan 15.02 ships only the canonical source.
- Plan 15.03 (matchPrefix) inherits the `idAlphabet` contract and can read `this.idAlphabet` for alphabet-disjointness gating.
</success_criteria>

<output>
Create `.planning/phases/15-adapter-surface-extensions-rename/15-02-SUMMARY.md` when done. Include:
- Per-backend literal strings shipped (verbatim quoted)
- adapter-contract.test.ts pass count for the new idAlphabet assertion (both backends, no skipIf trip)
- backends.test.ts regression test result
- Commit ID(s) — staged-red+green or combined-green choice noted
- TSC noEmit timing (should be sub-5s on the post-15.01 baseline)
</output>
</content>
</invoke>