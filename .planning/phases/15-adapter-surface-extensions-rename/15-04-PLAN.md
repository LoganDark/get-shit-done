---
phase: 15-adapter-surface-extensions-rename
plan: 04
type: execute
wave: 4
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - sdk/src/vcs/types.ts
  - sdk/src/vcs/backends.ts
  - sdk/src/vcs/backends/git.ts
  - sdk/src/vcs/backends/jj.ts
  - sdk/src/vcs/jj/workspace-cleanup.ts
  - sdk/src/vcs/jj/parallel.ts
  - sdk/src/vcs/git/parallel.ts
  - sdk/src/query/workspace-parallel-cancel.ts
  - sdk/src/query/command-static-catalog-domain.ts
  - sdk/src/query/command-manifest.non-family.ts
  - sdk/src/query/command-aliases.generated.ts
  - sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts
  - sdk/src/vcs/jj/__tests__/workspace-cleanup.test.ts
  - sdk/src/vcs/__tests__/backends.test.ts
autonomous: true
requirements: [PARALLEL-07]
requirements_addressed: [PARALLEL-07]
tags: [adapter-surface, parallel, cancel, workspace-cleanup, cli-bridge, sdk, vcs, ip-5]

must_haves:
  truths:
    - "`vcs.workspace.parallel.cancel(handle): CancelResult` exists on both backends with synchronous teardown semantics (CF-05 / Pitfall 5 — no signal handling; threat T-15.04-01 mitigated)."
    - "`CancelResult` is the 4-field envelope `{abandoned, failedReaped, surplusBookmarks, surplusWorkspaces}` per D-01 — mirrors `FanInResult.failedReaped` + `FanInResult.surplusBookmarks` naming verbatim."
    - "`CancelResult` is pure frozen JSON — `Object.isFrozen(result) === true`, no closures/methods/Symbols (Phase 9 D-05 invariant, `project_no_orchestrator_sidecar_state`)."
    - "Cancel is idempotent per D-03 — re-calling on the same handle returns all-empty arrays (threat T-15.04-03 mitigated)."
    - "Cancel does NOT signal subagent processes; the `vcsExec` exec layer at `sdk/src/vcs/exec.ts:19` is `spawnSync`-only and cannot accept AbortSignal (verified empirically in RESEARCH §State of the Art; threat T-15.04-01 mitigated structurally)."
    - "Shared helper `cleanupSubagentWorkspaces(mainRepoRoot, phaseNumber): CleanupSubagentWorkspacesResult` is exported from `sdk/src/vcs/jj/workspace-cleanup.ts` per D-04 (new sidecar; v1.3 sidecar pattern matching `conflict-paths.ts`)."
    - "Helper first parameter is named `mainRepoRoot` (NOT `phaseRoot` per D-05 — A6 / Open Question 2 resolved per planner default; the file-system semantics require the colocated jj repo root, not the .planning/phases/{NN}/ directory)."
    - "Helper signature returns `{abandoned: readonly string[], failedReaped: readonly string[]}` (partial-CancelResult shape per D-05) so cancel verb body unifies the helper output into the full 4-field envelope; Phase 16 fanIn clean-path and dogfood-restore can consume the same shape."
    - "Helper imports do NOT include any `from '../backends/jj'` line (UPSTREAM-02 sidecar discipline per D-07; threat T-15.04-04 mitigated)."
    - "Helper is idempotent per D-06 — missing dirs are not errors; `workspace.forget` is best-effort (ignored exit code); `rm -rf` errors go to `failedReaped`."
    - "New CLI bridge `sdk/src/query/workspace-parallel-cancel.ts` is registered at ALL THREE sites (catalog-domain + manifest.non-family + aliases.generated) per CF-07 (threat T-15.04-02 mitigated)."
    - "`BACKENDS_AVAILABLE_FOR_VERB['workspace.parallel.cancel']` is `['git', 'jj-colocated']`."
    - "Per-backend test files cover cancel-clean-abandon, cancel-idempotent-recall, cancel-partial-state-recovery on both jj and git fixtures (3 scenarios × 2 backends = 6 scenarios minimum)."
    - "CLI smoke test exercises the actual dispatcher path (`gsd-sdk query workspace.parallel.cancel --help` or equivalent) — proves all three CLI sites are wired (Pitfall 8 / threat T-15.04-02 mitigated)."
    - "No `gsd-sdk query cleanup-subagent-workspaces` CLI bridge ships in Phase 15 — that ships in Phase 16 per D-08 (cross-phase coherence note only)."
  artifacts:
    - path: "sdk/src/vcs/types.ts"
      provides: "`VcsWorkspaceParallel` extended with `cancel(handle: ParallelDispatchHandle): CancelResult;` (third method); new `CancelResult` interface adjacent to `FanInResult` at ~:533."
      contains: "cancel(handle: ParallelDispatchHandle): CancelResult"
    - path: "sdk/src/vcs/jj/workspace-cleanup.ts"
      provides: "Shared helper `cleanupSubagentWorkspaces(mainRepoRoot, phaseNumber): CleanupSubagentWorkspacesResult` + exported `CleanupSubagentWorkspacesResult` interface."
      contains: "export function cleanupSubagentWorkspaces"
      min_lines: 40
    - path: "sdk/src/vcs/jj/parallel.ts"
      provides: "New export `performJjParallelCancel(mainRepoRoot, handle): CancelResult` consuming `cleanupSubagentWorkspaces`."
      contains: "export function performJjParallelCancel"
    - path: "sdk/src/vcs/git/parallel.ts"
      provides: "New export `performGitParallelCancel(mainRepoRoot, handle): CancelResult`."
      contains: "export function performGitParallelCancel"
    - path: "sdk/src/query/workspace-parallel-cancel.ts"
      provides: "CLI bridge `workspaceParallelCancelQuery` mirroring `workspace-parallel-dispatch.ts` / `workspace-parallel-fan-in.ts` shape."
      contains: "export const workspaceParallelCancelQuery"
    - path: "sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts"
      provides: "3 jj scenarios + frozen-JSON assertion + CLI smoke."
    - path: "sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts"
      provides: "3 git scenarios + frozen-JSON assertion + CLI smoke."
    - path: "sdk/src/vcs/jj/__tests__/workspace-cleanup.test.ts"
      provides: "Helper unit test — idempotency, missing-dir handling, partial-state behavior, UPSTREAM-02 import-discipline gate."
  key_links:
    - from: "sdk/src/vcs/jj/parallel.ts (performJjParallelCancel)"
      to: "sdk/src/vcs/jj/workspace-cleanup.ts (cleanupSubagentWorkspaces)"
      via: "import + delegation call"
      pattern: "import \\{ cleanupSubagentWorkspaces \\} from './workspace-cleanup"
    - from: "sdk/src/vcs/backends/jj.ts (parallel.cancel wire-in)"
      to: "sdk/src/vcs/jj/parallel.ts (performJjParallelCancel)"
      via: "import + Object.freeze property delegation"
      pattern: "performJjParallelCancel"
    - from: "sdk/src/vcs/backends/git.ts (parallel.cancel wire-in)"
      to: "sdk/src/vcs/git/parallel.ts (performGitParallelCancel)"
      via: "import + Object.freeze property delegation"
      pattern: "performGitParallelCancel"
    - from: "sdk/src/query/workspace-parallel-cancel.ts"
      to: "sdk/src/vcs/backends/{git,jj}.ts (vcs.workspace.parallel.cancel)"
      via: "createVcsAdapter → vcs.workspace.parallel.cancel(handle) invocation"
      pattern: "vcs\\.workspace\\.parallel\\.cancel\\("
    - from: "sdk/src/query/command-{static-catalog-domain,manifest.non-family,aliases.generated}.ts"
      to: "sdk/src/query/workspace-parallel-cancel.ts (workspaceParallelCancelQuery)"
      via: "three-site CLI registration per CF-07"
      pattern: "workspace.parallel.cancel"
---

<objective>
Add `vcs.workspace.parallel.cancel(handle): CancelResult` to the cross-backend `VcsAdapter` surface — synchronous teardown only (CF-05 / Pitfall 5; no signal handling; `spawnSync` exec layer cannot accept AbortSignal per RESEARCH §State of the Art). Two-wave plan internally:

- **Wave 1:** Extract the `cleanupSubagentWorkspaces` shared helper as a new sidecar (`sdk/src/vcs/jj/workspace-cleanup.ts`) per D-04 / IP-5 single-owner pattern. Helper consumers across phases: (1) this plan's cancel verb body, (2) Phase 16 fanIn clean-path branch reap, (3) Phase 16 dogfood-restore.sh post-restore step.
- **Wave 2:** Wire cancel into both backends; ship the CLI bridge at three sites (catalog-domain + manifest.non-family + aliases.generated per CF-07); add per-backend test files covering 3 scenarios each.

Purpose: Close v1.3 deferred `vcs.workspace.parallel.cancel` — gives operators a graceful-abandonment primitive for partially-dispatched workspaces. The FEATURES "interrupt mid-Agent" use case is explicitly OUT OF SCOPE per CF-05 (would require a future `vcsExecAsync` primitive in a separate milestone).

Output: 1 new helper sidecar + 2 new perform* exports + 1 new CLI bridge + 3 CLI registration edits + 2 new per-backend test files + 1 new helper unit test + interface and capability-matrix additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md
@.planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md
@.planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-VALIDATION.md
@.planning/phases/15-adapter-surface-extensions-rename/15-03-SUMMARY.md
@sdk/src/vcs/types.ts
@sdk/src/vcs/backends.ts
@sdk/src/vcs/backends/git.ts
@sdk/src/vcs/backends/jj.ts
@sdk/src/vcs/exec.ts
@sdk/src/vcs/jj/parallel.ts
@sdk/src/vcs/jj/reap.ts
@sdk/src/vcs/jj/conflict-paths.ts
@sdk/src/vcs/jj/incomplete-work.ts
@sdk/src/vcs/jj/octopus.ts
@sdk/src/vcs/git/parallel.ts
@sdk/src/query/workspace-parallel-dispatch.ts
@sdk/src/query/workspace-parallel-fan-in.ts
@sdk/src/query/command-static-catalog-domain.ts
@sdk/src/query/command-manifest.non-family.ts
@sdk/src/query/command-aliases.generated.ts
@sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
@sdk/src/vcs/__tests__/vcs-fixture.ts

<interfaces>
<!-- Verified insertion points + signatures + skeletons -->

From sdk/src/vcs/types.ts:453-456 (VcsWorkspaceParallel interface):
  export interface VcsWorkspaceParallel {
    dispatch(opts: ParallelDispatchOpts): ParallelDispatchHandle;
    fanIn(handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult;
    // 15.04 inserts: `cancel(handle: ParallelDispatchHandle): CancelResult;`
  }

From sdk/src/vcs/types.ts:493-518 (ParallelDispatchHandle — cancel input):
  export interface ParallelDispatchHandle {
    phaseRoot: string;
    phaseNumber: number;
    mainBookmark: string;
    manifest: string;
    workspaces: readonly Readonly<{
      name: string;
      path: string;
      baseRev: string;
      agentId: string;
      baselineOpId?: string;
    }>[];
  }

From sdk/src/vcs/types.ts:533-541 (FanInResult — CancelResult mirror precedent):
  export interface FanInResult {
    merged: readonly string[];
    conflicted: boolean;
    conflictedPaths: readonly string[];
    incompleteQueued: number;
    failedReaped: readonly string[];           // ← CancelResult mirrors this name verbatim per D-01
    surplusBookmarks: readonly string[];       // ← CancelResult mirrors this name verbatim per D-01
  }

CancelResult shape (post-15.04 — D-01 4-field envelope):
  export interface CancelResult {
    abandoned: readonly string[];              // identifiers (workspace name) fully torn down per D-02
    failedReaped: readonly string[];           // identifiers that resisted teardown
    surplusBookmarks: readonly string[];       // bookmark names force-deleted (jj: typically []; git: worktree-agent-*)
    surplusWorkspaces: readonly string[];      // workspace paths still on disk pre-cancel (counted at entry)
  }

CleanupSubagentWorkspacesResult shape (helper return, D-05):
  export interface CleanupSubagentWorkspacesResult {
    abandoned: readonly string[];
    failedReaped: readonly string[];
  }

From sdk/src/vcs/exec.ts:91-126 (vcsExec signature — load-bearing for helper):
  export function vcsExec(cwd: string, bin: string, args: string[], options?: ExecOptions): ExecResult;
  // ExecResult = {exitCode, stdout, stderr, timedOut, error}; does NOT throw on non-zero exit

From sdk/src/vcs/jj/reap.ts:174-189 (per-workspace teardown precedent — reap shape):
  const forgetArgs = [...jjArgvFlags(opts.mainRepoRoot), 'workspace', 'forget', '--', entry.name];
  const forgetRes = vcsExec(opts.mainRepoRoot, 'jj', forgetArgs);
  if (forgetRes.exitCode !== 0) {
    throw new Error(`reap: jj workspace forget ${entry.name} failed: ${forgetRes.stderr || forgetRes.stdout}`);
  }
  if (existsSync(entry.path)) {
    rmSync(entry.path, { recursive: true, force: true });
  }

From sdk/src/vcs/jj/conflict-paths.ts:22-30 (UPSTREAM-02 sidecar discipline template):
  import { vcsExec } from '../exec.js';
  function jjArgvFlags(repo: string): string[] {
    return ['--repository', repo, '--no-pager', '--color', 'never', '--quiet'];
  }
  // ↑ inlines the mandatory-flags prefix; does NOT import from backends/jj.ts (D-07)

Helper skeleton (15-PATTERNS.md / 15-RESEARCH.md §Helper Sidecar Pattern lines 514-583 — paste-ready):
  import { readdirSync, rmSync, existsSync, statSync } from 'node:fs';
  import { join } from 'node:path';
  import { vcsExec } from '../exec.js';
  function jjArgvFlags(repo: string): string[] { return ['--repository', repo, '--no-pager', '--color', 'never', '--quiet']; }
  export interface CleanupSubagentWorkspacesResult { abandoned: readonly string[]; failedReaped: readonly string[]; }
  export function cleanupSubagentWorkspaces(mainRepoRoot: string, phaseNumber: number): CleanupSubagentWorkspacesResult {
    const phaseTag = String(phaseNumber).padStart(2, '0');
    const workspacesParent = join(mainRepoRoot, '.claude', 'jj-workspaces');
    const abandoned: string[] = []; const failedReaped: string[] = [];
    if (!existsSync(workspacesParent)) return Object.freeze({ abandoned: Object.freeze([]) as readonly string[], failedReaped: Object.freeze([]) as readonly string[] });
    const entries = readdirSync(workspacesParent);
    const matchPrefix = `phase-${phaseTag}-subagent-`;
    for (const name of entries) {
      if (!name.startsWith(matchPrefix)) continue;
      const path = join(workspacesParent, name);
      if (!statSync(path, { throwIfNoEntry: false })?.isDirectory()) continue;
      const forgetArgs = [...jjArgvFlags(mainRepoRoot), 'workspace', 'forget', '--', name];
      vcsExec(mainRepoRoot, 'jj', forgetArgs); // ignore exitCode per D-06
      try { rmSync(path, { recursive: true, force: true }); abandoned.push(name); }
      catch { failedReaped.push(name); }
    }
    return Object.freeze({ abandoned: Object.freeze(abandoned.slice()) as readonly string[], failedReaped: Object.freeze(failedReaped.slice()) as readonly string[] });
  }

performJjParallelCancel skeleton (15-RESEARCH.md §performJjParallelCancel sketch lines 1094-1124):
  // Add to sdk/src/vcs/jj/parallel.ts, adjacent to performJjParallelFanIn
  import { cleanupSubagentWorkspaces } from './workspace-cleanup.js';
  export function performJjParallelCancel(mainRepoRoot: string, handle: ParallelDispatchHandle): CancelResult {
    const surplusWorkspaces: string[] = [];
    for (const ws of handle.workspaces) { if (existsSync(ws.path)) surplusWorkspaces.push(ws.path); }
    const { abandoned, failedReaped } = cleanupSubagentWorkspaces(mainRepoRoot, handle.phaseNumber);
    const surplusBookmarks: string[] = [];  // jj: no per-subagent bookmarks (Phase 11 D-02)
    return Object.freeze({
      abandoned: Object.freeze(abandoned.slice()) as readonly string[],
      failedReaped: Object.freeze(failedReaped.slice()) as readonly string[],
      surplusBookmarks: Object.freeze(surplusBookmarks) as readonly string[],
      surplusWorkspaces: Object.freeze(surplusWorkspaces.slice()) as readonly string[],
    }) satisfies CancelResult;
  }

performGitParallelCancel skeleton (15-RESEARCH.md §performGitParallelCancel sketch lines 1130-1169):
  // Add to sdk/src/vcs/git/parallel.ts
  export function performGitParallelCancel(mainRepoRoot: string, handle: ParallelDispatchHandle): CancelResult {
    const surplusWorkspaces: string[] = [];
    for (const ws of handle.workspaces) { if (existsSync(ws.path)) surplusWorkspaces.push(ws.path); }
    const abandoned: string[] = []; const failedReaped: string[] = []; const surplusBookmarks: string[] = [];
    for (const ws of handle.workspaces) {
      const agentBookmark = `worktree-agent-${ws.agentId}`;
      const wtRes = vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', '--force', ws.path]);
      if (wtRes.exitCode === 0) {
        const brRes = vcsExec(mainRepoRoot, 'git', ['branch', '-D', agentBookmark]);
        if (brRes.exitCode === 0) { surplusBookmarks.push(agentBookmark); abandoned.push(ws.agentId); }
        else { failedReaped.push(ws.agentId); }
      } else { failedReaped.push(ws.agentId); }
    }
    return Object.freeze({ abandoned: ..., failedReaped: ..., surplusBookmarks: ..., surplusWorkspaces: ... }) satisfies CancelResult;
  }

CLI bridge skeleton (15-RESEARCH.md §CLI bridge sketch lines 1174-1217):
  // sdk/src/query/workspace-parallel-cancel.ts
  import { readFileSync } from 'node:fs';
  import { createVcsAdapter } from '../vcs/index.js';
  import type { ParallelDispatchHandle } from '../vcs/types.js';
  import type { QueryHandler } from './utils.js';
  function resolveFileOrStdin(raw: string): string { ... }
  export const workspaceParallelCancelQuery: QueryHandler = async (args, projectDir) => {
    let cwd = projectDir; let handleRaw: string | undefined;
    for (let i = 0; i < args.length; i++) {
      if (args[i] === '--cwd' && args[i+1]) cwd = args[++i];
      else if (args[i] === '--handle' && args[i+1]) handleRaw = args[++i];
    }
    if (handleRaw === undefined) return { data: { ok: false, reason: 'handle_required' } };
    let handle: ParallelDispatchHandle;
    try { handle = JSON.parse(resolveFileOrStdin(handleRaw)); }
    catch (err) { return { data: { ok: false, reason: 'handle_json_parse_failed', error: err instanceof Error ? err.message : String(err) } }; }
    const vcs = createVcsAdapter(cwd);
    const result = vcs.workspace.parallel.cancel(handle);
    return { data: result };
  };

Three-site CLI registration (15-RESEARCH.md §CLI Bridge Three-Site Registration Pattern lines 662-716):

Site 1 (command-static-catalog-domain.ts):
  import { workspaceParallelCancelQuery } from './workspace-parallel-cancel.js';
  ['workspace.parallel.cancel', workspaceParallelCancelQuery],
  ['workspace parallel.cancel', workspaceParallelCancelQuery],

Site 2 (command-manifest.non-family.ts):
  { canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true, outputMode: 'json' },

Site 3 (command-aliases.generated.ts — alphabetical insertion BEFORE 'workspace.parallel.dispatch'):
  { canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true },

From sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts:64-87 (Pattern B mkdtemp fixture — paste-ready for cancel tests, adjust phase number from 9 to 15):
  function setupJjRepo(): string {
    const dir = mkdtempSync(join(tmpdir(), `gsd-jj-parallel-${Math.random().toString(36).slice(2, 10)}-`));
    execSync('jj git init --colocate', { cwd: dir, stdio: 'pipe' });
    execSync('jj config set --repo user.email "test@test.com"', { cwd: dir, stdio: 'pipe' });
    execSync('jj config set --repo user.name "Test"', { cwd: dir, stdio: 'pipe' });
    writeFileSync(join(dir, 'seed.txt'), 'seed\n');
    execSync('jj squash -B @ -k -m "seed"', { cwd: dir, stdio: 'pipe' });
    mkdirSync(join(dir, '.planning', 'phases', '15-test'), { recursive: true });
    return dir;
  }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 1): Extract `cleanupSubagentWorkspaces` helper sidecar + unit test + types.ts CancelResult declaration</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/jj/workspace-cleanup.ts, sdk/src/vcs/jj/__tests__/workspace-cleanup.test.ts</files>
  <read_first>
    - sdk/src/vcs/jj/reap.ts (UPSTREAM-02 sidecar pattern + per-workspace teardown shape at lines 174-189 — adapted with throws → failedReaped per D-06)
    - sdk/src/vcs/jj/conflict-paths.ts (file-header template at lines 1-30: file path comment, phase reference, UPSTREAM-02 statement, `import { vcsExec } from '../exec.js'`, inline `jjArgvFlags`)
    - sdk/src/vcs/jj/incomplete-work.ts (sibling sidecar header shape — phase + REQ ID + carry-decision references)
    - sdk/src/vcs/jj/octopus.ts (subagent workspace path pattern — `.claude/jj-workspaces/phase-{NN}-subagent-{idx}` — verifies the `matchPrefix` glob target)
    - sdk/src/vcs/exec.ts (vcsExec signature — `(cwd, bin, args, options?): ExecResult`; does NOT throw on non-zero exit; helper must inspect or ignore exit code per D-06)
    - sdk/src/vcs/types.ts (FanInResult at lines 533-541 — exact mirror precedent for CancelResult naming per D-01; VcsWorkspaceParallel at 453-456 — the cancel-method insertion site is in Task 2, not here)
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (Pattern B mkdtemp fixture at lines 64-87 — paste-ready, only need to adjust phase number from 9 to 15)
    - .planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md (D-01 CancelResult shape; D-04 helper file location; D-05 helper signature; D-06 idempotency; D-07 UPSTREAM-02 discipline; Open Question 2 — param name `mainRepoRoot` not `phaseRoot`)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (§Helper Sidecar Pattern lines 488-583 — paste-ready skeleton; Pitfall 5 explains divergence from reap.ts contract; A6 confirms param name choice)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (§`sdk/src/vcs/jj/workspace-cleanup.ts` block lines 52-170 — full pattern explanation; §Pattern S1 UPSTREAM-02 discipline; §Pattern S2 Frozen pure-JSON; divergences from reap.ts listed under "Divergences (don't copy)")
  </read_first>
  <behavior>
    - `CancelResult` interface in `sdk/src/vcs/types.ts` declares the 4-field envelope `{abandoned, failedReaped, surplusBookmarks, surplusWorkspaces}` exactly mirroring D-01.
    - Helper signature: `cleanupSubagentWorkspaces(mainRepoRoot: string, phaseNumber: number): CleanupSubagentWorkspacesResult` (first param `mainRepoRoot` per Open Question 2 resolution).
    - `CleanupSubagentWorkspacesResult` returns the partial-CancelResult `{abandoned: readonly string[], failedReaped: readonly string[]}` per D-05.
    - Helper enumerates filesystem dirs at `<mainRepoRoot>/.claude/jj-workspaces/phase-${phaseTag}-subagent-*` (zero-padded phaseTag); subagent dirs not matching this prefix are SKIPPED (do not interfere with other phases).
    - Test 1 (helper-idempotency): create tmpdir with materialized `.claude/jj-workspaces/phase-15-subagent-{1..3}/` dirs; call helper twice; first call returns `abandoned.length === 3, failedReaped.length === 0`; second call returns `abandoned.length === 0, failedReaped.length === 0` (D-03/D-06 idempotency; threat T-15.04-03 mitigated).
    - Test 2 (missing-parent-dir): call helper against tmpdir with no `.claude/jj-workspaces/` directory; returns empty arrays, no error (D-06).
    - Test 3 (partial-state): materialize 3 workspace dirs, `rmSync` one of them manually before calling helper; helper returns `abandoned.length === 2` (only the surviving dirs are reported as abandoned; the pre-removed dir is silently skipped via the `statSync({throwIfNoEntry: false})?.isDirectory()` check).
    - Test 4 (UPSTREAM-02 import-discipline gate, structural-grep): `grep -E "from\\s+['\"]\\.\\./backends/jj['\"]" sdk/src/vcs/jj/workspace-cleanup.ts` returns 0 lines. Threat T-15.04-04 mitigated.
    - Test 5 (helper return is frozen): `const result = cleanupSubagentWorkspaces(...); expect(Object.isFrozen(result)).toBe(true);` AND `expect(Object.isFrozen(result.abandoned)).toBe(true);` AND `expect(Object.isFrozen(result.failedReaped)).toBe(true);` — Phase 9 D-05 invariant.
    - Test 6 (only matching prefix is reaped): materialize `phase-15-subagent-1/`, `phase-15-subagent-2/`, `phase-14-subagent-1/`, and an unrelated `other-dir/`; call helper with `phaseNumber=15`; only the two `phase-15-subagent-*` dirs end up in `abandoned`, the other dirs are untouched on disk.
    - Helper body does NOT throw `Error` for any expected condition (missing dir, missing parent, partial state, jj workspace forget non-zero exit, rm failure). All non-success outcomes are encoded in the return value (D-06).
    - Helper does NOT import from `backends/jj.ts` (UPSTREAM-02 / D-07); does import from `../exec.js`, `node:fs`, `node:path`.
  </behavior>
  <action>
    In `sdk/src/vcs/types.ts`, add the `CancelResult` interface immediately after `FanInResult` (currently lines 533-541). Match the exact field order from D-01: `abandoned`, `failedReaped`, `surplusBookmarks`, `surplusWorkspaces` — all typed `readonly string[]`. Add a JSDoc block above the interface citing D-01 / D-02 / D-03 verbatim from CONTEXT.md (4-field envelope, mirrors FanInResult.failedReaped + .surplusBookmarks naming, pure JSON per Phase 9 D-05, idempotent per D-03). Also export `CleanupSubagentWorkspacesResult` from `sdk/src/vcs/jj/workspace-cleanup.ts` (NOT from `types.ts` — the helper's return type lives with the helper per Pattern S2 + the conflict-paths.ts precedent of declaring sidecar-local types adjacent to the sidecar). NOTE: Do NOT add the `cancel` method to `VcsWorkspaceParallel` here; that lands in Task 2 to keep this task focused on Wave 1 / helper extraction.

    Create `sdk/src/vcs/jj/workspace-cleanup.ts` mirroring `sdk/src/vcs/jj/conflict-paths.ts` file-header template per §Pattern S1 + §Pattern S2 in 15-PATTERNS.md. File-header docblock includes: (1) file-path header `sdk/src/vcs/jj/workspace-cleanup.ts — Phase 15 (PARALLEL-07, IP-5)`, (2) one-paragraph "what + why" summary citing D-04 file-boundary divergence from reap.ts contract, (3) the UPSTREAM-02 sidecar discipline statement verbatim ("this file does NOT import from `backends/jj.ts`"), (4) list of carried decisions (D-04, D-05, D-06, D-07). Imports: `import { readdirSync, rmSync, existsSync, statSync } from 'node:fs';`, `import { join } from 'node:path';`, `import { vcsExec } from '../exec.js';` — no other imports. Inline `function jjArgvFlags(repo: string): string[]` mirrors `conflict-paths.ts:22-26` verbatim. Export `interface CleanupSubagentWorkspacesResult { abandoned: readonly string[]; failedReaped: readonly string[]; }`. Export `function cleanupSubagentWorkspaces(mainRepoRoot: string, phaseNumber: number): CleanupSubagentWorkspacesResult` with the body skeleton in `<interfaces>` above — paste-ready from 15-RESEARCH.md §Helper Sidecar Pattern. The JSDoc on the function must note that param naming diverges from D-05 (which said `phaseRoot`) to `mainRepoRoot` per Open Question 2 + A6 — the helper enumerates `<mainRepoRoot>/.claude/jj-workspaces/`, which is the colocated jj repo root, NOT a `.planning/phases/{NN}/` directory.

    Create `sdk/src/vcs/jj/__tests__/workspace-cleanup.test.ts` mirroring `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` Pattern B (mkdtemp fixture). Imports: vitest `describe`, `it`, `expect`, `beforeAll`, `afterAll`; `mkdtempSync`, `writeFileSync`, `mkdirSync`, `rmSync`, `existsSync` from `node:fs`; `tmpdir` from `node:os`; `join` from `node:path`; `execSync` from `node:child_process` (for the `jj git init --colocate` setup); the helper itself via `import { cleanupSubagentWorkspaces } from '../workspace-cleanup.js';`. Implement the 6 tests in `<behavior>`. Test 4 (UPSTREAM-02 gate) uses `readFileSync` to read the helper's source and a regex `/from\s+['"]\.\.\/backends\/jj['"]/` — assert no match. Use `describe.skipIf(!jjAvailable)` to gate jj-dependent tests behind `try { execSync('jj --version', { stdio: 'pipe' }); jjAvailable = true; } catch { jjAvailable = false; }` (Pattern A from cmd-parallel-jj.test.ts:45-51).

    Commit via `gsd-sdk query commit` with message `"feat($PHASE): extract cleanupSubagentWorkspaces helper sidecar (PARALLEL-07 Wave 1)"`.
  </action>
  <verify>
    <automated>pnpm --filter sdk test -- --run --no-coverage -t "workspace-cleanup" &amp;&amp; pnpm --filter sdk test -- --run --no-coverage -t "cleanupSubagentWorkspaces" &amp;&amp; grep -E "from[[:space:]]+['\"]\\.\\./backends/jj['\"]" sdk/src/vcs/jj/workspace-cleanup.ts ; [ $? -ne 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/vcs/jj/workspace-cleanup.ts` exists with at least 40 lines (file-header + types + impl)
    - `grep -E "from[[:space:]]+['\"]\\.\\./backends/jj['\"]" sdk/src/vcs/jj/workspace-cleanup.ts` returns 0 lines (UPSTREAM-02 discipline / threat T-15.04-04)
    - `grep -E "from[[:space:]]+['\"]\\.\\./exec['\"]" sdk/src/vcs/jj/workspace-cleanup.ts` returns 1 line (imports vcsExec from `../exec.js`)
    - `grep -n "export function cleanupSubagentWorkspaces" sdk/src/vcs/jj/workspace-cleanup.ts` returns 1 line
    - `grep -n "export interface CleanupSubagentWorkspacesResult" sdk/src/vcs/jj/workspace-cleanup.ts` returns 1 line
    - `grep -n "mainRepoRoot: string" sdk/src/vcs/jj/workspace-cleanup.ts` returns at least 1 line (param name resolution for Open Question 2 — NOT `phaseRoot`)
    - `pnpm --filter sdk test -- --run -t "workspace-cleanup"` exits 0 with all 6 helper tests green
    - `grep -n "export interface CancelResult" sdk/src/vcs/types.ts` returns 1 line
    - CancelResult interface has all four fields with `readonly string[]` typing — verified by `grep -A 6 "export interface CancelResult" sdk/src/vcs/types.ts | grep -E "(abandoned|failedReaped|surplusBookmarks|surplusWorkspaces): readonly string\\[\\]" | wc -l` equals 4
    - Helper return is frozen (Test 5 — `Object.isFrozen(result) === true`)
    - `pnpm --filter sdk test -- --run --no-coverage` (full SDK suite) exits 0
  </acceptance_criteria>
  <done>Helper sidecar shipped under UPSTREAM-02 discipline; helper unit test green; CancelResult interface declared adjacent to FanInResult; ready for Task 2 to consume in cancel verb bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Wave 2): Wire cancel verb on both backends + CLI bridge three-site registration + per-backend test files</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/backends.ts, sdk/src/vcs/backends/git.ts, sdk/src/vcs/backends/jj.ts, sdk/src/vcs/jj/parallel.ts, sdk/src/vcs/git/parallel.ts, sdk/src/query/workspace-parallel-cancel.ts, sdk/src/query/command-static-catalog-domain.ts, sdk/src/query/command-manifest.non-family.ts, sdk/src/query/command-aliases.generated.ts, sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts, sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts, sdk/src/vcs/__tests__/backends.test.ts</files>
  <read_first>
    - sdk/src/vcs/jj/workspace-cleanup.ts (Task 1 output — consumed by `performJjParallelCancel`)
    - sdk/src/vcs/jj/parallel.ts (sibling exports `performJjParallelDispatch`, `performJjParallelFanIn` give the shape for the new `performJjParallelCancel` export; UPSTREAM-02 discipline applies — check imports do not pull from backends/jj.ts)
    - sdk/src/vcs/git/parallel.ts (sibling exports — same shape considerations on the git side; verify `vcsExec` import is already present for the worktree-remove + branch-delete shell-outs)
    - sdk/src/vcs/backends/git.ts (parallel Object.freeze block at lines 738-745 — the `cancel:` property insertion site; verify `performGitParallelCancel` is added to the import block)
    - sdk/src/vcs/backends/jj.ts (parallel Object.freeze block at lines 1257-1264 — `cancel:` property insertion site; verify `performJjParallelCancel` is added to the import block)
    - sdk/src/vcs/types.ts (Task 1 added CancelResult; this task adds the `cancel(handle): CancelResult` method to VcsWorkspaceParallel at lines 453-456)
    - sdk/src/query/workspace-parallel-dispatch.ts (CLI bridge precedent — paste-ready handler shape; the cancel bridge mirrors verbatim except no `loadConfig` call and `--handle` rejects inline JSON per the 15-PATTERNS.md Divergences note)
    - sdk/src/query/workspace-parallel-fan-in.ts (second CLI bridge precedent — same shape)
    - sdk/src/query/command-static-catalog-domain.ts (Site 1 registration — imports at :21-22; entries at :71-74)
    - sdk/src/query/command-manifest.non-family.ts (Site 2 registration — entries at :60-62; `mutation: true, outputMode: 'json'` for parallel verbs)
    - sdk/src/query/command-aliases.generated.ts (Site 3 registration — alphabetical insertion before `dispatch` per 15-PATTERNS.md §Site 3)
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (lines 1-200 — Pattern A skipIf + Pattern B mkdtemp + W2 lifecycle; the 3 cancel scenarios mirror Pattern A/B but with NO octopus merge involved per 15-PATTERNS.md divergences)
    - .planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md (D-01 CancelResult shape; D-02 field semantics; D-03 idempotency; CF-05 synchronous-teardown rule; CF-06 cleanup mechanics; CF-07 three-site registration)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (§performJjParallelCancel sketch; §performGitParallelCancel sketch; §CLI bridge sketch; §CLI Bridge Three-Site Registration Pattern lines 662-728; §Cross-Backend Test Harness lines 805-812 — three cancel scenarios)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (§`sdk/src/query/workspace-parallel-cancel.ts` block; §`cmd-parallel-cancel-jj.test.ts` + `cmd-parallel-cancel-git.test.ts` block; §Pattern S4 Three-site CLI registration; §Site 1/2/3 verified entries with line numbers)
  </read_first>
  <behavior>
    - `VcsWorkspaceParallel.cancel(handle: ParallelDispatchHandle): CancelResult` is declared in types.ts adjacent to `fanIn`.
    - Capability matrix entry `'workspace.parallel.cancel': Object.freeze(['git', 'jj-colocated'] as const)` added to `backends.ts`.
    - `performJjParallelCancel(mainRepoRoot: string, handle: ParallelDispatchHandle): CancelResult` exported from `sdk/src/vcs/jj/parallel.ts`; delegates to `cleanupSubagentWorkspaces` from Task 1 (D-04 IP-5 single-owner per 15-PATTERNS.md §Pattern S1).
    - `performGitParallelCancel(mainRepoRoot: string, handle: ParallelDispatchHandle): CancelResult` exported from `sdk/src/vcs/git/parallel.ts`; uses `git worktree remove --force` (CF-06 — cancel diverges from fanIn's non-force pattern) + `git branch -D <agentBookmark>` per workspace; populates `surplusBookmarks` with the deleted `worktree-agent-<agentId>` names.
    - Both backends' `parallel:` Object.freeze blocks include `cancel: (handle) => performBackendParallelCancel(cwd, handle)` adjacent to `dispatch:` and `fanIn:`.
    - New CLI bridge `sdk/src/query/workspace-parallel-cancel.ts` exports `workspaceParallelCancelQuery` mirroring dispatch/fan-in shape; `--cwd` flag accepted; `--handle @-` (stdin) or `@<path>` (file) accepted; rejects inline JSON for `--handle` per 15-PATTERNS.md divergence note.
    - CLI bridge registered at all three sites per CF-07; Site 3 (`command-aliases.generated.ts`) inserts BEFORE `workspace.parallel.dispatch` (alphabetical sort `cancel < dispatch < fan-in`).
    - Backends.test.ts gets a new presence assertion `expect(BACKENDS_AVAILABLE_FOR_VERB['workspace.parallel.cancel']).toEqual(['git', 'jj-colocated']);`.
    - jj cancel test scenarios (3 cases via Pattern B mkdtemp + jj fixture):
      1. **cancel-clean-abandon (N=2):** `vcs.workspace.parallel.dispatch({plan: [{agentId:'a1', planId:'p1'}, {agentId:'a2', planId:'p2'}], phaseNumber:15, mainBookmark:'main'})` → no agents run → `vcs.workspace.parallel.cancel(handle)` → assert `abandoned.length === 2`, `surplusWorkspaces.length === 2`, `failedReaped.length === 0`, `surplusBookmarks.length === 0`; after the call, `existsSync(handle.workspaces[0].path) === false` AND `existsSync(handle.workspaces[1].path) === false`.
      2. **cancel-idempotent-recall:** dispatch → cancel → cancel again → second cancel returns all-empty arrays (D-03; threat T-15.04-03).
      3. **cancel-partial-state-recovery:** dispatch → manually `rmSync(handle.workspaces[0].path, {recursive: true, force: true})` → cancel → that workspace is NOT in `abandoned` (statSync skips it) AND NOT in `failedReaped` (pre-removed dir is silently ignored per D-06); the remaining workspace(s) are torn down.
    - git cancel test scenarios (mirror the 3 jj scenarios but on a git fixture):
      1. **cancel-clean-abandon (N=2):** same shape; assert `surplusBookmarks` contains both `worktree-agent-a1` and `worktree-agent-a2` entries (git diverges from jj: git has per-agent bookmarks).
      2. **cancel-idempotent-recall:** second call returns empty.
      3. **cancel-partial-state-recovery:** same shape; partial state is `git worktree remove --force` returning non-zero exit (the worktree was already gone), goes to `failedReaped`.
    - Frozen-JSON contract: each cancel test asserts `Object.isFrozen(result) === true && JSON.parse(JSON.stringify(result))` round-trip yields the same shape (Phase 9 D-05).
    - CLI smoke test in each backend's cancel test file: `execSync('node bin/gsd-sdk.cjs query workspace.parallel.cancel --help', {stdio: 'pipe'})` exits with code 0 — proves all three CLI registration sites are wired (Pitfall 8 / threat T-15.04-02).
  </behavior>
  <action>
    In `sdk/src/vcs/types.ts`, add `cancel(handle: ParallelDispatchHandle): CancelResult;` to `VcsWorkspaceParallel` (currently at lines 453-456) immediately after `fanIn(...)`. JSDoc on the new method: "Phase 15 (PARALLEL-07): synchronous teardown of already-materialized workspaces from a prior `dispatch` call. CF-05 / Pitfall 5: does NOT signal subagent processes (the `vcsExec` exec layer at `sdk/src/vcs/exec.ts:19` is `spawnSync`-only; the Phase 9 D-01 orchestrator-awaits-Agent invariant makes mid-flight cancel a non-problem in production). Idempotent per D-03 — re-call on a teared-down handle returns all-empty arrays."

    In `sdk/src/vcs/backends.ts`, add a new capability matrix entry `'workspace.parallel.cancel': Object.freeze(['git', 'jj-colocated'] as const),` immediately after the `'workspace.parallel.fan-in'` entry (or inside the workspace.* block adjacent to `'workspace.parallel.dispatch'` / `'workspace.parallel.fan-in'`).

    In `sdk/src/vcs/jj/parallel.ts`, add an export `performJjParallelCancel(mainRepoRoot: string, handle: ParallelDispatchHandle): CancelResult` adjacent to the existing `performJjParallelFanIn` export. Imports: add `import { cleanupSubagentWorkspaces } from './workspace-cleanup.js';` to the existing import block; ensure `existsSync` from `node:fs` is imported (it likely already is — verify in read_first). The body matches the skeleton in `<interfaces>` above: build `surplusWorkspaces` by checking `existsSync(ws.path)` for each handle workspace BEFORE cleanup; call `cleanupSubagentWorkspaces(mainRepoRoot, handle.phaseNumber)` to get `{abandoned, failedReaped}`; jj has no per-subagent bookmarks (Phase 11 D-02) so `surplusBookmarks = []` by construction; return the frozen 4-field envelope.

    In `sdk/src/vcs/git/parallel.ts`, add an export `performGitParallelCancel(mainRepoRoot: string, handle: ParallelDispatchHandle): CancelResult` adjacent to existing `performGitParallelFanIn`. Body matches the skeleton in `<interfaces>` above: for each `ws` in `handle.workspaces`, derive `agentBookmark = "worktree-agent-" + ws.agentId`, run `vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', '--force', ws.path])` (CF-06 force per the cancel-divergence-from-fanIn note), inspect exit code; on success, run `vcsExec(mainRepoRoot, 'git', ['branch', '-D', agentBookmark])` and on success push `agentBookmark` to `surplusBookmarks` AND push `ws.agentId` to `abandoned`; on either non-zero exit, push `ws.agentId` to `failedReaped`. Build `surplusWorkspaces` by `existsSync(ws.path)` check at function entry BEFORE the teardown loop. Return the frozen 4-field envelope.

    In `sdk/src/vcs/backends/git.ts`, add `import { performGitParallelCancel } from '../git/parallel.js';` to the import block (adjacent to existing performGit* imports). Edit the `parallel: Object.freeze({...})` block at lines 738-745: add a new property `cancel: (handle: ParallelDispatchHandle): CancelResult => performGitParallelCancel(cwd, handle),` immediately after `fanIn: ...`.

    In `sdk/src/vcs/backends/jj.ts`, add `import { performJjParallelCancel } from '../jj/parallel.js';` to the import block. Edit the `parallel: Object.freeze({...})` block at lines 1257-1264: add a new property `cancel: (handle: ParallelDispatchHandle): CancelResult => performJjParallelCancel(cwd, handle),` immediately after `fanIn: ...`.

    Create `sdk/src/query/workspace-parallel-cancel.ts` mirroring `sdk/src/query/workspace-parallel-dispatch.ts` shape per 15-PATTERNS.md §`sdk/src/query/workspace-parallel-cancel.ts` block. Imports: `import { readFileSync } from 'node:fs'; import { createVcsAdapter } from '../vcs/index.js'; import type { ParallelDispatchHandle } from '../vcs/types.js'; import type { QueryHandler } from './utils.js';`. NO `import { loadConfig } from '../config.js';` (Divergence — cancel does NOT consult parallelization gate). Implement `resolveFileOrStdin(raw: string): string` that accepts `'@-'` for stdin (`readFileSync(0, 'utf-8')`) and `'@<path>'` for file (`readFileSync(raw.slice(1), 'utf-8')`); else `throw new Error('expected @<path> or @- but got inline string (inline JSON form is not accepted)')` per Divergence. Implement `workspaceParallelCancelQuery: QueryHandler` per the skeleton in `<interfaces>`. Envelope return shape: `{ data: cancelResult }` on success; `{ data: { ok: false, reason: 'handle_required' } }` on missing `--handle`; `{ data: { ok: false, reason: 'handle_json_parse_failed', error: msg } }` on JSON parse failure.

    Register at Site 1 (`sdk/src/query/command-static-catalog-domain.ts`): add `import { workspaceParallelCancelQuery } from './workspace-parallel-cancel.js';` adjacent to existing dispatch/fan-in imports; add two catalog entries `['workspace.parallel.cancel', workspaceParallelCancelQuery]` and `['workspace parallel.cancel', workspaceParallelCancelQuery]` adjacent to existing entries.

    Register at Site 2 (`sdk/src/query/command-manifest.non-family.ts`): add entry `{ canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true, outputMode: 'json' },` adjacent to existing entries.

    Register at Site 3 (`sdk/src/query/command-aliases.generated.ts`): add entry `{ canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true },` — **insert BEFORE** the existing `workspace.parallel.dispatch` entry to preserve the alphabetical sort (`cancel < dispatch < fan-in`).

    In `sdk/src/vcs/__tests__/backends.test.ts`, add `expect(BACKENDS_AVAILABLE_FOR_VERB['workspace.parallel.cancel']).toEqual(['git', 'jj-colocated']);` adjacent to existing presence assertions. Bump the keys-count assertion by +1 if present.

    Create `sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts` mirroring `cmd-parallel-jj.test.ts:1-200` structure. Pattern A skipIf gate at the top: `try { execSync('jj --version', { stdio: 'pipe' }); jjAvailable = true; } catch { jjAvailable = false; }`. Pattern B `setupJjRepo()` helper per the read_first skeleton (adjust phase number from 9 to 15 in the `mkdir .planning/phases/15-test/` line). Implement the 3 scenarios from `<behavior>` PLUS the frozen-JSON assertion PLUS the CLI smoke test. Each scenario is its own `it()` block within a `describe.sequential.skipIf(!jjAvailable)('workspace.parallel.cancel — jj', () => { ... })` envelope. The frozen-JSON assertion checks `Object.isFrozen(result) === true` and that `JSON.parse(JSON.stringify(result))` deep-equals `result` (per Phase 9 D-05). The CLI smoke test uses `execSync` to invoke `node bin/gsd-sdk.cjs query workspace.parallel.cancel --help` (or `--handle @-` with stdin input) and asserts exit code 0.

    Create `sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts` mirroring the jj version but with `git init` setup instead of `jj git init --colocate` (per 15-PATTERNS.md divergences). Git scenario 1 asserts `surplusBookmarks` contains both `worktree-agent-a1` and `worktree-agent-a2` (NOT empty as jj does). Same frozen-JSON assertion + CLI smoke at the bottom.

    Run `pnpm --filter sdk build` to regenerate `dist-cjs/`.

    Commit via `gsd-sdk query commit` with message `"feat($PHASE): wire vcs.workspace.parallel.cancel + CLI bridge (PARALLEL-07 Wave 2)"`.
  </action>
  <verify>
    <automated>pnpm --filter sdk test -- --run --no-coverage -t "workspace.parallel.cancel" &amp;&amp; pnpm --filter sdk test -- --run --no-coverage -t "BACKENDS_AVAILABLE_FOR_VERB.workspace.parallel.cancel" &amp;&amp; pnpm --filter sdk build &amp;&amp; for site in sdk/src/query/command-static-catalog-domain.ts sdk/src/query/command-manifest.non-family.ts sdk/src/query/command-aliases.generated.ts; do c=$(grep -c 'workspace.parallel.cancel\|workspace-parallel-cancel' "$site"); [ "$c" -ge 1 ] || { echo "FAIL: $site missing cancel registration ($c hits)"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter sdk test -- --run -t "workspace.parallel.cancel"` exits 0 with at least 6 test cases passing (3 jj scenarios + 3 git scenarios)
    - `pnpm --filter sdk test -- --run -t "BACKENDS_AVAILABLE_FOR_VERB.workspace.parallel.cancel"` exits 0 (presence assertion green)
    - `grep -n "cancel(handle: ParallelDispatchHandle): CancelResult" sdk/src/vcs/types.ts` returns 1 line (method declaration)
    - `grep -n "'workspace.parallel.cancel':" sdk/src/vcs/backends.ts` returns 1 line (capability matrix entry)
    - `grep -n "export function performJjParallelCancel" sdk/src/vcs/jj/parallel.ts` returns 1 line
    - `grep -n "export function performGitParallelCancel" sdk/src/vcs/git/parallel.ts` returns 1 line
    - `grep -n "import { cleanupSubagentWorkspaces }" sdk/src/vcs/jj/parallel.ts` returns 1 line (IP-5 single-owner consumption)
    - `grep -n "cancel: (handle" sdk/src/vcs/backends/git.ts` returns 1 line (wire-in)
    - `grep -n "cancel: (handle" sdk/src/vcs/backends/jj.ts` returns 1 line (wire-in)
    - All three CLI registration sites have at least 1 matching entry: `grep -c 'workspace.parallel.cancel\|workspace-parallel-cancel' sdk/src/query/command-static-catalog-domain.ts` >= 1 (expect 2 for catalog: dotted + space form); `grep -c 'workspace.parallel.cancel' sdk/src/query/command-manifest.non-family.ts` >= 1; `grep -c 'workspace.parallel.cancel' sdk/src/query/command-aliases.generated.ts` >= 1
    - Site 3 alphabetical insertion: line containing `'workspace.parallel.cancel'` in `command-aliases.generated.ts` appears BEFORE the line containing `'workspace.parallel.dispatch'` — verifiable via `grep -n` line numbers
    - Frozen-JSON contract: each `cmd-parallel-cancel-*.test.ts` includes assertion `expect(Object.isFrozen(result)).toBe(true)` AND a JSON round-trip equality check
    - CLI smoke: at least one of the two test files invokes `gsd-sdk query workspace.parallel.cancel` via `execSync` and asserts exit code 0 (Pitfall 8 / threat T-15.04-02 mitigation)
    - `pnpm --filter sdk test -- --run --no-coverage` (full SDK suite) exits 0
    - `grep -c 'cancel' sdk/dist-cjs/vcs/types.d.ts` >= 1 (post-build dist-cjs carries the new method declaration)
    - Cancel ≤2s for N=8 (PARALLEL-07 SC4 from ROADMAP — verify in test by wrapping `cancel(handle)` in a `performance.now()` measurement at N=2; the N=8 limit is implicit; record the N=2 actual time in 15-04-SUMMARY for reference)
  </acceptance_criteria>
  <done>cancel verb shipped on both backends with synchronous-teardown semantics; CLI bridge at all three sites; per-backend test files green (3 scenarios each + frozen-JSON + CLI smoke); helper consumed via IP-5 single-owner pattern; full SDK suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter cancel vs subagent processes | `vcs.workspace.parallel.cancel` runs in the orchestrator process; subagent processes (if any are mid-flight) are NOT signaled. The Phase 9 D-01 orchestrator-awaits-Agent invariant makes mid-flight cancel a non-problem in production. CF-05 (synchronous teardown only) is the structural mitigation. |
| `vcsExec` exec layer vs AbortSignal | `vcsExec` is a `spawnSync` wrapper at `sdk/src/vcs/exec.ts:19`; `spawnSync` does not meaningfully accept `AbortSignal` (verified empirically per RESEARCH §State of the Art). Anyone proposing AbortSignal threading must instead introduce a future `vcsExecAsync` primitive in a separate milestone (OOS per PROJECT.md). |
| Helper file `workspace-cleanup.ts` vs `backends/jj.ts` | UPSTREAM-02 sidecar discipline (D-07) — circular import would break the SDK build. Acceptance criterion structurally enforces. |
| Three CLI registration sites vs runtime verb dispatch | Missing any one site breaks runtime resolution differently per dispatcher path (Pitfall 8). All three structurally enforced by grep gate + CLI smoke. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.04-01 | Denial of Service | cancel attempting to signal subagent process via vcsExec AbortSignal | mitigate | CF-05 / Pitfall 5: cancel is synchronous teardown only; `spawnSync` exec layer cannot accept AbortSignal (empirically verified); no `AbortController` or `signal:` keyword anywhere in the new code. Acceptance criterion: `grep -E "AbortController\|signal:" sdk/src/vcs/jj/workspace-cleanup.ts sdk/src/vcs/jj/parallel.ts sdk/src/vcs/git/parallel.ts sdk/src/query/workspace-parallel-cancel.ts` returns 0 lines (verified by hand during code review). |
| T-15.04-02 | Spoofing | CLI bridge missing registration at any of 3 sites | mitigate | CF-07 / Pitfall 8 — structural-grep gate confirms all 3 sites have the entry; CLI smoke test in `cmd-parallel-cancel-{jj,git}.test.ts` invokes `gsd-sdk query workspace.parallel.cancel --help` via `execSync` and asserts exit code 0, exercising the real dispatcher path. |
| T-15.04-03 | Repudiation | `cleanupSubagentWorkspaces` not idempotent — double-call throws | mitigate | D-03 / D-06 contract is idempotent; vitest helper test asserts second call returns empty arrays; per-backend cancel test scenario `cancel-idempotent-recall` exercises the same property through the cancel verb. |
| T-15.04-04 | Tampering | Helper imports from `backends/jj.ts` accidentally → circular import / UPSTREAM-02 violation | mitigate | D-07 / Pattern S1 — structural-grep gate fails on any line matching `from\s+['"]\.\.\/backends\/jj['"]`. Acceptance criterion in Task 1 + helper unit test Test 4. |
| T-15.04-05 | Information Disclosure | cancel envelope reveals workspace paths (`surplusWorkspaces`) which may contain sensitive filenames | accept | Workspace paths under `.claude/jj-workspaces/phase-{NN}-subagent-{idx}` are predictable by phase number + agent index; no user data in the path; envelope is operator-facing only (not user-facing). Low risk. |
| T-15.04-SC | Tampering | npm/pip/cargo installs in this plan | accept | Phase 15 introduces ZERO new packages (RESEARCH §Package Legitimacy Audit); no install verbs in plan; gate trivially satisfied. |
</threat_model>

<verification>
**Plan-level gates:**

1. `pnpm --filter sdk test -- --run --no-coverage` exits 0 (full SDK suite — covers helper unit + 6 cancel scenarios + presence assertion)
2. All three CLI registration sites have at least 1 entry: `for site in sdk/src/query/command-{static-catalog-domain,manifest.non-family,aliases.generated}.ts; do grep -c workspace.parallel.cancel "$site" || echo 0; done` returns three values each >= 1 (catalog typically 2 for dotted+space; manifest 1; aliases 1)
3. UPSTREAM-02 discipline: `grep -E "from\s+['\"]\.\./backends/jj['\"]" sdk/src/vcs/jj/workspace-cleanup.ts` returns 0 lines
4. AbortSignal absent: `grep -E "AbortController|signal:" sdk/src/vcs/jj/workspace-cleanup.ts sdk/src/vcs/jj/parallel.ts sdk/src/vcs/git/parallel.ts sdk/src/query/workspace-parallel-cancel.ts` returns 0 lines (threat T-15.04-01 structurally verified)
5. `grep -c 'cancel' sdk/dist-cjs/vcs/types.d.ts >= 1` after `pnpm --filter sdk build`
6. CLI smoke executed: `node bin/gsd-sdk.cjs query workspace.parallel.cancel --help` exits 0 (or `node bin/gsd-sdk.cjs query workspace.parallel.cancel` with no args returns the structured `{ok:false, reason:'handle_required'}` envelope — proves the bridge is wired)
7. Plans 15.01, 15.02, 15.03 SUMMARYs exist (depends_on prerequisites — plan 15.04 should never start before all three prior plans commit)
</verification>

<success_criteria>
**Measurable completion (Phase 15 SC4 + SC5 / PARALLEL-07 / Open Question 2):**

- `vcs.workspace.parallel.cancel(handle)` returns within ≤2s for handles with ≤8 workspaces (SC4) — verified in test by `performance.now()` measurement at N=2 (extrapolation: N=8 stays under 2s because cleanup is per-workspace independent + filesystem-bound)
- Tears down materialized workspaces via `workspace.forget` + `rm -rf` on jj OR `worktree remove --force` + `branch -D` on git (CF-06)
- Does NOT signal subagent processes (CF-05 / threat T-15.04-01 — `spawnSync` exec layer cannot accept AbortSignal; structurally verified by AbortController-absent grep gate)
- Returns structured `CancelResult` envelope per D-01 (4 fields: `abandoned`, `failedReaped`, `surplusBookmarks`, `surplusWorkspaces`); NOT void or boolean
- Result is frozen pure JSON per Phase 9 D-05 invariant (test asserts `Object.isFrozen(result) === true`)
- CLI bridge `sdk/src/query/workspace-parallel-cancel.ts` registered at ALL THREE sites per CF-07 (catalog + manifest + aliases) — CLI smoke test exercises the actual dispatcher path
- Shared helper `cleanupSubagentWorkspaces` is the single owner (Task 1 Wave 1); Phase 16 CLEANUP-02 will consume the same helper for fanIn clean-path + dogfood-restore.sh (IP-5 cross-phase coherence)
- Open Question 2 resolved: helper first parameter named `mainRepoRoot` (NOT `phaseRoot` per D-05 — divergence documented inline in helper JSDoc and in this plan's SUMMARY)
- Full SDK suite green; dist-cjs rebuilt with cancel method + CancelResult interface
</success_criteria>

<output>
Create `.planning/phases/15-adapter-surface-extensions-rename/15-04-SUMMARY.md` when done. Capture:
- Open Question 2 resolution (param renamed from `phaseRoot` to `mainRepoRoot`; divergence-from-D-05 rationale)
- The 3 cancel test scenarios' results per backend (pass/fail + observed timing for `cancel-clean-abandon N=2`)
- The CLI smoke test result + the exact command used
- Confirmation that all three CLI sites are registered (grep evidence)
- Helper-isolation evidence (UPSTREAM-02 grep gate)
- Output of `pnpm --filter sdk test -- --run -t "workspace.parallel.cancel"` and `pnpm --filter sdk test -- --run -t "cleanupSubagentWorkspaces"` (pass count + duration)
- Phase-level SUMMARY pointers: all 4 plans (15.01..15.04) shipped; cross-phase note that Phase 16 CLEANUP-02 will consume `cleanupSubagentWorkspaces` (cite IP-5)
</output>
</content>
</invoke>