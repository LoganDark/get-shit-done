---
phase: 15-adapter-surface-extensions-rename
plan: 04
type: execute
wave: 4
depends_on:
  - 15-01
  - 15-02
  - 15-03
files_modified:
  - sdk/src/vcs/types.ts
  - sdk/src/vcs/backends.ts
  - sdk/src/vcs/backends/jj.ts
  - sdk/src/vcs/backends/git.ts
  - sdk/src/vcs/jj/parallel.ts
  - sdk/src/vcs/git/parallel.ts
  - sdk/src/vcs/jj/workspace-cleanup.ts
  - sdk/src/query/workspace-parallel-cancel.ts
  - sdk/src/query/command-static-catalog-domain.ts
  - sdk/src/query/command-manifest.non-family.ts
  - sdk/src/query/command-aliases.generated.ts
  - sdk/src/vcs/__tests__/jj-workspace-cleanup.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts
  - sdk/src/vcs/__tests__/backends.test.ts
  - tests/cli-workspace-parallel-cancel.test.cjs
autonomous: true
requirements:
  - PARALLEL-07
requirements_addressed:
  - PARALLEL-07
tags:
  - vcs-adapter
  - parallel
  - cancel
  - cleanup-helper
  - cli-bridge
  - upstream-02
user_setup: []

must_haves:
  truths:
    - "**N1 decision (helper signature):** This plan ships the 3-arg + `mainRepoRoot` rename variant per RESEARCH A1 + Open Q1 RESOLVED user ratification 2026-05-24 (CONTEXT D-05 + ROADMAP SC5 amended to match). Signature: `cleanupSubagentWorkspaces(mainRepoRoot: string, phaseNumber: number, workspaces?: readonly { name: string; path: string }[]): CleanupSubagentWorkspacesResult`. The first parameter is `mainRepoRoot` (NOT `phaseRoot`) because the body needs the colocated repo root for `jjArgvFlags + jj workspace forget`, not the phase root. The optional third parameter `workspaces` lets the cancel verb pass the Handle's authoritative list (resolves Pitfall 4 — `workspacePath` overrides leak when using pure-readdirSync enumeration); when omitted, the helper falls back to `readdirSync` for the dogfood-restore.sh recovery use case. Historical-context note in `<deviations>`."
    - "**N4 decision (CLI smoke placement):** Repo-side CLI smoke ships at `tests/cli-workspace-parallel-cancel.test.cjs` (node:test domain) per RESEARCH Wave 0 Gaps line 1400 + PATTERNS N4. Per-backend cancel scenarios ship at `sdk/src/vcs/__tests__/cmd-parallel-cancel-{jj,git}.test.ts` (vitest domain). Two domains keep responsibilities clean: the CLI smoke verifies three-site bridge resolution end-to-end via the `gsd-sdk query` dispatcher; the vitest scenarios verify per-backend cancel behavior at the adapter API."
    - "New `CancelResult` interface in `sdk/src/vcs/types.ts` declares the **4-field envelope** per CONTEXT D-01: `{abandoned: readonly string[]; failedReaped: readonly string[]; surplusBookmarks: readonly string[]; surplusWorkspaces: readonly string[]}`. JSDoc spells out the field semantics per D-02: `abandoned` = workspace identifiers fully torn down (agentId form per orchestrator-identifier consistency); `failedReaped` = identifiers that resisted teardown (length 0 on clean cancel); `surplusBookmarks` = bookmark names that needed force-delete; `surplusWorkspaces` = workspace paths still on disk pre-cancel (counted at entry regardless of cleanup outcome)."
    - "JSDoc on `CancelResult` documents idempotency invariant per D-03: re-calling `cancel(handle)` on an already-cancelled handle returns `CancelResult` with empty arrays (no error). Matches Phase 11 D-01 no-orchestrator-sidecar-state — the handle's `workspaces[]` array is the source of truth at call time."
    - "Interface `VcsWorkspaceParallel` at `sdk/src/vcs/types.ts:453-456` extends with `cancel(handle: ParallelDispatchHandle): CancelResult;` as the third method after `fanIn`. JSDoc mentions Phase 9 D-01 orchestrator-awaits-Agent invariant + `sdk/src/vcs/exec.ts:19` spawnSync STACK constraint + PROJECT.md OOS for mid-Agent process-kill cancellation (CF-05 enforcement)."
    - "New sidecar at `sdk/src/vcs/jj/workspace-cleanup.ts` per CONTEXT D-04 file boundary rationale (isolates from `reap.ts`'s W3(a) inspection contract). Header follows UPSTREAM-02 discipline per CONTEXT D-07: imports `vcsExec` from `'../exec.js'`, imports `existsSync`/`readdirSync`/`rmSync` from `'node:fs'`, imports `join` from `'node:path'`. NEVER imports from `'../backends/jj.js'` (UPSTREAM-02 enforcement)."
    - "Helper exports `CleanupSubagentWorkspacesResult` interface `{abandoned: readonly string[]; failedReaped: readonly string[]}` (partial-CancelResult shape per D-05) so 15.04 cancel verb body and 16.02 fanIn clean-branch + dogfood-restore.sh all unify helper output into respective envelopes without re-enumerating disk state."
    - "Helper body inlines `jjArgvFlags(repo: string): string[]` verbatim per UPSTREAM-02 sidecar template (PATTERNS Pattern S1; copy from `conflict-paths.ts:25-30` / `reap.ts:33-41`). Returns `['--repository', repo, '--no-pager', '--color', 'never', '--quiet']`."
    - "Helper is idempotent by contract per D-06: missing dirs are not errors (`existsSync` gate before `rmSync`); `jj workspace forget` non-zero is best-effort soft-fail (no `failedReaped` push on forget-fail alone); `rmSync` uses `{recursive: true, force: true}` (makes ENOENT a no-op); only `rmSync` exceptions populate `failedReaped`."
    - "Per-workspace teardown order per PATTERNS Pattern 5 + RESEARCH Example 5 (lines 1016-1041): (1) `vcsExec(mainRepoRoot, 'jj', [...jjArgvFlags(mainRepoRoot), 'workspace', 'forget', '--', ws.name])` — best-effort, exit code ignored; (2) `if (existsSync(ws.path))` gate, then `rmSync(ws.path, {recursive: true, force: true})` in try/catch — push to `abandoned[]` on success, `failedReaped[]` on caught exception; (3) if dir already gone pre-rm, skip silently (D-06 idempotency invariant — already-gone is not abandoned-this-call; second cancel returns all-empty arrays per D-03)."
    - "Helper enumeration strategy per D-06 + Pitfall 4 + N1 decision: when `workspaces` parameter is provided AND `length > 0`, iterate that list (authoritative — used by `cancel(handle)`). When omitted/empty, fall back to `readdirSync(join(mainRepoRoot, '.claude', 'jj-workspaces'))` filtered by regex `^phase-${String(phaseNumber).padStart(2, '0')}-subagent-\\d+$` (best-effort recovery — used by dogfood-restore.sh in Phase 16)."
    - "New `performJjParallelCancel(mainRepoRoot, handle): CancelResult` export added to `sdk/src/vcs/jj/parallel.ts` (sibling of `performJjParallelFanIn` at lines 287-508). Imports helper from `./workspace-cleanup.js`. Counts `surplusWorkspaces` at entry (per D-02 — counted regardless of cleanup outcome) by filtering `handle.workspaces` via `existsSync(ws.path)`. Delegates per-workspace teardown to the helper with `handle.workspaces` passed authoritatively. Sets `surplusBookmarks = []` on jj side by construction (Phase 11 D-02 retired per-subagent agent-bookmarks). Returns frozen pure-JSON `CancelResult` per Pattern S3 + Phase 9 D-05 invariant."
    - "New `performGitParallelCancel(mainRepoRoot, handle): CancelResult` export added to `sdk/src/vcs/git/parallel.ts` (sibling of `performGitParallelFanIn`). Inline teardown (NO shared helper — git's `worktree remove --force` already handles tree cleanup; orphan-dirs are jj-only per PROJECT.md OOS for cross-backend helper). Per-workspace: `vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', '--force', ws.path])` → push to `failedReaped[]` on non-zero, else `vcsExec(mainRepoRoot, 'git', ['branch', '-D', '--', 'worktree-agent-${ws.agentId}'])` → push to `surplusBookmarks[]` if branch was already gone, else push agentId to `abandoned[]`. Returns frozen pure-JSON `CancelResult`."
    - "Both backend wire-ins extend `workspace.parallel` Object.freeze block with the third `cancel:` entry: `backends/jj.ts:1257-1264` adds `cancel: (handle) => performJjParallelCancel(cwd, handle)`; `backends/git.ts:738-745` adds `cancel: (handle) => performGitParallelCancel(cwd, handle)`."
    - "Capability-matrix key `BACKENDS_AVAILABLE_FOR_VERB['workspace.parallel.cancel']` exists at `sdk/src/vcs/backends.ts` and equals `Object.freeze(['git', 'jj-colocated'] as const)`."
    - "New CLI bridge at `sdk/src/query/workspace-parallel-cancel.ts` mirrors the `workspace-parallel-fan-in.ts` shape verbatim per PATTERNS (lines 376-457): `--cwd <path>` defaults to projectDir; `--handle @-` reads stdin or `@<path>` reads file; rejects inline JSON form with `'expected @<path> or @-'` error; parses Handle JSON; on parse fail returns `{data: {ok: false, reason: 'handle_json_parse_failed', error}}`; on success calls `vcs.workspace.parallel.cancel(handle)` and returns `{data: cancelResult}` flat envelope."
    - "Three-site CLI bridge registration per CF-07 + PATTERNS Pattern S2 — ALL THREE files touched in the same commit (Pitfall 6 mitigation; missing any one breaks runtime verb resolution): (1) `sdk/src/query/command-static-catalog-domain.ts` adds import + 2 catalog entries (dot AND space alias forms — `['workspace.parallel.cancel', workspaceParallelCancelQuery]` + `['workspace parallel.cancel', workspaceParallelCancelQuery]`); (2) `sdk/src/query/command-manifest.non-family.ts` adds `{ canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true, outputMode: 'json' }`; (3) `sdk/src/query/command-aliases.generated.ts` adds `{ canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true }`."
    - "Per-backend cancel scenarios in `sdk/src/vcs/__tests__/cmd-parallel-cancel-{jj,git}.test.ts` (NEW files; mirror SC5 scenario shape at `cmd-parallel-jj.test.ts:474-545`). Each file covers 3 scenarios per VALIDATION lines 58-61: (1) `cancel-clean-abandon` — dispatch N=2, cancel, assert `abandoned.length === 2`, `failedReaped.length === 0`, `surplusWorkspaces.length === 2`, both workspace dirs gone post-cancel; (2) `cancel-idempotent-recall` — dispatch N=1, cancel, second cancel returns all-empty arrays per D-03; (3) `cancel-partial-state-recovery` — dispatch N=2, pre-delete one workspace dir before cancel, assert `failedReaped` length 0, `surplusWorkspaces` reflects the pre-cancel observable count. Plus a 4th invariant scenario: `Object.isFrozen(result) === true` AND JSON round-trip is lossless (Phase 9 D-05 invariant)."
    - "Cancel scenarios use `setupJjRepo()` / `setupGitRepo()` Pattern B random-prefix mkdtemp + per-describe `beforeAll`/`afterAll` rmSync per PATTERNS Pattern S5 (D-15 / Pitfall 9). NEVER use `retry: N`. NEVER use `skip` modifier on describe/it/test — flakes fixed at fixture level."
    - "Cancel scenarios OMIT `mainBookmarks` from dispatch opts entirely (cancel verb does NOT need bookmark-driven advance — the scenarios test workspace teardown, which is bookmark-orthogonal per the Phase 14.1 PARALLEL-08 bookmark-less-dispatch contract). NO stale `mainBookmark: 'main'` literal anywhere in either file (per PATTERNS Pitfall 7 — the prior 15-04-PLAN.md was already deleted as part of this replan per orchestrator N2 note; no pre-patch required since the plans are being built fresh)."
    - "New helper-unit-test file `sdk/src/vcs/__tests__/jj-workspace-cleanup.test.ts` (mirrors `jj-id-alphabet-probe.test.ts` single-purpose jj-skipIf shape per PATTERNS lines 742-784) covers 4 unit scenarios per VALIDATION lines 56-57: (1) idempotency — missing dirs are not errors, second call returns empty arrays; (2) UPSTREAM-02 import-discipline — grep-style assertion that helper file has zero `from '../backends/jj'` imports; (3) Handle-authoritative enumeration — covers `workspacePath` overrides (Pitfall 4); (4) readdirSync fallback — when `workspaces` omitted, helper enumerates `phase-${NN}-subagent-*` correctly."
    - "Repo-side CLI smoke at `tests/cli-workspace-parallel-cancel.test.cjs` (node:test domain — N4 decision) covers: (1) `gsd-sdk query workspace.parallel.cancel` (no handle, canonical dot form) returns `{ok: false, reason: 'handle_required'}` JSON — proves the canonical verb resolves to the registered bridge; (2) `gsd-sdk query workspace.parallel.cancel --handle @-` with invalid JSON on stdin returns `{ok: false, reason: 'handle_json_parse_failed'}`; (3) `gsd-sdk query 'workspace parallel.cancel'` (space-alias form) without handle returns the same `{ok: false, reason: 'handle_required'}` envelope — proves the alias-table site registered the space form; (4) `gsd-sdk query workspace.parallel.cancellation-xyz` (bogus verb negative control) exits non-zero with an unknown-verb-style error marker — proves the verb-resolution layer is the gate. NOTE: NO `--help` flag is tested (the gsd-sdk CLI bridge does not implement `--help`; out of scope for this plan)."
    - "backends regression test in `sdk/src/vcs/__tests__/backends.test.ts` asserts new `'workspace.parallel.cancel'` capability-matrix key is defined and equals `['git', 'jj-colocated']` (Pitfall 1 / Pitfall 3 prevention)."
    - "`pnpm tsc --noEmit` green after type additions; `pnpm vitest run` over the touched test files green; `node --test tests/cli-workspace-parallel-cancel.test.cjs` green; per-backend cancel scenarios complete within ≤2s for handles with ≤8 workspaces per ROADMAP Phase 15 SC4."
  artifacts:
    - path: "sdk/src/vcs/jj/workspace-cleanup.ts"
      provides: "Shared cleanupSubagentWorkspaces helper (Wave 1 — single owner per IP-5)"
      contains: "export function cleanupSubagentWorkspaces"
      min_lines: 50
    - path: "sdk/src/vcs/types.ts"
      provides: "CancelResult interface (4-field envelope per D-01) + VcsWorkspaceParallel.cancel method"
      contains: "export interface CancelResult"
    - path: "sdk/src/vcs/jj/parallel.ts"
      provides: "performJjParallelCancel sidecar export"
      contains: "export function performJjParallelCancel"
    - path: "sdk/src/vcs/git/parallel.ts"
      provides: "performGitParallelCancel sidecar export"
      contains: "export function performGitParallelCancel"
    - path: "sdk/src/vcs/backends.ts"
      provides: "Capability matrix entry 'workspace.parallel.cancel'"
      contains: "'workspace.parallel.cancel'"
    - path: "sdk/src/query/workspace-parallel-cancel.ts"
      provides: "CLI bridge mirroring workspace-parallel-fan-in.ts shape"
      contains: "workspaceParallelCancelQuery"
    - path: "sdk/src/vcs/__tests__/jj-workspace-cleanup.test.ts"
      provides: "Helper unit tests (4 scenarios — idempotency, UPSTREAM-02, Handle-auth, readdirSync fallback)"
      contains: "cleanupSubagentWorkspaces"
    - path: "sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts"
      provides: "jj-side per-backend cancel scenarios (3 + frozen-JSON invariant)"
      contains: "workspace.parallel.cancel"
    - path: "sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts"
      provides: "git-side per-backend cancel scenarios (3 + frozen-JSON invariant)"
      contains: "workspace.parallel.cancel"
    - path: "tests/cli-workspace-parallel-cancel.test.cjs"
      provides: "CLI bridge integration smoke (three-site resolution + JSON envelope shape)"
      contains: "workspace.parallel.cancel"
  key_links:
    - from: "sdk/src/vcs/types.ts (VcsWorkspaceParallel.cancel + CancelResult)"
      to: "sdk/src/vcs/backends/{git,jj}.ts (workspace.parallel.cancel wire-in)"
      via: "satisfies-style structural typing on the parallel Object.freeze"
      pattern: "cancel:\\s*\\(handle"
    - from: "sdk/src/vcs/backends/jj.ts (cancel wire-in)"
      to: "sdk/src/vcs/jj/parallel.ts (performJjParallelCancel)"
      via: "delegation through Object.freeze"
      pattern: "performJjParallelCancel\\(cwd,\\s*handle\\)"
    - from: "sdk/src/vcs/jj/parallel.ts (performJjParallelCancel)"
      to: "sdk/src/vcs/jj/workspace-cleanup.ts (cleanupSubagentWorkspaces helper)"
      via: "ES import from sidecar"
      pattern: "import\\s*\\{\\s*cleanupSubagentWorkspaces\\s*\\}\\s*from\\s*['\"]\\.\\/workspace-cleanup"
    - from: "sdk/src/query/workspace-parallel-cancel.ts (CLI bridge)"
      to: "Three-site registration (catalog + manifest + aliases)"
      via: "ES import in command-static-catalog-domain.ts + manual entries in 2 other files"
      pattern: "workspaceParallelCancelQuery|workspace\\.parallel\\.cancel"
    - from: "sdk/src/vcs/jj/parallel.ts (performJjParallelCancel return)"
      to: "Frozen pure-JSON CancelResult"
      via: "Object.freeze({...}) satisfies CancelResult per Pattern S3"
      pattern: "Object\\.freeze\\(\\{[^}]*abandoned"
---

<objective>
Plan 15.04 (PARALLEL-07) ships the `vcs.workspace.parallel.cancel(handle)` synchronous-teardown verb on both backends per CF-05 STACK-lens semantics. Two phases within this plan: **Wave 1** extracts the shared `cleanupSubagentWorkspaces` helper into a new UPSTREAM-02 sidecar at `sdk/src/vcs/jj/workspace-cleanup.ts` (single owner per IP-5 / Pitfall 11); **Wave 2** wires the cancel verb body (per-backend `performXParallelCancel` exports + backend wire-ins + capability-matrix row + 4-field `CancelResult` interface + CLI bridge with three-site registration + per-backend scenarios + helper unit tests + CLI smoke).

The cancel verb is **post-mortem cleanup**, NOT in-flight signaling. The `spawnSync` exec layer at `sdk/src/vcs/exec.ts:19` cannot accept `AbortSignal`; the Phase 9 D-01 orchestrator-awaits-`Agent()` invariant + Phase 11 D-01 orchestrator-manages-workspaces invariant make mid-flight cancel a non-problem in production. Operator kills subagents via Claude Code UI/CLI; cancel cleans up the materialized workspaces afterward.

Cancel returns a **structured `CancelResult` envelope** (NOT void / boolean) per CONTEXT D-01: `{abandoned, failedReaped, surplusBookmarks, surplusWorkspaces}` — 4 fields mirroring `FanInResult.failedReaped` + `surplusBookmarks` naming verbatim. The handle's `workspaces[]` array is the source of truth at call time (per Phase 11 D-01 no-orchestrator-sidecar-state invariant). Cancel is idempotent — re-call on already-cancelled handle returns all-empty arrays (no error).

Output:
- **Wave 1:** New sidecar `sdk/src/vcs/jj/workspace-cleanup.ts` (UPSTREAM-02; idempotent; 3-arg signature per N1 decision deviating from CONTEXT D-05 literal) + helper unit test file `sdk/src/vcs/__tests__/jj-workspace-cleanup.test.ts`
- **Wave 2:** `CancelResult` interface + `cancel` method on `VcsWorkspaceParallel` in `types.ts` + `performJjParallelCancel` export in `jj/parallel.ts` (delegates to helper) + `performGitParallelCancel` export in `git/parallel.ts` (inline teardown — no shared helper) + both backend wire-ins extending `workspace.parallel` freeze block + capability-matrix row + CLI bridge + three-site registration + per-backend scenario test files + repo-side CLI smoke + backends.test.ts regression entry

Sequential after 15.01 + 15.02 + 15.03. Inherits clean `rootRevisions`/`idAlphabet`/`matchPrefix` namespace. File-overlap with prior plans on `types.ts` + `backends.ts` + both backend files — sequential ordering forced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md
@.planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md
@.planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md
@.planning/phases/15-adapter-surface-extensions-rename/15-VALIDATION.md
@.planning/phases/15-adapter-surface-extensions-rename/15-01-SUMMARY.md
@.planning/phases/15-adapter-surface-extensions-rename/15-02-SUMMARY.md
@.planning/phases/15-adapter-surface-extensions-rename/15-03-SUMMARY.md
@sdk/src/vcs/jj/conflict-paths.ts
@sdk/src/vcs/jj/reap.ts
@sdk/src/vcs/jj/parallel.ts
@sdk/src/vcs/git/parallel.ts
@sdk/src/vcs/jj/octopus.ts
@sdk/src/query/workspace-parallel-fan-in.ts
@sdk/src/query/workspace-parallel-dispatch.ts
@sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
@sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts

<interfaces>
<!-- Live anchors verified against post-14.1, post-15.01..15.03 working tree. -->

UPSTREAM-02 sidecar template (sdk/src/vcs/jj/conflict-paths.ts:1-30):
- File header doc block: contract notes + UPSTREAM-02 discipline statement
- `import { vcsExec } from '../exec.js';` (canonical import path)
- Inline `function jjArgvFlags(repo: string): string[]` returning `['--repository', repo, '--no-pager', '--color', 'never', '--quiet']`
- NEVER imports from `'../backends/jj.js'`

reap.ts per-workspace teardown body (sdk/src/vcs/jj/reap.ts:170-189):
- Per-workspace forget+rm-rf pattern (but with strict-throw semantics — cancel diverges to soft-fail per D-06)
- `[...jjArgvFlags(repo), 'workspace', 'forget', '--', entry.name]`
- `if (existsSync(entry.path)) { rmSync(entry.path, {recursive: true, force: true}); }`

octopus.ts workspace path convention (sdk/src/vcs/jj/octopus.ts:300-302):
- `const workspaceName = 'phase-${phaseTag}-subagent-${opts.idx}';`
- `opts.workspacePath ?? join(mainRepoRoot, '.claude/jj-workspaces', workspaceName);`
- Helper enumeration default uses same path convention

jj/parallel.ts FanInResult freeze pattern (sdk/src/vcs/jj/parallel.ts:500-507):
- `return Object.freeze({merged: Object.freeze(merged.slice()) as readonly string[], conflicted, ..., failedReaped: Object.freeze(...), surplusBookmarks: Object.freeze(...)}) satisfies FanInResult;`
- Apply same freeze pattern to CancelResult per Pattern S3

jj/parallel.ts mainBookmarks two-pass loop (sdk/src/vcs/jj/parallel.ts:428-449):
- Pattern 7 validate-then-mutate; cancel does NOT need this for primary path (handle.workspaces already validated at dispatch time per CF-04 alignment), documented for future defensive use only

git/parallel.ts validators (sdk/src/vcs/git/parallel.ts:113-119):
- Inline `validateAgentId` regex — used by git-side worktree path validation
- `worktree-agent-{agentId}` branch naming convention per Phase 10 D-01

backends/jj.ts parallel namespace wire-in (sdk/src/vcs/backends/jj.ts:1257-1264):
- Existing `parallel: Object.freeze({dispatch, fanIn})` block — cancel adds as third entry
- `dispatch: (opts) => performJjParallelDispatch({mainRepoRoot: cwd, vcs: {workspace}, ...opts})`
- `fanIn: (handle, results) => performJjParallelFanIn(cwd, handle, results)`

backends/git.ts parallel namespace wire-in (sdk/src/vcs/backends/git.ts:738-745):
- Identical shape to jj — cancel adds as third entry

CLI bridge precedent (sdk/src/query/workspace-parallel-fan-in.ts — full file):
- `import { readFileSync } from 'node:fs';`
- `import { createVcsAdapter } from '../vcs/index.js';`
- `function resolveFileOrStdin(raw: string): string` — `@-` reads stdin, `@<path>` reads file, inline rejected
- `export const workspaceParallelFanInQuery: QueryHandler = async (args, projectDir) => { ... }`
- Cancel mirrors the shape minus `--results` parameter; envelope `{data: cancelResult}` per Phase 11 plan 02 convention

Three CLI registration sites (verified live, post-15.03 unchanged):
- sdk/src/query/command-static-catalog-domain.ts:21-22 (imports) + :71-74 (catalog entries with dot + space aliases)
- sdk/src/query/command-manifest.non-family.ts:61-62 (mutation/outputMode declaration)
- sdk/src/query/command-aliases.generated.ts:156-157 (alias lookup entry)

SC5 scenario test template (sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts:474-545):
- `describe.sequential.skipIf(!jjAvailable)(...)` Pattern A
- `setupJjRepo()` helper at lines 64-87 — random-prefix mkdtemp; `jj git init --colocate`; user/email config; seed commit; materialize `.planning/phases/{NN}-test/` for derivePhaseRoot
- `beforeAll` / `afterAll` (rmSync recursive)
- `it('...', { timeout: 30000 }, () => { ... })`
- D-15 invariants: NEVER use retry; NEVER use skip modifier

Helper unit test template (sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts — full file):
- jj-availability probe + skipIf
- mkdtempSync + jj init colocate
- single describe.skipIf(!jjAvailable)
- focused test scope per file
</interfaces>
</context>

<deviations>
**N1 (helper signature):** Ships 3-arg + `mainRepoRoot` rename variant — amended CONTEXT D-05 per Open Q1 RESOLVED user ratification 2026-05-24. CONTEXT D-05 was re-amended 2026-05-24 to align the `workspaces?` element type with the plan and Pitfall 4 mitigation (iteration-1 amendment carried a typo — `readonly string[]` instead of the object form `readonly { name: string; path: string }[]` that Pitfall 4 requires for `workspacePath` overrides). (Historical context: original CONTEXT D-05 literal was 2-arg `cleanupSubagentWorkspaces(phaseRoot, phaseNumber)`; user-approved amendment on 2026-05-24 updated D-05 + ROADMAP SC5 to the 3-arg form documented here.) Rationale: (a) `jjArgvFlags + jj workspace forget` need the colocated repo root, NOT the phase-dir path which sits 3 levels deep inside `.planning/phases/{NN-slug}/`; (b) optional third parameter `workspaces?: readonly { name: string; path: string }[]` lets the cancel verb pass the Handle's authoritative list resolving Pitfall 4 (`workspacePath` overrides leak when using pure-readdirSync); (c) matches existing sidecar signature pattern (`conflict-paths.ts::enumerateConflictedPaths(cwd, rev)` takes repo root, not phase root). Recorded here so Phase 16 CLEANUP-02 consumer stays aligned. The signature documents the dual-mode (Handle-provided vs readdirSync-fallback) in JSDoc.

**N2 (pre-patch of obsolete 15-04 references):** N/A — the prior 15-04-PLAN.md was deleted as part of this replan per orchestrator context line 109. Build clean; no stale `mainBookmark: 'main'` literals exist in the new test files (Pitfall 7 mitigation is built-in to fresh content).

**N3 (rootCommits hit count):** Handled by Plan 15.01 — the audit JSON `totalCount` field is the source of truth at execute time. Out of scope for 15.04.

**N4 (CLI smoke placement):** Repo-side CLI smoke at `tests/cli-workspace-parallel-cancel.test.cjs` (node:test domain) per RESEARCH Wave 0 Gaps line 1400. Per-backend cancel scenarios at `sdk/src/vcs/__tests__/cmd-parallel-cancel-{jj,git}.test.ts` (vitest domain). Two-domain split keeps responsibilities clean.
</deviations>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `cancel(handle)` caller → workspace teardown | Handle's `workspaces[]` array is the source of truth (Phase 11 D-01 no-orchestrator-sidecar-state); helper uses Handle-authoritative enumeration to avoid Pitfall 4 leaks |
| Helper `vcsExec(... 'jj', ['workspace', 'forget', '--', ws.name])` | Workspace names derived from validated `phase-{NN}-subagent-{idx}` pattern at dispatch; `--` separator before name is defense-in-depth |
| Helper `rmSync(ws.path, {recursive: true, force: true})` | Path bounded by Handle-supplied list; no glob expansion; `force: true` makes ENOENT a no-op (idempotency invariant) |
| CLI bridge JSON Handle parsing | Caller-supplied JSON via stdin or file; `JSON.parse` fail returns `{ok: false, reason: 'handle_json_parse_failed'}` envelope; no eval/Function-constructor |
| Three-site CLI registration | Missing any one site breaks runtime verb resolution (Pitfall 6 / CF-07) |
| Helper UPSTREAM-02 sidecar discipline | NEVER import from `'../backends/jj.js'` — would force merge conflict on upstream-rebase cycles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.04-01 | Tampering | Cancel violates spawnSync STACK invariant (introduces AbortController / async-spawn) | mitigate | CF-05 explicit prohibition; cancel JSDoc cites PROJECT.md OOS for mid-Agent process-kill; PR diff review-gate via grep `-E 'AbortController\|AbortSignal\|child\\.kill\|child_process\\.spawn\\(' sdk/src/vcs/{jj,git}/parallel.ts` returning zero new matches |
| T-15.04-02 | Tampering | Three-site CLI bridge registration misses one site (Pitfall 6) | mitigate | Plan task atomically touches all 3 files in single commit; backends.test.ts regression-asserts capability-matrix presence; tests/cli-workspace-parallel-cancel.test.cjs smoke verifies end-to-end resolution via the no-handle test (canonical dot form) AND the space-alias test (alias-table site), AND the bogus-verb negative control test confirms the verb-resolution layer is the gate — proves all three registration sites are wired. (No `--help` flag is tested; CLI bridge does not implement it.) |
| T-15.04-03 | Tampering | Idempotency violation — re-call on cancelled handle throws | mitigate | Helper D-06 contract: `existsSync` gate, `force: true` on rmSync, soft-fail on `jj workspace forget` non-zero, skip silently on already-gone path (D-06 idempotency); cmd-parallel-cancel-{jj,git}.test.ts scenario 2 asserts second-call returns all-empty arrays (D-03 invariant) |
| T-15.04-04 | Tampering | UPSTREAM-02 violation — helper imports from backends/jj.ts | mitigate | Helper unit test (`jj-workspace-cleanup.test.ts` scenario 2) asserts via file-read grep that the helper source has zero `from '../backends/jj'` imports |
| T-15.04-05 | Tampering | Sidecar Handle-list enumeration misses non-standard `workspacePath` overrides (Pitfall 4) | mitigate | N1 decision — helper takes optional `workspaces` parameter; cancel verb passes `handle.workspaces` authoritatively; readdirSync fallback used only by dogfood-restore.sh recovery path (Phase 16) |
| T-15.04-06 | Tampering | Path traversal via crafted `workspacePath` | mitigate | Workspace path components come from validated `agentId` at dispatch (octopus.ts:300-302); helper iterates Handle-supplied paths only; no shell glob expansion; `rm -rf` bounded to Handle-listed paths |
| T-15.04-07 | DoS | Memory exhaustion via huge `handle.workspaces[]` array | accept | Bounded by orchestrator dispatch (maxConcurrency default ≤8 workspaces typical); ROADMAP SC4 timing budget ≤2s for ≤8 workspaces |
| T-15.04-08 | Information disclosure | CancelResult leaks internal paths (workspace.path values) in surplusWorkspaces | accept | Paths are caller-supplied via the Handle the caller already holds; no privilege escalation |
| T-15.04-09 | Tampering | CancelResult not frozen pure-JSON (Phase 9 D-05 violation) | mitigate | Per-backend scenarios assert `Object.isFrozen(result) === true` AND `JSON.parse(JSON.stringify(result))` round-trips losslessly |
| T-15.04-10 | Tampering | Stale `mainBookmark: 'main'` references survive into new test files (Pitfall 7) | mitigate | N2 — prior 15-04-PLAN.md deleted as part of replan; cancel scenarios OMIT mainBookmarks entirely (cancel is bookmark-orthogonal); grep `-c 'mainBookmark:' sdk/src/vcs/__tests__/cmd-parallel-cancel-{jj,git}.test.ts` returns 0 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 1): Extract cleanupSubagentWorkspaces helper sidecar + unit tests</name>
  <files>sdk/src/vcs/jj/workspace-cleanup.ts, sdk/src/vcs/__tests__/jj-workspace-cleanup.test.ts</files>
  <read_first>
    - sdk/src/vcs/jj/conflict-paths.ts (full file — UPSTREAM-02 sidecar template; copy header block + jjArgvFlags inline verbatim)
    - sdk/src/vcs/jj/reap.ts (lines 1-50 for header conventions; lines 170-189 for per-workspace forget+rm-rf body pattern — adapt to soft-fail semantics per D-06)
    - sdk/src/vcs/jj/octopus.ts (lines 295-310 for `phase-${phaseTag}-subagent-${idx}` path convention)
    - sdk/src/vcs/__tests__/jj-id-alphabet-probe.test.ts (full file — single-purpose jj-skipIf test template)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (Example 5 helper body shape at lines 929-1041)
    - .planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md (D-04 through D-08 — helper location, signature, idempotency, UPSTREAM-02, Phase 16 noted-not-built note)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (lines 306-372 — Wave 1 helper extraction guidance + Pitfall 4 + N1 decision note)
  </read_first>
  <action>
    Create `sdk/src/vcs/jj/workspace-cleanup.ts` per PATTERNS Pattern S1 + Pattern 5. File contents organized as:

    **Header doc block** (~25 lines): file purpose ("Phase 15.04 Wave 1 — shared helper for tearing down materialized subagent workspaces"); three consumers per IP-5 single-owner pattern (cancel verb body, 16.02 fanIn clean-path branch, 16.02 dogfood-restore.sh via Phase 16 CLI bridge); contract divergence from reap.ts (W3(a) inspection contract preserved by reap; this helper tears down everything explicitly requested — file boundary makes divergence enforceable per D-04); UPSTREAM-02 discipline statement; idempotency contract per D-06.

    **Imports:** `import { existsSync, readdirSync, rmSync } from 'node:fs';` + `import { join } from 'node:path';` + `import { vcsExec } from '../exec.js';`. NEVER `from '../backends/jj.js'`.

    **Inline jjArgvFlags helper** (verbatim copy from conflict-paths.ts:25-30 template): `function jjArgvFlags(repo: string): string[] { return ['--repository', repo, '--no-pager', '--color', 'never', '--quiet']; }`.

    **Exported interface `CleanupSubagentWorkspacesResult`** per D-05: `export interface CleanupSubagentWorkspacesResult { abandoned: readonly string[]; failedReaped: readonly string[]; }`. JSDoc cites D-05 partial-CancelResult shape rationale.

    **Exported function `cleanupSubagentWorkspaces`** per N1 decision (3-arg + mainRepoRoot rename): `export function cleanupSubagentWorkspaces(mainRepoRoot: string, phaseNumber: number, workspaces?: readonly { name: string; path: string }[]): CleanupSubagentWorkspacesResult`. JSDoc covers: (1) UPSTREAM-02 discipline; (2) idempotency contract per D-06; (3) per-workspace teardown order (forget → existsSync gate → rmSync try/catch → push to abandoned/failedReaped); (4) dual-mode enumeration — Handle-authoritative when `workspaces` provided, readdirSync fallback when omitted; (5) N1 deviation note pointing to plan 15-04 `<deviations>` block.

    **Function body per RESEARCH Example 5 (lines 996-1040):**
    1. Compute `phaseTag = String(phaseNumber).padStart(2, '0')`.
    2. Build `iterList`: if `workspaces && workspaces.length > 0` use it directly; else `workspacesDir = join(mainRepoRoot, '.claude', 'jj-workspaces')`, if `!existsSync(workspacesDir)` return `{abandoned: [], failedReaped: []}` early; else `pattern = new RegExp('^phase-' + phaseTag + '-subagent-\\\\d+$')`, `iterList = readdirSync(workspacesDir).filter(d => pattern.test(d)).map(d => ({name: d, path: join(workspacesDir, d)}))`.
    3. Initialize `abandoned: string[] = []; failedReaped: string[] = [];`.
    4. For each `ws` in `iterList`:
       - `const forgetArgs = [...jjArgvFlags(mainRepoRoot), 'workspace', 'forget', '--', ws.name];`
       - `const forgetRes = vcsExec(mainRepoRoot, 'jj', forgetArgs);` (best-effort; exit code IGNORED per D-06 — do NOT push to failedReaped on forget-fail alone)
       - `if (existsSync(ws.path)) { try { rmSync(ws.path, {recursive: true, force: true}); abandoned.push(ws.name); } catch (err) { failedReaped.push(ws.name); } } else { /* dir already gone — D-06 idempotent skip; do NOT push to abandoned[] (would violate D-03 empty-arrays-on-re-call) */ }` (already-gone is a silent no-op per D-06; the second cancel returns all-empty arrays per D-03)
    5. Return `{abandoned, failedReaped}` (raw arrays; the cancel verb body wraps them in `Object.freeze(...slice()) as readonly string[]` per Pattern S3).

    Create `sdk/src/vcs/__tests__/jj-workspace-cleanup.test.ts` per PATTERNS lines 742-784 (mirrors jj-id-alphabet-probe.test.ts shape):

    **File structure:**
    - `import { describe, it, expect, beforeAll, afterAll } from 'vitest';`
    - `import { mkdtempSync, rmSync, mkdirSync, writeFileSync, readFileSync, existsSync } from 'node:fs';`
    - `import { tmpdir } from 'node:os';`
    - `import { join } from 'node:path';`
    - `import { execSync } from 'node:child_process';`
    - `import { cleanupSubagentWorkspaces } from '../jj/workspace-cleanup.js';`
    - jj-availability probe (try `execSync('jj --version', {stdio: 'pipe'})`; set `jjAvailable = true` on success, `false` on catch).

    **`describe.skipIf(!jjAvailable)('cleanupSubagentWorkspaces helper', () => { ... })` block with 4 unit tests:**

    1. `it('idempotency: missing dirs are not errors; second call returns empty arrays', () => {...})`. Setup: mkdtempSync + jj git init --colocate. Call helper with empty `workspaces` array and a nonexistent `mainRepoRoot/.claude/jj-workspaces/` — assert returns `{abandoned: [], failedReaped: []}`. Then mkdir the workspaces dir + create a fake `phase-15-subagent-0` directory; call helper with `workspaces = [{name: 'phase-15-subagent-0', path: <fake_path>}]` — assert `abandoned` contains the name AND the fake dir is gone post-call. Second-call: re-invoke with same args; assert returns all-empty arrays (idempotency invariant per D-03/D-06).

    2. `it('UPSTREAM-02: helper source has zero imports from backends/jj', () => {...})`. Read the file contents via `readFileSync('sdk/src/vcs/jj/workspace-cleanup.ts', 'utf-8')`. Assert `expect(contents).not.toMatch(/from\\s+['"]\\.\\.\\/backends\\/jj/);` (no relative import path resolves to backends/jj.ts).

    3. `it('Handle-authoritative enumeration: covers non-standard workspacePath overrides (Pitfall 4)', () => {...})`. Setup colocated jj repo. Create a workspace dir at a non-standard path (e.g. `/tmp/custom-ws-cleanup-test/foo`). Call helper with `workspaces = [{name: 'override-name', path: '/tmp/custom-ws-cleanup-test/foo'}]`. Assert the override path is cleaned up.

    4. `it('readdirSync fallback: enumerates phase-NN-subagent-* when workspaces omitted', () => {...})`. Setup colocated jj repo. mkdir `mainRepoRoot/.claude/jj-workspaces/phase-15-subagent-{0,1,2}` directories. Call helper WITHOUT the `workspaces` parameter (or empty array). Assert all 3 phase-15-subagent-* dirs are enumerated AND cleaned up. Also create a `phase-16-subagent-0` directory (different phaseNumber); assert the phaseNumber=15 helper call does NOT touch the phase-16 dir.

    All scenarios use `beforeAll` to set up the mkdtemp repo and `afterAll` to `rmSync(dir, {recursive: true, force: true})`.

    Run `cd sdk && pnpm vitest run src/vcs/__tests__/jj-workspace-cleanup.test.ts --reporter=dot` — must exit 0 with all 4 tests passing (assuming jjAvailable=true on the build host).

    Commit Wave 1 as `feat(15-04): extract cleanupSubagentWorkspaces helper sidecar (Wave 1 — single owner per IP-5)`.
  </action>
  <verify>
    <automated>cd sdk &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm vitest run src/vcs/__tests__/jj-workspace-cleanup.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/vcs/jj/workspace-cleanup.ts` exists with the file-header doc block citing UPSTREAM-02, IP-5 single-owner pattern, three consumers, and D-04 contract divergence from reap.ts.
    - `grep -c "from '../backends/jj" sdk/src/vcs/jj/workspace-cleanup.ts` returns 0 (UPSTREAM-02 enforcement).
    - `grep -c "function jjArgvFlags" sdk/src/vcs/jj/workspace-cleanup.ts` returns 1 (inlined per template).
    - `grep -c "export interface CleanupSubagentWorkspacesResult" sdk/src/vcs/jj/workspace-cleanup.ts` returns 1.
    - `grep -c "export function cleanupSubagentWorkspaces" sdk/src/vcs/jj/workspace-cleanup.ts` returns 1.
    - Signature line contains all three parameters: `mainRepoRoot: string`, `phaseNumber: number`, AND `workspaces?: readonly` — verifiable by grep `-E 'mainRepoRoot.*phaseNumber.*workspaces' sdk/src/vcs/jj/workspace-cleanup.ts`.
    - JSDoc on the function references N1 deviation (mentions `mainRepoRoot` vs `phaseRoot` rationale) and the `<deviations>` block.
    - Helper body uses `existsSync` gate before `rmSync`, uses `{recursive: true, force: true}` on rmSync, and uses try/catch around rmSync to populate `failedReaped` — verifiable by greps on the file.
    - Helper body does NOT push to `failedReaped` on `jj workspace forget` non-zero (best-effort soft-fail per D-06) — verifiable by inspecting that the `forgetRes.exitCode` value is not branched-on with a `failedReaped.push` consequence.
    - `sdk/src/vcs/__tests__/jj-workspace-cleanup.test.ts` exists with 4 `it(...)` blocks corresponding to the 4 unit scenarios.
    - `cd sdk && pnpm tsc --noEmit && pnpm vitest run src/vcs/__tests__/jj-workspace-cleanup.test.ts --reporter=dot` exits 0 with 4 tests passing (when jjAvailable; otherwise all skip per the jj-skipIf).
    - Working copy is clean after Wave 1 commit (`assert_clean_wc`).
  </acceptance_criteria>
  <done>Wave 1 helper sidecar + unit tests landed. The helper is the canonical surface that 15.04 cancel verb body + 16.02 fanIn clean-branch + 16.02 dogfood-restore.sh all consume — single owner per IP-5 / Pitfall 11.</done>
</task>

<task type="auto">
  <name>Task 2 (Wave 2a): Declare CancelResult interface + extend VcsWorkspaceParallel.cancel + capability-matrix row</name>
  <files>sdk/src/vcs/types.ts, sdk/src/vcs/backends.ts</files>
  <read_first>
    - sdk/src/vcs/types.ts (lines 453-456 — VcsWorkspaceParallel; lines 504-537 — ParallelDispatchHandle; lines 552-560 — FanInResult precedent for failedReaped + surplusBookmarks field names)
    - sdk/src/vcs/backends.ts (lines 75-138 — capability matrix; insertion alongside `'workspace.parallel.dispatch'` + `'workspace.parallel.fan-in'` if those entries exist, or in the standard 'workspace.*' cluster)
    - .planning/phases/15-adapter-surface-extensions-rename/15-CONTEXT.md (D-01 through D-03 — CancelResult 4-field envelope + idempotency)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (Example 4 CancelResult interface + cancel body shape at lines 787-927)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (lines 33-86 — types.ts CancelResult insertion notes)
  </read_first>
  <action>
    Add to `sdk/src/vcs/types.ts` after the `FanInResult` interface (currently at lines 552-560 — insertion point is after the closing `}` of FanInResult, before the next interface).

    JSDoc block must reference: `15.04`, `PARALLEL-07`, the 4-field shape per D-01, field semantics per D-02 (abandoned/failedReaped/surplusBookmarks/surplusWorkspaces meanings), idempotency invariant per D-03 (re-call returns all-empty arrays), Phase 9 D-05 frozen-pure-JSON invariant (survives JSON.stringify→JSON.parse round-trip with no semantic loss), and the FanInResult `failedReaped + surplusBookmarks` naming mirror at lines 558-559.

    Interface declaration:

    `export interface CancelResult {`
    `  abandoned: readonly string[];`
    `  failedReaped: readonly string[];`
    `  surplusBookmarks: readonly string[];`
    `  surplusWorkspaces: readonly string[];`
    `}`

    Extend `VcsWorkspaceParallel` interface at `sdk/src/vcs/types.ts:453-456` to add `cancel` as the third method after `fanIn`:

    `export interface VcsWorkspaceParallel {`
    `  dispatch(opts: ParallelDispatchOpts): ParallelDispatchHandle;`
    `  fanIn(handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult;`
    `  cancel(handle: ParallelDispatchHandle): CancelResult;`
    `}`

    JSDoc on `cancel` cites: CF-05 STACK-lens (synchronous teardown only, no signal handling); Phase 9 D-01 orchestrator-awaits-Agent invariant; Phase 11 D-01 no-orchestrator-sidecar-state; PROJECT.md OOS for mid-Agent process-kill cancellation; `sdk/src/vcs/exec.ts:19` spawnSync STACK constraint.

    Add capability-matrix row in `sdk/src/vcs/backends.ts` adjacent to the existing `'workspace.parallel.dispatch'` + `'workspace.parallel.fan-in'` entries (search the file for those entries; insert below them):

    `'workspace.parallel.cancel': Object.freeze(['git', 'jj-colocated'] as const),`

    Run `cd sdk && pnpm tsc --noEmit` — expect non-zero exit flagging missing `cancel` in both backend `parallel` namespace freeze blocks. EXPECTED FAILURE — Task 3 wires both backends.

    Commit as `feat(15-04): declare CancelResult + VcsWorkspaceParallel.cancel contract` (staged-red allowed) OR defer commit and combine with Task 3 into one green commit.
  </action>
  <verify>
    <automated>grep -c "export interface CancelResult" sdk/src/vcs/types.ts &amp;&amp; grep -c "cancel(handle: ParallelDispatchHandle): CancelResult" sdk/src/vcs/types.ts &amp;&amp; grep -c "'workspace.parallel.cancel'" sdk/src/vcs/backends.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export interface CancelResult" sdk/src/vcs/types.ts` returns 1.
    - CancelResult interface body contains all 4 fields: `abandoned: readonly string[]`, `failedReaped: readonly string[]`, `surplusBookmarks: readonly string[]`, `surplusWorkspaces: readonly string[]` — verifiable by grep on each field name within ~10 lines of `export interface CancelResult`.
    - JSDoc preceding CancelResult mentions `15.04`, `PARALLEL-07`, `D-01`, `idempotent` / `idempotency`, and `JSON.stringify` (or `frozen pure-JSON`) — verifiable by grep on the file region.
    - `grep -c "cancel(handle: ParallelDispatchHandle): CancelResult" sdk/src/vcs/types.ts` returns 1 (VcsWorkspaceParallel extended).
    - JSDoc on `cancel` mentions `CF-05`, `spawnSync`, AND `synchronous teardown` or `STACK` — verifiable by grep.
    - `grep -c "'workspace.parallel.cancel'" sdk/src/vcs/backends.ts` returns 1 (capability matrix row).
    - If staged-red commit chosen: TSC exits non-zero with `cancel` flagged as missing on both backend parallel namespaces. If combined-green: TSC green after Task 3.
  </acceptance_criteria>
  <done>Interface + capability-matrix entry shipped. Backend wire-ins land in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3 (Wave 2b): Wire performJjParallelCancel + performGitParallelCancel exports + both backend wire-ins</name>
  <files>sdk/src/vcs/jj/parallel.ts, sdk/src/vcs/git/parallel.ts, sdk/src/vcs/backends/jj.ts, sdk/src/vcs/backends/git.ts</files>
  <read_first>
    - sdk/src/vcs/jj/parallel.ts (lines 287-508 — performJjParallelFanIn sibling; lines 500-507 — frozen pure-JSON return pattern to mirror)
    - sdk/src/vcs/git/parallel.ts (full file — adapter-internal sidecar shape; validators at lines 113-119)
    - sdk/src/vcs/backends/jj.ts (lines 1245-1264 — parallel namespace wire-in; cancel adds as third entry)
    - sdk/src/vcs/backends/git.ts (lines 730-746 — parallel namespace wire-in; cancel adds as third entry)
    - sdk/src/vcs/jj/workspace-cleanup.ts (Wave 1 helper shipped in Task 1 — cancel verb imports cleanupSubagentWorkspaces from here)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (Example 4 cancel body shape at lines 832-927 — verbatim implementation guide)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (lines 207-302 — both sidecars: jj/parallel.ts performJjParallelCancel + git/parallel.ts performGitParallelCancel)
  </read_first>
  <action>
    Add to `sdk/src/vcs/jj/parallel.ts` (recommend placement after `performJjParallelFanIn`, end of file).

    Imports: add `import { cleanupSubagentWorkspaces } from './workspace-cleanup.js';` to the existing import block. The `CancelResult` type imports from `../types.js` (add to existing types import line).

    New exported function per RESEARCH Example 4 lines 832-862:

    `export function performJjParallelCancel(`
    `  mainRepoRoot: string,`
    `  handle: ParallelDispatchHandle,`
    `): CancelResult {`
    `  const surplusWorkspaces: string[] = handle.workspaces`
    `    .filter((ws) => existsSync(ws.path))`
    `    .map((ws) => ws.path);`
    `  const { abandoned, failedReaped } = cleanupSubagentWorkspaces(`
    `    mainRepoRoot,`
    `    handle.phaseNumber,`
    `    handle.workspaces,`
    `  );`
    `  const surplusBookmarks: string[] = [];  // jj-side empty by construction (Phase 11 D-02)`
    `  return Object.freeze({`
    `    abandoned: Object.freeze(abandoned.slice()) as readonly string[],`
    `    failedReaped: Object.freeze(failedReaped.slice()) as readonly string[],`
    `    surplusBookmarks: Object.freeze(surplusBookmarks) as readonly string[],`
    `    surplusWorkspaces: Object.freeze(surplusWorkspaces.slice()) as readonly string[],`
    `  }) satisfies CancelResult;`
    `}`

    JSDoc on `performJjParallelCancel` cites: Wave 2 cancel body; D-04 file-boundary helper extraction; D-06 idempotency; Pattern S3 frozen pure-JSON; CF-05 synchronous teardown; comment on `surplusBookmarks = []` cites Phase 11 D-02 retirement of per-subagent agent-bookmarks.

    Add to `sdk/src/vcs/git/parallel.ts` (recommend placement after `performGitParallelFanIn`, end of file). Git-side does NOT import the helper — inline teardown per RESEARCH Example 4 lines 865-903:

    `export function performGitParallelCancel(`
    `  mainRepoRoot: string,`
    `  handle: ParallelDispatchHandle,`
    `): CancelResult {`
    `  const surplusWorkspaces: string[] = handle.workspaces`
    `    .filter((ws) => existsSync(ws.path))`
    `    .map((ws) => ws.path);`
    `  const abandoned: string[] = [];`
    `  const failedReaped: string[] = [];`
    `  const surplusBookmarks: string[] = [];`
    `  for (const ws of handle.workspaces) {`
    `    const wtRes = vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', '--force', ws.path]);`
    `    if (wtRes.exitCode !== 0) {`
    `      failedReaped.push(ws.name);`
    `      continue;`
    `    }`
    `    const branchName = 'worktree-agent-' + ws.agentId;`
    `    const brRes = vcsExec(mainRepoRoot, 'git', ['branch', '-D', '--', branchName]);`
    `    if (brRes.exitCode !== 0) {`
    `      surplusBookmarks.push(branchName);`
    `    }`
    `    abandoned.push(ws.agentId);`
    `  }`
    `  return Object.freeze({`
    `    abandoned: Object.freeze(abandoned.slice()) as readonly string[],`
    `    failedReaped: Object.freeze(failedReaped.slice()) as readonly string[],`
    `    surplusBookmarks: Object.freeze(surplusBookmarks.slice()) as readonly string[],`
    `    surplusWorkspaces: Object.freeze(surplusWorkspaces.slice()) as readonly string[],`
    `  }) satisfies CancelResult;`
    `}`

    JSDoc on `performGitParallelCancel` cites: inline teardown (no shared helper — git's `worktree remove --force` handles tree cleanup; orphan-dirs are a jj-only problem per PROJECT.md OOS for cross-backend cleanup helper); failure semantics (worktree remove non-zero → failedReaped; branch -D non-zero → surplusBookmarks, NOT failedReaped, since the branch may legitimately already be gone).

    Wire both backends. Extend `sdk/src/vcs/backends/jj.ts:1257-1264` `parallel: Object.freeze({...})` block to add `cancel` as the third entry:

    `cancel: (handle: ParallelDispatchHandle): CancelResult =>`
    `  performJjParallelCancel(cwd, handle),`

    Add `performJjParallelCancel` to the existing import from `'../jj/parallel.js'` in `sdk/src/vcs/backends/jj.ts`.

    Mirror on git side. Extend `sdk/src/vcs/backends/git.ts:738-745` `parallel: Object.freeze({...})` block with:

    `cancel: (handle: ParallelDispatchHandle): CancelResult =>`
    `  performGitParallelCancel(cwd, handle),`

    Add `performGitParallelCancel` to the existing import from `'../git/parallel.js'`. Add `CancelResult` to types import on both backends.

    Run `cd sdk && pnpm tsc --noEmit` — must exit 0. Both `VcsWorkspaceParallel.cancel` structural-typing closures are satisfied.

    Commit Wave 2b as `feat(15-04): wire performJjParallelCancel + performGitParallelCancel + backend cancel entries (PARALLEL-07)`.
  </action>
  <verify>
    <automated>cd sdk &amp;&amp; pnpm tsc --noEmit &amp;&amp; cd .. &amp;&amp; grep -c "export function performJjParallelCancel" sdk/src/vcs/jj/parallel.ts &amp;&amp; grep -c "export function performGitParallelCancel" sdk/src/vcs/git/parallel.ts &amp;&amp; grep -c "performJjParallelCancel(cwd, handle)" sdk/src/vcs/backends/jj.ts &amp;&amp; grep -c "performGitParallelCancel(cwd, handle)" sdk/src/vcs/backends/git.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function performJjParallelCancel" sdk/src/vcs/jj/parallel.ts` returns 1.
    - `grep -c "import { cleanupSubagentWorkspaces }" sdk/src/vcs/jj/parallel.ts` returns 1 (Wave 1 helper consumed by Wave 2).
    - `grep -c "handle.phaseNumber" sdk/src/vcs/jj/parallel.ts` returns >= 1 within the performJjParallelCancel body (phaseNumber threaded to helper per N1 signature).
    - `grep -c "handle.workspaces" sdk/src/vcs/jj/parallel.ts` returns >= 2 in performJjParallelCancel (surplusWorkspaces filter + helper authoritative-list pass).
    - `grep -c "satisfies CancelResult" sdk/src/vcs/jj/parallel.ts` returns >= 1 (frozen pure-JSON return per Pattern S3).
    - `grep -c "export function performGitParallelCancel" sdk/src/vcs/git/parallel.ts` returns 1.
    - `grep -c "worktree.*remove.*--force" sdk/src/vcs/git/parallel.ts` returns >= 1 within performGitParallelCancel.
    - `grep -c "worktree-agent-" sdk/src/vcs/git/parallel.ts` returns >= 1 within performGitParallelCancel (branch naming convention).
    - `grep -c "satisfies CancelResult" sdk/src/vcs/git/parallel.ts` returns >= 1.
    - `grep -c "performJjParallelCancel(cwd, handle)" sdk/src/vcs/backends/jj.ts` returns 1 (backend wire-in).
    - `grep -c "performGitParallelCancel(cwd, handle)" sdk/src/vcs/backends/git.ts` returns 1.
    - `grep -E 'AbortController|AbortSignal|child\\.kill|spawn\\(' sdk/src/vcs/jj/parallel.ts sdk/src/vcs/git/parallel.ts` returns no NEW matches within the performXParallelCancel function bodies (CF-05 STACK enforcement per T-15.04-01).
    - `cd sdk && pnpm tsc --noEmit` exits 0 (structural typing closures satisfied on both backends).
    - Working copy clean after Wave 2b commit.
  </acceptance_criteria>
  <done>Both backend cancel bodies + wire-ins landed. TSC green. The cancel verb is callable from the cross-backend adapter surface on both backends; CLI bridge in Task 4 unlocks it from the dispatcher.</done>
</task>

<task type="auto">
  <name>Task 4 (Wave 2c): Ship CLI bridge + three-site registration + per-backend cancel scenarios + repo-side CLI smoke + backends regression</name>
  <files>sdk/src/query/workspace-parallel-cancel.ts, sdk/src/query/command-static-catalog-domain.ts, sdk/src/query/command-manifest.non-family.ts, sdk/src/query/command-aliases.generated.ts, sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts, sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts, sdk/src/vcs/__tests__/backends.test.ts, tests/cli-workspace-parallel-cancel.test.cjs</files>
  <read_first>
    - sdk/src/query/workspace-parallel-fan-in.ts (full file — CLI bridge template; copy header conventions, resolveFileOrStdin helper, QueryHandler shape, envelope `{data: result}`)
    - sdk/src/query/command-static-catalog-domain.ts (lines 21-22 imports + lines 71-74 catalog entries — exact insertion shape)
    - sdk/src/query/command-manifest.non-family.ts (lines 61-62 — insertion alignment)
    - sdk/src/query/command-aliases.generated.ts (lines 156-157 — insertion shape)
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (lines 64-87 setupJjRepo helper; lines 474-545 SC5 scenario template — Pattern A/B/W2)
    - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts (git-side counterpart of SC5 — find setupGitRepo equivalent)
    - sdk/src/vcs/__tests__/backends.test.ts (post-15.03 with 3 describe blocks — add fourth)
    - .planning/phases/15-adapter-surface-extensions-rename/15-RESEARCH.md (Example 6 CLI bridge lines 1045-1121; Example 7 three-site registration lines 1123-1137; Example 9 cancel scenarios lines 1216-1292)
    - .planning/phases/15-adapter-surface-extensions-rename/15-PATTERNS.md (lines 376-512 — CLI bridge + three-site registration verbatim shapes)
  </read_first>
  <action>
    Create `sdk/src/query/workspace-parallel-cancel.ts` mirroring `workspace-parallel-fan-in.ts` verbatim per PATTERNS lines 376-457:

    File header doc block cites Phase 15.04 (PARALLEL-07); CLI bridge purpose; flag list (`--cwd <path>` defaults to projectDir; `--handle @-` reads stdin OR `@<path>` reads file; inline JSON form rejected); envelope `{data: cancelResult}` flat shape per Phase 11 plan 02 convention.

    Imports: `import { readFileSync } from 'node:fs';` + `import { createVcsAdapter } from '../vcs/index.js';` + `import type { ParallelDispatchHandle } from '../vcs/types.js';` + `import type { QueryHandler } from './utils.js';`

    Inline `function resolveFileOrStdin(raw: string): string` verbatim from PATTERNS lines 406-417 (the fan-in precedent): `@-` reads stdin via `readFileSync(0, 'utf-8')`; `@<path>` reads file via `readFileSync(raw.slice(1), 'utf-8')`; inline form throws `Error('expected @<path> or @- but got inline string (inline JSON form is not accepted)')`.

    Export `workspaceParallelCancelQuery: QueryHandler` per PATTERNS lines 422-456:
    - Initialize `cwd = projectDir; handleRaw: string | undefined;`.
    - Argv parser loop: `--cwd <path>` sets cwd; `--handle <input>` sets handleRaw.
    - If `handleRaw === undefined`: return `{data: {ok: false, reason: 'handle_required'}}`.
    - Try/catch resolveFileOrStdin + JSON.parse → handle. On catch: return `{data: {ok: false, reason: 'handle_json_parse_failed', error: err message}}`.
    - `const vcs = createVcsAdapter(cwd);`
    - `const cancelResult = vcs.workspace.parallel.cancel(handle);`
    - Return `{data: cancelResult}` (flat envelope).

    Three-site registration per CF-07 + PATTERNS Pattern S2. Touch ALL THREE files in this same commit (atomic — missing any one breaks runtime verb resolution per Pitfall 6):

    1. `sdk/src/query/command-static-catalog-domain.ts`:
       - Add to imports (after line 22): `import { workspaceParallelCancelQuery } from './workspace-parallel-cancel.js';`
       - Add to catalog (after line 74): `['workspace.parallel.cancel', workspaceParallelCancelQuery],` and `['workspace parallel.cancel', workspaceParallelCancelQuery],` (BOTH dot + space alias forms — see existing dispatch/fan-in entries).

    2. `sdk/src/query/command-manifest.non-family.ts` (after line 62, matching aligned column-format of existing rows):
       `{ canonical: 'workspace.parallel.cancel',       aliases: ['workspace parallel.cancel'],       mutation: true,  outputMode: 'json' },`

    3. `sdk/src/query/command-aliases.generated.ts` (after line 157):
       `{ canonical: 'workspace.parallel.cancel', aliases: ['workspace parallel.cancel'], mutation: true },`

    Create `sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts`. Mirror PATTERNS lines 623-678 (SC5 template):

    - Imports: vitest + node:fs + node:os + node:path + node:child_process + `createJjAdapter` from `'../backends/jj.js'` + jj-availability probe.
    - `setupJjRepo()` helper at top of file: mkdtempSync + `jj git init --colocate` + config user.email/name + seed commit + materialize `.planning/phases/15-test/` directory.
    - Top-level `describe.sequential.skipIf(!jjAvailable)('workspace.parallel.cancel — jj backend', () => { ... })` with per-describe `beforeAll` (setupJjRepo) + `afterAll` (rmSync).
    - 4 `it(...)` blocks per VALIDATION lines 58-62 + RESEARCH Example 9:
      1. `it('cancel-clean-abandon: dispatch N=2, cancel tears down both workspaces', { timeout: 30000 }, () => { ... })` — dispatch with `plan: [{agentId: 'a1', planId: 'p1'}, {agentId: 'a2', planId: 'p2'}], phaseNumber: 15`; assert both `handle.workspaces[i].path` exist via existsSync; run cancel; assert `result.abandoned.length === 2`, `result.surplusWorkspaces.length === 2`, `result.failedReaped.length === 0`, `result.surplusBookmarks.length === 0`; both paths gone post-cancel.
      2. `it('cancel-idempotent-recall: second cancel returns all-empty arrays per D-03', { timeout: 30000 }, () => { ... })` — dispatch N=1; first cancel returns abandoned.length===1; second cancel on same handle returns `{abandoned: [], failedReaped: [], surplusBookmarks: [], surplusWorkspaces: []}` (all 4 fields empty).
      3. `it('cancel-partial-state-recovery: pre-deleted workspace dir survives cancel', { timeout: 30000 }, () => { ... })` — dispatch N=2; pre-delete `handle.workspaces[0].path` via `rmSync(...,{recursive,force})` BEFORE calling cancel; run cancel; assert `result.failedReaped.length === 0` (force:true makes ENOENT a no-op) AND `result.surplusWorkspaces.length === 1` (counted pre-cancel — only the still-existing one shows up).
      4. `it('cancel returns frozen pure-JSON CancelResult — survives JSON round-trip (Phase 9 D-05)', { timeout: 30000 }, () => { ... })` — dispatch N=1; run cancel; assert `Object.isFrozen(result) === true`, `Object.isFrozen(result.abandoned) === true`; `expect(JSON.parse(JSON.stringify(result))).toEqual(result)`.

    NONE of the it blocks pass `mainBookmarks` to dispatch — cancel is bookmark-orthogonal (Pitfall 7 mitigation per N2 decision; no stale `mainBookmark: 'main'` literal anywhere).

    Create `sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts` as the git-side counterpart. Same 4 it blocks; substitute `createGitAdapter` for `createJjAdapter`; substitute `setupGitRepo()` (git init + initial commit + materialize `.gsd-workspaces/` directory if needed); skipIf on `!gitAvailable`. The git-side `cancel-clean-abandon` scenario MAY see `result.surplusBookmarks.length > 0` because `worktree-agent-${agentId}` branches are created by dispatch — adjust the assertion to `result.abandoned.length === 2` AND `result.failedReaped.length === 0` AND check that surplusBookmarks contains the expected `worktree-agent-{a1,a2}` names (or is empty if dispatch already cleaned them; the test should accept either outcome per git-side asymmetry documented in PATTERNS Pattern S3).

    Extend `sdk/src/vcs/__tests__/backends.test.ts` with the 4th describe block (sibling of 15.01/15.02/15.03 blocks):

    `describe('BACKENDS_AVAILABLE_FOR_VERB capability matrix — Phase 15.04 workspace.parallel.cancel (PARALLEL-07)', () => {`
    `  it('exposes workspace.parallel.cancel for both backends', () => {`
    `    expect(BACKENDS_AVAILABLE_FOR_VERB['workspace.parallel.cancel']).toEqual(['git', 'jj-colocated']);`
    `  });`
    `});`

    Create `tests/cli-workspace-parallel-cancel.test.cjs` (node:test domain per N4 decision):

    - `const { test } = require('node:test'); const assert = require('node:assert/strict'); const { execSync, spawnSync } = require('node:child_process');`
    - Path to the dispatcher: `node bin/gsd-sdk.js query workspace.parallel.cancel ...` (per `package.json` `bin` field: `"gsd-sdk": "bin/gsd-sdk.js"`).
    - 4 tests (NOTE: do NOT add a `--help` smoke test — the gsd-sdk CLI bridge does not implement `--help` flag handling, and adding it is out of scope for Plan 15.04. The combination of the no-handle test (asserts the canonical verb resolves to the registered bridge), the space-alias test (asserts the alias form resolves identically), and the unknown-verb negative test below is sufficient to harden three-site verb-resolution coverage):
      1. `test('CLI smoke: no --handle returns {ok: false, reason: handle_required}', () => { ... })` — `spawnSync(...)` without `--handle`; parse stdout as JSON; assert `parsed.data.ok === false && parsed.data.reason === 'handle_required'`. This implicitly verifies the canonical dot-form verb resolves to the registered bridge (anything else would surface as an "unknown verb"-style error rather than the structured `handle_required` envelope).
      2. `test('CLI smoke: invalid JSON on stdin returns {ok: false, reason: handle_json_parse_failed}', () => { ... })` — spawn with `--handle @-` and pass `not-json{` on stdin via `input` option; parse stdout; assert `parsed.data.ok === false && parsed.data.reason === 'handle_json_parse_failed'`.
      3. `test('CLI smoke: space-alias form resolves identically', () => { ... })` — same as test 1 but using `workspace parallel.cancel` (space form) without `--handle`; assert resolution succeeds and yields the same `{ok: false, reason: 'handle_required'}` envelope (NOT an "unknown verb"-style error). Verifies the alias-table site registered the space form.
      4. `test('CLI smoke: bogus verb returns unknown-verb error (negative control)', () => { ... })` — `spawnSync(...)` with a deliberately bogus verb such as `workspace.parallel.cancellation-xyz`; assert the exit code is non-zero AND stderr/stdout contains an "unknown verb"-style marker (read the dispatcher source for the exact wording — likely the string `unknown` near `verb` in the error envelope). Pairs with tests 1 and 3 to prove the verb-resolution layer is the gate the bridge sits behind.

    Run the full Wave 2c gate: `cd sdk && pnpm tsc --noEmit && pnpm vitest run src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts src/vcs/__tests__/cmd-parallel-cancel-git.test.ts src/vcs/__tests__/backends.test.ts --reporter=dot && cd .. && node --test tests/cli-workspace-parallel-cancel.test.cjs`. All must exit 0.

    Commit as `feat(15-04): CLI bridge workspace-parallel-cancel + three-site registration + per-backend scenarios + smoke (PARALLEL-07 closure)`.
  </action>
  <verify>
    <automated>cd sdk &amp;&amp; pnpm tsc --noEmit &amp;&amp; pnpm vitest run src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts src/vcs/__tests__/cmd-parallel-cancel-git.test.ts src/vcs/__tests__/backends.test.ts --reporter=dot &amp;&amp; cd .. &amp;&amp; node --test tests/cli-workspace-parallel-cancel.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/query/workspace-parallel-cancel.ts` exists; `grep -c "workspaceParallelCancelQuery" sdk/src/query/workspace-parallel-cancel.ts` returns >= 1 (export); `grep -c "vcs.workspace.parallel.cancel" sdk/src/query/workspace-parallel-cancel.ts` returns >= 1.
    - Bridge envelope on parse-fail: `grep -c "handle_json_parse_failed" sdk/src/query/workspace-parallel-cancel.ts` returns >= 1.
    - Bridge envelope on missing-handle: `grep -c "handle_required" sdk/src/query/workspace-parallel-cancel.ts` returns >= 1.
    - Three-site registration: each of the three files contains `workspace.parallel.cancel` references — verifiable by `for f in sdk/src/query/command-static-catalog-domain.ts sdk/src/query/command-manifest.non-family.ts sdk/src/query/command-aliases.generated.ts; do grep -c 'workspace.parallel.cancel\\|workspaceParallelCancelQuery' $f; done` each returns >= 1.
    - command-static-catalog-domain.ts contains BOTH dot-form and space-form aliases for cancel — `grep -c "'workspace.parallel.cancel'" sdk/src/query/command-static-catalog-domain.ts` AND `grep -c "'workspace parallel.cancel'" sdk/src/query/command-static-catalog-domain.ts` each return >= 1.
    - `sdk/src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts` exists with the 4 `it(...)` blocks (clean-abandon, idempotent-recall, partial-state-recovery, frozen-JSON) — verifiable by greps on each scenario keyword.
    - `sdk/src/vcs/__tests__/cmd-parallel-cancel-git.test.ts` exists with the same 4 scenarios mirrored for git.
    - Neither cancel-jj nor cancel-git test file contains `mainBookmark:` (Pitfall 7 / N2 enforcement): `grep -c 'mainBookmark:' sdk/src/vcs/__tests__/cmd-parallel-cancel-{jj,git}.test.ts` returns 0 each.
    - `sdk/src/vcs/__tests__/backends.test.ts` contains the new describe block referencing `Phase 15.04` AND `workspace.parallel.cancel` AND `PARALLEL-07`.
    - `tests/cli-workspace-parallel-cancel.test.cjs` exists with `node:test` shape and at least 4 test blocks covering missing-handle (canonical), parse-fail, space-alias resolution, and bogus-verb negative control (unknown-verb error). MUST NOT exercise a `--help` flag (CLI bridge does not implement it; out of scope for this plan).
    - `cd sdk && pnpm tsc --noEmit` exits 0.
    - `cd sdk && pnpm vitest run src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts src/vcs/__tests__/cmd-parallel-cancel-git.test.ts src/vcs/__tests__/backends.test.ts --reporter=dot` exits 0 with NO skipped tests under cancel filter (both backends should run).
    - `node --test tests/cli-workspace-parallel-cancel.test.cjs` exits 0.
    - Per-backend cancel scenarios complete within ≤2s for handles with ≤8 workspaces per ROADMAP Phase 15 SC4 (verifiable by vitest test duration in the dot reporter — each `it` should print sub-2000ms).
    - Working copy clean after the Wave 2c commit (`assert_clean_wc`).
  </acceptance_criteria>
  <done>CLI bridge + three-site registration + per-backend scenarios + repo-side CLI smoke + capability-matrix regression all landed. PARALLEL-07 contract is observable from the adapter API (`vcs.workspace.parallel.cancel(handle)`), the CLI dispatcher (`gsd-sdk query workspace.parallel.cancel`), and the test surface (per-backend scenarios + smoke). Phase 15 complete.</done>
</task>

</tasks>

<verification>
- `cd sdk && pnpm tsc --noEmit` is green after Task 3 (structural-typing closures for CancelResult / VcsWorkspaceParallel.cancel satisfied on both backends).
- `cd sdk && pnpm vitest run src/vcs/__tests__/jj-workspace-cleanup.test.ts src/vcs/__tests__/cmd-parallel-cancel-jj.test.ts src/vcs/__tests__/cmd-parallel-cancel-git.test.ts src/vcs/__tests__/backends.test.ts --reporter=dot` is green after Task 4.
- `node --test tests/cli-workspace-parallel-cancel.test.cjs` is green after Task 4.
- All 4 capability-matrix regression assertions in `backends.test.ts` pass (15.01 rootRevisions, 15.02 idAlphabet, 15.03 matchPrefix, 15.04 workspace.parallel.cancel).
- `gsd-sdk query workspace.parallel.cancel` (canonical dot form, no handle) resolves to the registered bridge and returns the structured `{ok: false, reason: 'handle_required'}` envelope (NOT an "unknown verb"-style error) — verified by the CLI smoke test 1; the space-alias form (`gsd-sdk query 'workspace parallel.cancel'`) returns the same envelope (smoke test 3); a deliberately bogus verb returns the unknown-verb error (smoke test 4 negative control).
- Cancel verb body does NOT introduce `AbortController` / `AbortSignal` / `child.kill` / `spawn` non-Sync (T-15.04-01 STACK enforcement) — verifiable by `grep -E 'AbortController|AbortSignal|child\\.kill' sdk/src/vcs/{jj,git}/parallel.ts` showing no new matches in the perform*ParallelCancel function bodies.
- Helper sidecar has zero `from '../backends/jj'` imports (UPSTREAM-02 enforcement / T-15.04-04) — verifiable by grep.
- Per-backend scenarios run within ≤2s timing budget for ≤8 workspaces (ROADMAP Phase 15 SC4).
- Working copy is clean after the final commit (`assert_clean_wc` per `feedback_workflow_assert_clean_wc`).
</verification>

<success_criteria>
- ROADMAP Phase 15 SC4 satisfied: `vcs.workspace.parallel.cancel(handle)` returns within ≤2s for handles with ≤8 workspaces; tears down materialized workspaces via `workspace.forget` + `rm -rf` (jj) or `worktree remove --force` (git) + `bookmarks.delete({force:true})`; does NOT signal subagent processes; returns structured `CancelResult` envelope `{abandoned, failedReaped, surplusBookmarks, surplusWorkspaces}` mirroring `FanInResult` field naming; new CLI bridge `sdk/src/query/workspace-parallel-cancel.ts` registered at all three sites (catalog-domain + manifest.non-family + aliases.generated).
- ROADMAP Phase 15 SC5 satisfied: shared `cleanupSubagentWorkspaces` helper exported from `sdk/src/vcs/jj/workspace-cleanup.ts` (sidecar per CONTEXT D-04) is consumed by PARALLEL-07's cancel body; Phase 16 CLEANUP-02 will import the SAME helper for fanIn-success-branch reap (no inline duplication, single owner per IP-5).
- CancelResult is frozen pure-JSON per Phase 9 D-05 invariant.
- Cancel is idempotent per D-03 (second-call returns all-empty arrays).
- N1 helper-signature decision recorded in `<deviations>` (3-arg + mainRepoRoot rename deviating from CONTEXT D-05 literal).
- N4 CLI smoke placement decision recorded in files_modified (CLI smoke at `tests/cli-workspace-parallel-cancel.test.cjs` node:test domain; per-backend scenarios at `sdk/src/vcs/__tests__/cmd-parallel-cancel-{jj,git}.test.ts` vitest domain).
- Phase 15 complete: 4 plans shipped sequentially per CF-01; clean `rootRevisions`/`idAlphabet`/`matchPrefix`/`workspace.parallel.cancel` surface on both backends; helper sidecar single-owner-ready for Phase 16 consumption.
</success_criteria>

<output>
Create `.planning/phases/15-adapter-surface-extensions-rename/15-04-SUMMARY.md` when done. Include:
- Wave 1 commit ID (helper sidecar + unit tests) and Wave 2 commit IDs (interface + bodies + wire-ins; CLI bridge + three-site + scenarios + smoke)
- N1 decision recorded: 3-arg + mainRepoRoot rename; whether any consumer of the helper needed adjustment
- Per-backend cancel scenario pass count (8 tests minimum: 4 per backend) + timing (each <2s per ROADMAP SC4)
- CLI smoke pass count (4 tests in tests/cli-workspace-parallel-cancel.test.cjs)
- Confirmation that grep `-E 'AbortController|AbortSignal|child\\.kill'` returns zero new matches (CF-05 enforcement)
- Confirmation that grep `'from \\'../backends/jj\\''` on workspace-cleanup.ts returns 0 (UPSTREAM-02 enforcement)
- Capability-matrix regression: all 4 entries (rootRevisions + idAlphabet + matchPrefix + workspace.parallel.cancel) tested in backends.test.ts
- Phase 15 closure note: 4 plans sequentially shipped; namespace clean for Phase 16 (CLEANUP-02 helper consumer) + Phase 17 + Phase 18
</output>
</content>
</invoke>