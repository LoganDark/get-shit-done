---
phase: 02.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 05
subsystem: vcs-adapter
tags: [statusentry, D-16, D-21, no-op, test-cleanup]
requires:
  - "2.1-01 (shape commit: removed `index: string` from StatusEntry in sdk/src/vcs/types.ts and stopped surfacing the field from backends/git.ts's status() entry literal)"
  - "2.1-04 (CommitInput.pathspec → files closure; orthogonal — does not interact with status() entry shape)"
provides:
  - "Verified: zero StatusEntry-shape `.index` references in the three test files prescribed by PLAN-05 (git-backend.test.ts, adapter-contract.test.ts, baseline-parity.test.ts) and zero repo-wide outside .planning/ history docs"
  - "Verified: existing status() test assertions in those files already use the post-D-16 `{ path, worktree }` shape — no test code change needed"
  - "Closes the StatusEntry.index dimension of the D-21 broken-lint window opened by plan 01 (the dimension was already mechanically closed by plan 01 itself; this plan is the audit that confirms no residue)"
affects:
  - "Plan 06 (gitDir/gitCommonDir → gitOnly migration — next remaining D-21 dimension per plan-04 handoff)"
tech_stack:
  added: []
  patterns:
    - "Post-shape-commit audit pattern: when a shape-removing plan (plan 01) lands ahead of its consumer-sweep plan (plan 05), the sweep plan may discover the consumers were already nonexistent and degrade to a no-op-with-verification"
key_files:
  created:
    - .planning/phases/02.1-vcs-abstraction-audit-drop-git-only-concepts/02.1-05-SUMMARY.md
  modified: []
decisions:
  - "No source-file commit issued. PLAN-05 task 2 prescribed `up to 3 files in diff (could be fewer if a file had no surfaced changes)`; the discovered count is zero. A docs-only metadata commit captures the audit result without an empty `refactor` commit. This honors D-21's `one dedicated commit per dimension` rule — the dimension's dedicated commit is this SUMMARY's metadata commit."
  - "No baseline-parity status-dispatch mutation. The dispatch at lines 112-114 reads only `s.raw` (the verbatim porcelain string from the baseline), never `s.entries[N].index`. Confirmed by direct read of the dispatch block."
requirements_completed: []
metrics:
  duration: "~5 min audit (2026-05-11 19:18Z window)"
  completed_date: "2026-05-11"
  files_modified: 0
  tasks_completed: 2
---

# Phase 2.1 Plan 05: Drop StatusEntry.index from Test Assertions Summary

**One-liner:** Verified zero StatusEntry-shape `.index` references in the three prescribed test files and zero repo-wide outside `.planning/` — the D-16 dimension was already mechanically closed by plan 01's shape commit; no source mutation required, dimension recorded as closed.

## Performance

- **Duration:** ~5 min (audit-only; no source mutation)
- **Started:** 2026-05-11T19:18Z
- **Completed:** 2026-05-11T19:25Z
- **Tasks:** 2 (audit + dimension-close)
- **Files modified:** 0 source; 1 SUMMARY

## Accomplishments

- Pre-grep confirmed zero StatusEntry-shape `.index` references in any of the three prescribed test files
- Pre-grep confirmed zero repo-wide StatusEntry-shape `.index` references outside `.planning/` docs
- Confirmed all status() consumers in those tests already use the post-D-16 `{ path, worktree }` shape
- D-21 broken-lint dimension for StatusEntry.index recorded as closed
- Plan-06 handoff identified next dimension: gitDir/gitCommonDir → gitOnly migration

## Task Commits

| # | Hash | Message |
|---|------|---------|
| (audit) | n/a | Source files required no changes — PLAN-05 task 1 acceptance criterion: zero StatusEntry-shape `.index` references is the target state; current state already matches |
| 2 (docs) | (this commit) | `docs(2.1-05): record StatusEntry.index dimension closed — no source residue` |

## Pre/Post Grep

| Source | Pre-count | Post-count |
|--------|-----------|------------|
| `grep -nE "\.index\b" <3 test files> \| grep -v "\.indexOf\b"` | **0** | **0** |
| `grep -rnE "\.index[^a-zA-Z_]" --include="*.ts" --include="*.cjs"` (entries/entry/StatusEntry/porcelain filtered, excl .planning/dist-cjs/node_modules/match.index/.indexOf) | **0** | **0** |
| `grep -rnE "entries\[[0-9]+\]\.index\|entry\.index\b"` (repo-wide, excl .planning/dist-cjs/node_modules) | **0** | **0** |

All `.index` matches in baseline-parity.test.ts (lines 187, 198, 315, 341, 342, 385, 448, 502, 581, 596, 597) are `Array.prototype.indexOf(...)` calls on `args` (raw-git command-line argv parsing inside dispatch clauses) — unrelated to StatusEntry.

The `tests/__tests__/git-backend.test.ts` line `261` is the comment "failed `git merge` leaves the index with stage 1/2/3 entries for the …" — narrative reference to git's index data structure, not a `.index` property read on a StatusEntry.

## Verification

### tsc gate

```
cd sdk && pnpm exec tsc --noEmit
```

Exit 0; no errors. (Same as plan 04 post-state.)

### vitest gates (per PLAN-05 task 1 verify)

```
cd sdk && pnpm exec vitest run src/vcs/__tests__/git-backend.test.ts
```

Result: `Tests  5 failed | 46 passed (51)`. **Tsc/Index delta:** 0 — none of the 5 failures involve `StatusEntry.index`. The failing tests:
- `createGitAdapter — hooks > hooks.fire returns ExecResult with exitCode 0 when no hook installed` (hooks dimension, owned by future plan per plan-04 handoff)
- `createGitAdapter — stage / unstage (02-03 Task 1) > stage([file]) makes file appear in diff` (vcs.stage hard-remove dimension, owned by future plan)
- `createGitAdapter — stage / unstage (02-03 Task 1) > unstage([file]) reverts a previously staged add` (vcs.stage hard-remove dimension)
- `createGitAdapter — diff.nameStatus (02-03 Task 2) > diff({staged:true,nameStatus:true}) returns nameStatus entries with status letters` (uses `vcs.stage` in setup)
- `createGitAdapter — workspace.context (02-03 Task 2 — Blocker 4) > on linked worktree, gitDir !== gitCommonDir, isLinked=true, mode=linked` (gitDir/gitCommonDir dimension, owned by plan 06 per handoff)

```
cd sdk && pnpm exec vitest run src/vcs/__tests__/adapter-contract.test.ts
```

Result: `Tests  1 failed | 15 passed (16)`. Single failure: `vcs.hooks.fire(no hook installed) is a no-op` — hooks dimension, NOT StatusEntry.index.

All status() assertions in both files pass; the post-D-16 `{ path, worktree }` shape is already in use:

```
git-backend.test.ts:118  const paths = r.entries.map((e) => e.path);
git-backend.test.ts:128  const paths = r.entries.map((e) => e.path);
adapter-contract.test.ts:40  expect(s.entries.some((e) => e.path === 'untracked.txt')).toBe(true);
```

### tsc delta for StatusEntry.index errors

Pre-plan-05: 0 tsc errors mentioning `StatusEntry.index` (plan 01 removed the field from the type with no surviving test consumers that asserted on the field — confirmed by RESEARCH Pitfall 7's contemporaneous grep).

Post-plan-05: 0 tsc errors mentioning `StatusEntry.index`. **Delta: 0**.

The dimension was closed by plan 01's shape commit; plan-05 is the auditor-of-record that confirms no source residue exists.

### baseline-parity status dispatch

Lines 112-114 of `sdk/src/vcs/__tests__/baseline-parity.test.ts`:

```ts
} else if (args[0] === 'status' && args.includes('--porcelain')) {
  const s = vcs.status({ porcelain: true });
  expect(s.raw).toBe(baseline.expected.stdout);
}
```

The dispatch reads only `s.raw` (the verbatim porcelain string), never `s.entries[N].index`. No mutation required.

## Self-Check: PASSED

- Branch: `phase/2.1-audit` ✓
- Plans 01–04 plus 7f4c8ffa fix-up commit present in `git log --oneline -10` ✓
- Zero StatusEntry-shape `.index` references repo-wide outside `.planning/` ✓
- All three prescribed test files: zero `.index` references after filtering `.indexOf` ✓
- All status() assertions in those files use `{ path, worktree }` only ✓
- baseline-parity status dispatch reads only `s.raw` — no entry-shape access ✓
- No `.planning/STATE.md` or `.planning/ROADMAP.md` touch in this plan per executor objective ✓
- tsc clean (exit 0) ✓
- Remaining test failures (5 + 1 = 6 across git-backend.test.ts + adapter-contract.test.ts) are in dimensions owned by future plans per plan-04 handoff ✓

## Decisions Made

1. **No source-file commit.** PLAN-05 task 2 acceptance criterion permits 0–3 files in diff. With zero source residue discovered, an empty `refactor(2.1-05): drop StatusEntry.index from test assertions` commit would be misleading (no drop occurred — there was nothing to drop). The dimension's dedicated D-21 commit becomes this SUMMARY's metadata commit instead.

2. **No defensive comment additions.** PLAN-05 task 1 prescribed inline `// 2.1 D-16: StatusEntry.index removed; staging is not cross-backend (see D-03)` comments "at each non-trivial change site." Zero change sites means zero comments — adding speculative comments at unrelated lines would create noise.

3. **Recording dimension-already-closed as a valid plan outcome.** Per D-21's "one dedicated commit per dimension" rule, the dimension's commit is whichever commit surfaces the closure. When the closure happens in an upstream plan (plan 01) and the consumer-sweep plan (plan 05) finds zero residue, the SUMMARY commit IS the dimension closure record. This is documented here so future audit can find it.

## Deviations from Plan

**None — but the plan executed to a no-op outcome rather than the expected 3-file edit.**

PLAN-05's `<context>` block explicitly anticipated this: "If grep returns NO StatusEntry-related `.index` matches outside the above three files, the cleanup scope is exactly these three files." It then prescribed task 1 work on those three files. Here, the grep further returns NO StatusEntry-related `.index` matches **inside** those three files either. The plan's structure permits this (task 2's acceptance: "Up to 3 files in diff (could be fewer if a file had no surfaced changes)").

This is not a deviation under any of Rules 1–4: there is no bug to fix, no missing critical functionality, no blocker, no architectural decision. It is a confirmation that plan 01's shape commit was complete with respect to its test-consumer fan-out.

## Issues Encountered

None.

## Threat Surface Audit

Per PLAN-05's `<threat_model>`:

- **T-2.1.05-01 (rename/copy detection regression)** — Mitigated by plan 01, not by this plan. The local `index` var at `sdk/src/vcs/backends/git.ts:233` was not touched by plan 05. Direct read of git.ts:261-270 confirms the rename/copy heuristic still uses the local `index` variable internally while not surfacing it on the `StatusEntry` literal. PATTERNS.md Category 6 baseline holds.
- **T-2.1.05-SC** — N/A (no npm installs).

No new threat flags surfaced during execution.

## Plan-06 Handoff

The next remaining D-21 broken-lint dimension on `phase/2.1-audit`, per plan-04 SUMMARY's handoff section:

**gitDir / gitCommonDir → gitOnly migration** (D-18). Failures surfacing this dimension:

- Baseline-parity: `worktree-safety-cjs-122-rev-parse-git-dir`
- Baseline-parity: `worktree-safety-cjs-123-rev-parse-common-dir`
- `sdk/src/vcs/__tests__/git-backend.test.ts > createGitAdapter — workspace.context (02-03 Task 2 — Blocker 4) > on linked worktree, gitDir !== gitCommonDir, isLinked=true, mode=linked`

The D-18 type-side migration is forward-complete (Blocker 4 closed for cross-backend `WorkspaceContext`). The remaining work is the consumer-narrowing sweep: callers reading `ctx.gitDir` / `ctx.gitCommonDir` from the cross-backend shape need to route through `vcs.gitOnly.gitDir()` / `vcs.gitOnly.gitCommonDir()` per D-18.

Plan-06 entry pre-grep:

```
grep -rnE "\.gitDir\b|\.gitCommonDir\b" \
  --include="*.ts" --include="*.cjs" sdk/src bin/lib tests get-shit-done \
  | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/"
```

Other remaining dimensions per plan-04 handoff (not for plan-06):

- **vcs.stage / vcs.unstage hard-remove** (8 sites in test-tree only) — owned by dedicated stage-hard-remove plan.
- **vcs.hooks** — 2 failing test sites; D-07 surface-scan needed.
- **#3061 design tension** — partially resolved by 7f4c8ffa fix-up commit; phase-level discussion may still be needed.
- **Baseline-parity JSON re-bless** — plan 09 owns.

## Known Stubs

None.
