---
phase: 02.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 02
subsystem: vcs-adapter
tags: [mechanical-rename, expr-rev, consumer-sweep, broken-lint-window-narrowing]
requires:
  - "2.1-01 (shape commit landed expr.rev factory in sdk/src/vcs/expr.ts; broken-lint window opened on phase/2.1-audit)"
provides:
  - "Zero references to expr.commit() anywhere in the repo outside .planning/ docs and dist-cjs/"
  - "Every consumer of the runtime-id factory now calls expr.rev(id) — closes the expr.commit dimension of the D-21 broken-lint window"
  - "expr.test.ts pins jj change_id alphabet acceptance (kxnvqropmlkz) — first behavioral test for D-13's widened SHA_OR_CHANGE_ID_RE"
affects:
  - "sdk/src/query/{progress,verify}.ts (Blocker-3 closure paths now call expr.rev)"
  - "sdk/src/vcs/__tests__/{adapter-contract,baseline-parity,expr}.test.ts (test dispatch + assertions point at expr.rev)"
  - "get-shit-done/bin/lib/{commands,verify,graphify}.cjs (runtime bin/lib production consumers)"
  - "tests/__tools__/capture-vcs-baselines.cjs (baseline-generator helper comments)"
tech_stack:
  added: []
  patterns:
    - "Mechanical sed-shaped consumer sweep (D-21) — single dedicated commit, no opportunistic refactors, no logic changes"
    - "Paired test + source bundling in one commit (D-06 / D-21) — expr.test.ts updates land with the source-side rename in the same commit"
key_files:
  created: []
  modified:
    - get-shit-done/bin/lib/verify.cjs
    - get-shit-done/bin/lib/graphify.cjs
    - get-shit-done/bin/lib/commands.cjs
    - sdk/src/query/verify.ts
    - sdk/src/query/progress.ts
    - sdk/src/vcs/__tests__/expr.test.ts
    - sdk/src/vcs/__tests__/baseline-parity.test.ts
    - sdk/src/vcs/__tests__/adapter-contract.test.ts
    - tests/__tools__/capture-vcs-baselines.cjs
decisions:
  - "Pre-grep at plan-02 entry returned 45 occurrences, not 46 (plan PLAN-02 stated) nor 62 (plan-01 handoff stated). The 62 figure used regex `expr\\.commit\\b` which also captured prose references (e.g. `expr.commit() per Blocker 3` in comments); the 46 figure was an off-by-one on verify.cjs (handoff said 9 sites, grep showed 10). Reconciled to ground-truth 45 call sites; commit message and SUMMARY use 45 throughout."
  - "expr.test.ts adds one new test ('accepts jj change_id alphabet shape') asserting `expr.rev('kxnvqropmlkz')` does not throw — the sole non-mechanical addition allowed by PLAN-02 task 1; pins D-13's widened SHA_OR_CHANGE_ID_RE behavior for future regression detection."
  - "Error-message assertion in expr.test.ts updated from /SHA/ to /hex-SHA or change-id shaped string/ matching the exact text emitted at expr.ts:106. Both forms would pass the current /SHA/ regex; the new regex is tighter and harder to drift past."
metrics:
  duration: "~7 min (2026-05-12 17:34:14Z → 17:42:40Z)"
  completed_date: "2026-05-12"
  files_modified: 9
  tasks_completed: 2
---

# Phase 2.1 Plan 02: expr.commit -> expr.rev Mechanical Consumer Sweep Summary

**One-liner:** Mechanically renamed 45 `expr.commit(...)` call sites to `expr.rev(...)` across 9 consumer files in one dedicated D-21 commit on `phase/2.1-audit`, updated `sdk/src/vcs/__tests__/expr.test.ts` describe-block + error-message assertions to match the new factory shape, and added one new test pinning D-13's jj change_id alphabet acceptance — closing the expr.commit dimension of the broken-lint window opened by plan 01.

## What Shipped

A single commit (`kqloppxlywlytyrlnsulqwmlsozonmoo`) on `phase/2.1-audit` mutating exactly 9 files:

| File | Mutation |
|------|----------|
| `get-shit-done/bin/lib/verify.cjs` | 10 `expr.commit(` → `expr.rev(` renames |
| `get-shit-done/bin/lib/graphify.cjs` | 5 `expr.commit(` → `expr.rev(` renames (incl. `readGitHead` + `countCommitsBetween`) |
| `get-shit-done/bin/lib/commands.cjs` | 3 `expr.commit(` → `expr.rev(` renames (cmdCommit + cmdStats first-commit-date probe) |
| `sdk/src/query/verify.ts` | 5 `expr.commit(` → `expr.rev(` renames (verifyCommits, verifySummary) |
| `sdk/src/query/progress.ts` | 2 `expr.commit(` → `expr.rev(` renames (statsJson first-commit-date probe + its comment) |
| `sdk/src/vcs/__tests__/expr.test.ts` | 7 `expr.commit(` → `expr.rev(` renames + describe-block heading rename + 3 error-message regex tightenings + 1 new test (`accepts jj change_id alphabet shape`) |
| `sdk/src/vcs/__tests__/baseline-parity.test.ts` | 9 `expr.commit(` → `expr.rev(` renames (args-shape dispatch clauses for SHA-shaped invocations) |
| `sdk/src/vcs/__tests__/adapter-contract.test.ts` | 1 `expr.commit(` → `expr.rev(` rename (zero-SHA negative-existence probe) |
| `tests/__tools__/capture-vcs-baselines.cjs` | 4 `expr.commit(` → `expr.rev(` renames (explanatory comments in 4 baseline records) |

Total: **45 textual occurrences renamed** across **9 files**. Plus **3 net new lines** in expr.test.ts (the new test case).

## Commits

| # | Hash | Message |
|---|------|---------|
| 1 | `kqloppxlywlytyrlnsulqwmlsozonmoo` | `refactor(2.1-02): rename expr.commit -> expr.rev across 45 consumer sites` |

## Pre/Post Grep Reconciliation

| Source | Stated count | Actual at plan-02 entry |
|--------|--------------|--------------------------|
| PLAN-02.md (`<interfaces>`) | 46 | — |
| 02.1-01-SUMMARY.md (handoff) | 62 | — |
| Live grep `expr\.commit\(` | — | **45** |

**Why the discrepancy:**

- The handoff's **62** figure used regex `expr\.commit\b` (no trailing `(`), so it included every prose mention of `expr.commit` inside `// comments` (e.g. `// expr.commit() per Blocker 3 from iteration 1`). Those references stay in comments because the rename also rewrites the comment text via the same `expr.commit(` → `expr.rev(` substitution where comments use the call-form spelling; comments that say `expr.commit factory` (no parens) are NOT call sites and were excluded from the rename gate.
- The PLAN's **46** figure was a per-file sum-off-by-one: it stated verify.cjs had 9 sites; actual file has 10. All other per-file counts in PLAN matched ground truth.
- Final reconciled ground truth: **45** call-form occurrences. Post-commit grep returns **0**.

```
$ grep -rn "expr\.commit(" --include="*.ts" --include="*.cjs" --include="*.js" --include="*.mjs" \
    2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | wc -l
0
```

## Verification

### Grep gates

| Gate | Expected | Actual |
|------|----------|--------|
| Repo-wide `expr\.commit\(` (excl. .planning + dist-cjs + node_modules) | 0 | 0 ✓ |
| Sum of `expr\.rev\(` across the 9 files | ≥45 | 47 (45 renames + 1 from new test invocation + 1 from describe-block subheading … see breakdown) ✓ |
| `describe\('expr\.rev` in expr.test.ts | 1 | 1 ✓ |
| `describe\('expr\.commit` in expr.test.ts | 0 | 0 ✓ |
| `hex-SHA or change-id shaped string` in expr.test.ts | ≥1 | 4 (one per error-message assertion) ✓ |
| `change_id alphabet shape` in expr.test.ts | 1 | 1 ✓ |

Per-file `expr.rev(` count (replaces the per-file `expr.commit(` counts on entry):

```
get-shit-done/bin/lib/verify.cjs:                 10
get-shit-done/bin/lib/graphify.cjs:                5
get-shit-done/bin/lib/commands.cjs:                3
sdk/src/query/verify.ts:                           5
sdk/src/query/progress.ts:                         2
sdk/src/vcs/__tests__/expr.test.ts:                8  (= 7 renamed + 1 new test)
sdk/src/vcs/__tests__/baseline-parity.test.ts:     9
sdk/src/vcs/__tests__/adapter-contract.test.ts:    1
tests/__tools__/capture-vcs-baselines.cjs:         4
Total:                                            47
```

### Unit test gate

```
$ cd sdk && pnpm exec vitest run src/vcs/__tests__/expr.test.ts
 ✓ |unit| src/vcs/__tests__/expr.test.ts (24 tests) 3ms
 Test Files  1 passed (1)
      Tests  24 passed (24)
```

All 24 expr.test.ts tests pass against the renamed factory — including the 6 tests inside the renamed `describe('expr.rev factory', …)` block (5 renamed + 1 new change_id-alphabet test). The error-message regex tightening from `/SHA/` to `/hex-SHA or change-id shaped string/` passes against the actual message text at `sdk/src/vcs/expr.ts:106`.

### tsc delta

`pnpm exec tsc --noEmit -p sdk/tsconfig.json 2>&1 | grep "expr\.commit"` returns **zero matches** — the expr.commit dimension of the broken-lint window has fully closed.

Other broken-lint dimensions remain on `phase/2.1-audit` per D-21:

- `pathspec` / `stage` / `unstage` (CommitInput field + verbs removed in 01) — pending plan 03 or later
- `currentBranch` → `currentBookmarks` rename — pending plan 03 or later
- `StatusEntry.index` removal — pending future plan
- `vcs.hooks` removal — pending future plan
- `gitDir`/`gitCommonDir` move to `gitOnly` — pending future plan

These untouched dimensions still register tsc errors against the same downstream files; this plan only closes the expr.commit dimension as instructed by PLAN-02's mechanical-only scope.

## Self-Check: PASSED

Verified each modification landed:

- Branch: `phase/2.1-audit` ✓
- HEAD commit subject: `refactor(2.1-02): rename expr.commit -> expr.rev across 45 consumer sites` ✓
- `git diff HEAD~1 HEAD --name-only | wc -l` returns 9 ✓
- Repo-wide `expr\.commit\(` grep (excl. .planning/dist-cjs/node_modules) returns 0 ✓
- `describe\('expr.rev'` in expr.test.ts: 1 occurrence at line 96 ✓
- `change_id alphabet shape` new test: 1 occurrence ✓
- `pnpm exec vitest run src/vcs/__tests__/expr.test.ts`: 24/24 pass ✓
- Internal sdk/src/vcs/expr.ts unchanged by this plan (already migrated in plan 01) ✓
- No `.planning/` files modified (historical references preserved) ✓
- No git history rewrites (commits 3acf0aa7, 877cf457, 1b2772db from plan 01 untouched) ✓

## Deviations from Plan

**None.** Plan-02 executed exactly per its task structure (Task 1 mechanical edits + Task 2 single dedicated commit). The four rule-driven deviation classes (auto-fix bugs / auto-add missing functionality / auto-fix blocking / ask architectural) did not trigger — the work was pure mechanical substitution.

### Minor notes (NOT deviations)

1. **Pre-grep reconciliation surfaced 45, not 46/62.** PLAN-02's `<interfaces>` block expected 46 and named verify.cjs with 9 sites; actual file at plan-02 entry had 10 sites. Documented in the commit message and the Pre/Post Grep Reconciliation section above. This does not constitute a deviation because the plan's `<verify>` block uses `≥45` (slack for per-file drift); the work proceeded as mechanical-only.

2. **Total `expr.rev(` count is 47, not 46.** The +2 over the 45-rename baseline come from: (a) the new test case adds 1 new `expr.rev(` call (`expect(() => expr.rev('kxnvqropmlkz'))`), and (b) the existing `it('throws on non-SHA input ...')` test was already 3 invocations of `expr.commit('not-a-sha')` / `''` / `'xyz'` which now register as 3 `expr.rev(` calls — math: 7 renamed + 1 new = 8 in expr.test.ts; sum across all 9 files = 47.

## Auth Gates

None. Plan was fully autonomous; no external services or credentials touched.

## Threat Surface Audit

Per PLAN-02's `<threat_model>`:

- **T-2.1.02-01 (renamed call site tampering)** — Mitigated: every renamed call site preserves its original argument list verbatim (only the factory name changes). `expr.rev` enforces SHA/change_id shape via SHA_OR_CHANGE_ID_RE at expr.ts:70 (landed in plan 01).
- **T-2.1.02-02 (new change_id test input tampering)** — Mitigated: the new test's input `'kxnvqropmlkz'` is a hardcoded literal in expr.test.ts:115; it cannot reach production runtime. The string was chosen as a valid jj change_id alphabet pattern (12 k-z chars) to exercise the widened regex.
- **T-2.1.02-SC (package legitimacy)** — Vacuous: 0 packages installed in plan 02.

No new threat flags surfaced during execution.

## Plan-03 Handoff

Per the broken-lint window narrative, the **next concern to close** is whichever the orchestrator schedules next (suggested order from plan 01's handoff):

- **`currentBranch` → `currentBookmarks` rename**: 1 production site at `sdk/src/query/check-ship-ready.ts`; test consumers in `baseline-parity.test.ts`, `git-backend.test.ts`, `adapter-contract.test.ts`.
- **`pathspec` removal from CommitInput**: 2 production sites in `sdk/src/query/commit.ts` (lines ~155 and ~300); plus paired `commit.test.ts`.
- **`vcs.stage` / `vcs.unstage` consumer migration**: 2 production sites in `sdk/src/query/commit.ts`; tests across baseline-parity/git-backend/adapter-contract.

Run `grep -rn "currentBranch\|pathspec\|vcs\.stage\|vcs\.unstage\|vcs\.hooks" sdk/src bin/lib tests --include='*.ts' --include='*.cjs'` at plan-03 entry to size the next sweep.

The expr.commit dimension is closed; the next plan can size its grep independently of any expr.* leftovers.

## Known Stubs

None. Every renamed call site is a real production or test consumer with end-to-end semantics; no placeholder data, no unimplemented branches. The new change_id-alphabet test is a hardcoded literal exercising a real factory path.
