---
phase: 02.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 09
subsystem: vcs-adapter
tags: [phase-close, learnings, baseline-sweep, allowlist-tighten, D-22]
requires:
  - "2.1-01 through 2.1-08 (full type-surface reshape + caller migration on phase/2.1-audit)"
  - "fixup commit 7f4c8ffa (plan-04 #3061 read-tree HEAD resolution)"
provides:
  - "Phase 2.1 closed: phase/2.1-audit is mergeable to main"
  - "Baseline corpus aligned with post-2.1 dispatch table (5 orphans deleted; 0 args drift)"
  - "Lint allowlist re-tightened per D-22 (24 → 14 file entries; 0 entries added)"
  - "02.1-LEARNINGS.md captured for Phase 3 consumption + retrospective rollup"
  - "Full test suite delta vs main: 0 new failures (every remaining failure pre-existing on main)"
affects:
  - "Phase 3 (jj backend) — change_id translator placement, VcsHooks-removal implications for HOOK-01..05, CommitInput WC-state-capture contract, status porcelain trim convention"
tech_stack:
  added: []
  patterns:
    - "Audit-driven allowlist re-tightening: per-file regex scan with the exact scanner GIT_PATTERNS to confirm each removable entry produces 0 hits; remove (a) files with zero raw-git invocations and (b) file entries redundant with an existing glob"
    - "Cosmetic-re-bless skip: when args field is unchanged and tests use regex:^\\[…\\] stdout matching, re-bless run produces only captured_at/SHA churn — revert and act only on true orphans"
    - "Per-concern phase-close commits: separate orphan-baseline deletion (chore), allowlist sweep (chore), Rule-1 test-string fix (fix), and LEARNINGS (docs) into individual commits for verifier auditability"
key_files:
  created:
    - .planning/phases/02.1-vcs-abstraction-audit-drop-git-only-concepts/02.1-LEARNINGS.md
    - .planning/phases/02.1-vcs-abstraction-audit-drop-git-only-concepts/02.1-09-SUMMARY.md
  modified:
    - scripts/lint-vcs-no-raw-git.allow.json
    - tests/bug-2767-gsd-sdk-commit-files-flag.test.cjs
  deleted:
    - tests/baselines/git-vcs/commit-ts-148-add.snap.json
    - tests/baselines/git-vcs/commit-ts-294-add-c-form.snap.json
    - tests/baselines/git-vcs/commands-cjs-330-rm-cached.snap.json
    - tests/baselines/git-vcs/commands-cjs-332-add.snap.json
    - tests/baselines/git-vcs/commands-cjs-398-add.snap.json
decisions:
  - "Re-bless run touched 49 baselines for cosmetic reasons only (captured_at date + ephemeral commit SHAs; commit baselines use regex:^\\[…\\] stdout matching so SHA churn is invisible). RESEARCH Assumption A4 predicted ~6 args-shape drift; actual args drift was 0. Cosmetic churn reverted; only the 5 stage/unstage dispatch-removal orphans deleted."
  - "Allowlist file-list shrunk 24 → 14 entries. Two removal classes: (a) zero raw-git invocations (parse/git-rev.ts, parse/jj-rev.ts, tests/helpers.cjs, scripts/run-tests.cjs, docs/adr/0004-*.md); (b) file entries redundant with an existing glob (sdk/src/vcs/__tests__/{adapter-contract,expr,parse-git-rev,backends}.test.ts redundant with the sdk/src/vcs/__tests__/** glob; tests/vcs-adapter-contract.test.cjs redundant with tests/**/*.test.cjs). Per D-22 hard rule: no entries added."
  - "Rule-1 auto-fix: tests/bug-2767-gsd-sdk-commit-files-flag.test.cjs:145 asserted reason='nothing staged' but the source had shifted to 'nothing to commit' in plan 2.1-04 (commit.ts:139-141 comment block documented the wording change but the sibling test wasn't paired into the D-06 commit). Fix the test assertion to match the documented source behavior."
  - "Plan-06 SUMMARY's pre-existing-failure attribution for tests/worktree-safety-policy.test.cjs was narrowly scoped (named 2 failures; plan-09 full-suite verified all 9 failures in that file are pre-existing on main). Documented in 02.1-LEARNINGS.md as Lesson + Surprise 4."
  - "Phase 2.1 was checkpoint-free across all 9 plans: zero auth gates, zero architectural Rule-4 surprises beyond the known #3061 carry-forward (which resolved via fixup commit 7f4c8ffa between plans 04 and 05). Pure type-surface + caller-sweep phases are inherently checkpoint-free."
requirements_completed: []
metrics:
  duration: "~50 min (re-bless analysis + allowlist audit + Rule-1 fix + LEARNINGS authoring)"
  completed_date: "2026-05-11"
  files_modified: 2
  files_deleted: 5
  files_created: 2
  tasks_completed: 3
---

# Phase 2.1 Plan 09: Phase Close — LEARNINGS + Final Verification Summary

**One-liner:** Closes phase/2.1-audit by deleting 5 orphaned baseline JSONs (stage/unstage dispatch removal), re-tightening the lint allowlist 24→14 file entries per D-22 (10 entries removed; 0 added), Rule-1-fixing one bug-2767 test that asserted the pre-plan-04 reason wording, running the full repo test suite (1283 pass / 15 fail — 0 new failures vs main, every remaining failure pre-existing), and authoring 02.1-LEARNINGS.md for Phase 3 + retrospective consumption.

## What Shipped

Four dedicated commits on `phase/2.1-audit`:

| Commit | Type | Description |
|--------|------|-------------|
| `vrnxvuprlonysvompnuxopruxsroopwy` | chore | Delete 5 orphaned baseline JSONs (stage/unstage dispatch removal) |
| `svtntlyovkpuoupykmlypnuwsyqtkszv` | chore | Re-tighten lint-vcs-no-raw-git allowlist (D-22): 24 → 14 file entries |
| `qxzmyxovvwurnqptlqxrymmkzossvlxt` | fix | Rule-1 auto-fix: update bug-2767 test to match Plan 2.1-04 reason wording |
| (this SUMMARY's pair) | docs | Land 02.1-LEARNINGS.md + 02.1-09-SUMMARY.md + ROADMAP/STATE updates |

## Task-by-Task

### Task 1: Baseline-parity sweep (commit `vrnxvuprlonysvompnuxopruxsroopwy`)

**Pre-state:** 55 baselines in `tests/baselines/git-vcs/`. baseline-parity.test.ts dispatched 55 it() cases. Plan-08 SUMMARY identified 5 orphan baselines (dispatch clauses removed in plan 08; baseline JSONs left behind).

**Action:**
1. Ran the capture tool (`node tests/__tools__/capture-vcs-baselines.cjs`); inspected the resulting 49-baseline diff. Every diff was `captured_at: "2026-05-10" → "2026-05-12"` plus ephemeral commit SHAs in stdout fields. Args field was unchanged in every baseline. Commit baselines use `regex:^\[…\]` stdout matching — SHA churn is invisible to the test. Reverted the cosmetic churn (`git checkout -- tests/baselines/git-vcs/`).
2. Deleted the 5 true orphans: `commit-ts-148-add`, `commit-ts-294-add-c-form`, `commands-cjs-330-rm-cached`, `commands-cjs-332-add`, `commands-cjs-398-add`.
3. Re-ran baseline-parity vitest: 50/50 pass (was 55/55; 5 baselines pruned).

**Post-state:** 50 baselines on disk; 50 dispatch clauses in baseline-parity.test.ts; baseline-parity vitest exits 0. No orphan clauses, no orphan baselines.

### Task 2: Allowlist sweep + full test suite (commit `svtntlyovkpuoupykmlypnuwsyqtkszv`)

**Pre-state:** 24 file entries + 9 globs in `scripts/lint-vcs-no-raw-git.allow.json`. Lint exits 0 on phase/2.1-audit.

**Action:**
1. Ran lint guard: exit 0 (890 files scanned, 0 violations). No new raw-git was accidentally introduced.
2. Per-file regex audit (applied the scanner's exact `GIT_PATTERNS` + `SHELL_GIT_PATTERNS`; honored `allow-line-annotation` and shell-comment skip) against every allowlist file. Tally:

   | File | Raw-git hits | Action |
   |------|--------------|--------|
   | sdk/src/vcs/exec.ts | 0 | Keep (defensive — legitimate `spawnSync(bin,…)` gate; future literal `git` adds would land here) |
   | sdk/src/vcs/backends/git.ts | 2 | Keep |
   | sdk/src/vcs/parse/git-rev.ts | 0 | **Remove** (pure parser; never had raw-git) |
   | sdk/src/vcs/parse/jj-rev.ts | 0 | **Remove** (pure parser; never had raw-git) |
   | sdk/src/vcs/__tests__/vcs-fixture.ts | 6 | Keep |
   | sdk/src/vcs/__tests__/adapter-contract.test.ts | 0 | **Remove** (redundant with `sdk/src/vcs/__tests__/**` glob) |
   | sdk/src/vcs/__tests__/git-backend.test.ts | 50 | Keep |
   | sdk/src/vcs/__tests__/exec.test.ts | 1 | Keep |
   | sdk/src/vcs/__tests__/index.test.ts | 1 | Keep |
   | sdk/src/vcs/__tests__/expr.test.ts | 0 | **Remove** (redundant with glob) |
   | sdk/src/vcs/__tests__/parse-git-rev.test.ts | 0 | **Remove** (redundant with glob) |
   | sdk/src/vcs/__tests__/backends.test.ts | 0 | **Remove** (redundant with glob) |
   | sdk/src/vcs/__tests__/baseline-parity.test.ts | 6 | Keep |
   | tests/helpers.cjs | 0 | **Remove** (migrated to VcsAdapter in 02-02/02-03) |
   | tests/vcs-adapter-contract.test.cjs | 0 | **Remove** (redundant with `tests/**/*.test.cjs` glob) |
   | tests/vcs-cjs-smoke.test.cjs | 5 | Keep |
   | tests/__tools__/capture-vcs-baselines.cjs | 8 | Keep |
   | scripts/lint-vcs-no-raw-git.cjs | 5 | Keep (scanner matches its own regex pattern definitions) |
   | scripts/check-skip-count.cjs | 2 | Keep |
   | scripts/run-tests.cjs | 0 | **Remove** (zero raw-git) |
   | docs/adr/0004-worktree-workstream-seam-module.md | 0 | **Remove** (markdown not scanned; also covered by `docs/**` glob) |
   | scripts/base64-scan.sh | 1 | Keep |
   | scripts/prompt-injection-scan.sh | 1 | Keep |
   | scripts/secret-scan.sh | 1 | Keep |

   Removed 10 entries total. Lint re-ran post-removal: exit 0 (890 files, 0 violations). Final file-list count: 14. Globs unchanged.

3. Full test suite (`pnpm test`):
   - **phase/2.1-audit:** 1283 pass / 15 fail (after the Task-3 Rule-1 fix added 1 net pass)
   - **main (baseline comparison):** 1283 pass / 15 fail
   - **comm-diff:** 0 new failures on the branch; 0 new passes lost
   - All 15 failures are pre-existing on main:
     - 9× `tests/worktree-safety-policy.test.cjs` (deps.execGit mock pathway non-functional since plan 02-04 migrated worktree-safety.cjs to VcsAdapter)
     - 2× `tests/bug-2838-summary-rescue-gitignored-planning.test.cjs` (gpg-sign required by test setup; host gpg config issue)
     - 2× `execute-phase.md / quick.md rescue block recovers SUMMARY when .planning/ is gitignored` (same gpg-sign issue)
     - 1× `rescue is idempotent when SUMMARY already present in main repo` (same gpg-sign issue)
     - 1× `worktree-safety policy module` (rollup of the 9-test failure above)

### Task 3: Rule-1 auto-fix bug-2767 test + LEARNINGS (commits `qxzmyxovvwurnqptlqxrymmkzossvlxt` + this commit)

**Pre-state:** Full suite on phase/2.1-audit showed 17 failures; main showed 15. Diff:
- `bug #2767 (behavioral): gsd-sdk query commit --files` (parent suite rollup)
- `positional form with no .planning/ change: returns "nothing staged"` (the actual failing child test)

**Action (Rule-1 auto-fix, commit `qxzmyxovvwurnqptlqxrymmkzossvlxt`):**
1. Traced the failure: test asserted `result.json.reason === 'nothing staged'` but the SDK returned `'nothing to commit'`.
2. Read `sdk/src/query/commit.ts:139-141` — the comment block explicitly documents that plan 2.1-04 (D-02 + D-04 + D-06) shifted the wording from `'nothing staged'` to `'nothing to commit'` to reflect the new WC-state-capture semantic. The source change was intentional; the test was missed in the D-06 paired commit (plan 04 paired commit.ts with commit.test.ts but not with the sibling bug-2767 test file).
3. Updated the test's expected reason string to `'nothing to commit'`, renamed the test title to match, and added a comment block referencing the commit.ts:139-141 explanation.
4. Re-ran the test file: 5 pass / 1 fail → 6 pass / 0 fail.
5. Re-ran the full suite: 1283 pass / 15 fail, 0 new failures vs main.

**Action (LEARNINGS, this commit):**
1. Read 02-LEARNINGS.md for structural precedent.
2. Aggregated findings from 2.1-01..08 SUMMARYs + RESEARCH (assumptions A1-A7, open questions 1-4, pitfalls 1-7).
3. Authored `02.1-LEARNINGS.md` with sections: Summary, Decisions (5), Lessons (7), Patterns (6), Surprises (8), Phase 3 Handoff, Carry-Forward.
4. Notable surprises documented:
   - Surprise 1: `status({porcelain:true})` trim-leading-space bug (commit `xkztxppkxsopnznuvolxlxvvwoqkmkxl` side fix)
   - Surprise 2: #3061 fixup architectural decision mid-phase (Option C: pre-add `git read-tree HEAD`)
   - Surprise 3: Plan 05 was a literal no-op
   - Surprise 4: Plan-06 worktree-safety pre-existing-failure attribution was narrowly scoped
   - Surprise 5: Test-wording paired-commit miss (bug-2767 vs commit.test.ts in plan 04 D-06 pairing)
   - Surprise 6: Stale `sdk/dist/` hid the Surprise-5 regression during interactive testing
   - Surprise 7: Re-bless run touched 49 baselines for cosmetic reasons only
   - Surprise 8: Allowlist audit found 10 removable entries (not the 0-2 plan-08 anticipated)

## Verification

### Lint gate
```
node scripts/lint-vcs-no-raw-git.cjs
# ok lint-vcs-no-raw-git: 890 files scanned in <root>, 0 violations
# exit 0
```

### Baseline-parity gate
```
cd sdk && pnpm exec vitest run src/vcs/__tests__/baseline-parity.test.ts
# Test Files  1 passed (1)
#      Tests  50 passed (50)
# exit 0
```

### Full suite gate
```
pnpm test
# ✔ 1283  ✖ 15
# exit 1 (only pre-existing failures; 0 new on branch vs main)
```

### Allowlist counts
```
node -e "const a=require('./scripts/lint-vcs-no-raw-git.allow.json'); console.log('files:', a.files.length, 'globs:', a.globs.length)"
# files: 14 globs: 9
```

Pre-plan-09: 24 files, 9 globs. Post-plan-09: 14 files, 9 globs. Delta: -10 files, 0 globs. D-22 honored (no entries added).

## Self-Check: PASSED

- Branch: `phase/2.1-audit` ✓
- Plan-09 commits on branch: `vrnxvuprlonysvompnuxopruxsroopwy` (orphan delete), `svtntlyovkpuoupykmlypnuwsyqtkszv` (allowlist sweep), `qxzmyxovvwurnqptlqxrymmkzossvlxt` (bug-2767 fix) — all three confirmed via `git log --all --oneline | grep` ✓
- 5 orphan baselines deleted: `ls tests/baselines/git-vcs/ | wc -l` reports 50 (was 55) ✓
- Allowlist 24 → 14 files, 0 entries added (D-22): `node -e "..."` reports `files: 14 globs: 9` ✓
- Lint exit 0: `ok lint-vcs-no-raw-git: 890 files scanned, 0 violations` ✓
- baseline-parity 50/50 pass ✓
- Full suite delta vs main: 0 new failures ✓
- 02.1-LEARNINGS.md exists at canonical path ✓
- 02.1-09-SUMMARY.md exists at canonical path ✓
- No STATE.md or ROADMAP.md touched in plan 09 commits (per success criteria) ✓
- No checkpoint/auth gate encountered ✓

## Decisions Made

See frontmatter `decisions` list. Highlights:

1. **Cosmetic-re-bless skip:** Re-bless run touched 49 baselines but only captured_at date + ephemeral commit SHAs differed; args field was unchanged everywhere. Reverted the cosmetic churn and acted only on the 5 stage/unstage dispatch-removal orphans. Documented as Pattern + Surprise 7 in LEARNINGS.
2. **Allowlist audit was broader than anticipated:** Per-file scan with exact scanner regex found 10 removable entries (5 zero-raw-git + 5 redundant-with-glob). Plan-08 anticipated 0-2; the gap is documented as Surprise 8.
3. **Rule-1 test-wording fix:** Plan 2.1-04's wording shift `'nothing staged'` → `'nothing to commit'` was documented in source but missed in the sibling `tests/bug-2767-*.test.cjs` test file (D-06 pairing covered `commit.test.ts` only). One-line test fix; production behavior unchanged.

## Deviations from Plan

**Re-bless task simplified to orphan-deletion only.** Plan-09 task 1 described a re-bless run followed by orphan deletion. Empirically the re-bless run produced only cosmetic diffs (captured_at + ephemeral SHAs, no args drift). The actually-load-bearing work was orphan deletion only. Decision documented in commit `vrnxvuprlonysvompnuxopruxsroopwy` body and in this SUMMARY's task 1 narrative.

**Allowlist sweep removed more entries than anticipated.** Plan-09 task 2 anticipated "any entry that is for a file that no longer has raw-git" would be removed. Per-file regex audit also surfaced 5 entries that were redundant with existing globs. Removed both classes (10 entries total) to fully honor D-22's "re-tighten" spirit. Documented in commit `svtntlyovkpuoupykmlypnuwsyqtkszv` body.

**Rule-1 auto-fix surfaced during full-suite gate.** Plan-09 task 2's full-suite gate was supposed to compare against STATE.md's known-failure list (`commit.test.ts:304` + `config-mutation.test.ts:441`). Both of those tests passed (the plan-04 fixup commit `xkztxppkxsopnznuvolxlxvvwoqkmkxl` resolved them). The full-suite diff vs main surfaced 1 new failure (`bug-2767 positional form`) which was auto-fixed inline per Rule 1. Documented as a deviation because the plan's task structure didn't anticipate this surfacing.

**No Rule 4 architectural decisions, no auth gates, no Rule 2 missing-functionality.** Phase 2.1 stayed checkpoint-free end-to-end.

## Auth Gates

None. Plan was fully autonomous.

## Issues Encountered

**Stale `sdk/dist/`:** The Rule-1 bug-2767 fix initially appeared to need a different fix because a stale dist served the pre-plan-04 `'nothing staged'` reason string. Rebuild (`pnpm run build:sdk`) restored consistency. Documented in LEARNINGS as Surprise 6. No action required beyond awareness — `pretest: pnpm run build:sdk` normally handles this.

## Threat Surface Audit

Per PLAN-09's `<threat_model>`:

- **T-2.1.09-01 (Tampering — re-blessed baselines):** N/A in this execution. The re-bless run's output was cosmetic-only; no baselines were re-blessed (re-bless reverted). The 5 deletions are not re-blesses.
- **T-2.1.09-02 (Information Disclosure — deleted-orphan-baseline regression):** N/A. Orphan baselines correspond to dispatch clauses that no longer exist (stage/unstage). Deletion is symmetric with plan-08's clause removal — no behavioral regression possible.
- **T-2.1.09-03 (Tampering — allowlist re-tighten):** N/A. Removing entries strengthens the lint guard; cannot regress security posture. D-22 hard rule preserved (no entries added).
- **T-2.1.09-SC (npm installs):** N/A.

No new threat flags surfaced during execution.

## Phase-Close Handoff

Phase 2.1 is closed. `phase/2.1-audit` is mergeable to main:

- Lint exits 0
- baseline-parity green (50/50)
- Full test suite green-modulo-pre-existing (1283 pass / 15 fail = main's exact count and exact failure set)
- LEARNINGS captured for Phase 3 + retrospective
- Allowlist at post-2.1 steady state (14 file entries; D-22 holds)
- No STATE.md or ROADMAP.md touched in plan-09 commits per the task spec — those updates land in a follow-up `docs(2.1): mark plan 09 complete` commit after this SUMMARY merges, owned by the phase-close orchestrator.

For Phase 3 (jj backend) consumption details, see `02.1-LEARNINGS.md` → **Phase 3 Handoff** section.

## Known Stubs

None. All edits are real deletions or real reshapes; no placeholder data, no skipped tests, no TODO markers introduced.
