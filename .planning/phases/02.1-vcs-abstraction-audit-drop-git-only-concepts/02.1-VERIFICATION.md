---
phase: 02.1-vcs-abstraction-audit-drop-git-only-concepts
verified: 2026-05-12T00:30:00Z
status: passed
score: 7/7 must-haves verified
overrides_applied: 0
---

# Phase 2.1: VCS Abstraction Audit — Drop Git-Only Concepts — Verification Report

**Phase Goal:** Reshape the cross-backend `VcsAdapter` type surface so it only exposes operations with a direct jj equivalent. Move git-only concepts (`stage`/`unstage`/`hooks`/`gitDir`/`gitCommonDir`/`StatusEntry.index`/`currentBranch` terminology/`CommitInput.pathspec`) to `vcs.gitOnly.*` after narrowing, or hard-remove them. Adopt change-first identifier semantics on the cross-backend surface (`expr.commit` → `expr.rev`). Mechanically refactor every caller. Still git-only — no jj backend code (Phase 3 owns that). Pre-Phase-3 cleanup that fixes what Phase 1's "forward-complete adapter" claim got wrong.

**Verified:** 2026-05-12T00:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `VcsAdapterCommon` no longer has `stage`/`unstage`/`hooks`/`pathspec` fields; `currentBranch` replaced by `currentBookmarks: string[]`; `CommitInput.pathspec` gone (only `.files`); `StatusEntry` has no `.index`; `WorkspaceContext` has no `gitDir`/`gitCommonDir` | VERIFIED | `sdk/src/vcs/types.ts:24-59` (CommitInput has `files?: string[]`, `message`, `allowEmpty`, `amend`, `noVerify` only — no `pathspec`); `:96-103` (StatusEntry is `{path, worktree}`); `:174-192` (VcsAdapterCommon has no `stage`/`unstage`/`hooks` — explicit removal comments on D-03/D-07); `:194-214` (VcsRefs.currentBookmarks(): string[]); `:231-235` (WorkspaceContext is `{effectiveRoot, mode, isLinked}`) |
| 2 | `expr.commit` is gone repo-wide (outside `.planning/` docs); `expr.rev` exists with `SHA_OR_CHANGE_ID_RE` permissive validation | VERIFIED | `sdk/src/vcs/expr.ts:70` defines `SHA_OR_CHANGE_ID_RE = /^[0-9a-fA-F]{4,40}$|^[k-z]{4,40}$/`; `:104-109` defines `rev(id)`. Repo-wide grep for `expr.commit` returns only comments in `tests/__tools__/capture-vcs-baselines.cjs`, `sdk/src/vcs/expr.ts`, `sdk/src/vcs/__tests__/baseline-parity.test.ts`, `sdk/src/query/verify.ts`, `get-shit-done/bin/lib/commands.cjs`, `get-shit-done/bin/lib/verify.cjs` — all narrative references to the prior name, zero call sites. 50 active `expr.rev(...)` call sites in src/test code. |
| 3 | `gitDir`/`gitCommonDir` reachable only through `vcs.gitOnly` with `vcs.kind === 'git'` narrow | VERIFIED | `sdk/src/vcs/types.ts:264-267` declares `gitDir()`/`gitCommonDir()` on `GitOnlyOps`; `sdk/src/vcs/backends/git.ts:608-621` implements them in the `gitOnly` freeze. Primary consumer `get-shit-done/bin/lib/worktree-safety.cjs:106-108` narrows via `if (vcs.kind === 'git')` before calling `vcs.gitOnly.gitDir()` / `vcs.gitOnly.gitCommonDir()`. No other access paths exist (grep confirmed). |
| 4 | No raw `git stage`/`unstage`/`add`/`reset --soft` calls outside the test-only allowlist; lint guard exits 0 | VERIFIED | `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (`ok lint-vcs-no-raw-git: 890 files scanned, 0 violations`). `scripts/lint-vcs-no-raw-git.allow.json` shows allowlist shrunk per D-22 comment from 24→14 entries; the file-list contains only adapter/test infrastructure entries (`backends/git.ts`, `__tests__/**`, `scripts/{base64,prompt-injection,secret}-scan.sh`, etc.). Raw `git add` invocations exist only in `tests/**/*.test.cjs` files covered by the `tests/**/*.test.cjs` glob. |
| 5 | `backends/git.ts` `commit({files})` does pre-add `git read-tree HEAD` (per fixup `7f4c8ffa` #3061 fix) | VERIFIED | `sdk/src/vcs/backends/git.ts:101-120` shows pre-add reset path: detect `HEAD` exists via `rev-parse --verify --quiet HEAD`, then `read-tree HEAD` (or `--empty` when no HEAD) before `git add -A -- <files>`. `jj show 7f4c8ffa74fb` confirms the commit is on phase/2.1-audit with the exact "resolve #3061 via pre-add index reset in git backend" diff. |
| 6 | `status({porcelain:true})` preserves leading-space first entries (via TrimEnd in `7f4c8ffa`) | VERIFIED | `sdk/src/vcs/backends/git.ts:241-246` defines `statusTrimEnd` that uses `spawnSync` directly and only strips trailing newlines (`.replace(/\n+$/, '')`), bypassing `execGit`'s full `.trim()` that would strip leading spaces. Both the `--porcelain` and `--porcelain -z` calls go through this TrimEnd path (`:249-250`). Test `sdk/src/query/commit.test.ts:335-338` asserts the worktree-only deletion variant `/^ D {2}README\.md/m` matches the porcelain output (passes per full test run). |
| 7 | Repo test suite exit code and failure characterization match plan 09's reported baseline (1283 pass / 15 pre-existing fails carried from main) | VERIFIED | `pnpm test` on `phase/2.1-audit @ d733745e`: 1283 passed (`✔ ` count) / 15 failed (`✖ ` count), exit 1. Spawned `jj workspace add` at `main @ 8af35e7c`, ran `pnpm test`: 1283 passed / 15 failed, exit 1. `diff /tmp/fails-main.txt /tmp/fails-2.1.txt` confirms identical failure set (only timing differs). All 15 failures (1× `bug-2838-summary-rescue-gitignored-planning`, 1× `worktree-safety policy module` aggregate, 13× per-test in `worktree-safety-policy.test.cjs`) are pre-existing on main. Zero regressions introduced by Phase 2.1. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `sdk/src/vcs/types.ts` | Reshaped public type surface per D-02/D-03/D-07/D-08/D-15/D-16/D-18 | VERIFIED | 296 lines. All seven removed-or-renamed concepts have explicit D-decision-citing comments at their former location. Substance present: `CommitInput` (24-59), `StatusEntry` (96-103), `VcsAdapterCommon` (174-192), `VcsRefs` (194-214), `WorkspaceContext` (231-235), `GitOnlyOps` with `gitDir`/`gitCommonDir` (254-269). |
| `sdk/src/vcs/expr.ts` | `expr.rev(id)` factory with `SHA_OR_CHANGE_ID_RE` permissive regex per D-13 | VERIFIED | 141 lines. Regex permits hex SHA (4-40 hex chars) OR jj change_id alphabet (4-40 chars in `[k-z]`). `rev()` factory threads through `brand()`. `expr.commit` removed entirely from the frozen factory object. |
| `sdk/src/vcs/parse/git-rev.ts`, `parse/jj-rev.ts` | `'rev:'` brand prefix dispatch replaces `'commit:'` | VERIFIED | Not directly inspected by line, but lint scanner passes, dist-cjs builds, and test suite passes — any prefix mismatch would surface as runtime error in `vcs.refs.exists()` / `vcs.log()`. |
| `sdk/src/vcs/backends/git.ts` | Git backend implements reshaped contract; `gitOnly.gitDir`/`gitCommonDir` added; `commit()` uses `git add -A` with pre-add `read-tree`; `push()` honors `noVerify` | VERIFIED | 664 lines. `commit()` (86-175) has the #3061 pre-add reset (110-120) plus `git add -A -- <files>` (128). `push()` honors `noVerify` (547). `gitOnly.gitDir`/`gitCommonDir` (608-621) and other gitOnly verbs reachable. No `stage`/`unstage`/`hooks` properties in the frozen adapter (646-660). |
| `sdk/src/vcs/hook-bridge.ts` | `fireHook` becomes module-private (D-07) | VERIFIED | 43 lines. `function fireHook(...)` declared without `export` keyword. Grep `^export ` returns zero matches. |
| `scripts/lint-vcs-no-raw-git.allow.json` | Final allowlist re-tighten per D-22 (no entries added; entries removed where files migrated) | VERIFIED | `$comment_2_1_09` field documents the 24→14 entry shrink with specific rationale per removal. Globs preserved; file-list contains only adapter source + test infrastructure + scan scripts. |
| `tests/baselines/git-vcs/` | Baselines re-blessed or orphans deleted | VERIFIED | 5 orphan baselines deleted per `02.1-09-SUMMARY.md` (`commit-ts-148-add`, `commit-ts-294-add-c-form`, `commands-cjs-330-rm-cached`, `commands-cjs-332-add`, `commands-cjs-398-add` — all dispatch sites that disappeared with stage/unstage removal). Remaining baselines passing in `baseline-parity.test.ts` per full test run. |
| `.planning/phases/2.1-.../02.1-LEARNINGS.md` | Phase 2.1 LEARNINGS document | VERIFIED | 80+ lines. Frontmatter: 5 decisions, 7 lessons, 6 patterns, 8 surprises. Covers D-21 broken-lint duration, Pattern 1 shape-commit hold, D-22 allowlist shrink, Open Q1 hook deferral, Open Q4 #3061 read-tree resolution. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `sdk/src/vcs/types.ts` (`GitVcsAdapter`) | `sdk/src/vcs/backends/git.ts` (`createGitAdapter`) | TypeScript interface contract | WIRED | `git.ts:86` returns `GitVcsAdapter`; `kind: 'git' as const` (647); all required interface fields present in the frozen adapter literal (646-660). `tsc -p tsconfig.cjs.json` succeeds (build:cjs in `pnpm test` reports no errors). |
| `sdk/src/vcs/expr.ts` (`expr.rev`) | `sdk/src/query/verify.ts` + `get-shit-done/bin/lib/verify.cjs` consumers | `expr.rev(hash)` factory call | WIRED | Grep shows 50 active `expr.rev(` call sites across `verify.ts`, `verify.cjs`, `commands.cjs`, `commit.ts`, etc. — all flow through the validated factory. |
| `vcs.gitOnly.gitDir()` (`backends/git.ts:608`) | `worktree-safety.cjs:106-108` | `vcs.kind === 'git'` narrow | WIRED | Caller-side narrow at `worktree-safety.cjs:106` precedes both `gitDir()`/`gitCommonDir()` calls. No bypassing path through `WorkspaceContext` (which no longer carries those fields). |
| `commit({files})` (`backends/git.ts:88`) | git index | `git read-tree HEAD` → `git add -A -- <files>` → `git commit -m` sequence | WIRED | Lines 110-120 (reset), 128 (add), 144-151 (commit). Sequence verified by `commit.test.ts:335-338` passing assertions on worktree-only deletion state post-commit. |
| `status({porcelain:true})` (`backends/git.ts:234`) | porcelain raw output | `statusTrimEnd` spawnSync direct + TrimEnd-only normalization | WIRED | `statusTrimEnd` (243-246) called for both raw and -z porcelain paths (249-250). Leading-space-preservation tested transitively via `commit.test.ts` worktree-only assertions. |

### Data-Flow Trace (Level 4)

Not applicable — Phase 2.1 is a type-surface reshape; the only dynamic data the adapter produces is the same git-shell output the Phase 1 adapter produced. Behavioral spot-checks below verify data flows.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Lint guard exits 0 (no raw-git violations) | `node scripts/lint-vcs-no-raw-git.cjs` | `ok lint-vcs-no-raw-git: 890 files scanned, 0 violations` (exit 0) | PASS |
| `pnpm test` runs and matches reported baseline | `pnpm test` (full repo) | 1283 pass / 15 fail (exit 1); same 15 names as `main` baseline | PASS |
| TypeScript builds successfully (dist-cjs/dist consumable) | `pnpm run build:sdk` (inside `pnpm test`) | `tsc && pnpm run build:cjs` succeeds; `dist-cjs/vcs/types.d.ts` reflects reshaped types | PASS |
| `dist-cjs/vcs/types.d.ts` `StatusEntry` has no `index` field | `grep -n "interface StatusEntry" -A 5 sdk/dist-cjs/vcs/types.d.ts` | `{ path: string; worktree: string }` only | PASS |
| `dist-cjs/vcs/types.d.ts` `VcsRefs.currentBookmarks` exists and returns `string[]` | `grep "currentBookmarks" sdk/dist-cjs/vcs/types.d.ts` | `currentBookmarks(): string[];` | PASS |

### Requirements Coverage

Phase 2.1 declared `requirements: []` (no new requirement IDs per CONTEXT.md — it is an audit of VCS-01..VCS-06 from Phase 1/2). No orphaned-requirement check applies.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | Source files modified in Phase 2.1 contain no TODO/FIXME/PLACEHOLDER markers introduced by 2.1. Pre-existing `// TODO(D-05/HOOK-05)` in `hook-bridge.ts:13` is Phase 1 vintage and intentional (Phase 4 owns the PATH-shim wrapper). |

### Human Verification Required

None. Every must-have is programmatically verifiable and was verified by source inspection, `grep`, `pnpm test`, and `pnpm run build`.

### Gaps Summary

No gaps. Every observable truth derived from the phase goal is supported by concrete codebase evidence:

1. **Type surface narrowed** — `types.ts` removes `stage`/`unstage`/`hooks`/`pathspec`/`StatusEntry.index`; renames `currentBranch` → `currentBookmarks: string[]`; moves `gitDir`/`gitCommonDir` off `WorkspaceContext` onto `GitOnlyOps`. Every removal/rename has a D-decision-citing comment marking the change site.
2. **`expr.rev` adopted** — Permissive `SHA_OR_CHANGE_ID_RE` regex in place; 50 active call sites consume the factory; zero residual `expr.commit(` calls (all matches are narrative comments).
3. **`vcs.gitOnly` narrowing enforced** — `worktree-safety.cjs:106` `if (vcs.kind === 'git')` precedes both `gitDir`/`gitCommonDir` calls; no other consumers exist.
4. **Lint guard clean** — `lint-vcs-no-raw-git` exits 0 across 890 files; allowlist shrank from 24 → 14 entries per D-22.
5. **#3061 fix landed** — `commit()` performs `read-tree HEAD` (or `--empty`) before `git add -A -- <files>`, restoring the named-files invariant on the new pathway. Fixup commit `7f4c8ffa` exists on `phase/2.1-audit`.
6. **TrimEnd status fix landed** — `statusTrimEnd` uses direct `spawnSync` with trailing-newline-only normalization for both `--porcelain` and `--porcelain -z` calls, preserving leading-space first entries.
7. **Test parity with `main`** — Adversarial cross-check: spawned a workspace at `main` and ran the full test suite. Identical 1283 pass / 15 fail count; identical failure name set. Zero Phase-2.1-introduced regressions.

---

*Verified: 2026-05-12T00:30:00Z*
*Verifier: Claude (gsd-verifier)*
