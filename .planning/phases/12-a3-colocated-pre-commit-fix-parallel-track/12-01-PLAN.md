---
phase: 12-a3-colocated-pre-commit-fix-parallel-track
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [HOOK-06, HOOK-07]

must_haves:
  truths:
    - "D-03: this plan IS the ROADMAP + REQUIREMENTS doc cascade-amendment — ROADMAP Phase 12 SC2/SC3 and REQUIREMENTS HOOK-06/HOOK-07 are rewritten in a single docs-only plan that runs before the regression-test and audit plans"
    - "D-02: REQUIREMENTS HOOK-06 names the fire surface as .githooks/pre-commit and carries an explicit .git/hooks/ out-of-scope note (the jj adapter binds firing to .githooks/<stage> only) plus a husky/pre-commit-framework migration callout"
    - "D-04: ROADMAP Phase 12 SC3 is rewritten to drop the jj-colocated-hooks.test.ts alternative filename and instead point the regression test at the in-file extension of the jj-colocated describe block at jj-hooks.test.ts:167"
    - "ROADMAP Phase 12 SC2 names the sentinel hook path as .githooks/pre-commit, not .git/hooks/pre-commit"
    - "REQUIREMENTS HOOK-07 affirms the sentinel location is .githooks/pre-commit and confirms the test lives in the extended jj-hooks.test.ts:167 block"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 12 SC2 + SC3 wording"
      contains: ".githooks/pre-commit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected HOOK-06 + HOOK-07 wording"
      contains: ".githooks/pre-commit"
  key_links:
    - from: ".planning/ROADMAP.md Phase 12 SC2/SC3"
      to: "12-02-PLAN.md + 12-03-PLAN.md task descriptions"
      via: "corrected .githooks/ wording referenced by downstream plans"
      pattern: "\\.githooks/pre-commit"
---

<objective>
Amend the ROADMAP Phase 12 success criteria (SC2, SC3) and the REQUIREMENTS HOOK-06/HOOK-07
entries so the authoritative phase documents name the jj adapter's hook fire surface correctly:
`.githooks/<stage>` (the GSD-managed convention `fireHook` shells), not git's `.git/hooks/<stage>`
namespace.

Purpose: Per CONTEXT D-03 cascade-amendment hygiene (Phase 10 plan 10-01 precedent), the
ROADMAP + REQUIREMENTS edits land in a single docs-only plan that runs BEFORE the regression-test
and audit plans, so those later plans' task text references the corrected `.githooks/` wording
rather than the stale `.git/hooks/` wording.

Output: Two edited planning documents — `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
No code, no test, no audit artifact in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-CONTEXT.md
@.planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP Phase 12 SC2 + SC3 wording</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (lines 147-160 — the Phase 12 entry; confirm current SC text before editing)
    - .planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-CONTEXT.md (D-03 — exact amendment intent; D-04 — why SC3 drops the alternative filename)
    - .planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-PATTERNS.md ("`.planning/ROADMAP.md` — Phase 12 SC2 + SC3 wording edit" section — verbatim edit targets)
  </read_first>
  <action>
    Make two verbatim string replacements inside the Phase 12 "Success Criteria" block of
    `.planning/ROADMAP.md`. Do NOT renumber SC items, do NOT touch SC1/SC4/SC5, do NOT modify any
    other phase section.

    SC2 (currently ROADMAP.md line 155): the criterion text begins "On a colocated jj fixture,
    installing a sentinel `.git/hooks/pre-commit` and running `vcs.commit` (or direct `jj squash`)
    fires the hook exactly once …". Replace the substring ``.git/hooks/pre-commit`` with
    ``.githooks/pre-commit``. Leave the rest of SC2 (the "exactly once", "or zero times if
    `GSD_HOOK_SKIP_COLOCATED` opt-out applies" clauses) byte-identical.

    SC3 (currently ROADMAP.md line 156): the criterion text reads "Regression test at
    `sdk/src/vcs/__tests__/jj-colocated-hooks.test.ts` (or extension of existing
    `jj-hooks.test.ts`) is green on jj-colocated CI lane." Rewrite this whole criterion to:
    "Regression test extends the existing `jj-colocated` describe block at
    `sdk/src/vcs/__tests__/jj-hooks.test.ts:167` (no new test file) and is green on the
    jj-colocated CI lane." The `jj-colocated-hooks.test.ts` alternative-filename wording must not
    survive — D-04 locks the test as an in-file extension.

    Preserve the two-space leading indentation and the `1.`/`2.`/`3.` numbered-list format used by
    the Phase 12 Success Criteria block.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; grep -n 'jj-colocated-hooks.test.ts' .planning/ROADMAP.md; test $(grep -c 'jj-colocated-hooks.test.ts' .planning/ROADMAP.md) -eq 0 &amp;&amp; grep -q '.githooks/pre-commit' .planning/ROADMAP.md &amp;&amp; grep -q 'jj-hooks.test.ts:167' .planning/ROADMAP.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'jj-colocated-hooks.test.ts' .planning/ROADMAP.md` returns 0 (stale alternative filename removed).
    - The Phase 12 SC2 line contains the literal `.githooks/pre-commit` and no longer contains `.git/hooks/pre-commit`.
    - The Phase 12 SC3 line contains the literal `jj-hooks.test.ts:167`.
    - Phase 12 still has exactly five numbered Success Criteria (SC1 through SC5); SC1, SC4, SC5 text is unchanged.
    - No section of ROADMAP.md other than the Phase 12 entry is modified (`git diff` / `jj diff` shows only Phase 12 lines).
  </acceptance_criteria>
  <done>ROADMAP Phase 12 SC2 names `.githooks/pre-commit`; SC3 points at `jj-hooks.test.ts:167`; no `jj-colocated-hooks.test.ts` string remains; SC1/SC4/SC5 untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS HOOK-06 + HOOK-07 wording</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 48-53 — the HOOK section; and lines 126-127 — the HOOK-06/HOOK-07 Traceability rows that must stay unchanged)
    - .planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-CONTEXT.md (D-02 — the `.git/hooks/` out-of-scope invariant; D-03 — amendment intent; `<specifics>` — the husky/pre-commit-framework migration callout)
    - .planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-PATTERNS.md ("`.planning/REQUIREMENTS.md` — HOOK-06 + HOOK-07 wording edit" section — verbatim edit targets and checkbox-preservation pattern)
  </read_first>
  <action>
    Edit the two HOOK requirement bullets in `.planning/REQUIREMENTS.md`. Keep the `- [ ]
    **HOOK-06**:` and `- [ ] **HOOK-07**:` checkbox prefixes byte-identical (do not check the
    boxes, do not change REQ-ID casing). Do NOT modify the Traceability table rows
    `| HOOK-06 | Phase 12 | Pending |` and `| HOOK-07 | Phase 12 | Pending |` (REQUIREMENTS.md
    lines 126-127). Do NOT modify any other requirement bullet.

    HOOK-06 (currently REQUIREMENTS.md line 52): the bullet currently states the fix makes jj 0.41
    colocated `jj squash` "reliably fires `.git/hooks/pre-commit`". Replace the substring
    ``.git/hooks/pre-commit`` with ``.githooks/pre-commit``. Then append to the HOOK-06 bullet two
    additional sentences:
      (a) An explicit out-of-scope note for the `.git/hooks/` namespace per the D-02 invariant —
          state that firing git's `.git/hooks/<stage>` under jj-driven commits in colocated mode
          is explicitly out of scope; the jj adapter's fire surface is bound to `.githooks/<stage>`
          only.
      (b) A one-line migration callout pointing husky / pre-commit-framework users (anyone whose
          tooling installs hooks at `.git/hooks/`) at the `.githooks/` convention — they must
          migrate via symlink, framework reconfiguration (a custom `--hooks-path`), or manual copy
          to get hook execution under jj-driven colocated commits.
    Keep the existing HOOK-06 sentence about the fix living in `sdk/src/vcs/backends/jj.ts::commit`
    (not an adapter interface change) intact.

    HOOK-07 (currently REQUIREMENTS.md line 53): the bullet describes the regression test for
    HOOK-06. Apply the same `.git/hooks/` → `.githooks/` rewrite anywhere `.git/hooks/pre-commit`
    appears in the bullet, affirm the sentinel hook location is `.githooks/pre-commit`, and add a
    short clause confirming the regression test lives in the extended `jj-colocated` describe block
    at `sdk/src/vcs/__tests__/jj-hooks.test.ts:167` (no new test file). Keep the existing clauses
    about "fires exactly once", the `GSD_HOOK_SKIP_COLOCATED` opt-out, and the idempotency-audit
    requirement intact.
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done &amp;&amp; HOOKLINES=$(grep -nE '^\- \[ \] \*\*HOOK-0[67]\*\*' .planning/REQUIREMENTS.md | cut -d: -f1) &amp;&amp; for L in $HOOKLINES; do sed -n "${L}p" .planning/REQUIREMENTS.md | grep -q '.git/hooks/pre-commit' &amp;&amp; { echo "FAIL stale .git/hooks on line $L"; exit 1; }; done &amp;&amp; grep -q '\- \[ \] \*\*HOOK-06\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '\- \[ \] \*\*HOOK-07\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| HOOK-06 | Phase 12 | Pending |' .planning/REQUIREMENTS.md &amp;&amp; grep -q '| HOOK-07 | Phase 12 | Pending |' .planning/REQUIREMENTS.md &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Neither the HOOK-06 bullet line nor the HOOK-07 bullet line contains the substring `.git/hooks/pre-commit`.
    - Both bullets still begin with the unchecked checkbox prefix `- [ ] **HOOK-06**:` / `- [ ] **HOOK-07**:`.
    - The HOOK-06 bullet contains the literal `.githooks/pre-commit`, an explicit statement that `.git/hooks/` firing is out of scope, and a husky / pre-commit-framework migration callout mentioning `.githooks/`.
    - The HOOK-07 bullet contains the literal `.githooks/pre-commit` and a reference to `jj-hooks.test.ts:167`.
    - The Traceability rows `| HOOK-06 | Phase 12 | Pending |` and `| HOOK-07 | Phase 12 | Pending |` are present and unchanged.
    - No requirement bullet other than HOOK-06 and HOOK-07 is modified (`git diff` / `jj diff` shows only the HOOK-06 + HOOK-07 lines).
  </acceptance_criteria>
  <done>REQUIREMENTS HOOK-06 names `.githooks/pre-commit` with a `.git/hooks/` out-of-scope note and migration callout; HOOK-07 affirms `.githooks/pre-commit` and references the in-file test extension; checkboxes and Traceability rows untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits two `.planning/` markdown files. No code path, no untrusted input, no process boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | `.planning/ROADMAP.md` / `.planning/REQUIREMENTS.md` doc edits | accept | Documentation-only string replacements with `grep` verify gates. No executable surface; an incorrect edit is caught by the per-task `<verify>` automated checks. No security-relevant content. |

No package-manager installs in this plan — no supply-chain (`T-12-SC`) entry required. No high-severity threats exist for a documentation-only plan; this is stated explicitly per the security gate.
</threat_model>

<verification>
- `git diff` / `jj diff` on `.planning/ROADMAP.md` shows changes confined to the Phase 12 entry.
- `git diff` / `jj diff` on `.planning/REQUIREMENTS.md` shows changes confined to the HOOK-06 + HOOK-07 bullets.
- `grep -c 'jj-colocated-hooks.test.ts' .planning/ROADMAP.md` returns 0.
- No occurrence of `.git/hooks/pre-commit` remains on the Phase 12 SC2 line or the HOOK-06/HOOK-07 bullet lines.
</verification>

<success_criteria>
- ROADMAP Phase 12 SC2 names `.githooks/pre-commit`; SC3 points at the extended `jj-hooks.test.ts:167` block.
- REQUIREMENTS HOOK-06 and HOOK-07 name `.githooks/pre-commit`; HOOK-06 carries the `.git/hooks/` out-of-scope note and migration callout.
- Checkbox prefixes, SC numbering, and the Traceability table are preserved unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-01-SUMMARY.md` when done.
</output>
