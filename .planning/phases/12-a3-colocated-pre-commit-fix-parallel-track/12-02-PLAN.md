---
phase: 12-a3-colocated-pre-commit-fix-parallel-track
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - sdk/src/vcs/__tests__/jj-hooks.test.ts
autonomous: true
requirements: [HOOK-07]

must_haves:
  truths:
    - "D-01: this regression test proves the already-shipped Path 1 fix (the jj backend's commit() unconditionally fires .githooks/<stage>, with GSD_HOOK_SKIP_COLOCATED=1 as the opt-out) — on a colocated jj fixture vcs.commit() fires the .githooks/pre-commit hook exactly once, proven by a counter hook body, not merely by marker existence"
    - "D-04: the regression test extends the existing jj-colocated describe block at jj-hooks.test.ts:167 — no new test file, no new fixture, no new helper"
    - "The new test reuses the shared beforeAll fixture, the writeHook/safeUnlink module helpers, and the describe.skipIf(!jjAvailable) guard"
    - "The new test introduces no raw `git ` shell invocation"
  artifacts:
    - path: "sdk/src/vcs/__tests__/jj-hooks.test.ts"
      provides: "HOOK-07 fires-exactly-once regression coverage in the jj-colocated block"
      contains: "exactly once"
  key_links:
    - from: "new it() block in jj-hooks.test.ts"
      to: "vcs.commit() → fireHook() → .githooks/pre-commit"
      via: "counter hook body asserts a single fire per commit"
      pattern: "vcs\\.commit"
---

<objective>
Add a HOOK-07 regression test to `sdk/src/vcs/__tests__/jj-hooks.test.ts` that proves, on a
colocated jj fixture, the jj adapter's `commit()` fires `.githooks/pre-commit` EXACTLY ONCE per
commit — the SC2-verbatim "fires exactly once" guarantee. The existing sibling tests at lines
199-247 only assert the hook marker *exists* (a >=1-fire assertion); the net-new coverage here is
the exact-count assertion that catches a future double-fire regression.

Purpose: Path 1 (D-01) is already shipped at `sdk/src/vcs/backends/jj.ts:249-289`. HOOK-07 is the
regression guard that locks the shipped behavior so a future change re-introducing a double-fire
(or the retired D-10 colocated no-op) fails CI.

Output: One new `it()` block inside the existing `jj-colocated` describe block at
`jj-hooks.test.ts:167`. No new test file, no new fixture, no new helper (CONTEXT D-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-CONTEXT.md
@.planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-PATTERNS.md
@sdk/src/vcs/__tests__/jj-hooks.test.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from sdk/src/vcs/__tests__/jj-hooks.test.ts and sdk/src/vcs/hook-bridge.ts. No codebase exploration needed. -->

Existing jj-colocated describe block — sdk/src/vcs/__tests__/jj-hooks.test.ts:167
  describe('jj-colocated: pre-commit always fires from adapter (D-32 — D-10 retired)', () => { ... })
  - beforeAll (lines 170-194): creates `dir` via random-prefix mkdtempSync,
    runs `jj git init --colocate`, sets jj user.email/user.name, seeds a commit,
    builds `vcs = createVcsAdapter(dir, { kind: 'jj' }) as JjVcsAdapter`.
  - afterAll (lines 195-197): rmSync(dir).
  - Three existing sibling it() blocks:
    * line 199 — 'D-32: colocated mode always fires adapter-side .githooks/pre-commit'
      (asserts marker file EXISTS after vcs.commit — a >=1-fire assertion)
    * line 220 — 'GSD_HOOK_SKIP_COLOCATED=1 suppresses the fire in colocated mode'
      (sets process.env.GSD_HOOK_SKIP_COLOCATED='1' in try/finally, asserts marker ABSENT)
    * line 253 — 'A3 assumption: git .git/hooks/pre-commit fires automatically' (observational)

Module-level helpers (do NOT redefine — lines 43-58):
  function writeHook(dir: string, stage: string, body: string): string
  function safeUnlink(p: string): void

Suite-skip guard (already in place — lines 35-41, 60):
  describe.skipIf(!jjAvailable)(...)  // gates the whole suite on `jj --version`

Adapter commit contract — sdk/src/vcs/backends/jj.ts (CommitInput/CommitResult, unchanged):
  vcs.commit({ message: string, files: string[] }): { exitCode: number, ... }

fireHook surface — sdk/src/vcs/hook-bridge.ts:19
  function fireHook(cwd: string, stage: HookStage, ctx?: HookContext): ExecResult
  // shells `<cwd>/.githooks/<stage>` if it exists; no-ops (exitCode 0) if absent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add HOOK-07 fires-exactly-once regression test to the jj-colocated block</name>
  <files>sdk/src/vcs/__tests__/jj-hooks.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/jj-hooks.test.ts (lines 167-283 — the full jj-colocated describe block: the beforeAll fixture, the three existing sibling it() blocks, the afterAll; and lines 43-58 — the writeHook/safeUnlink module helpers)
    - .planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-CONTEXT.md (D-04 — in-file extension, no new file; `<code_context>` — vcs.commit precedent at :199-243)
    - .planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-PATTERNS.md ("`sdk/src/vcs/__tests__/jj-hooks.test.ts` — extend `jj-colocated` block" section — the body-shape templates and the net-new fires-exactly-once assertion guidance)
  </read_first>
  <behavior>
    - Test: vcs.commit() on the shared colocated fixture fires .githooks/pre-commit exactly once
      → a counter hook body that appends one line per fire leaves a marker file with exactly 1 line.
    - Edge: a second independent vcs.commit() (distinct staged file) again fires exactly once
      → after the second commit the freshly-reset counter marker again has exactly 1 line
      (proves "once per commit", not a cumulative count drift).
    - The assertion is on the line COUNT (=== 1), not on file existence — existence is already
      covered by the sibling test at line 199.
  </behavior>
  <action>
    Add ONE new `it()` block INSIDE the existing `describe('jj-colocated: pre-commit always fires
    from adapter (D-32 — D-10 retired)', …)` block at `jj-hooks.test.ts:167` — place it after the
    `GSD_HOOK_SKIP_COLOCATED` test (ends line 247) and before the observational A3 test (starts
    line 253), or after the A3 test; either position is acceptable as long as it is a sibling
    inside the same describe block. Do NOT create a new test file. Do NOT add a new `mkdtempSync`,
    a new `beforeAll`, or a new helper — reuse the block's shared `dir`/`vcs` and the module-level
    `writeHook`/`safeUnlink`.

    Name the test exactly: `'HOOK-07: colocated vcs.commit fires .githooks/pre-commit exactly once'`.

    Test body — use the counter-hook pattern (the "fires exactly once" verbatim proof from
    PATTERNS.md):
      1. Compute `markerPath = join(dir, '.hook07-fire-count')`. Call `safeUnlink(markerPath)` to
         clear any prior marker.
      2. Install the pre-commit hook via `writeHook(dir, 'pre-commit', body)` where `body` is a
         bash script that APPENDS one line per invocation — i.e. `echo` a constant token
         redirected with `>>` (append, not truncate) into `markerPath` — and exits 0. The hook
         shebang and exit-0 line follow the sibling tests' body shape (lines 202-206).
      3. Write a uniquely-named staged file (keep the `co-*.txt` naming convention so the shared
         `dir` does not collide with sibling tests — e.g. `co-hook07.txt`) via `writeFileSync`.
      4. Call `vcs.commit({ message: 'hook07 fire-count test', files: ['co-hook07.txt'] })` and
         assert `r.exitCode` is 0.
      5. Read `markerPath` with `readFileSync(markerPath, 'utf8')`, split on `\n`, filter out the
         trailing empty string from the final newline, and assert the resulting array length is
         exactly `1` — proving the hook fired exactly once for one commit.
      6. Edge assertion (second commit): call `safeUnlink(markerPath)` again, write a second
         distinct staged file (e.g. `co-hook07-b.txt`), call `vcs.commit` again with that file,
         assert `exitCode` 0, re-read the marker, and assert the line count is again exactly `1`.

    Use the `readFileSync` / `writeFileSync` / `existsSync` imports already present at the top of
    the file (the file already imports from `node:fs`). If `readFileSync` is not yet in the
    `node:fs` import group, add it to that existing import statement — do NOT add a new import
    line. Introduce NO raw `git ` shell invocation anywhere in the new block (the
    `project_no_raw_git` guard applies; the block must use only `vcs.commit`, `writeHook`,
    `safeUnlink`, and `node:fs` calls).
  </action>
  <verify>
    <automated>cd /Users/LoganDark/Documents/Projects/get-shit-done/sdk &amp;&amp; npx vitest run src/vcs/__tests__/jj-hooks.test.ts -t "HOOK-07"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/vcs/__tests__/jj-hooks.test.ts -t "HOOK-07"` reports 1 test passing (or the whole suite skipped if `jj --version` is unavailable on the runner — the new test inherits `describe.skipIf(!jjAvailable)`).
    - The new `it()` block is a sibling inside the `describe('jj-colocated: pre-commit always fires from adapter (D-32 — D-10 retired)', …)` block — `grep -c "describe('jj-colocated" sdk/src/vcs/__tests__/jj-hooks.test.ts` still returns 1 (no second jj-colocated describe block was created).
    - No new `mkdtempSync(` call was added to the file (`mkdtempSync` count unchanged from before the edit).
    - The new block contains an assertion comparing a line count to the literal `1` (the fires-exactly-once proof), not merely an `existsSync` check.
    - `grep -nE '(^|[^a-zA-Z.])git ' sdk/src/vcs/__tests__/jj-hooks.test.ts` shows no NEW raw-git invocation introduced by the new block (pre-existing `jj git init --colocate` is a `jj` subcommand and is allowed; the A3 observational test's pre-existing `.git/hooks` lines are not modified).
    - `sdk/src/vcs/backends/jj.ts` and `sdk/src/vcs/hook-bridge.ts` are NOT modified by this plan.
  </acceptance_criteria>
  <done>A `'HOOK-07: …'` it() block exists inside the jj-colocated describe block, asserts the pre-commit hook fired exactly once across two independent commits via a counter hook body, reuses the shared fixture/helpers, introduces no raw git, and passes (or skips) under vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → `.githooks/pre-commit` shell script | The regression test installs a hook script into an ephemeral `mkdtemp` fixture; `vcs.commit` → `fireHook` shells that script via `vcsExec`. The script body is authored by the test itself (a constant counter body), not by external input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Elevation of Privilege | `fireHook` shelling a `.githooks/pre-commit` script during the test | accept | The hook script is written by the test into a per-run `mkdtemp` directory with a randomized prefix; its body is a fixed counter script with no interpolated external input. `fireHook` already runs hooks with a 60s timeout (`hook-bridge.ts:38,41`). No untrusted data reaches the shell. Confined to test scope. |
| T-12-03 | Tampering | shared `dir` fixture reused across sibling `it()` blocks | mitigate | The new block uses a unique `co-hook07*.txt` staged-file stem and a dedicated `.hook07-fire-count` marker path, and calls `safeUnlink` before each commit, so it cannot collide with the sibling tests' `.colocated-*` markers or `co-a.txt`/`co-skip.txt` files. |

No package-manager installs in this plan — no supply-chain (`T-12-SC`) entry required. The phase touches only an ephemeral test fixture and authors its own hook body; no high-severity threats exist, stated explicitly per the security gate.
</threat_model>

<verification>
- `npx vitest run src/vcs/__tests__/jj-hooks.test.ts -t "HOOK-07"` passes (or skips when jj is unavailable).
- The full `jj-hooks.test.ts` suite still passes: `npx vitest run src/vcs/__tests__/jj-hooks.test.ts`.
- `git diff` / `jj diff` shows `sdk/src/vcs/__tests__/jj-hooks.test.ts` as the only modified file; `jj.ts` and `hook-bridge.ts` are untouched.
</verification>

<success_criteria>
- A HOOK-07 regression test inside the existing jj-colocated describe block asserts the pre-commit hook fires exactly once per commit.
- The test reuses the shared fixture, the `writeHook`/`safeUnlink` helpers, and the suite-skip guard; no new file, fixture, or helper.
- No raw `git ` invocation is introduced; no production source file is modified.
</success_criteria>

<output>
Create `.planning/phases/12-a3-colocated-pre-commit-fix-parallel-track/12-02-SUMMARY.md` when done.
</output>
