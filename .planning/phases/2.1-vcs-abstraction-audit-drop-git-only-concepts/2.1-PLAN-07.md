---
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 07
type: execute
wave: 1
depends_on:
  - 2.1-06
files_modified:
  - sdk/src/vcs/__tests__/git-backend.test.ts
  - sdk/src/vcs/__tests__/adapter-contract.test.ts
  - sdk/src/vcs/__tests__/index.test.ts
  - tests/vcs-adapter-contract.test.cjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Per D-07: vcs.hooks is gone from the public surface (plan 01 removed it from types.ts and backends/git.ts). This plan removes the remaining 7 test consumers."
    - "Per RESEARCH Open Question 1: this plan is PURELY cosmetic on the test side; NO internal fireHook invocations are added inside commit()/push() in 2.1. Phase 4 (HOOK-01..05) owns that wiring."
    - "Per D-22: no lint allowlist additions; no new raw-git invocations."
    - "Zero `vcs.hooks` references remain in repo source after this plan (excluding .planning/ docs and hook-bridge.ts's now-private fireHook)."
  artifacts:
    - path: sdk/src/vcs/__tests__/git-backend.test.ts
      provides: "2 hooks test sites reframed or removed"
    - path: sdk/src/vcs/__tests__/adapter-contract.test.ts
      provides: "2 hooks test sites reframed or removed"
  key_links: []
---

<objective>
Remove the remaining 7 `vcs.hooks` test references repo-wide. Per D-07 + RESEARCH Open Question 1, this is a PURELY COSMETIC removal — `fireHook` stays in `hook-bridge.ts` as a private helper (plan 01 already removed the `export`), and the internal invocation from `commit()` / `push()` is deferred to Phase 4.

Plan 01 removed `vcs.hooks` from `VcsAdapterCommon` and from the git backend's adapter freeze. This plan handles the test consumers that previously asserted the namespace's presence or invoked `vcs.hooks.fire(...)`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-06-SUMMARY.md

<interfaces>
<!-- Plan 01 deleted VcsHooks interface, deleted the hooks Object.freeze in git.ts,
     deleted fireHook import, and made hook-bridge.ts's fireHook private. The
     remaining vcs.hooks references are all in test files asserting either:
     (a) the namespace exists on the adapter shape, or
     (b) calling vcs.hooks.fire(...) to verify backend hook invocation. -->

Live grep at plan-time:
```
grep -rn "vcs\.hooks" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/"
```
Expected (per RESEARCH grep audit):
- sdk/src/vcs/__tests__/git-backend.test.ts: 2 (line 210-212 per CONTEXT.md)
- sdk/src/vcs/__tests__/adapter-contract.test.ts: 2 (line 74 per CONTEXT.md)
- sdk/src/vcs/__tests__/index.test.ts: 1
- tests/vcs-adapter-contract.test.cjs: 1
- sdk/src/vcs/hook-bridge.ts: 1 (the fireHook function itself — DOES NOT COUNT, already private)
Total test consumers: 6-7. (The hook-bridge.ts match is the function declaration; it's not a `vcs.hooks` consumer.)

Decision matrix per test site:
- If test asserts `expect(vcs.hooks).toBeDefined()` or similar shape check → DELETE the test (with rationale comment).
- If test calls `vcs.hooks.fire('pre-commit', ...)` to verify hook firing → DELETE the test entirely. The public surface no longer exposes this; Phase 4 will add internal invocation tests against `fireHook` directly (via module-internal access) or via observable side-effects (e.g., a hook script that touches a file). Phase 2.1 is purely cosmetic per Open Question 1.
- If a fireHook-related contract test was used in 2 places to test the same behavior → keep one and add a `// TODO: Phase 4 HOOK-01..05 — re-introduce as internal-invocation test once commit()/push() wire fireHook` comment.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove or reframe vcs.hooks test consumers + commit</name>
  <files>sdk/src/vcs/__tests__/git-backend.test.ts, sdk/src/vcs/__tests__/adapter-contract.test.ts, sdk/src/vcs/__tests__/index.test.ts, tests/vcs-adapter-contract.test.cjs</files>
  <read_first>
    - sdk/src/vcs/__tests__/git-backend.test.ts (focus lines 210-212 per CONTEXT.md; full file for `vcs.hooks` grep)
    - sdk/src/vcs/__tests__/adapter-contract.test.ts (focus line 74; full file)
    - sdk/src/vcs/__tests__/index.test.ts (1 site)
    - tests/vcs-adapter-contract.test.cjs (1 site)
    - sdk/src/vcs/hook-bridge.ts (verify fireHook is private per plan 01)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md (D-07, D-08, D-10)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md (Pitfall 5, Open Question 1)
  </read_first>
  <behavior>
    - Every `vcs.hooks` reference in the 4 test files is either DELETED (the test is no longer meaningful post-D-07) or REFRAMED (as an internal-invocation test deferred to Phase 4 with a TODO comment).
    - Default action: DELETE. Reframing requires a meaningful behavioral assertion that survives without the public namespace, which is unlikely in 2.1 because zero production callers exist.
    - For each deletion, add a comment block immediately above the deletion site (in the file's neighboring context) explaining:
      ```
      // 2.1 D-07 + RESEARCH Open Q1: vcs.hooks public surface removed.
      // The fireHook helper stays private in hook-bridge.ts; Phase 4 (HOOK-01..05)
      // wires the internal invocation from commit() / push(). Re-introduce hook-
      // firing observability tests there via side-effect assertion (e.g., a hook
      // script that touches a file).
      ```
    - Do NOT add `vcs.hooks` calls anywhere new. Do NOT re-export `fireHook` from hook-bridge.ts.
    - Do NOT delete `sdk/src/vcs/hook-bridge.ts` itself — the file stays with the private function for Phase 4 to consume.
    - Commit all 4 files in one commit on phase/2.1-audit.
  </behavior>
  <action>
    1. For each of the 4 test files:
       - `grep -n "vcs\.hooks" <file>` to find each site.
       - For each site, identify the enclosing describe block / it block / test function. Delete the entire test (the `it(...)` block or `test(...)` block) — not just the line. If the entire describe block is now empty after deletion, delete the describe block too.
       - Add the rationale comment near the deletion site (preserve the surrounding test context).
    2. Verify `grep -c "vcs\.hooks" sdk/src/vcs/hook-bridge.ts` returns 0 (the file only has `fireHook` private function, not a `vcs.hooks` consumer).
    3. Stage the 4 files; commit:
       ```
       git commit -m "$(cat <<'EOF'
       refactor(2.1-07): remove vcs.hooks public surface test consumers (D-07)

       Per D-07, vcs.hooks is gone from VcsAdapterCommon (plan 01 deleted the
       namespace from types.ts and the freeze block from backends/git.ts). This
       plan removes the 6 test consumers — all assertion/invocation sites that
       previously verified the public surface.

       fireHook stays in sdk/src/vcs/hook-bridge.ts as a private helper for
       Phase 4 (HOOK-01..05) to wire from inside commit() / push(). Per RESEARCH
       Open Question 1, internal-invocation rewiring is explicitly deferred to
       Phase 4 — this plan is purely cosmetic on the test side.

       Files:
       - sdk/src/vcs/__tests__/git-backend.test.ts: 2 sites
       - sdk/src/vcs/__tests__/adapter-contract.test.ts: 2 sites
       - sdk/src/vcs/__tests__/index.test.ts: 1 site
       - tests/vcs-adapter-contract.test.cjs: 1 site

       Per D-22, no allowlist additions; no new raw-git invocations.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
  </action>
  <verify>
    <automated>echo "--- repo-wide vcs.hooks (excluding .planning, hook-bridge.ts internal) ---" && grep -rn "vcs\.hooks" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | wc -l; echo "--- hook-bridge.ts fireHook still private ---" && grep -cE "^function fireHook" sdk/src/vcs/hook-bridge.ts; echo "--- commit ---" && git log --oneline -1; echo "--- vitest run (git-backend + adapter-contract) ---" && cd sdk && pnpm exec vitest run src/vcs/__tests__/git-backend.test.ts src/vcs/__tests__/adapter-contract.test.ts 2>&1 | grep -E "(PASS|FAIL|passed|failed)" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide `vcs\.hooks` grep returns 0 outside .planning/ and hook-bridge.ts.
    - `grep -cE "^function fireHook" sdk/src/vcs/hook-bridge.ts` returns 1 (private function preserved).
    - HEAD subject starts with `refactor(2.1-07): remove vcs.hooks public surface`.
    - Diff lists exactly 4 files.
    - git-backend.test.ts + adapter-contract.test.ts vitest exit 0 (deleted hook tests no longer fail; remaining tests still pass).
  </acceptance_criteria>
  <done>vcs.hooks public surface fully removed from tests; fireHook private function preserved for Phase 4.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1.07-01 | Information Disclosure | hook-firing regression on existing flows | accept | RESEARCH Pitfall 5 + Open Q1: zero production callers of vcs.hooks today; git's `git commit` natively fires `.git/hooks/pre-commit` already (unaffected by 2.1). No regression in git-flow hook firing. Phase 4 owns `.githooks/<stage>` orchestration. |
| T-2.1.07-02 | Tampering | future hooks misuse | mitigate | Public surface removed makes mis-call impossible; Phase 4 will land a strict internal wiring without re-exposing the namespace. |
| T-2.1.07-SC | Tampering | npm installs | mitigate | N/A. |
</threat_model>

<verification>
- Repo-wide vcs.hooks grep returns 0 outside .planning and hook-bridge.ts.
- fireHook private function preserved.
- One atomic commit with 4 files.
</verification>

<success_criteria>
- 6 test consumers removed; no internal invocation added (defers to Phase 4 per Open Q1).
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-07-SUMMARY.md`.
</output>
