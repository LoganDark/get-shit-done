---
status: complete
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
source:
  - 2.1-01-SUMMARY.md
  - 2.1-02-SUMMARY.md
  - 2.1-03-SUMMARY.md
  - 2.1-04-SUMMARY.md
  - 2.1-05-SUMMARY.md
  - 2.1-06-SUMMARY.md
  - 2.1-07-SUMMARY.md
  - 2.1-08-SUMMARY.md
  - 2.1-09-SUMMARY.md
started: 2026-05-12T19:30:00Z
updated: 2026-05-12T19:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. SDK tsc clean
expected: `cd sdk && pnpm exec tsc --noEmit` exits 0. All consumers of the reshaped VcsAdapter surface compile.
result: pass
reported: "exits 0 and no output"

### 2. dist-cjs build
expected: `pnpm -F sdk build:cjs` exits 0 and produces `sdk/dist-cjs/vcs/index.js` plus the `{type:commonjs}` package.json shim. `bin/lib/*.cjs` can `require()` the CJS bundle without ESM-interop errors.
result: pass
reported: "exits 0 and looks to work correctly - cat sdk/dist-cjs/package.json contains commonjs"

### 3. Lint guard clean (no raw git)
expected: `node scripts/lint-vcs-no-raw-git.cjs` exits 0 with output similar to "0 violations across 890 files". Allowlist (`scripts/lint-vcs-no-raw-git.allow.json`) has 14 file entries + 9 globs (down from 24 + 9 pre-phase per D-22; no entries added).
result: pass
reported: "ok lint-vcs-no-raw-git: 890 files scanned in /Users/LoganDark/Documents/Projects/get-shit-done, 0 violations"

### 4. Baseline-parity tests green
expected: `pnpm -F sdk exec vitest run src/vcs/__tests__/baseline-parity.test.ts` exits 0 with 50/50 passing. Five orphan baselines (stage/unstage dispatch removal) were deleted in plan 09; no orphan dispatch clauses, no orphan baselines.
result: pass
reported: "Test Files 1 passed (1); Tests 50 passed (50); 8.75s"

### 5. #3061 commit scope tests pass
expected: `pnpm -F sdk exec vitest run src/query/commit.test.ts` exits 0 with 21/21 passing. The 3 named-files scope (#3061) tests verify that pre-staged unrelated entries do not leak into a `commit --files` call — fixed in 7f4c8ffa via pre-add `git read-tree HEAD` in the git backend.
result: pass
reported: "Test Files 1 passed (1); Tests 21 passed (21); 3.29s — all three #3061 scope tests green plus the two #3061 input-validation/option-safety follow-ups"

### 6. Full repo test suite parity with main
expected: `pnpm test` from the repo root completes with **1283 pass / 15 fail** — exactly the baseline observed on `main @ 8af35e7c`. All 15 remaining failures are pre-existing (9× `worktree-safety-policy.test.cjs` deps.execGit mock pathway, 5× gpg-sign rescue tests, 1× rollup) — zero regressions introduced by Phase 2.1.
result: pass
reported: "node --test layer: 8623 pass / 12 fail. All 12 failures pre-existing categories: 3× bug-2838 gpg-sign env, 1× bug-3242 known-pending fix, 8× worktree-safety-policy.test.cjs deps.execGit mock pathway. Zero failures in 2.1-touched files. (User ran `pnpm test` which runs node --test, not vitest — the verifier's 1283/15 figure was the vitest layer; both views show pre-existing-only failures.)"

### 7. API surface spot-check — currentBookmarks
expected: In a Node REPL (or quick script) loading the SDK against this repo, `createVcsAdapter('.', { kind: 'git' }).refs.currentBookmarks()` returns a `string[]` (typically `['main']` if you're on main, or `[]` for detached HEAD / anonymous head). NOT a `string | null`. NOT a method named `currentBranch`.
result: pass
reported: "typeof: object isArray: true value: [ 'main' ] — user had merged phase/2.1-audit back into main before running the spot-check"

### 8. API surface spot-check — expr.rev
expected: `expr.rev('a1b2c3d4')` succeeds (hex SHA shape). `expr.rev('kxnvqropmlkz')` succeeds (jj change_id alphabet, k-z lowercase). `expr.rev('not-a-rev!')` throws "expr.rev: not a hex-SHA or change-id shaped string". There is no `expr.commit` function on the export. Note: `expr` is a top-level export from `sdk/src/vcs/index.ts`, not a property of the adapter — verified by the first attempt throwing `Cannot read properties of undefined (reading 'rev')` when accessed via `vcs.expr.rev`.
result: pass
reported: "hex SHA: rev:a1b2c3d4 / jj change_id: rev:kxnvqropmlkz / expr.commit exists? undefined / rejected garbage: expr.rev: not a hex-SHA or change-id shaped string: 'not-a-rev!'"

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
