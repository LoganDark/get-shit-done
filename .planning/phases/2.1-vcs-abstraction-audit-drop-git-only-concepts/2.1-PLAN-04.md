---
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 04
type: execute
wave: 1
depends_on:
  - 2.1-03
files_modified:
  - sdk/src/query/commit.ts
  - sdk/src/query/commit.test.ts
  - get-shit-done/bin/lib/commands.cjs
  - tests/commands.test.cjs
  - tests/commit-files-deletion.test.cjs
  - sdk/src/vcs/__tests__/baseline-parity.test.ts
  - tests/__tools__/capture-vcs-baselines.cjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Per D-02: every CommitInput consumer uses `files` only; `pathspec` is gone repo-wide (excluding .planning/ history docs)."
    - "Per D-04: the git backend's commit() captures WC state via `git add -A -- <paths>` (landed in plan 01); callers pass post-WC-modification path lists in `files` instead of upstream-staging-then-pathspec."
    - "Per D-06: cmdCommit's #2014 pre-probe migrates from `vcs.diff({staged:true,nameOnly:true})` to `vcs.status({porcelain:true})` filtering — invariant preserved that delete-missing-file does not produce a deletion commit."
    - "Per D-08: noVerify is the only hook-skip knob on CommitInput; no new fields, no old ones survive."
    - "commit.test.ts: the pre-existing #2014 invariant test (and its sibling cases) re-pass against the WC-state-capture flow."
  artifacts:
    - path: sdk/src/query/commit.ts
      provides: "pathspec field consumers reshape onto files + caller-side vcs.status pre-probe per D-06"
    - path: get-shit-done/bin/lib/commands.cjs
      provides: "cmdCommit #2014 invariant migrates to vcs.status() pre-probe; pathspec call sites collapse to files"
    - path: sdk/src/query/commit.test.ts
      provides: "Test setup reshaped onto vcs.commit({files}) WC-state-capture flow"
    - path: tests/commit-files-deletion.test.cjs
      provides: "Re-asserts #2014 invariant against the new WC-state-capture semantics (D-06)"
  key_links:
    - from: get-shit-done/bin/lib/commands.cjs
      to: sdk/src/vcs/backends/git.ts
      via: "vcs.status({porcelain:true}) → vcs.commit({files,...})"
      pattern: "vcs\\.status\\(\\{\\s*porcelain"
    - from: sdk/src/query/commit.ts
      to: sdk/src/vcs/backends/git.ts
      via: "vcs.commit({files, message, ...}) with WC-state-capture semantics"
      pattern: "vcs\\.commit\\(\\{"
---

<objective>
Reshape every `CommitInput.pathspec` consumer onto `CommitInput.files` with WC-state-capture semantics per D-02 + D-04. Migrate the #2014 caller-side invariant from `vcs.diff({staged:true,nameOnly:true})` to `vcs.status({porcelain:true})` filtering per D-06. Drop `pathspec` from the type surface (plan 01 already did) is consumed here.

This is the largest single-concern plan in 2.1 because the semantic shift (stage-then-commit-pathspec → commit-WC-state-files) requires per-caller verification per RESEARCH Pitfall 2 + Assumption A5. The primary high-risk callers are commit.ts (lines 124-156 and 285-301) and commands.cjs's cmdCommit block.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-03-SUMMARY.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-09-PLAN.md

<interfaces>
<!-- Plan 01 (shape commit) removed CommitInput.pathspec entirely and changed
     CommitInput.files semantics from "stage these paths" to "capture WC state of
     these paths". The git backend's commit() body now runs `git add -A -- <files>`
     instead of `git add -- <files>`, then `git commit -m <msg>` (no -a).
     PushOpts.noVerify was added; CommitInput.noVerify was preserved. -->

Live grep at plan-time (executor MUST re-run):
```
grep -rnE "pathspec\s*[:=]" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/"
```
Expected:
- sdk/src/query/commit.ts: 2 sites (lines ~140-155 and ~289-301)
- sdk/src/query/commit.test.ts: 3 sites
- get-shit-done/bin/lib/commands.cjs: 2 sites (cmdCommit block)
- tests/commands.test.cjs: 3 sites
- tests/commit-files-deletion.test.cjs: 1 site
- sdk/src/vcs/__tests__/baseline-parity.test.ts: 4 sites (dispatch clauses)
Total: 15 sites across 7 files.

Caller analog 1 — pathspec → files collapse (PATTERNS.md Category 5 lines 482-525):
```typescript
// BEFORE (sdk/src/query/commit.ts):
const pathsToCommit = filePaths.length > 0 ? filePaths : ['.planning/'];
for (const file of pathsToCommit) {
  const addResult = vcs.stage([file]);
  if (addResult.exitCode !== 0) { return { data: { committed: false, ... } }; }
}
const diffResult = vcs.diff({ staged: true, nameOnly: true, paths: pathsToCommit });
const stagedFiles = diffResult.nameOnly;
if (stagedFiles.length === 0) {
  return { data: { committed: false, reason: 'nothing staged' } };
}
const commitResult = vcs.commit({
  message: sanitized ?? '',
  amend: hasAmend,
  noVerify: hasNoVerify,
  pathspec: pathsToCommit,
});

// AFTER (Phase 2.1, D-02 + D-06):
const pathsToCommit = filePaths.length > 0 ? filePaths : ['.planning/'];

// D-06: caller-side #2014 pre-probe via vcs.status (not staged-diff).
const status = vcs.status({ porcelain: true });
const surviving = status.entries.filter(e => pathsToCommit.some(p => e.path.startsWith(p)));
if (surviving.length === 0) {
  return { data: { committed: false, reason: 'nothing to commit' } };
}

const commitResult = vcs.commit({
  message: sanitized ?? '',
  amend: hasAmend,
  noVerify: hasNoVerify,
  files: pathsToCommit,   // WC-state-capture per D-02 + D-04.
});
```

Caller analog 2 — cmdCommit #2014 invariant (commands.cjs):
```javascript
// BEFORE (Phase 2 plan 02-09):
const diffResult = vcs.diff({ staged: true, nameOnly: true, paths: pathsToCommit });
if (diffResult.nameOnly.length === 0) {
  return { committed: false, reason: 'nothing staged' };
}

// AFTER (Phase 2.1, D-06):
const status = vcs.status({ porcelain: true });
const surviving = status.entries.filter(e => pathsToCommit.some(p => e.path.startsWith(p)));
if (surviving.length === 0) {
  return { committed: false, reason: 'nothing to commit' };
}
```

Test fixture analog — stage+commit({pathspec}) → commit({files}) (PATTERNS.md Pattern E):
```javascript
// BEFORE:
vcs.stage(['foo.txt']);
vcs.commit({ pathspec: ['foo.txt'], message: 'add foo' });

// AFTER (Phase 2.1, D-02 — file is already in WC; vcs.commit captures WC state):
vcs.commit({ files: ['foo.txt'], message: 'add foo' });

// For deletes (D-04):
fs.unlinkSync(path.join(cwd, 'old.txt'));
vcs.commit({ files: ['old.txt'], message: 'remove old.txt' });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reshape sdk/src/query/commit.ts pathspec consumers onto files + caller-side pre-probe</name>
  <files>sdk/src/query/commit.ts</files>
  <read_first>
    - sdk/src/query/commit.ts (focus lines 124-156 and 285-301 per PATTERNS.md; full file for context)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Category 5 lines 482-525)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md (Pitfall 2, Assumption A5, Open Question 4)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md (D-02, D-04, D-06)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-08-PLAN.md (Phase 2's commit.ts migration adding pathspec — we are now removing it)
  </read_first>
  <behavior>
    - Both pathspec sites in commit.ts (lines ~140-155 and ~289-301) collapse onto `files: pathsToCommit`.
    - Upstream `vcs.stage([file])` loops are REMOVED — WC-state-capture obviates the explicit stage.
    - The "nothing staged" pre-probe via `vcs.diff({staged:true,nameOnly:true,paths})` is REPLACED with `vcs.status({porcelain:true})` + path-prefix filter per D-06. Return shape preserved (`{ committed: false, reason: 'nothing to commit' }` — note the wording shift from 'nothing staged' to 'nothing to commit' per the new semantic).
    - All other commit.ts logic (amend handling, noVerify pass-through, message sanitization, error pathways) UNCHANGED.
  </behavior>
  <action>
    Open sdk/src/query/commit.ts. For each of the 2 pathspec sites (find via `grep -n "pathspec" sdk/src/query/commit.ts`):
    1. Replace the upstream `vcs.stage([file])` for-loop with NOTHING (deleted; WC already in expected state).
    2. Replace the `vcs.diff({staged:true,...})` pre-probe with the `vcs.status({porcelain:true})` filter per `<interfaces>` analog. Use `pathsToCommit.some(p => e.path.startsWith(p))` filter (or per-call-site equivalent matching the existing path-set var).
    3. Replace `pathspec: pathsToCommit` with `files: pathsToCommit` in the `vcs.commit({...})` call.
    4. Update the `reason` string in the early-return case from `'nothing staged'` to `'nothing to commit'`.

    Verify no `pathspec` references remain in commit.ts.
  </action>
  <verify>
    <automated>grep -c pathspec sdk/src/query/commit.ts; grep -c "vcs\.stage(" sdk/src/query/commit.ts; grep -c "vcs\.status({\s*porcelain" sdk/src/query/commit.ts; grep -c "files:\s*pathsToCommit" sdk/src/query/commit.ts; cd sdk && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep "src/query/commit.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c pathspec sdk/src/query/commit.ts` returns 0.
    - `grep -c "vcs\.stage(" sdk/src/query/commit.ts` returns 0.
    - `grep -c "vcs\.status({" sdk/src/query/commit.ts` returns ≥1 (the new pre-probe pattern, with or without porcelain spacing).
    - `grep -c "files:\s*pathsToCommit" sdk/src/query/commit.ts` returns ≥1.
    - `cd sdk && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep "src/query/commit.ts"` returns 0 (commit.ts type-checks against the reshaped types.ts).
  </acceptance_criteria>
  <done>commit.ts is pathspec-free, stage-free, uses status-based pre-probe, and type-checks cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Reshape get-shit-done/bin/lib/commands.cjs cmdCommit pathspec + #2014 invariant</name>
  <files>get-shit-done/bin/lib/commands.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/commands.cjs (focus cmdCommit block ~line 377 per STATE.md "[Phase 02-09]: #2014 invariant safeguard via stagedOrUnstaged tracking")
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-09-PLAN.md (#2014 invariant context)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Pattern B caller-side pre-probe lines 753-774)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md (Pitfall 2, Open Question 4)
  </read_first>
  <behavior>
    - cmdCommit's `stagedOrUnstaged` short-circuit (Phase 2 plan 02-09) migrates from `vcs.diff({staged:true,...})` to `vcs.status({porcelain:true})` filtering per D-06. The invariant must hold: when an explicit --files list has all-missing entries, the function short-circuits to `nothing_to_commit` BEFORE invoking vcs.commit.
    - The 2 pathspec sites in commands.cjs replace `pathspec: ...` with `files: ...` in the vcs.commit call.
    - Any upstream `vcs.stage()` calls in cmdCommit's path are DELETED — WC-state-capture obviates them.
    - The 1 vcs.stage call in commands.cjs is queued for plan 08 (the dedicated stage/unstage hard-remove sweep), NOT removed here. Verify via grep that exactly 0 pathspec, 1 vcs.stage remain after this task (the lingering stage is in some non-cmdCommit codepath if any).
    - WAIT — re-check: cmdCommit IS the primary site per D-06. Inspect at plan-time. If cmdCommit's vcs.stage is the ONLY stage call in commands.cjs, then removing it as part of the pathspec collapse is the natural mechanical edit and we do not need to wait for plan 08. Plan 08 then becomes a cleanup of the OTHER files only.

    Decision rule: If `grep -nE "vcs\.stage\(" get-shit-done/bin/lib/commands.cjs` shows the stage call sits inside cmdCommit's body upstream of the vcs.commit that gets the pathspec migrated, REMOVE it as part of this task. If it sits in an unrelated function, LEAVE it for plan 08.
  </behavior>
  <action>
    1. Run `grep -nE "pathspec|vcs\.stage\(|vcs\.unstage\(|vcs\.diff\(\{\s*staged" get-shit-done/bin/lib/commands.cjs` to enumerate every relevant site.
    2. For each pathspec site: apply the `<interfaces>` caller analog 2 — replace the `vcs.diff` pre-probe with `vcs.status` filter; replace `pathspec: ...` with `files: ...`.
    3. For any cmdCommit-upstream `vcs.stage([...])` calls: delete them. The new `vcs.commit({files})` captures WC state directly.
    4. Update the `reason` string `'nothing staged'` → `'nothing to commit'`.
    5. If any vcs.stage / vcs.unstage calls remain in commands.cjs OUTSIDE of cmdCommit (e.g., other commands like cmdAdd), leave them — plan 08 handles them. Note their existence in SUMMARY.
  </action>
  <verify>
    <automated>grep -c pathspec get-shit-done/bin/lib/commands.cjs; echo "vcs.stage count:" && grep -cE "vcs\.stage\(" get-shit-done/bin/lib/commands.cjs; echo "vcs.unstage count:" && grep -cE "vcs\.unstage\(" get-shit-done/bin/lib/commands.cjs; echo "vcs.status porcelain pre-probe:" && grep -cE "vcs\.status\(\{\s*porcelain" get-shit-done/bin/lib/commands.cjs; echo "nothing to commit:" && grep -c "nothing to commit" get-shit-done/bin/lib/commands.cjs; echo "nothing staged remaining:" && grep -c "nothing staged" get-shit-done/bin/lib/commands.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c pathspec get-shit-done/bin/lib/commands.cjs` returns 0.
    - `grep -cE "vcs\.status\(" get-shit-done/bin/lib/commands.cjs` returns ≥1 (pre-probe lands).
    - `grep -c "nothing to commit" get-shit-done/bin/lib/commands.cjs` returns ≥1.
    - `grep -c "nothing staged" get-shit-done/bin/lib/commands.cjs` returns 0 (old reason string gone).
    - Any remaining `vcs.stage(` / `vcs.unstage(` counts are documented in SUMMARY for plan 08's audit.
  </acceptance_criteria>
  <done>cmdCommit pathspec consumers collapsed onto files; #2014 invariant preserved via vcs.status pre-probe; reason wording updated.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Reshape paired tests (commit.test.ts, commands.test.cjs, commit-files-deletion.test.cjs, baseline-parity.test.ts, capture-vcs-baselines.cjs)</name>
  <files>sdk/src/query/commit.test.ts, tests/commands.test.cjs, tests/commit-files-deletion.test.cjs, sdk/src/vcs/__tests__/baseline-parity.test.ts, tests/__tools__/capture-vcs-baselines.cjs</files>
  <read_first>
    - sdk/src/query/commit.test.ts (full file — 3 pathspec test setups)
    - tests/commands.test.cjs (full file — 3 pathspec test setups; #2014 cases)
    - tests/commit-files-deletion.test.cjs (full file — single pathspec site; primary #2014 re-assertion target per D-06)
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (4 dispatch sites for pathspec args-shape)
    - tests/__tools__/capture-vcs-baselines.cjs (commit-with-pathspec baseline-capture invocations)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Pattern E test reshape)
  </read_first>
  <behavior>
    - Every test that calls `vcs.commit({ pathspec: [...] })` reshapes to `vcs.commit({ files: [...] })` (per Pattern E).
    - Any `vcs.stage([...])` invocation immediately upstream of a `vcs.commit({pathspec: same paths})` collapses — the explicit stage is dropped; the file is left in the WC and vcs.commit({files}) captures the state.
    - commit-files-deletion.test.cjs's #2014 invariant test asserts: when a file is listed in `files` but the file was deleted in the WC, the commit RECORDS the deletion (D-04 WC-state-capture) UNLESS the caller's pre-probe (via vcs.status) short-circuits first. Re-read the test, identify which exact behavior it pins, and update the assertion to match the post-D-02/D-04/D-06 contract: the caller-side pre-probe is in cmdCommit (commands.cjs), not in the test itself, so commit-files-deletion.test.cjs may need to call cmdCommit (or its delegate) and assert the higher-level return value.
    - baseline-parity.test.ts dispatch: the `args[0] === 'commit'` clauses that previously had a `--` separator with pathspec args now have a clean `git commit -m ...` form (no trailing pathspec). The `git add -- <paths>` invocations (baseline-captured) now become `git add -A -- <paths>` per D-04 — re-bless those baselines OR confirm via PATTERNS.md that the dispatch test re-issues the canonical command to compute expected output dynamically (in which case the dispatch logic auto-adapts). Plan 09 does the final baseline re-bless; Task 3 here only updates the dispatch CALLS that construct the args to invoke (e.g., the test setup that drives the baseline capture).
    - capture-vcs-baselines.cjs: same shape — invocations that capture `vcs.commit({pathspec: ...})` baselines reshape to capture `vcs.commit({files: ...})`. If a baseline JSON file becomes orphaned (no longer dispatched), plan 09 deletes it.
  </behavior>
  <action>
    For each of the 5 files, run `grep -n "pathspec\|vcs\.stage(" <file>` and apply Pattern E mechanical reshape:
    1. Replace `vcs.commit({ ..., pathspec: paths, ... })` with `vcs.commit({ ..., files: paths, ... })`.
    2. Delete the upstream `vcs.stage(paths)` calls (file already in WC; vcs.commit({files}) captures state).
    3. For deletion test cases (commit-files-deletion.test.cjs), preserve the `fs.unlinkSync` (or equivalent) that creates the deleted-file precondition; the new contract is "file deleted in WC + files: [path] → commit records the deletion."
    4. baseline-parity.test.ts: rename pathspec dispatch clauses inside `args[0] === 'commit'` matchers. The CommitInput.files shape replaces pathspec; the dispatch's adapter-side call signature changes. Plan 09 sweeps the actual baseline JSON re-bless.
    5. capture-vcs-baselines.cjs: rename pathspec to files in the capture-time CommitInput literals.

    Do NOT delete baseline JSON files in this task — plan 09 owns baseline housekeeping.
  </action>
  <verify>
    <automated>echo "--- remaining pathspec references (excluding .planning) ---" && grep -rn "pathspec" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | wc -l; echo "--- files: replacement in tests ---" && grep -c "files:" sdk/src/query/commit.test.ts tests/commands.test.cjs tests/commit-files-deletion.test.cjs 2>/dev/null; echo "--- commit.test.ts vitest ---" && cd sdk && pnpm exec vitest run src/query/commit.test.ts 2>&1 | grep -E "(PASS|FAIL|passed|failed)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide `grep -rn "pathspec" --include="*.ts" --include="*.cjs" | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/"` returns 0.
    - commit.test.ts, commands.test.cjs, commit-files-deletion.test.cjs each contain ≥1 `files:` literal inside a vcs.commit({...}) call.
    - `cd sdk && pnpm exec vitest run src/query/commit.test.ts` exits 0 OR documents specific failures in SUMMARY (e.g., baseline drift that plan 09 will re-bless).
    - commit-files-deletion.test.cjs explicit #2014 case passes (or documented as needing plan 09 baseline re-bless if a baseline-driven test).
  </acceptance_criteria>
  <done>All paired test pathspec sites reshaped onto files; vcs.stage upstream calls in test setup collapsed; baseline JSON re-bless deferred to plan 09.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit CommitInput reshape</name>
  <files>(none — git only)</files>
  <read_first>
    - Tasks 1-3 verification output
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md (D-02, D-04, D-06, D-08, D-21)
  </read_first>
  <behavior>
    - All 7 files from Tasks 1-3 land in one atomic commit on phase/2.1-audit.
  </behavior>
  <action>
    Stage exactly the 7 files; verify diff list. Commit:
    ```
    git commit -m "$(cat <<'EOF'
    refactor(2.1-04): collapse CommitInput.pathspec onto files (WC-state-capture)

    Per D-02 + D-04, CommitInput.pathspec is removed and CommitInput.files semantics
    shift from "stage these paths" to "capture WC state of these paths" (mix of
    adds/mods/dels). Plan 01's shape commit already changed the git backend's
    commit() body to `git add -A -- <paths>`; this plan reshapes all 15 caller
    sites onto the new shape.

    Per D-06, cmdCommit's #2014 invariant (Phase 2 plan 02-09) migrates from
    `vcs.diff({staged:true,nameOnly:true})` to `vcs.status({porcelain:true})`
    + path-prefix filter. The pre-probe stays in the caller, not the adapter.

    Files:
    - sdk/src/query/commit.ts: 2 pathspec sites + vcs.stage upstream loop removed
      + vcs.status pre-probe added per D-06.
    - get-shit-done/bin/lib/commands.cjs: 2 pathspec sites + cmdCommit #2014
      invariant migrated to vcs.status pre-probe.
    - sdk/src/query/commit.test.ts (3 sites), tests/commands.test.cjs (3 sites),
      tests/commit-files-deletion.test.cjs (1 site): pathspec → files; vcs.stage
      upstream collapse per Pattern E.
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (4 dispatch sites): dispatch
      clauses renamed; baseline JSON re-bless deferred to plan 09.
    - tests/__tools__/capture-vcs-baselines.cjs: capture-time CommitInput literals
      renamed pathspec → files.

    The reason string 'nothing staged' is replaced with 'nothing to commit'
    everywhere it appeared, reflecting the WC-state-capture semantic.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>git branch --show-current; git log --oneline -1; git diff HEAD~1 HEAD --name-only | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Branch is `phase/2.1-audit`.
    - HEAD subject starts with `refactor(2.1-04): collapse CommitInput.pathspec`.
    - 7 files in diff.
  </acceptance_criteria>
  <done>CommitInput reshape committed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1.04-01 | Tampering | files: paths from caller | mitigate | git.ts commit() preserves `--` separator (CR-01); argv-array invocation only. |
| T-2.1.04-02 | Information Disclosure | #2014 invariant regression | mitigate | commit-files-deletion.test.cjs re-asserts the invariant against the new vcs.status pre-probe per D-06. Task 3's vitest run is the gate. |
| T-2.1.04-03 | Tampering | unintended `git add -A` scope | mitigate | The `--` separator between `add -A` and `<paths>` prevents argv injection; paths come from caller's pre-filtered list. |
| T-2.1.04-SC | Tampering | npm installs | mitigate | N/A. |
</threat_model>

<verification>
- Repo-wide pathspec grep returns 0.
- commit.test.ts vitest exits 0 (or documents baseline-related failures for plan 09).
- One atomic commit with 7 files.
</verification>

<success_criteria>
- pathspec field gone from all code (excluding .planning/ history).
- #2014 invariant preserved via caller-side vcs.status pre-probe (D-06).
- 'nothing to commit' replaces 'nothing staged' everywhere.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-04-SUMMARY.md`.
</output>
