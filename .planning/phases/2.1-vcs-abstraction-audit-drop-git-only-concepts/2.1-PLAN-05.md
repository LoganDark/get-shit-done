---
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 05
type: execute
wave: 1
depends_on:
  - 2.1-04
files_modified:
  - sdk/src/vcs/__tests__/git-backend.test.ts
  - sdk/src/vcs/__tests__/adapter-contract.test.ts
  - sdk/src/vcs/__tests__/baseline-parity.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Per D-16: StatusEntry has no `index` field anywhere — zero `entry.index` reads in repo source (excluding .planning/ docs)."
    - "Per RESEARCH Pitfall 7: no production caller reads `entry.index`; only test internals reference it, and those are reframed or removed in this plan."
    - "git-backend.test.ts entries-shape assertion uses `{ path, worktree }` only (no index property in object-equality assertions)."
  artifacts:
    - path: sdk/src/vcs/__tests__/git-backend.test.ts
      provides: "Test assertions for StatusEntry shape exclude the `index` field."
    - path: sdk/src/vcs/__tests__/adapter-contract.test.ts
      provides: "Contract test for StatusEntry shape excludes `index`."
  key_links:
    - from: sdk/src/vcs/__tests__/git-backend.test.ts
      to: sdk/src/vcs/types.ts
      via: "StatusEntry contract"
      pattern: "entries\\["
---

<objective>
Drop the remaining references to `StatusEntry.index` in test files. Plan 01 already removed the field from `types.ts` and stopped surfacing it from `git.ts`. This plan cleans up the test assertions and dispatch that previously asserted the field's presence.

Per RESEARCH Pitfall 7: zero production reads of `entry.index` exist (verified by grep at research time and at plan-04 time). This is a trivial 3-file cleanup of test internals.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-04-SUMMARY.md

<interfaces>
<!-- Plan 01 removed `index: string` from StatusEntry in types.ts; backends/git.ts's
     status() body no longer pushes `index` into the entry literal (the local `index`
     var is still used for rename/copy detection but not surfaced). Test files that
     asserted the field's presence (typically in toEqual / toMatchObject calls)
     must update their assertions. -->

Live grep at plan-time (executor MUST re-run):
```
grep -rnE "\.index[^a-zA-Z_]" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | grep -v "Array.prototype" | grep -v "match\.index"
```
Then filter for StatusEntry-shaped contexts (entries[N].index, entry.index inside status() consumer patterns). RESEARCH Pitfall 7 says: zero production reads. Test consumers likely live in:
- sdk/src/vcs/__tests__/git-backend.test.ts (status-related test blocks)
- sdk/src/vcs/__tests__/adapter-contract.test.ts (contract test for status())
- sdk/src/vcs/__tests__/baseline-parity.test.ts (status dispatch clause)

If grep returns NO StatusEntry-related `.index` matches outside the above three files, the cleanup scope is exactly these three files. If grep returns ANY other file, escalate (planner did not anticipate the site).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit and update StatusEntry.index references in test files</name>
  <files>sdk/src/vcs/__tests__/git-backend.test.ts, sdk/src/vcs/__tests__/adapter-contract.test.ts, sdk/src/vcs/__tests__/baseline-parity.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/git-backend.test.ts (search for `.index` and `entries` in proximity)
    - sdk/src/vcs/__tests__/adapter-contract.test.ts (search for `status()` consumer assertions)
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (search for `args[0] === 'status'` dispatch clauses)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Category 6 lines 532-589)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md (Pitfall 7)
  </read_first>
  <behavior>
    - Every test assertion of the form `expect(entry.index).toBe(...)` or `expect(entries).toEqual([{ path, index, worktree }, ...])` updates: either drop the `.index` assertion (if the test was specifically pinning the field's value) or drop `index:` from the object-literal in toEqual.
    - baseline-parity.test.ts: if a `status` dispatch clause used `e.index` to compare against the baseline's char-position-1 token, the dispatch now reads `e.worktree` only and the baseline JSON is updated by plan 09. For this task, just remove the `.index` reference from the dispatch's adapter-side mapping.
    - If a test was ONLY about the index field (e.g., a dedicated "status reports staged state" test that has no remaining purpose post-D-03), DELETE the test entirely with a comment `// removed per Phase 2.1 D-03/D-16: vcs.stage and StatusEntry.index hard-removed; staging is not a cross-backend concept`.
    - The local-var `index` in git.ts's status() body stays (rename/copy heuristic uses it internally per plan 01). Tests of the rename/copy detection itself continue to work because the `worktree` and `path` fields still surface.
  </behavior>
  <action>
    For each of the 3 test files:
    1. `grep -nE "\.index" <file>` to enumerate candidate sites; filter manually for StatusEntry-shaped contexts (skip `match.index`, array-index reads, etc.).
    2. For each shape-related site, decide between (a) dropping `.index` from the assertion / object literal, or (b) deleting the entire test if its sole purpose was the index field.
    3. For baseline-parity.test.ts status dispatch (search `args[0] === 'status'`), drop any `index` field references from the entry-construction map in the dispatch body.
    4. Add inline rationale comments at each non-trivial change site: `// 2.1 D-16: StatusEntry.index removed; staging is not cross-backend (see D-03).`

    Do NOT touch sdk/src/vcs/backends/git.ts's local `index` var — that stays for rename/copy detection per plan 01.
  </action>
  <verify>
    <automated>echo "--- StatusEntry-shaped .index references (excluding match.index, array indices) ---" && grep -rnE "\.index" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | grep -vE "match\.index|results\[[0-9]+\]\.index|matches\[[0-9]+\]\.index|\.indexOf\(" | grep -E "entries|entry|StatusEntry|porcelain" | wc -l; echo "--- git-backend.test.ts vitest ---" && cd sdk && pnpm exec vitest run src/vcs/__tests__/git-backend.test.ts 2>&1 | grep -E "(PASS|FAIL|passed|failed)" | tail -3; echo "--- adapter-contract.test.ts vitest ---" && cd sdk && pnpm exec vitest run src/vcs/__tests__/adapter-contract.test.ts 2>&1 | grep -E "(PASS|FAIL|passed|failed)" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - StatusEntry-shaped `.index` reference count returns 0 across the test corpus.
    - `cd sdk && pnpm exec vitest run src/vcs/__tests__/git-backend.test.ts` exits 0 (or documents baseline-driven failures for plan 09).
    - `cd sdk && pnpm exec vitest run src/vcs/__tests__/adapter-contract.test.ts` exits 0 (or documents baseline-driven failures).
    - Comments referencing "D-16" / "D-03" rationale appear at each non-trivial change site.
  </acceptance_criteria>
  <done>Test assertions and dispatch updated to the post-D-16 StatusEntry shape.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Commit StatusEntry.index drop</name>
  <files>(none — git only)</files>
  <read_first>
    - Task 1 verification output
  </read_first>
  <behavior>
    - All 3 files from Task 1 land in one atomic commit on phase/2.1-audit.
  </behavior>
  <action>
    Stage the 3 files; verify diff list. Commit:
    ```
    git commit -m "$(cat <<'EOF'
    refactor(2.1-05): drop StatusEntry.index from test assertions

    Per D-16, StatusEntry's `index` field was removed from types.ts and from
    backends/git.ts's entries-construction in plan 01. This plan cleans up the
    only remaining consumers — test assertions in:
    - sdk/src/vcs/__tests__/git-backend.test.ts
    - sdk/src/vcs/__tests__/adapter-contract.test.ts
    - sdk/src/vcs/__tests__/baseline-parity.test.ts

    Zero production reads of `entry.index` exist per RESEARCH Pitfall 7; this is
    a 3-file test-internal cleanup. The local `index` var in git.ts's status()
    body remains (rename/copy heuristic uses it internally; it's just not surfaced).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>git branch --show-current; git log --oneline -1; git diff HEAD~1 HEAD --name-only | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Branch is `phase/2.1-audit`.
    - HEAD subject starts with `refactor(2.1-05): drop StatusEntry.index`.
    - Up to 3 files in diff (could be fewer if a file had no surfaced changes).
  </acceptance_criteria>
  <done>StatusEntry.index drop committed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1.05-01 | Information Disclosure | rename/copy detection regression | mitigate | Local `index` var in git.ts:233 stays per plan 01; the field is just not surfaced in the public entry literal. PATTERNS.md Category 6 confirms. |
| T-2.1.05-SC | Tampering | npm installs | mitigate | N/A. |
</threat_model>

<verification>
- Test grep confirms zero `entry.index` references in StatusEntry contexts.
- git-backend.test.ts + adapter-contract.test.ts vitest exit 0.
</verification>

<success_criteria>
- StatusEntry-shaped `.index` references gone from tests; rename/copy detection preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-05-SUMMARY.md`.
</output>
