---
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 08
type: execute
wave: 1
depends_on:
  - 2.1-07
files_modified:
  - tests/helpers.cjs
  - tests/commands.test.cjs
  - tests/verify.test.cjs
  - tests/commit-files-deletion.test.cjs
  - sdk/src/vcs/__tests__/git-backend.test.ts
  - sdk/src/vcs/__tests__/baseline-parity.test.ts
  - tests/__tools__/capture-vcs-baselines.cjs
  - get-shit-done/bin/lib/commands.cjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "Per D-03: vcs.stage and vcs.unstage are gone from VcsAdapterCommon (plan 01) and from every caller. Zero repo references remain (excluding .planning/ docs and dist-cjs/)."
    - "Per Pattern E: tests that did `vcs.stage(['x'])` + `vcs.commit({message})` collapse to `vcs.commit({files:['x'], message})`. Tests that did `vcs.unstage(...)` migrate per the deletion analog (delete file in WC + vcs.commit({files:['x']}))."
    - "Per D-21: all 19+4 caller sites land in one commit; lint allowlist is not modified (plan 09 owns final allowlist sweep)."
    - "Test fixture (tests/helpers.cjs) seed paths no longer invoke vcs.stage; the vcsTest fixture's snapshot/restore primitives are unaffected (D-14 carried)."
  artifacts:
    - path: tests/helpers.cjs
      provides: "vcsTest fixture seeding migrates to vcs.commit({files}) per Pattern E"
    - path: get-shit-done/bin/lib/commands.cjs
      provides: "Remaining vcs.stage / vcs.unstage call sites in non-cmdCommit functions hard-removed"
    - path: sdk/src/vcs/__tests__/baseline-parity.test.ts
      provides: "stage / unstage dispatch clauses deleted; corresponding baselines re-classified for plan 09 cleanup"
  key_links: []
---

<objective>
Hard-remove every `vcs.stage(...)` and `vcs.unstage(...)` consumer per D-03. Plan 01 deleted the verbs from the type surface and the git backend. This plan reshapes the 19+4 remaining caller sites onto `vcs.commit({files})` with WC-state-capture semantics per Pattern E.

After this plan, `grep -rn "vcs\\.(stage|unstage)\\(" --include="*.ts" --include="*.cjs"` returns 0 outside `.planning/`. Plans 04-07 already migrated some sites incidentally; this plan finishes the sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-07-SUMMARY.md

<interfaces>
<!-- Plan 01 deleted stage/unstage from VcsAdapterCommon and from backends/git.ts's
     adapter freeze. After plans 02-07, some incidental sites were also migrated
     (e.g., plan 04 dropped vcs.stage upstream of pathspec→files collapses in
     commit.ts and commands.cjs's cmdCommit). This plan finishes the remaining
     19 stage + 4 unstage sites. -->

Live grep at plan-time:
```
grep -rnE "vcs\.(stage|unstage)\(" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/"
```
Expected before plan 04 ran (per RESEARCH grep audit):
- sdk/src/vcs/__tests__/baseline-parity.test.ts: 4 stage + 1 unstage
- tests/commands.test.cjs: 3 stage
- sdk/src/vcs/__tests__/git-backend.test.ts: 3 stage + 1 unstage
- tests/helpers.cjs: 2 stage
- tests/__tools__/capture-vcs-baselines.cjs: 2 stage + 1 unstage
- tests/verify.test.cjs: 1 stage
- tests/commit-files-deletion.test.cjs: 1 stage
- sdk/src/query/commit.ts: 1 stage (likely already removed by plan 04)
- sdk/src/query/commit.test.ts: 1 stage (likely already removed by plan 04)
- get-shit-done/bin/lib/commands.cjs: 1 stage (likely already removed by plan 04)

At plan 08 start, executor re-greps to get the actual remaining set. Some sites may already be 0 due to plans 04-07 incidental cleanup.

Pattern E (PATTERNS.md lines 818-838):
```javascript
// BEFORE:
vcs.stage(['foo.txt']);
vcs.commit({ message: 'add foo' });

// AFTER (Phase 2.1, D-02 + D-04):
vcs.commit({ files: ['foo.txt'], message: 'add foo' });

// BEFORE (unstage / delete):
vcs.unstage(['old.txt']);
vcs.commit({ message: 'remove old.txt' });

// AFTER (Phase 2.1, D-04 WC-state-capture for deletes):
fs.unlinkSync(path.join(cwd, 'old.txt'));
vcs.commit({ files: ['old.txt'], message: 'remove old.txt' });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Hard-remove vcs.stage callers via Pattern E (stage + commit collapse)</name>
  <files>tests/helpers.cjs, tests/commands.test.cjs, tests/verify.test.cjs, tests/commit-files-deletion.test.cjs, sdk/src/vcs/__tests__/git-backend.test.ts, sdk/src/vcs/__tests__/baseline-parity.test.ts, tests/__tools__/capture-vcs-baselines.cjs, get-shit-done/bin/lib/commands.cjs</files>
  <read_first>
    - tests/helpers.cjs (focus the 2 lazy-getter sites per RESEARCH Assumption A7)
    - tests/commands.test.cjs (3 stage sites)
    - tests/verify.test.cjs (1 stage)
    - tests/commit-files-deletion.test.cjs (1 stage)
    - sdk/src/vcs/__tests__/git-backend.test.ts (3 stage)
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (4 stage dispatch clauses — args[0] === 'add' shape)
    - tests/__tools__/capture-vcs-baselines.cjs (2 stage capture-time invocations)
    - get-shit-done/bin/lib/commands.cjs (any remaining stage call outside cmdCommit)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Pattern E lines 818-838)
  </read_first>
  <behavior>
    - Every `vcs.stage([paths])` invocation in a test seed/setup context COLLAPSES with the immediately downstream `vcs.commit({message})` into a single `vcs.commit({files: [paths], message})` per Pattern E.
    - If `vcs.stage` is called without a matching downstream `vcs.commit`, inspect the test purpose:
      - If staging is asserted directly (e.g., `expect(vcs.status()).toContain(...)` after stage to verify the staging side effect) → DELETE the test (with rationale: "staging is git-only; cross-backend tests should not assert on it").
      - If staging precedes a non-adapter assertion (e.g., direct git CLI probe inside an allowlisted file) → also re-shape onto WC-state probe.
    - baseline-parity.test.ts: 4 `args[0] === 'add'` dispatch clauses for `vcs.stage`'s baselines — DELETE the clauses entirely. The baselines (JSON files at tests/baselines/git-vcs/) become orphaned and plan 09 deletes them.
    - capture-vcs-baselines.cjs: 2 stage-capture invocations — DELETE; the corresponding baseline JSON files are stale and plan 09 deletes them.
    - tests/helpers.cjs: per RESEARCH Assumption A7, the 2 vcs.stage occurrences sit inside lazy-getter accessors. Confirm at plan-time; if they're seed paths (e.g., vcsTest fixture's initial commit seed), apply Pattern E.
    - commands.cjs: any vcs.stage outside cmdCommit (which was migrated by plan 04) becomes either a Pattern-E collapse with a sibling vcs.commit, or DELETED if it's an orphaned legacy path with no surviving consumer.
  </behavior>
  <action>
    1. Run `grep -nE "vcs\.stage\(" <file>` for each of the 8 files.
    2. For each site, identify the surrounding test context (which describe/it/test block, what's the assertion target).
    3. Apply Pattern E collapse: combine the stage call with the immediately downstream commit({message}) into commit({files, message}).
    4. For orphan stage calls (no downstream commit), DELETE the call + add a rationale comment `// 2.1 D-03: vcs.stage hard-removed; staging is git-only and not a cross-backend concept.`
    5. For baseline-parity.test.ts dispatch clauses (args[0] === 'add'), DELETE the entire `} else if (args[0] === 'add' && ...) {...}` block. Note which baseline JSON files become orphaned.
    6. For capture-vcs-baselines.cjs stage captures, DELETE the capture-invocation block. Note which baselines become stale.
    7. Maintain a list of orphaned baseline files in SUMMARY for plan 09 cleanup: `tests/baselines/git-vcs/<file>.snap.json`.
  </action>
  <verify>
    <automated>echo "--- repo-wide vcs.stage (excluding .planning) ---" && grep -rnE "vcs\.stage\(" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | wc -l; echo "--- baseline-parity 'add' dispatch clauses ---" && grep -cE "args\[0\]\s*===\s*['\"]add['\"]" sdk/src/vcs/__tests__/baseline-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide `vcs.stage(` grep returns 0 outside .planning/.
    - `args[0] === 'add'` baseline-parity dispatch clauses: 0 remain (all deleted).
    - List of orphaned baseline JSON files documented in SUMMARY for plan 09 cleanup.
  </acceptance_criteria>
  <done>All vcs.stage call sites hard-removed via Pattern E or deletion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Hard-remove vcs.unstage callers via delete-in-WC analog</name>
  <files>sdk/src/vcs/__tests__/git-backend.test.ts, sdk/src/vcs/__tests__/baseline-parity.test.ts, tests/__tools__/capture-vcs-baselines.cjs, get-shit-done/bin/lib/commands.cjs</files>
  <read_first>
    - sdk/src/vcs/__tests__/git-backend.test.ts (1 unstage)
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (1 unstage dispatch clause — args[0] === 'rm' shape)
    - tests/__tools__/capture-vcs-baselines.cjs (1 unstage capture)
    - get-shit-done/bin/lib/commands.cjs (1 unstage — likely cmdUnstage CLI command or similar)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Pattern E lines 831-838 — delete-in-WC analog)
  </read_first>
  <behavior>
    - Every `vcs.unstage([paths])` invocation reshapes onto the delete-in-WC analog (Pattern E): `fs.unlinkSync(path.join(cwd, p))` + `vcs.commit({files: [p], message})`.
    - baseline-parity.test.ts dispatch: `args[0] === 'rm'` clause DELETED.
    - capture-vcs-baselines.cjs: unstage capture-invocation DELETED.
    - commands.cjs: if a CLI command (e.g., cmdUnstage) exposed unstage as a user-facing op, REMOVE the command entirely with a deprecation rationale OR reshape onto an explicit "delete file then commit" flow. Decide at plan-time based on the command's actual semantic and any test coverage of it.
    - Orphaned baseline JSON files (unstage-related) noted in SUMMARY for plan 09 cleanup.
  </behavior>
  <action>
    1. Run `grep -nE "vcs\.unstage\(" <file>` for each of the 4 files.
    2. For each site, apply the delete-in-WC analog from Pattern E or DELETE the call/block per the decision matrix.
    3. For commands.cjs: if cmdUnstage (or equivalent) exists as a CLI command, the command becomes meaningless under D-03 — REMOVE the command. Update any command-registry table that listed it. Update README/help-text if it mentions the command. (If those changes are NOT mechanical or touch out-of-scope files, surface the scope issue in SUMMARY and defer to a follow-up plan.)
    4. Document orphaned baseline files in SUMMARY.
  </action>
  <verify>
    <automated>echo "--- repo-wide vcs.unstage (excluding .planning) ---" && grep -rnE "vcs\.unstage\(" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | wc -l; echo "--- baseline-parity 'rm' dispatch clauses ---" && grep -cE "args\[0\]\s*===\s*['\"]rm['\"]" sdk/src/vcs/__tests__/baseline-parity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide `vcs.unstage(` grep returns 0 outside .planning/.
    - `args[0] === 'rm'` baseline-parity dispatch: 0 remain.
    - SUMMARY lists orphaned unstage-related baseline files for plan 09.
  </acceptance_criteria>
  <done>All vcs.unstage call sites hard-removed; CLI commands deprecated if applicable.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit stage/unstage hard-remove sweep</name>
  <files>(none — git only)</files>
  <read_first>
    - Tasks 1+2 verification output
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md (D-03, D-21)
  </read_first>
  <behavior>
    - All files from Tasks 1 + 2 land in one atomic commit on phase/2.1-audit.
  </behavior>
  <action>
    Stage the 8 files (or fewer if some had no remaining sites after plan-04's incidental cleanup); verify diff list. Commit:
    ```
    git commit -m "$(cat <<'EOF'
    refactor(2.1-08): hard-remove vcs.stage and vcs.unstage callers (D-03)

    Per D-03, vcs.stage and vcs.unstage were removed from VcsAdapterCommon and
    from backends/git.ts in plan 01's shape commit. This plan finishes the
    sweep by hard-removing all 19+4 caller sites via Pattern E:
    - stage+commit collapse: vcs.stage(['x']) + vcs.commit({message}) -> vcs.commit({files:['x'], message})
    - unstage analog: vcs.unstage(['x']) + vcs.commit({message}) -> fs.unlinkSync + vcs.commit({files:['x'], message})

    Orphaned baseline JSON files (stage/unstage captures) are tracked in this
    plan's SUMMARY and deleted by plan 09's baseline-parity sweep.

    Files:
    - tests/helpers.cjs: 2 lazy-getter seed sites (RESEARCH Assumption A7)
    - tests/commands.test.cjs: 3 stage
    - tests/verify.test.cjs: 1 stage
    - tests/commit-files-deletion.test.cjs: 1 stage
    - sdk/src/vcs/__tests__/git-backend.test.ts: 3 stage + 1 unstage
    - sdk/src/vcs/__tests__/baseline-parity.test.ts: 4 'add' + 1 'rm' dispatch clauses deleted
    - tests/__tools__/capture-vcs-baselines.cjs: 2 stage + 1 unstage capture sites
    - get-shit-done/bin/lib/commands.cjs: residual stage/unstage post plan-04

    No lint allowlist additions (D-22). Internal git.ts local index var still
    in use (rename/copy heuristic, plan 05 D-16 carry).

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```
  </action>
  <verify>
    <automated>git branch --show-current; git log --oneline -1; git diff HEAD~1 HEAD --name-only | wc -l; echo "--- final stage/unstage check ---" && grep -rcE "vcs\.(stage|unstage)\(" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v ":0$" | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning"</automated>
  </verify>
  <acceptance_criteria>
    - Branch is `phase/2.1-audit`.
    - HEAD subject starts with `refactor(2.1-08): hard-remove vcs.stage and vcs.unstage`.
    - Repo-wide `vcs.(stage|unstage)\\(` grep returns 0 outside .planning/.
    - SUMMARY lists orphaned baseline JSON files for plan 09 cleanup.
  </acceptance_criteria>
  <done>stage/unstage hard-remove sweep committed.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1.08-01 | Tampering | fs.unlinkSync in tests | mitigate | All deletions are inside vcsTest fixture cwd (isolated tmpdir per Phase 1 D-14 snapshot/restore). |
| T-2.1.08-02 | Information Disclosure | unintended file deletion outside fixture | mitigate | path.join(cwd, ...) confines deletion to fixture root; vcsTest restore primitive reverts post-test. |
| T-2.1.08-03 | Tampering | CLI cmdUnstage removal | accept | If a CLI command was deprecated, README/help-text updates may need a follow-up plan (documented in SUMMARY). |
| T-2.1.08-SC | Tampering | npm installs | mitigate | N/A. |
</threat_model>

<verification>
- Repo-wide vcs.stage/unstage grep returns 0.
- One atomic commit.
- Orphan baseline list captured for plan 09.
</verification>

<success_criteria>
- 23 caller sites hard-removed.
- WC-state-capture analog landed for unstage cases.
- Baseline orphans tracked for plan 09.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-08-SUMMARY.md`.
</output>
