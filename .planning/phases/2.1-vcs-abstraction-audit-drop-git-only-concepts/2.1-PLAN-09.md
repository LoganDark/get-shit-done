---
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 09
type: execute
wave: 1
depends_on:
  - 2.1-08
files_modified:
  - tests/baselines/git-vcs/
  - sdk/src/vcs/__tests__/baseline-parity.test.ts
  - scripts/lint-vcs-no-raw-git.allow.json
  - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-LEARNINGS.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All baselines that captured pre-reshape behavior are either re-blessed (current behavior captured) or deleted (orphan from stage/unstage/hooks removal)."
    - "The lint guard (lint-vcs-no-raw-git.cjs) exits 0 on phase/2.1-audit — no remaining allowlist entries reference deleted files or removed verbs (D-22). No new allowlist entries added (D-22)."
    - "baseline-parity.test.ts vitest run exits 0 — every dispatch clause matches a current baseline JSON; no orphan clauses, no orphan baselines."
    - "Full repo test suite (`pnpm test`) exits 0 OR documents any pre-existing failures (carried from STATE.md known issues like commit.test.ts:304)."
    - "LEARNINGS captures the 2.1 surprises (e.g., shape-commit broken-lint duration, baseline drift count, any pitfall hits)."
  artifacts:
    - path: tests/baselines/git-vcs/
      provides: "Baselines re-blessed for CommitInput shape change; orphan baselines deleted"
    - path: scripts/lint-vcs-no-raw-git.allow.json
      provides: "Final allowlist re-tighten — entries for sites that became allowlist-unnecessary post-2.1 are removed"
    - path: .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-LEARNINGS.md
      provides: "Phase 2.1 LEARNINGS document for downstream phases + RETROSPECTIVE rollup"
  key_links: []
---

<objective>
Close Phase 2.1 by:
1. Re-blessing baselines that drifted due to plan 04's CommitInput shape change.
2. Deleting orphan baselines from plans 07-08's stage/unstage/hooks dispatch removal.
3. Final lint allowlist sweep per D-22 — remove entries that became unnecessary after every caller migrated (still no NEW entries).
4. Running the full test suite to confirm the long-lived branch is green.
5. Writing 2.1-LEARNINGS.md for downstream phase consumption.

After this plan, phase/2.1-audit is mergeable to main: lint clean, tests green, LEARNINGS captured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-08-SUMMARY.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-LEARNINGS.md

<interfaces>
<!-- Plans 04 and 08 left baseline drift and orphans:
     - Plan 04 (CommitInput reshape): commit-related baselines that captured the
       pre-D-04 'git add -- <paths>' invocation now need re-blessing to capture
       'git add -A -- <paths>'. Likely ~6 baselines per RESEARCH Assumption A4.
     - Plan 08 (stage/unstage hard-remove): all baselines whose dispatch clause
       was deleted (args[0] === 'add' for stage; args[0] === 'rm' for unstage)
       are orphaned and must be deleted.
     - Plan 07 (vcs.hooks removal): no baselines existed for hooks since
       hooks.fire was test-only; nothing to clean here.

The baseline-capture tool (tests/__tools__/capture-vcs-baselines.cjs) is the
re-bless mechanism: running it re-emits baseline JSONs from current behavior. -->

Suggested re-bless commands (executor verifies during task 1):
```
node tests/__tools__/capture-vcs-baselines.cjs              # full re-capture
# OR — selective per drifted baseline:
node tests/__tools__/capture-vcs-baselines.cjs --filter=commit-ts
node tests/__tools__/capture-vcs-baselines.cjs --filter=commands-cjs
```

Lint check:
```
node scripts/lint-vcs-no-raw-git.cjs
```
Expected: exit 0 on phase/2.1-audit if every caller migrated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Baseline-parity sweep — re-bless drifted, delete orphans</name>
  <files>tests/baselines/git-vcs/, sdk/src/vcs/__tests__/baseline-parity.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (full file — check dispatch clauses post-plans 02-08)
    - tests/__tools__/capture-vcs-baselines.cjs (the re-bless tool)
    - ls tests/baselines/git-vcs/ — list of all current baseline JSON files
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-08-SUMMARY.md (orphan list from plan 08)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md (Pitfall 3, Assumption A4)
  </read_first>
  <behavior>
    - Every baseline JSON file in tests/baselines/git-vcs/ whose underlying dispatch clause was deleted by plans 07-08 is DELETED.
    - Every baseline JSON whose captured args drifted due to plan 04 (e.g., `git add --` → `git add -A --`) is RE-BLESSED via the capture tool.
    - baseline-parity.test.ts: confirm zero dispatch clauses are orphan (every clause matches at least one baseline). If a clause has no matching baseline, delete the clause; if a baseline has no matching clause, delete the baseline.
    - Final `baseline-parity.test.ts` vitest run exits 0.
  </behavior>
  <action>
    1. Take inventory:
       - `ls tests/baselines/git-vcs/ | wc -l` (current count).
       - Cross-reference with plan 08 SUMMARY's orphan list.
    2. Run the capture tool to re-bless drifted baselines:
       - `node tests/__tools__/capture-vcs-baselines.cjs` (or per-filter if the tool supports it).
       - Inspect the git diff: which baselines changed? Per RESEARCH Assumption A4 the expected drift is ~6 baselines (commands-cjs-339-commit, commands-cjs-402-commit, commit-ts-170-commit, commit-ts-301-commit-c-form, commit-files-deletion.test.cjs baselines if present).
    3. Delete orphaned baselines:
       - For each file in plan 08's orphan list (stage / unstage captures), `git rm tests/baselines/git-vcs/<file>.snap.json`.
    4. Re-run `cd sdk && pnpm exec vitest run src/vcs/__tests__/baseline-parity.test.ts`. If it still fails, investigate per-baseline mismatches and either re-bless or fix the dispatch clause.
    5. If a baseline-parity assertion exists for `args[0] === 'commit'` with pathspec-shape args, that clause should have been simplified by plan 04 — verify and re-bless if needed.
  </action>
  <verify>
    <automated>echo "--- baseline count ---" && ls tests/baselines/git-vcs/ | wc -l; echo "--- baseline-parity vitest ---" && cd sdk && pnpm exec vitest run src/vcs/__tests__/baseline-parity.test.ts 2>&1 | grep -E "(PASS|FAIL|passed|failed|Tests)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd sdk && pnpm exec vitest run src/vcs/__tests__/baseline-parity.test.ts` exits 0.
    - Baseline directory count is internally consistent with dispatch clauses (no orphans either direction).
    - Re-blessed baseline diffs documented in SUMMARY (per-file list).
  </acceptance_criteria>
  <done>Baselines aligned with post-2.1 dispatch; baseline-parity test green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final lint allowlist sweep + full test suite gate</name>
  <files>scripts/lint-vcs-no-raw-git.allow.json</files>
  <read_first>
    - scripts/lint-vcs-no-raw-git.allow.json (current state post plan-01 audit)
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (post task 1)
    - scripts/lint-vcs-no-raw-git.cjs (the scanner — understand what triggers a violation)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md (D-22)
  </read_first>
  <behavior>
    - The lint guard `scripts/lint-vcs-no-raw-git.cjs` exits 0 on phase/2.1-audit.
    - The allowlist is re-tightened: any entry that exists only because of a pre-2.1 raw-git invocation that has since been migrated is REMOVED. Per D-22: no new entries.
    - The full test suite (`pnpm test`) exits 0 OR documents specific failures that are pre-existing (e.g., commit.test.ts:304 per STATE.md "Known Pre-Existing Test Failures").
  </behavior>
  <action>
    1. Run `node scripts/lint-vcs-no-raw-git.cjs`. Capture exit code and output.
       - If exit 0: allowlist is consistent. Audit each existing entry to confirm it still has a justifiable raw-git invocation. If any entry is for a file that no longer has raw-git, REMOVE the entry.
       - If exit ≠ 0: investigate. The scanner found a raw-git invocation not covered by the allowlist. Since plans 02-08 migrated every consumer, this would indicate either a residual raw-git call or a new accidental introduction. INVESTIGATE — do NOT add an allowlist entry per D-22.
    2. Per-entry audit: for each file in the allowlist, run `grep -nE "execSync\\(['\"]git\\s|spawnSync\\(['\"]git['\"]|\\bgit\\s+\\w+" <file>` to confirm a raw-git invocation still exists. If zero, REMOVE the entry.
    3. Run `pnpm test` (full suite). Capture results. Compare with STATE.md's "Known Pre-Existing Test Failures" list:
       - sdk/src/query/commit.test.ts:304 (gpgsign fixture)
       - sdk/src/query/config-mutation.test.ts:441 (commit_docs assertion)
       If failures match this set exactly, document as "no regression"; if new failures, investigate and fix or document as a follow-up gap.
    4. Commit the allowlist change if any (with stage just the allowlist file).
  </action>
  <verify>
    <automated>echo "--- lint exit ---" && node scripts/lint-vcs-no-raw-git.cjs; echo "exit code: $?"; echo "--- allowlist count ---" && node -e "const a=require('./scripts/lint-vcs-no-raw-git.allow.json');console.log('files:',a.files.length,'globs:',a.globs.length)"; echo "--- full test ---" && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/lint-vcs-no-raw-git.cjs` exits 0.
    - Allowlist file count is ≤ the post-plan-01 baseline (D-22: never larger).
    - `pnpm test` either exits 0 OR fails only on the known pre-existing tests from STATE.md (documented in SUMMARY).
    - If allowlist was modified, a commit message `chore(2.1-09): re-tighten lint-vcs-no-raw-git allowlist (D-22)` lands on phase/2.1-audit.
  </acceptance_criteria>
  <done>Lint clean; full suite green or only pre-existing failures; allowlist re-tightened.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 2.1-LEARNINGS.md + final commit</name>
  <files>.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-LEARNINGS.md</files>
  <read_first>
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-LEARNINGS.md (precedent format / structure)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-01-SUMMARY.md through 2.1-08-SUMMARY.md (if they exist by the time this task runs)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md (pitfalls, assumptions, open questions)
  </read_first>
  <behavior>
    - 2.1-LEARNINGS.md captures:
      - Pattern outcomes (did shape-commit-first work as RESEARCH predicted? broken-lint duration / per-plan tsc error count progression).
      - Pitfall hits (which RESEARCH pitfalls actually fired? which were false alarms?).
      - Assumption validation (which assumptions A1-A7 were confirmed/refuted?).
      - Open Question resolutions (the Open Questions 1-4 in RESEARCH — what was decided?).
      - Surprises that RESEARCH did NOT predict (the most useful kind of LEARNINGS entry).
      - Handoff notes for Phase 3 (jj backend): change_id translator placement (D-11/D-12 context), VcsHooks-removal implications for HOOK-01..05.
      - Cost / velocity notes if relevant.
    - Final cleanup commit lands the LEARNINGS file + any residual state updates.
  </behavior>
  <action>
    1. Read 02-LEARNINGS.md for structural precedent.
    2. Aggregate findings from each plan SUMMARY (2.1-01 through 2.1-08).
    3. Author 2.1-LEARNINGS.md with sections (suggested):
       - `## Summary` — one paragraph of what landed.
       - `## Pattern Outcomes` — shape-commit-first verdict; per-concern commit boundary verdict; baseline-parity sweep efficacy.
       - `## Pitfall Hits` — which of the 7 RESEARCH pitfalls fired; what was the impact; what was the fix.
       - `## Assumption Validation` — A1-A7 status table.
       - `## Open Question Resolutions` — Q1 (deferred fireHook internal to Phase 4), Q2 (re-grep at plan time worked), Q3 (permissive regex in 2.1), Q4 (commit.ts pathspec handler analyzed).
       - `## Surprises` — anything new.
       - `## Phase 3 Handoff` — change_id translator placement, VcsHooks-removal context for HOOK-01..05 wiring, any other unblockers.
    4. Stage the LEARNINGS file + any updated STATE.md or SUMMARY entries. Commit:
       ```
       git commit -m "$(cat <<'EOF'
       docs(2.1-09): close Phase 2.1 with LEARNINGS + final verification

       Lands 2.1-LEARNINGS.md capturing pattern outcomes, pitfall hits, assumption
       validation, open-question resolutions, surprises, and Phase 3 handoff notes.

       baseline-parity sweep (task 1): N baselines re-blessed, M deleted as orphans
       from stage/unstage/hooks dispatch removal.
       Lint allowlist (task 2): K entries removed; final state matches post-2.1
       steady state per D-22 (no new entries).

       Phase 2.1 closure: phase/2.1-audit is mergeable to main; lint clean,
       baseline-parity green, only pre-existing test failures remain.

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
  </action>
  <verify>
    <automated>echo "--- LEARNINGS file ---" && ls -la .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-LEARNINGS.md; echo "--- commit ---" && git log --oneline -3; echo "--- final lint ---" && node scripts/lint-vcs-no-raw-git.cjs; echo "exit: $?"; echo "--- final baseline-parity ---" && cd sdk && pnpm exec vitest run src/vcs/__tests__/baseline-parity.test.ts 2>&1 | grep -E "(PASS|FAIL|passed|failed|Tests)" | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-LEARNINGS.md` exists with all required sections.
    - HEAD subject starts with `docs(2.1-09): close Phase 2.1`.
    - Lint exits 0.
    - baseline-parity vitest exits 0.
  </acceptance_criteria>
  <done>Phase 2.1 closed; LEARNINGS landed; phase/2.1-audit mergeable to main.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1.09-01 | Tampering | re-blessed baselines | mitigate | Re-bless tool runs against the canonical git CLI; the lint guard's allowlist authorizes capture-vcs-baselines.cjs to invoke git directly. |
| T-2.1.09-02 | Information Disclosure | deleted-orphan-baseline regression | accept | Orphan baselines correspond to dispatch clauses that no longer exist (stage/unstage/hooks). Deletion is symmetric with clause removal — no behavioral regression possible. |
| T-2.1.09-03 | Tampering | allowlist re-tighten | mitigate | Removing entries strengthens the lint guard; cannot regress security posture. D-22 prevents adding entries. |
| T-2.1.09-SC | Tampering | npm installs | mitigate | N/A. |
</threat_model>

<verification>
- Lint exits 0.
- baseline-parity vitest exits 0.
- Full suite either green or only pre-existing failures.
- LEARNINGS file exists with all sections.
</verification>

<success_criteria>
- phase/2.1-audit is mergeable to main.
- LEARNINGS captured for Phase 3 consumption.
- Baseline corpus reflects post-2.1 steady state.
- Allowlist re-tightened per D-22.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-09-SUMMARY.md`.
</output>
