---
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 06
type: execute
wave: 1
depends_on:
  - 2.1-05
files_modified:
  - get-shit-done/bin/lib/worktree-safety.cjs
  - sdk/src/vcs/__tests__/git-backend.test.ts
  - sdk/src/vcs/__tests__/adapter-contract.test.ts
  - sdk/src/vcs/__tests__/baseline-parity.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Per D-18: every consumer of `gitDir`/`gitCommonDir` reads them via `vcs.gitOnly.gitDir()` / `vcs.gitOnly.gitCommonDir()` after `vcs.kind === 'git'` narrowing."
    - "Per ADR-0004: the `deps = {}` injection seam in worktree-safety.cjs is preserved verbatim (W4)."
    - "Linked-worktree detection logic at worktree-safety.cjs (the primary consumer) preserves its `gitDir !== gitCommonDir` predicate, now via gitOnly methods."
    - "WorkspaceContext no longer surfaces gitDir/gitCommonDir; consumers either narrow on vcs.kind === 'git' or stop probing those values."
  artifacts:
    - path: get-shit-done/bin/lib/worktree-safety.cjs
      provides: "Linked-worktree detection via vcs.gitOnly.gitDir() / vcs.gitOnly.gitCommonDir() with vcs.kind === 'git' narrow"
    - path: sdk/src/vcs/__tests__/git-backend.test.ts
      provides: "gitOnly.gitDir/gitCommonDir contract tests (move from workspace.context coverage)"
  key_links:
    - from: get-shit-done/bin/lib/worktree-safety.cjs
      to: sdk/src/vcs/backends/git.ts
      via: "vcs.gitOnly.gitDir() / gitCommonDir() call"
      pattern: "gitOnly\\.(gitDir|gitCommonDir)\\("
---

<objective>
Reshape every consumer of `ctx.gitDir` / `ctx.gitCommonDir` (where `ctx = vcs.workspace.context()`) onto `vcs.gitOnly.gitDir()` / `vcs.gitOnly.gitCommonDir()` with the required `vcs.kind === 'git'` narrowing per D-18. The primary production consumer is `worktree-safety.cjs` (ADR-0004's linked-worktree detection at lines 97-99 per PATTERNS.md).

Plan 01's shape commit dropped the fields from `WorkspaceContext` and added the gitOnly methods. This plan migrates the 6 consumer call sites (2 in worktree-safety.cjs production + 4 in tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-RESEARCH.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md
@.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-05-SUMMARY.md
@docs/adr/0004-worktree-workstream-seam-module.md

<interfaces>
<!-- Plan 01 reshaped WorkspaceContext to {effectiveRoot, mode, isLinked} and added
     gitOnly.gitDir() + gitOnly.gitCommonDir() to GitOnlyOps. The git backend's
     workspace.context() body still computes the resolved paths internally for the
     isLinked flag, but no longer surfaces them in the return literal. -->

Live grep at plan-time:
```
grep -rnE "(ctx|context\(\))\.(gitDir|gitCommonDir)" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/"
```
Expected (per RESEARCH grep audit):
- get-shit-done/bin/lib/worktree-safety.cjs: 2 sites (lines 97-99 per PATTERNS.md)
- sdk/src/vcs/__tests__/git-backend.test.ts: 2 sites (workspace.context contract test)
- sdk/src/vcs/__tests__/adapter-contract.test.ts: 1 site
- sdk/src/vcs/__tests__/baseline-parity.test.ts: 1 site (dispatch clause for git rev-parse --git-dir / --git-common-dir)
Total: 6 sites.

Caller analog (PATTERNS.md Pattern A lines 717-750):
```javascript
// BEFORE (worktree-safety.cjs:97-99):
const ctx = vcs.workspace.context();
if (ctx.gitDir !== ctx.gitCommonDir) {
  return { effectiveRoot: path.dirname(ctx.gitCommonDir), mode: 'linked_worktree_root', reason: 'linked_worktree' };
}

// AFTER (Phase 2.1, D-18):
const ctx = vcs.workspace.context();
if (vcs.kind === 'git') {
  const gitDir = vcs.gitOnly.gitDir();
  const gitCommonDir = vcs.gitOnly.gitCommonDir();
  if (gitDir !== gitCommonDir) {
    return { effectiveRoot: path.dirname(gitCommonDir), mode: 'linked_worktree_root', reason: 'linked_worktree' };
  }
}
```

Test analog: replace `ctx.gitDir` / `ctx.gitCommonDir` reads with `vcs.gitOnly.gitDir()` / `vcs.gitOnly.gitCommonDir()` calls (after `if (vcs.kind === 'git')` narrow when adapter type is a union; can be unconditional when test pins git backend).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate worktree-safety.cjs gitDir/gitCommonDir consumers (production, ADR-0004 seam preserved)</name>
  <files>get-shit-done/bin/lib/worktree-safety.cjs</files>
  <read_first>
    - get-shit-done/bin/lib/worktree-safety.cjs (full file — focus lines 65-110 for the resolveWorktreeContext function per PATTERNS.md Pattern A)
    - docs/adr/0004-worktree-workstream-seam-module.md (W4 deps={} injection seam)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Pattern A lines 680-751)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-CONTEXT.md (D-18)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-04-PLAN.md (Phase 2's worktree-safety.cjs migration analog)
  </read_first>
  <behavior>
    - The `resolveWorktreeContext(cwd, deps = {})` function preserves its ADR-0004 deps={} signature (W4). No injection-seam change.
    - The 2 `ctx.gitDir` / `ctx.gitCommonDir` reads (lines 97-99) migrate to the Pattern A `<interfaces>` form: narrow on `vcs.kind === 'git'`, then call `vcs.gitOnly.gitDir()` / `vcs.gitOnly.gitCommonDir()`.
    - The conditional `if (gitDir !== gitCommonDir)` semantic is preserved verbatim — only the source of those two values changes.
    - The narrow `if (vcs.kind === 'git')` always succeeds at runtime in this caller because `createVcsAdapter(cwd, { kind: 'git' })` is pinned at the top of the function (line ~70 per PATTERNS.md). The narrow exists for static type-checking only.
  </behavior>
  <action>
    1. Read worktree-safety.cjs in full to locate the resolveWorktreeContext function and any other gitDir/gitCommonDir consumers.
    2. Apply the Pattern A `<interfaces>` analog at the two consumer sites.
    3. Preserve the `deps = {}` signature, the deps.vcs / deps.existsSync injection points, and the surrounding return-shape literals (`{ effectiveRoot, mode, reason }`).
    4. Verify ADR-0004 seam invariants: `grep -cE "deps\s*=\s*\{\}" get-shit-done/bin/lib/worktree-safety.cjs` returns ≥1.
  </action>
  <verify>
    <automated>echo "ctx.gitDir consumers:" && grep -cE "ctx\.(gitDir|gitCommonDir)" get-shit-done/bin/lib/worktree-safety.cjs; echo "gitOnly.gitDir/gitCommonDir consumers:" && grep -cE "gitOnly\.(gitDir|gitCommonDir)\(" get-shit-done/bin/lib/worktree-safety.cjs; echo "vcs.kind === 'git' narrow:" && grep -cE "vcs\.kind\s*===\s*['\"]git['\"]" get-shit-done/bin/lib/worktree-safety.cjs; echo "ADR-0004 deps seam:" && grep -cE "deps\s*=\s*\{\}" get-shit-done/bin/lib/worktree-safety.cjs; echo "--- worktree-safety paired tests run ---" && pnpm test -- tests/orphan-worktree-detection.test.cjs tests/bug-2774-worktree-cleanup-workspace-safety.test.cjs 2>&1 | grep -E "(PASS|FAIL|passed|failed)" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "ctx\.(gitDir|gitCommonDir)" get-shit-done/bin/lib/worktree-safety.cjs` returns 0.
    - `grep -cE "gitOnly\.(gitDir|gitCommonDir)\(" get-shit-done/bin/lib/worktree-safety.cjs` returns ≥2.
    - `grep -cE "vcs\.kind\s*===\s*['\"]git['\"]" get-shit-done/bin/lib/worktree-safety.cjs` returns ≥1.
    - `grep -cE "deps\s*=\s*\{\}" get-shit-done/bin/lib/worktree-safety.cjs` returns ≥1 (ADR-0004 W4 seam preserved).
    - Paired worktree tests pass (or document failures requiring plan 09 baseline re-bless).
  </acceptance_criteria>
  <done>worktree-safety.cjs gitDir/gitCommonDir consumers narrowed onto gitOnly; ADR-0004 seam preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate test consumers of ctx.gitDir/gitCommonDir + commit gitDir/gitCommonDir → gitOnly</name>
  <files>sdk/src/vcs/__tests__/git-backend.test.ts, sdk/src/vcs/__tests__/adapter-contract.test.ts, sdk/src/vcs/__tests__/baseline-parity.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/git-backend.test.ts (search for `gitDir` / `gitCommonDir` test blocks)
    - sdk/src/vcs/__tests__/adapter-contract.test.ts (single site)
    - sdk/src/vcs/__tests__/baseline-parity.test.ts (dispatch clause for `args[0] === 'rev-parse' && args.includes('--git-dir')` per PATTERNS.md Pattern F line 845)
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-PATTERNS.md (Pattern F lines 840-867)
  </read_first>
  <behavior>
    - Test reads of `ctx.gitDir` / `ctx.gitCommonDir` (where ctx = workspace.context()) reshape to:
      - either: drop the test block entirely if its sole purpose was to assert the field was exposed (now the field doesn't exist on the cross-backend shape).
      - or: replace with a dedicated test of `vcs.gitOnly.gitDir()` / `vcs.gitOnly.gitCommonDir()` — `expect(vcs.gitOnly.gitDir()).toMatch(/\.git$/)` etc.
    - baseline-parity.test.ts dispatch clause (`args.includes('--git-dir')`): the dispatch is keyed on the git command args; the canonical canonical-vs-adapter comparison now calls `vcs.gitOnly.gitDir()` (instead of `vcs.workspace.context().gitDir`). The args-shape key is unchanged.
    - Final commit lands all 4 files (worktree-safety.cjs from Task 1 + 3 test files) in one atomic commit per D-21.
  </behavior>
  <action>
    1. For each test file, run `grep -nE "(ctx|context\(\))\.(gitDir|gitCommonDir)" <file>` to find each site.
    2. For each site:
       - If inside a workspace.context() contract test: either reshape to assert the new shape `{ effectiveRoot, mode, isLinked }` (no gitDir/gitCommonDir), OR move the gitDir/gitCommonDir assertion to a sibling describe block named `'gitOnly.gitDir' / 'gitOnly.gitCommonDir'` and update the call to `vcs.gitOnly.gitDir()`.
       - If inside baseline-parity dispatch (Pattern F line 849): change the adapter-side call from `vcs.workspace.context().gitDir` to `vcs.gitOnly.gitDir()` (and similarly for gitCommonDir). Args-shape key unchanged.
    3. Add inline comments: `// 2.1 D-18: WorkspaceContext.{gitDir,gitCommonDir} moved to GitOnlyOps; narrow on vcs.kind === 'git' to access.`

    4. Stage the 4 files (worktree-safety.cjs + 3 test files); verify diff list; commit:
       ```
       git commit -m "$(cat <<'EOF'
       refactor(2.1-06): move gitDir/gitCommonDir from WorkspaceContext to gitOnly (D-18)

       Per D-18, WorkspaceContext drops the two pure-git path fields. Consumers
       narrow on vcs.kind === 'git' and call vcs.gitOnly.gitDir() / gitCommonDir()
       per Pattern A.

       Files:
       - get-shit-done/bin/lib/worktree-safety.cjs: primary production consumer
         (linked-worktree detection at lines ~97-99); ADR-0004 deps={} seam
         preserved (W4); vcs.kind === 'git' narrow is statically required and
         always succeeds at runtime (createVcsAdapter pinned to kind:'git').
       - sdk/src/vcs/__tests__/git-backend.test.ts: 2 sites — workspace.context
         contract test reshaped to new {effectiveRoot, mode, isLinked} shape;
         dedicated gitOnly.gitDir/gitCommonDir tests added.
       - sdk/src/vcs/__tests__/adapter-contract.test.ts: 1 site — same reshape.
       - sdk/src/vcs/__tests__/baseline-parity.test.ts: 1 site — dispatch
         clause updated to call vcs.gitOnly.* (args-shape key unchanged).

       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       EOF
       )"
       ```
  </action>
  <verify>
    <automated>echo "--- repo-wide ctx.gitDir / ctx.gitCommonDir (excluding .planning) ---" && grep -rnE "(ctx|context\(\))\.(gitDir|gitCommonDir)" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/" | wc -l; echo "--- gitOnly.gitDir/gitCommonDir consumers ---" && grep -rcE "gitOnly\.(gitDir|gitCommonDir)\(" --include="*.ts" --include="*.cjs" 2>/dev/null | grep -v ":0$" | grep -v node_modules | grep -v dist-cjs; echo "--- commit ---" && git log --oneline -1; echo "--- diff file count ---" && git diff HEAD~1 HEAD --name-only | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Repo-wide `(ctx|context\(\))\.(gitDir|gitCommonDir)` grep returns 0 (excluding .planning/).
    - `gitOnly\.(gitDir|gitCommonDir)\(` appears in at least worktree-safety.cjs + 1-2 test files.
    - HEAD subject starts with `refactor(2.1-06): move gitDir/gitCommonDir`.
    - Diff lists exactly 4 files (or 3 if a test file had no actionable site).
  </acceptance_criteria>
  <done>Commit lands; gitDir/gitCommonDir consumers fully migrated to gitOnly.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1.06-01 | Tampering | vcs.gitOnly.gitDir/gitCommonDir argv | mitigate | argv-array invocation; rev-parse output trimmed + path-resolved; no eval. |
| T-2.1.06-02 | Information Disclosure | linked-worktree detection regression | mitigate | Task 1's paired worktree tests gate the migration; ADR-0004 W4 seam preserved. |
| T-2.1.06-SC | Tampering | npm installs | mitigate | N/A. |
</threat_model>

<verification>
- ctx.gitDir/gitCommonDir grep returns 0 outside .planning/.
- worktree tests pass (or documented for plan 09 baseline re-bless).
- gitOnly call sites land in production + tests.
</verification>

<success_criteria>
- 6 consumer sites migrated; one atomic commit.
- ADR-0004 W4 seam preserved.
- vcs.kind === 'git' narrow lands at production site.
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-06-SUMMARY.md`.
</output>
