---
phase: 2.1-vcs-abstraction-audit-drop-git-only-concepts
plan: 08
subsystem: vcs-adapter
tags: [stage-unstage-hard-remove, D-03, D-21, D-22, pattern-E]
requires:
  - "2.1-01 (shape commit: vcs.stage and vcs.unstage deleted from VcsAdapterCommon and from backends/git.ts's adapter freeze)"
  - "2.1-04 (CommitInput.pathspec→files collapse + helpers.cjs Rule-3 fix; drained 4 of the 8 plan-08 frontmatter files before plan-08 ran)"
  - "2.1-07 (vcs.hooks public-surface cleanup — no overlap with stage/unstage, but its handoff listed the 3 remaining git-backend.test.ts stage/unstage failures as plan-08 scope)"
provides:
  - "Repo-wide vcs.stage/vcs.unstage grep returns 0 functional matches outside .planning/, dist-cjs/, node_modules/"
  - "3 previously-failing git-backend.test.ts contract probes resolved (TypeError: vcs.stage is not a function): stage([file])-into-diff, unstage([file])-from-diff, diff.nameStatus + pre-staged file"
  - "Closes the D-03 broken-lint dimension on phase/2.1-audit; 5 orphaned baseline JSON files documented for plan-09 baseline-parity sweep"
  - "D-22 compliance: no lint allowlist modifications (plan 09 owns the final allowlist sweep)"
affects:
  - "Plan 09 (baseline-parity sweep + LEARNINGS): orphaned baseline JSON files to delete, baseline-parity test re-bless against the post-2.1 dispatch table, allowlist sweep"
tech_stack:
  added: []
  patterns:
    - "Pattern E (PATTERNS.md lines 818-838): stage+commit collapse — vcs.stage([x]) + vcs.commit({message}) → vcs.commit({files:[x], message})"
    - "Test-only raw-git stage synthesis (lift from plan-04 LEARNINGS): for tests asserting the staged side-effect of vcs.diff, replace vcs.stage with raw execSync('git add -- <file>'). Lint-allowed via test globs (sdk/src/vcs/__tests__/**, tests/**/*.test.cjs)"
    - "DELETE-not-reframe for adapter-shape probes (carry plan-07 default action): the stage/unstage describe block in git-backend.test.ts had no post-D-03 cross-backend assertion to land in its place — deletion is the correct action, with a D-03 breadcrumb comment preserving the audit trail"
    - "Baseline-parity dispatch-clause deletion: when a verb is hard-removed, the corresponding `args[0]==='…'` dispatch clause and capture-definition tuple both delete in the same commit; the JSON file becomes orphaned and plan 09 owns deletion (separates the test-side surgery from baseline-corpus housekeeping)"
key_files:
  created:
    - .planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-08-SUMMARY.md
  modified:
    - tests/verify.test.cjs
    - sdk/src/vcs/__tests__/git-backend.test.ts
    - sdk/src/vcs/__tests__/baseline-parity.test.ts
    - tests/__tools__/capture-vcs-baselines.cjs
decisions:
  - "Live grep at plan-08 entry counted 9 functional vcs.stage/unstage sites across 4 files (not the 19+4 across 8 the plan frontmatter expected). Plans 04-07's incidental cleanup — primarily plan 04's pathspec→files collapse across commit.ts / commands.cjs / commit.test.ts / commands.test.cjs / commit-files-deletion.test.cjs and the Rule-3 helpers.cjs migration — already drained the other 4 frontmatter files. Plan-08 frontmatter wording matches RESEARCH pre-grep, not phase-08 entry state."
  - "git-backend.test.ts: `describe('createGitAdapter — stage / unstage (02-03 Task 1)')` deleted in full. The block contained 2 it() blocks (stage→diff and unstage→diff round-trip); both exercise verbs that no longer exist on the adapter shape. There is no D-04 / WC-state-capture replacement assertion that belongs in this slot — the equivalent contract probes live in the `commit` describe block above and assert on `vcs.commit({files,message})` outputs. Plan-07's DELETE-not-reframe default applies."
  - "git-backend.test.ts: diff.nameStatus test reshaped (not deleted). The probe asserts the staged-diff response shape; deleting the test would lose nameStatus coverage. Synthesizing pre-staged state via raw `execSync('git add -- <file>')` preserves the assertion. The file is lint-allowlisted via the `sdk/src/vcs/__tests__/**` glob (plan 01-05); plan 04's LEARNINGS established this pattern for commit.test.ts's #3061 synthesis."
  - "verify.test.cjs:419: the vcs.stage(['src/app.js']) call was directly followed by vcs.commit({message, files:['src/app.js']}) — the explicit `files:` argument already performs WC-state-capture under D-04. Delete the stage line (no reshape needed); the commit already does the work."
  - "baseline-parity.test.ts: 3 dispatch clauses (add+`--`, rm+`--cached`, add no-`--`) deleted entirely. The clauses' raison d'être was byte-identity assertion against vcs.stage / vcs.unstage outputs — those verbs are gone. The 5 baselines they previously serviced (commit-ts-148-add, commit-ts-294-add-c-form, commands-cjs-330-rm-cached, commands-cjs-332-add, commands-cjs-398-add) become orphaned. The existing fall-through behavior in the dispatch chain silently skips baselines that match no clause, so the orphaned JSON files don't cause test failures — they're just dead weight. Plan 09 deletes them as part of the baseline-parity sweep."
  - "capture-vcs-baselines.cjs: 5 baseline-definition objects deleted (matching the orphaned JSON files). The script is a one-shot generator; keeping the definitions would cause plan-09's re-bless run to re-generate stale baseline JSONs against verbs that no longer exist. D-03 breadcrumb comments left at each removal site so the audit trail survives. The 3 comment-only references at original lines 360, 371, 401 disappeared with their parent objects."
  - "No lint allowlist additions (D-22). The one new raw `git add` invocation lives inside sdk/src/vcs/__tests__/git-backend.test.ts which is already on the allowlist (file entry + the `sdk/src/vcs/__tests__/**` glob, both per plan 01-05)."
requirements_completed: []
metrics:
  duration: "~14 min"
  completed_date: "2026-05-11"
  files_modified: 4
  tasks_completed: 3
---

# Phase 2.1 Plan 08: Hard-Remove vcs.stage / vcs.unstage Callers Summary

**One-liner:** Removed the 9 remaining functional `vcs.stage` / `vcs.unstage` call sites across 4 files via Pattern E (stage+commit collapse) + DELETE-not-reframe (adapter-shape probes) + raw-git stage synthesis for one staged-diff probe; closes D-03 dimension on `phase/2.1-audit` in commit `378241e9`; 5 baseline JSON files now orphaned for plan-09 cleanup; no lint allowlist modifications.

## Performance

- **Duration:** ~14 min
- **Started:** 2026-05-11T19:35Z
- **Completed:** 2026-05-11T19:49Z
- **Tasks:** 3 (Pattern E sweep + dispatch-clause deletion + atomic commit) collapsed into one atomic commit per D-21
- **Files modified:** 4

## Pre/Post Grep

`grep -rnE "vcs\.(stage|unstage)\(" --include="*.ts" --include="*.cjs"` (excl `node_modules` / `dist-cjs` / `.planning/`):

| Scope | Pre-count | Post-count |
|-------|-----------|------------|
| Functional invocations | **9** | **0** |
| Documentation comments referencing the verbs (in capture-vcs-baselines.cjs) | 3 (orphaned with their parent objects) | 0 (parent objects deleted; new D-03 breadcrumb comments use natural prose, not the literal `vcs.stage(` token) |
| Total grep matches | 12 | 0 |

**Per-file site breakdown (pre→post):**

| File | Pre-sites | Post-sites | Action |
|------|-----------|------------|--------|
| `tests/verify.test.cjs` | 1 stage | 0 | Stage line deleted; the immediately-following `vcs.commit({files:['src/app.js']})` already performs WC-state-capture, no reshape needed |
| `sdk/src/vcs/__tests__/git-backend.test.ts` | 3 stage + 1 unstage = 4 | 0 | `describe('createGitAdapter — stage / unstage (02-03 Task 1)')` deleted in full (3 of 4 sites); `vcs.stage(['newfile.txt'])` in diff.nameStatus test replaced with raw `execSync('git add -- newfile.txt')` to synthesize pre-staged state for the staged-diff probe |
| `sdk/src/vcs/__tests__/baseline-parity.test.ts` | 2 stage + 1 unstage = 3 (inside 3 dispatch clauses) | 0 | All 3 dispatch clauses deleted in full; D-03 breadcrumb added near the dispatch chain head listing 5 orphaned baselines for plan 09 |
| `tests/__tools__/capture-vcs-baselines.cjs` | 0 functional (only 3 comment-only refs at lines 360, 371, 401) | 0 | 5 baseline-definition objects deleted (the ones the 3 deleted dispatch clauses serviced); D-03 breadcrumb comments left at each removal site |

Sites in 4 plan-08 frontmatter files that **did not need edits** (drained by plan 04's incidental cleanup before plan 08 ran):

- `tests/helpers.cjs` (plan 04 Rule-3 fix migrated `createTempGitProject`)
- `tests/commands.test.cjs` (plan 04 collapsed 3 stage sites + retargeted 2 check-commit setup sites to raw `execFileSync('git', ['add', '--', …])`)
- `tests/commit-files-deletion.test.cjs` (plan 04 reshaped the `beforeEach` init commit)
- `get-shit-done/bin/lib/commands.cjs` (plan 04 collapsed cmdCommit's stage/unstage if/else block onto `vcs.commit({files})`; cmdCommitToSubrepo's stage loop removed)

The plan-08 frontmatter's 19+4 caller-count came from RESEARCH's pre-grep at phase-2.1 entry, not the post-plan-04 state. Live grep at plan-08 entry was the source of truth.

## Verification

### Grep gates

| Gate | Expected | Actual |
|------|----------|--------|
| Repo-wide `vcs\.(stage\|unstage)\(` (excl .planning + dist-cjs + node_modules) | 0 | 0 ✓ |
| baseline-parity `args[0] === 'add'` dispatch clauses | 0 | 0 ✓ |
| baseline-parity `args[0] === 'rm'` dispatch clauses | 0 | 0 ✓ |
| no-raw-git lint guard | 0 violations | 0 ✓ (890 files scanned) |

### tsc gate

```
cd sdk && pnpm exec tsc --noEmit
```

Exit 0; no errors.

### vitest gates (2 touched test files)

```
cd sdk && pnpm exec vitest run \
  src/vcs/__tests__/git-backend.test.ts \
  src/vcs/__tests__/baseline-parity.test.ts
```

| Test file | Plan 07 baseline | Plan 08 result | Delta |
|-----------|------------------|----------------|-------|
| git-backend.test.ts | 3 failed / 47 passed (50) | 0 failed / 48 passed (48) | -2 tests (stage/unstage describe deleted); -3 failures (TypeError: vcs.stage is not a function on lines 502, 511, 546) resolved; **file now fully green** |
| baseline-parity.test.ts | 5 failed / 50 passed (55, per plan-06 handoff) | 0 failed / 55 passed (55) | No test count change; 5 failures (commit-ts-148-add, commit-ts-294-add-c-form, commands-cjs-330-rm-cached, commands-cjs-332-add, commands-cjs-398-add — all TypeError: vcs.stage/unstage is not a function) resolved by dispatch-clause deletion (those baselines fall through to no clause and are silently skipped); **file now fully green** |

**Combined: 103/103 passed.**

The 3 vcs.stage/unstage TypeError failures called out in plan-07's handoff are all resolved (tests deleted, not failing). The 5 baseline-parity TypeError failures called out in plan-06's handoff are all resolved (dispatch clauses deleted; orphaned baselines silently skipped).

### node --test gate (tests/verify.test.cjs)

```
node --test tests/verify.test.cjs
```

Result: 42 pass, 0 fail. The `passes for valid summary with real files and commits` test (which previously had the redundant vcs.stage line) still passes — the vcs.commit({files:['src/app.js']}) call captures the WC state correctly.

### no-raw-git lint gate (D-22 compliance)

```
node scripts/lint-vcs-no-raw-git.cjs
```

Result: `ok lint-vcs-no-raw-git: 890 files scanned in <root>, 0 violations`. The one new raw `execSync('git add -- newfile.txt')` in git-backend.test.ts is covered by the existing `sdk/src/vcs/__tests__/**` glob entry in `scripts/lint-vcs-no-raw-git.allow.json`. No allowlist modifications made.

## Self-Check: PASSED

- Branch: `phase/2.1-audit` ✓
- HEAD commit hash: `378241e9` ✓
- HEAD commit subject: `refactor(2.1-08): hard-remove vcs.stage and vcs.unstage callers (D-03)` ✓
- `git diff HEAD~1 HEAD --name-only` lists exactly 4 files ✓
- Repo-wide `vcs\.(stage|unstage)\(` grep returns 0 ✓
- baseline-parity `args[0] === 'add' || === 'rm'` dispatch grep returns 0 ✓
- SDK tsc clean (exit 0) ✓
- sdk vitest on touched files: 103/103 passed ✓
- node --test tests/verify.test.cjs: 42/42 passed ✓
- no-raw-git lint guard: 0 violations across 890 files ✓
- No `.planning/STATE.md` or `.planning/ROADMAP.md` touched ✓
- No lint allowlist modifications (D-22) ✓
- One atomic commit per D-21 ✓
- 5 orphaned baseline JSON files documented for plan 09 ✓

## Decisions Made

See frontmatter `decisions` list. Highlights:

1. **Live grep at plan-08 entry, not RESEARCH pre-grep, drove the file list.** Plans 04-07's incidental cleanup drained 4 of the 8 frontmatter files before plan 08 ran. Only 4 files had active functional sites.
2. **DELETE-not-reframe for the stage/unstage describe block** in git-backend.test.ts — carries plan-07's default action. No cross-backend assertion belongs in that slot post-D-03.
3. **Reshape (not delete) the diff.nameStatus test** — it asserts on the staged-diff response shape, which is real cross-backend coverage that survives D-03. Use raw `git add` synthesis (plan-04 pattern).
4. **Verify.test.cjs simplest case** — the stage line was already redundant after plan-04's pattern shift. Delete the line, no reshape needed.
5. **3 dispatch clauses deleted from baseline-parity** — same rationale as plan 07's hook test deletions. The clauses tested verbs that no longer exist; orphan-baseline cleanup is plan 09's mandate (D-21 separation).
6. **5 baseline-definition objects deleted from capture-vcs-baselines.cjs** — keeps the one-shot generator script honest. Future re-bless runs (plan 09) won't try to regenerate baselines for verbs that don't exist.
7. **No allowlist modifications** — D-22 compliance. Plan 09 owns the final sweep.

## Deviations from Plan

**Task count reshaped (single-commit consolidation per D-21):** Plan-08 listed Task 1 (stage callers) + Task 2 (unstage callers) + Task 3 (commit). All three landed in a single atomic commit `378241e9` per D-21's "one dedicated commit per dimension" rule. The plan's Task 3 explicitly describes the single-commit pattern; Tasks 1 and 2 are logical groupings within the single commit, not separate commits.

**Smaller file list than frontmatter listed:** Plan-08 frontmatter listed 8 files; live grep at entry found active sites in only 4. Plans 04-07's incidental cleanup drained the other 4 (`tests/helpers.cjs`, `tests/commands.test.cjs`, `tests/commit-files-deletion.test.cjs`, `get-shit-done/bin/lib/commands.cjs`). This is documented in the per-file breakdown above and is **not a deviation from the plan's intent** — the plan's preamble explicitly notes "Some sites may already be 0 due to plans 04-07 incidental cleanup. At plan 08 start, executor re-greps to get the actual remaining set." Live grep is the source of truth.

**No Rule 4 architectural decisions surfaced.** All 9 site reshapes are mechanical applications of Pattern E or DELETE-not-reframe. No new threat flags. No auth gates.

## Auth Gates

None. Plan was fully autonomous.

## Issues Encountered

None.

## Threat Surface Audit

Per PLAN-08's `<threat_model>`:

- **T-2.1.08-01 (Tampering — fs.unlinkSync in tests)** — N/A. No `fs.unlinkSync` calls added in this plan (the unstage analog from Pattern E was not needed; the 1 unstage site was inside a deleted describe block, not a Pattern E target).
- **T-2.1.08-02 (Information Disclosure — unintended file deletion outside fixture)** — N/A (same as T-2.1.08-01).
- **T-2.1.08-03 (Tampering — CLI cmdUnstage removal)** — N/A. No CLI `cmdUnstage` command exists in `get-shit-done/bin/lib/commands.cjs`; the 1 unstage site that plan-08's preamble expected to find there was already removed by plan 04's cmdCommit `if(deletion){rm-cached}else{add}` collapse. No README/help-text updates needed.
- **T-2.1.08-SC (npm installs)** — N/A.

No new threat flags surfaced during execution.

## Plan-09 Handoff

The remaining D-21 dimensions on `phase/2.1-audit` after plan 08:

### 1. Baseline-parity JSON re-bless + orphan cleanup (plan 09 primary mandate)

Orphaned baseline JSON files to delete (no surviving dispatch clause):

```
tests/baselines/git-vcs/commit-ts-148-add.snap.json
tests/baselines/git-vcs/commit-ts-294-add-c-form.snap.json
tests/baselines/git-vcs/commands-cjs-330-rm-cached.snap.json
tests/baselines/git-vcs/commands-cjs-332-add.snap.json
tests/baselines/git-vcs/commands-cjs-398-add.snap.json
```

Plan-06's handoff also flagged baseline-parity failures in dimensions resolved by intervening plans:
- `worktree-safety-cjs-122-rev-parse-git-dir` and `worktree-safety-cjs-123-rev-parse-common-dir` — resolved by plan 06's D-18 migration; verify the baselines still match the post-D-18 args shape and re-bless if needed.

Re-bless mechanical step:
```
node tests/__tools__/capture-vcs-baselines.cjs
# Then inspect git status — baselines for sites that changed shape will show diffs.
git add tests/baselines/git-vcs/
git commit -m "chore(2.1-09): re-bless baselines + delete orphans"
```

### 2. Lint allowlist sweep (D-22 closing)

Plan-08 deferred all allowlist edits to plan 09. Candidates to audit:

- `sdk/src/vcs/__tests__/git-backend.test.ts` — now has one new raw `execSync('git add -- …')`; the file is already on the allowlist via both the file entry and the `sdk/src/vcs/__tests__/**` glob. No change needed but worth confirming the entry still warrants the line.
- `tests/__tools__/capture-vcs-baselines.cjs` — surviving baseline-definition objects still legitimately invoke raw git for capture; entry stays.
- Files retired from the allowlist by phase 2 completion: revisit per the original D-17/D-18 narrowing intent.

Reference: `scripts/lint-vcs-no-raw-git.allow.json` + the `$comment` field documenting per-phase narrowing intent.

### 3. LEARNINGS document

Capture phase-2.1 LEARNINGS in `.planning/phases/2.1-vcs-abstraction-audit-drop-git-only-concepts/2.1-LEARNINGS.md`:
- Pattern E collapse coverage (~30 sites across plans 04 + 08)
- DELETE-not-reframe default for hard-removed verbs (plans 07 + 08)
- Test-only raw-git synthesis pattern (plan 04 + plan 08)
- One-commit-per-D-21-dimension cadence consistency

### 4. Carry-forward (not plan 09's scope)

- **#3061 design tension** — plan-04 deferred. Three failing tests in `commit.test.ts` named-files-scope describe block. Resolved by plan 04's #3061 fix-up commit `7f4c8ffa` (pre-add index reset in git backend). Should now pass; plan 09 can re-run the failing trio to confirm.
- **`tests/worktree-safety-policy.test.cjs:231/266`** — pre-existing, unrelated to D-03; carry forward.

### Plan-09 entry pre-grep recipe

```
# Confirm D-03 closure (should be zero):
grep -rnE "vcs\.(stage|unstage)\(" --include="*.ts" --include="*.cjs" \
  | grep -v node_modules | grep -v dist-cjs | grep -v "\.planning/"

# Find any other lint allowlist candidates:
node scripts/lint-vcs-no-raw-git.cjs

# Re-bless baselines after orphan deletion:
ls tests/baselines/git-vcs/ | wc -l        # count before
rm tests/baselines/git-vcs/{commit-ts-148-add,commit-ts-294-add-c-form,commands-cjs-330-rm-cached,commands-cjs-332-add,commands-cjs-398-add}.snap.json
node tests/__tools__/capture-vcs-baselines.cjs
ls tests/baselines/git-vcs/ | wc -l        # count after — should be (before - 5)
```

## Known Stubs

None. All edits are real deletions or real reshapes; no placeholder data, no skipped tests, no TODO markers introduced.
