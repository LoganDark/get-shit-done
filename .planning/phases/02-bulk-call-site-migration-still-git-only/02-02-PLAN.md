---
phase: 02-bulk-call-site-migration-still-git-only
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/helpers.cjs
  - scripts/lint-vcs-no-raw-git.allow.json
  - sdk/src/vcs/jj/.gitkeep
autonomous: true
requirements:
  - TEST-05
  - UPSTREAM-02
  - MIGR-03
must_haves:
  truths:
    - "tests/helpers.cjs::createTempGitProject's signature is unchanged; all 14 callers continue to work without modification."
    - "Day-one allowlist shrink removes 9 entries from scripts/lint-vcs-no-raw-git.allow.json (1 broad bin/lib glob + 7 sdk/src/query/*.ts entries + sdk/src/init-runner.ts)."
    - "After the shrink, every still-raw-git source file in scope (bin/lib/*.cjs, sdk/src/query/*.ts, sdk/src/init-runner.ts) is a lint violation — the allowlist is the live progress tracker (D-14)."
    - "sdk/src/vcs/jj/ directory exists as a zero-conflict surface for Phase 3 (UPSTREAM-02 / D-15)."
    - "Phase 2's long-lived branch phase/02-migration carries broken-lint state intentionally — main stays green per D-12."
  artifacts:
    - path: tests/helpers.cjs
      provides: "Adapter-aware createTempGitProject; raw-git inside this file is allowlist-permitted bootstrap only."
      contains: "createVcsAdapter"
    - path: scripts/lint-vcs-no-raw-git.allow.json
      provides: "Shrunk allowlist matching post-Phase-2 steady state."
      contains: "sdk/src/vcs/exec.ts"
    - path: sdk/src/vcs/jj/.gitkeep
      provides: "Empty zero-conflict sidecar surface."
      min_lines: 1
  key_links:
    - from: tests/helpers.cjs (_loadVcs lazy getter)
      to: sdk/src/vcs/index.js (createVcsAdapter)
      via: require('../sdk/dist-cjs/vcs/index.js')
      pattern: "require\\('\\.\\./sdk/dist-cjs/vcs"
    - from: scripts/lint-vcs-no-raw-git.allow.json (post-shrink)
      to: lint scanner (scripts/lint-vcs-no-raw-git.cjs)
      via: globs and files arrays
      pattern: "sdk/src/vcs/(exec\\.ts|backends/git\\.ts|parse/git-rev\\.ts)"
---

<objective>
Land three coordinated, mechanical changes that establish the Phase 2 working surface:
1. **Helpers migration (D-09):** `tests/helpers.cjs::createTempGitProject` becomes adapter-aware. Function signature is unchanged so 14 callers continue working. Internal raw-git remains permitted under the existing tests/helpers.cjs allowlist entry — the migration uses the adapter for post-init operations and keeps the bootstrap (`git init` + config) raw inside this allowlisted file. **Note:** full conversion to `vcs.gitOnly.init()` and `vcs.gitOnly.configSet(...)` waits for plan 02-03's gap-fill (W2 fix: 02-03 now adds `configSet` and a closing helpers-migration task that retires the remaining 4 raw-git calls in `createTempGitProject`); this plan makes `createTempGitProject` route the post-init `commit` and `add` calls through the adapter.
2. **Day-one allowlist shrink (D-13/D-14):** Remove 9 entries from `scripts/lint-vcs-no-raw-git.allow.json` — 1 broad glob `get-shit-done/bin/lib/**/*.cjs` + 7 explicit `sdk/src/query/*.ts` entries + `sdk/src/init-runner.ts`. Every still-raw-git source file becomes a lint violation immediately. The allowlist is now the live progress tracker (D-14). Lint is **broken** on `phase/02-migration`; this is intentional per D-12. CI on `phase/02-migration` only runs at PR-open time per RESEARCH §Day-One Allowlist Shrink CI behavior.
3. **Sidecar surface (D-15 / UPSTREAM-02):** Create `sdk/src/vcs/jj/.gitkeep` as a zero-conflict sidecar surface for Phase 3. Per CONTEXT D-15 and PATTERNS §Pattern 4, the planner picked `.gitkeep` over a stub TS module (matches Phase 1 precedent of `_placeholder.ts` deletion).

Purpose: D-09 — helpers migration MUST land before any per-file source/test migration in subsequent plans, otherwise per-file commits would also rewrite shared infra (forbidden by D-07/D-09). D-13 forces aggressive momentum: every file MUST migrate before `phase/02-migration` can merge to `main`.

Output: One commit per task (3 commits total) on `phase/02-migration`. Lint will report violations after this plan lands — that is by design.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-CONTEXT.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-RESEARCH.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-PATTERNS.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing _loadVcs pattern in tests/helpers.cjs (lines 183-197): -->
```javascript
let _vcsModule = null;
function _loadVcs() {
  if (_vcsModule) return _vcsModule;
  try {
    _vcsModule = require('../sdk/dist-cjs/vcs/index.js');
  } catch (err) {
    throw new Error('VCS adapter not built. Run: pnpm -F sdk build:cjs ...');
  }
  return _vcsModule;
}
```

<!-- Adapter consumption surface (Phase 1, sdk/src/vcs/index.ts): -->
```typescript
export function createVcsAdapter(cwd: string, opts?: { kind?: 'git' | 'jj' }): VcsAdapter;
// VcsAdapter = GitVcsAdapter | JjVcsAdapter (discriminated union)
// vcs.commit({ files, message }): ExecResult
```

<!-- Current allowlist shape (scripts/lint-vcs-no-raw-git.allow.json): -->
```json
{
  "globs": ["get-shit-done/bin/lib/**/*.cjs", "sdk/src/vcs/__tests__/**", "tests/**/*.test.cjs", "..."],
  "files": ["sdk/src/vcs/exec.ts", "sdk/src/vcs/backends/git.ts", "tests/helpers.cjs", "..."]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate tests/helpers.cjs::createTempGitProject to adapter-aware bootstrap</name>
  <files>tests/helpers.cjs</files>
  <read_first>
    - tests/helpers.cjs (full file — read createTempGitProject at lines 86-104 and the _loadVcs lazy getter at lines 183-197)
    - sdk/src/vcs/index.ts (createVcsAdapter signature; auto-detection logic at line 38)
    - sdk/src/vcs/types.ts (VcsAdapter discriminated union; vcs.commit signature)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-RESEARCH.md (§Helpers Migration — Option B recommendation, lines 642-682)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-PATTERNS.md (§tests/helpers.cjs::createTempGitProject — exact target shape, lines 376-407)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-CONTEXT.md (D-09, D-08 — helpers migrate before per-file; mechanical-only)
    - sdk/src/vcs/__tests__/vcs-fixture.ts (lines 25-26 — canonical fixture init pattern)
  </read_first>
  <action>
**Per RESEARCH §Helpers Migration Option B (lines 658-679) and PATTERNS §tests/helpers.cjs::createTempGitProject (lines 376-407):** rewrite `tests/helpers.cjs::createTempGitProject` (lines 86-104) to consume the VcsAdapter for the post-init commit step. The function signature stays `function createTempGitProject(prefix = 'gsd-test-')` returning `tmpDir`.

**Concrete edit (preserve every line that doesn't need changing):**

The existing function performs: (a) `mkdtempSync`, (b) `mkdirSync` for `.planning/phases`, (c) raw `execSync('git init', ...)`, (d) `execSync` for user.email/user.name/commit.gpgsign config, (e) write PROJECT.md, (f) raw `execSync('git add .', ...)` and `execSync('git commit ...', ...)`. The migration changes ONLY (f) into an adapter call:

```javascript
// AFTER (within createTempGitProject, replacing the final stage+commit block):
const { createVcsAdapter } = _loadVcs();
const vcs = createVcsAdapter(tmpDir, { kind: 'git' });
vcs.commit({ files: ['.'], message: 'initial commit' });
```

Steps (a)-(e) STAY raw-git in this commit — `tests/helpers.cjs` is allowlisted (Phase 1 D-18 entry; remains in the post-Phase-2 steady-state per D-14) and step (c) `git init` requires the gap-fill verb `vcs.gitOnly.init()` which lands in plan 02-03. **A closing helpers-migration task in plan 02-03 (W2 fix from revision iteration 1) will retire the remaining 4 raw-git calls in (c)+(d) using `vcs.gitOnly.init()` + `vcs.gitOnly.configSet(...)`.** In THIS plan, only (f) migrates — D-09's pre-condition (helpers migrated before per-file plans) is satisfied because the per-file source migrations don't touch `createTempGitProject`'s internals; they call it as a black box.

**Forbidden by D-08:**
- Do NOT change the function signature.
- Do NOT add a new `vcsTest`-style helper export from this file (Option A in RESEARCH was explicitly rejected).
- Do NOT migrate steps (a)-(e). They stay raw-git inside this allowlisted file (closing migration in 02-03).
- Do NOT touch the `_loadVcs` lazy getter (lines 183-197). Use it as-is.
- Do NOT migrate other helpers in this file (e.g., `mockExecOnce`, `restoreExec`). Out of scope.

**Top-of-file imports:** Add no new imports. The `_loadVcs()` lazy-getter at line ~183 already returns the SDK module; destructure `createVcsAdapter` from it inside the function body to preserve the lazy-load semantics (per Phase 1 STATE.md note: "lazy getters defer dist-cjs require until first access, keeps pre-build guard friendly for non-VCS tests").

Commit on `phase/02-migration` with message: `refactor(02-02): tests/helpers.cjs createTempGitProject commits via VcsAdapter`.
  </action>
  <verify>
    <automated>cd sdk && pnpm build:cjs && cd .. && node --test tests/helpers.test.cjs 2>/dev/null || node --test tests/core.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "createVcsAdapter" tests/helpers.cjs` returns at least one match inside the `createTempGitProject` function body.
    - `grep -nE "vcs\\.commit\\(\\{" tests/helpers.cjs` returns at least one match inside `createTempGitProject`.
    - `grep -cE "execSync\\(['\"]git (add|commit)" tests/helpers.cjs` returns 0 (the stage+commit pair has migrated).
    - `grep -cE "execSync\\(['\"]git (init|config)" tests/helpers.cjs` returns ≥3 (init + 3 config calls REMAIN per the migration scope above; closing migration lands in 02-03).
    - The `createTempGitProject` function signature still matches `function createTempGitProject(prefix = 'gsd-test-')` (verify with `grep -nE "^function createTempGitProject" tests/helpers.cjs`).
    - At least one of the existing 14 caller test files runs end-to-end against the new helper (e.g., `node --test tests/core.test.cjs` exits 0).
    - `git diff main..HEAD -- tests/helpers.cjs` shows changes ONLY inside the createTempGitProject function body (lines ~86-104) — no edits elsewhere in the file.
  </acceptance_criteria>
  <done>`createTempGitProject` consumes the VcsAdapter for the final commit step. The 14 callers continue to work unchanged. Steps (a)-(e) remain raw-git, allowlisted under tests/helpers.cjs (D-14 steady-state); the closing migration of init+config lands in 02-03 once `vcs.gitOnly.init` and `vcs.gitOnly.configSet` exist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Day-one allowlist shrink (remove 9 entries)</name>
  <files>scripts/lint-vcs-no-raw-git.allow.json</files>
  <read_first>
    - scripts/lint-vcs-no-raw-git.allow.json (full file — current state of globs and files arrays)
    - scripts/lint-vcs-no-raw-git.cjs (the scanner — confirm how globs and files are consumed; do NOT modify the scanner)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-CONTEXT.md (D-12, D-13, D-14 — broken-lint-on-branch model + day-one shrink + live progress tracker)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-RESEARCH.md (§Day-One Allowlist Shrink, lines 683-743 — exact 9 entries to remove + steady-state list)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-PATTERNS.md (§scripts/lint-vcs-no-raw-git.allow.json — analog commit 9c1344e8, the inverse-diff)
    - .planning/phases/01-adapter-foundation-git-backend/01-CONTEXT.md (D-18 — original allowlist composition; entries to KEEP per Phase 1 design)
  </read_first>
  <action>
**Per CONTEXT D-13 and RESEARCH §Day-One Allowlist Shrink (lines 686-699):** edit `scripts/lint-vcs-no-raw-git.allow.json` to remove exactly these 9 entries. The diff is the algebraic inverse of commit `nnquxytmlsmkxklzpwulsvtnoquqpttu` "feat(01-05): add lint-vcs-no-raw-git allowlist (D-18)" (PATTERNS analog reference).

**Remove from the `globs` array:**
1. `"get-shit-done/bin/lib/**/*.cjs"` — broad glob covering all 6 hotspot files

**Remove from the `files` array (or wherever they're listed; verify exact JSON structure first):**
2. `"sdk/src/query/commit.ts"`
3. `"sdk/src/query/init.ts"`
4. `"sdk/src/query/verify.ts"`
5. `"sdk/src/query/progress.ts"`
6. `"sdk/src/query/check-ship-ready.ts"`
7. `"sdk/src/query/check-decision-coverage.ts"`
8. `"sdk/src/query/docs-init.ts"`
9. `"sdk/src/init-runner.ts"`

**Verified inventory of unmigrated TS files (read at revision time, 2026-05-09):**
- `sdk/src/query/check-decision-coverage.ts` has **1** raw-git invocation at line 385:
  `await execFile('git', ['log', '-n', String(limit), '--pretty=%s%n%b'], { cwd: projectDir, maxBuffer: 4 * 1024 * 1024 })`.
  RESEARCH §line 696 originally said "6 git subs" but a fresh grep at revision time finds only 1 site. Plan 02-06 owns its migration (per the revision applied to that plan in iteration 1).
- `sdk/src/query/docs-init.ts` has **ZERO** raw-git invocations (verified by `grep -nE "execFile|execSync|spawnSync|child_process" sdk/src/query/docs-init.ts` returning empty; the file only mentions `.git` in an exclusion-paths array and `.github/workflows/...`). **Removing its allowlist entry is benign** — the lint scanner has nothing to flag in that file. **No source migration is required for `docs-init.ts`.** This is documented to prevent a future executor from hunting for sites that don't exist; the entry removal is bookkeeping only.

**Entries that MUST REMAIN** (per Phase 1 D-18 + RESEARCH steady-state list, lines 700-725):
- All `sdk/src/vcs/...` files (`exec.ts`, `backends/git.ts`, `parse/git-rev.ts`, `parse/jj-rev.ts`, all `__tests__/**`)
- `tests/helpers.cjs`, `tests/__tools__/capture-vcs-baselines.cjs`
- `tests/vcs-adapter-contract.test.cjs`, `tests/vcs-cjs-smoke.test.cjs`
- `tests/**/*.test.cjs`, `tests/**/*.test.ts` globs
- `scripts/lint-vcs-no-raw-git.cjs`, `scripts/check-skip-count.cjs`, `scripts/run-tests.cjs`
- `scripts/base64-scan.sh`, `scripts/prompt-injection-scan.sh`, `scripts/secret-scan.sh`
- `.github/workflows/**`, `.githooks/**`, `docs/**`, `.planning/**`
- ADR markdown entries (e.g., `docs/adr/0004-worktree-workstream-seam-module.md`)

**Forbidden by D-08:**
- Do NOT add new entries.
- Do NOT reformat or reorder unrelated entries (mechanical-only edit).
- Do NOT touch `scripts/lint-vcs-no-raw-git.cjs` (the scanner).
- Do NOT add comments to the JSON (it's strict JSON; comments are not supported).

**Expected post-shrink lint state:** Running `node scripts/lint-vcs-no-raw-git.cjs` will FAIL with violations across the 6 `bin/lib/*.cjs` hotspot files + the still-unmigrated `sdk/src/query/*.ts` files (commit, init, verify, progress, check-ship-ready, check-decision-coverage) + `sdk/src/init-runner.ts`. (`docs-init.ts` will NOT add a violation because it has no raw-git to flag.) **This failure is the desired Phase 2 working state per D-12.** CI on `phase/02-migration` is not triggered on push (per RESEARCH §Day-One Allowlist Shrink CI behavior, only triggered on PR to main); the broken-lint state lives on the branch until per-file migrations close every gap.

Commit on `phase/02-migration` with message: `chore(02-02): day-one allowlist shrink — remove 9 entries (D-13)`.
  </action>
  <verify>
    <automated>node -e "const a=require('./scripts/lint-vcs-no-raw-git.allow.json'); const removed=['get-shit-done/bin/lib/**/*.cjs','sdk/src/query/commit.ts','sdk/src/query/init.ts','sdk/src/query/verify.ts','sdk/src/query/progress.ts','sdk/src/query/check-ship-ready.ts','sdk/src/query/check-decision-coverage.ts','sdk/src/query/docs-init.ts','sdk/src/init-runner.ts']; const all=[...(a.globs||[]),...(a.files||[])]; const found=removed.filter(e=>all.includes(e)); if(found.length){console.error('STILL PRESENT:',found);process.exit(1)} const kept=['sdk/src/vcs/exec.ts','sdk/src/vcs/backends/git.ts','tests/helpers.cjs']; const missing=kept.filter(e=>!all.includes(e)); if(missing.length){console.error('MISTAKENLY REMOVED:',missing);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The verification node script above prints `OK` and exits 0.
    - The 9 listed entries do NOT appear anywhere in `scripts/lint-vcs-no-raw-git.allow.json` (search both arrays).
    - The Phase 1 steady-state entries (`sdk/src/vcs/exec.ts`, `sdk/src/vcs/backends/git.ts`, `tests/helpers.cjs`, `.github/workflows/**`, etc.) DO still appear.
    - `node scripts/lint-vcs-no-raw-git.cjs` exits NON-ZERO with violations in the unallowlisted files (this is the expected broken-lint state per D-12 — record the violation list in the plan SUMMARY).
    - `git diff main..HEAD -- scripts/lint-vcs-no-raw-git.allow.json` shows ONLY removals (`-` lines) — no additions, no reorderings.
  </acceptance_criteria>
  <done>The allowlist shrunk to its post-Phase-2 steady state per D-14. Lint is now broken on `phase/02-migration` — this is the intentional D-12 forcing function. Subsequent plans must close each violation by migrating the corresponding source file (or, in `docs-init.ts`'s case, doing nothing because there is no violation to close).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create sdk/src/vcs/jj/.gitkeep zero-conflict sidecar surface</name>
  <files>sdk/src/vcs/jj/.gitkeep</files>
  <read_first>
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-CONTEXT.md (D-15 — sidecar dir + .gitkeep vs stub index.ts trade-off)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-RESEARCH.md (§Sidecar Conventions, lines 745-757 — .gitkeep recommended; exact file content)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-PATTERNS.md (§sdk/src/vcs/jj/.gitkeep, lines 450-461 — analog Phase 1 _placeholder.ts precedent)
    - .planning/phases/01-adapter-foundation-git-backend/01-CONTEXT.md (D-15-related: Phase 1's _placeholder.ts pattern that this mirrors)
    - sdk/tsconfig.json + sdk/tsconfig.cjs.json (verify the new dir doesn't break tsc include — `.gitkeep` is not a `.ts` file, so it's safe)
  </read_first>
  <action>
**Per CONTEXT D-15 + RESEARCH §Sidecar Conventions (lines 745-757):** create the directory `sdk/src/vcs/jj/` and a single file `sdk/src/vcs/jj/.gitkeep` with **exactly** this content (verbatim from RESEARCH lines 751-755 — no extra blank lines, no extra comments):

```
# UPSTREAM-02 (Phase 2 D-15): zero-conflict sidecar surface.
# Phase 3 populates with sdk/src/vcs/jj/jj.ts (the jj backend).
# Delete this file when the first real .ts module lands here.
```

**Why `.gitkeep` over a stub `index.ts`** (per RESEARCH §Pattern 4 verdict + Phase 1 precedent of `_placeholder.ts` deletion in plan 01-02): `.gitkeep` is the lowest-friction zero-conflict surface. Phase 3 deletes it on first commit that adds `sdk/src/vcs/jj/jj.ts`. A stub TS module would beg the "what was this for?" question and may need to be overwritten — adds noise to the Phase 3 first-commit diff.

**Forbidden by D-08:**
- Do NOT create any other file in `sdk/src/vcs/jj/`.
- Do NOT modify `sdk/tsconfig.cjs.json` to include the new dir (`.gitkeep` is not a `.ts` source; tsc empty-include guard does NOT fire on a non-TS file in the tree).
- Do NOT add the new dir to any package.json `files` array — the dir is empty-but-present and will populate in Phase 3.

Commit on `phase/02-migration` with message: `feat(02-02): create sdk/src/vcs/jj/ sidecar surface (UPSTREAM-02 / D-15)`.
  </action>
  <verify>
    <automated>test -d sdk/src/vcs/jj && test -f sdk/src/vcs/jj/.gitkeep && grep -q "UPSTREAM-02" sdk/src/vcs/jj/.gitkeep && cd sdk && pnpm build && pnpm build:cjs</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/src/vcs/jj/` directory exists.
    - `sdk/src/vcs/jj/.gitkeep` file exists.
    - `grep -q "UPSTREAM-02" sdk/src/vcs/jj/.gitkeep` succeeds (the file content references the requirement ID).
    - `grep -q "Phase 3" sdk/src/vcs/jj/.gitkeep` succeeds (the file content notes Phase 3 will populate).
    - `wc -l sdk/src/vcs/jj/.gitkeep` reports ≥3 (the three comment lines).
    - `wc -l sdk/src/vcs/jj/.gitkeep` reports ≤5 (no extra padding).
    - `cd sdk && pnpm build && pnpm build:cjs` both succeed (no tsc empty-include guard fires).
    - `git diff --name-only main..HEAD` lists `sdk/src/vcs/jj/.gitkeep` and no other new file in the sidecar dir.
  </acceptance_criteria>
  <done>`sdk/src/vcs/jj/` is reachable as a zero-conflict surface (UPSTREAM-02). Phase 3's first jj-backend commit will delete the `.gitkeep` and add real TS modules.</done>
</task>

</tasks>

<verification>
- All three commits land on `phase/02-migration` in order.
- `node scripts/lint-vcs-no-raw-git.cjs` exits NON-ZERO with the expected violation list (broken-lint state per D-12 — recorded in SUMMARY, not a regression).
- `cd sdk && pnpm build && pnpm build:cjs` exit 0 (sidecar dir doesn't break tsc).
- At least one downstream caller of `createTempGitProject` runs end-to-end (e.g., `node --test tests/core.test.cjs`).
- `git log --oneline phase/02-migration ^main -- tests/helpers.cjs scripts/lint-vcs-no-raw-git.allow.json sdk/src/vcs/jj/.gitkeep` shows three independent commits.
</verification>

<success_criteria>
- TEST-05 partially advanced: `createTempGitProject` (the shared bootstrap helper) is adapter-aware for its post-init commit step.
- UPSTREAM-02 satisfied: `sdk/src/vcs/jj/` exists.
- D-13 satisfied: 9 entries removed from allowlist; broken-lint state intentionally established.
- D-14 satisfied: allowlist matches its post-Phase-2 steady state for non-migration entries.
- D-12 honored: all commits on `phase/02-migration`; main untouched.
- D-08 honored: each commit's diff is mechanical and limited to declared files.
- 14 existing callers of `createTempGitProject` continue to work without modification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bulk-call-site-migration-still-git-only/02-02-SUMMARY.md` recording: (a) the helpers migration scope (post-init commit only; pre-init steps stay raw under existing allowlist; closing migration scheduled for 02-03), (b) the 9 allowlist entries removed (with the resulting lint violation count for downstream tracking; note that `docs-init.ts` removal contributes 0 new violations because the file has no raw-git), (c) confirmation `sdk/src/vcs/jj/.gitkeep` exists.
</output>
