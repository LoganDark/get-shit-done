---
phase: 02-bulk-call-site-migration-still-git-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/commit.test.ts
autonomous: true
requirements:
  - MIGR-03
must_haves:
  truths:
    - "All currently-failing tests in sdk/src/query/commit.test.ts pass when run with global commit.gpgsign=true."
    - "The fix is mechanical (D-08): only the beforeEach fixture is modified; no test bodies change."
    - "commit.test.ts migration gate (D-03/D-04) is opened — Phase 2 can plan paired migration of commit.ts + commit.test.ts in a later plan."
  artifacts:
    - path: sdk/src/query/commit.test.ts
      provides: "beforeEach with both gpgsign disablers"
      contains: "git config commit.gpgsign false"
  key_links:
    - from: sdk/src/query/commit.test.ts (beforeEach line 23-25)
      to: sdk/src/vcs/__tests__/git-backend.test.ts (lines 31-32 — canonical disabler shape)
      via: identical execSync invocation pattern
      pattern: "git config (commit|tag)\\.gpgsign false"
---

<objective>
Triage the pre-existing `sdk/src/query/commit.test.ts:304` failure by adding two mechanical fixture lines that disable git commit/tag gpgsign in the per-test tmpDir. The "fatal: failed to write commit object" error reproduced locally on 2026-05-09 (RESEARCH §commit.test.ts:304 Triage) is **environmental**, not a code-correctness bug — it fires only when the developer's global git config has `commit.gpgsign=true`, which CI machines don't. All other Phase 1 fixtures (`vcs-fixture.ts`, `git-backend.test.ts`, `helpers.cjs`) already disable both `commit.gpgsign` and `tag.gpgsign`; `commit.test.ts` was missed.

Purpose: D-03/D-04 — closing this triage gate inside Phase 2 unblocks the paired migration of `commit.ts` source + `commit.test.ts` tests in plan 02-08. Per CONTEXT D-08 mechanical-only invariant, this plan adds **only** the two fixture lines, performs **no** other refactoring, and does **not** migrate any call sites.

Output: One commit on `phase/02-migration` branch — `fix(02-01): commit.test.ts beforeEach disables gpgsign for fixture isolation`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-CONTEXT.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-RESEARCH.md
@.planning/phases/02-bulk-call-site-migration-still-git-only/02-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The exact text being added is the same shape as Phase 1's canonical disabler block. -->
<!-- Reference: sdk/src/vcs/__tests__/git-backend.test.ts lines 31-32 -->

```typescript
// Canonical disabler block (Phase 1, lifted to commit.test.ts beforeEach):
execSync('git config commit.gpgsign false', { cwd: tmpDir, stdio: 'pipe' });
execSync('git config tag.gpgsign false', { cwd: tmpDir, stdio: 'pipe' });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add gpgsign disablers to commit.test.ts beforeEach</name>
  <files>sdk/src/query/commit.test.ts</files>
  <read_first>
    - sdk/src/query/commit.test.ts (the file being modified — read the full beforeEach block at lines ~18-30 plus surrounding imports to confirm tmpDir name and execSync import)
    - sdk/src/vcs/__tests__/git-backend.test.ts (lines 25-39 — the canonical initRepo pattern; lines 31-32 are the exact text being lifted)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-RESEARCH.md (§commit.test.ts:304 Triage — root-cause analysis and the exact two lines to add)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-CONTEXT.md (D-03, D-04, D-08 — the gate this opens and the mechanical-only invariant)
    - .planning/phases/02-bulk-call-site-migration-still-git-only/02-PATTERNS.md (§commit.test.ts triage fix — pattern reference)
  </read_first>
  <action>
**Per CONTEXT D-04 (triage in 02-01) and D-08 (mechanical-only):** locate the `beforeEach` block in `sdk/src/query/commit.test.ts` (currently at lines ~18-26 per RESEARCH §commit.test.ts:304 Triage). After the existing `git config user.name "Test User"` line, insert exactly these two lines verbatim, preserving indentation:

```typescript
execSync('git config commit.gpgsign false', { cwd: tmpDir, stdio: 'pipe' });
execSync('git config tag.gpgsign false', { cwd: tmpDir, stdio: 'pipe' });
```

The `tmpDir` variable name and the `execSync` import are already in scope (verified by RESEARCH lines 605-613). Do NOT rename `tmpDir`. Do NOT collapse the four `execSync` invocations into a loop. Do NOT extract a helper. Do NOT add comments justifying the fix beyond a single-line `// Phase 2 D-03 fix: isolate fixture from global commit.gpgsign=true`. The diff is exactly 2 added lines + (optionally) 1 single-line comment.

**Forbidden by D-08:**
- Do NOT migrate any call sites in this plan. The four pre-existing `execSync('git ...')` calls in beforeEach REMAIN raw-git — they are inside `sdk/src/query/commit.test.ts` which is allowlisted as a test file by `tests/**/*.test.cjs` and `sdk/src/query/**/*.test.ts` globs.
- Do NOT touch any other file. No allowlist changes (that's plan 02-02), no helper changes, no `commit.ts` source migration.

After the edit, commit on the `phase/02-migration` branch with message `fix(02-01): commit.test.ts beforeEach disables gpgsign for fixture isolation`.
  </action>
  <verify>
    <automated>cd sdk && pnpm exec vitest run src/query/commit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "git config commit\\.gpgsign false" sdk/src/query/commit.test.ts` returns at least one match in the beforeEach block (around line 25-27).
    - `grep -nE "git config tag\\.gpgsign false" sdk/src/query/commit.test.ts` returns at least one match in the beforeEach block.
    - `cd sdk && pnpm exec vitest run src/query/commit.test.ts` exits with status 0 and reports zero failed tests (all 21 tests pass per RESEARCH §commit.test.ts:304 Triage line 631).
    - The git diff for this commit (`git diff main..HEAD -- sdk/src/query/commit.test.ts`) contains ≤4 added lines (2 execSync lines + at most 1 comment + at most 1 blank trailing line).
    - No other file changed in this commit (`git diff --name-only main..HEAD` lists only `sdk/src/query/commit.test.ts`).
    - The branch in use is `phase/02-migration` (verify with `git branch --show-current`).
  </acceptance_criteria>
  <done>The 9 currently-failing tests in `commit.test.ts` pass on a developer machine with global `commit.gpgsign=true` set. The triage gate per D-03/D-04 is open: plan 02-08 may now pair `commit.ts` source migration with `commit.test.ts` test retargeting in a single per-file commit (D-06).</done>
</task>

</tasks>

<verification>
- All 21 tests in `sdk/src/query/commit.test.ts` pass via `cd sdk && pnpm exec vitest run src/query/commit.test.ts`.
- `git diff main..HEAD -- sdk/src/query/commit.test.ts` shows only fixture additions, no test-body changes.
- The triage gate per D-03 is recorded as open in the plan SUMMARY.
</verification>

<success_criteria>
- `pnpm -F sdk exec vitest run src/query/commit.test.ts` exits 0 with 21/21 tests passing.
- D-08 mechanical-only invariant honored: the diff for this plan's single commit is purely the gpgsign-disabler additions in beforeEach.
- Plan 02-08 (commit.ts paired migration) is unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/02-bulk-call-site-migration-still-git-only/02-01-SUMMARY.md` recording the triage outcome and confirming the gate per D-03/D-04 is open.
</output>
