---
phase: 17-drift-control-reconciliation
plan: 17.01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ARCHITECTURE.md
  - docs/ja-JP/ARCHITECTURE.md
  - docs/ko-KR/ARCHITECTURE.md
  - docs/INVENTORY.md
autonomous: true
requirements: [DOCS-08]
requirements_addressed: [DOCS-08]
tags: [drift-control, docs, prose-fix, locale-parity]
must_haves:
  truths:
    - "D-04: all-strict lockstep policy upheld — en + ja-JP + ko-KR ARCHITECTURE.md prose counts move atomically in this single commit (pt-BR carved out per RESEARCH key finding #2 — no numeric prose-counts; CONTEXT D-04 lockstep applies only to locales that carry the dimensions)"
    - "D-11: strict-sequential wave ordering (1 → 2 → 3 → 4); this plan IS Wave 1, no Wave 2/3/4 work begins until this commit lands"
    - "EN docs/ARCHITECTURE.md prose counts match live filesystem (commands=68, workflows=89, agents=33, lib=59, hooks=12, references=60, installer LOC ~11,000)"
    - "ja-JP docs/ja-JP/ARCHITECTURE.md prose counts match live filesystem (commands=68, workflows=89, agents=33, installer ~11,000行)"
    - "ko-KR docs/ko-KR/ARCHITECTURE.md prose counts match live filesystem (commands=68, workflows=89, agents=33, installer ~11,000줄)"
    - "docs/INVENTORY.md `## Commands (68 shipped)` headline matches live commands/gsd/ count of 68"
    - "docs/INVENTORY.md `## Workflows (89 shipped)` headline matches live get-shit-done/workflows/ count of 89"
    - "Existing tests/inventory-counts.test.cjs passes green after this plan commits (no regression on Agents/References/CLI Modules/Hooks headlines)"
    - "pt-BR docs/pt-BR/ARCHITECTURE.md NOT modified (verified 81-line summary with zero numeric prose-counts per RESEARCH §DOCS-08)"
    - "docs/pt-BR/ARCHITECTURE.md exists and is non-empty (structural-presence preserved for Wave 2 reference)"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "EN ARCHITECTURE.md with corrected prose counts at lines 185, 267, 282, 337, 599"
      contains: "INVENTORY.md#references-60-shipped"
    - path: "docs/ja-JP/ARCHITECTURE.md"
      provides: "ja-JP ARCHITECTURE.md with corrected prose counts at lines 116, 127, 137, 427"
      contains: "**コマンド総数:** 68"
    - path: "docs/ko-KR/ARCHITECTURE.md"
      provides: "ko-KR ARCHITECTURE.md with corrected prose counts at lines 116, 127, 137, 427"
      contains: "**전체 명령어 수:** 68개"
    - path: "docs/INVENTORY.md"
      provides: "INVENTORY headline counts aligned with filesystem for Commands + Workflows"
      contains: "## Commands (68 shipped)"
  key_links:
    - from: "docs/ARCHITECTURE.md L185"
      to: "docs/INVENTORY.md#references-60-shipped"
      via: "markdown anchor link"
      pattern: "INVENTORY\\.md#references-60-shipped"
    - from: "docs/ARCHITECTURE.md L267"
      to: "docs/INVENTORY.md#hooks-12-shipped"
      via: "markdown anchor link"
      pattern: "INVENTORY\\.md#hooks-12-shipped"
    - from: "docs/ARCHITECTURE.md L282"
      to: "docs/INVENTORY.md#cli-modules-59-shipped"
      via: "markdown anchor link"
      pattern: "INVENTORY\\.md#cli-modules-59-shipped"
    - from: "docs/ARCHITECTURE.md L337"
      to: "docs/INVENTORY.md#agents-33-shipped"
      via: "markdown anchor link"
      pattern: "INVENTORY\\.md#agents-33-shipped"
---

<objective>
Wave 1 of Phase 17 — fix every prose-count drift site in ARCHITECTURE.md across en + ja-JP + ko-KR, plus the two pre-existing drift sites in INVENTORY.md (Commands headline 67→68, Workflows headline 88→89). pt-BR is NOT modified (RESEARCH key finding #2: 81-line summary with no numeric prose-counts).

Purpose: Per Pitfall 4 + CF-01 strict wave ordering, the drift FIX must commit BEFORE the drift TEST (Wave 2 / Plan 17.02) lands. Tests-first would ship day-1 red CI. After this plan commits, the filesystem-state-locked drift tests authored in 17.02 will be GREEN on land.

Output: A single commit "docs(17.01): align ARCHITECTURE.md + INVENTORY.md prose counts with filesystem (DOCS-08)" touching exactly 4 files (3 ARCHITECTURE locales + INVENTORY.md). Per D-04 all-strict locale policy: en + ja-JP + ko-KR move in lockstep atomically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-drift-control-reconciliation/17-CONTEXT.md
@.planning/phases/17-drift-control-reconciliation/17-RESEARCH.md
@.planning/phases/17-drift-control-reconciliation/17-PATTERNS.md

<interfaces>
<!-- Live filesystem counts verified 2026-05-25 — Wave 1 prose targets -->

| Dimension | Live count | Source |
|-----------|------------|--------|
| commands  | 68 | ls commands/gsd/*.md |
| workflows | 89 | ls get-shit-done/workflows/*.md |
| agents | 33 | ls agents/gsd-*.md |
| references | 60 | ls get-shit-done/references/*.md |
| lib modules (CLI) | 59 | ls get-shit-done/bin/lib/*.cjs |
| hooks | 12 | ls hooks/*.{js,sh} |
| installer LOC | 10978 | wc -l bin/install.js (rounds to ~11,000 per D-03) |

<!-- Per RESEARCH §DOCS-08 lines 419-460 — every site enumerated below -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix EN docs/ARCHITECTURE.md drift sites + INVENTORY.md headlines</name>
  <files>docs/ARCHITECTURE.md, docs/INVENTORY.md</files>
  <read_first>
    - docs/ARCHITECTURE.md (read lines 115-205, 263-285, 333-340, 595-605 — every drift site)
    - docs/INVENTORY.md (read lines 1-60 for `## Commands (67 shipped)`; lines 165-170 for `## Workflows (88 shipped)`)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-08 lines 431-440 (EN drift inventory table) + lines 514-520 (INVENTORY pre-existing drift note)
    - .planning/phases/17-drift-control-reconciliation/17-CONTEXT.md §D-02 (en source drift surface) + §D-03 (rounded-bucket convention for installer LOC)
  </read_first>
  <action>
    Edit `docs/ARCHITECTURE.md` at the following exact sites (per RESEARCH §DOCS-08 complete EN inventory; CONTEXT D-02/D-03 govern; lines 121/143/181 are CORRECT and MUST NOT be edited):

    1. **Line 185** — change `[INVENTORY.md](INVENTORY.md#references-41-shipped)` to `[INVENTORY.md](INVENTORY.md#references-60-shipped)`. This is an anchor reference whose live target is `## References (60 shipped)`.
    2. **Line 267** — change BOTH `INVENTORY.md#hooks-11-shipped` to `INVENTORY.md#hooks-12-shipped` AND the prose `11-hook roster` to `12-hook roster` (two replacements on the same line; verify both land).
    3. **Line 282** — change `[INVENTORY.md](INVENTORY.md#cli-modules-33-shipped)` to `[INVENTORY.md](INVENTORY.md#cli-modules-59-shipped)`.
    4. **Line 337** — three replacements on the same prose line: `21 primary agents` (KEEP — verify still says 21 primary, since 33 total − 12 advanced = 21 primary; if current text says different math, recompute), `10 advanced/specialized agents` → `12 advanced/specialized agents`, `[INVENTORY.md](INVENTORY.md#agents-31-shipped)` → `[INVENTORY.md](INVENTORY.md#agents-33-shipped)`, and the prose `31-agent roster` → `33-agent roster`. Math: 33 total − 21 primary = 12 advanced (per RESEARCH §DOCS-08 table line 439).
    5. **Line 599** — change `The installer (\`bin/install.js\`, ~10,700 lines) handles:` to `The installer (\`bin/install.js\`, ~11,000 lines) handles:`. Per D-03 nearest-1000 rounding (live 10978 → ~11,000).

    Edit `docs/INVENTORY.md` at TWO pre-existing drift sites (per RESEARCH key finding #5 + RESEARCH §DRIFT-02 line 520; verified live counts via `ls`):

    6. **Line 57** — change `## Commands (67 shipped)` to `## Commands (68 shipped)`.
    7. **Line 167** — change `## Workflows (88 shipped)` to `## Workflows (89 shipped)`.

    Do NOT edit any other lines in either file. Do NOT touch lines 121/143/181 of ARCHITECTURE.md (already correct per D-02). Do NOT renumber sections, do NOT reformat tables. Atomic prose-edit only.
  </action>
  <verify>
    <automated>node --test tests/inventory-counts.test.cjs 2>&amp;1 | tail -5</automated>
    <automated>grep -c "INVENTORY.md#references-60-shipped" docs/ARCHITECTURE.md</automated>
    <automated>grep -c "INVENTORY.md#hooks-12-shipped" docs/ARCHITECTURE.md</automated>
    <automated>grep -c "INVENTORY.md#cli-modules-59-shipped" docs/ARCHITECTURE.md</automated>
    <automated>grep -c "INVENTORY.md#agents-33-shipped" docs/ARCHITECTURE.md</automated>
    <automated>grep -c "~11,000 lines" docs/ARCHITECTURE.md</automated>
    <automated>grep -cE "^## Commands \(68 shipped\)" docs/INVENTORY.md</automated>
    <automated>grep -cE "^## Workflows \(89 shipped\)" docs/INVENTORY.md</automated>
    <automated>grep -c "references-41-shipped\|hooks-11-shipped\|cli-modules-33-shipped\|agents-31-shipped\|~10,700 lines" docs/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/inventory-counts.test.cjs` exits 0 (green; INVENTORY headline drift fixed)
    - Each forward-grep count equals 1 (every replacement landed): references-60-shipped, hooks-12-shipped, cli-modules-59-shipped, agents-33-shipped, ~11,000 lines
    - `grep -cE "^## Commands \(68 shipped\)" docs/INVENTORY.md` returns 1
    - `grep -cE "^## Workflows \(89 shipped\)" docs/INVENTORY.md` returns 1
    - The reverse-grep on old strings (references-41-shipped, hooks-11-shipped, cli-modules-33-shipped, agents-31-shipped, ~10,700 lines) returns 0 across docs/ARCHITECTURE.md
    - `git diff --stat docs/ARCHITECTURE.md docs/INVENTORY.md` shows only the 5 EN sites + 2 INVENTORY headlines (no incidental edits)
    - Lines 121, 143, 181 of docs/ARCHITECTURE.md are UNCHANGED (verified: `grep -n "Total agents:\*\* 33" docs/ARCHITECTURE.md` still returns the same line number)
  </acceptance_criteria>
  <done>
    EN ARCHITECTURE.md + INVENTORY.md prose-counts match live filesystem. inventory-counts.test.cjs green. No drift remains in either EN file. pt-BR untouched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix ja-JP + ko-KR ARCHITECTURE.md prose-count drift (D-04 lockstep)</name>
  <files>docs/ja-JP/ARCHITECTURE.md, docs/ko-KR/ARCHITECTURE.md</files>
  <read_first>
    - docs/ja-JP/ARCHITECTURE.md (read lines 110-145 + 420-435 — the 4 drift sites)
    - docs/ko-KR/ARCHITECTURE.md (read lines 110-145 + 420-435 — the 4 drift sites)
    - docs/pt-BR/ARCHITECTURE.md (read full file to confirm 81-line summary with NO prose-counts — RESEARCH key finding #2; verifies pt-BR exclusion)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-08 lines 442-460 (ja-JP + ko-KR drift inventory tables) + §user_constraints D-04 (all-strict locale policy)
    - .planning/phases/17-drift-control-reconciliation/17-CONTEXT.md §D-04 (translations stay in lockstep with en going forward)
  </read_first>
  <action>
    Edit `docs/ja-JP/ARCHITECTURE.md` at exactly 4 sites (per RESEARCH §DOCS-08 ja-JP inventory):

    1. **Line 116** — change `**コマンド総数:** 44` to `**コマンド総数:** 68`
    2. **Line 127** — change `**ワークフロー総数:** 46` to `**ワークフロー総数:** 89`
    3. **Line 137** — change `**エージェント総数:** 16` to `**エージェント総数:** 33`
    4. **Line 427** — change `インストーラー（\`bin/install.js\`、約3,000行）は以下を処理します：` to `インストーラー（\`bin/install.js\`、約11,000行）は以下を処理します：` (per D-03 nearest-1000 rounding; live 10978 → 約11,000)

    Edit `docs/ko-KR/ARCHITECTURE.md` at exactly 4 sites (per RESEARCH §DOCS-08 ko-KR inventory):

    5. **Line 116** — change `**전체 명령어 수:** 44개` to `**전체 명령어 수:** 68개`
    6. **Line 127** — change `**전체 워크플로우 수:** 46개` to `**전체 워크플로우 수:** 89개`
    7. **Line 137** — change `**전체 에이전트 수:** 16개` to `**전체 에이전트 수:** 33개`
    8. **Line 427** — change `인스톨러(\`bin/install.js\`, ~3,000줄)는 다음을 처리합니다.` to `인스톨러(\`bin/install.js\`, ~11,000줄)는 다음을 처리합니다.`

    Do NOT modify `docs/pt-BR/ARCHITECTURE.md`. The pt-BR file is an 81-line high-level summary with NO numeric prose-counts (RESEARCH key finding #2); it stays excluded from the drift surface by structural-reality, not by policy choice. Per D-04 the lockstep policy applies only to locales that carry the dimensions.

    Do NOT add new prose-count claims to pt-BR. Do NOT renumber sections in any file. Atomic prose-edit only across en + ja-JP + ko-KR per D-04 lockstep policy.
  </action>
  <verify>
    <automated>grep -c "コマンド総数:\*\* 68" docs/ja-JP/ARCHITECTURE.md</automated>
    <automated>grep -c "ワークフロー総数:\*\* 89" docs/ja-JP/ARCHITECTURE.md</automated>
    <automated>grep -c "エージェント総数:\*\* 33" docs/ja-JP/ARCHITECTURE.md</automated>
    <automated>grep -c "約11,000行" docs/ja-JP/ARCHITECTURE.md</automated>
    <automated>grep -c "전체 명령어 수:\*\* 68개" docs/ko-KR/ARCHITECTURE.md</automated>
    <automated>grep -c "전체 워크플로우 수:\*\* 89개" docs/ko-KR/ARCHITECTURE.md</automated>
    <automated>grep -c "전체 에이전트 수:\*\* 33개" docs/ko-KR/ARCHITECTURE.md</automated>
    <automated>grep -c "~11,000줄" docs/ko-KR/ARCHITECTURE.md</automated>
    <automated>grep -cE "コマンド総数:\*\* 44|ワークフロー総数:\*\* 46|エージェント総数:\*\* 16|約3,000行" docs/ja-JP/ARCHITECTURE.md</automated>
    <automated>grep -cE "전체 명령어 수:\*\* 44개|전체 워크플로우 수:\*\* 46개|전체 에이전트 수:\*\* 16개|~3,000줄" docs/ko-KR/ARCHITECTURE.md</automated>
    <automated>test -s docs/pt-BR/ARCHITECTURE.md &amp;&amp; echo PT_BR_EXISTS</automated>
    <automated>git diff --name-only docs/pt-BR/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - Forward-grep counts all return 1 for ja-JP: コマンド総数:** 68, ワークフロー総数:** 89, エージェント総数:** 33, 約11,000行
    - Forward-grep counts all return 1 for ko-KR: 전체 명령어 수:** 68개, 전체 워크플로우 수:** 89개, 전체 에이전트 수:** 33개, ~11,000줄
    - Reverse-grep on old strings returns 0 across ja-JP file (no 44/46/16/3,000 prose remains)
    - Reverse-grep on old strings returns 0 across ko-KR file
    - `test -s docs/pt-BR/ARCHITECTURE.md` succeeds (file still exists, non-empty)
    - `git diff --name-only docs/pt-BR/ARCHITECTURE.md` outputs nothing (pt-BR NOT modified)
    - `git diff --stat docs/ja-JP/ARCHITECTURE.md docs/ko-KR/ARCHITECTURE.md` shows only the 4 lines edited per locale
  </acceptance_criteria>
  <done>
    ja-JP + ko-KR ARCHITECTURE.md prose-counts match live filesystem. pt-BR UNCHANGED. All 3 active prose-count-carrying locales now in lockstep with en (D-04 all-strict policy satisfied). Wave 2 (17.02) can ship day-1 GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Pure docs+prose edits across 4 files; no executable surface; no I/O, no network, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.01-01 | Information Disclosure | docs/INVENTORY.md + docs/ARCHITECTURE.md prose | accept | Files are already public; this plan adjusts counts to match the already-public filesystem. No new disclosure surface. |
| T-17.01-02 | Tampering | locale parity (D-04 all-strict) | mitigate | grep gates in Task 2 verify each locale edit landed exactly; reverse-grep gates ensure old prose was fully replaced (no stale counts surviving in any locale). |

**No in-scope threats requiring new mitigations.** Phase 17 is a pure docs+tests+reconciliation phase (CONTEXT §code_context: "No new SDK surface, no new CLI bridge, no new lint, no new agents"). No new attack surface introduced.
</threat_model>

<verification>
## Plan-level verification

1. `node --test tests/inventory-counts.test.cjs` exits 0 (covers Agents/Commands/Workflows/References/CLI Modules/Hooks headlines all green post-edit).
2. No old prose count strings remain in en/ja-JP/ko-KR ARCHITECTURE.md (reverse-grep gates return 0).
3. `docs/pt-BR/ARCHITECTURE.md` UNCHANGED (no git diff).
4. Single commit "docs(17.01): align ARCHITECTURE.md + INVENTORY.md prose counts with filesystem (DOCS-08)" touching exactly 4 files.
5. Wave 1 commit has landed; Wave 2 (Plan 17.02) gate is open.
</verification>

<success_criteria>
- DOCS-08 closed: en + ja-JP + ko-KR ARCHITECTURE.md prose-count drift eliminated atomically per D-04 all-strict policy.
- INVENTORY.md Commands + Workflows headlines aligned with live filesystem (pre-existing drift swept alongside per RESEARCH §DRIFT-02 line 520 + RESEARCH key finding #5).
- `tests/inventory-counts.test.cjs` GREEN (no regression on the existing inventory headline check).
- pt-BR file UNCHANGED (no incidental prose introduced).
- Wave 2 (Plan 17.02) can author the drift tests with confidence they ship day-1 GREEN per Pitfall 4.
</success_criteria>

<output>
Create `.planning/phases/17-drift-control-reconciliation/17-01-SUMMARY.md` when done.
</output>
