---
phase: 17-drift-control-reconciliation
plan: 17.04
type: execute
wave: 4
depends_on: [17.03]
files_modified:
  - .planning/intel/project-validated-truth.md
  - .planning/PROJECT.md
autonomous: true
requirements: [PROJECT-01]
requirements_addressed: [PROJECT-01]
tags: [reconciliation, truth-source, project-md, two-pass]
decisions:
  - id: PROJECT-01-form
    summary: "Truth source ships as INLINE researcher computation (NOT a permanent `scripts/reconcile-project-validated.cjs` script)."
    rationale: "REQUIREMENTS.md PROJECT-01 explicitly says 'One-shot prose sweep — NOT a recurring SDK verb (YAGNI; no second consumer)'. RESEARCH §user_constraints + §Don't Hand-Roll + §PROJECT-01 reusability analysis (lines 723-732) all converge: inline computation is the correct YAGNI fit. Permanent script form remains a v1.5+ candidate if a second consumer emerges. Out of scope for Phase 17 either way per REQUIREMENTS.md OOS clause."
must_haves:
  truths:
    - "17.03 committed before this plan starts — DOCS-09 close-gate passed at ≥ 99% pass rate"
    - "Wave 4 runs LAST per IP-4 — every v1.4 REQ-ID that landed in Waves 1-3 (DOCS-08, DRIFT-01, DRIFT-02, DOCS-01..07, DOCS-09) appears in the truth source"
    - ".planning/intel/project-validated-truth.md exists (machine pass; one bullet per REQ-ID with status from STATE.md + milestones/v*-REQUIREMENTS.md)"
    - "PROJECT.md `### Validated` section reconciled (human pass; cites truth file; preserves hand-curated parentheticals per CF-06 + Pitfall 8)"
    - "Two-pass approach satisfied: machine pass commits FIRST, human pass commits SECOND (per Pitfall 8 — RESEARCH §Architecture Patterns Wave 4 flow)"
    - "v1.4 REQ-ID full roster represented (27 total): PARALLEL-08, MERGE-08, NAMING-01, VCS-21, VCS-22, PARALLEL-07, LINT-06, CLEANUP-02, DOCS-08, DRIFT-01, DRIFT-02, DOCS-01..07, DOCS-09, PROJECT-01, CLEANUP-01, CLEANUP-03..07, TEST-17"
    - "Phase 18 REQ-IDs (CLEANUP-01, CLEANUP-03..07, TEST-17) listed as ⏳ Pending (not ✓ Validated) per IP-4 — they don't claim shipped state when run from Wave 4 of Phase 17"
    - "MERGE-08 listed as Deferred-by-design per REQUIREMENTS.md OOS clause"
    - "PROJECT.md is NOT regenerate-overwrite output — narrative authored by human pass citing the structured truth file"
    - "Hand-curated parentheticals like '(caveat: A3 colocated pre-commit gap remains open, see Active)' preserved verbatim from prior PROJECT.md text where they still apply"
  artifacts:
    - path: ".planning/intel/project-validated-truth.md"
      provides: "Machine-generated truth source for PROJECT.md ### Validated reconciliation"
      contains: "PROJECT-VALIDATED-TRUTH"
      min_lines: 60
    - path: ".planning/PROJECT.md"
      provides: "Updated ### Validated section citing the truth source, preserving parentheticals"
      contains: "### Validated"
  key_links:
    - from: ".planning/PROJECT.md ### Validated"
      to: ".planning/intel/project-validated-truth.md"
      via: "citation in narrative"
      pattern: "project-validated-truth\\.md"
    - from: ".planning/intel/project-validated-truth.md"
      to: ".planning/REQUIREMENTS.md + .planning/milestones/v*-REQUIREMENTS.md"
      via: "REQ-ID enumeration"
      pattern: "v1\\.[1-4]"
---

<objective>
Wave 4 of Phase 17 — reconcile `.planning/PROJECT.md` `### Validated` section against MILESTONES.md + per-milestone REQUIREMENTS files + STATE.md current snapshot. Runs LAST per IP-4 so v1.4's OWN REQ-IDs that landed in Waves 1-3 (DOCS-08, DRIFT-01, DRIFT-02, DOCS-01..07, DOCS-09) appear in the reconciled truth source.

Purpose: Two-pass reconciliation per Pitfall 8 + CF-06. Pass 1 (machine) produces a structured `.planning/intel/project-validated-truth.md` enumerating every REQ-ID across v1.0..v1.4 with status. Pass 2 (human) edits PROJECT.md `### Validated` narrative to cite the truth source while PRESERVING hand-curated parentheticals (e.g., "(caveat: A3 colocated pre-commit gap remains open, see Active)"). PROJECT.md is NOT regenerate-overwrite output.

Output: Two commits — one for each pass. Plan decision `PROJECT-01-form`: inline researcher computation (NOT a permanent `scripts/reconcile-project-validated.cjs` script) per REQUIREMENTS.md OOS clause + YAGNI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.1-REQUIREMENTS.md
@.planning/milestones/v1.2-REQUIREMENTS.md
@.planning/milestones/v1.3-REQUIREMENTS.md
@.planning/phases/17-drift-control-reconciliation/17-CONTEXT.md
@.planning/phases/17-drift-control-reconciliation/17-RESEARCH.md
@.planning/phases/17-drift-control-reconciliation/17-PATTERNS.md
@.planning/phases/17-drift-control-reconciliation/17-03-SUMMARY.md
@.planning/intel/vcs-adapter-surface-audit.md
@.planning/intel/id-namespace-audit.md

<interfaces>
<!-- Complete v1.4 REQ-ID roster (27 total — RESEARCH §PROJECT-01 line 719) -->

PARALLEL-08, MERGE-08, NAMING-01, VCS-21, VCS-22, PARALLEL-07, LINT-06, CLEANUP-02, DOCS-08, DRIFT-01, DRIFT-02, DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06, DOCS-07, DOCS-09, PROJECT-01, CLEANUP-01, CLEANUP-03, CLEANUP-04, CLEANUP-05, CLEANUP-06, CLEANUP-07, TEST-17

<!-- Status taxonomy (mirrors STATE.md Phase Status conventions) -->

✅ Validated (shipped + verified)
🟡 Active (in-progress; current phase)
⏳ Pending (not yet shipped; future phase)
⏸️ Deferred-by-design (intentionally not shipped; v1.5+ candidate)
❌ Rejected (decided against)

<!-- Truth file template skeleton (RESEARCH §PROJECT-01 lines 660-715) -->

# PROJECT-VALIDATED-TRUTH

**Generated:** 2026-05-25
**Generator:** Phase 17.04 inline computation (NOT a recurring script — REQUIREMENTS.md PROJECT-01 OOS clause)
**Source:**
- .planning/REQUIREMENTS.md (v1.4 active milestone)
- .planning/milestones/v{1.1,1.2,1.3}-REQUIREMENTS.md
- .planning/STATE.md (status snapshot)
- .planning/MILESTONES.md (shipped-milestone summaries)

## v1.0 — Dual-backend foundation
- ✓ VCS-01..07 — Adapter foundation
- ... [one bullet per REQ-ID]

## v1.1 — first upstream sync
- ✓ VCS-08..15 — 8 new VcsAdapter verbs
- ... [enumerate]

## v1.2 — unified revision model
- ✓ AUDIT-01..04 ...
- ...

## v1.3 — jj octopus merge for subagents
- ✓ PARALLEL-01..06, VCS-16..20 ...
- ...

## v1.4 — Clean, consistent state for next upstream pull
### Shipped (as of 2026-05-25)
- ✓ PARALLEL-08, NAMING-01, VCS-21, VCS-22, PARALLEL-07, LINT-06, CLEANUP-02, DOCS-08, DRIFT-01, DRIFT-02, DOCS-01..07, DOCS-09, PROJECT-01
### Pending (Phase 18)
- ⏳ CLEANUP-01, CLEANUP-03..07, TEST-17
### Deferred-by-design (v1.5+)
- ⏸️ MERGE-08
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass 1 (machine) — Write .planning/intel/project-validated-truth.md</name>
  <files>.planning/intel/project-validated-truth.md</files>
  <read_first>
    - .planning/MILESTONES.md (read full file — for per-milestone REQ-ID summaries)
    - .planning/REQUIREMENTS.md (read full file — v1.4 REQ-ID roster + Traceability table; this is the authoritative roster)
    - .planning/milestones/v1.1-REQUIREMENTS.md (read full file — v1.1 REQ-ID roster)
    - .planning/milestones/v1.2-REQUIREMENTS.md (read full file — v1.2 REQ-ID roster)
    - .planning/milestones/v1.3-REQUIREMENTS.md (read full file — v1.3 REQ-ID roster)
    - .planning/STATE.md (read § Decisions + § Pending Todos + § Deferred Items — current REQ-ID status snapshot)
    - .planning/PROJECT.md (read § Validated section in full — to identify existing parentheticals that will be preserved in Pass 2)
    - .planning/intel/vcs-adapter-surface-audit.md (analog shape — table form + verdict legend + sourcing rules)
    - .planning/intel/id-namespace-audit.md (alternative analog — header + summary-counts pattern)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §PROJECT-01 lines 658-732 (full truth-file skeleton with all 5 milestone sections drafted + reusability analysis)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §.planning/intel/project-validated-truth.md lines 244-289 (analog at vcs-adapter-surface-audit.md + column shape + sourcing rules)
    - .planning/ROADMAP.md (full read — for per-milestone phase enumeration + Phase 14.1 emergency placement)
  </read_first>
  <action>
    Create `.planning/intel/project-validated-truth.md` as a machine-pass structured truth source. Use the skeleton from RESEARCH §PROJECT-01 lines 660-715 verbatim, customized with the actual REQ-ID lists from `.planning/REQUIREMENTS.md` + per-milestone files.

    Required structure:

    1. **Header** (per RESEARCH skeleton):
       - `# PROJECT-VALIDATED-TRUTH`
       - `**Generated:** 2026-05-25`
       - `**Generator:** Phase 17.04 inline computation (NOT a recurring script — REQUIREMENTS.md PROJECT-01 OOS clause; see plan decision PROJECT-01-form)`
       - `**Source:**` list (REQUIREMENTS files, STATE.md, MILESTONES.md)

    2. **Per-milestone sections** (one `## v{X.Y} — {name}` heading each):
       - `## v1.0 — Dual-backend foundation` — enumerate REQ-IDs from v1.0 (read MILESTONES.md or v1.0-equivalent source). Per RESEARCH §PROJECT-01 line 675, v1.0 covers categories like VCS-01..07, GIT-01..03, MIGR-01..04, JJ-01..07, SQUASH-01..07, REFS-01..06, CONFLICT-01..03, WS-01..13, HOOK-01..05, CMD-01..11, PROMPT-01..03, BROWN-01..02, GREEN-01..03, TEST-01..08, UPSTREAM-01..03, CI-01..04. Verify the exact roster against MILESTONES.md before committing.
       - `## v1.1 — first upstream sync` — enumerate v1.1 REQ-IDs (VCS-08..15, WAVE-01, PROMPT-04, MIGR-05, TEST-09..11 per RESEARCH §PROJECT-01 line 681). Verify against `.planning/milestones/v1.1-REQUIREMENTS.md`.
       - `## v1.2 — unified revision model` — verify against `.planning/milestones/v1.2-REQUIREMENTS.md`. RESEARCH cites AUDIT-01..04, FLIP-01..04, LINT-01..03, PROMPT-05, TEST-12, MIGR-06.
       - `## v1.3 — jj octopus merge for subagents` — verify against `.planning/milestones/v1.3-REQUIREMENTS.md`. RESEARCH cites PARALLEL-01..06, VCS-16..20, TEST-13..16, HOOK-06..07, CI-05..06, LINT-04..05, CONFIG-01..02, PROMPT-06..09, DOGFOOD-01..02.
       - `## v1.4 — Clean, consistent state for next upstream pull (CURRENT MILESTONE)` — three sub-sections:
         - `### Shipped (as of 2026-05-25)` — ✓-marked: PARALLEL-08 (Phase 14.1), NAMING-01 (15.01), VCS-21 (15.02), VCS-22 (15.03), PARALLEL-07 (15.04), LINT-06 (16.01), CLEANUP-02 (16.02), DOCS-08 (17.01), DRIFT-01 (17.02), DRIFT-02 (17.02), DOCS-01..07 (17.03), DOCS-09 (17.03), PROJECT-01 (17.04 — self-reference: this truth file IS the artifact for PROJECT-01)
         - `### Pending (Phase 18)` — ⏳-marked: CLEANUP-01 (18.01), CLEANUP-03..07 (18.02), TEST-17 (18.03)
         - `### Deferred-by-design (v1.5+)` — ⏸️-marked: MERGE-08

    3. Each bullet MUST include: REQ-ID, one-line description, status marker (✓/⏳/⏸️), phase (e.g., "Phase 15.01"), and closure note (commit short-id, date, or "this phase" for PROJECT-01's self-reference).

    4. The file MUST be self-contained — do NOT rely on external references for verification. The truth source itself enumerates everything PROJECT.md `### Validated` should claim.

    Re-verify per-milestone REQ-ID rosters against the actual `.planning/milestones/v{1.1,1.2,1.3}-REQUIREMENTS.md` files BEFORE finalizing. If a milestone REQUIREMENTS file enumerates additional/fewer REQ-IDs than the RESEARCH skeleton suggests, use the actual file (RESEARCH skeleton is grounded in 2026-05-25 read, but should be re-verified).

    Commit message (verbatim): `docs(17.04): emit project-validated-truth.md machine truth source (PROJECT-01 pass 1/2)`

    Do NOT modify `.planning/PROJECT.md` in this task. Two-pass: machine FIRST (this task), human SECOND (Task 2).
    Do NOT write a `scripts/reconcile-project-validated.cjs` (per plan decision PROJECT-01-form: inline computation only).
  </action>
  <verify>
    <automated>test -f .planning/intel/project-validated-truth.md</automated>
    <automated>grep -c "^# PROJECT-VALIDATED-TRUTH" .planning/intel/project-validated-truth.md</automated>
    <automated>grep -cE "^## v1\.[0-4]" .planning/intel/project-validated-truth.md</automated>
    <automated>grep -c "PARALLEL-08\|NAMING-01\|VCS-21\|VCS-22\|PARALLEL-07\|LINT-06\|CLEANUP-02" .planning/intel/project-validated-truth.md</automated>
    <automated>grep -c "DOCS-08\|DRIFT-01\|DRIFT-02\|DOCS-01\|DOCS-02\|DOCS-03\|DOCS-04\|DOCS-05\|DOCS-06\|DOCS-07\|DOCS-09\|PROJECT-01" .planning/intel/project-validated-truth.md</automated>
    <automated>grep -c "CLEANUP-01\|CLEANUP-03\|CLEANUP-04\|CLEANUP-05\|CLEANUP-06\|CLEANUP-07\|TEST-17" .planning/intel/project-validated-truth.md</automated>
    <automated>grep -c "MERGE-08" .planning/intel/project-validated-truth.md</automated>
    <automated>grep -c "⏳\|⏸️\|✓\|✅" .planning/intel/project-validated-truth.md</automated>
    <automated>wc -l .planning/intel/project-validated-truth.md</automated>
    <automated>test ! -f scripts/reconcile-project-validated.cjs</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/intel/project-validated-truth.md`
    - Title line `# PROJECT-VALIDATED-TRUTH` present (exactly 1 occurrence)
    - All 5 milestone sections present (`## v1.0`, `## v1.1`, `## v1.2`, `## v1.3`, `## v1.4`) — grep returns 5
    - All 7 v1.4 Phase 15+16 REQ-IDs present (PARALLEL-08 through CLEANUP-02): grep returns ≥7
    - All 12 Phase 17 REQ-IDs present (DOCS-08 + DRIFT-01/02 + DOCS-01..07 + DOCS-09 + PROJECT-01): grep returns ≥12
    - All 7 Phase 18 REQ-IDs present (CLEANUP-01, CLEANUP-03..07, TEST-17): grep returns ≥7
    - MERGE-08 present (deferred-by-design)
    - Status markers present (mix of ✓/✅, ⏳, ⏸️)
    - File length ≥ 60 lines (per artifact min_lines; truth source is substantive)
    - `scripts/reconcile-project-validated.cjs` does NOT exist (plan decision PROJECT-01-form enforced)
    - `git log -1 --format=%s` after this task's commit contains "PROJECT-01 pass 1/2"
  </acceptance_criteria>
  <done>
    Pass 1 (machine pass) complete. Structured truth source enumerates every REQ-ID across all 5 milestones with status. Pass 2 (human) can now cite this file from PROJECT.md narrative.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pass 2 (human) — Reconcile .planning/PROJECT.md ### Validated narrative</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (full read — focus on the `### Validated` section and every hand-curated parenthetical inside it, e.g., "(caveat: A3 colocated pre-commit gap remains open, see Active)")
    - .planning/intel/project-validated-truth.md (the just-committed Pass 1 output — the source of truth this narrative cites)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §Pitfall 8 lines 377-388 (two-pass narrative; parenthetical preservation requirement)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §.planning/PROJECT.md `### Validated` lines 397-407 (two-pass pattern + anti-pattern)
    - .planning/phases/17-drift-control-reconciliation/17-CONTEXT.md §CF-06 (PROJECT.md is NOT regenerate-overwrite output)
  </read_first>
  <action>
    Edit `.planning/PROJECT.md` `### Validated` section to align with `.planning/intel/project-validated-truth.md` (Pass 1 output) while PRESERVING the hand-curated narrative voice and parentheticals.

    Required edits:

    1. **Cite the truth file** — add a leading sentence (or footer note, planner discretion) inside the `### Validated` section that points to `.planning/intel/project-validated-truth.md` as the structured source: e.g., "Per-REQ-ID truth source: `.planning/intel/project-validated-truth.md` (machine pass, Phase 17.04)."
    2. **Add missing REQ-IDs** — for every REQ-ID in the truth file's "Shipped" lists that is currently absent from PROJECT.md `### Validated`, add a bullet. New REQ-IDs to add (v1.4 specifically): NAMING-01, VCS-21, VCS-22, PARALLEL-07 (Phase 15); LINT-06, CLEANUP-02 (Phase 16); DOCS-08, DRIFT-01, DRIFT-02, DOCS-01..07, DOCS-09 (Phase 17); PROJECT-01 (this phase — self-reference; the addition of this bullet IS the act that closes PROJECT-01). Phase 14.1's PARALLEL-08 may already be present (verify by reading current `### Validated`); if absent, add.
    3. **Mark Phase 18 REQ-IDs as Pending** — for CLEANUP-01, CLEANUP-03..07, TEST-17 use ⏳ status markers consistent with the truth file. Do NOT claim them as shipped.
    4. **Mark MERGE-08 as Deferred-by-design** — ⏸️ status marker consistent with the truth file + REQUIREMENTS.md OOS clause.
    5. **PRESERVE hand-curated parentheticals** verbatim. Examples from CONTEXT/RESEARCH:
       - `(caveat: A3 colocated pre-commit gap remains open, see Active)` — if this parenthetical exists in current PROJECT.md AND the A3 caveat still applies (verify against STATE.md — Phase 12 closed A3, so this caveat may no longer be appropriate; if Phase 12 fully closed it, the parenthetical may be retired with a note). The principle is: preserve hand-written narrative voice; don't robotically strip parentheticals.
       - Any other parentheticals like "(closed in Phase X)", "(see issue #N)", "(per IP-N)" — preserve verbatim where the underlying claim still holds.
    6. **PRESERVE existing prose voice** — the rest of PROJECT.md outside `### Validated` is UNCHANGED. The narrative within `### Validated` is augmented, not regenerated.

    **Anti-pattern to avoid** (RESEARCH §Anti-Patterns + §Pitfall 8): do NOT replace the entire `### Validated` section with a flat bulleted list lifted from the truth file. The human pass preserves narrative voice; the machine pass already produced the structured form.

    Commit message (verbatim): `docs(17.04): reconcile PROJECT.md ### Validated against truth source (PROJECT-01 pass 2/2)`

    Do NOT touch any other section of PROJECT.md. Do NOT touch the truth file in this task (Pass 1 is sealed).
    Do NOT add a `## Tests` family heading anywhere.
  </action>
  <verify>
    <automated>grep -c "^### Validated" .planning/PROJECT.md</automated>
    <automated>grep -c "project-validated-truth\.md" .planning/PROJECT.md</automated>
    <automated>grep -c "NAMING-01\|VCS-21\|VCS-22\|PARALLEL-07" .planning/PROJECT.md</automated>
    <automated>grep -c "LINT-06\|CLEANUP-02" .planning/PROJECT.md</automated>
    <automated>grep -c "DOCS-08\|DRIFT-01\|DRIFT-02\|DOCS-01\|DOCS-09\|PROJECT-01" .planning/PROJECT.md</automated>
    <automated>grep -c "CLEANUP-01\|CLEANUP-03\|TEST-17" .planning/PROJECT.md</automated>
    <automated>grep -c "MERGE-08" .planning/PROJECT.md</automated>
    <automated>git diff --stat .planning/PROJECT.md | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Validated" .planning/PROJECT.md` returns ≥1 (section heading preserved/present)
    - `grep -c "project-validated-truth.md" .planning/PROJECT.md` returns ≥1 (truth file cited in narrative)
    - All v1.4 shipped REQ-IDs (Phase 15: NAMING-01, VCS-21, VCS-22, PARALLEL-07; Phase 16: LINT-06, CLEANUP-02) present in PROJECT.md
    - All Phase 17 REQ-IDs (DOCS-08, DRIFT-01, DRIFT-02, DOCS-01..07, DOCS-09, PROJECT-01) present in PROJECT.md
    - Phase 18 REQ-IDs (CLEANUP-01, CLEANUP-03..07, TEST-17) present and marked Pending (not Validated)
    - MERGE-08 present and marked Deferred-by-design (not Validated)
    - `git diff --stat` shows the change is bounded — the rest of PROJECT.md outside `### Validated` has not been touched
    - `git log -1 --format=%s` contains "PROJECT-01 pass 2/2"
  </acceptance_criteria>
  <done>
    PROJECT-01 closed. Two-pass reconciliation complete: machine truth source at `.planning/intel/project-validated-truth.md` + human-edited `.planning/PROJECT.md ### Validated` citing it. Hand-curated parentheticals preserved per Pitfall 8 + CF-06. IP-4 satisfied — v1.4's own REQ-IDs (closed in Waves 1-3) land in the truth source because this plan runs LAST.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Two-pass docs reconciliation across 2 files (intel artifact + PROJECT.md). No executable surface; no I/O; no network. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.04-01 | Tampering | hand-curated narrative voice in PROJECT.md | mitigate | Task 2 acceptance criteria specifically require preserving parentheticals (CF-06 + Pitfall 8). The `git diff --stat` check ensures the rest of PROJECT.md outside `### Validated` is untouched. |
| T-17.04-02 | Information Disclosure | truth file under .planning/intel/ | accept | `.planning/intel/` is already a public artifact directory (analogous to existing files like `id-namespace-audit.md`, `vcs-adapter-surface-audit.md`); the truth source enumerates REQ-IDs and statuses, all of which are already in REQUIREMENTS.md / STATE.md. No new disclosure. |
| T-17.04-03 | Repudiation | per-pass commit granularity | mitigate | Two atomic commits (machine pass + human pass) per Pitfall 8 two-pass framing. Each commit is recoverable independently; the human pass commit can be reverted without losing the machine pass. |

**No in-scope threats requiring new mitigations.** Pure docs+reconciliation phase per CONTEXT §code_context.
</threat_model>

<verification>
## Plan-level verification

1. `.planning/intel/project-validated-truth.md` exists and enumerates every REQ-ID across all 5 milestones with status markers (Task 1 acceptance gates).
2. `.planning/PROJECT.md ### Validated` cites the truth file and contains every v1.4 shipped REQ-ID with proper status (Task 2 acceptance gates).
3. Two atomic commits ("PROJECT-01 pass 1/2" + "PROJECT-01 pass 2/2") — machine pass committed FIRST, human pass committed SECOND.
4. `scripts/reconcile-project-validated.cjs` does NOT exist (plan decision PROJECT-01-form enforced — inline computation only).
5. Hand-curated parentheticals in PROJECT.md preserved verbatim where still applicable (Pitfall 8 + CF-06).
6. IP-4 satisfied: this plan runs LAST in Phase 17, so v1.4's own REQ-IDs (closed in Waves 1-3) land in the reconciled truth source.
</verification>

<success_criteria>
- PROJECT-01 closed (12th and final Phase 17 REQ-ID).
- Truth source `.planning/intel/project-validated-truth.md` exists, structured, enumerates 27+ REQ-IDs across v1.0..v1.4.
- `.planning/PROJECT.md ### Validated` reconciled: v1.4 shipped REQ-IDs added, Phase 18 REQ-IDs marked Pending, MERGE-08 marked Deferred-by-design, hand-curated parentheticals preserved.
- Two-pass approach honored (Pitfall 8): machine pass commits before human pass; PROJECT.md is NOT regenerate-overwrite output.
- IP-4 satisfied: v1.4 REQ-IDs from this very milestone (including PROJECT-01 self-reference) appear in the truth source because Wave 4 runs LAST.
- Phase 17 complete: 12 REQ-IDs closed (DOCS-08, DRIFT-01, DRIFT-02, DOCS-01..07, DOCS-09, PROJECT-01) ready for milestone close.
</success_criteria>

<output>
Create `.planning/phases/17-drift-control-reconciliation/17-04-SUMMARY.md` when done.
</output>
