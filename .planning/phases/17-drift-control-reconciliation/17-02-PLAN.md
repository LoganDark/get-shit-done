---
phase: 17-drift-control-reconciliation
plan: 17.02
type: execute
wave: 2
depends_on: [17.01]
files_modified:
  - tests/architecture-counts.test.cjs
  - tests/command-count-sync.test.cjs
autonomous: true
requirements: [DRIFT-01, DRIFT-02]
requirements_addressed: [DRIFT-01, DRIFT-02]
tags: [drift-control, tests, node-test, live-scan]
decisions:
  - id: D-10
    summary: "Tests is NOT added as an INVENTORY.md family; auto-resolve path chosen."
    rationale: "Inspected docs/INVENTORY.md headings — current FAMILIES are Agents, Commands, Workflows, References, CLI Modules, Hooks (verified via grep `^##` against INVENTORY.md). There is no `## Tests (N shipped)` heading today. Adding one would grow Phase 17 surface beyond DOCS-08 + DRIFT-01/02 + DOCS-01..09 scope (CONTEXT D-10 explicitly defers; RESEARCH §user_constraints recommends auto-resolve as minimal-surface). The INVENTORY.md L9/L59 self-references that claim the two new drift tests exist will auto-resolve when this plan commits. If a Tests family is genuinely valuable later, file as v1.5+ deferred (CONTEXT §Deferred Ideas)."
must_haves:
  truths:
    - "17.01 committed before this plan starts"
    - "tests/architecture-counts.test.cjs exists, uses node:test framework, and exits 0 on first run (day-1 GREEN per Pitfall 4 same-PR coupling)"
    - "tests/command-count-sync.test.cjs exists, uses node:test framework, and exits 0 on first run"
    - "Both new tests live-scan filesystem at runtime (readdirSync + wc -l); NO hardcoded counts, NO snapshots (CF-02)"
    - "architecture-counts.test.cjs covers en + ja-JP + ko-KR (3 prose-count-carrying locales); pt-BR excluded by structural reality per RESEARCH key finding #2"
    - "D-04: lockstep locale strictness honored — the test ASSERTS the 3 prose-count-carrying locales in parallel (pt-BR carve-out is structural, not a policy softening; CONTEXT D-04 lockstep is upheld for every locale that carries the dimensions)"
    - "D-05: per-locale describe blocks × applicable dimensions encoded — CONTEXT D-05's `4 locales × 5 dimensions` is honored as `3 locales × applicable-dimensions` after the structural pt-BR carve-out (RESEARCH override; D-05 substance preserved — per-locale describe block, all relevant dimensions asserted)"
    - "D-11: strict-sequential ordering — this plan IS Wave 2, runs only after Wave 1 (17.01) commits, before Wave 3 begins; no within-phase parallelism"
    - "architecture-counts.test.cjs uses per-locale exact-string regex (D-06) — one regex set per locale encoded in the test file"
    - "architecture-counts.test.cjs uses rounded-bucket assertion (nearest 1000) for installer LOC dimension per D-03"
    - "command-count-sync.test.cjs asserts INVENTORY.md `## Commands` table-row count == ls commands/gsd/*.md count"
    - "Both test files are verbatim-shape copies of tests/inventory-counts.test.cjs (CF-02 + ROADMAP SC1)"
    - "Both test files cross-link via header comment (CF-03 single-responsibility separation)"
    - "tests/inventory-counts.test.cjs still passes (no regression on the existing inventory headline check)"
    - "INVENTORY.md NOT modified by this plan (D-10 auto-resolve: Tests is NOT added as a family; L9/L59 self-references auto-resolve when test files land)"
  artifacts:
    - path: "tests/architecture-counts.test.cjs"
      provides: "DRIFT-01 — ARCHITECTURE.md prose-count locale-parity guard"
      contains: "require('node:test')"
      min_lines: 80
    - path: "tests/command-count-sync.test.cjs"
      provides: "DRIFT-02 — INVENTORY.md ## Commands table-row sync guard"
      contains: "require('node:test')"
      min_lines: 30
  key_links:
    - from: "tests/architecture-counts.test.cjs"
      to: "bin/install.js"
      via: "fs.readFileSync + split('\\n').length for installer LOC"
      pattern: "bin/install\\.js"
    - from: "tests/architecture-counts.test.cjs"
      to: "docs/{ARCHITECTURE.md, ja-JP/ARCHITECTURE.md, ko-KR/ARCHITECTURE.md}"
      via: "fs.readFileSync + per-locale regex extraction"
      pattern: "ARCHITECTURE\\.md"
    - from: "tests/command-count-sync.test.cjs"
      to: "docs/INVENTORY.md `## Commands` section + commands/gsd/*.md"
      via: "INVENTORY.md slice + readdirSync"
      pattern: "## Commands"
---

<objective>
Wave 2 of Phase 17 — ship two new `node:test` drift-control tests (`tests/architecture-counts.test.cjs` + `tests/command-count-sync.test.cjs`) that lock prose claims to live filesystem state. Verbatim-shape copy of `tests/inventory-counts.test.cjs` per CF-02 + ROADMAP SC1.

Purpose: Plan 17.01 (Wave 1) fixed the underlying drift; this plan adds the guard that PREVENTS recurrence. Per Pitfall 4 + CF-01 strict wave ordering, **17.01 MUST be committed before this plan starts** — the same-PR coupling pattern from v1.3 D-09/D-10/D-11 applies verbatim here. Day-1 GREEN is non-negotiable.

Output: Two test files committed under `tests/`. Both wire into existing `npm test` lane. No SDK / CLI / lint surface added (CONTEXT code_context). Closes DRIFT-01 + DRIFT-02. INVENTORY.md L9/L59 self-references auto-resolve when these files land (per D-10 auto-resolve path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-drift-control-reconciliation/17-CONTEXT.md
@.planning/phases/17-drift-control-reconciliation/17-RESEARCH.md
@.planning/phases/17-drift-control-reconciliation/17-PATTERNS.md
@.planning/phases/17-drift-control-reconciliation/17-01-SUMMARY.md
@tests/inventory-counts.test.cjs

<interfaces>
<!-- Verbatim template — both new test files copy this shape exactly -->
<!-- Source: tests/inventory-counts.test.cjs (verified 2026-05-25) -->

Required imports (every test file):
  const { describe, test } = require('node:test');
  const assert = require('node:assert/strict');
  const fs = require('node:fs');
  const path = require('node:path');

Anchor pattern: const ROOT = path.resolve(__dirname, '..');

Headline regex shape (inventory-counts.test.cjs:38):
  new RegExp(`^##\\s+${label}\\s+\\((\\d+)\\s+shipped\\)`, 'm')

Filesystem count helper (inventory-counts.test.cjs:44-50):
  function fsCount(relDir, filter) {
    return fs.readdirSync(path.join(ROOT, relDir))
      .filter((name) => fs.statSync(path.join(ROOT, relDir, name)).isFile())
      .filter(filter)
      .length;
  }

Per-locale label regex (D-06; from RESEARCH lines 467-495):
  EN_LABELS: agents=/\*\*Total agents:\*\*\s+(\d+)/, installerLoc=/The installer \(`bin\/install\.js`,\s+~([\d,]+)\s+lines\)/
  JA_LABELS: commands=/\*\*コマンド総数:\*\*\s+(\d+)/, workflows=/\*\*ワークフロー総数:\*\*\s+(\d+)/, agents=/\*\*エージェント総数:\*\*\s+(\d+)/, installerLoc=/インストーラー（`bin\/install\.js`、約([\d,]+)行）/
  KO_LABELS: commands=/\*\*전체 명령어 수:\*\*\s+(\d+)/, workflows=/\*\*전체 워크플로우 수:\*\*\s+(\d+)/, agents=/\*\*전체 에이전트 수:\*\*\s+(\d+)/, installerLoc=/인스톨러\(`bin\/install\.js`,\s+~([\d,]+)줄\)/

Rounded-bucket assertion shape (D-03; nearest 1000):
  const documentedBucket = Math.round(documented / 1000) * 1000;
  const actualBucket     = Math.round(actual / 1000) * 1000;
  assert.strictEqual(documentedBucket, actualBucket, '<message>');
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/architecture-counts.test.cjs (DRIFT-01)</name>
  <files>tests/architecture-counts.test.cjs</files>
  <read_first>
    - tests/inventory-counts.test.cjs (full file — verbatim template per CF-02; copy this shape exactly)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DRIFT-01 lines 464-510 (full per-locale regex table EN_LABELS / JA_LABELS / KO_LABELS) + §Pattern 1 lines 237-301 (full assembled skeleton)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §tests/architecture-counts.test.cjs lines 43-146 (customization checklist + full pattern map)
    - .planning/phases/17-drift-control-reconciliation/17-CONTEXT.md §D-03 (rounding mechanism) + §D-04 (all-strict locale policy) + §D-05 (scope = 4 locales × 5 dimensions, but pt-BR excluded by RESEARCH structural finding) + §D-06 (per-locale exact-string regex)
    - docs/ARCHITECTURE.md (post-Wave-1 state; verify Wave 1 commit landed before reading)
    - docs/ja-JP/ARCHITECTURE.md, docs/ko-KR/ARCHITECTURE.md (post-Wave-1; for regex grounding)
    - bin/install.js (only for `wc -l` confirmation that live count is 10978 → rounds to 11000)
  </read_first>
  <behavior>
    Test file under `tests/architecture-counts.test.cjs` exposes the following testable behavior under `node --test`:

    - **en source describe block** asserts (a) `**Total agents:** N` matches `fsCount('agents', /^gsd-.*\.md$/)` = 33, (b) installer LOC rounded-bucket matches `wc -l bin/install.js` rounded-bucket — both ~11000.
    - **ja-JP translation describe block** asserts 4 dimensions × per-locale regex: commands=68, workflows=89, agents=33, installerLoc bucket=11000.
    - **ko-KR translation describe block** asserts the same 4 dimensions for the ko-KR locale.
    - **pt-BR NOT covered** — RESEARCH key finding #2 (pt-BR ARCHITECTURE.md has no numeric prose-counts). One inline comment in the LOCALES array documents the carve-out.
    - **Failure mode:** any single drift causes a clear assertion message that names the file path, the locale, the dimension, and BOTH numbers (documented vs actual filesystem count).
    - **Day-1 status:** GREEN. Wave 1 prose fixes (17.01) put every assertion target into agreement with the filesystem.
  </behavior>
  <action>
    Create `tests/architecture-counts.test.cjs` as a verbatim-shape copy of `tests/inventory-counts.test.cjs`, customized per RESEARCH §DRIFT-01 + §Pattern 1.

    Required file contents (paraphrased — implementer writes actual JavaScript following the template):

    1. Open with `'use strict';` and the `// allow-test-rule: pending-migration-to-typed-ir [#2974]` two-line comment block from `inventory-counts.test.cjs:3-6` (verbatim — both new drift tests qualify per PATTERNS.md line 439).
    2. JSDoc header comment describing intent: "Locks docs/ARCHITECTURE.md (and translations) prose-count claims against the filesystem live at test runtime. Both sides computed at test runtime — no hardcoded numbers. Cross-linked to `tests/command-count-sync.test.cjs` (single-responsibility per CF-03) and `tests/inventory-counts.test.cjs` (sibling drift guard)."
    3. Import block: `node:test` / `node:assert/strict` / `node:fs` / `node:path` — identical to inventory-counts.
    4. `ROOT = path.resolve(__dirname, '..')` anchor.
    5. Define `EN_LABELS`, `JA_LABELS`, `KO_LABELS` constants using the exact regexes from RESEARCH §DRIFT-01 lines 467-495 (paste those three constant declarations verbatim).
    6. Define `LOCALES` array with three entries: `{ id: 'en', file: 'docs/ARCHITECTURE.md', labels: EN_LABELS }`, `{ id: 'ja-JP', file: 'docs/ja-JP/ARCHITECTURE.md', labels: JA_LABELS }`, `{ id: 'ko-KR', file: 'docs/ko-KR/ARCHITECTURE.md', labels: KO_LABELS }`. Inline comment immediately above the array: `// pt-BR excluded — file has no numeric prose-counts (verified 2026-05-25, RESEARCH key finding #2)`.
    7. Define `DIMENSIONS` array with: `{ key: 'commands', dir: 'commands/gsd', filter: (f) => f.endsWith('.md') }`, `{ key: 'workflows', dir: 'get-shit-done/workflows', filter: (f) => f.endsWith('.md') }`, `{ key: 'agents', dir: 'agents', filter: (f) => /^gsd-.*\.md$/.test(f) }`. Do NOT include libModules / hooks / references — RESEARCH §DRIFT-01 dimension matrix shows those dimensions exist only on en as anchor-form refs (deferred to INVENTORY.md), not as direct prose counts on translations.
    8. Copy `fsCount(relDir, filter)` helper verbatim from inventory-counts.test.cjs:44-50.
    9. Add new helper: `function installerLoc() { return fs.readFileSync(path.join(ROOT, 'bin/install.js'), 'utf8').split('\n').length - 1; }` per RESEARCH §Code Examples lines 866-874.
    10. Write the per-locale describe block from RESEARCH §Pattern 1 lines 274-300 verbatim: outer `for (const locale of LOCALES)` produces three `describe(...)` blocks (one per locale). Inside each, iterate `DIMENSIONS` but ONLY emit a `test(...)` for dimensions whose label exists in `locale.labels` (so en's commands+workflows dimensions are skipped — they defer to INVENTORY.md and are covered by `inventory-counts.test.cjs`; en still asserts agents). After the dimension loop, emit one additional `test(...)` for the rounded-bucket installer LOC assertion (D-03 form): `assert.strictEqual(Math.round(documented/1000)*1000, Math.round(actual/1000)*1000, '<message>')`.
    11. The assertion message for each dimension MUST name both sides: e.g., ``${locale.file} ${dim.key}=${documented} disagrees with ${dim.dir}/=${actual}``.
    12. The assertion message for installer LOC MUST name BOTH the bucket and the actual value: e.g., ``${locale.file} installer LOC bucket ${documentedBucket} disagrees with bin/install.js bucket ${actualBucket} (actual: ${actual})``.

    Do NOT inline pt-BR. Do NOT use snapshots. Do NOT hardcode the number 68, 89, 33, 10978, or 11000 anywhere in the test file. Every count comes from a live `readdirSync` / `wc -l` operation at test runtime per CF-02.

    Do NOT add a `## Tests` family heading to INVENTORY.md (D-10 auto-resolve path — see plan frontmatter `decisions`). INVENTORY.md is NOT in this plan's `files_modified`.
  </action>
  <verify>
    <automated>node --test tests/architecture-counts.test.cjs</automated>
    <automated>grep -c "require('node:test')" tests/architecture-counts.test.cjs</automated>
    <automated>grep -c "require('vitest')" tests/architecture-counts.test.cjs</automated>
    <automated>grep -c "EN_LABELS\|JA_LABELS\|KO_LABELS" tests/architecture-counts.test.cjs</automated>
    <automated>grep -c "Math.round(documented / 1000)" tests/architecture-counts.test.cjs</automated>
    <automated>grep -cE "(===|==) (68|89|33|10978|11000)" tests/architecture-counts.test.cjs</automated>
    <automated>grep -cE "pt-BR" tests/architecture-counts.test.cjs</automated>
    <automated>node --test tests/inventory-counts.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/architecture-counts.test.cjs` exits 0 (day-1 GREEN per Pitfall 4)
    - `grep -c "require('node:test')" tests/architecture-counts.test.cjs` returns 1 (uses node:test, NOT vitest per CF-02)
    - `grep -c "require('vitest')" tests/architecture-counts.test.cjs` returns 0
    - `grep -c "EN_LABELS\|JA_LABELS\|KO_LABELS" tests/architecture-counts.test.cjs` returns at least 3 (per-locale regex constants present per D-06)
    - `grep -c "Math.round(documented / 1000)" tests/architecture-counts.test.cjs` returns ≥1 (rounded-bucket assertion per D-03)
    - `grep -cE "(===|==) (68|89|33|10978|11000)" tests/architecture-counts.test.cjs` returns 0 (no hardcoded counts per CF-02 live-scan policy)
    - `grep -cE "pt-BR" tests/architecture-counts.test.cjs` returns ≥1 (carve-out comment present documenting RESEARCH key finding #2 — but file path docs/pt-BR/ARCHITECTURE.md MUST NOT appear in a `file:` entry of the LOCALES array)
    - `node --test tests/inventory-counts.test.cjs` still exits 0 (sibling guard not regressed)
    - File contains `LOCALES` array with exactly 3 entries (en, ja-JP, ko-KR) — no pt-BR entry
  </acceptance_criteria>
  <done>
    DRIFT-01 closed. `tests/architecture-counts.test.cjs` is live-scan, per-locale, day-1 GREEN, and references the verbatim template shape from `inventory-counts.test.cjs`. Pitfall 4 same-PR coupling satisfied (Wave 1 committed before this task ran).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/command-count-sync.test.cjs (DRIFT-02)</name>
  <files>tests/command-count-sync.test.cjs</files>
  <read_first>
    - tests/inventory-counts.test.cjs (same verbatim template — CF-03 mandates separate file but same shape)
    - tests/architecture-counts.test.cjs (the sibling drift guard — for cross-link comment)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DRIFT-02 lines 512-562 (full test skeleton + INVENTORY.md `## Commands` section format note)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §tests/command-count-sync.test.cjs lines 150-195 (section-extraction pattern + filesystem count pattern)
    - .planning/phases/17-drift-control-reconciliation/17-CONTEXT.md §CF-03 (TWO separate test files locked) + §domain (REQUIREMENTS Out of Scope explicitly prohibits merging)
    - docs/INVENTORY.md (post-Wave-1; verify L57 reads `## Commands (68 shipped)` and the table-row count is now 68)
  </read_first>
  <behavior>
    Test file under `tests/command-count-sync.test.cjs` exposes the following testable behavior under `node --test`:

    - Single describe block: "docs/INVENTORY.md ## Commands table row count matches commands/gsd/"
    - Single test inside it: "row count equals filesystem command file count"
    - The test slices `docs/INVENTORY.md` from `## Commands ` to the next `\n## ` boundary, counts rows matching `^|\s+`\/gsd-[^`]+``, and asserts that count equals `fs.readdirSync('commands/gsd').filter(f => f.endsWith('.md')).length`.
    - **Day-1 status:** GREEN. Wave 1 bumped INVENTORY headline 67→68 and pre-existing live count was 68; no row-list edit needed (rows were already 68; only the headline lagged).
    - **Failure mode:** assertion message names BOTH numbers: e.g., `INVENTORY.md ## Commands table has 67 rows but commands/gsd/ has 68 .md files`.
  </behavior>
  <action>
    Create `tests/command-count-sync.test.cjs` as a verbatim-shape copy of `tests/inventory-counts.test.cjs`, customized per RESEARCH §DRIFT-02 + §Pattern 2 from PATTERNS.md.

    Required file contents (paraphrased):

    1. Open with `'use strict';` and the `// allow-test-rule: pending-migration-to-typed-ir [#2974]` two-line comment block from `inventory-counts.test.cjs:3-6` verbatim.
    2. JSDoc header comment: "Locks docs/INVENTORY.md `## Commands` table-row count against `commands/gsd/*.md` filesystem count. Cross-linked to `tests/architecture-counts.test.cjs` (single-responsibility per CF-03; separate failure messages, single test concern here) and `tests/inventory-counts.test.cjs` (sibling — asserts headline counts only; this file asserts table-row count). Both sides computed at test runtime — no hardcoded numbers."
    3. Import block: `node:test` / `node:assert/strict` / `node:fs` / `node:path` — identical to inventory-counts.
    4. `ROOT = path.resolve(__dirname, '..')` anchor.
    5. Constants: `INVENTORY_MD = path.join(ROOT, 'docs', 'INVENTORY.md')`, `INVENTORY = fs.readFileSync(INVENTORY_MD, 'utf8')`, `COMMANDS_DIR = path.join(ROOT, 'commands/gsd')`.
    6. Function `commandsTableRowCount()` per RESEARCH §DRIFT-02 lines 538-546: slice from `INVENTORY.indexOf('## Commands ')` to next `'\n## '` boundary (or EOF), then count regex matches against `/^\|\s+`\/gsd-[^`]+`/gm`.
    7. Function `filesystemCommandCount()` per RESEARCH §DRIFT-02 lines 548-553: `readdirSync(COMMANDS_DIR).filter(isFile-and-md).length`.
    8. Single describe + test block per RESEARCH §DRIFT-02 lines 555-562: assert `commandsTableRowCount()` strict-equals `filesystemCommandCount()` with the assertion message `\`INVENTORY.md ## Commands table has ${documented} rows but commands/gsd/ has ${actual} .md files\``.

    Do NOT merge any logic from `architecture-counts.test.cjs` into this file (CF-03 + REQUIREMENTS §Out of Scope explicit prohibition).
    Do NOT hardcode 68 anywhere.
    Do NOT modify `tests/inventory-counts.test.cjs` (that file's headline check is separate and must stay green).
  </action>
  <verify>
    <automated>node --test tests/command-count-sync.test.cjs</automated>
    <automated>grep -c "require('node:test')" tests/command-count-sync.test.cjs</automated>
    <automated>grep -c "require('vitest')" tests/command-count-sync.test.cjs</automated>
    <automated>grep -cE "(===|==) 68" tests/command-count-sync.test.cjs</automated>
    <automated>grep -c "commandsTableRowCount\|filesystemCommandCount" tests/command-count-sync.test.cjs</automated>
    <automated>grep -c "architecture-counts.test.cjs" tests/command-count-sync.test.cjs</automated>
    <automated>node --test tests/architecture-counts.test.cjs tests/command-count-sync.test.cjs tests/inventory-counts.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/command-count-sync.test.cjs` exits 0 (day-1 GREEN)
    - `grep -c "require('node:test')" tests/command-count-sync.test.cjs` returns 1 (uses node:test per CF-02)
    - `grep -c "require('vitest')" tests/command-count-sync.test.cjs` returns 0
    - `grep -cE "(===|==) 68" tests/command-count-sync.test.cjs` returns 0 (no hardcoded count per CF-02 live-scan)
    - `grep -c "commandsTableRowCount\|filesystemCommandCount" tests/command-count-sync.test.cjs` returns ≥2 (both helpers defined per RESEARCH §DRIFT-02)
    - `grep -c "architecture-counts.test.cjs" tests/command-count-sync.test.cjs` returns ≥1 (cross-link comment per CF-03)
    - Running all three drift guards together exits 0: `node --test tests/architecture-counts.test.cjs tests/command-count-sync.test.cjs tests/inventory-counts.test.cjs`
    - File is a standalone test (no imports from architecture-counts.test.cjs; CF-03 enforces single-responsibility)
  </acceptance_criteria>
  <done>
    DRIFT-02 closed. `tests/command-count-sync.test.cjs` is live-scan, day-1 GREEN, separate from architecture-counts.test.cjs per CF-03. INVENTORY.md L9/L59 self-references auto-resolve (D-10 path).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | New test files run under `node --test` against the local filesystem. No network, no untrusted input, no executable surface added beyond test invocation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.02-01 | Tampering | drift test correctness | mitigate | grep gates verify no hardcoded counts, no vitest imports, node:test framework usage, and cross-link comments per CF-03. Live-scan policy (CF-02) enforced by acceptance criteria. |
| T-17.02-02 | Denial of Service | test runtime | accept | Tests do bounded `readdirSync` over small dirs (≤89 entries) + bounded `readFileSync` over a ≤11kLOC file. No exploitable runtime cost. |

**No in-scope threats requiring new mitigations.** Pure docs+tests phase per CONTEXT §code_context. No new SDK / CLI / lint surface introduced.
</threat_model>

<verification>
## Plan-level verification

1. `node --test tests/architecture-counts.test.cjs tests/command-count-sync.test.cjs` exits 0 (both new guards green on first run — Pitfall 4 satisfied via Wave 1 same-PR coupling).
2. `node --test tests/inventory-counts.test.cjs` exits 0 (existing guard not regressed; Wave 1 Commands+Workflows headline fix keeps it green).
3. Both new files use `node:test` (CF-02), live-scan filesystem (CF-02), and exist as separate files (CF-03).
4. INVENTORY.md NOT modified by this plan (D-10 auto-resolve confirmed).
5. Two commits land: "test(17.02): add architecture-counts drift guard (DRIFT-01)" + "test(17.02): add command-count-sync drift guard (DRIFT-02)".
</verification>

<success_criteria>
- DRIFT-01 closed: `tests/architecture-counts.test.cjs` exists, exits 0, uses node:test, live-scans 3 locales × applicable dimensions per locale per D-04/D-05/D-06.
- DRIFT-02 closed: `tests/command-count-sync.test.cjs` exists, exits 0, uses node:test, live-scans INVENTORY `## Commands` table vs filesystem.
- Pitfall 4 same-PR coupling honored: Wave 1 was already committed when this plan started; both new tests pass on first run.
- CF-02 (node:test, live-scan) + CF-03 (two separate files) + D-06 (per-locale regex) all upheld in the source.
- D-10 resolved: INVENTORY.md NOT modified; Tests is not added as a family; auto-resolve path chosen.
</success_criteria>

<output>
Create `.planning/phases/17-drift-control-reconciliation/17-02-SUMMARY.md` when done.
</output>
