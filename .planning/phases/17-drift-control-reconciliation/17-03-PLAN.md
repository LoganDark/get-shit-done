---
phase: 17-drift-control-reconciliation
plan: 17.03
type: execute
wave: 3
depends_on: [17.02]
files_modified:
  - .planning/intel/docs-update-fix-triage.md
  - docs/ja-JP/superpowers/plans/2026-03-18-materialize-new-project-config.md
  - docs/ko-KR/superpowers/plans/2026-03-18-materialize-new-project-config.md
  - docs/ja-JP/superpowers/specs/2026-03-20-multi-project-workspaces-design.md
  - docs/ko-KR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md
  - docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md
  - docs/adr/0009-shell-command-projection-module.md
  - docs/adr/0010-file-operation-engine-module.md
  - docs/USER-GUIDE.md
  - docs/FEATURES.md
  - docs/AGENTS.md
  - docs/CONTRIBUTING.md
  - CONTRIBUTING.md
  - CHANGELOG.md
  - docs/test-triage/jj-bugs.md
  - docs/pt-BR/superpowers/README.md
  - docs/pt-BR/superpowers/plans/2026-03-23-materialize-new-project-config.md
  - CONTEXT.md
  - docs/ja-JP/AGENTS.md
  - .planning/intel/docs-update-verify-only-2026-05-25-postfix-pass-rate.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06, DOCS-07, DOCS-09]
requirements_addressed: [DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06, DOCS-07, DOCS-09]
tags: [docs-fix, batched-themes, adr-supersession, per-theme-commits]
decisions:
  - id: D-09-resolution
    summary: "Theme 8 mutation-subprocess.integration.test.ts: in-line strip the filename from CHANGELOG.md:389 parenthetical (file was never shipped per RESEARCH finding #4)."
    rationale: "RESEARCH §DOCS-07 + RESEARCH key finding #4 confirmed the file never existed — sdk/src/golden/golden-mutation-covered.ts:3 says verbatim 'see mutation-subprocess.integration.test.ts when present. Empty until those tests land'. The CHANGELOG L389 reference (issue #2302) was aspirational. CONTEXT D-09 offered two fallback paths (in-line strip OR append-only correction note); in-line strip is the simpler resolution and matches the rest of Theme 8's mechanical singleton-fix shape. The append-only correction note remains a valid alternative if in-line strip would obscure CHANGELOG-as-historical-record convention (planner judgement during execute)."
  - id: D-08-symmetry
    summary: "Theme 8 USER-PROFILE.md qualifier applied to BOTH en (docs/AGENTS.md:404) and ja-JP (docs/ja-JP/AGENTS.md:389) for symmetry."
    rationale: "RESEARCH finding #5 + RESEARCH Open Question #2: the en and ja-JP files use IDENTICAL unqualified prose. The verifier flagged ja-JP only because en wasn't audited at the same line. Fixing only ja-JP creates new asymmetry; fixing both upholds D-04 lockstep policy across translation pairs. Qualifier text: `$HOME/.claude/USER-PROFILE.md` (matches runtime artifact location)."
  - id: D-01-changelog-form
    summary: "Theme 4 CHANGELOG.md:346 keeps the historical path with a `(now at get-shit-done/bin/...)` parenthetical."
    rationale: "RESEARCH §DOCS-04 table line 617 explicitly recommends this form for CHANGELOG since changelog-as-historical-record convention preserves the original location at time of authorship. CONTRIBUTING.md:403 gets the direct path-rewrite since it's a current-state authoring guide."
  - id: theme-grouping
    summary: "7 themes ship in 7 separate commits (per Pitfall 12 preferred form). No grouping."
    rationale: "CONTEXT §Claude's Discretion bullet 6 allowed planner to group closely-related themes (e.g., theme 4 + theme 8 both path-rewrite singletons). Chose per-theme to keep forensic bisect granularity at maximum — every REQ-ID maps 1:1 to a commit. Triage doc (CF-07) lists the per-theme commit message stems; executor produces 7 atomic commits + 1 close-gate commit for DOCS-09."
  - id: DOCS-09-task-form
    summary: "DOCS-09 (verify-only re-run close-gate) is a SEPARATE task at the end of this plan (not folded into theme 8 commit)."
    rationale: "RESEARCH §DOCS-09 + §Assumptions A3 recommended separate task for clear close-gate auditability. The verify-only re-run is conceptually distinct from theme-fix commits — it confirms the prior 7 commits collectively achieved ≥99% pass rate."
must_haves:
  truths:
    - "17.02 committed before this plan starts (Wave 2 drift tests are green so 17.03 prose edits don't trip them)"
    - ".planning/intel/docs-update-fix-triage.md exists BEFORE any theme-fix commit lands (CF-07 sequencing)"
    - "Theme 1 (DOCS-01): all 14 author-machine path leaks in 2 ja-JP/ko-KR superpowers plans rewritten to $HOME/.claude/get-shit-done/bin/gsd-tools.cjs"
    - "Theme 2 (DOCS-02): 3 split-workspace-command refs in each of 3 translation specs rewritten to unified commands/gsd/workspace.md form"
    - "Theme 3 (DOCS-03): ADR 0009 + 0010 carry an append-only `## Update — 2026-05-25 (Phase 17, DOCS-03)` section appended BELOW all existing content (NEVER in-place edit above the line; ADR immutability per Pitfall 12 + CF-05)"
    - "Theme 4 (DOCS-04): 5 renamed-hook/missing-helper references corrected across docs/USER-GUIDE.md, docs/FEATURES.md, docs/AGENTS.md, docs/CONTRIBUTING.md (plus root CONTRIBUTING.md if it carries the same reference), CHANGELOG.md"
    - "Theme 5 (DOCS-05): docs/test-triage/jj-bugs.md L5/L8/L33 references anchored to closure change_ids 41db442f (Phase 3 03-07 + 03-RESEARCH) and 2fbcd590 (Phase 3 03-06); references NOT deleted (Pitfall 12 forensic trail)"
    - "Theme 7 (DOCS-06): 3 pt-BR translation date/link mismatches fixed (file rename 2026-03-23→2026-03-18, README L7 link, specs L4 circular ref)"
    - "Theme 8 (DOCS-07): 3 singletons fixed — CONTEXT.md:610 path-rewrite (D-07), docs/AGENTS.md:404 + docs/ja-JP/AGENTS.md:389 USER-PROFILE.md qualifier (D-08 + D-08-symmetry), CHANGELOG.md:389 mutation-subprocess filename stripped (D-09 resolution = in-line strip)"
    - "Per-theme commits — 7 atomic commits (one per theme), not bulk-edit (CF-05 + Pitfall 12)"
    - "DOCS-09: /gsd:docs-update --verify-only re-run shows pass rate ≥ 99% post-fix; result recorded at .planning/intel/docs-update-verify-only-2026-05-25-postfix-pass-rate.md"
  artifacts:
    - path: ".planning/intel/docs-update-fix-triage.md"
      provides: "Per-theme triage table written BEFORE execute per CF-07"
      contains: "## Findings"
      min_lines: 20
    - path: "docs/adr/0009-shell-command-projection-module.md"
      provides: "ADR 0009 with appended supersession note for Phase 17 DOCS-03"
      contains: "## Update — 2026-05-25 (Phase 17, DOCS-03)"
    - path: "docs/adr/0010-file-operation-engine-module.md"
      provides: "ADR 0010 with appended supersession note for Phase 17 DOCS-03 below existing ADR-0009 supersession header"
      contains: "## Update — 2026-05-25 (Phase 17, DOCS-03)"
    - path: ".planning/intel/docs-update-verify-only-2026-05-25-postfix-pass-rate.md"
      provides: "DOCS-09 close-gate verify-only re-run record"
      contains: "pass rate"
  key_links:
    - from: "docs/test-triage/jj-bugs.md L5"
      to: "Phase 3 plan 03-07 closure commit"
      via: "change_id citation"
      pattern: "41db442f"
    - from: "docs/test-triage/jj-bugs.md L33"
      to: "Phase 3 plan 03-06 closure commit"
      via: "change_id citation"
      pattern: "2fbcd590"
    - from: "docs/AGENTS.md:404 + docs/ja-JP/AGENTS.md:389"
      to: "$HOME/.claude/USER-PROFILE.md"
      via: "path qualifier"
      pattern: "\\$HOME/\\.claude/USER-PROFILE\\.md"
---

<objective>
Wave 3 of Phase 17 — batched docs-update theme fixes per Pitfall 12 per-theme commits (CF-05). Resolves the 45 `/gsd:docs-update --verify-only` failures across 7 docs themes (theme 6 = drift tests, folded into Wave 2; this plan handles themes 1, 2, 3, 4, 5, 7, 8 = 7 themes total). Closes DOCS-01..07 + DOCS-09 (8 REQ-IDs total — DOCS-09 = close-gate).

Purpose: With prose-counts locked (Waves 1+2), this plan resolves the remaining drift surface from the v1.3-close docs audit. Per CF-05 + Pitfall 12, each theme commits separately to preserve forensic bisect granularity. Per CF-07, the per-theme decisions are recorded in `.planning/intel/docs-update-fix-triage.md` BEFORE any theme commits land.

Output: 7 per-theme commits + 1 DOCS-09 close-gate commit (8 commits total), 1 new intel artifact, ~18 docs files touched across the 7 themes. ADRs (theme 3) get append-only supersession notes only — no in-place edit above the line (CF-05 + Pitfall 12 ADR immutability gotcha).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-drift-control-reconciliation/17-CONTEXT.md
@.planning/phases/17-drift-control-reconciliation/17-RESEARCH.md
@.planning/phases/17-drift-control-reconciliation/17-PATTERNS.md
@.planning/phases/17-drift-control-reconciliation/17-02-SUMMARY.md
@.planning/todos/pending/v14-docs-verify-only-followups.md
@.changeset/jolly-newts-roam.md
@.changeset/happy-jays-greet.md

<interfaces>
<!-- Per-theme commit message stems (Pitfall 12 — RESEARCH §Code Examples lines 877-902) -->

docs(17.03): theme 1 — replace author-machine path leaks in ja-JP/ko-KR superpowers plans (DOCS-01, 14 sites)
docs(17.03): theme 2 — update 3 translation specs to reference unified workspace command (DOCS-02, 7 sites)
docs(17.03): theme 3 — append supersession notes to ADR 0009 + 0010 (DOCS-03, 7 issues)
docs(17.03): theme 4 — fix renamed-hook + missing-helper references (DOCS-04, 5 sites)
docs(17.03): theme 5 — anchor phase-3 archived references to closure change_ids (DOCS-05, 3 sites)
docs(17.03): theme 7 — fix pt-BR translation date/link mismatches (DOCS-06, 3 sites)
docs(17.03): theme 8 — singleton path-rewrites: lint-no-source-grep, USER-PROFILE qualifier, mutation-subprocess strip (DOCS-07, 3 sites)
docs(17.03): docs-update verify-only close-gate (DOCS-09) — pass rate ≥ 99%

<!-- Theme 5 closure change_ids (RESEARCH §DOCS-05 + key finding #3, verified 2026-05-25) -->

41db442f — docs(03-07): phase-close finalization — REQUIREMENTS / ROADMAP / STATE / triage / tracker / SUMMARY (covers 03-07-PLAN.md + 03-RESEARCH.md)
e190eee3 — docs(03-06): TEST-08 triage — populate 7 bug-test verdicts on jj-colocated (TEST-08 specific anchor)
2fbcd590 — docs(03-06): complete push/fetch + workspace + TEST-08 triage plan (covers 03-06-PLAN.md)

<!-- ADR supersession note shape (Pattern 2 from RESEARCH lines 311-320) -->

## Update — 2026-05-25 (Phase 17, DOCS-03)

[one-paragraph purpose statement: "This update reconciles ADR NNNN with the post-Phase-N expansion state of ... and resolves audit findings from the /gsd:docs-update --verify-only Theme 3 sweep."]

- **L<N> <claim>:** [corrected fact, citing the surface that now owns the concept]
- ...

The body of this ADR above this line records the original YYYY-MM-DD decision verbatim. Subsequent drift is captured here per ADR-supersession convention.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Author .planning/intel/docs-update-fix-triage.md (CF-07 prerequisite)</name>
  <files>.planning/intel/docs-update-fix-triage.md</files>
  <read_first>
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §Per-Item tables for every theme (lines 565-647) — exact source rows for the triage table
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §.planning/intel/docs-update-fix-triage.md lines 199-241 (analog at .planning/intel/id-namespace-audit.md + verdict-legend pattern)
    - .planning/intel/id-namespace-audit.md (first 30 lines — analog shape; copy the header + verdict-legend convention)
    - .planning/phases/17-drift-control-reconciliation/17-CONTEXT.md §CF-07 (this file is the prerequisite)
    - .planning/todos/pending/v14-docs-verify-only-followups.md (the 8 themes — theme 6 is folded into 17.02, so 7 themes in scope here)
  </read_first>
  <action>
    Create `.planning/intel/docs-update-fix-triage.md`. Follow the analog shape from `.planning/intel/id-namespace-audit.md` per PATTERNS.md §.planning/intel/docs-update-fix-triage.md.

    Required structure:

    1. Title line: `# Docs-Update Verify-Only Fix Triage — Phase 17 (CF-07)`
    2. Header block:
       - `**Generated:** 2026-05-25`
       - `**Total themes:** 7` (theme 6 = drift tests, folded into Wave 2 Plan 17.02 — note explicitly)
       - `**Verdict counts:** path-rewrite=3, adr-supersession-append=1, change-id-anchor=1, qualifier-add=1, filename-strip=1, rename-file=1, link-rewrite=1, prose-correction=1` (planner refines exact tallies after enumerating per-theme acceptance)
    3. `## Verdict legend` section enumerating the resolution-class taxonomy per PATTERNS line 226: `path-rewrite`, `adr-supersession-append`, `change-id-anchor`, `qualifier-add`, `filename-strip`, `rename-file`, `link-rewrite`, `prose-correction`. Each gets a one-line description.
    4. `## Findings` table with columns `| Theme | REQ-ID | File:line | Drift | Verdict | Acceptance criterion |` and one row per theme (Theme 8 may have 3 sub-rows for D-07/D-08/D-09 since each has a distinct resolution per RESEARCH §DOCS-07 table).
    5. Footer paragraph citing RESEARCH §Per-Item tables for the exact site list and noting D-09 resolution = in-line strip + D-08 symmetry = both en + ja-JP edited (cite plan frontmatter `decisions`).

    Each row's "Acceptance criterion" cell MUST be a concrete grep-able condition (e.g., `grep -c "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" docs/ja-JP/superpowers/plans/2026-03-18-materialize-new-project-config.md returns 7`). NO subjective acceptance language.

    Do NOT edit any docs/ files in this task. This task ONLY writes the triage artifact per CF-07 sequencing.
  </action>
  <verify>
    <automated>test -f .planning/intel/docs-update-fix-triage.md</automated>
    <automated>grep -c "^# Docs-Update Verify-Only Fix Triage — Phase 17 (CF-07)" .planning/intel/docs-update-fix-triage.md</automated>
    <automated>grep -c "^## Verdict legend" .planning/intel/docs-update-fix-triage.md</automated>
    <automated>grep -c "^## Findings" .planning/intel/docs-update-fix-triage.md</automated>
    <automated>grep -cE "DOCS-(01|02|03|04|05|06|07)" .planning/intel/docs-update-fix-triage.md</automated>
    <automated>wc -l .planning/intel/docs-update-fix-triage.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/intel/docs-update-fix-triage.md`
    - Contains title line "Docs-Update Verify-Only Fix Triage — Phase 17 (CF-07)"
    - Contains `## Verdict legend` section
    - Contains `## Findings` section with at least 7 rows (one per theme; Theme 8 may have sub-rows)
    - All 7 REQ-IDs (DOCS-01..07) referenced at least once in the table
    - Total line count ≥ 20 (per artifacts min_lines)
    - Each Findings row's acceptance criterion is grep-able / mechanically verifiable (no "looks correct" prose)
  </acceptance_criteria>
  <done>
    CF-07 prerequisite satisfied. The triage doc records every per-theme decision (path-rewrite vs supersession-append vs change-id-anchor vs filename-strip) BEFORE any theme commit lands.
  </done>
</task>

<task type="auto">
  <name>Task 1: Theme 1 commit — DOCS-01 author-machine path leaks (14 sites)</name>
  <files>docs/ja-JP/superpowers/plans/2026-03-18-materialize-new-project-config.md, docs/ko-KR/superpowers/plans/2026-03-18-materialize-new-project-config.md</files>
  <read_first>
    - docs/ja-JP/superpowers/plans/2026-03-18-materialize-new-project-config.md (read full file — find every occurrence of `/Users/diego/Dev/get-shit-done/get-shit-done/bin/gsd-tools.cjs`)
    - docs/ko-KR/superpowers/plans/2026-03-18-materialize-new-project-config.md (same)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-01 lines 566-572 (count = 14 sites = 7 per file)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §Theme 1 + Theme 2 + Theme 7 mechanical sweeps lines 387-393 (single sed sweep + IP-3 commit-immediately pattern)
  </read_first>
  <action>
    Mechanical sed sweep across both files. Find: `/Users/diego/Dev/get-shit-done/get-shit-done/bin/gsd-tools.cjs`. Replace: `$HOME/.claude/get-shit-done/bin/gsd-tools.cjs`.

    Expected: 7 replacements per file × 2 files = 14 total replacements per RESEARCH §DOCS-01 table.

    Commit message (verbatim): `docs(17.03): theme 1 — replace author-machine path leaks in ja-JP/ko-KR superpowers plans (DOCS-01, 14 sites)`

    Per IP-3: commit IMMEDIATELY after the sweep returns — do NOT batch with other themes. The `assert_clean_wc` workflow gate runs LAST and would trip on intermediate sed output if the commit lagged.

    Do NOT edit any OTHER occurrences of `diego` or author-machine paths in OTHER files — Theme 1 scope is strictly these 2 files (per RESEARCH §DOCS-01 table). If a sweep reveals leaks in additional files, file them as a v1.5+ deferred todo, do not in-scope.
  </action>
  <verify>
    <automated>grep -c "\$HOME/\.claude/get-shit-done/bin/gsd-tools\.cjs" docs/ja-JP/superpowers/plans/2026-03-18-materialize-new-project-config.md</automated>
    <automated>grep -c "\$HOME/\.claude/get-shit-done/bin/gsd-tools\.cjs" docs/ko-KR/superpowers/plans/2026-03-18-materialize-new-project-config.md</automated>
    <automated>grep -c "/Users/diego/Dev/get-shit-done" docs/ja-JP/superpowers/plans/2026-03-18-materialize-new-project-config.md docs/ko-KR/superpowers/plans/2026-03-18-materialize-new-project-config.md</automated>
  </verify>
  <acceptance_criteria>
    - ja-JP file forward-grep on `$HOME/.claude/get-shit-done/bin/gsd-tools.cjs` returns 7
    - ko-KR file forward-grep on `$HOME/.claude/get-shit-done/bin/gsd-tools.cjs` returns 7
    - Combined reverse-grep on `/Users/diego/Dev/get-shit-done` across both files returns 0
    - `git log -1 --format=%s` after this commit contains "theme 1" and "DOCS-01"
  </acceptance_criteria>
  <done>
    DOCS-01 closed. 14 author-machine path leaks eliminated across 2 translated planning docs in a single mechanical commit per IP-3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Theme 2 commit — DOCS-02 split-workspace-command refs (7 sites, 3 files)</name>
  <files>docs/ja-JP/superpowers/specs/2026-03-20-multi-project-workspaces-design.md, docs/ko-KR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md, docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md</files>
  <read_first>
    - docs/ja-JP/superpowers/specs/2026-03-20-multi-project-workspaces-design.md (read lines 160-175 — drift zone)
    - docs/ko-KR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md (same)
    - docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md (same — verify prose structure matches RESEARCH §DOCS-02 note "researcher CONFIRM during plan-phase" so the count may be 1 ref instead of 3)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-02 lines 574-582
    - commands/gsd/workspace.md (target — confirm subcommands `--new`, `--list`, `--remove` are accurate names; if the live command uses different subcommand syntax, use that)
  </read_first>
  <action>
    Rewrite references to the 3 split files (`commands/gsd/new-workspace.md`, `commands/gsd/list-workspaces.md`, `commands/gsd/remove-workspace.md`) to one ref to `commands/gsd/workspace.md` with subcommand descriptions. Per RESEARCH §DOCS-02 + REQUIREMENTS DOCS-02:

    - In each file, replace the 3 separate ref lines with one unified ref. The pattern depends on local prose structure (each translation may phrase it slightly differently). The principle is: ONE file ref, with subcommand syntax (`/gsd-workspace --new`, `--list`, `--remove`) capturing what the 3 separate files described.
    - Before editing, verify the actual subcommand syntax in `commands/gsd/workspace.md` and use the live subcommand names (not the speculative ones from RESEARCH). If the live command uses a different syntax (e.g., positional verbs `/gsd-workspace new`), use the live form.
    - pt-BR may have a different prose structure (RESEARCH flag: "researcher CONFIRM during plan-phase") — adapt the unified-ref rewrite to whatever local structure exists there; the principle (3 refs → 1 ref) is what matters.

    Commit message (verbatim): `docs(17.03): theme 2 — update 3 translation specs to reference unified workspace command (DOCS-02, 7 sites)`

    Do NOT modify `commands/gsd/workspace.md` itself. Do NOT create the deleted split files (they were intentionally never created).
  </action>
  <verify>
    <automated>grep -c "commands/gsd/new-workspace\.md\|commands/gsd/list-workspaces\.md\|commands/gsd/remove-workspace\.md" docs/ja-JP/superpowers/specs/2026-03-20-multi-project-workspaces-design.md docs/ko-KR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md</automated>
    <automated>grep -c "commands/gsd/workspace\.md" docs/ja-JP/superpowers/specs/2026-03-20-multi-project-workspaces-design.md docs/ko-KR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md</automated>
  </verify>
  <acceptance_criteria>
    - Reverse-grep on the 3 split-command paths across all 3 files returns 0
    - Forward-grep on `commands/gsd/workspace.md` across all 3 files returns ≥3 (each file now references the unified command)
    - `git log -1 --format=%s` after this commit contains "theme 2" and "DOCS-02"
  </acceptance_criteria>
  <done>
    DOCS-02 closed. All 7 split-workspace-command references rewritten to unified form across 3 translation specs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Theme 3 commit — DOCS-03 ADR supersession notes (ADR 0009 + 0010)</name>
  <files>docs/adr/0009-shell-command-projection-module.md, docs/adr/0010-file-operation-engine-module.md</files>
  <read_first>
    - docs/adr/0009-shell-command-projection-module.md (full read — locate the END of the file; the new section appends BELOW all existing content including the existing `## Update — 2026-05-13` block at line 113)
    - docs/adr/0010-file-operation-engine-module.md (full read — locate the existing "Superseded by ADR-0009" header at line 3 AND the END of the file; the new section appends BELOW everything including the existing supersession header)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §Pattern 2 lines 305-320 (drafted text for ADR 0009) + §DOCS-03 lines 586-606 (drafted text for ADR 0010)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §docs/adr/0010-file-operation-engine-module.md lines 292-329 (analog at ADR 0009:113 — existing `## Update — 2026-05-13` block is the format precedent; ADR immutability rule)
    - get-shit-done/bin/lib/shell-command-projection.cjs (read header — verify `projectShellCommand`, `isManagedHookBasename` function names exist; reword if not)
    - get-shit-done/bin/lib/core.cjs (grep for `normalizeMd` to verify it still exists at the line RESEARCH cites)
    - get-shit-done/bin/lib/installer-migrations.cjs (grep for `atomicWriteInstallState` to verify the rename)
    - bin/install.js (grep for `isGsdHookCommand` and `STALE_HOOK_BASENAMES` — confirm absence as RESEARCH states)
  </read_first>
  <action>
    Append a `## Update — 2026-05-25 (Phase 17, DOCS-03)` section to EACH of the two ADRs. Both sections go AT THE END of their respective files (after every existing line, including prior `## Update` blocks). NEVER edit any existing line in either ADR (Integration Gotchas / ADR immutability per Pitfall 12 + CF-05).

    For ADR 0009 (`docs/adr/0009-shell-command-projection-module.md`): append the drafted text from RESEARCH §Pattern 2 lines 311-320 verbatim. The section addresses 2 drift items: L14 settings.json qualifier + L41 formatHookCommandForShell rename. Before pasting, RE-VERIFY (a) the current function name in `shell-command-projection.cjs` (RESEARCH says `projectShellCommand`) — if the live name differs, adjust the supersession note to cite the actually-current name; (b) that `formatHookCommandForShell` is in fact absent from `bin/install.js`.

    For ADR 0010 (`docs/adr/0010-file-operation-engine-module.md`): append the drafted text from RESEARCH §DOCS-03 lines 595-606 verbatim. The section addresses 5 drift items: L7 normalizeMd, L36 isGsdHookCommand, L37 STALE_HOOK_BASENAMES, L50 contradiction, L52 writeFileAtomicSync rename. Before pasting, RE-VERIFY each function name claim against the live code (RESEARCH grounding date = 2026-05-25; if any name has moved since, update). The section MUST appear BELOW the existing line-3 "Superseded by ADR-0009" header, NOT above it.

    Both sections end with the closing line: `The body above this line records the original 2026-05-12 decision and the [supersession date if applicable] verbatim. Subsequent drift is captured here per ADR-supersession convention.`

    Commit message (verbatim): `docs(17.03): theme 3 — append supersession notes to ADR 0009 + 0010 (DOCS-03, 7 issues)`

    Do NOT touch the existing 2026-05-13 supersession block in ADR 0009. Do NOT edit the existing line-3 supersession header in ADR 0010. The new sections are PURELY additive.
  </action>
  <verify>
    <automated>grep -c "^## Update — 2026-05-25 (Phase 17, DOCS-03)" docs/adr/0009-shell-command-projection-module.md</automated>
    <automated>grep -c "^## Update — 2026-05-25 (Phase 17, DOCS-03)" docs/adr/0010-file-operation-engine-module.md</automated>
    <automated>grep -c "^## Update — 2026-05-13" docs/adr/0009-shell-command-projection-module.md</automated>
    <automated>tail -1 docs/adr/0009-shell-command-projection-module.md | grep -c "ADR-supersession convention"</automated>
    <automated>tail -1 docs/adr/0010-file-operation-engine-module.md | grep -c "ADR-supersession convention"</automated>
    <automated>git diff docs/adr/0009-shell-command-projection-module.md docs/adr/0010-file-operation-engine-module.md | grep -c "^-" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Both ADRs contain exactly 1 occurrence of `## Update — 2026-05-25 (Phase 17, DOCS-03)`
    - ADR 0009's existing `## Update — 2026-05-13` block is unchanged (still present, exactly 1 occurrence)
    - The last non-blank line of each updated ADR contains "ADR-supersession convention" (the standard closing line)
    - `git diff` shows append-only changes — count of removed lines (`^-` excluding diff headers `^---`) is 0 across both files
    - `git log -1 --format=%s` contains "theme 3" and "DOCS-03"
  </acceptance_criteria>
  <done>
    DOCS-03 closed. ADRs 0009 + 0010 carry append-only supersession notes for the 7 drift items. ADR immutability above-the-line preserved per CF-05 + Pitfall 12 Integration Gotchas.
  </done>
</task>

<task type="auto">
  <name>Task 4: Theme 4 commit — DOCS-04 renamed-hook + missing-helper references (6 sites, ~5 files)</name>
  <files>docs/USER-GUIDE.md, docs/FEATURES.md, docs/AGENTS.md, docs/CONTRIBUTING.md, CONTRIBUTING.md, CHANGELOG.md</files>
  <read_first>
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-04 lines 610-618 (full per-site fix table including D-01 path-rewrite)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §Theme 4 path-rewrites lines 373-383 (mechanical-rewrite pattern)
    - .changeset/jolly-newts-roam.md (evidence trail for D-01 verify-reapply-patches move)
    - .changeset/happy-jays-greet.md (same evidence trail)
    - docs/USER-GUIDE.md (line 1091 — verify drift target)
    - docs/FEATURES.md (line 2002 — verify the `gsd-commit-docs.js` hook claim)
    - docs/AGENTS.md (line 719 — verify the `references/doc-conflict-engine.md` path)
    - docs/CONTRIBUTING.md (lines 403 + 571 — verify both drift sites)
    - CONTRIBUTING.md (root copy — check if the same `scripts/verify-reapply-patches.cjs` reference exists at the parallel line; if not, omit this file from the commit)
    - CHANGELOG.md (line 346 — verify path; this is D-01-changelog-form decision: keep historical path + parenthetical)
    - package.json (read scripts section — verify exact build script names for the L571 fix; RESEARCH suggests `npm run build:hooks && npm run build:sdk && npm run build:cjs`)
    - get-shit-done/bin/verify-reapply-patches.cjs (verify file exists at this location — D-01)
    - hooks/gsd-read-guard.js (verify file exists at this location — for L1091 fix)
  </read_first>
  <action>
    Six per-site edits per RESEARCH §DOCS-04 table:

    1. `docs/USER-GUIDE.md:1091` — replace `gsd-read-before-edit.js` with `hooks/gsd-read-guard.js` (verify exact filename before commit; RESEARCH grounded this on 2026-05-25 inspection).
    2. `docs/FEATURES.md:2002` — REMOVE the claim that `gsd-commit-docs.js` is a hook. The concept lives in `tests/bug-2399-commit-docs-plan-phase.test.cjs` + `tests/commit-docs-bypass.test.cjs`, not a hook file. Either delete the sentence or rewrite to mention the test files instead — preserve surrounding prose context.
    3. `docs/AGENTS.md:719` — replace `references/doc-conflict-engine.md` with `get-shit-done/references/doc-conflict-engine.md` (add missing prefix).
    4. `docs/CONTRIBUTING.md:403` — replace `scripts/verify-reapply-patches.cjs` with `get-shit-done/bin/verify-reapply-patches.cjs` (D-01 path-rewrite per `.changeset/jolly-newts-roam.md` issue #2994).
    5. Root `CONTRIBUTING.md` — IF the file contains the same `scripts/verify-reapply-patches.cjs` reference (e.g., line 403 in the root copy, since `docs/CONTRIBUTING.md` and root `CONTRIBUTING.md` may both exist and may both drift), apply the same path-rewrite. If grep returns 0 hits on the root file, OMIT root `CONTRIBUTING.md` from the commit (the `files_modified` list is permissive — actually-touched files only land in the commit).
    6. `CHANGELOG.md:346` — per D-01-changelog-form decision: KEEP the historical `scripts/verify-reapply-patches.cjs` path and APPEND a parenthetical `(now at get-shit-done/bin/verify-reapply-patches.cjs)`. This preserves changelog-as-historical-record convention.
    7. `docs/CONTRIBUTING.md:571` (and root `CONTRIBUTING.md:571` if applicable) — replace `npm run build` with the actual per-target invocation. Read `package.json` `scripts` section first; if the build is decomposed into `build:hooks` + `build:sdk` + `build:cjs` as RESEARCH suggests, rewrite as `npm run build:hooks && npm run build:sdk && npm run build:cjs` (or the live equivalent).

    Commit message (verbatim): `docs(17.03): theme 4 — fix renamed-hook + missing-helper references (DOCS-04, 5 sites)`

    Do NOT modify code in `hooks/`, `tests/`, `get-shit-done/bin/`, or `package.json`. This commit is docs-only.
  </action>
  <verify>
    <automated>grep -c "hooks/gsd-read-guard\.js" docs/USER-GUIDE.md</automated>
    <automated>grep -c "gsd-read-before-edit\.js" docs/USER-GUIDE.md</automated>
    <automated>grep -c "gsd-commit-docs\.js" docs/FEATURES.md</automated>
    <automated>grep -c "get-shit-done/references/doc-conflict-engine\.md" docs/AGENTS.md</automated>
    <automated>grep -c "^references/doc-conflict-engine\.md\|[^/]references/doc-conflict-engine\.md" docs/AGENTS.md</automated>
    <automated>grep -c "get-shit-done/bin/verify-reapply-patches\.cjs" docs/CONTRIBUTING.md CHANGELOG.md</automated>
    <automated>grep -c "now at get-shit-done/bin/verify-reapply-patches\.cjs" CHANGELOG.md</automated>
    <automated>grep -cE "npm run build:(hooks|sdk|cjs)" docs/CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/USER-GUIDE.md`: forward-grep on `hooks/gsd-read-guard.js` returns ≥1; reverse-grep on `gsd-read-before-edit.js` returns 0
    - `docs/FEATURES.md`: reverse-grep on `gsd-commit-docs.js` (as a hook claim) returns 0 OR the surrounding prose now references test files instead
    - `docs/AGENTS.md:719`: the bare `references/doc-conflict-engine.md` (without `get-shit-done/` prefix) no longer appears; the prefixed form is present
    - `docs/CONTRIBUTING.md`: forward-grep on `get-shit-done/bin/verify-reapply-patches.cjs` returns ≥1
    - `CHANGELOG.md`: contains BOTH `scripts/verify-reapply-patches.cjs` (historical) AND `now at get-shit-done/bin/verify-reapply-patches.cjs` (parenthetical) — preserves D-01-changelog-form
    - `docs/CONTRIBUTING.md`: build invocation uses decomposed `npm run build:hooks`/`:sdk`/`:cjs` or equivalent per live package.json
    - `git log -1 --format=%s` contains "theme 4" and "DOCS-04"
  </acceptance_criteria>
  <done>
    DOCS-04 closed. All 5 renamed-hook/missing-helper references fixed per RESEARCH table + D-01 + D-01-changelog-form decisions.
  </done>
</task>

<task type="auto">
  <name>Task 5: Theme 5 commit — DOCS-05 phase-3 archived references anchored to closure change_ids</name>
  <files>docs/test-triage/jj-bugs.md</files>
  <read_first>
    - docs/test-triage/jj-bugs.md (read lines 1-50 — locate L5, L8, L33 references)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-05 lines 624-630 (closure change_id table) + §Pattern 3 lines 330-334 (replacement table) + key finding #3 (the three change_ids verified)
    - .planning/phases/17-drift-control-reconciliation/17-PATTERNS.md §docs/test-triage/jj-bugs.md lines 332-352 (closure change_id anchoring pattern)
  </read_first>
  <action>
    Replace 3 dangling references with closure change_id anchors per RESEARCH §DOCS-05 + §Pattern 3:

    1. `docs/test-triage/jj-bugs.md:5` — replace `(03-07-PLAN.md)` with ``(Phase 3 plan 03-07 closure commit `41db442f` — docs(03-07): phase-close finalization, 2026-05-12)``
    2. `docs/test-triage/jj-bugs.md:8` — replace `(03-RESEARCH.md)` with ``(Phase 3 research synthesis archived; see closure commit `41db442f` 2026-05-12)``
    3. `docs/test-triage/jj-bugs.md:33` — replace `(03-06-PLAN.md)` with ``(Phase 3 plan 03-06 closure commit `2fbcd590` — docs(03-06): complete push/fetch + workspace + TEST-08 triage plan)``

    Before each replacement, RE-VERIFY the line numbers (RESEARCH date = 2026-05-25; if other Phase 17 edits or unrelated edits shifted the lines, locate by anchor text rather than line number). Use the exact change_ids verbatim — `41db442f` for the Phase 3 03-07 closure (covers both 03-07-PLAN.md and 03-RESEARCH.md), `2fbcd590` for the Phase 3 03-06 closure.

    Do NOT DELETE the surrounding bullet text — the references are part of larger sentences; preserve sentence structure (per Pitfall 12 forensic trail; CF-05).
    Do NOT use `e190eee3` for the 03-06-PLAN.md reference — RESEARCH explicitly identifies `2fbcd590` as the 03-06-PLAN.md closure (e190eee3 is the TEST-08 triage commit, a different artifact).

    Commit message (verbatim): `docs(17.03): theme 5 — anchor phase-3 archived references to closure change_ids (DOCS-05, 3 sites)`
  </action>
  <verify>
    <automated>grep -c "41db442f" docs/test-triage/jj-bugs.md</automated>
    <automated>grep -c "2fbcd590" docs/test-triage/jj-bugs.md</automated>
    <automated>grep -cE "\(03-(06|07)-PLAN\.md\)" docs/test-triage/jj-bugs.md</automated>
    <automated>grep -c "(03-RESEARCH\.md)" docs/test-triage/jj-bugs.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "41db442f" docs/test-triage/jj-bugs.md` returns ≥2 (used for both 03-07-PLAN and 03-RESEARCH anchors)
    - `grep -c "2fbcd590" docs/test-triage/jj-bugs.md` returns ≥1 (used for 03-06-PLAN anchor)
    - Reverse-grep on the bare-parenthetical forms `(03-06-PLAN.md)`, `(03-07-PLAN.md)`, `(03-RESEARCH.md)` returns 0 in `docs/test-triage/jj-bugs.md`
    - `git log -1 --format=%s` contains "theme 5" and "DOCS-05"
  </acceptance_criteria>
  <done>
    DOCS-05 closed. 3 dangling archived-planning references anchored to stable closure change_ids per Pitfall 12.
  </done>
</task>

<task type="auto">
  <name>Task 6: Theme 7 commit — DOCS-06 pt-BR translation date/link mismatches</name>
  <files>docs/pt-BR/superpowers/README.md, docs/pt-BR/superpowers/plans/2026-03-23-materialize-new-project-config.md, docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md</files>
  <read_first>
    - docs/pt-BR/superpowers/README.md (read lines 1-20 — locate L7 date mismatch)
    - docs/pt-BR/superpowers/plans/2026-03-23-materialize-new-project-config.md (read line 4 — links to en `2026-03-18-...`)
    - docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md (read line 4 — circular ref)
    - docs/superpowers/specs/2026-03-20-multi-project-workspaces-design.md (verify en source path — for fixing the circular ref)
    - docs/superpowers/plans/2026-03-18-materialize-new-project-config.md (verify en source path — for the README L7 + plans L4 link targets)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-06 lines 634-638 (full per-site fix table; recommended fix (a) = rename file 2026-03-23 → 2026-03-18)
  </read_first>
  <action>
    Three pt-BR fixes per RESEARCH §DOCS-06:

    1. **Rename file** `docs/pt-BR/superpowers/plans/2026-03-23-materialize-new-project-config.md` → `docs/pt-BR/superpowers/plans/2026-03-18-materialize-new-project-config.md`. This matches the en convention (en uses `2026-03-18`). Use `git mv` (or `jj` equivalent — see project skills); a rename preserves history. After rename, verify the file's internal L4 link points at the en source `docs/superpowers/plans/2026-03-18-materialize-new-project-config.md` (or the appropriate en target — RESEARCH says it should match the en convention).
    2. `docs/pt-BR/superpowers/README.md:7` — update the link to match the renamed file (now `2026-03-18`). If the README currently links to `2026-03-23`, change to `2026-03-18`; if it currently links to `2026-03-18` and the date label says `2026-03-23`, fix the label.
    3. `docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md:4` — fix the circular ref. RESEARCH suggests either (a) replace with link to en source `docs/superpowers/specs/2026-03-20-multi-project-workspaces-design.md` (verify en source exists), OR (b) remove the circular line entirely. Pick (a) if the en source exists; (b) otherwise.

    Commit message (verbatim): `docs(17.03): theme 7 — fix pt-BR translation date/link mismatches (DOCS-06, 3 sites)`

    The `files_modified` list reflects the POST-rename state; the rename itself is a single git mv (one file move + content edits on its content). If using jj, prefer `jj` native rename semantics per project skills.
  </action>
  <verify>
    <automated>test -f docs/pt-BR/superpowers/plans/2026-03-18-materialize-new-project-config.md</automated>
    <automated>test ! -f docs/pt-BR/superpowers/plans/2026-03-23-materialize-new-project-config.md</automated>
    <automated>grep -c "2026-03-23" docs/pt-BR/superpowers/README.md</automated>
    <automated>grep -c "2026-03-18" docs/pt-BR/superpowers/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/pt-BR/superpowers/plans/2026-03-18-materialize-new-project-config.md` EXISTS
    - File `docs/pt-BR/superpowers/plans/2026-03-23-materialize-new-project-config.md` does NOT exist
    - `docs/pt-BR/superpowers/README.md` references `2026-03-18` (forward) ≥1 time; references `2026-03-23` (reverse) 0 times (or all such refs have been updated)
    - `docs/pt-BR/superpowers/specs/2026-03-20-multi-project-workspaces-design.md:4` no longer links to itself (circular ref resolved)
    - `git log -1 --format=%s` contains "theme 7" and "DOCS-06"
  </acceptance_criteria>
  <done>
    DOCS-06 closed. 3 pt-BR date/link mismatches fixed per RESEARCH recommended fix (a) (file rename to match en convention).
  </done>
</task>

<task type="auto">
  <name>Task 7: Theme 8 commit — DOCS-07 singletons (CONTEXT.md path, USER-PROFILE qualifier, mutation-subprocess strip)</name>
  <files>CONTEXT.md, docs/AGENTS.md, docs/ja-JP/AGENTS.md, CHANGELOG.md</files>
  <read_first>
    - CONTEXT.md (read line 610 — locate `tests/lint-no-source-grep.cjs` reference; D-07)
    - docs/AGENTS.md (read line 404 — verify the IDENTICAL unqualified `USER-PROFILE.md` prose RESEARCH key finding #5 documents)
    - docs/ja-JP/AGENTS.md (read line 389 — same prose in ja-JP; D-08)
    - CHANGELOG.md (read around line 389 — locate the `mutation-subprocess.integration.test.ts` reference in the issue-#2302 parenthetical; D-09)
    - scripts/lint-no-source-grep.cjs (verify file exists at this target path)
    - sdk/src/golden/golden-mutation-covered.ts (verify the "*see mutation-subprocess.integration.test.ts when present*. Empty until those tests land" docstring still exists — confirms D-09 resolution)
    - .planning/phases/17-drift-control-reconciliation/17-RESEARCH.md §DOCS-07 lines 642-647 (full singleton fix table) + key finding #4 (D-09 file never shipped) + key finding #5 (D-08 en+ja-JP symmetry)
    - .planning/phases/17-drift-control-reconciliation/17-CONTEXT.md §D-07 + §D-08 + §D-09
  </read_first>
  <action>
    Three singleton fixes per RESEARCH §DOCS-07 + plan-frontmatter decisions D-08-symmetry and D-09-resolution:

    1. **D-07 — CONTEXT.md:610**: replace `tests/lint-no-source-grep.cjs` with `scripts/lint-no-source-grep.cjs`. Single-line mechanical path-rewrite. Before commit verify `scripts/lint-no-source-grep.cjs` exists at that target path.

    2. **D-08 (with symmetry) — USER-PROFILE.md qualifier**:
       - `docs/AGENTS.md:404` — add path qualifier so the bare `USER-PROFILE.md` reads `$HOME/.claude/USER-PROFILE.md`. Preserve surrounding sentence structure; replace only the bare filename reference.
       - `docs/ja-JP/AGENTS.md:389` — apply the IDENTICAL qualifier (per plan decision D-08-symmetry) so en + ja-JP stay in lockstep per D-04. The qualifier text is the same (`$HOME/.claude/USER-PROFILE.md`); only the surrounding Japanese prose differs.
       - Re-verify exact line numbers before each edit (other tasks in this plan may have shifted the line numbers; locate by anchor prose if line drift suspected).

    3. **D-09 — CHANGELOG.md:389**: per plan decision D-09-resolution (in-line strip): remove the specific filename `mutation-subprocess.integration.test.ts` from the parenthetical at L389, keeping the surrounding bullet text and other items in the same parenthetical intact. The aim: the rest of the sentence remains a coherent record of issue #2302's coverage, just without naming a file that was never shipped. If in-line strip would leave a syntactically broken parenthetical (e.g., the strip leaves orphan punctuation), fall back to the append-only correction-note path (CONTEXT D-09 fallback): leave L389 intact and append a `## Update — 2026-05-25 (Phase 17, DOCS-07)` section at the end of CHANGELOG.md noting the filename never landed.

    Commit message (verbatim): `docs(17.03): theme 8 — singleton path-rewrites: lint-no-source-grep, USER-PROFILE qualifier, mutation-subprocess strip (DOCS-07, 3 sites)`

    Do NOT touch `sdk/src/golden/golden-mutation-covered.ts` (it correctly documents the file's never-shipped status).
    Do NOT add a `## Tests` family to INVENTORY.md (already decided D-10 in 17.02).
  </action>
  <verify>
    <automated>grep -c "scripts/lint-no-source-grep\.cjs" CONTEXT.md</automated>
    <automated>grep -c "tests/lint-no-source-grep\.cjs" CONTEXT.md</automated>
    <automated>grep -c "\$HOME/\.claude/USER-PROFILE\.md" docs/AGENTS.md docs/ja-JP/AGENTS.md</automated>
    <automated>grep -c "mutation-subprocess\.integration\.test\.ts" CHANGELOG.md</automated>
    <automated>test -f scripts/lint-no-source-grep.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `CONTEXT.md`: forward-grep on `scripts/lint-no-source-grep.cjs` returns ≥1; reverse-grep on `tests/lint-no-source-grep.cjs` returns 0
    - `docs/AGENTS.md` + `docs/ja-JP/AGENTS.md` combined forward-grep on `$HOME/.claude/USER-PROFILE.md` returns ≥2 (both locales qualify per D-08-symmetry)
    - `CHANGELOG.md`: forward-grep on `mutation-subprocess.integration.test.ts` returns 0 (in-line strip resolution) OR the file contains `## Update — 2026-05-25 (Phase 17, DOCS-07)` (fallback append path)
    - `scripts/lint-no-source-grep.cjs` exists (target path is valid)
    - `git log -1 --format=%s` contains "theme 8" and "DOCS-07"
  </acceptance_criteria>
  <done>
    DOCS-07 closed. All 3 singleton drift sites fixed per D-07 + D-08-symmetry + D-09-resolution.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: DOCS-09 close-gate — /gsd:docs-update --verify-only re-run</name>
  <what-built>
    Tasks 1-7 of this plan committed 7 atomic per-theme fixes addressing DOCS-01..07 per Pitfall 12 + CF-05. This task is the close-gate: re-run `/gsd:docs-update --verify-only` (or equivalent local invocation) to confirm the cumulative pass rate is ≥ 99%. Per RESEARCH §DOCS-09 lines 651-657 the pre-Phase-17 baseline was 45 failures at 97.6% pass; the post-fix expectation is failures ≤ ~2 (legitimate flags only).
  </what-built>
  <how-to-verify>
    1. From the repo root, run `/gsd:docs-update --verify-only` (or the equivalent local invocation — workflow is shipped under `get-shit-done/workflows/docs-update.md`).
    2. Capture the run's exit code and failure-count summary into `.planning/intel/docs-update-verify-only-2026-05-25-postfix-pass-rate.md`. Required content in that file:
       - Header: `# Docs-Update Verify-Only Postfix Pass Rate — Phase 17 (DOCS-09)`
       - `**Run date:** 2026-05-25`
       - `**Pre-fix baseline:** 45 failures across 8 themes (97.6% pass), per .planning/todos/pending/v14-docs-verify-only-followups.md`
       - `**Post-fix result:** <N> failures (<P>% pass rate)`
       - List of any remaining legitimate-flag failures (with rationale per flag).
       - Closing line: `DOCS-09 close-gate: PASSED` (if ≥ 99%) or `DOCS-09 close-gate: BLOCKED — escalate` (if < 99%).
    3. ALSO re-run the Wave 2 drift tests: `node --test tests/architecture-counts.test.cjs tests/command-count-sync.test.cjs tests/inventory-counts.test.cjs` — both must exit 0 (no regression from Wave 3 prose edits).
    4. Commit the close-gate artifact: `docs(17.03): docs-update verify-only close-gate (DOCS-09) — pass rate ≥ 99%`. Files in commit: `.planning/intel/docs-update-verify-only-2026-05-25-postfix-pass-rate.md` only.
    5. If pass rate < 99%: STOP. Do NOT mark Phase 17 ready for Wave 4. Surface the failing themes to the user; planner may need to extend Wave 3 with additional gap-closure tasks.
  </how-to-verify>
  <resume-signal>Type "approved" (pass rate ≥ 99% confirmed; artifact committed; drift tests still green) or describe issues (residual failures + your proposed next step).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in scope) | Pure docs+prose edits across ~18 markdown files plus 2 intel artifacts. No executable surface added; no I/O, no network, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17.03-01 | Tampering | ADR immutability above-the-line (Theme 3) | mitigate | Acceptance criteria for Task 3 verify zero removed lines (`grep ^- in diff returns 0`) — append-only changes only. Both ADRs preserve their pre-2026-05-25 content verbatim. |
| T-17.03-02 | Information Disclosure | Theme 1 path leaks | mitigate | The 14 sites currently leak an author's machine path; this plan replaces them with `$HOME`-relative form. Net reduction in disclosure surface. |
| T-17.03-03 | Tampering | Theme 5 forensic trail | mitigate | Task 5 anchors archived-planning references to closure change_ids rather than deleting them (Pitfall 12). The forensic trail to Phase 3 closure commits is preserved. |
| T-17.03-04 | Repudiation | Per-theme commit granularity | mitigate | 7 atomic per-theme commits (Pitfall 12) plus 1 close-gate commit preserve per-REQ-ID bisect granularity; each commit is recoverable independently. |

**No in-scope threats requiring new mitigations.** Pure docs phase per CONTEXT §code_context.
</threat_model>

<verification>
## Plan-level verification

1. `.planning/intel/docs-update-fix-triage.md` exists and was written BEFORE any theme commit (CF-07 sequencing — Task 0 runs first).
2. Exactly 7 per-theme commits + 1 close-gate commit (8 commits total) for this plan.
3. ADRs 0009 + 0010 carry append-only `## Update — 2026-05-25 (Phase 17, DOCS-03)` sections; no in-place edits above existing content (Task 3 acceptance gate).
4. `node --test tests/architecture-counts.test.cjs tests/command-count-sync.test.cjs tests/inventory-counts.test.cjs` still exits 0 (Wave 2 drift guards not regressed).
5. `/gsd:docs-update --verify-only` post-fix pass rate ≥ 99% (DOCS-09 close-gate artifact records the result).
</verification>

<success_criteria>
- DOCS-01..07 + DOCS-09 closed (8 REQ-IDs).
- 7 per-theme commits + 1 close-gate commit (Pitfall 12 + CF-05 per-theme commits preserved).
- ADR immutability above-the-line preserved (Theme 3).
- Forensic trail to Phase 3 closure commits preserved (Theme 5).
- D-04 lockstep across en + ja-JP for USER-PROFILE qualifier (D-08-symmetry).
- D-10 (Tests inventory family) NOT added — INVENTORY.md untouched in this plan.
- CF-07 prerequisite (`docs-update-fix-triage.md`) authored as Task 0 before any theme fix.
</success_criteria>

<output>
Create `.planning/phases/17-drift-control-reconciliation/17-03-SUMMARY.md` when done.
</output>
