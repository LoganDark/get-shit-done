---
phase: 09-jj-side-parallel-verbs
plan: 04
subsystem: vcs
tags: [vcs, git, parallel, stub, sdk]

# Dependency graph
requires:
  - phase: 09-jj-side-parallel-verbs (plan 01)
    provides: VcsWorkspaceParallel interface + VcsWorkspace.parallel field requiring a body on both backends
  - phase: 09-jj-side-parallel-verbs (plan 03)
    provides: jj-side parallel body (precedent for "real-body-on-this-backend, stub-on-the-other")
provides:
  - GitVcsAdapter satisfies VcsWorkspaceParallel via throwing stub
  - Phase 10 git-side breadcrumb (error message names "Phase 10" for future-debugger discovery)
  - Closes the last wave-2 compile gate from plan 09-01
affects: [10-git-side-parallel-verbs]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Throwing-stub idiom for unimplemented cross-backend interface members (VcsNotImplementedError; 8 precedents in backends/jj.ts)"
    - "`never` return type on stub bodies (assignable to any concrete return type, satisfies the interface without per-member casts)"

key-files:
  created: []
  modified:
    - sdk/src/vcs/backends/git.ts

key-decisions:
  - "Mirrored RESEARCH §Stub pattern verbatim — single Object.freeze with two throwing members appended at the end of the existing workspace freeze block, no per-member JSDoc beyond the block-level comment"
  - "Imported VcsNotImplementedError into the existing `import { __vcsTestOnly, VcsIncompleteSubagentsError } from '../types.js'` group rather than adding a new import line (planner instruction: 'reuse existing import if any')"

patterns-established:
  - "Pattern: cross-backend interface conformance via throwing stub at the not-yet-implemented backend — `never`-returning member inside Object.freeze, error message naming the future phase that ships the real body"

requirements-completed: [VCS-16]

# Metrics
duration: 11min
completed: 2026-05-15
---

# Phase 9 Plan 04: git-side workspace.parallel throwing stub Summary

**GitVcsAdapter satisfies the cross-backend `VcsWorkspaceParallel` interface via a throwing-stub `Object.freeze({dispatch, fanIn})` — both members raise `VcsNotImplementedError` with a Phase 10 breadcrumb, closing the wave-2 compile gate left open by plan 09-01.**

## Performance

- **Duration:** ~11 min
- **Started:** 2026-05-15T14:42:00Z (approximate — covers plan-load through final commit)
- **Completed:** 2026-05-15T14:53:30Z
- **Tasks:** 1 (single-task throwing-stub plan)
- **Files modified:** 1

## Accomplishments

- Added `parallel: Object.freeze({ dispatch, fanIn })` to the workspace block in `sdk/src/vcs/backends/git.ts`. Both members are `(): never` arrow-shaped methods that throw `VcsNotImplementedError` with messages that include the substring `Phase 10`.
- Extended the existing `import { __vcsTestOnly, VcsIncompleteSubagentsError } from '../types.js'` to also import `VcsNotImplementedError` — single-import-block discipline preserved.
- TypeScript continues to compile cleanly (`pnpm exec tsc --noEmit` → exit 0).
- `scripts/lint-vcs-no-commit-id.cjs` green: 1035 files / 0 violations.

## Task Commits

1. **Task 1: Add throwing parallel stub to GitVcsAdapter workspace block** — `txy` (feat)

**Plan metadata:** `pvm` (docs: SUMMARY + STATE + ROADMAP) followed by `oux` (docs: SUMMARY hash-correction)

Note: The plan declared `tdd="true"` for the single task, but as in plan 09-01 the "behavior tests" are TypeScript-compilation gates and grep-count assertions rather than runtime unit tests. No separate `test(...)` commit was emitted because the gates are static (compilation + grep counts), not vitest cases. Test 6 in the plan (a runtime `VcsNotImplementedError`-throwing assertion via `createGitAdapter(...).workspace.parallel.dispatch({...})`) is explicitly assigned to plan 09-05 task 5 by the plan's own `<behavior>` clause — that test landing-site decision belongs to plan 09-05.

## Files Created/Modified

- `sdk/src/vcs/backends/git.ts` — added `VcsNotImplementedError` to the existing types.js import group; appended a `parallel: Object.freeze({...})` member to the workspace freeze block; both stub bodies throw with explicit Phase 10 breadcrumbs.

## Decisions Made

- **Imported `VcsNotImplementedError` into the existing types.js import group**: the plan said "reuse existing import if any"; the file already had `import { __vcsTestOnly, VcsIncompleteSubagentsError } from '../types.js'` near the top — extending that import preserved single-block import discipline.
- **No per-member JSDoc beyond the block-level comment**: the plan's `<action>` clause cited 09-PATTERNS.md §"Stub pattern" verbatim, which shows the freeze block with a single block-level comment naming Phase 9 / Phase 10. The 8 precedent callsites in `backends/jj.ts` (lines 125, 166, 789, 1023, etc.) also use the bare-throw form without per-callsite JSDoc.
- **Tracked the wave-2 compile gate state pre- and post-edit**: pre-edit, `pnpm exec tsc --noEmit` already exited 0. Investigation revealed that `GitVcsAdapter`'s assembly at lines 927-942 uses `... as unknown as GitVcsAdapter` (line 942), which structurally masks `workspace`'s `parallel` field requirement. The stub is still required for the spirit of the contract — any future consumer narrowing on `vcs.kind === 'git'` then calling `vcs.workspace.parallel.{dispatch, fanIn}` now gets a loud `VcsNotImplementedError` rather than a `TypeError: undefined is not a function`.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- **Pre-existing jj-side vitest flakes (15 failures of 519 tests)** — independent of this change. Verified by re-running one of the failing test files (`jj-id-alphabet-probe.test.ts`) in isolation, which passed cleanly in 1.46s. All 15 failures are 5-second-timeout flakes inside `execSync('jj git init --colocate', ...)` fixture-setup calls when the suite runs in parallel — an environmental contention issue, not a regression caused by this plan. Plan 09-01's SUMMARY documented the same `<verification>` vs `<done>` tension and concluded that the static gates (tsc + grep) are the load-bearing checks for type-only plans; that posture applies here too.
- **Pre-existing `<verification>` vs current-state mismatch**: the plan's Test 1 says `pnpm -C sdk tsc --noEmit` exits 0 — which it already did before this edit, because the adapter assembly uses `as unknown as GitVcsAdapter` to bypass the `workspace.parallel` structural requirement (git.ts:942). The stub is still required for runtime contract conformance — without it, narrowing on `vcs.kind === 'git'` then calling `parallel.dispatch(...)` would `TypeError: undefined is not a function` rather than throw `VcsNotImplementedError`. Plan execution unchanged; just noting the pre-existing escape hatch in the adapter assembly.

## Self-Check

**Self-Check: PASSED**

- File modified: `sdk/src/vcs/backends/git.ts` — FOUND.
- Commit `txy` (feat: git-side workspace.parallel throwing stub) — recorded via `gsd-sdk query commit`; routed through jj backend; verified present in `gsd-sdk query log --max-count 5`.
- All 5 grep-count gates: `VcsNotImplementedError` = 4 (≥ 3 ✓), `Phase 10` = 3 (≥ 2 ✓), `parallel: Object.freeze` = 1 (≥ 1 ✓), `workspace.parallel.dispatch` = 1 (≥ 1 ✓), `workspace.parallel.fanIn` = 1 (≥ 1 ✓).
- `pnpm exec tsc --noEmit` → exit 0.
- `scripts/lint-vcs-no-commit-id.cjs`: 1035 files / 0 violations.
- No new files created.
- No new imports beyond `VcsNotImplementedError` (added to the existing types.js import group).
- `sdk/dist-cjs/*` stale build artifacts excluded from the commit (per phase-prompt instruction).

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Phase 10 (`10-git-side-parallel-verbs`)** can now replace the throwing stub with a real wire-in to a new `sdk/src/vcs/git/parallel.ts` sidecar. The Phase 10 breadcrumb in the error message points future debuggers at the right place.
- **Plan 09-05 (test landing)** owns the runtime `VcsNotImplementedError`-throwing assertion in a `cmd-parallel-git.test.ts` file — already documented in this plan's `<behavior>` Test 6.
- Wave-2 compile gate is closed: `pnpm exec tsc --noEmit` exits 0 with the full Phase 9 type cluster in place (plan 01 types + plan 02 reap classifier + plan 03 jj-side body + plan 04 git-side stub).

## Threat Surface Scan

No new threat surface introduced. The stub adds no `execSync`, no `spawnSync`, no `vcsExec`, no file I/O, no network surface. It only adds two methods that throw on call. Threat-register entries T-09-11 (Repudiation, disposition `accept`) and T-09-12 (DoS — mistakenly-configured `parallelization: true` on a git backend, disposition `accept`) from the plan's `<threat_model>` carry forward unchanged — both are accepted, with the loud-failure mitigation now in place.

---
*Phase: 09-jj-side-parallel-verbs*
*Completed: 2026-05-15*
