---
phase: 09-jj-side-parallel-verbs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/vcs/types.ts
autonomous: true
requirements:
  - VCS-16
  - VCS-17
  - VCS-19
  - PARALLEL-05
tags:
  - vcs
  - types
  - parallel
  - jj

must_haves:
  truths:
    - "VcsAdapterCommon.workspace exposes a typed `parallel` sub-namespace declaring `dispatch` and `fanIn`."
    - "VcsWorkspaceParallel.fanIn signature is exactly `(handle, results): FanInResult` (two args, per D-04 — rejects the one-arg `fanIn(wave)` ARCHITECTURE.md draft)."
    - "ParallelDispatchHandle is a pure-JSON shape with the field set locked by D-06."
    - "ParallelAgentResult (the elements of the fanIn `results` array) is exactly `{ agentId: string; exitCode: number; lastChangeId?: string; stderr?: string }` (D-07; `lastChangeId` optional — reap covers crash-recovery)."
    - "IncompleteWorkEntry.reason is a closed string-literal union of exactly 2 values per D-09."
    - "ParallelDispatchHandle.workspaces[].baseRev JSDoc carries the D-14 canonical wording (PARALLEL-05)."
  artifacts:
    - path: sdk/src/vcs/types.ts
      provides: "VcsWorkspaceParallel + ParallelDispatchOpts + ParallelDispatchHandle + ParallelAgentResult + FanInResult exports; tightened IncompleteWorkEntry.reason union; `parallel` field on VcsWorkspace."
      contains: "export interface VcsWorkspaceParallel"
  key_links:
    - from: sdk/src/vcs/types.ts
      to: sdk/src/vcs/backends/jj.ts
      via: "VcsWorkspace.parallel field consumed by jj adapter wire-in"
      pattern: "VcsWorkspaceParallel"
    - from: sdk/src/vcs/types.ts
      to: sdk/src/vcs/backends/git.ts
      via: "VcsWorkspace.parallel field consumed by git adapter throwing stub"
      pattern: "VcsWorkspaceParallel"
---

<objective>
Land the cross-backend type surface for the `vcs.workspace.parallel.*` verbs in `sdk/src/vcs/types.ts`: add five new exports (`VcsWorkspaceParallel`, `ParallelDispatchOpts`, `ParallelDispatchHandle`, `ParallelAgentResult`, `FanInResult`); extend `VcsWorkspace` with a `parallel: VcsWorkspaceParallel` field; tighten `IncompleteWorkEntry.reason` from free-form `string` to the closed union `'crashed-with-uncommitted-work' | 'merge-in-tree-conflict'`.

Purpose: Both backends (jj body in plan 03; git throwing stub in plan 04) and the reap classifier extension (plan 02) compile against this contract. Tightening `IncompleteWorkEntry.reason` here makes the parse-time validator extension in plan 02 a type-narrowing operation rather than an interface change.

Output: One modified file; `pnpm -C sdk tsc --noEmit` green; downstream wave-2 plans unblocked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md
@.planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md
@sdk/src/vcs/types.ts

<interfaces>
<!-- Existing in-tree shapes the new declarations sit alongside / extend. -->
<!-- Verbatim file:line citations from 09-PATTERNS.md and 09-RESEARCH.md. -->

From sdk/src/vcs/types.ts:242-247 (current IncompleteWorkEntry — Phase 9 tightens reason field):
- subagentName: string
- changeIdShort: string
- workspacePath: string
- reason: string          // tightens to literal union

From sdk/src/vcs/types.ts:356-379 (VcsBookmarks — sub-namespace precedent for VcsWorkspaceParallel placement):
- typed sub-namespace inside an adapter top-level property
- mirrors the shape this plan adds for `parallel`

From sdk/src/vcs/types.ts:392-425 (VcsWorkspace — extension point):
- adds new `parallel: VcsWorkspaceParallel` member (append after existing members)

From sdk/src/vcs/types.ts:573-578 (VcsNotImplementedError — already exists, NO extension; consumed by plan 04 git stub).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the five new types + extend VcsWorkspace.parallel + tighten IncompleteWorkEntry.reason</name>
  <files>sdk/src/vcs/types.ts</files>
  <read_first>
    - sdk/src/vcs/types.ts (lines 230-247 for IncompleteWorkEntry + WorkspaceMergeResult shape precedent; lines 356-379 for VcsBookmarks sub-namespace precedent; lines 392-425 for VcsWorkspace extension point; lines 573-578 for VcsNotImplementedError class — already exists, do not duplicate)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/src/vcs/types.ts (type declarations — MOD)" — exact shapes to land for each interface, plus the IncompleteWorkEntry tightening recipe and the PARALLEL-05 JSDoc canonical wording)
    - .planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md (D-05, D-06, D-07, D-08, D-09, D-14 for shape/JSDoc constraints)
  </read_first>
  <behavior>
    - Test 1: TypeScript compiles — `pnpm -C sdk tsc --noEmit` exits 0 with the new exports present.
    - Test 2: `VcsWorkspaceParallel` exists and exposes exactly two members named `dispatch` and `fanIn`, whose call signatures match `(opts: ParallelDispatchOpts) => ParallelDispatchHandle` and `(handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]) => FanInResult`.
    - Test 3: `ParallelDispatchHandle` field set is exactly `phaseRoot`, `phaseNumber`, `mainBookmark`, `manifest`, `workspaces`. The `workspaces` element shape is exactly `{ name; path; baseRev; agentId; baselineOpId? }` (all string fields except `baselineOpId?` which is `string | undefined`).
    - Test 4: `FanInResult` field set is exactly `merged: readonly string[]`, `conflicted: boolean`, `conflictedPaths: readonly string[]`, `incompleteQueued: number`, `failedReaped: readonly string[]`, `surplusBookmarks: readonly string[]`. No `liveWorkspaces` field. No `partial` field.
    - Test 5: `ParallelAgentResult` field set is exactly `agentId: string`, `exitCode: number`, `lastChangeId?: string`, `stderr?: string`.
    - Test 6: `ParallelDispatchOpts` field set is exactly `plan: readonly { agentId; planId; workspacePath? }[]`, `phaseNumber: number`, `mainBookmark: string`, `maxConcurrency?: number`.
    - Test 7: `IncompleteWorkEntry.reason` type resolves to the string-literal union `'crashed-with-uncommitted-work' | 'merge-in-tree-conflict'` (verified by `grep -c "'crashed-with-uncommitted-work' | 'merge-in-tree-conflict'" sdk/src/vcs/types.ts` ≥ 1, excluding comments).
    - Test 8: `VcsWorkspace` interface contains a member `parallel: VcsWorkspaceParallel` (grep for `parallel: VcsWorkspaceParallel` inside the `interface VcsWorkspace` block; appears exactly once).
    - Test 9: PARALLEL-05 JSDoc on `ParallelDispatchHandle.workspaces[].baseRev` contains the canonical D-14 strings `change_id on jj`, `commit_id on git`, `rebase-stable`, and `(PARALLEL-05, D-14.)` (verified by `grep -c` ≥ 1 on each substring).
    - Test 10: D-01 forward-compat note on `baselineOpId` references `D-01` and `forward-compat` in JSDoc (or the field is omitted entirely — planner discretion per D-06; if omitted, baselineOpId MUST NOT appear in the interface at all).
  </behavior>
  <action>
    Modify `sdk/src/vcs/types.ts` in-place. Five additions and two edits, ordered:

    1. **Tighten `IncompleteWorkEntry.reason` (replaces existing `reason: string` at types.ts:246):** change the field's type annotation from `string` to the closed union literal `'crashed-with-uncommitted-work' | 'merge-in-tree-conflict'`. Add JSDoc immediately above the field documenting both values and citing `D-09` plus the fail-safe phase-merge gate at `backends/jj.ts:182-194`. Reference 09-PATTERNS.md §"sdk/src/vcs/types.ts" subsection "IncompleteWorkEntry tightening" for exact JSDoc text.

    2. **Add `ParallelDispatchOpts`** (export interface): field set per `<behavior>` Test 6. Place adjacent to `WorkspaceMergeResult` block at types.ts:230-235 OR adjacent to the new VcsWorkspaceParallel block — planner discretion per CONTEXT D-06 "Claude's Discretion" bullet 1.

    3. **Add `ParallelAgentResult`** (export interface): field set per `<behavior>` Test 5. D-07 shape; same placement neighborhood as `ParallelDispatchOpts`.

    4. **Add `ParallelDispatchHandle`** (export interface): field set per `<behavior>` Test 3. D-06 shape with `Object.freeze`-ready (no methods, no Symbols, no closures — interface-level only; runtime freeze is plan 03's concern). PARALLEL-05 JSDoc on `baseRev` MUST contain the canonical D-14 wording per 09-PATTERNS.md §"change_id-only on cross-backend surface" excerpt. `baselineOpId?: string` MUST carry a JSDoc note citing D-01 forward-compat reservation (or be omitted entirely per CONTEXT D-06 "Claude's Discretion" bullet 3 — if omitted, do NOT include in any handle shape).

    5. **Add `FanInResult`** (export interface): field set per `<behavior>` Test 4. D-08 shape. JSDoc on `conflicted: boolean` explains Pitfall 2 distinction (in-tree-conflict-success vs crash) citing D-08.

    6. **Add `VcsWorkspaceParallel`** (export interface): two members `dispatch(opts: ParallelDispatchOpts): ParallelDispatchHandle` and `fanIn(handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult`. Place after `VcsWorkspace` (types.ts:425+ region per 09-PATTERNS.md §"VcsWorkspaceParallel placement"). Header comment cites `Phase 9 (VCS-16)` and notes "jj backend body: sdk/src/vcs/jj/parallel.ts; git backend: throwing stub in Phase 9; real body in Phase 10."

    7. **Extend `VcsWorkspace`** (types.ts:392-425): append a single new member `parallel: VcsWorkspaceParallel;` with a one-line `// Phase 9 (VCS-16, PARALLEL-01/02)` comment.

    Constraint: no fenced code blocks in this action — see 09-PATTERNS.md §"sdk/src/vcs/types.ts (type declarations — MOD)" for verbatim shapes. Sidecar discipline (D-12) is a body-file concern (`jj/parallel.ts` in plan 03), not a types-file concern.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; grep -c "export interface VcsWorkspaceParallel" sdk/src/vcs/types.ts; grep -c "export interface ParallelDispatchHandle" sdk/src/vcs/types.ts; grep -c "export interface FanInResult" sdk/src/vcs/types.ts; grep -c "export interface ParallelAgentResult" sdk/src/vcs/types.ts; grep -c "export interface ParallelDispatchOpts" sdk/src/vcs/types.ts; grep -v '^[[:space:]]*\(//\|\*\|/\*\)' sdk/src/vcs/types.ts | grep -c "'crashed-with-uncommitted-work' | 'merge-in-tree-conflict'"; grep -c "parallel: VcsWorkspaceParallel" sdk/src/vcs/types.ts; grep -c "PARALLEL-05, D-14" sdk/src/vcs/types.ts</automated>
  </verify>
  <done>
    `pnpm -C sdk tsc --noEmit` exits 0; all 8 grep counts ≥ 1 (the reason-union grep filters comments first); the new `VcsWorkspaceParallel` interface compiles into the cross-backend surface so both `backends/jj.ts` and `backends/git.ts` will fail TypeScript compilation until they wire a `parallel` field on their `workspace` block (this compile-failure is the wave-2 wire-in gate).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller code → SDK types | TypeScript callers consume the new interfaces; runtime data shape is enforced by the body files in plans 02/03/04, not here |
| no subprocess boundary | This plan is type-declarations-only; no `vcsExec`, no `spawnSync`, no file I/O at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `IncompleteWorkEntry.reason` parser | mitigate | Tightening to a closed string-literal union here lets plan 02 reject unknown reason values at parse time (`incomplete-work.ts` extension); the D-14 phase-merge gate at `backends/jj.ts:182-194` treats unknown reasons fail-safe per 09-CONTEXT A1 |
| T-09-02 | Information Disclosure | `ParallelDispatchHandle` JSON serialization | accept | Handle is pure JSON; no credentials, paths are repo-relative; no PII; the SDK CLI surface (`gsd-sdk query`) round-trips this verbatim |

No high-severity threats — type-declarations-only surface with no I/O, no subprocess, no credentials.
</threat_model>

<verification>
- `pnpm -C sdk tsc --noEmit` is the load-bearing gate. It MUST exit 0 — and downstream `backends/{jj,git}.ts` MUST fail compilation until plans 03 and 04 wire `parallel` (this compile-failure is intentional; do not pre-emptively stub the backend wiring from this plan).
- All grep counts in `<verify>` ≥ 1.
- No new imports added (this is a types-only edit; `Object.freeze` lives in plan 03).
- `scripts/lint-vcs-no-commit-id.cjs` passes (the PARALLEL-05 JSDoc text says "commit_id on git" — that string is in a JSDoc comment block, which the lint allowlist tolerates; if the lint complains, fix at the JSDoc level, not by removing the documentation).
</verification>

<success_criteria>
- New exports `VcsWorkspaceParallel`, `ParallelDispatchOpts`, `ParallelDispatchHandle`, `ParallelAgentResult`, `FanInResult` exist.
- `VcsWorkspace.parallel: VcsWorkspaceParallel` field exists.
- `IncompleteWorkEntry.reason` is the closed 2-value union (D-09).
- TypeScript compiles in `sdk/`.
- Downstream wave-2 plans (02/03/04) can import the new types.
</success_criteria>

<output>
Create `.planning/phases/09-jj-side-parallel-verbs/09-01-SUMMARY.md` when done.
</output>
