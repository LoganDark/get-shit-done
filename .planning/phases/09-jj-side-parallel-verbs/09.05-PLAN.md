---
phase: 09-jj-side-parallel-verbs
plan: 05
type: execute
wave: 3
depends_on:
  - 09-02
  - 09-03
  - 09-04
files_modified:
  - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
autonomous: true
requirements:
  - TEST-13
  - TEST-14
tags:
  - vcs
  - jj
  - parallel
  - tests
  - contract

must_haves:
  truths:
    - "TEST-13 jj contract: N=2 + N=3 + N=4 dispatch creates N workspaces with distinct change_ids."
    - "TEST-13 jj contract: clean fanIn returns merged.length === N and conflicted === false."
    - "TEST-13 jj contract: fanIn with one in-tree conflict yields conflicted === true, conflictedPaths populated, AND an incomplete-work queue entry with reason='merge-in-tree-conflict' — ALL THREE assertions co-occur in a SINGLE `it` block (W3 (a) — joint assertion lock-in per D-16). The queue entry is produced by fanIn itself per plan 03 action §7 W3 (a), NOT by reap; the test does NOT split D-16 across two scenarios."
    - "TEST-13 jj contract: fanIn with one crashed worker yields an incomplete-work queue entry with reason='crashed-with-uncommitted-work'."
    - "TEST-14 topology: post-fanIn `jj log -r 'divergent()' --no-graph` is empty for N ∈ {2,3,4}."
    - "Post-fanIn clean-path surplusBookmarks is empty (D-08)."
    - "W2 lifecycle: each value of N ∈ {2,3,4} owns an INDEPENDENT describe block with its own beforeAll (fresh mkdtemp) and afterAll (rmSync) so dispatch state from N=2 cannot contaminate N=3 or N=4. The for-loop wraps `describe.sequential.skipIf` blocks, NOT `it` blocks inside a shared describe."
  artifacts:
    - path: sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
      provides: "Pattern A/B vitest contract for vcs.workspace.parallel.{dispatch,fanIn} on jj."
      min_lines: 250
  key_links:
    - from: sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
      to: sdk/src/vcs/backends/jj.ts
      via: "createJjAdapter(dir).workspace.parallel call surface"
      pattern: "createJjAdapter"
    - from: sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
      to: sdk/src/vcs/jj/incomplete-work.ts
      via: "readIncomplete used to assert queue contents"
      pattern: "readIncomplete"
---

<objective>
Land the jj-side contract test file `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` covering both TEST-13 (dispatch + fanIn behavior on N ∈ {2,3,4}, plus the conflict and crash scenarios) and TEST-14 (the `jj log -r 'divergent()' --no-graph` topology assertion). Pattern A (`describe.sequential.skipIf(!jjAvailable)`) and Pattern B (random-prefix `mkdtemp`) per D-15. Never `retry: N`; never `describe.skip` (D-15 / Pitfall 9).

Purpose: This is the behavioral gate for Phase 9. plans 01-04 are type + body deliverables that compile but are not behaviorally proven until these tests pass. The "in-tree conflict" scenario gates plan 02's classifier extension AND plan 03's fanIn-side enqueue (per W3 (a)) end-to-end; the N-parent octopus + divergent() assertion gates plan 03's fanIn semantics; the crashed-worker scenario gates plan 02's existing `'crashed-with-uncommitted-work'` branch behavior under the new closed-enum invariant.

Output: One net-new test file; `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/cmd-parallel-jj.test.ts` green on a machine with `jj` available; skip-count baseline unchanged (`scripts/check-skip-count.cjs` green per TEST-16 — this Phase 10 requirement is also observed here as a forward-compat invariant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md
@.planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md
@sdk/src/vcs/__tests__/jj-octopus.test.ts
@sdk/src/vcs/__tests__/jj-reap.test.ts

<interfaces>
<!-- Fixture template — copy verbatim shape from jj-octopus.test.ts:31-74 per 09-PATTERNS.md. -->

From sdk/src/vcs/__tests__/jj-octopus.test.ts:19-74:
- jjAvailable probe via `execSync('jj --version', { stdio: 'pipe' })` in try/catch
- describe.sequential.skipIf(!jjAvailable)('label', () => { beforeAll, afterAll, it scenarios })
- mkdtempSync(join(tmpdir(), `gsd-jj-parallel-${Math.random().toString(36).slice(2, 10)}-`)) (Pattern B)
- `jj git init --colocate` + `jj config set --repo user.email/user.name` setup
- `writeFileSync(join(dir, 'seed.txt'), 'seed\n')`; `jj squash -B @ -k -m "seed"` to create a single root change

From sdk/src/vcs/__tests__/jj-reap.test.ts:115-151:
- Pattern for non-empty-head squashes + queue-append assertion (model for the new merge-in-tree-conflict scenario)

From sdk/src/vcs/backends/jj.ts (plan 03 wire-in):
- createJjAdapter(dir) → adapter; adapter.workspace.parallel.dispatch(opts) → ParallelDispatchHandle; adapter.workspace.parallel.fanIn(handle, results) → FanInResult

From sdk/src/vcs/jj/incomplete-work.ts:
- readIncomplete(phaseDir): IncompleteWorkEntry[] — used by tests to assert queue contents

In-tree-conflict fixture mechanism (09-PATTERNS.md §"In-tree-conflict fixture mechanism"):
- seed/CONFLICT.txt = "base content\n" at root change
- dispatch N=2 workspaces (both fork from parent slot)
- workspace 1: writeFileSync(join(ws1.path, 'CONFLICT.txt'), 'version A\n'); execSync(`jj squash -B @ -k -m "subagent 1 work"`, { cwd: ws1.path })
- workspace 2: writeFileSync ... 'version B\n'; same squash
- fanIn → N-parent jj new → in-tree conflict on CONFLICT.txt

Crashed-worker fixture mechanism (09-PATTERNS.md §"Crashed-worker fixture mechanism"):
- In workspace path: writeFile crashed-work.txt; execSync('jj st', {cwd: wsPath}) auto-snapshot
- Pass results=[{ agentId, exitCode: 1, stderr: 'crashed' }] to fanIn
- Assert IncompleteWorkEntry.reason === 'crashed-with-uncommitted-work' in queue

divergent() revset assertion (TEST-14):
- `jj --repository ${dir} log -r 'divergent()' --no-graph -T 'change_id ++ "\n"'`
- Expect stdout.trim() === ''
- Run for N ∈ {2, 3, 4}

W2 lifecycle (per checker fix — chosen approach):
- For N ∈ {2,3,4}: each value of N gets its OWN `describe.sequential.skipIf(!jjAvailable)` block, wrapped in a top-level `for (const N of [2, 3, 4] as const)` that runs at module-load time (vitest evaluates `describe` callbacks synchronously during collection).
- Each per-N describe owns its own `let dir: string;` `let vcs;` at describe scope, its own `beforeAll` (fresh `mkdtemp` Pattern B), and its own `afterAll` (`rmSync(dir, { recursive: true, force: true })`).
- This guarantees N=3's beforeAll cannot see N=2's leftover bookmarks/workspaces/queue files. NOT a shared-`dir` describe with three `it` blocks.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cmd-parallel-jj.test.ts with full TEST-13 + TEST-14 scenarios</name>
  <files>sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/jj-octopus.test.ts (full file — Pattern A/B template)
    - sdk/src/vcs/__tests__/jj-reap.test.ts (full file — non-empty-head queue-append assertion model + how to force a non-empty head in a fixture)
    - sdk/src/vcs/jj/parallel.ts (plan 03 — verify the dispatch + fanIn signatures the test calls; CONFIRM that fanIn enqueues the `merge-in-tree-conflict` IncompleteWorkEntry on the octopus-conflict path per W3 (a))
    - sdk/src/vcs/backends/jj.ts (plan 03 wire-in — verify `vcs.workspace.parallel.dispatch` is the call surface)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/tests/vcs/jj/parallel.contract.test.ts" — fixture skeleton + scenarios + fixture mechanisms)
    - .planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md (D-15, D-16 — test pattern + scenarios locked)
  </read_first>
  <behavior>
    - Test 1 (meta — file compiles): `pnpm -C sdk tsc --noEmit` exits 0.
    - Test 2 (meta — file is discoverable by vitest): `pnpm -C sdk vitest list 2>&1 | grep cmd-parallel-jj.test.ts` returns ≥ 1 match.
    - Test 3 (Pattern A): `grep -c "describe.sequential.skipIf" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 3 (W2 — one per-N describe for N ∈ {2,3,4}, plus the conflict and crash describes at minimum).
    - Test 4 (Pattern B random-prefix mkdtemp): `grep -c "Math.random().toString(36)" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1.
    - Test 5 (no retry / no skip — D-15): `grep -c "retry:" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` returns 0 AND `grep -c "describe.skip\|it.skip\|test.skip" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` returns 0.
    - Test 6 (N ∈ {2,3,4} parametrization — W2 form): `grep -c "for (const N of \[2, 3, 4\]" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1 AND the for-loop body is `describe.sequential.skipIf(...)`, NOT `it(...)`. Verified by inspecting the source: each iteration produces an independent describe with its own beforeAll/afterAll.
    - Test 7 (divergent() topology assertion — TEST-14): `grep -c "'divergent()'" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1.
    - Test 8 (TEST-13 in-tree-conflict joint assertion — W3 (a), D-16 lock-in): `grep -c "merge-in-tree-conflict" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1 AND `grep -c "CONFLICT.txt" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1. Source must contain a SINGLE `it` block that asserts ALL THREE: (i) `result.conflicted === true`, (ii) `result.conflictedPaths.length > 0`, (iii) `readIncomplete(handle.phaseRoot).some(e => e.reason === 'merge-in-tree-conflict')`. Verified secondarily by `grep -B 30 "merge-in-tree-conflict" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts | grep -c "conflicted).toBe(true)\|conflictedPaths"` ≥ 1 (the three assertions are co-located in the same it-block).
    - Test 9 (TEST-13 crashed-worker scenario): `grep -c "crashed-with-uncommitted-work" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1 AND `grep -c "exitCode: 1" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1.
    - Test 10 (surplusBookmarks D-08 invariant): `grep -c "surplusBookmarks" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` ≥ 1 — at least one assertion checks the clean-path result is `[]` / `.length === 0`.
    - Test 11 (the suite runs and passes): `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/cmd-parallel-jj.test.ts 2>&1 | tail -20` shows all scenarios passing (or all skipped iff jj is not available on the CI lane — in which case rerun on a jj-available machine).
    - Test 12 (skip-count baseline unchanged — TEST-16 forward-compat): `node scripts/check-skip-count.cjs 2>&1 | tail -5` exits 0.
  </behavior>
  <action>
    Create `sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts` from scratch. Structure per 09-PATTERNS.md §"Fixture-skeleton pattern" and the W2 lifecycle lock-in:

    1. **Imports** at top: `describe, it, expect, beforeAll, afterAll` from `'vitest'`; `mkdtempSync, writeFileSync, rmSync, readFileSync, existsSync` from `'node:fs'`; `tmpdir` from `'node:os'`; `join` from `'node:path'`; `execSync` from `'node:child_process'`; `createJjAdapter` from `'../backends/jj.js'`; `readIncomplete` from `'../jj/incomplete-work.js'`.

    2. **jjAvailable probe** at module scope: `let jjAvailable = false; try { execSync('jj --version', { stdio: 'pipe' }); jjAvailable = true; } catch {}`.

    3. **Setup helper** at module scope (NOT inside any describe — pure function): `function setupJjRepo(): string { const dir = mkdtempSync(join(tmpdir(), \`gsd-jj-parallel-${Math.random().toString(36).slice(2, 10)}-\`)); execSync('jj git init --colocate', { cwd: dir, stdio: 'pipe' }); execSync('jj config set --repo user.email "test@example.com"', { cwd: dir, stdio: 'pipe' }); execSync('jj config set --repo user.name "Test"', { cwd: dir, stdio: 'pipe' }); writeFileSync(join(dir, 'seed.txt'), 'seed\\n'); execSync('jj squash -B @ -k -m "seed"', { cwd: dir, stdio: 'pipe' }); return dir; }`.

    4. **W2 lock-in: per-N describe blocks via top-level for-loop.** Wrap N parametrization in a synchronous `for (const N of [2, 3, 4] as const)` at module scope (vitest collects describes during synchronous module evaluation):

       ```
       for (const N of [2, 3, 4] as const) {
         describe.sequential.skipIf(!jjAvailable)(\`workspace.parallel — N=${N} clean dispatch+fanIn+divergent()\`, () => {
           let dir: string;
           let vcs: ReturnType<typeof createJjAdapter>;

           beforeAll(() => {
             dir = setupJjRepo();
             vcs = createJjAdapter(dir);
           });

           afterAll(() => {
             if (dir) rmSync(dir, { recursive: true, force: true });
           });

           it(\`N=${N}: dispatch creates ${N} workspaces with distinct change_ids\`, () => { ... });
           it(\`N=${N}: clean fanIn returns merged.length===1, conflicted===false, surplusBookmarks empty\`, () => { ... });
           it(\`N=${N}: TEST-14 — post-fanIn divergent() revset is empty\`, () => { ... });
         });
       }
       ```

       Each iteration of the for-loop produces a SEPARATE describe with its own `dir` variable, its own beforeAll (fresh mkdtemp), and its own afterAll (rmSync). N=2's state is fully torn down before N=3's beforeAll runs (vitest's describe.sequential ordering at top level). This is W2's chosen approach — `describe.sequential.skipIf` per-N, NOT shared describe with multiple `it` iterations over N.

       Inside each describe's `it` blocks:
       - `it(\`N=${N}: dispatch creates ${N} workspaces with distinct change_ids\`, () => { ... })`:
         - `const handle = vcs.workspace.parallel.dispatch({ plan: Array.from({length: N}, (_, i) => ({ agentId: \`agent-${i+1}\`, planId: \`plan-${i+1}\` })), phaseNumber: 9, mainBookmark: 'main' });`
         - `expect(handle.workspaces.length).toBe(N);`
         - `expect(new Set(handle.workspaces.map(w => w.baseRev)).size).toBe(N);` — distinct change_ids
         - `expect(Object.isFrozen(handle)).toBe(true);` — D-05 frozen JSON
       - `it(\`N=${N}: clean fanIn returns merged.length===1, conflicted===false, surplusBookmarks empty\`, () => { ... })`:
         - Build the handle as above.
         - For each workspace, simulate clean work: `writeFileSync(join(ws.path, \`agent-${ws.agentId}.txt\`), 'clean work\\n');` `execSync(\`jj squash -B @ -k -m "subagent ${i+1} clean"\`, {cwd: ws.path, stdio: 'pipe'});`
         - Call `const result = vcs.workspace.parallel.fanIn(handle, handle.workspaces.map(w => ({ agentId: w.agentId, exitCode: 0 })));`
         - `expect(result.conflicted).toBe(false);`
         - `expect(result.merged.length).toBe(1);` — one octopus merge change_id
         - `expect(result.surplusBookmarks.length).toBe(0);` — D-08 invariant
         - `expect(result.incompleteQueued).toBe(0);`
       - `it(\`N=${N}: TEST-14 — post-fanIn divergent() revset is empty\`, () => { ... })`:
         - Re-dispatch + re-do clean fanIn inside THIS it block (state was reset by previous it-block side effects per Pattern A sequential; the safe approach is to NOT depend on prior it-block fanIn — re-run the fixture locally). Then:
         - `const r = execSync(\`jj --repository ${dir} log -r 'divergent()' --no-graph -T 'change_id ++ "\\n"'\`, { encoding: 'utf-8' });`
         - `expect(r.trim()).toBe('');`

    5. **One-off scenarios** (each in its OWN `describe.sequential.skipIf` with its own beforeAll/afterAll — outside the for-loop):

       **W3 (a) joint assertion lock-in — SINGLE `it` block, ALL THREE assertions co-located.** Per CONTEXT.md D-16 and plan 03 task 1 §7 W3 (a), fanIn itself enqueues a `merge-in-tree-conflict` IncompleteWorkEntry when the N-parent octopus produces an in-tree conflict on clean agents. The test asserts (i) `conflicted: true`, (ii) `conflictedPaths` populated, (iii) the queue entry — all three in ONE `it` block. The test does NOT split D-16 across multiple scenarios.

       ```
       describe.sequential.skipIf(!jjAvailable)('workspace.parallel — in-tree conflict joint assertion (D-16, W3 (a))', () => {
         let dir: string;
         let vcs: ReturnType<typeof createJjAdapter>;

         beforeAll(() => {
           dir = setupJjRepo();
           // Seed CONFLICT.txt at the root change BEFORE dispatch.
           writeFileSync(join(dir, 'CONFLICT.txt'), 'base content\\n');
           execSync('jj squash -B @ -k -m "seed conflict file"', { cwd: dir, stdio: 'pipe' });
           vcs = createJjAdapter(dir);
         });

         afterAll(() => {
           if (dir) rmSync(dir, { recursive: true, force: true });
         });

         it('in-tree-conflict on N=2: conflicted===true AND conflictedPaths populated AND queue entry with reason="merge-in-tree-conflict" — ALL THREE in ONE scenario', () => {
           const handle = vcs.workspace.parallel.dispatch({ plan: [{ agentId: 'agent-1', planId: 'plan-1' }, { agentId: 'agent-2', planId: 'plan-2' }], phaseNumber: 9, mainBookmark: 'main' });
           // Workspace 1: version A
           writeFileSync(join(handle.workspaces[0].path, 'CONFLICT.txt'), 'version A\\n');
           execSync('jj squash -B @ -k -m "subagent 1 work"', { cwd: handle.workspaces[0].path, stdio: 'pipe' });
           // Workspace 2: version B
           writeFileSync(join(handle.workspaces[1].path, 'CONFLICT.txt'), 'version B\\n');
           execSync('jj squash -B @ -k -m "subagent 2 work"', { cwd: handle.workspaces[1].path, stdio: 'pipe' });
           // Both agents exit cleanly — exitCode 0. fanIn-side W3 (a) producer must enqueue the queue entry.
           const result = vcs.workspace.parallel.fanIn(handle, handle.workspaces.map(w => ({ agentId: w.agentId, exitCode: 0 })));
           // Assertion (i): conflicted === true
           expect(result.conflicted).toBe(true);
           // Assertion (ii): conflictedPaths populated (CONFLICT.txt OR <UNRESOLVABLE> sentinel)
           expect(result.conflictedPaths.length).toBeGreaterThan(0);
           expect(result.conflictedPaths.some(p => p.includes('CONFLICT.txt') || p === '<UNRESOLVABLE>')).toBe(true);
           // Assertion (iii): queue entry with reason='merge-in-tree-conflict' — produced by fanIn per plan 03 §7 W3 (a)
           const queue = readIncomplete(handle.phaseRoot);
           expect(queue.some(e => e.reason === 'merge-in-tree-conflict')).toBe(true);
           // Bonus: incompleteQueued counter reflects the enqueue
           expect(result.incompleteQueued).toBeGreaterThanOrEqual(1);
           // Bonus: main bookmark NOT advanced; agent bookmarks NOT deleted; merged is empty on conflicted path
           expect(result.merged.length).toBe(0);
         });
       });
       ```

       **Crashed-worker describe** (own block, own lifecycle):

       ```
       describe.sequential.skipIf(!jjAvailable)('workspace.parallel — crashed worker queue entry', () => {
         let dir: string;
         let vcs: ReturnType<typeof createJjAdapter>;

         beforeAll(() => { dir = setupJjRepo(); vcs = createJjAdapter(dir); });
         afterAll(() => { if (dir) rmSync(dir, { recursive: true, force: true }); });

         it('one crashed worker: queue entry reason="crashed-with-uncommitted-work"', () => {
           const handle = vcs.workspace.parallel.dispatch({ plan: [{ agentId: 'agent-1', planId: 'plan-1' }, { agentId: 'agent-2', planId: 'plan-2' }], phaseNumber: 9, mainBookmark: 'main' });
           // Workspace 1: clean
           writeFileSync(join(handle.workspaces[0].path, 'clean.txt'), 'clean\\n');
           execSync('jj squash -B @ -k -m "subagent 1 clean"', { cwd: handle.workspaces[0].path, stdio: 'pipe' });
           // Workspace 2: crashed — non-empty head, no squash
           writeFileSync(join(handle.workspaces[1].path, 'crashed-work.txt'), 'partial\\n');
           execSync('jj st', { cwd: handle.workspaces[1].path, stdio: 'pipe' });
           const result = vcs.workspace.parallel.fanIn(handle, [{ agentId: 'agent-1', exitCode: 0 }, { agentId: 'agent-2', exitCode: 1, stderr: 'crashed' }]);
           expect(result.incompleteQueued).toBeGreaterThanOrEqual(1);
           const queue = readIncomplete(handle.phaseRoot);
           expect(queue.some(e => e.reason === 'crashed-with-uncommitted-work')).toBe(true);
         });
       });
       ```

       **Parse-time reason validation describe** (own block, own lifecycle — covers plan 02 task 3):

       ```
       describe.sequential.skipIf(!jjAvailable)('readIncomplete parse-time validation', () => {
         let phaseDir: string;
         beforeAll(() => { phaseDir = mkdtempSync(join(tmpdir(), \`gsd-jj-parse-${Math.random().toString(36).slice(2, 10)}-\`)); });
         afterAll(() => { if (phaseDir) rmSync(phaseDir, { recursive: true, force: true }); });

         it('readIncomplete throws on unknown reason value', () => {
           writeFileSync(join(phaseDir, 'incomplete-work.md'), '# Incomplete work\\n\\n- subagent-1: head=abc123def456, workspace=/tmp/x, reason=garbage-value\\n');
           expect(() => readIncomplete(phaseDir)).toThrow(/unknown reason/);
         });
       });
       ```

    6. Constraint: tests MUST be deterministic — Pattern B `mkdtemp` plus per-describe fresh fixture (W2 lock-in) eliminates state leakage between N values and between scenarios. NO shared describe with a single `dir` reused across N values.

    7. Constraint: do NOT use `retry: N` and do NOT use `describe.skip`/`it.skip` (D-15 / Pitfall 9). If a scenario is intermittent, fix the fixture — do not paper over flakiness.

    8. Constraint: no fenced code blocks in the FINAL test file (the planner-written code blocks above are templates for THIS PLAN's specification, not for inclusion verbatim in the test file as fenced code — the executor writes actual TypeScript, not markdown). However the executor SHOULD copy the structural shapes (describe/beforeAll/afterAll skeleton, assertion order, the for-loop wrapping describe rather than it) verbatim. The W3 (a) joint-assertion `it` block in particular MUST keep ALL THREE assertions co-located — NOT split across multiple `it` blocks.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; pnpm -C sdk vitest list 2>&1 | grep -c "cmd-parallel-jj.test.ts"; grep -c "describe.sequential.skipIf" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "Math.random().toString(36)" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "retry:" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "describe.skip\|it.skip\|test.skip" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "for (const N of \[2, 3, 4\]" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "'divergent()'" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "merge-in-tree-conflict" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "crashed-with-uncommitted-work" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; grep -c "surplusBookmarks" sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts; pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/cmd-parallel-jj.test.ts 2>&1 | tail -20; node scripts/check-skip-count.cjs 2>&1 | tail -3</automated>
  </verify>
  <done>
    `cmd-parallel-jj.test.ts` exists; all grep gates hit; per-N describe blocks isolate fixture state (W2); D-16 joint assertion lives in ONE `it` block consuming fanIn's W3 (a) queue producer (W3); the suite runs and all scenarios pass (or skip cleanly if jj is not available on the executor's lane); skip-count baseline unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → host filesystem | Pattern B `mkdtemp(os.tmpdir(), 'gsd-jj-parallel-...')` creates a random-prefix temp dir; `afterAll` cleans up with `rmSync(recursive, force)`; each describe owns its own dir per W2 |
| test execSync calls → jj subprocess | Tests use `execSync` directly (NOT vcsExec) — this is the established test-fixture idiom per `jj-octopus.test.ts`; SDK production code still routes through vcsExec |
| no network, no credentials | All test fixtures are local jj repos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Denial of Service | parallel test files colliding under `tmpdir()` | mitigate | Pattern B `Math.random().toString(36).slice(2, 10)` random prefix on every `mkdtemp` call; afterAll cleanup removes the entire tree; W2 per-describe lifecycle prevents N=2/N=3/N=4 from sharing a dir |
| T-09-14 | Tampering | hand-crafted incomplete-work.md in regression test | accept | Test writes the file inside its own ephemeral temp dir; no influence on production state |
| T-09-15 | Repudiation | flaky test masking real bug | mitigate | D-15 / Pitfall 9: no `retry: N`, no `describe.skip` — flakes must be fixed at the fixture level, not papered over; W2 lifecycle eliminates the cross-N state-leak failure mode |

No high-severity threats — test-only deliverable; no production code path; no network; no credentials.
</threat_model>

<verification>
- `cmd-parallel-jj.test.ts` exists with all behavior assertions from `<behavior>`.
- `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/cmd-parallel-jj.test.ts` green on jj-available machine.
- All grep gates in `<verify>` hit thresholds.
- W2: per-N describe blocks observable in source — the for-loop wraps `describe.sequential.skipIf(...)` NOT `it(...)`; each describe has its own beforeAll(setupJjRepo) and afterAll(rmSync); ≥ 3 describes for N values plus ≥ 3 more for conflict/crash/parse-time scenarios (total ≥ 5 describe.sequential.skipIf invocations).
- W3 (a): the in-tree-conflict scenario is a SINGLE `it` block asserting all three of (conflicted=true, conflictedPaths populated, queue entry reason='merge-in-tree-conflict'). D-16 is NOT split across multiple `it` blocks.
- Skip-count baseline (`scripts/check-skip-count.cjs`) unchanged — TEST-16 forward-compat invariant honored.
- `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/` overall suite green (no regressions from plans 01-04).
</verification>

<success_criteria>
- TEST-13 (jj contract) end-to-end behavioral gate satisfied: N=2/3/4 dispatch (each in its own describe per W2), clean fanIn, in-tree-conflict fanIn (D-16 joint assertion in ONE it block per W3 (a)), crashed-worker fanIn.
- TEST-14 (topology assertion) satisfied: post-fanIn `divergent()` revset empty for N ∈ {2,3,4}.
- D-08 surplusBookmarks-empty invariant asserted on the clean-fanIn path.
- Plan 02 parse-time reason validator regression covered.
- All Phase 9 ROADMAP success criteria (SC1-SC6) gated by this test file in combination with plans 01-04 behavior.
</success_criteria>

<output>
Create `.planning/phases/09-jj-side-parallel-verbs/09-05-SUMMARY.md` when done.
</output>
