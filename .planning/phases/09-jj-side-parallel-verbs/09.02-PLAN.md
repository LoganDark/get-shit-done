---
phase: 09-jj-side-parallel-verbs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/vcs/jj/conflict-paths.ts
  - sdk/src/vcs/backends/jj.ts
  - sdk/src/vcs/jj/reap.ts
  - sdk/src/vcs/jj/incomplete-work.ts
autonomous: true
requirements:
  - PARALLEL-01
tags:
  - vcs
  - jj
  - reap
  - classifier
  - parallel

must_haves:
  truths:
    - "enumerateConflictedPaths is sidecar-callable from sdk/src/vcs/jj/conflict-paths.ts without importing backends/jj.ts (UPSTREAM-02)."
    - "performJjReap classifies non-empty heads into THREE branches: empty → abandon; conflict → squash-as-incomplete reason='merge-in-tree-conflict'; else → squash-as-incomplete reason='crashed-with-uncommitted-work'."
    - "The in-tree-conflict probe uses the existing `conflicts()` revset directly via vcsExec (D-11), not a new SDK verb."
    - "incomplete-work.ts parser rejects unknown reason strings at read time."
  artifacts:
    - path: sdk/src/vcs/jj/conflict-paths.ts
      provides: "enumerateConflictedPaths(cwd, rev) extracted from backends/jj.ts:524-557; sidecar discipline."
      exports: ["enumerateConflictedPaths"]
    - path: sdk/src/vcs/jj/reap.ts
      provides: "performJjReap extended with hasInTreeConflict probe + 'merge-in-tree-conflict' classifier branch (D-09/D-10/D-11)."
      contains: "hasInTreeConflict"
    - path: sdk/src/vcs/jj/incomplete-work.ts
      provides: "Parse-time reason validation against the closed 2-value union."
      contains: "KNOWN_REASONS"
    - path: sdk/src/vcs/backends/jj.ts
      provides: "Existing findConflicts call site at :580-603 rebound to the extracted sidecar import."
      contains: "from '../jj/conflict-paths"
  key_links:
    - from: sdk/src/vcs/jj/reap.ts
      to: sdk/src/vcs/jj/conflict-paths.ts
      via: "enumerateConflictedPaths import for new conflict-classifier branch"
      pattern: "from './conflict-paths"
    - from: sdk/src/vcs/backends/jj.ts
      to: sdk/src/vcs/jj/conflict-paths.ts
      via: "rebind of existing enumerateConflictedPaths after extraction"
      pattern: "from '../jj/conflict-paths"
---

<objective>
Extend the reap classifier in `sdk/src/vcs/jj/reap.ts` from one non-empty-head reason (`'crashed-with-uncommitted-work'`) to two (adds `'merge-in-tree-conflict'`) per D-09/D-10/D-11. To preserve UPSTREAM-02 sidecar discipline, extract `enumerateConflictedPaths` from `backends/jj.ts:524-557` into a new sidecar file `sdk/src/vcs/jj/conflict-paths.ts` and rebind the existing call site in `backends/jj.ts`. Tighten `incomplete-work.ts` parse-time validation to reject unknown reason strings against the closed union landed in plan 01.

Purpose: The classifier extension lands the jj-side producer for the new `'merge-in-tree-conflict'` value (Phase 10 adds the git-side producer). Sidecar extraction lets both `jj/reap.ts` AND the new `jj/parallel.ts` (plan 03) consume the conflict-paths helper without violating UPSTREAM-02. The parse-time validator backs up the closed-enum invariant: any malformed queue file fails loudly at read time rather than silently propagating an unknown `reason` to the D-14 phase-merge gate at `backends/jj.ts:182-194`.

Output: One net-new file (`conflict-paths.ts`); three modified files (`reap.ts`, `backends/jj.ts`, `incomplete-work.ts`); existing `jj-reap.test.ts` still green; sidecar-discipline invariant (no `from '../backends/jj'` import inside `sdk/src/vcs/jj/*.ts`) preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md
@.planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md
@sdk/src/vcs/jj/reap.ts
@sdk/src/vcs/jj/incomplete-work.ts

<interfaces>
<!-- Verbatim from 09-PATTERNS.md / 09-RESEARCH.md with file:line citations. -->
<!-- All identifiers below are existing in-tree symbols at the cited locations. -->

From sdk/src/vcs/backends/jj.ts:524-557 (function body to extract verbatim):
- enumerateConflictedPaths(rev: string): string[]
  - primary: `jj resolve --list -r <rev>`
  - fallback: `jj diff -r <rev> --summary` filtered for `C ` status (IN-04: U letter removed in 0.41)
  - WR-04: returns `['<UNRESOLVABLE>']` when neither form succeeds
  - lives inside backends/jj.ts as a closure today; extraction lifts to pure function taking `(cwd, rev)`

From sdk/src/vcs/backends/jj.ts:580-603 (findConflicts({scope}) caller — already uses the `conflicts()` revset; remains the substrate per D-11):
- findConflicts({scope: 'working-copy' | string}) → ConflictRecord[]

From sdk/src/vcs/jj/reap.ts:54-70 (isEmptyHead probe — pattern model for hasInTreeConflict):
- function isEmptyHead(mainRepoRoot, parent, headChange): boolean
- uses vcsExec(mainRepoRoot, 'jj', [...jjArgvFlags(mainRepoRoot), 'log', ...])
- throws on non-zero exitCode with context-bearing error string

From sdk/src/vcs/jj/reap.ts:117-198 (performJjReap body — extension site):
- empty-branch: lines 131-159 (abandon + forget + rm)
- crashed branch: lines 160-194 (squash + queue with reason='crashed-with-uncommitted-work')
- NEW branch inserts between these two, runs when `hasInTreeConflict(...) === true`
- new branch mirrors squash-and-queue subpattern at reap.ts:165-190 but swaps reason literal

From sdk/src/vcs/jj/incomplete-work.ts:36 (writer line format):
- `- ${entry.subagentName}: head=${entry.changeIdShort}, workspace=${entry.workspacePath}, reason=${entry.reason}\n`

From sdk/src/vcs/jj/incomplete-work.ts:59 (reader ENTRY_RE):
- regex `/^\s*-\s+([^:]+):\s+head=([^,]+),\s+workspace=([^,]+),\s+reason=(.*)$/`

From sdk/src/vcs/jj/incomplete-work.ts:60-76 (loop with malformed-line throw — extension model):
- existing branch throws on no-regex-match — Phase 9 adds a parallel throw branch for unknown-reason

From sdk/src/vcs/types.ts (after plan 01 lands):
- IncompleteWorkEntry.reason: 'crashed-with-uncommitted-work' | 'merge-in-tree-conflict'

Sidecar discipline rule (UPSTREAM-02, carry from D-12):
- files under sdk/src/vcs/jj/*.ts MUST NOT import from '../backends/jj' or '../backends/jj.js'
- inline `jjArgvFlags(repo)` per octopus.ts:39-47 / reap.ts:33-41 template
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract enumerateConflictedPaths into sdk/src/vcs/jj/conflict-paths.ts and rebind backends/jj.ts to use the sidecar</name>
  <files>sdk/src/vcs/jj/conflict-paths.ts, sdk/src/vcs/backends/jj.ts</files>
  <read_first>
    - sdk/src/vcs/backends/jj.ts (lines 524-557 — the exact function body to extract; the closure uses a local `jjArgv(...subcommand)` builder defined at backends/jj.ts:84-92 that combines flags + subcommand; the extraction MUST translate `jjArgv(...x)` calls to `[...jjArgvFlags(cwd), ...x]` per the octopus.ts:39-47 / :56 template. The only other in-closure dependency is `vcsExec` from `../exec.js`.)
    - sdk/src/vcs/jj/octopus.ts (lines 39-47 — `jjArgvFlags` template to inline as a top-level helper inside conflict-paths.ts)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/src/vcs/jj/conflict-paths.ts" — exact extraction excerpt)
  </read_first>
  <behavior>
    - Test 1: `pnpm -C sdk tsc --noEmit` passes.
    - Test 2: Existing test `pnpm -C sdk vitest run src/vcs/__tests__/jj-conflict.test.ts` (or whatever test file currently covers `backends/jj.ts::findConflicts`/`enumerateConflictedPaths` — discover via `grep -rln "enumerateConflictedPaths\|findConflicts" sdk/src/vcs/__tests__/`) passes unchanged.
    - Test 3: `grep -c "from '\.\./backends/jj'" sdk/src/vcs/jj/conflict-paths.ts` returns 0 (sidecar discipline — no backend import).
    - Test 4: `grep -c "from '\.\./backends/jj" sdk/src/vcs/jj/conflict-paths.ts` returns 0 (same as Test 3, broader pattern catching `.js` suffix too).
    - Test 5: Existing `backends/jj.ts::findConflicts` returns the same shape pre- and post-extraction on the `<UNRESOLVABLE>` sentinel path (covered by Test 2's regression).
  </behavior>
  <action>
    1. **Create `sdk/src/vcs/jj/conflict-paths.ts`** (NEW FILE). Lift the function body from `backends/jj.ts:524-557`, retyped as a pure exported function `enumerateConflictedPaths(cwd: string, rev: string): string[]`. The source closure uses a local `jjArgv(...subcommand)` helper (defined at `backends/jj.ts:84-92`) that prepends `['--repository', cwd, '--no-pager', '--color', 'never', '--quiet']` to its subcommand argv. In the extracted file, define a top-level `function jjArgvFlags(repo: string): string[]` returning that same flag list (matching the `octopus.ts:39-47` template) and translate every `jjArgv(...args)` callsite at `:526` and `:542` to `[...jjArgvFlags(cwd), ...args]`. Do NOT import from `backends/jj.ts` — UPSTREAM-02. Import only `vcsExec` from `../exec.js`. Header comment block cites Phase 9 / VCS-17 / UPSTREAM-02 and the source location `backends/jj.ts:524-557` per 09-PATTERNS.md §"Why extract".
    2. **Modify `sdk/src/vcs/backends/jj.ts`:** add an import `import { enumerateConflictedPaths as _enumerateConflictedPaths } from '../jj/conflict-paths.js';` near the existing sidecar imports (alongside `import { performJjReap } from '../jj/reap.js'` template at line 33). Replace the existing closure body at `:524-557` with a thin binding: `const enumerateConflictedPaths = (rev: string): string[] => _enumerateConflictedPaths(cwd, rev);`. Keep the `cwd`-bound closure shape so `findConflicts({scope})` at `:580-603` continues to call `enumerateConflictedPaths(rev)` with unchanged arity.
    3. Constraint: `enumerateConflictedPaths` MUST return the `<UNRESOLVABLE>` sentinel on both-paths-fail per the original WR-04 contract — do NOT alter the semantics during extraction.
    4. Constraint: No fenced code blocks in this action — see 09-PATTERNS.md §"Extraction excerpt" for the verbatim body.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/ 2>&1 | tail -20; grep -c "from '\.\./backends/jj'" sdk/src/vcs/jj/conflict-paths.ts; grep -c "export function enumerateConflictedPaths" sdk/src/vcs/jj/conflict-paths.ts; grep -c "from '\.\./jj/conflict-paths" sdk/src/vcs/backends/jj.ts</automated>
  </verify>
  <done>
    `conflict-paths.ts` exists with `enumerateConflictedPaths` exported; `backends/jj.ts` imports from the new sidecar; sidecar has zero `backends/jj` imports; TypeScript compiles; existing conflict-related tests pass unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hasInTreeConflict probe + 'merge-in-tree-conflict' classifier branch to performJjReap</name>
  <files>sdk/src/vcs/jj/reap.ts</files>
  <read_first>
    - sdk/src/vcs/jj/reap.ts (full file — extension site is lines 130-194; new helper sits adjacent to `isEmptyHead` at lines 54-70)
    - sdk/src/vcs/jj/conflict-paths.ts (Task 1 output — exports `enumerateConflictedPaths` for the new branch)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/src/vcs/jj/reap.ts (classifier extension — MOD)" — exact helper and branch shapes)
    - sdk/src/vcs/__tests__/jj-reap.test.ts (existing test surface — Phase 9 contract tests in plan 05 will EXTEND scenarios here for `'merge-in-tree-conflict'`; this task MUST NOT break the existing scenarios)
  </read_first>
  <behavior>
    - Test 1: `pnpm -C sdk tsc --noEmit` passes.
    - Test 2: `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/jj-reap.test.ts` — all existing scenarios green (no regression).
    - Test 3: Source contains a new helper `hasInTreeConflict(mainRepoRoot: string, headChange: string): boolean` adjacent to `isEmptyHead`; verified by `grep -c "function hasInTreeConflict" sdk/src/vcs/jj/reap.ts` ≥ 1.
    - Test 4: The classifier emits `reason: 'merge-in-tree-conflict'` exactly once (a single string-literal occurrence inside the conflict branch); verified by `grep -c "'merge-in-tree-conflict'" sdk/src/vcs/jj/reap.ts` ≥ 1.
    - Test 5: The classifier still emits `reason: 'crashed-with-uncommitted-work'` exactly once (existing branch); verified by `grep -c "'crashed-with-uncommitted-work'" sdk/src/vcs/jj/reap.ts` ≥ 1.
    - Test 6: Sidecar discipline — `grep -c "from '\.\./backends/jj" sdk/src/vcs/jj/reap.ts` returns 0.
    - Test 7: New branch uses the sidecar `enumerateConflictedPaths` import — `grep -c "from './conflict-paths" sdk/src/vcs/jj/reap.ts` ≥ 1.
  </behavior>
  <action>
    1. **Add helper `hasInTreeConflict(mainRepoRoot, headChange)`** adjacent to `isEmptyHead` at `reap.ts:54-70`. Body uses `vcsExec(mainRepoRoot, 'jj', [...jjArgvFlags(mainRepoRoot), 'log', '-r', \`conflicts() & ${headChange}\`, '-T', 'change_id ++ "\\n"', '--no-graph'])`. Non-zero exit throws `Error('reap: conflict probe failed: ${r.stderr || r.stdout}')`. Returns `r.stdout.trim().length > 0`. JSDoc cites D-11 + reuse of jj 0.41 `conflicts()` PLURAL revset.
    2. **Add import** at the top of `reap.ts`: `import { enumerateConflictedPaths } from './conflict-paths.js';` (relative path because both files are in `sdk/src/vcs/jj/`).
    3. **Insert NEW classifier branch** in `performJjReap` between the existing `if (empty)` block (`:131-159`) and the existing `else` crashed-work block (`:160-194`). New ordering: `if (empty) { ... } else if (hasInTreeConflict(opts.mainRepoRoot, entry.headChange)) { /* NEW */ } else { /* existing crashed-work */ }`. The NEW branch:
       - calls `enumerateConflictedPaths(opts.mainRepoRoot, entry.headChange)` (may return `['<UNRESOLVABLE>']` sentinel — record verbatim; downstream phase-merge gate is fail-safe per A1)
       - mirrors the squash-and-queue subpattern from existing `reap.ts:165-190`: emits `jj squash -r <head> -k -m "subagent <idx>: incomplete work"`, throws on non-zero exitCode with context-bearing error
       - constructs `queueEntry: IncompleteWorkEntry` with `reason: 'merge-in-tree-conflict'` literal (the closed-union type from plan 01 ensures TypeScript accepts only the 2 valid literals)
       - calls `appendIncomplete(opts.phaseDir, queueEntry)`
       - pushes onto `incomplete` array
       - leaves the on-disk dir and workspace tracking intact (mirror comment from `reap.ts:191-194`)
    4. Constraint: do NOT modify the existing empty-branch or crashed-work branch bodies — they are regression surface for Test 2. The insert is purely additive between the two existing branches.
    5. Constraint: do NOT remove or relocate the existing `'crashed-with-uncommitted-work'` literal at `:187` — the existing branch keeps it.
    6. Constraint: no fenced code blocks in this action — see 09-PATTERNS.md §"sdk/src/vcs/jj/reap.ts" for exact shapes.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/jj-reap.test.ts 2>&1 | tail -10; grep -c "function hasInTreeConflict" sdk/src/vcs/jj/reap.ts; grep -c "'merge-in-tree-conflict'" sdk/src/vcs/jj/reap.ts; grep -c "'crashed-with-uncommitted-work'" sdk/src/vcs/jj/reap.ts; grep -c "from '\.\./backends/jj" sdk/src/vcs/jj/reap.ts; grep -c "from './conflict-paths" sdk/src/vcs/jj/reap.ts</automated>
  </verify>
  <done>
    `hasInTreeConflict` helper exists; classifier has three branches (empty / conflict / crashed); the 2 reason literals appear exactly once each; sidecar import works; existing jj-reap tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tighten incomplete-work.ts parse-time reason validation</name>
  <files>sdk/src/vcs/jj/incomplete-work.ts</files>
  <read_first>
    - sdk/src/vcs/jj/incomplete-work.ts (full file — extension site is the for-loop at lines 60-76 with the existing malformed-line throw at 64-68 as the model)
    - sdk/src/vcs/types.ts (after plan 01 — `IncompleteWorkEntry['reason']` resolves to the closed union)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/src/vcs/jj/incomplete-work.ts" — exact KNOWN_REASONS recipe)
  </read_first>
  <behavior>
    - Test 1: `pnpm -C sdk tsc --noEmit` passes (the cast `as IncompleteWorkEntry['reason']` resolves cleanly against the closed union).
    - Test 2: Existing incomplete-work tests pass — `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/incomplete-work.test.ts` (or whichever file covers `readIncomplete` — `grep -rln "readIncomplete" sdk/src/vcs/__tests__/`).
    - Test 3: Source declares a module-level constant `KNOWN_REASONS` whose value is a `Set` containing the 2 valid literals; verified by `grep -c "KNOWN_REASONS" sdk/src/vcs/jj/incomplete-work.ts` ≥ 2 (one declaration + one use) and `grep -c "'crashed-with-uncommitted-work'" sdk/src/vcs/jj/incomplete-work.ts` ≥ 1 plus `grep -c "'merge-in-tree-conflict'" sdk/src/vcs/jj/incomplete-work.ts` ≥ 1.
    - Test 4: A round-trip behavior test (already-existing or planner-added in plan 05) demonstrates: a queue file containing `reason=garbage-value` causes `readIncomplete` to throw with a message containing the literal `unknown reason` and the offending value. (If no existing test covers this, plan 05 task 4 owns the new test — this plan does NOT scaffold a vitest; it adds the throw branch only.)
  </behavior>
  <action>
    1. Add a module-level constant `const KNOWN_REASONS = new Set<IncompleteWorkEntry['reason']>(['crashed-with-uncommitted-work', 'merge-in-tree-conflict']);` adjacent to the existing `ENTRY_RE` regex at `:59`. Import `IncompleteWorkEntry` from `'../types.js'` (already imported at `:22-23` per 09-RESEARCH.md).
    2. Inside the for-loop at `:60-76`, after the existing regex-match-failure throw, before the `entries.push({...})` call, extract `const reasonRaw = m[4].trim();`. If `!KNOWN_REASONS.has(reasonRaw as IncompleteWorkEntry['reason'])`, throw a new `Error('incomplete-work.md: unknown reason "${reasonRaw}" in ${p}: ${line.slice(0, 120)}')`. Otherwise push the entry with `reason: reasonRaw as IncompleteWorkEntry['reason']` (replacing the existing `reason: m[4].trim()` field).
    3. Constraint: do NOT loosen the existing malformed-line throw at `:64-68`. The new validation is a SECOND throw on a different failure mode.
    4. Constraint: the cast `as IncompleteWorkEntry['reason']` works only against the closed union from plan 01 — if plan 01 has not landed in your context, TypeScript will reject the cast against `string`, which is the intended compile-time gate.
    5. Constraint: no fenced code blocks in this action — see 09-PATTERNS.md §"sdk/src/vcs/jj/incomplete-work.ts" for exact shapes.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/ 2>&1 | tail -10; grep -c "KNOWN_REASONS" sdk/src/vcs/jj/incomplete-work.ts; grep -c "unknown reason" sdk/src/vcs/jj/incomplete-work.ts; grep -c "'crashed-with-uncommitted-work'" sdk/src/vcs/jj/incomplete-work.ts; grep -c "'merge-in-tree-conflict'" sdk/src/vcs/jj/incomplete-work.ts</automated>
  </verify>
  <done>
    `KNOWN_REASONS` constant + new throw branch present; TypeScript compiles; existing tests pass; downstream readers see a loud failure on unknown reasons rather than silently propagating to D-14 phase-merge gate.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-tree `conflicts()` revset → reap classifier | jj-side substrate; trusted output of `vcsExec`; non-zero exit code throws context-bearing error |
| on-disk `incomplete-work.md` → reader | The queue file is on-repo state; an adversary with file write access already owns the repo, but a corrupted or hand-edited file should fail loudly rather than poison the phase-merge gate |
| extracted sidecar `conflict-paths.ts` → both `reap.ts` and `parallel.ts` (plan 03) | Single source of truth for the `<UNRESOLVABLE>` sentinel semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | `incomplete-work.md` queue file | mitigate | Task 3 parse-time validation rejects unknown `reason` values — preserves the closed-enum invariant for the D-14 phase-merge gate fail-safe |
| T-09-04 | Information Disclosure | `<UNRESOLVABLE>` sentinel | accept | The sentinel string is data, not credential leakage; downstream consumers (plan 05 tests) gate behavior on it |
| T-09-05 | Tampering | Sidecar discipline boundary | mitigate | Tasks 1 + 2 grep gates prove no `backends/jj` imports inside the new sidecar; UPSTREAM-02 invariant preserved |

No high-severity threats — surface is SDK-internal classifier extension with no new I/O channels or external trust boundaries.
</threat_model>

<verification>
- TypeScript compiles in `sdk/`.
- All pre-existing `jj-reap.test.ts` and incomplete-work tests pass unchanged.
- Sidecar discipline grep gates (Test 6 task 2; Test 3+4 task 1) all return 0 for backend imports.
- The 3-branch classifier ordering (empty / conflict / crashed) is observable in `reap.ts` source.
</verification>

<success_criteria>
- `sdk/src/vcs/jj/conflict-paths.ts` exists, exports `enumerateConflictedPaths`, has zero `backends/jj` imports.
- `backends/jj.ts:524-557` body is now a thin closure delegating to the sidecar.
- `reap.ts` has `hasInTreeConflict` helper + 3-branch classifier; `'merge-in-tree-conflict'` reason emitted in the new branch.
- `incomplete-work.ts` has `KNOWN_REASONS` set + parse-time throw on unknown reasons.
- Sidecar discipline holds; UPSTREAM-02 invariant unbroken.
</success_criteria>

<output>
Create `.planning/phases/09-jj-side-parallel-verbs/09-02-SUMMARY.md` when done.
</output>
