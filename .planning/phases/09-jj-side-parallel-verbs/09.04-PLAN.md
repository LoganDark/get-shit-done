---
phase: 09-jj-side-parallel-verbs
plan: 04
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - sdk/src/vcs/backends/git.ts
autonomous: true
requirements:
  - VCS-16
tags:
  - vcs
  - git
  - parallel
  - stub

must_haves:
  truths:
    - "GitVcsAdapter satisfies the VcsWorkspaceParallel interface declared in plan 01."
    - "git-side workspace.parallel.dispatch and fanIn each throw VcsNotImplementedError with a 'Phase 10' breadcrumb."
    - "No new files created; no new imports beyond the existing VcsNotImplementedError class from sdk/src/vcs/types.ts:573-578."
  artifacts:
    - path: sdk/src/vcs/backends/git.ts
      provides: "workspace.parallel = Object.freeze({dispatch, fanIn}) — both throwing stubs."
      contains: "VcsNotImplementedError"
  key_links:
    - from: sdk/src/vcs/backends/git.ts
      to: sdk/src/vcs/types.ts
      via: "VcsNotImplementedError import (already exists; reuse existing import if any)"
      pattern: "VcsNotImplementedError"
---

<objective>
Satisfy the cross-backend `VcsWorkspaceParallel` interface (landed in plan 01) on the git backend by adding a throwing stub `workspace.parallel = Object.freeze({ dispatch, fanIn })` inside `sdk/src/vcs/backends/git.ts`. Both members throw `VcsNotImplementedError` with a breadcrumb pointing at Phase 10 — the same-PR coupling on `FanInResult` shape (per v1.2 retro precedent and 09-CONTEXT.md domain section) is type-contract-only at Phase 9; Phase 10 ships the real git body.

Purpose: Without this stub, plan 01's extension of `VcsWorkspace` with `parallel: VcsWorkspaceParallel` would break TypeScript compilation on the git backend. Throwing on call (rather than returning a default) is the established idiom — `VcsNotImplementedError` callsites already exist at `backends/jj.ts:125, 166, 789, 1023` (8 instances) per 09-PATTERNS.md §"sdk/src/vcs/backends/git.ts (throwing stub — MOD)". The error message MUST name "Phase 10" so a future debugger searching the codebase finds the breadcrumb immediately.

Output: One modified file (`backends/git.ts`); TypeScript compiles for the full SDK; calling `vcs.workspace.parallel.dispatch(...)` or `.fanIn(...)` on a `GitVcsAdapter` throws `VcsNotImplementedError`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md
@.planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md
@sdk/src/vcs/backends/git.ts

<interfaces>
<!-- From plan 01 output: -->
- VcsWorkspaceParallel.dispatch(opts: ParallelDispatchOpts): ParallelDispatchHandle
- VcsWorkspaceParallel.fanIn(handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult

<!-- From sdk/src/vcs/types.ts:573-578 (existing): -->
- class VcsNotImplementedError extends Error { readonly name = 'VcsNotImplementedError'; constructor(message: string) }

<!-- Stub idiom precedent: -->
- backends/jj.ts:125, 166, 789, 1023 — 8 existing `throw new VcsNotImplementedError(...)` callsites; copy their construction shape verbatim.

<!-- Insertion site: -->
- The `workspace = Object.freeze({ ... })` block in backends/git.ts (locate via `grep -n "workspace = Object.freeze\|workspace: Object.freeze" sdk/src/vcs/backends/git.ts`). The `parallel: Object.freeze({...})` member is appended before the closing brace, mirroring plan 03's wire-in placement in backends/jj.ts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add throwing parallel stub to GitVcsAdapter workspace block</name>
  <files>sdk/src/vcs/backends/git.ts</files>
  <read_first>
    - sdk/src/vcs/backends/git.ts (full file — locate workspace = Object.freeze block; locate any existing `VcsNotImplementedError` import OR adjacent import block for types from `'../types.js'`)
    - sdk/src/vcs/backends/jj.ts (lines 120-170 — example `throw new VcsNotImplementedError(...)` shape from one of the 8 existing callsites)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/src/vcs/backends/git.ts (throwing stub — MOD)" — exact pattern)
  </read_first>
  <behavior>
    - Test 1: `pnpm -C sdk tsc --noEmit` exits 0 — git adapter satisfies the extended `VcsWorkspace.parallel` interface.
    - Test 2: Import of `VcsNotImplementedError` is present (either added by this task OR already present); verified by `grep -c "VcsNotImplementedError" sdk/src/vcs/backends/git.ts` ≥ 3 (one import + two stub bodies). If the import was already present, count starts at ≥ 4 (import + prior callsites + 2 new stubs).
    - Test 3: Stub messages name Phase 10 — `grep -c "Phase 10" sdk/src/vcs/backends/git.ts` ≥ 2.
    - Test 4: Both stub members exist — `grep -c "parallel: Object.freeze" sdk/src/vcs/backends/git.ts` ≥ 1 AND the block contains both `dispatch` and `fanIn` keys. Verifiable secondarily by `grep -c "workspace.parallel.dispatch" sdk/src/vcs/backends/git.ts` ≥ 1 AND `grep -c "workspace.parallel.fanIn" sdk/src/vcs/backends/git.ts` ≥ 1.
    - Test 5: Existing tests pass — `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/ 2>&1 | tail -10` (the throwing stubs add no observable behavior to any test scenario; existing scenarios unchanged).
    - Test 6: A direct test confirming throw behavior — invoking `createGitAdapter(<some-temp-repo>).workspace.parallel.dispatch({plan: [], phaseNumber: 1, mainBookmark: 'main'})` throws an instance of `VcsNotImplementedError` whose message includes the string `workspace.parallel.dispatch` and the string `Phase 10`. (Plan 05 task 5 owns landing this assertion in a `cmd-parallel-git.test.ts` file — this task only ships the throwing behavior; the test landing-site decision is documented in plan 05.)
  </behavior>
  <action>
    1. **Add import** (if not already present): `import { VcsNotImplementedError } from '../types.js';` Locate any existing import from `'../types.js'`; if present, add `VcsNotImplementedError` to the named import list there. Otherwise add a new import line in the existing import block.
    2. **Locate the `workspace = Object.freeze({...})` block** — search by grep. (If the git backend uses `workspace: Object.freeze({...})` as a property on a returned object literal rather than a const, follow that exact shape.)
    3. **Append a `parallel: Object.freeze({...})` member** to that block, BEFORE the closing brace. Per 09-PATTERNS.md §"Stub pattern":
       - `dispatch(): never { throw new VcsNotImplementedError('workspace.parallel.dispatch is not yet implemented on the git backend; Phase 10 ships the body'); }`
       - `fanIn(): never { throw new VcsNotImplementedError('workspace.parallel.fanIn is not yet implemented on the git backend; Phase 10 ships the body'); }`
       - The return type `never` is required so TypeScript accepts the stub against the `VcsWorkspaceParallel` interface members that return concrete types — `never` is assignable to any return type.
    4. Constraint: do NOT introduce any other behavior change on the git backend. No new imports beyond `VcsNotImplementedError`. No modification of any existing workspace adapter member.
    5. Constraint: do NOT pre-implement any git-side parallel body — that is Phase 10's deliverable (PARALLEL-01 git-side, PARALLEL-02 git-side, VCS-18). The Phase 10 work happens in a NEW sidecar file `sdk/src/vcs/git/parallel.ts` plus this stub being replaced with a real wire-in.
    6. Constraint: no fenced code blocks in this action — see 09-PATTERNS.md §"Stub pattern" for the verbatim text.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; grep -c "VcsNotImplementedError" sdk/src/vcs/backends/git.ts; grep -c "Phase 10" sdk/src/vcs/backends/git.ts; grep -c "parallel: Object.freeze" sdk/src/vcs/backends/git.ts; grep -c "workspace.parallel.dispatch" sdk/src/vcs/backends/git.ts; grep -c "workspace.parallel.fanIn" sdk/src/vcs/backends/git.ts; pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/ 2>&1 | tail -10</automated>
  </verify>
  <done>
    git adapter compiles against the extended `VcsWorkspace` interface; both stubs throw `VcsNotImplementedError` with Phase 10 breadcrumbs; no behavior change for existing git-backend consumers; TypeScript and existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → git workspace.parallel | The stub throws on call; the error class carries no payload beyond the message string |
| no subprocess boundary | This plan adds no `execSync`, no `spawnSync`, no `vcsExec` — pure interface-conformance stub |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Repudiation | git-side parallel call | accept | Throwing `VcsNotImplementedError` is the established stub idiom (8 in-tree precedents in `backends/jj.ts`); error name + message provide loud, attributable failure |
| T-09-12 | Denial of Service | mistakenly running git-backend with parallelization config = true | accept | CONFIG-02 pre-flight (Phase 14) refuses on explicit `false` but Phase 14 does NOT gate `true` against backend capability; until Phase 10 ships the real body, an operator who configures `parallelization: true` on a git-backend repo will hit `VcsNotImplementedError` on the first dispatch — loud failure, no silent corruption |

No high-severity threats — interface-conformance stub only.
</threat_model>

<verification>
- TypeScript compiles in `sdk/`.
- All `<verify>` grep counts hit thresholds.
- Existing tests pass.
- `scripts/lint-vcs-no-commit-id.cjs` green (stub adds no `commit_id` references).
</verification>

<success_criteria>
- `GitVcsAdapter.workspace.parallel.{dispatch, fanIn}` exists and throws `VcsNotImplementedError`.
- Error messages name "Phase 10".
- TypeScript compiles end-to-end.
- No regressions on existing git-backend tests.
</success_criteria>

<output>
Create `.planning/phases/09-jj-side-parallel-verbs/09-04-SUMMARY.md` when done.
</output>
