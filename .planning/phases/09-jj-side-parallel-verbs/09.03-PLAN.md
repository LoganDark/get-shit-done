---
phase: 09-jj-side-parallel-verbs
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - sdk/src/vcs/jj/parallel.ts
  - sdk/src/vcs/backends/jj.ts
autonomous: true
requirements:
  - PARALLEL-01
  - PARALLEL-02
  - VCS-17
  - VCS-19
tags:
  - vcs
  - jj
  - parallel
  - composition

must_haves:
  truths:
    - "performJjParallelDispatch returns a frozen pure-JSON ParallelDispatchHandle (D-05/D-06)."
    - "performJjParallelFanIn signature is `(cwd, handle, results): FanInResult` — the two-arg fanIn contract from plan 01 (D-04) plus the SDK-internal `cwd` prefix; `results` elements match the ParallelAgentResult shape locked by D-07."
    - "performJjParallelFanIn produces a single N-parent jj new + batched bookmark delete (PARALLEL-02 jj-side)."
    - "FanInResult.surplusBookmarks is always empty post-fanIn (D-08): the dispatch-side agent-bookmark set is fully deleted in one batched call."
    - "FanInResult.conflicted is true iff the N-parent merge produces in-tree conflicts (Pitfall 2 surface; D-08)."
    - "When the octopus produces an in-tree conflict, fanIn ALSO appends an IncompleteWorkEntry with reason='merge-in-tree-conflict' to the queue at handle.phaseRoot (W3 rationale: makes D-16 a single coherent scenario — `conflicted:true` + `conflictedPaths` populated + queue entry with reason='merge-in-tree-conflict' co-occur on the SAME octopus-conflict path; without this, D-16's joint assertion would have no producer for the queue entry when all agents exited cleanly)."
    - "WAVE_WORKTREE_MANIFEST written at dispatch carries plan_id + agent_id + backend (VCS-19)."
    - "jj/parallel.ts honors sidecar discipline UPSTREAM-02 — no import from backends/jj."
    - "vcs.workspace.parallel.{dispatch,fanIn} is exposed on JjVcsAdapter via Object.freeze."
    - "dispatch validates every agentId against /^[A-Za-z0-9._/-]+$/ BEFORE writing the manifest entry so worktree-safety.cjs:334 normalizeCleanupManifestEntry never silently drops a workspace."
  artifacts:
    - path: sdk/src/vcs/jj/parallel.ts
      provides: "performJjParallelDispatch + performJjParallelFanIn composition layer over octopus.ts + reap.ts + N-parent jj new."
      exports: ["performJjParallelDispatch", "performJjParallelFanIn"]
    - path: sdk/src/vcs/backends/jj.ts
      provides: "workspace.parallel = Object.freeze({dispatch, fanIn}) wire-in inside JjVcsAdapter."
      contains: "parallel: Object.freeze"
  key_links:
    - from: sdk/src/vcs/jj/parallel.ts
      to: sdk/src/vcs/jj/octopus.ts
      via: "createPhaseStructure + createSubagentSlot composition"
      pattern: "from './octopus"
    - from: sdk/src/vcs/jj/parallel.ts
      to: sdk/src/vcs/jj/reap.ts
      via: "performJjReap call during fanIn for crash-classification"
      pattern: "from './reap"
    - from: sdk/src/vcs/jj/parallel.ts
      to: sdk/src/vcs/jj/conflict-paths.ts
      via: "enumerateConflictedPaths for FanInResult.conflictedPaths population"
      pattern: "from './conflict-paths"
    - from: sdk/src/vcs/jj/parallel.ts
      to: sdk/src/vcs/jj/incomplete-work.ts
      via: "appendIncomplete for octopus-merge-in-tree-conflict queue entry (W3 (a) — joint-assertion producer for D-16)"
      pattern: "from './incomplete-work"
    - from: sdk/src/vcs/backends/jj.ts
      to: sdk/src/vcs/jj/parallel.ts
      via: "workspace.parallel adapter wiring imports performJjParallel* functions"
      pattern: "from '../jj/parallel"
---

<objective>
Create the new composition file `sdk/src/vcs/jj/parallel.ts` exporting `performJjParallelDispatch(opts) → ParallelDispatchHandle` and `performJjParallelFanIn(cwd, handle, results) → FanInResult`. Wire both onto `JjVcsAdapter` via `workspace.parallel = Object.freeze({dispatch, fanIn})` in `sdk/src/vcs/backends/jj.ts`. Sidecar discipline UPSTREAM-02 holds: `parallel.ts` does NOT import from `backends/jj.ts`. Returns are frozen pure JSON (D-05).

Purpose: This is the architectural payload of Phase 9. PARALLEL-01 (jj-side) and PARALLEL-02 (jj-side) ship here. `dispatch` composes `octopus.createPhaseStructure` + N× `createSubagentSlot` and writes the extended `WAVE_WORKTREE_MANIFEST` with new `plan_id` and `backend` fields (VCS-19). `fanIn` runs a single N-parent `jj new` (extending the 2-parent shape at `backends/jj.ts:1180-1188` to N parents per Pitfall 4 invariant), probes conflicts via the existing `findConflicts({scope:'working-copy'})` substrate at `:580-603`, and on the non-conflicted path advances the main bookmark and runs one batched `jj bookmark delete -- <a1> <a2> ... <aN>` so `surplusBookmarks` is always empty (D-08).

Output: One net-new file (`parallel.ts`); one modified file (`backends/jj.ts`). TypeScript compiles. Plan 05 contract tests gate end-to-end behavior in wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md
@.planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md
@sdk/src/vcs/jj/octopus.ts
@sdk/src/vcs/jj/reap.ts

<interfaces>
<!-- All identifiers below exist in-tree at the cited locations. Imports are sidecar-discipline-compliant. -->

From sdk/src/vcs/types.ts (after plan 01 lands):
- ParallelDispatchOpts: { plan: readonly { agentId; planId; workspacePath? }[]; phaseNumber: number; mainBookmark: string; maxConcurrency? }
- ParallelDispatchHandle: { phaseRoot; phaseNumber; mainBookmark; manifest; workspaces: readonly { name; path; baseRev; agentId; baselineOpId? }[] }
- ParallelAgentResult: { agentId; exitCode; lastChangeId?; stderr? }
- FanInResult: { merged: readonly string[]; conflicted: boolean; conflictedPaths: readonly string[]; incompleteQueued: number; failedReaped: readonly string[]; surplusBookmarks: readonly string[] }
- IncompleteWorkEntry.reason: closed union from plan 01

From sdk/src/vcs/exec.ts (existing):
- vcsExec(cwd, 'jj', args): { exitCode; stdout; stderr } — sole subprocess primitive (D-12 forbids inline spawnSync)

From sdk/src/vcs/expr.ts:90-95 (existing):
- expr.rev(idOrRevset): RevisionExpr — brand-wrapping for raw change_ids

From sdk/src/vcs/jj/octopus.ts:
- jjArgvFlags(repo): string[] — inline-copy template at :39-47 (sidecar discipline)
- resolveChangeId(mainRepoRoot, revset): string — copy verbatim at :49-66
- createPhaseStructure(mainRepoRoot, parentRevset, phaseNum): PhaseStructureResult — :102-189; idempotent via marker bookmarks
- createSubagentSlot(mainRepoRoot, vcs, opts): { headChange, workspaceName, workspacePath } — :280-324; opts = { parentChange, mergeChange, idx, phaseNum, workspacePath? }
- octopus.ts:215 — workspace name convention `phase-${phaseTag}-subagent-${idx}` (zero-padded phase per D-04)
- octopus.ts:319 — expr.rev brand-wrap precedent for handing change_id into vcs.workspace.add({baseRef})

From sdk/src/vcs/jj/reap.ts:
- performJjReap({ mainRepoRoot, phaseNamePrefix, phaseDir, entries }): { abandoned, incomplete }

From sdk/src/vcs/jj/conflict-paths.ts (plan 02 output):
- enumerateConflictedPaths(cwd, rev): string[]  // may return ['<UNRESOLVABLE>']

From sdk/src/vcs/jj/incomplete-work.ts:
- appendIncomplete(phaseDir, entry): void
- readIncomplete(phaseDir): IncompleteWorkEntry[]

From sdk/src/vcs/backends/jj.ts:
- :1175-1245 — workspace.merge body (2-parent precedent shape for N-parent fanIn)
- :1180-1188 — exact 2-parent `jj new -r @ -r <branchRev> -m <msg>` template to lift to N parents
- :1209, 1226 — validateRefname callsites (apply before bookmark set/delete)
- :1230 — `--` end-of-options separator before user-influenced positionals
- :1135-1157 — workspace.reap wiring template (model for parallel: Object.freeze wire-in)
- :1275 — end of workspace Object.freeze block (insertion site for parallel: member)

WAVE_WORKTREE_MANIFEST schema (VCS-19 extension):
- writer (this plan owns): { worktrees: [{ agent_id, plan_id (NEW), backend (NEW), worktree_path, branch, expected_base, main_bookmark }] }
- reader (existing, unchanged): get-shit-done/bin/lib/worktree-safety.cjs:319-342 `normalizeCleanupManifestEntry`
- branch field regex constraint at worktree-safety.cjs:334: /^worktree-agent-[A-Za-z0-9._/-]+$/  (apply to the branch literal `worktree-agent-${agentId}`)
- caller-supplied agentIds containing whitespace or characters outside [A-Za-z0-9._/-] silently produce manifest entries that `normalizeCleanupManifestEntry` returns null for — losing track of the workspace. validateAgentId MUST throw BEFORE the manifest is written.

Refname validation (apply before bookmark set/delete):
- validateRefname(name) from backends/jj.ts:1209 — sidecar files cannot import this from backends/jj; planner reuses an existing sidecar-tier validator if one exists OR inlines the regex check directly per 09-PATTERNS.md §"Refname validation before bookmark writes"

End-of-options pattern (D-12 inline; carry from backends/jj.ts:1052-1062, 1080-1082, 1230):
- vcsExec(cwd, 'jj', [...jjArgvFlags(cwd), 'bookmark', 'delete', '--', ...agentBookmarkNames])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sdk/src/vcs/jj/parallel.ts with performJjParallelDispatch + performJjParallelFanIn</name>
  <files>sdk/src/vcs/jj/parallel.ts</files>
  <read_first>
    - sdk/src/vcs/jj/octopus.ts (full file — extract jjArgvFlags + resolveChangeId verbatim; understand createPhaseStructure + createSubagentSlot signatures + return shapes; note octopus.ts:215 naming convention and octopus.ts:319 expr.rev pattern)
    - sdk/src/vcs/jj/reap.ts (lines 1-50 for header + jjArgvFlags pattern; lines 117-198 for performJjReap signature shape that fanIn invokes)
    - sdk/src/vcs/backends/jj.ts (lines 524-603 for findConflicts substrate; lines 1175-1245 for 2-parent jj new precedent; lines 1209-1230 for bookmark validate + delete pattern)
    - sdk/src/vcs/jj/conflict-paths.ts (plan 02 output — enumerateConflictedPaths signature)
    - sdk/src/vcs/jj/incomplete-work.ts (appendIncomplete + readIncomplete signatures — appendIncomplete is the W3 (a) producer for the merge-in-tree-conflict queue entry)
    - get-shit-done/bin/lib/worktree-safety.cjs (lines 319-342, especially the regex at :334 — `/^worktree-agent-[A-Za-z0-9._/-]+$/` — the constraint validateAgentId pre-empts)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/src/vcs/jj/parallel.ts" — header + imports + helpers + fanIn N-parent shape + frozen-JSON return)
    - .planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md (D-04..D-16 — exact shapes + invariants; D-16 in particular for the joint-assertion shape that §7 below produces)
  </read_first>
  <behavior>
    - Test 1: `pnpm -C sdk tsc --noEmit` exits 0.
    - Test 2: File exports exactly two named functions: `performJjParallelDispatch` and `performJjParallelFanIn`; verified by `grep -c "export function performJjParallelDispatch" sdk/src/vcs/jj/parallel.ts` ≥ 1 and `grep -c "export function performJjParallelFanIn" sdk/src/vcs/jj/parallel.ts` ≥ 1.
    - Test 3: Sidecar discipline — `grep -c "from '\.\./backends/jj" sdk/src/vcs/jj/parallel.ts` returns 0.
    - Test 4: File inlines `jjArgvFlags(repo)` matching octopus.ts:39-47 — verified by `grep -c "function jjArgvFlags" sdk/src/vcs/jj/parallel.ts` ≥ 1.
    - Test 5: Returns are frozen — `grep -c "Object.freeze" sdk/src/vcs/jj/parallel.ts` ≥ 2 (handle return + inner workspaces array; per D-05).
    - Test 6: fanIn invokes the N-parent jj new — `grep -c "'new'" sdk/src/vcs/jj/parallel.ts` ≥ 1 AND the argv-construction is loop-based over `handle.workspaces` (verifiable by `grep -c "for.*handle.workspaces" sdk/src/vcs/jj/parallel.ts` ≥ 1).
    - Test 7: fanIn uses the existing conflicts() substrate — `grep -c "from './conflict-paths" sdk/src/vcs/jj/parallel.ts` ≥ 1.
    - Test 8: fanIn batches the bookmark delete with the `--` separator — `grep -c "'--'" sdk/src/vcs/jj/parallel.ts` ≥ 1 AND `grep -c "'bookmark', 'delete'" sdk/src/vcs/jj/parallel.ts` ≥ 1.
    - Test 9: dispatch writes the manifest with `backend: 'jj'` and the new `plan_id` field — `grep -c "backend: 'jj'" sdk/src/vcs/jj/parallel.ts` ≥ 1 AND `grep -c "plan_id" sdk/src/vcs/jj/parallel.ts` ≥ 1.
    - Test 10: dispatch composes octopus primitives — `grep -c "createPhaseStructure" sdk/src/vcs/jj/parallel.ts` ≥ 1 AND `grep -c "createSubagentSlot" sdk/src/vcs/jj/parallel.ts` ≥ 1.
    - Test 11: No acquireJjWriteLock / acquireJjRepoLock usage (D-02) — `grep -c "acquireJj" sdk/src/vcs/jj/parallel.ts` returns 0.
    - Test 12: No raw spawnSync (D-12) — `grep -c "spawnSync" sdk/src/vcs/jj/parallel.ts` returns 0.
    - Test 13: agentId validator exists and matches the worktree-safety.cjs:334 character class — `grep -c "function validateAgentId\|validateAgentId(" sdk/src/vcs/jj/parallel.ts` ≥ 2 (one declaration + at least one call from inside the dispatch loop, before manifest write); `grep -E -c "/\\^\\[A-Za-z0-9._/-\\]\\+\\\$/" sdk/src/vcs/jj/parallel.ts` ≥ 1 (the exact regex literal `/^[A-Za-z0-9._/-]+$/` appears at least once).
    - Test 14: fanIn enqueues a `merge-in-tree-conflict` IncompleteWorkEntry on the octopus-conflict path (W3 (a)) — `grep -c "'merge-in-tree-conflict'" sdk/src/vcs/jj/parallel.ts` ≥ 1 AND `grep -c "appendIncomplete" sdk/src/vcs/jj/parallel.ts` ≥ 1 AND `grep -c "from './incomplete-work" sdk/src/vcs/jj/parallel.ts` ≥ 1.
  </behavior>
  <action>
    Create `sdk/src/vcs/jj/parallel.ts` from scratch. Follow 09-PATTERNS.md §"sdk/src/vcs/jj/parallel.ts" excerpts verbatim. Structure:

    1. **Header comment block** (UPSTREAM-02 / Phase 9 / VCS-17 / PARALLEL-01/02 — cite the requirements and the sidecar-discipline invariant per 09-PATTERNS.md §"Header pattern").
    2. **Imports**: `vcsExec` from `'../exec.js'`; `expr` from `'../expr.js'`; types `RevisionExpr`, `ParallelDispatchOpts`, `ParallelDispatchHandle`, `ParallelAgentResult`, `FanInResult`, `IncompleteWorkEntry` from `'../types.js'`; `createPhaseStructure`, `createSubagentSlot` from `'./octopus.js'`; `performJjReap` from `'./reap.js'`; `enumerateConflictedPaths` from `'./conflict-paths.js'`; `appendIncomplete` from `'./incomplete-work.js'`; node stdlib `writeFileSync`, `mkdtempSync` from `'node:fs'`; `tmpdir` from `'node:os'`; `join` from `'node:path'`.
    3. **Inline `jjArgvFlags(repo: string): string[]`** matching octopus.ts:39-47 verbatim.
    4. **Inline `resolveChangeId(mainRepoRoot, revset): string`** matching octopus.ts:49-66 verbatim.
    5. **Inline `validateAgentBookmarkName(name)`** that asserts `/^gsd\/phase-\d+-subagent-\d+$/` (this plan owns this validation because backends/jj.ts:validateRefname cannot be imported under UPSTREAM-02). Throws context-bearing error on mismatch.
    5a. **Inline `validateAgentId(name: string): void`** (W1) — asserts `/^[A-Za-z0-9._/-]+$/.test(name)` and throws `Error(\`parallel.dispatch: agentId "${name}" violates worktree-safety manifest character class /^[A-Za-z0-9._/-]+$/ (see get-shit-done/bin/lib/worktree-safety.cjs:334)\`)` on mismatch. This MUST be called once per plan item, in the dispatch loop, BEFORE any manifest entry is constructed for that agentId. Without this gate, caller-supplied agentIds with whitespace or other invalid characters silently produce manifest entries that `normalizeCleanupManifestEntry` returns null for — losing track of the workspace.
    6. **`performJjParallelDispatch(opts: ParallelDispatchOpts & { mainRepoRoot: string; vcs: { workspace: { add(input: WorkspaceAdd): unknown } } }): ParallelDispatchHandle`**:
       - calls `createPhaseStructure(mainRepoRoot, '@-', opts.phaseNumber)` → `{ parentChange, mergeChange }`
       - **Dispatch loop** for each `(item, idx)` in `opts.plan`:
         - **FIRST step**: `validateAgentId(item.agentId)` — throws context-bearing error if mismatch; the entire dispatch aborts (no manifest written, no workspaces left half-created beyond what createPhaseStructure already produced; createPhaseStructure is idempotent so retry is safe).
         - call `createSubagentSlot(mainRepoRoot, opts.vcs, { parentChange, mergeChange, idx: idx + 1, phaseNum: opts.phaseNumber, workspacePath: item.workspacePath })`; record `{ workspaceName, workspacePath, headChange, agentId: item.agentId, planId: item.planId }`
       - **Eager agent-bookmark creation** (per 09-RESEARCH.md §"What's missing for Phase 9" recommendation (a)): for each slot, `vcsExec(mainRepoRoot, 'jj', [...jjArgvFlags(mainRepoRoot), 'bookmark', 'create', '--', \`gsd/phase-${zeroPaddedPhase}-subagent-${idx+1}\`, '-r', expr.rev(headChange) /* unwrap via raw .toString() if vcsExec needs string */])`. Validate the bookmark name first via `validateAgentBookmarkName`. Throw on non-zero exit. (If `expr.rev` returns a branded string type, pass the underlying string per existing octopus.ts:319 pattern — review octopus.ts to confirm the unwrap convention before implementing.)
       - **Write WAVE_WORKTREE_MANIFEST** at a fresh `mkdtempSync(join(tmpdir(), 'gsd-wave-manifest-'))` path. By the time this code runs, every agentId has already passed `validateAgentId` so the `branch: \`worktree-agent-${agentId}\`` template is guaranteed to satisfy the `/^worktree-agent-[A-Za-z0-9._/-]+$/` regex at worktree-safety.cjs:334. JSON shape per 09-PATTERNS.md §"SDK-side writer pattern": `{ worktrees: [{ agent_id, plan_id, backend: 'jj' as const, worktree_path: slot.workspacePath, branch: \`worktree-agent-${agentId}\`, expected_base: slot.headChange, main_bookmark: opts.mainBookmark }, ...] }`. Use `2` as JSON indent for diff readability.
       - **Return `Object.freeze({ phaseRoot, phaseNumber: opts.phaseNumber, mainBookmark: opts.mainBookmark, manifest: manifestPath, workspaces: Object.freeze(slots.map(s => Object.freeze({ name: s.workspaceName, path: s.workspacePath, baseRev: s.headChange, agentId: s.agentId /* baselineOpId omitted per D-06 planner discretion — DO include if plan 01 kept the field on the type */ }))) }) satisfies ParallelDispatchHandle`. **If plan 01 kept `baselineOpId?` on the interface, include the field set to `undefined` here for forward-compat per D-06; otherwise omit.**
       - `maxConcurrency` is accepted on opts but NOT acted on in Phase 9 (the dispatch primitive returns the handle synchronously; concurrency-capping lives at the orchestrator's `Agent()` boundary per CONTEXT.md domain — wire-through only, do not implement throttling).
    7. **`performJjParallelFanIn(mainRepoRoot: string, handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult`**:
       - **W3 (a) rationale** (per 09-CONTEXT.md D-16 joint-assertion lock-in): D-16 requires that the in-tree-conflict scenario assert BOTH `conflicted: true` AND `conflictedPaths` populated AND an `IncompleteWorkEntry` with `reason === 'merge-in-tree-conflict'` queued. To make this a SINGLE coherent scenario rather than splitting across two test cases (clean-agents-with-octopus-conflict vs crashed-with-conflict-on-head), fanIn enqueues a `merge-in-tree-conflict` queue entry for the MERGE HEAD itself whenever the N-parent octopus produces an in-tree conflict — regardless of whether any agent crashed. Reap.ts's `merge-in-tree-conflict` branch (plan 02 task 2) covers the orthogonal case where a crashed agent's OWN head is conflicted. Both producers emit the same reason literal; both are valid; they queue under different `subagentName` / `changeIdShort` values.
       - **Build N-parent jj new argv**: start with `['new', '-r', '@']`; for each `ws of handle.workspaces` push `'-r', ws.baseRev`. Append `'-m', \`phase ${handle.phaseNumber} merge: ${handle.workspaces.length} parents\``.
       - vcsExec with the full flags + argv. Throw on non-zero.
       - **Resolve the new merge change_id** via `resolveChangeId(mainRepoRoot, '@')`.
       - **Probe conflicts**: call `enumerateConflictedPaths(mainRepoRoot, mergeChangeId)`. Set `conflicted = paths.length > 0` (the sentinel `'<UNRESOLVABLE>'` is itself evidence of conflict per WR-04 — record paths verbatim including sentinel).
       - **Conflicted path (W3 (a))**:
         - populate `conflictedPaths` from the enumeration result
         - **enqueue merge-in-tree-conflict entry**: construct `IncompleteWorkEntry` with `subagentName: \`phase-${zeroPaddedPhase}-merge\`` (or equivalent stable identifier for the octopus merge head; planner choice if existing convention differs), `changeIdShort: mergeChangeId.slice(0, 12)`, `workspacePath: handle.phaseRoot` (the merge happens at the orchestrator working copy in `mainRepoRoot`, but recording phaseRoot here makes the queue entry self-describing per the cross-backend invariant), `reason: 'merge-in-tree-conflict'`. Call `appendIncomplete(handle.phaseRoot, mergeEntry)`. Increment `incompleteQueued` counter by 1 for this entry.
         - do NOT advance main bookmark; do NOT delete agent bookmarks; agent workspaces remain available for the user to inspect
       - **Non-conflicted path**: validateRefname on `handle.mainBookmark` (inline regex), then `jj bookmark set <mainBookmark> -r @`; then ONE batched `jj bookmark delete -- <agentBookmark-1> ... <agentBookmark-N>` (agent bookmark names derived as in dispatch). Throw on non-zero exit on either.
       - **Reap crashed agents**: from `results` filter those with `exitCode !== 0`. For each, prepare an `entries[]` payload matching `performJjReap`'s `PerformJjReapOpts.entries` shape (derive from agent-bookmark/workspace metadata recorded in `handle.workspaces`). Call `performJjReap({ mainRepoRoot, phaseNamePrefix: \`phase-${zeroPaddedPhase}\`, phaseDir: handle.phaseRoot, entries })`. The reap classifier (extended in plan 02) returns `{ abandoned, incomplete }`. Map `failedReaped = abandoned.map(a => a.name)` and accumulate `incompleteQueued += incomplete.length` (added to any merge-in-tree-conflict entry already enqueued above).
       - **`surplusBookmarks`**: after the batched delete (non-conflicted path only), re-list `gsd/phase-${zeroPaddedPhase}-subagent-*` bookmarks via `jj bookmark list -T 'name ++ "\n"' --no-graph` filtered by prefix; on the non-conflicted clean path the result MUST be empty (D-08 invariant — plan 05 contract test gates this). On the conflicted path, no delete was attempted; return `[]` (D-08 "surplus" means UNEXPECTED leftovers from a delete-attempt, not all-bookmarks-still-alive).
       - **`merged`**: on clean path, `[mergeChangeId]`. On conflicted path, `[]`.
       - **Return `Object.freeze({ merged, conflicted, conflictedPaths, incompleteQueued, failedReaped, surplusBookmarks }) satisfies FanInResult`**.
    8. Constraint: NO `acquireJjWriteLock` and NO `acquireJjRepoLock` invocations (D-01/D-02). Single orchestrator-process fanIn does not contend.
    9. Constraint: NO `--ignore-working-copy` flag (D-13 — squash-only commit model). The probes here are reads; they don't need the flag, and adding it violates project_squash_model memory.
    10. Constraint: no fenced code blocks in this action — see 09-PATTERNS.md §"sdk/src/vcs/jj/parallel.ts" for exact shapes.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; grep -c "export function performJjParallelDispatch" sdk/src/vcs/jj/parallel.ts; grep -c "export function performJjParallelFanIn" sdk/src/vcs/jj/parallel.ts; grep -c "from '\.\./backends/jj" sdk/src/vcs/jj/parallel.ts; grep -c "function jjArgvFlags" sdk/src/vcs/jj/parallel.ts; grep -c "Object.freeze" sdk/src/vcs/jj/parallel.ts; grep -c "createPhaseStructure" sdk/src/vcs/jj/parallel.ts; grep -c "createSubagentSlot" sdk/src/vcs/jj/parallel.ts; grep -c "'bookmark', 'delete'" sdk/src/vcs/jj/parallel.ts; grep -c "'--'" sdk/src/vcs/jj/parallel.ts; grep -c "backend: 'jj'" sdk/src/vcs/jj/parallel.ts; grep -c "plan_id" sdk/src/vcs/jj/parallel.ts; grep -c "acquireJj" sdk/src/vcs/jj/parallel.ts; grep -c "spawnSync" sdk/src/vcs/jj/parallel.ts; grep -c "validateAgentId" sdk/src/vcs/jj/parallel.ts; grep -E -c "/\\^\\[A-Za-z0-9._/-\\]\\+\\\$/" sdk/src/vcs/jj/parallel.ts; grep -c "'merge-in-tree-conflict'" sdk/src/vcs/jj/parallel.ts; grep -c "appendIncomplete" sdk/src/vcs/jj/parallel.ts; grep -c "from './incomplete-work" sdk/src/vcs/jj/parallel.ts; node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" lint scripts/lint-vcs-no-commit-id.cjs 2>/dev/null || pnpm -C . exec node scripts/lint-vcs-no-commit-id.cjs</automated>
  </verify>
  <done>
    `parallel.ts` exists with both exported functions; sidecar discipline holds (Test 3 = 0); Object.freeze appears ≥ 2 times; no lock acquisition; no raw spawnSync; manifest carries backend + plan_id; `validateAgentId` is invoked before manifest write; fanIn's octopus-conflict path enqueues a `merge-in-tree-conflict` IncompleteWorkEntry (W3 (a)); TypeScript compiles; `scripts/lint-vcs-no-commit-id.cjs` passes (commit_id appears only in JSDoc comments on the cross-backend types from plan 01, not in `parallel.ts` itself).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire workspace.parallel onto JjVcsAdapter via Object.freeze in backends/jj.ts</name>
  <files>sdk/src/vcs/backends/jj.ts</files>
  <read_first>
    - sdk/src/vcs/backends/jj.ts (lines 1135-1157 — workspace.reap wire-in template; lines 1175-1275 — workspace Object.freeze block end; line 33 for the existing `import { performJjReap } from '../jj/reap.js'` neighborhood)
    - .planning/phases/09-jj-side-parallel-verbs/09-PATTERNS.md (§"sdk/src/vcs/backends/jj.ts (adapter wiring — MOD)" — exact wire-in shape)
    - sdk/src/vcs/jj/parallel.ts (task 1 output — performJjParallel{Dispatch,FanIn} signatures)
  </read_first>
  <behavior>
    - Test 1: `pnpm -C sdk tsc --noEmit` exits 0 — the `parallel` member type-checks against `VcsWorkspaceParallel` from plan 01.
    - Test 2: Import added — `grep -c "from '\.\./jj/parallel" sdk/src/vcs/backends/jj.ts` ≥ 1 with `performJjParallelDispatch` AND `performJjParallelFanIn` in the imported names.
    - Test 3: `workspace = Object.freeze({...})` block contains a `parallel: Object.freeze({ dispatch:`-style member — verified by `grep -c "parallel: Object.freeze" sdk/src/vcs/backends/jj.ts` ≥ 1.
    - Test 4: `dispatch` adapter member delegates to `performJjParallelDispatch` passing `mainRepoRoot: cwd` and `vcs: { workspace }` — verified by `grep -c "performJjParallelDispatch" sdk/src/vcs/backends/jj.ts` ≥ 1.
    - Test 5: `fanIn` adapter member delegates to `performJjParallelFanIn(cwd, handle, results)` — verified by `grep -c "performJjParallelFanIn" sdk/src/vcs/backends/jj.ts` ≥ 1.
    - Test 6: All existing backends/jj.ts tests pass — `pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/ 2>&1 | tail -10` (no regressions).
  </behavior>
  <action>
    1. **Add import** at the top of `sdk/src/vcs/backends/jj.ts` alongside the existing `import { performJjReap } from '../jj/reap.js';` (line 33 neighborhood): `import { performJjParallelDispatch, performJjParallelFanIn } from '../jj/parallel.js';`. Also import `ParallelDispatchOpts`, `ParallelDispatchHandle`, `ParallelAgentResult`, `FanInResult` from `'../types.js'` if not already present.
    2. **Locate the `workspace = Object.freeze({...})` block** that ends around `:1275`. Append a new member `parallel: Object.freeze({...})` BEFORE the closing brace of that block. Shape per 09-PATTERNS.md §"Wire-in shape":
       - `dispatch: (opts: ParallelDispatchOpts): ParallelDispatchHandle => performJjParallelDispatch({ mainRepoRoot: cwd, vcs: { workspace }, ...opts })`
       - `fanIn: (handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult => performJjParallelFanIn(cwd, handle, results)`
       - Inline comment cites `Phase 9 (VCS-16, PARALLEL-01/02): cross-backend parallel namespace; delegates to UPSTREAM-02 sidecar in sdk/src/vcs/jj/parallel.ts`.
    3. Constraint: do NOT modify any existing adapter member (reap, merge, add, remove, list, etc.) — the wire-in is purely additive.
    4. Constraint: the closure passed to `performJjParallelDispatch` MUST give the sidecar access to the adapter's own `workspace.add` so `createSubagentSlot` can call it (per the `vcs: { workspace: { add(input) }}` parameter shape established in `octopus.ts:280-324`). Use the in-scope `workspace` reference inside the Object.freeze block — this is a closure-over-`workspace` self-reference that is valid because the `Object.freeze({...})` literal is evaluated AFTER `workspace`'s identifier binding completes; if a temporal-dead-zone issue arises, hoist the `parallel` block into a `parallel: Object.freeze({ dispatch: (opts) => performJjParallelDispatch({ mainRepoRoot: cwd, vcs: <adapter> /* the parent adapter object, which exposes .workspace at run time */, ...opts }) })` form. Review the `workspace.reap` precedent at `:1135-1157` for the analogous self-reference (it accesses `workspace.list()` inside its body) and follow that pattern.
    5. Constraint: no fenced code blocks in this action — see 09-PATTERNS.md §"Wire-in shape" for exact text.
  </action>
  <verify>
    <automated>pnpm -C sdk tsc --noEmit; grep -c "from '\.\./jj/parallel" sdk/src/vcs/backends/jj.ts; grep -c "parallel: Object.freeze" sdk/src/vcs/backends/jj.ts; grep -c "performJjParallelDispatch" sdk/src/vcs/backends/jj.ts; grep -c "performJjParallelFanIn" sdk/src/vcs/backends/jj.ts; pnpm -C sdk vitest run --reporter=dot --no-coverage src/vcs/__tests__/ 2>&1 | tail -10</automated>
  </verify>
  <done>
    `vcs.workspace.parallel.{dispatch,fanIn}` is callable on a JjVcsAdapter instance; TypeScript accepts the wire-in against the VcsWorkspaceParallel interface from plan 01; existing tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller orchestrator → parallel.dispatch | The caller provides agentIds and planIds; these flow into the manifest and into the agent-bookmark name; validateAgentId pre-empts character-class drift between caller input and worktree-safety.cjs:334 manifest reader |
| jj subprocess via vcsExec | Sole subprocess primitive (D-12); all jj invocations are flagged with `--no-pager --color never --quiet --repository <abs path>` |
| WAVE_WORKTREE_MANIFEST file | On-disk JSON written to a fresh `mkdtemp` path; consumed by worktree-safety.cjs reader (existing, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | agent-bookmark name + agentId | mitigate | `validateAgentBookmarkName` regex `/^gsd\/phase-\d+-subagent-\d+$/` rejects caller-influenced strings before `jj bookmark create`/`delete`; `validateAgentId` regex `/^[A-Za-z0-9._/-]+$/` rejects caller-supplied agentIds before manifest write so they cannot silently drop at the worktree-safety.cjs:334 reader; batched delete uses `--` separator to neutralize leading-hyphen positionals |
| T-09-07 | Information Disclosure | manifest contents | accept | The manifest path lives under `os.tmpdir()` with a random suffix; contents are repo metadata (paths, change_ids, agentIds); no credentials |
| T-09-08 | Tampering | N-parent jj new argv | mitigate | All revision parameters flow through `expr.rev(...)`-branded change_ids resolved via `resolveChangeId`; no caller-supplied strings reach the `-r` positionals without that round-trip |
| T-09-09 | Denial of Service | fanIn N-parent merge | accept | jj 0.41 octopus form scales linearly with N; orchestrator caps N via `maxConcurrency` (wire-through honored by Phase 11 PARALLEL-06; not enforced here) |
| T-09-10 | Repudiation | crashed-agent reap + octopus-conflict queue | mitigate | `performJjReap` (plan 02-extended) appends to `incomplete-work.md` queue with reason literal for CRASHED agents whose heads are conflicted; fanIn (this plan, W3 (a)) appends to the same queue with reason='merge-in-tree-conflict' when the N-parent octopus itself produces an in-tree conflict on clean agents; D-14 phase-merge gate fail-safe-blocks on non-empty queue (verified by plan 02 parse-time validator) |

No high-severity threats — sidecar composition over already-shipped primitives; no new I/O channels; no credentials; no network.
</threat_model>

<verification>
- TypeScript compiles in `sdk/`.
- Sidecar discipline holds (`from '../backends/jj'` grep count = 0 in `parallel.ts`).
- No `acquireJj*` lock usage in `parallel.ts` (D-01/D-02).
- No `spawnSync` in `parallel.ts` (D-12).
- `scripts/lint-vcs-no-commit-id.cjs` green.
- Existing tests pass (no regressions on `backends/jj.ts` other adapter members).
- W1 grep gate: `validateAgentId` declared AND invoked AND the regex literal `/^[A-Za-z0-9._/-]+$/` present.
- W3 (a) grep gate: `'merge-in-tree-conflict'` literal + `appendIncomplete` call + `from './incomplete-work'` import all present in `parallel.ts`.
- End-to-end behavioral gates land in plan 05.
</verification>

<success_criteria>
- `sdk/src/vcs/jj/parallel.ts` exists, exports `performJjParallelDispatch` and `performJjParallelFanIn`, zero sidecar-discipline violations.
- `backends/jj.ts` exposes `workspace.parallel.{dispatch, fanIn}` via Object.freeze.
- Returns are frozen pure JSON (D-05).
- Manifest writer emits `plan_id` + `backend: 'jj'` (VCS-19) AND every agentId is gated by `validateAgentId` against the worktree-safety.cjs:334 character class BEFORE manifest write.
- fanIn uses single N-parent jj new + batched bookmark delete (PARALLEL-02 jj-side).
- fanIn enqueues a `merge-in-tree-conflict` IncompleteWorkEntry when the octopus produces an in-tree conflict on clean agents (W3 (a) — joint-assertion producer for D-16).
- TypeScript compiles; all pre-existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/09-jj-side-parallel-verbs/09-03-SUMMARY.md` when done.
</output>
