---
phase: 09-jj-side-parallel-verbs
plan: 01
subsystem: vcs
tags: [vcs, types, parallel, jj, sdk]

# Dependency graph
requires:
  - phase: 08-unified-revision-model
    provides: unified `.id` cross-backend revision contract; `lint-vcs-no-commit-id.cjs` CI enforcer
  - phase: 04-workspaces-and-octopus
    provides: IncompleteWorkEntry type; reap.ts classifier substrate; octopus.ts topology helpers
  - phase: 07-upstream-rebase-1
    provides: VcsWorkspace.merge 2-parent shape; WorkspaceMergeResult precedent for FanInResult
provides:
  - VcsWorkspaceParallel cross-backend interface (dispatch + fanIn)
  - ParallelDispatchOpts / ParallelDispatchHandle / ParallelAgentResult / FanInResult contracts
  - Tightened IncompleteWorkEntry.reason closed-union (2 values per D-09)
  - VcsWorkspace.parallel field as wave-2 compile gate
affects: [09.02-reap-classifier, 09.03-jj-backend-body, 09.04-git-backend-stub, 10-git-side-parallel-verbs, 13-ci-parallel-lane]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Sub-namespace under VcsWorkspace (parallel:) — precedent: VcsBookmarks under VcsRefs"
    - "Frozen-JSON Handle pattern at the type level (D-05/D-06) — pure data, no closures/methods/Symbols"
    - "Closed string-literal union for parser-validated enum fields (IncompleteWorkEntry.reason)"
    - "Cross-backend baseRev JSDoc canonical wording (change_id-on-jj / commit_id-on-git rebase-stable; PARALLEL-05/D-14)"

key-files:
  created: []
  modified:
    - sdk/src/vcs/types.ts

key-decisions:
  - "Honored <done> over <verification>: downstream backends/jj.ts + incomplete-work.ts compile-failure is intentional wave-2 wire-in gate"
  - "baselineOpId? kept (not stripped) per planner discretion (CONTEXT D-06 bullet 3) — forward-compat reservation for re-introduced liveness probe"
  - "VcsWorkspaceParallel placed immediately after VcsWorkspace block, with the parallel-dispatch type cluster (Opts / AgentResult / Handle / FanInResult) following — single contiguous Phase-9 section for readability"

patterns-established:
  - "Pattern: cross-backend parallel-dispatch verb namespace = VcsWorkspace.parallel sub-namespace"
  - "Pattern: pure-JSON Handle shape at interface level (Object.freeze enforcement lives in adapter body, plan 03)"
  - "Pattern: D-14 canonical JSDoc on every rebase-stable revision field (change_id on jj; commit_id on git; (PARALLEL-05, D-14.))"

requirements-completed: [VCS-16, VCS-17, VCS-19, PARALLEL-05]

# Metrics
duration: 6min
completed: 2026-05-15
---

# Phase 9 Plan 01: jj-side parallel verbs (type surface) Summary

**Cross-backend `vcs.workspace.parallel.*` type contract — five new exports plus tightened IncompleteWorkEntry.reason union — landing the compile gate for plans 02/03/04.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-05-15T14:15:34Z
- **Completed:** 2026-05-15T14:21:40Z
- **Tasks:** 1 (single-task type-declarations-only plan)
- **Files modified:** 1

## Accomplishments

- Five new exports added to `sdk/src/vcs/types.ts`: `VcsWorkspaceParallel`, `ParallelDispatchOpts`, `ParallelDispatchHandle`, `ParallelAgentResult`, `FanInResult`.
- `VcsWorkspace` extended with `parallel: VcsWorkspaceParallel` field.
- `IncompleteWorkEntry.reason` tightened from free-form `string` to closed literal union `'crashed-with-uncommitted-work' | 'merge-in-tree-conflict'` (D-09).
- D-14 canonical JSDoc wording landed on `ParallelDispatchHandle.workspaces[].baseRev` (PARALLEL-05).
- `lint-vcs-no-commit-id.cjs` re-verified green (1033 files / 0 violations) — D-14 JSDoc lives in a comment block tolerated by the lint allowlist.

## Task Commits

1. **Task 1: Add five new types + extend VcsWorkspace.parallel + tighten IncompleteWorkEntry.reason** — `ktw` (feat)

Note: TDD frontmatter `tdd="true"` was declared on the task, but the behavior tests are TypeScript-compilation gates and grep-count gates rather than runtime unit tests. The RED phase would be "before my edits, every grep returned 0 and downstream compile errors were absent"; the GREEN phase is the post-edit verification documented below. No separate `test(...)` commit was emitted because the gates are static (compilation + grep), not vitest cases.

## Files Created/Modified

- `sdk/src/vcs/types.ts` — added the `VcsWorkspaceParallel` namespace + 4 supporting types, appended `parallel:` member to `VcsWorkspace`, tightened `IncompleteWorkEntry.reason`.

## Decisions Made

- **Kept `baselineOpId?` reserved** (CONTEXT D-06 "Claude's Discretion" bullet 3): documented as forward-compat for a future re-introduced liveness probe, with JSDoc explicitly noting D-01 dropped PARALLEL-03 at Phase 9 discuss-phase. Planner judgment: shape stability across hypothetical future re-introduction outweighs the YAGNI-noise cost (one optional field, fully documented).
- **Honored `<done>` clause over `<verification>`**: the plan's `<done>` explicitly states "downstream `backends/{jj,git}.ts` MUST fail compilation until plans 03 and 04 wire `parallel` (this compile-failure is intentional; do not pre-emptively stub the backend wiring from this plan)" — this is the more-specific clause and reflects the wave-2 gate intent. The `<verification>` "MUST exit 0" line is the planner's idealized post-wave-2 target, not the post-this-plan target.
- **Placed the Phase 9 type cluster contiguously** after the `VcsWorkspace` interface, with a clear section header comment. This colocates the namespace declaration (`VcsWorkspaceParallel`) with its argument/return types (`ParallelDispatchOpts`, `ParallelAgentResult`, `ParallelDispatchHandle`, `FanInResult`) — easier to grep, easier to amend in plan 03.

## Deviations from Plan

None - plan executed exactly as written.

The intentional compile-failures (`backends/jj.ts:1045` missing `parallel` field; `incomplete-work.ts:74` free-form reason string) are NOT deviations — they are the wave-2 wire-in gate documented in the plan's `<done>` clause.

## Issues Encountered

- **`<verification>` vs `<done>` clause tension**: plan's `<verification>` says "`pnpm -C sdk tsc --noEmit` is the load-bearing gate. It MUST exit 0", but `<done>` says downstream files MUST fail compilation. Resolved by honoring `<done>` (more specific clause, explicitly addresses post-this-plan state); the `<verification>` clause is interpreted as the post-wave-2 target.
- **Invocation form for `tsc`**: `pnpm -C sdk` form failed (no recursive script "sdk" in workspace); `pnpm --filter sdk` form failed (no "tsc" script in sdk/package.json). Used `cd sdk && pnpm exec tsc --noEmit`. No deviation — just a CLI-form mechanical fix, not a substantive change.

## Self-Check: PASSED

- File modified: `sdk/src/vcs/types.ts` — FOUND.
- Commit `ktw` (feat: VcsWorkspaceParallel + parallel-dispatch types) — recorded via `gsd-sdk query commit`; routed through jj backend.
- All 8 grep-count gates ≥ 1.
- D-14 canonical wording substrings (`change_id on jj`, `commit_id on git`, `rebase-stable`, `(PARALLEL-05, D-14.)`) all present in JSDoc.
- `lint-vcs-no-commit-id.cjs`: 1033 files / 0 violations.
- types.ts itself contains zero TypeScript errors; the two errors emitted by `tsc --noEmit` are the documented wave-2 gate failures in `backends/jj.ts` + `jj/incomplete-work.ts`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **Plan 09.02 (reap classifier extension)** can now import `IncompleteWorkEntry` and rely on the closed-union `reason` field — parse-time validator in `jj/incomplete-work.ts` becomes a type-narrowing operation.
- **Plan 09.03 (jj backend body)** can now import `VcsWorkspaceParallel`, `ParallelDispatchOpts`, `ParallelDispatchHandle`, `ParallelAgentResult`, `FanInResult` from `../types.js` for the new `sdk/src/vcs/jj/parallel.ts` sidecar.
- **Plan 09.04 (git backend throwing stub)** can wire `parallel: Object.freeze({ dispatch, fanIn })` with `VcsNotImplementedError` throws, closing the compile gate on `backends/git.ts`.

Both wave-2 compile-failures (`backends/jj.ts:1045`, `incomplete-work.ts:74`) are the documented wire-in gates — they will resolve as plans 02/03/04 land. No blockers for downstream plans.

---
*Phase: 09-jj-side-parallel-verbs*
*Completed: 2026-05-15*
