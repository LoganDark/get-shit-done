---
phase: 05-command-translations-brownfield-validation-ci-hardening
plan: 05
type: execute
wave: 3
depends_on: [05-01, 05-02, 05-03, 05-04]
files_modified:
  - sdk/src/vcs/__tests__/jj-octopus.test.ts
  - sdk/src/vcs/__tests__/jj-lock.test.ts
  - sdk/src/vcs/__tests__/jj-hooks.test.ts
  - sdk/src/vcs/__tests__/jj-workspace.test.ts
  - sdk/src/vcs/__tests__/jj-push-fetch.test.ts
  - sdk/src/vcs/__tests__/jj-commit.test.ts
  - sdk/src/vcs/__tests__/exec-env-passthrough.test.ts
  - .github/workflows/test.yml
  - .planning/intel/ci-jj-soak.md
  - get-shit-done/bin/lib/core.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/init.cjs
  - get-shit-done/bin/lib/graphify.cjs
  - get-shit-done/bin/lib/drift.cjs
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - CI-03
  - PROMPT-03
  - MIGR-02

must_haves:
  truths:
    - "Seven flake-prone jj-* test files have flake-fix patches applied (describe.sequential for multi-workspace suites; per-invocation random-prefix mkdtemp for tmpdir-bound; afterEach cleanup where afterAll was insufficient)"
    - "A `.planning/intel/ci-jj-soak.md` soak-metric file exists, tracks 10-consecutive-green nightly runs across jj-colocated + jj-native + git lanes, and is updated either manually or via a small helper script"
    - "`.github/workflows/test.yml` matrix flips both jj lanes from `continue-on-error: true` to required-blocking AFTER the 10-green soak passes (D-36 step 2)"
    - "CI-03 docs note added to `.github/workflows/test.yml` (or a sibling doc) explicitly stating 'GitHub Actions workflows stay on git — GitHub *is* git'"
    - "MIGR-02 cosmetic sweep done on the 6 cjs files (core/verify/commands/init/graphify/drift): error-message strings + comments updated from `git worktree …` references to `gsd-sdk query worktree-list` / `vcs.workspace.*` references"
    - "PROMPT-03 trust-installer closure note recorded in plan SUMMARY (no per-runtime smoke matrix added per D-37)"
    - "REQUIREMENTS.md MIGR-02 status updated to `Complete` after cosmetic sweep; CI-03 and PROMPT-03 marked Complete"
    - "No regressions: lint-vcs-no-raw-git stays at 0 violations; existing tests in sdk/ pass; plans 05-01..05-04 outputs all still pass"
  artifacts:
    - path: ".planning/intel/ci-jj-soak.md"
      provides: "10-consecutive-green CI soak tracker for D-36 step 2"
      contains: "consecutive green"
    - path: ".github/workflows/test.yml"
      provides: "required-blocking jj-colocated + jj-native matrix entries (post-soak)"
      contains: "GitHub *is* git"
    - path: "sdk/src/vcs/__tests__/jj-octopus.test.ts"
      provides: "describe.sequential + flake-fix patches"
      contains: "describe.sequential"
    - path: ".planning/REQUIREMENTS.md"
      provides: "MIGR-02 / CI-03 / PROMPT-03 marked Complete"
      contains: "MIGR-02 | Phase 2 | Complete"
  key_links:
    - from: ".planning/intel/ci-jj-soak.md"
      to: ".github/workflows/test.yml matrix flip"
      via: "10-consecutive-green gate"
      pattern: "10/10|Final Graduation"
    - from: "Seven flake-fixed test files"
      to: ".planning/intel/ci-jj-soak.md run log"
      via: "nightly runs over 10-day window"
      pattern: "describe\\.sequential|mkdtempSync.*random"
---

<objective>
Close the Phase 5 gate by (1) fixing the 7 specific flake sources Phase 4 LEARNINGS cited (jj-octopus, jj-lock, jj-hooks, jj-workspace, jj-push-fetch, jj-commit, exec-env-passthrough), (2) running a 10-consecutive-green soak window across both jj CI lanes tracked in `.planning/intel/ci-jj-soak.md`, (3) flipping the `.github/workflows/test.yml` matrix entries from `continue-on-error: true` to required-blocking once the soak passes (D-36), (4) doing the MIGR-02 cosmetic sweep on the 6 `bin/lib/*.cjs` files (D-35 sweep clause), (5) adding the CI-03 docs note about GitHub Actions staying on git, and (6) closing PROMPT-03 per D-37 (no per-runtime smoke matrix; trust the installer).

Purpose: This plan is the phase close. CI graduation is the named hand-off from Phase 3 (D-11) and Phase 4 (D-22) — both deferred to "Phase 5". MIGR-02 has been "in progress" since Phase 2; 05-RESEARCH.md verified all 6 cjs files already pass the lint guard (0 violations across 923 files on 2026-05-13) — what remains is cosmetic. PROMPT-03 has no implementation gate per D-37 — it's a documented-decision close.

Output: Patches to 7 test files (no test additions, only flake fixes), one new soak-tracker file, one CI workflow flip (gated on a CHECKPOINT — human verifies soak passed), comment / error-string updates in 6 cjs files, requirements status updates. Autonomous=false because the CI matrix flip is a `checkpoint:human-verify` gate (the executor cannot itself confirm 10 nightly green runs without ≥10 days of calendar time).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-CONTEXT.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-01-SUMMARY.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-03-SUMMARY.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-04-SUMMARY.md
@.planning/phases/04-workspaces-octopus-structure-hooks/04-LEARNINGS.md
@.github/workflows/test.yml
@sdk/src/vcs/__tests__/jj-octopus.test.ts
@sdk/src/vcs/__tests__/jj-lock.test.ts
@sdk/src/vcs/__tests__/jj-hooks.test.ts
@sdk/src/vcs/__tests__/jj-workspace.test.ts
@sdk/src/vcs/__tests__/jj-push-fetch.test.ts
@sdk/src/vcs/__tests__/jj-commit.test.ts
@sdk/src/vcs/__tests__/exec-env-passthrough.test.ts
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/lib/verify.cjs

<interfaces>
<!-- Flake fix patterns from 05-PATTERNS.md §"Flake-fix sweep across 7 jj-* test files". -->

Pattern A — describe.sequential (multi-workspace suites):
```typescript
// BEFORE:
describe('jj octopus — Phase 4 plan 05', () => { /* uses vcsMultiWsTest */ });

// AFTER:
describe.sequential('jj octopus — Phase 4 plan 05 (sequential to avoid concurrency contention)', () => { /* same body */ });
```

Pattern B — per-invocation random-prefix mkdtemp (tmpdir-bound suites):
```typescript
// BEFORE (afterAll-only — collides across parallel files):
beforeAll(() => { dir = mkdtempSync(join(tmpdir(), 'gsd-jj-hooks-')); /* ... */ });
afterAll(() => { rmSync(dir, ...); });

// AFTER (afterEach with re-init when test body mutates destructively):
beforeEach(() => {
  dir = mkdtempSync(join(tmpdir(), `gsd-jj-hooks-${Math.random().toString(36).slice(2, 10)}-`));
  /* ... rest of init ... */
});
afterEach(() => { if (dir) rmSync(dir, { recursive: true, force: true }); });
```

(Planner discretion: keep `beforeAll`/`afterAll` for suites that don't mutate destructively; only escalate to `beforeEach`/`afterEach` where state actually leaks. The Math.random suffix is the cheapest collision-avoidance for parallel test files.)

CI matrix block (.github/workflows/test.yml:56-71 — the only line that changes is line 64):
```yaml
# BEFORE (line 64):
continue-on-error: ${{ matrix.backend == 'jj-colocated' || matrix.backend == 'jj-native' }}

# AFTER (post-soak):
# CI-01 / CI-04 graduation event landed (Phase 5 plan 05-05 per D-36 step 2).
# Both jj lanes now required-blocking after 10-consecutive-green soak window.
# GitHub Actions workflows themselves (canary, release-sdk, hotfix, branch-cleanup,
# auto-branch) stay on git per CI-03 — GitHub *is* git.
continue-on-error: false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply flake fixes to the 7 jj-* test files</name>
  <files>sdk/src/vcs/__tests__/jj-octopus.test.ts, sdk/src/vcs/__tests__/jj-lock.test.ts, sdk/src/vcs/__tests__/jj-hooks.test.ts, sdk/src/vcs/__tests__/jj-workspace.test.ts, sdk/src/vcs/__tests__/jj-push-fetch.test.ts, sdk/src/vcs/__tests__/jj-commit.test.ts, sdk/src/vcs/__tests__/exec-env-passthrough.test.ts</files>
  <read_first>
    - All 7 test files in full (each is 100-400 LOC; review the full file before patching to identify whether the suite is concurrency-bound, tmpdir-bound, or both)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"CI Flake Analysis (D-36 Step 1)" — the categorization and recommended fix sequence
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"Flake-fix sweep across 7 jj-* test files" — the exact pattern excerpts
    - `.planning/phases/04-workspaces-octopus-structure-hooks/04-LEARNINGS.md` §"Pre-existing Failures Still Deferred" + §"Velocity / Context-Cost Notes" — the original flake citation including the "~50% wall-clock overhead vs local isolation" observation that names this work
  </read_first>
  <action>
    Apply per-file flake patches based on the per-file categorization. Categorize each file before patching (read the source; identify `vcsMultiWsTest` usage = concurrency-bound; identify `mkdtempSync(.., 'gsd-jj-<name>-')` prefix that doesn't include a random suffix = tmpdir-bound). Most files are both.

    **`jj-octopus.test.ts` (concurrency-bound; uses lazy octopus + multi-workspace contract tests):**
    - Wrap the top-level `describe(...)` with `describe.sequential(...)`. Update the describe-block string to note the sequential-execution rationale (a one-liner like `'octopus.ts — Phase 4 plan 05 (WS-05..10, run sequential to avoid jj working-copy contention)'`).
    - Switch the existing `beforeAll`/`afterAll` to `beforeEach`/`afterEach` if (and only if) tests within the describe mutate `dir`'s working-copy state in ways the next test depends on being clean. If the existing suite is structured around shared-fixture reuse, leave the lifecycle as-is and rely solely on `describe.sequential` for in-suite serialization.
    - Add per-invocation random suffix to the `mkdtemp` prefix: `mkdtempSync(join(tmpdir(), \`gsd-jj-octopus-${Math.random().toString(36).slice(2, 10)}-\`))`.

    **`jj-lock.test.ts` (concurrency-bound by definition — lock tests):**
    - The suite intentionally races on a shared lock primitive — DO NOT serialize the test body. The flake is in fixture-tmpdir contention, not in the lock logic itself.
    - Apply Pattern B only: per-invocation random-prefix mkdtemp. Keep `describe(...)` non-sequential (the lock tests exercise concurrency by design).

    **`jj-hooks.test.ts` (tmpdir-bound; includes the inverted assertions from plan 05-01):**
    - Apply Pattern B (per-invocation random-prefix mkdtemp). Existing prefix is likely `'gsd-jj-hooks-'` or `'gsd-jj-hooks-native-'`; extend to `\`gsd-jj-hooks-${Math.random().toString(36).slice(2, 10)}-\``.
    - Keep the assertions from plan 05-01 (D-32 inverted assertions) intact — this task is flake-fix only, no semantic changes.

    **`jj-workspace.test.ts` (concurrency-bound — multi-workspace tests):**
    - Wrap top-level describe with `describe.sequential(...)`.
    - Apply Pattern B (random-prefix mkdtemp).

    **`jj-push-fetch.test.ts` (tmpdir-bound; remote-simulation can be flaky on `/tmp` reuse):**
    - Apply Pattern B (random-prefix mkdtemp).
    - Verify whether tests internally chain a setup→push→fetch flow that mutates the same dir across `it()` calls; if so, escalate to `beforeEach`/`afterEach`.

    **`jj-commit.test.ts` (likely both):**
    - Wrap describe with `describe.sequential(...)` IF the suite includes hook-firing tests (the post-squash hook fire from plan 05-01 makes timing-sensitive); apply Pattern B regardless.

    **`exec-env-passthrough.test.ts` (env-mutation tests — likely tmpdir-bound + process-env-bound):**
    - Apply Pattern B (random-prefix mkdtemp).
    - If the suite mutates `process.env.JJ_USER` / `process.env.JJ_EMAIL` without a cleanup `afterEach`, add a `vi.stubEnv` / `vi.unstubAllEnvs` lifecycle wrapper. This is a separate flake category (process env leakage across tests in the same file).

    For each patched file:
    1. Verify the file still compiles after the patch (`pnpm tsc --noEmit`).
    2. Run the file in isolation 3 times in a row (locally, NOT in CI): `pnpm vitest run sdk/src/vcs/__tests__/<file>.test.ts --reporter=default` — assert it passes 3/3.
    3. Document the patch shape (which patterns A/B applied) in the plan SUMMARY.

    Do NOT add new test cases. Do NOT reshape test logic. The fixes are mechanical / structural — patterns A and B applied where the file's flake category dictates.
  </action>
  <acceptance_criteria>
    - `grep -lP "describe\\.sequential" sdk/src/vcs/__tests__/jj-octopus.test.ts sdk/src/vcs/__tests__/jj-workspace.test.ts sdk/src/vcs/__tests__/jj-commit.test.ts | wc -l` is at least 2 (jj-octopus + jj-workspace minimum; jj-commit conditional).
    - `grep -cP "mkdtempSync\\(join\\(tmpdir\\(\\), \\`gsd-jj-\\w+-\\$\\{Math\\.random" sdk/src/vcs/__tests__/{jj-octopus,jj-lock,jj-hooks,jj-workspace,jj-push-fetch,jj-commit,exec-env-passthrough}.test.ts` is at least 7 (one per file).
    - `cd sdk && pnpm tsc --noEmit` exits 0.
    - Three consecutive local runs of each patched file pass: `cd sdk && for i in 1 2 3; do pnpm vitest run sdk/src/vcs/__tests__/jj-octopus.test.ts sdk/src/vcs/__tests__/jj-lock.test.ts sdk/src/vcs/__tests__/jj-hooks.test.ts sdk/src/vcs/__tests__/jj-workspace.test.ts sdk/src/vcs/__tests__/jj-push-fetch.test.ts sdk/src/vcs/__tests__/jj-commit.test.ts sdk/src/vcs/__tests__/exec-env-passthrough.test.ts || exit 1; done`.
    - Full suite passes once: `cd sdk && pnpm vitest run` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd sdk && pnpm tsc --noEmit && for i in 1 2 3; do pnpm vitest run sdk/src/vcs/__tests__/jj-octopus.test.ts sdk/src/vcs/__tests__/jj-lock.test.ts sdk/src/vcs/__tests__/jj-hooks.test.ts sdk/src/vcs/__tests__/jj-workspace.test.ts sdk/src/vcs/__tests__/jj-push-fetch.test.ts sdk/src/vcs/__tests__/jj-commit.test.ts sdk/src/vcs/__tests__/exec-env-passthrough.test.ts || exit 1; done</automated>
  </verify>
  <done>
    Seven flake-prone test files patched with the mechanical fix patterns A/B per their category; three consecutive local runs of each pass; full suite passes; types compile cleanly. No new test cases added (this is fix-only).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `.planning/intel/ci-jj-soak.md` soak tracker + MIGR-02 cosmetic sweep</name>
  <files>.planning/intel/ci-jj-soak.md, get-shit-done/bin/lib/core.cjs, get-shit-done/bin/lib/verify.cjs, get-shit-done/bin/lib/commands.cjs, get-shit-done/bin/lib/init.cjs, get-shit-done/bin/lib/graphify.cjs, get-shit-done/bin/lib/drift.cjs</files>
  <read_first>
    - `.planning/intel/git-touchpoints.md` (the soak-file shape analog — see 05-PATTERNS.md §"`.planning/intel/ci-jj-soak.md` (new soak-metric file)")
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"CI Soak Metric File Shape (D-36 Step 2)"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"MIGR-02 File-by-File Remaining Work" — confirms cosmetic-only nature
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"MIGR-02 cosmetic sweep — 6 cjs files" — per-file natural touch surface table
    - Each of the 6 cjs files at the specific line ranges flagged in 05-PATTERNS.md (`core.cjs` ~763, ~788; `verify.cjs` ~949-982; `commands.cjs` ~334-341; `init.cjs` ~1507, ~1536; `graphify.cjs` sweep; `drift.cjs` sweep)
  </read_first>
  <action>
    **Sub-task A — soak tracker:** Create `.planning/intel/ci-jj-soak.md` per the shape in 05-PATTERNS.md §"`.planning/intel/ci-jj-soak.md`":

    ```markdown
    # CI jj-Backend Soak Window

    **Started:** YYYY-MM-DD (the executor fills in the actual date)
    **Target:** 10 consecutive green nightly runs across both `jj-colocated` and `jj-native` lanes (D-36 step 2)
    **Status:** 0/10 consecutive (last update: YYYY-MM-DD)

    ## Run Log

    | # | Date | Run ID | jj-colocated | jj-native | git | Notes |
    |---|------|--------|--------------|-----------|-----|-------|
    | _ | _    | _      | _            | _         | _   | (populate as runs complete) |

    ## Reset Events

    (populate when the counter resets)

    ## Final Graduation

    Date: TBD
    Commit removing `continue-on-error`: TBD
    ```

    Add a short header comment naming D-36 step 2 as the source decision.

    Per 05-RESEARCH.md Open Q5: choose Option C (manual append) for v1. Optionally ship `scripts/show-ci-soak.cjs` if manual auditing becomes onerous — that's the planner's call at execute time and out of scope of this plan unless the executor chooses to add it (small ~30 LOC helper).

    **Sub-task B — MIGR-02 cosmetic sweep on 6 cjs files:**

    Per 05-PATTERNS.md table, update the following surfaces (line numbers approximate — grep to confirm before edit):

    1. **`get-shit-done/bin/lib/core.cjs`** (~763, ~788): Find error-message strings like `'Run: \`git worktree prune\`'` and rewrite to `'Run: \`gsd-sdk query worktree-list\` (then call vcs.workspace.forget/prune as appropriate)'`. Update any `// git worktree …` comments to `// vcs.workspace.* — see sdk/src/vcs/types.ts VcsWorkspace`.

    2. **`get-shit-done/bin/lib/verify.cjs`** (~949-982): Error-message strings about worktree health → `gsd-sdk query worktree-list` / `vcs.workspace.list()`.

    3. **`get-shit-done/bin/lib/commands.cjs`** (~334-341): Comments only — already references `vcs.commit` via the adapter; spot-check the comments mention the right verb names.

    4. **`get-shit-done/bin/lib/init.cjs`** (~1507, ~1536): Child-repo detection prose — verify the comments describe what the adapter actually does post-Phase-2 migration.

    5. **`get-shit-done/bin/lib/graphify.cjs`** (sweep — no specific lines): grep for any `git worktree` / `git branch` strings in comments or error messages; update to adapter terminology.

    6. **`get-shit-done/bin/lib/drift.cjs`** (sweep — no specific lines): same as graphify.cjs.

    These changes are STRING-ONLY (comments + error-message strings). No call-site code changes. Verified by 05-RESEARCH.md: lint-vcs-no-raw-git already reports 0 violations across 923 files; the cjs files already passed Phase 2 plan 02-11. This task is purely cosmetic — D-35 sweep clause.

    After all 6 files: update `.planning/REQUIREMENTS.md` MIGR-02 row to `Complete` (currently `In Progress`). Per the existing format, the line at ~211 should change from:
    ```
    | MIGR-02 | Phase 2 | In Progress |
    ```
    to:
    ```
    | MIGR-02 | Phase 2 | Complete (cosmetic sweep landed in Phase 5 plan 05-05) |
    ```

    Add an update note to REQUIREMENTS.md footer per the existing convention.
  </action>
  <acceptance_criteria>
    - `test -f .planning/intel/ci-jj-soak.md` succeeds.
    - `grep -P "10 consecutive green|10/10|Final Graduation" .planning/intel/ci-jj-soak.md | wc -l` is at least 2.
    - `grep -P "D-36" .planning/intel/ci-jj-soak.md | wc -l` is at least 1.
    - `grep -cP "git\\s+worktree" get-shit-done/bin/lib/{core,verify,commands,init,graphify,drift}.cjs` decreased compared to pre-sweep baseline (verify by reading the files; the exact count threshold is "fewer than before"). Acceptable post-sweep value: ≤5 across all 6 files combined (some legitimate git-only mentions may stay, e.g., in fallback prose for git-only escape hatches).
    - `node scripts/lint-vcs-no-raw-git.cjs` continues to exit 0 (NO new violations introduced by the cosmetic sweep).
    - `grep -P "MIGR-02 \\| Phase 2 \\| Complete" .planning/REQUIREMENTS.md | wc -l` is at least 1.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/intel/ci-jj-soak.md && grep -P "10 consecutive green" .planning/intel/ci-jj-soak.md && grep -P "MIGR-02 \| Phase 2 \| Complete" .planning/REQUIREMENTS.md && node scripts/lint-vcs-no-raw-git.cjs</automated>
  </verify>
  <done>
    Soak tracker file exists with the canonical shape; MIGR-02 cosmetic sweep applied to 6 cjs files (error-message strings + comments only); lint guard stays green; REQUIREMENTS.md MIGR-02 row flipped to Complete.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human checkpoint — verify 10-consecutive-green CI soak passed</name>
  <what-built>
    Tasks 1 and 2 have landed: 7 flake-prone test files patched, soak tracker created, MIGR-02 swept. The CI workflow is unchanged (jj lanes still `continue-on-error: true`). What this checkpoint gates: the human verifies the soak file shows 10 consecutive green runs (or chooses one of the D-36 fallback paths per the discretion clause) BEFORE Task 5 flips the CI matrix to required-blocking.
  </what-built>
  <how-to-verify>
    1. Open `.planning/intel/ci-jj-soak.md`. Read the Run Log section.
    2. Confirm the most recent 10 nightly runs (or 10 most recent runs) have ✓ for both `jj-colocated` and `jj-native` columns.
    3. If yes (counter at 10/10): reply `approved — proceed to CI flip` and Task 5 lands.
    4. If counter <10 but ≥7: planner discretion per D-36 — extend the window (reply `extend — N more days`) or approve with a documented known-flake gate (reply `approve with known-flake gate: <test-name>`).
    5. If counter <7 or repeated resets: investigate the flake pattern (reply `investigate — pause for fix iteration`). The plan executor returns to Task 1 to re-patch.

    Optional automation help (if the human wants to inspect CI history):
    ```bash
    export GITHUB_TOKEN=$(grep GITHUB_TOKEN .envrc | cut -d\' -f2)
    gh api repos/gsd-build/get-shit-done/actions/runs?per_page=15 --jq '.workflow_runs[] | select(.name == "test") | {id, status, conclusion, created_at, head_branch}'
    ```
    This isn't a gate (the soak file is the source of truth) but helps the human cross-reference run conclusions to the manual log.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `approved — proceed to CI flip` (counter at 10/10; Task 5 lands)
    - `extend — <N> more days` (counter <10; restart the wait period, no Task 5 yet)
    - `approve with known-flake gate: <test-name>` (D-36 discretion clause; Task 5 lands but with a documented exception)
    - `investigate — pause for fix iteration` (counter resetting repeatedly; back to Task 1)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 4: Sweep verb-gap TODOs and branch-create-gap TODOs from plan 05-02 + 05-03</name>
  <files>get-shit-done/workflows/execute-phase.md, get-shit-done/workflows/quick.md, get-shit-done/workflows/undo.md, get-shit-done/workflows/complete-milestone.md, get-shit-done/workflows/code-review.md, agents/gsd-code-fixer.md, agents/gsd-executor.md</files>
  <read_first>
    - All seven markdown files at the post-plan-05-02/05-03 state (grep their `# TODO(05-05 sweep):` and `<!-- TODO: branch-create gap fill -->` site lists from plan 05-02-SUMMARY.md and 05-03-SUMMARY.md)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md` §"Deferred to plan 05-05 sweep" — the verb-gap TODO inventory
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-03-SUMMARY.md` §"Deferred to plan 05-05 sweep" + the `<!-- TODO: branch-create gap fill -->` enumeration (from the per-file site list)
    - `sdk/src/vcs/types.ts` `VcsWorkspace.add` (lines 165-173) and `VcsBookmarks.create` (line 292) — needed to decide between adapter gap-fill vs per-site rewrite for the branch-create surface
  </read_first>
  <action>
    Two parallel sweep passes:

    **Sub-task A — verb-gap sweep (plan 05-02 INFO1 closure):**

    Plan 05-02 left up to ~12 sites in `execute-phase.md` and up to ~10 in `quick.md` tagged `# TODO(05-05 sweep): add gsd-sdk query <verb> verb` (plus a small number in 05-03's three workflow files). For each verb mentioned in the TODOs, decide one of:

    1. **Add the verb to the SDK catalog (preferred when the verb is reusable):** if 3+ TODOs name the same missing verb (e.g., `git stash` → `gsd-sdk query stash`, `git worktree list` → `gsd-sdk query worktree-list`, `git branch -d` → `gsd-sdk query branch-delete`, `git tag -a` → `gsd-sdk query tag`), add a new `sdk/src/query/<verb>.ts` shim mirroring plan 05-01 Task 2 shape, register in catalog + manifest, add paired test. Then rewrite all matching TODO sites to call the new verb.
    2. **Per-site rewrite (preferred when the verb is one-off):** if only 1-2 TODOs name a verb, rewrite each site using a closer-fit existing verb (e.g., `git stash push` followed by `git stash pop` may collapse to a single `gsd-sdk query commit --amend` if the surrounding workflow logic allows; the planner picks per-site).
    3. **Document as permanent git-only escape hatch:** if the verb is structurally git-only (e.g., `git merge-base` on jj has no direct equivalent — jj uses revsets), remove the TODO comment, replace with a prose comment `# git-only escape hatch — jj equivalent uses revsets per FEATURES.md`, and document in 05-LEARNINGS.md.

    Target: `grep -cP "# TODO\(05-05 sweep\)" get-shit-done/workflows/*.md agents/*.md` is 0 after this sub-task.

    **Sub-task B — branch-create-gap sweep (plan 05-03 W4 closure):**

    Plan 05-03 Task 2 left `<!-- TODO: branch-create gap fill -->` HTML comment markers at every `git worktree add -b` rewrite site in `agents/gsd-code-fixer.md` (and possibly elsewhere). Count the sites:

    ```bash
    grep -rcP "<!-- TODO: branch-create gap fill -->" get-shit-done/workflows/ agents/
    ```

    If the count is ≥3 across the codebase: GAP-FILL the adapter (preferred — single source of truth):

    1. Extend `VcsWorkspace.add` signature in `sdk/src/vcs/types.ts` (line 165-173 region) to accept `branch?: { name: string; create: boolean }`.
    2. Implement in `sdk/src/vcs/backends/git.ts` `workspace.add` (around line 499): when `branch.create` is true, after `git worktree add` succeeds, run `execGit(input.path, ['checkout', '-b', input.branch.name])` (or pass to the existing `git worktree add -b <name>` form if cleaner).
    3. Implement in `sdk/src/vcs/backends/jj.ts` `workspace.add`: when `branch.create` is true, after `jj workspace add` succeeds, run `execJj(input.path, ['bookmark', 'create', input.branch.name, '-r', '@'])`.
    4. Add a paired test `sdk/src/vcs/__tests__/workspace-branch-create.test.ts` exercising both backends.
    5. Rewrite every `<!-- TODO: branch-create gap fill -->` site to use the new shape (single SDK call instead of two).

    If the count is ≤2: per-site rewrite using the existing two-step shape (`vcs.workspace.add` followed by `vcs.refs.bookmarks.create`), and remove the HTML comment.

    Target: `grep -rcP "<!-- TODO: branch-create gap fill -->" get-shit-done/workflows/ agents/` is 0 after this sub-task.

    Both sub-tasks land in the same commit (single git commit covering all 7 markdown files plus any SDK additions). The plan SUMMARY enumerates: count of verb-gap sweeps closed (and which strategy per verb); count of branch-create sites swept (and which strategy: adapter gap-fill vs per-site).
  </action>
  <acceptance_criteria>
    - `grep -rcP "# TODO\(05-05 sweep\)" get-shit-done/workflows/ agents/` is 0.
    - `grep -rcP "<!-- TODO: branch-create gap fill -->" get-shit-done/workflows/ agents/` is 0.
    - `node scripts/lint-vcs-no-raw-git.cjs` continues to exit 0.
    - `cd sdk && pnpm tsc --noEmit` exits 0 (in case the adapter gap-fill path is taken).
    - `cd sdk && pnpm vitest run` exits 0 (no regressions from any SDK additions).
    - If the branch-create adapter gap-fill landed: `grep -P "branch\\?: \\{ name: string; create: boolean \\}" sdk/src/vcs/types.ts | wc -l` is at least 1 AND the paired test file exists.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rqP "# TODO\(05-05 sweep\)" get-shit-done/workflows/ agents/ && ! grep -rqP "<!-- TODO: branch-create gap fill -->" get-shit-done/workflows/ agents/ && node scripts/lint-vcs-no-raw-git.cjs && cd sdk && pnpm tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <done>
    Both TODO surfaces are at 0 sites across all 7 modified markdown files. Verb gaps absorbed via SDK verb additions OR per-site rewrites OR documented git-only escape hatches. Branch-create gaps absorbed via adapter gap-fill (if ≥3 sites) or per-site rewrite (if ≤2). No regressions; lint guard green; all SDK tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Flip CI matrix to required-blocking (D-36 step 2); add CI-03 docs note; close PROMPT-03</name>
  <files>.github/workflows/test.yml, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.github/workflows/test.yml` (full file — 174 LOC; the matrix block at lines 56-71 is the only edit surface)
    - `.planning/intel/ci-jj-soak.md` (verify the Final Graduation date is filled in)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-CONTEXT.md` §"D-36 (CI graduation: fix-specific-flakes + N consecutive greens)"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`.github/workflows/test.yml` (CI graduation — lines 60-64)"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"Anti-Patterns to Avoid" — make sure no allowlist entries are introduced in this final flip
  </read_first>
  <action>
    Gated on Task 3 checkpoint = `approved`.

    **Part A — flip the CI matrix:**

    In `.github/workflows/test.yml` around lines 56-71, the current block reads:

    ```yaml
    test:
      runs-on: ${{ matrix.os }}
      timeout-minutes: 10

      # Phase 3 plan 03-07 (CI-01): the jj-colocated lane is allowed to fail —
      # CI-01 graduates to required-blocking in Phase 5 (per D-11). Phase 4
      # plan 01 (D-22) adds the jj-native lane with the same allow-failure
      # posture; both graduate together in Phase 5.
      continue-on-error: ${{ matrix.backend == 'jj-colocated' || matrix.backend == 'jj-native' }}

      strategy:
        fail-fast: false
        matrix:
          os: [ubuntu-latest]
          node-version: [22, 24]
          backend: [git, jj-colocated, jj-native]
    ```

    Replace with:

    ```yaml
    test:
      runs-on: ${{ matrix.os }}
      timeout-minutes: 10

      # CI-01 + CI-04 graduation event (Phase 5 plan 05-05 per D-36 step 2).
      # Both jj lanes (jj-colocated, jj-native) flipped to required-blocking after
      # a 10-consecutive-green nightly soak window (tracked in
      # .planning/intel/ci-jj-soak.md). The previous `continue-on-error` clause
      # is retired.
      #
      # CI-03 note: GitHub Actions workflows themselves (canary, release-sdk,
      # hotfix, branch-cleanup, auto-branch) stay on git per CI-03 —
      # GitHub *is* git. They are NOT jj-ported.
      continue-on-error: false

      strategy:
        fail-fast: false
        matrix:
          os: [ubuntu-latest]
          node-version: [22, 24]
          backend: [git, jj-colocated, jj-native]
    ```

    If the soak checkpoint resolved with the `approve with known-flake gate: <test-name>` outcome, instead of `continue-on-error: false`, add a conditional with explicit env-flag scoping:

    ```yaml
    # Soak approved with known-flake gate for <test-name>; the specific test
    # is gated by GSD_SKIP_<UPPER_CASE_NAME>=1 env in CI per D-36 discretion clause.
    continue-on-error: false
    env:
      GSD_SKIP_<UPPER_CASE_NAME>: '1'
    ```

    **Part B — update `.planning/intel/ci-jj-soak.md` Final Graduation section** with the actual flip commit hash (the executor will know this only after the commit lands — use the convention of inserting `(commit hash TBD — will be filled by Task 5 SUMMARY)` if running before commit).

    **Part C — update `.planning/REQUIREMENTS.md`** status entries:

    - `CI-03` row: `| CI-03 | Phase 5 | Pending |` → `| CI-03 | Phase 5 | Complete (Phase 5 plan 05-05) |`
    - `PROMPT-03` row: `| PROMPT-03 | Phase 5 | Pending |` → `| PROMPT-03 | Phase 5 | Complete (Phase 5 plan 05-05 — trust-installer closure per D-37; no per-runtime smoke matrix added) |`
    - Add `CI-01` and `CI-04` graduation notes to their existing Phase 3 / Phase 4 rows: append `; jj lanes graduated to required-blocking in Phase 5 plan 05-05 per D-36`.

    Add an update note to the REQUIREMENTS.md footer per the existing convention.

    **Part D — close PROMPT-03 (D-37 trust-installer) inline:** add a one-paragraph note to plan SUMMARY (Task 5 owns SUMMARY creation) stating: "PROMPT-03 closed without per-runtime smoke matrix addition (D-37). Source-of-truth canonical Claude markdown is processed by `bin/install.js` transform pipeline for 15+ runtimes. No regression observed in spot-check (planner's optional verification per D-37)."
  </action>
  <acceptance_criteria>
    - `grep -P "continue-on-error: false" .github/workflows/test.yml | wc -l` is at least 1.
    - `grep -P "continue-on-error: .*matrix.backend == 'jj-colocated'" .github/workflows/test.yml | wc -l` is 0 (the old conditional is gone).
    - `grep -P "GitHub \\*is\\* git" .github/workflows/test.yml | wc -l` is at least 1 (CI-03 docs note in place).
    - `grep -P "CI-01 \\+ CI-04 graduation event" .github/workflows/test.yml | wc -l` is at least 1.
    - `grep -P "CI-03 \\| Phase 5 \\| Complete" .planning/REQUIREMENTS.md | wc -l` is at least 1.
    - `grep -P "PROMPT-03 \\| Phase 5 \\| Complete" .planning/REQUIREMENTS.md | wc -l` is at least 1.
    - From repo root, `node -e "const yaml = require('js-yaml'); const fs = require('fs'); yaml.load(fs.readFileSync('.github/workflows/test.yml', 'utf8'));"` exits 0 (the YAML still parses). If `js-yaml` is not in repo, use any other YAML validator or skip this check after manual confirmation.
  </acceptance_criteria>
  <verify>
    <automated>grep -P "continue-on-error: false" .github/workflows/test.yml && grep -P "GitHub \*is\* git" .github/workflows/test.yml && grep -P "CI-03 \| Phase 5 \| Complete" .planning/REQUIREMENTS.md && grep -P "PROMPT-03 \| Phase 5 \| Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    CI matrix entries flipped from `continue-on-error: ${{ matrix.backend == 'jj-colocated' || matrix.backend == 'jj-native' }}` to `continue-on-error: false`; CI-03 docs note prominently in the workflow; REQUIREMENTS.md CI-01/03/04 + PROMPT-03 statuses marked Complete with traceable plan references; PROMPT-03 trust-installer closure note ready for SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow YAML → GitHub Actions runner | `.github/workflows/test.yml` controls what's required-blocking for every PR |
| soak tracker file → human read | `.planning/intel/ci-jj-soak.md` is the source-of-truth for the graduation decision |
| jj-test concurrency model → vitest scheduler | `describe.sequential` opt-in serializes within-suite tests; `Math.random()` suffix prevents tmpdir collision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.05-01 | Tampering | Required-blocking flip before flakes are actually fixed → all PRs fail intermittently | mitigate | Task-3 human checkpoint gates the flip on 10-consecutive-green soak (D-36 step 2); Pitfall 4 of 05-RESEARCH.md explicitly calls out this risk. Reset counter on any failure; restart the window. |
| T-05.05-02 | Tampering | `describe.sequential` change could mask a real race-condition bug (the bug serializes away rather than getting fixed) | accept | Phase 4 LEARNINGS already characterized the contention as test-harness contention (~50% wall-clock overhead in CI), not a production-side bug. The vcsTest fixture is the contended resource. Sequencing is appropriate. Documented in plan SUMMARY for future-reference. |
| T-05.05-03 | Repudiation | Soak tracker is manual-update → a human could enter false data | accept | This is a developer-internal tool; the source-of-truth is the GitHub Actions run history (queryable via `gh api`). If false data is entered, the next PR run will surface the actual state. |
| T-05.05-04 | Tampering | `Math.random()` for tmpdir suffix is not cryptographic and could theoretically collide | accept | 8-char base36 random = 36^8 ≈ 2.8 trillion combinations. Birthday-paradox collision probability across even 10000 parallel test invocations is negligible. Standard practice. |
| T-05.05-05 | Information Disclosure | MIGR-02 cosmetic sweep edits user-facing error strings → could leak internal terminology (e.g., "vcs.workspace.forget") to end users | accept | The error messages already mention `git worktree` internals; switching to `vcs.workspace.forget` doesn't degrade the UX. Documented in plan SUMMARY. |
</threat_model>

<verification>
- Phase 5 success criterion #4 (CI matrix graduates jj-backend from allow-failure to required-blocking + GitHub Actions stays on git): Task 4 lands the matrix flip + CI-03 docs note. ✓
- Phase 5 success criterion #5 (full v1 commitment holds: every command works on jj-only, no regression on git, no `.skip` accumulation): Task 1 keeps `.skip` count flat (no new skips introduced); plan-level test run confirms it.
- Phase-level final assertion: `cd sdk && pnpm vitest run` exits 0 AND skip count from `--reporter=json | jq '.numPendingTests'` matches the pre-Phase-5 baseline.
- Lint guard final: `node scripts/lint-vcs-no-raw-git.cjs` exits 0.
- The soak tracker is non-automated; human verifies before Task 4 lands (Task 3 checkpoint).
</verification>

<success_criteria>
- Seven flake-prone test files patched and passing 3/3 locally.
- Soak tracker exists with the canonical 10-green shape.
- CI matrix flipped to required-blocking after human verifies the soak passed (Task 3 checkpoint).
- CI-03 docs note (`GitHub *is* git`) prominent in the workflow YAML.
- MIGR-02 cosmetic sweep landed on 6 cjs files; REQUIREMENTS.md MIGR-02 row marked Complete.
- PROMPT-03 closed per D-37 (trust-installer; no smoke matrix added); status marked Complete in REQUIREMENTS.md.
- CI-01 / CI-04 graduation notes appended to their existing Phase 3 / Phase 4 rows.
- The phase-level 5 success criteria from ROADMAP Phase 5 are all addressable in 05-LEARNINGS.md based on the work that landed across plans 05-01..05-05.
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-05-SUMMARY.md` covering:
- Per-flake-file: which patterns (A / B / both) applied; pre-soak 3/3 local pass evidence.
- Soak window dates (start / end / final count); any reset events; the final graduation commit hash.
- MIGR-02 sweep stats: per-file line-count of error-string / comment changes.
- CI-03 docs note location in `.github/workflows/test.yml`.
- PROMPT-03 trust-installer closure paragraph verbatim per D-37.
- A "Phase 5 close" section asserting all 5 ROADMAP success criteria are met (success criterion #3 per the amended D-31 wording from plan 05-01).
- Updated REQUIREMENTS.md status counts (every Phase 5 ID's Complete status).

ALSO at phase close, create `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-LEARNINGS.md` covering:
- Phase 5 success criteria evidence (each of the 5 criteria + the plan that landed it).
- A "Hand-off to Phase 6" section listing: BROWN-01 / BROWN-02 still pending; sticky vcs.adapter flip; .planning SHA→change_id rewriter; the verb gaps deferred in plans 05-02/03 sweep that may surface in dogfood.
- Verbatim D-34 brownfield-coverage-gap prose.
- Format-migration tracker (D-19 inheritance from Phase 3 / Phase 4) — confirm zero new entries from Phase 5.
- Velocity / context-cost notes for STATE.md.
- Pre-existing failures still deferred (jj-test perf maintenance bucket — now partially addressed by flake fixes; document residual).
</output>
