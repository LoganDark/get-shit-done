---
phase: 05-command-translations-brownfield-validation-ci-hardening
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - get-shit-done/workflows/undo.md
  - get-shit-done/workflows/complete-milestone.md
  - get-shit-done/workflows/code-review.md
  - agents/gsd-code-fixer.md
  - agents/gsd-executor.md
  - sdk/src/vcs/__tests__/cmd-undo-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-pr-branch-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-hotfix-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-ship-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-complete-milestone-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-verify-work-jj.test.ts
autonomous: true
requirements:
  - CMD-06
  - CMD-07
  - CMD-08
  - CMD-09
  - CMD-11
  - PROMPT-01
  - PROMPT-02

must_haves:
  truths:
    - "`/gsd-undo` (CMD-06): markdown documents the destructive jj semantic shift explicitly (Pitfall 6); the new `gsd-sdk query revert` verb dispatches to `git revert --no-commit` on git and `jj abandon <change_id>` on jj"
    - "`/gsd-pr-branch` (CMD-07): the workflow uses a revset expression to filter `.planning/`-only commits and materializes via `jj duplicate` onto a new `gsd/pr/<id>` bookmark"
    - "`/gsd-hotfix` (CMD-08): the workflow roots at a historical change via `jj new <past-change-id>`, advances `gsd/hotfix/<id>` bookmark, and explicitly pushes"
    - "`/gsd-ship` (CMD-09): explicit `gsd-sdk query push --remote origin --bookmark gsd/release/<version>` (no auto-push); release tag is a bookmark on jj backend per REFS-06"
    - "`/gsd-complete-milestone` (CMD-04 spillover + CMD-11): the 36 raw-git mentions in complete-milestone.md route through `gsd-sdk query commit/branch-list/merge/push`"
    - "`/gsd-verify-work` (CMD-04 spillover): assertion verbs dispatch through `gsd-sdk query log/status/diff`"
    - "`/gsd-code-review` (CMD-11 surface): 11 mentions in code-review.md route through SDK queries; agent files `gsd-code-fixer.md` and `gsd-executor.md` follow same rewrite rule"
    - "Six new lifecycle CMD-* integration tests run against jj-colocated fixtures (CMD-06/07/08/09/11 + verify-work); revset behavior for CMD-07 is empirically verified inline"
    - "All rewrites are mechanical (UPSTREAM-03); no backend conditionals (D-33)"
  artifacts:
    - path: "get-shit-done/workflows/undo.md"
      provides: "VCS-agnostic /gsd-undo (CMD-06) with explicit jj-destructive-semantics prose"
      contains: "destructive on jj"
    - path: "get-shit-done/workflows/complete-milestone.md"
      provides: "VCS-agnostic /gsd-complete-milestone (CMD-04 spillover + CMD-11)"
      contains: "gsd-sdk query merge"
    - path: "get-shit-done/workflows/code-review.md"
      provides: "VCS-agnostic /gsd-code-review (CMD-11 surface)"
      contains: "gsd-sdk query log"
    - path: "agents/gsd-code-fixer.md"
      provides: "VCS-agnostic agent prompt (PROMPT-02)"
      contains: "gsd-sdk query"
    - path: "agents/gsd-executor.md"
      provides: "VCS-agnostic agent prompt (PROMPT-02)"
      contains: "gsd-sdk query"
    - path: "sdk/src/vcs/__tests__/cmd-undo-jj.test.ts"
      provides: "CMD-06 integration test asserting destructive jj abandon semantics"
      contains: "jj abandon"
    - path: "sdk/src/vcs/__tests__/cmd-pr-branch-jj.test.ts"
      provides: "CMD-07 integration test asserting revset filter + jj duplicate materialization"
      contains: "jj duplicate"
    - path: "sdk/src/vcs/__tests__/cmd-hotfix-jj.test.ts"
      provides: "CMD-08 integration test asserting jj new <past-change-id> + bookmark naming"
      contains: "gsd/hotfix/"
    - path: "sdk/src/vcs/__tests__/cmd-ship-jj.test.ts"
      provides: "CMD-09 integration test asserting explicit push via SDK query"
      contains: "gsd-sdk query push"
    - path: "sdk/src/vcs/__tests__/cmd-complete-milestone-jj.test.ts"
      provides: "CMD-04 + CMD-11 milestone-close integration test"
      contains: "gsd/release/"
    - path: "sdk/src/vcs/__tests__/cmd-verify-work-jj.test.ts"
      provides: "CMD-04 verify-work integration test"
      contains: "vcs.findConflicts"
  key_links:
    - from: "get-shit-done/workflows/undo.md (CMD-06 prose)"
      to: "sdk/src/query/revert.ts (verb added in 05-01)"
      via: "gsd-sdk query revert --no-commit <rev>"
      pattern: "gsd-sdk query revert"
    - from: "get-shit-done/workflows/complete-milestone.md (around 727-748)"
      to: "sdk/src/query/push.ts + gsd/release/<version> bookmark path"
      via: "gsd-sdk query push --remote origin --bookmark"
      pattern: "gsd-sdk query push.*gsd/release"
    - from: "agents/gsd-code-fixer.md (workspace cleanup blocks 223-260, 297-355)"
      to: "vcs.workspace.list/forget + vcs.refs.bookmarks.delete"
      via: "gsd-sdk query workspace + branch-list"
      pattern: "gsd-sdk query workspace|gsd-sdk query branch-list"
---

<objective>
Translate the lifecycle commands (/gsd-undo, /gsd-pr-branch, /gsd-hotfix, /gsd-ship, /gsd-verify-work, /gsd-complete-milestone) and the two heavy agent prompts (gsd-code-fixer.md, gsd-executor.md) to be VCS-agnostic per D-33, and add six new integration tests covering each lifecycle command. CMD-06 carries the most semantic weight: the jj `abandon` model is destructive (Pitfall 6 from 05-RESEARCH.md), and undo.md must document this shift explicitly rather than papering over it.

Purpose: Plan 05-02 covered the daily-driver hot path. This plan covers the rest of the upstream CMD surface plus the two agent files Phase 4 LEARNINGS identified as the highest-mention surface (`gsd-code-fixer.md` 37 mentions, `gsd-executor.md` 24 mentions). Six new lifecycle tests close the CMD-06..11 gate (modulo CMD-10 brownfield, owned by plan 05-04).

Output: Three rewritten workflow markdown files, two rewritten agent prompt files (mechanical-only — UPSTREAM-03), six new integration tests under `sdk/src/vcs/__tests__/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-CONTEXT.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-01-SUMMARY.md
@get-shit-done/workflows/undo.md
@get-shit-done/workflows/complete-milestone.md
@get-shit-done/workflows/code-review.md
@agents/gsd-code-fixer.md
@agents/gsd-executor.md
@sdk/src/vcs/__tests__/jj-hooks.test.ts
@sdk/src/vcs/__tests__/jj-octopus.test.ts
@sdk/src/vcs/__tests__/vcs-fixture.ts

<interfaces>
<!-- Per-command jj idiom reference from 05-RESEARCH.md "jj Idioms for New Command Translations (CMD-06..08)". -->

CMD-06 /gsd-undo translation table:
- `git revert --no-commit <HASH>` → `gsd-sdk query revert --no-commit <rev>`
  - Git backend: creates inverse-content commit (preserves history)
  - jj backend: `jj abandon <change_id>` — DESTRUCTIVE (op-log is the recovery path via `jj op restore`)
  - Pitfall 6 mitigation: rewritten undo.md MUST contain a prose paragraph naming this semantic shift and pointing to `jj op restore` as the jj-side recovery path

CMD-07 /gsd-pr-branch revset candidates (jj 0.41 syntax; planner picks from these):
- Strict planning-only exclusion: `~ files(glob:".planning/**")` — keeps any rev that touches at least one non-planning file
- Phase-range scoped: `(parents('phase-bookmark')..@) & ~ files(glob:".planning/**")` — same scope as the phase work
- **Verify empirically inline** — write the integration test against synthetic fixtures with mixed + planning-only commits and assert the materialized bookmark contains exactly the non-planning-only revs.

CMD-08 /gsd-hotfix:
- Root: `jj new <past-change-id>` where `<past-change-id>` is resolved from `gsd/release/<version>` bookmark
- Bookmark: `gsd/hotfix/<id>` where `<id>` is recommended as `YYYYMMDD-HHMM` (planner picks)
- Explicit push: `gsd-sdk query push --remote origin --bookmark gsd/hotfix/<id>`

CMD-09 /gsd-ship:
- jj backend: `vcs.refs.bookmarks.create('gsd/release/v1.0', '@')` + `vcs.push({ bookmark: 'gsd/release/v1.0' })` — REFS-06 (no annotated tags on jj)
- Git backend: `vcs.gitOnly.createAnnotatedTag('v1.0', message)` + `vcs.push({...})`
- The SDK query verb is `gsd-sdk query push --remote origin --bookmark <name>` (verb added in 05-01)

From 05-RESEARCH.md §"`agents/gsd-code-fixer.md` (37 mentions, 668 LOC)":
- Lines 223-260 workspace+branch cleanup → `vcs.workspace.list/forget` + `vcs.refs.bookmarks.delete`
- Lines 269-279, 297-301 workspace creation `git worktree add -b <branch>` → `vcs.workspace.add(path, { atRevision, branch: 'create' })`. If `branch: 'create'` shape is not in adapter (Open Q4 from 05-RESEARCH.md), the executor must add the field to the adapter signature in the same task before rewriting the consumer.
- Lines 77, 87, 608 `git checkout -- {file}` → `gsd-sdk query restore --files <file>` (verb added in 05-01)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite undo.md (CMD-06) + complete-milestone.md (CMD-04 spillover + CMD-11) + code-review.md (CMD-11 surface)</name>
  <files>get-shit-done/workflows/undo.md, get-shit-done/workflows/complete-milestone.md, get-shit-done/workflows/code-review.md</files>
  <read_first>
    - `get-shit-done/workflows/undo.md` (full file — 314 LOC; rewrite scope is 15 raw-git invocations per 05-RESEARCH.md inventory)
    - `get-shit-done/workflows/complete-milestone.md` (full file — 847 LOC; rewrite scope is 36 raw-git invocations)
    - `get-shit-done/workflows/code-review.md` (full file — 523 LOC; rewrite scope is 11 raw-git invocations)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`get-shit-done/workflows/undo.md` (15 mentions, 314 LOC)" + §"Pitfall 6: jj idiom mismatch in CMD-06 undo semantics"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`get-shit-done/workflows/complete-milestone.md` (36 mentions, 847 LOC)"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`get-shit-done/workflows/code-review.md` (11 mentions, 523 LOC)"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`get-shit-done/workflows/execute-phase.md` (and the 4 sibling workflow files)" for the mechanical-rewrite pattern
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md` for the rewrite pattern Plan 05-02 already established
  </read_first>
  <action>
    Apply the per-file rewrite tables from 05-RESEARCH.md inventory. Three sub-tasks:

    **Sub-task A — undo.md (CMD-06 — 15 sites):**

    Per 05-RESEARCH.md inventory:
    - Lines 53, 85-87, 97: `git log --oneline --no-merges` (with `--all` and grep filters) → `gsd-sdk query log --no-merges --grep "<pat>" --all`
    - Line 205: `git status --porcelain` → `gsd-sdk query status --porcelain`
    - Lines 217-228: `git revert --no-commit <HASH>` / `git revert --abort` / `git reset HEAD` / `git restore .` →
      - `git revert --no-commit <H>` → `gsd-sdk query revert --no-commit <H>` (verb added in 05-01)
      - `git revert --abort` → `gsd-sdk query revert --abort` (only relevant on git backend — verb errors clearly on jj since `jj abandon` is one-shot; document this in prose)
      - `git reset HEAD` → keep as a sweep TODO if no SDK verb exists, OR use the git-only `gsd-sdk query reset --mode mixed` shim from 05-01
    - Lines 249-259: `git commit -m "revert(...)"` → `gsd-sdk query commit "revert(...)" --files <files>`

    **MANDATORY Pitfall 6 prose insertion:** Add a new paragraph immediately before the section that describes the per-commit revert loop. Suggested location: just before line ~213 (the section that begins "For each commit to undo"). Exact wording (literal — paste into the file):

    ```markdown
    > **Backend semantic shift (CMD-06 / 05-RESEARCH Pitfall 6).** On the **git** backend, undo creates inverse-content commits and preserves history: a reverted commit stays in the log, and a follow-up `revert` of the revert restores the original content. On the **jj** backend, undo is **destructive**: `jj abandon <change_id>` removes the change from the visible history, and the operation log (`jj op log` / `jj op restore`) is the recovery path. This is jj's idiomatic model. If you need git-style preservation on jj, defer to `jj op restore` after the abandon, or use `jj duplicate <change_id> --destination <safe>` to copy the content to a bookmark before abandoning. The destructive jj model is intentional for v1; inverse-content primitive on jj is deferred to JJOP-01 v2.
    ```

    **Sub-task B — complete-milestone.md (CMD-04 spillover + CMD-11 — 36 sites):**

    Per 05-RESEARCH.md inventory:
    - Lines 172-176: `git log --oneline --grep` / `git diff --stat` / `git log --format` → `gsd-sdk query log` / `gsd-sdk query diff --name-status` / `gsd-sdk query log --format <s>`
    - Lines 505, 758: `git rm <path>` + `git commit -m` → `gsd-sdk query commit "..." --files <path>` (rm semantics: on git, `git add -A` after deleting works; on jj, `jj file untrack` is the equivalent — the SDK `vcs.commit` API handles both via path inclusion + WC state)
    - Lines 610-704 (the branch CRUD + merge block):
      - `git branch --list` → `gsd-sdk query branch-list`
      - `git branch --show-current` → `gsd-sdk query current-branch`
      - `git checkout <ref>` → (no checkout verb exists; sweep TODO with a `# TODO(05-05 sweep): add gsd-sdk query checkout verb (or workspace.switch shim)` comment)
      - `git merge --squash` / `git merge --no-ff --no-commit` → `gsd-sdk query merge <ref> --squash` / `gsd-sdk query merge <ref> --no-ff --no-commit` (verb added in 05-01; jj backend errors clearly per merge.ts)
      - `git reset HEAD .planning/` → `gsd-sdk query reset --ref HEAD --mode mixed -- .planning/` (git-only shim)
      - `git commit -m` → `gsd-sdk query commit`
      - `git branch -d/D` → `gsd-sdk query branch-delete <name>` (no such verb after 05-01; sweep TODO unless using existing `vcs.refs.bookmarks.delete` via a new shim — sweep TODO is the safer call)
    - Lines 727-730, 748: `git tag -a v[X.Y] -m` + `git push origin v[X.Y]` →
      - On git backend: keep `git tag -a` as a `vcs.gitOnly.createAnnotatedTag` site OR sweep-TODO for a `gsd-sdk query tag` shim
      - On jj backend (REFS-06): replace with `gsd-sdk query branch-list --create gsd/release/v[X.Y] --rev @` (no such verb) — sweep-TODO for a bookmark-create shim, OR use the existing tagging path under `vcs.gitOnly` with explicit prose noting jj uses bookmarks
      - `git push origin v[X.Y]` → `gsd-sdk query push --remote origin --bookmark v[X.Y]` (or `gsd/release/v[X.Y]` on jj — see CMD-09 context)

    **Sub-task C — code-review.md (CMD-11 surface — 11 sites):**

    Per 05-RESEARCH.md inventory:
    - Line 128: `git rev-parse --show-toplevel` → `gsd-sdk query head-ref --show-toplevel` (verb argv shape: `--show-toplevel` flag; if not present in head-ref.ts from 05-01, sweep TODO)
    - Lines 212-232: `git log --oneline --all --grep="${PADDED_PHASE}"` + `git rev-parse` + `git diff --name-only <BASE>..HEAD` → `gsd-sdk query log --all --grep --format oneline` + `gsd-sdk query head-ref` + `gsd-sdk query diff --name-only --range <BASE>..HEAD`
    - Line 329: same as line 212-232 pattern

    Apply the `# TODO(05-05 sweep): …` convention from plan 05-02 for any verb gaps. Mechanical-only rewrite (UPSTREAM-03). NO backend conditionals (D-33).
  </action>
  <acceptance_criteria>
    - `grep -P "destructive on jj|jj op restore|JJOP-01|destructive jj model" get-shit-done/workflows/undo.md | wc -l` is at least 1 (Pitfall 6 prose landed).
    - `grep -P "gsd-sdk query revert" get-shit-done/workflows/undo.md | wc -l` is at least 1.
    - `grep -v '^#' get-shit-done/workflows/undo.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+(commit|revert|reset|restore|stash|hook)\\b"` is at most 3 (allow ≤3 sweep TODOs).
    - `grep -P "gsd-sdk query (merge|branch-list|push)" get-shit-done/workflows/complete-milestone.md | wc -l` is at least 3 (per the three verbs the milestone-close flow uses).
    - `grep -v '^#' get-shit-done/workflows/complete-milestone.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+(commit|push|merge|branch|reset|tag)\\b"` is at most 8 (the milestone has more legitimate sweep-TODO sites — branch-delete, tag, etc. — that are out of scope of 05-01 verb additions).
    - `grep -P "gsd-sdk query log" get-shit-done/workflows/code-review.md | wc -l` is at least 2.
    - `grep -v '^#' get-shit-done/workflows/code-review.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+(commit|push|log|diff|rev-parse)\\b"` is at most 3.
    - `grep -cP "if vcs.adapter == 'jj'" get-shit-done/workflows/{undo,complete-milestone,code-review}.md` is 0 (D-33 anti-pattern guard).
    - Each `# TODO(05-05 sweep): …` count in the three files matches the SUMMARY's Deferred section.
  </acceptance_criteria>
  <verify>
    <automated>grep -P "destructive|jj op restore" get-shit-done/workflows/undo.md && grep -P "gsd-sdk query revert" get-shit-done/workflows/undo.md && grep -P "gsd-sdk query merge" get-shit-done/workflows/complete-milestone.md && grep -P "gsd-sdk query log" get-shit-done/workflows/code-review.md</automated>
  </verify>
  <done>
    undo.md carries the Pitfall 6 semantic-shift prose AND routes the CMD-06 revert loop through `gsd-sdk query revert`; complete-milestone.md routes merge/branch-list/push through SDK queries; code-review.md routes log/diff/head-ref through SDK queries. All three rewrites are mechanical (UPSTREAM-03); no backend conditionals (D-33); verb gaps are TODO-tagged for plan 05-05 sweep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite gsd-code-fixer.md + gsd-executor.md agent prompts (PROMPT-02)</name>
  <files>agents/gsd-code-fixer.md, agents/gsd-executor.md</files>
  <read_first>
    - `agents/gsd-code-fixer.md` (full file — 668 LOC; rewrite scope is 37 raw-git mentions per 05-RESEARCH.md inventory)
    - `agents/gsd-executor.md` (full file — 728 LOC; 24 mentions, 33 git-prefixed lines total)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`agents/gsd-code-fixer.md`" + §"`agents/gsd-executor.md`"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`agents/gsd-code-fixer.md`, `agents/gsd-executor.md`"
    - `.planning/phases/04-workspaces-octopus-structure-hooks/04-LEARNINGS.md` §"Hand-off to Phase 5" — replacement-target list for agent prompts (especially `git worktree` cleanup blocks; the `performJjReap` consumer pattern is the analog)
    - `sdk/src/vcs/types.ts` (`VcsWorkspace` shape) — needed to verify Open Q4 from 05-RESEARCH.md about `branch: 'create'` field
  </read_first>
  <action>
    Apply mechanical rewrites to both files using the analogs from 05-PATTERNS.md. No clean agent-prompt analog exists (per 05-PATTERNS.md "No Analog Found"); use the workflow-markdown shape (autonomous.md:252, complete-milestone.md:497) as the prose-shape reference.

    **agents/gsd-code-fixer.md (37 mentions):**

    1. Lines 77, 87, 608: `git checkout -- {file}` → `gsd-sdk query restore --files {file}` (verb added in 05-01; on jj, dispatches `jj restore --from @-`)
    2. Lines 223-260 workspace cleanup block: replace `git branch --show-current` / `git worktree list --porcelain` / `git worktree remove --force` / `git branch -D` with `gsd-sdk query current-branch` / `gsd-sdk query workspace --list` (use existing workspace query if available; sweep TODO otherwise) / via `vcs.workspace.forget` indirection — sweep TODO for the explicit branch-D verb
    3. Lines 269-279, 297-301: `git worktree add -b "$reviewfix_branch" "$wt" "$branch"` — **PRE-RESOLVED via Open Q4 (05-RESEARCH.md):** confirmed by direct read of `sdk/src/vcs/types.ts` `VcsWorkspace.add` signature (lines 165-173): current shape accepts `WorkspaceAdd = { path, baseRef?, name? }` and does NOT support `branch: { name: string; create: boolean }`. **Path B locked (sweep-TODO):** Do NOT gap-fill the adapter in this plan. Rewrite the consumer using today's shape (the closest substitute is `gsd-sdk query workspace --add <path> --at-revision <branch>` paired with a separate `vcs.refs.bookmarks.create` step), and tag the gap site with an HTML comment marker:

       ```bash
       # The original was:
       #   git worktree add -b "$reviewfix_branch" "$wt" "$branch"
       # Two-step replacement (today's adapter shape; branch-create field is gap-flagged below):
       <!-- TODO: branch-create gap fill — VcsWorkspace.add lacks branch: {name, create} per Open Q4 — plan 05-05 sweep -->
       gsd-sdk query workspace --add "$wt" --at-revision "$branch"
       gsd-sdk query branch-list --create "$reviewfix_branch" --rev "$wt"  # or equivalent post-add bookmark-create shim
       ```

       Every such site MUST carry the literal `<!-- TODO: branch-create gap fill -->` HTML comment so plan 05-05's branch-create-sweep task can grep-find them. Plan 05-05 Task 4 (added in the same revision pass) absorbs the cleanup — either by adapter gap-fill (if the pattern recurs across multiple consumers) or per-site rewrite using two existing SDK verbs.
    4. Lines 315-355: workspace merge-back + cleanup → `gsd-sdk query workspace` + `gsd-sdk query merge --ff-only` (verb added in 05-01; jj backend errors)
    5. Lines 421, 477, 587-589, 595, 629, 636, 661: prose-only mentions of git operations — rewrite the prose to reference the SDK verb name (no shell invocations involved; just terminology).

    **agents/gsd-executor.md (24 mentions, 33 git-prefixed lines):**

    1. Lines 354, 380, 629: `git log --oneline -5` / `git log --oneline --all` → `gsd-sdk query log --max-count 5` / `gsd-sdk query log --all`
    2. Lines 414-463: `git rev-parse --git-dir` / `git rev-parse --show-toplevel` / `git symbolic-ref --quiet HEAD` / `git rev-parse --abbrev-ref HEAD` → `gsd-sdk query head-ref` variants + `gsd-sdk query current-branch`. For `--git-dir` / `--show-toplevel`, sweep TODO if the head-ref verb doesn't expose those flags after 05-01 (verify by reading `sdk/src/query/head-ref.ts`).
    3. Lines 477-516: status + commit cycle → `gsd-sdk query status --short` + `gsd-sdk query commit "<msg>" --files <list>` (the highest-priority commit verb is exercised here)
    4. Lines 521-557: `git diff --diff-filter=D --name-only HEAD~1 HEAD` / `git checkout -- file` → `gsd-sdk query diff --diff-filter D --name-only --range HEAD~1..HEAD` / `gsd-sdk query restore --files <file>`. **Prohibition prose** (the `git clean -fd PROHIBITED` and `git push --force PROHIBITED` lines) STAYS AS-IS — they are not invocations, they are policy prose.

    Apply the `# TODO(05-05 sweep): …` convention everywhere a verb is missing. Both files MUST stay mechanical (UPSTREAM-03); the rewrite is shape-for-shape only.

    The adapter `branch: 'create'` Open-Q4 question is locked PATH B (sweep-TODO) by this plan's revision — see step 3 above. No adapter gap-fill in plan 05-03. The plan SUMMARY enumerates every `<!-- TODO: branch-create gap fill -->` site by file:line for plan 05-05 Task 4.
  </action>
  <acceptance_criteria>
    - `grep -P "gsd-sdk query (restore|workspace|branch-list|current-branch)" agents/gsd-code-fixer.md | wc -l` is at least 4 (broad SDK coverage proves the rewrite landed in the workspace-cleanup blocks).
    - `grep -v '^#' agents/gsd-code-fixer.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+(commit|push|reset|revert|checkout|worktree|branch|stash)\\b"` is at most 10 (allows ≤10 sweep TODOs given the file's complexity).
    - `grep -P "gsd-sdk query (commit|log|status|diff|head-ref|current-branch|restore)" agents/gsd-executor.md | wc -l` is at least 5.
    - `grep -v '^#' agents/gsd-executor.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+(commit|push|reset|revert|checkout|worktree|branch|stash|symbolic-ref|rev-parse|status|diff|log|add)\\b"` is at most 8.
    - **Prohibition prose preserved:** `grep -P "git clean -fd|git push --force.*PROHIBITED|never.*git clean" agents/gsd-executor.md | wc -l` is at least 1 (the safety policy stays as-is).
    - `grep -cP "if vcs.adapter == 'jj'" agents/{gsd-code-fixer,gsd-executor}.md` is 0.
    - `grep -P "<!-- TODO: branch-create gap fill -->" agents/gsd-code-fixer.md | wc -l` is at least 1 (every `git worktree add -b` site rewritten via Path B carries the HTML comment marker that plan 05-05 Task 4 will sweep).
    - `grep -P "branch\\?: \\{ name: string; create: boolean \\}" sdk/src/vcs/types.ts | wc -l` is 0 (adapter shape unchanged by this plan; gap-fill is Path B, not Path A).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query (restore|workspace|branch-list|current-branch)" agents/gsd-code-fixer.md && grep -P "gsd-sdk query (commit|log|status|diff|head-ref|current-branch|restore)" agents/gsd-executor.md && cd sdk && pnpm tsc --noEmit</automated>
  </verify>
  <done>
    Both agent files route their VCS mutations through `gsd-sdk query <verb>` calls (PROMPT-02); the prohibition prose against `git clean -fd` and `git push --force` is preserved verbatim; mechanical UPSTREAM-03 rewrite (no surrounding logic reshape); verb gaps tagged for plan 05-05 sweep with SUMMARY enumeration; every `git worktree add -b` site carries the `<!-- TODO: branch-create gap fill -->` HTML comment marker per Path B (no adapter gap-fill in this plan — locked by Open Q4 pre-resolution).
  </done>
</task>

<task type="auto">
  <name>Task 3: Six lifecycle CMD-* integration tests (CMD-06/07/08/09/11 + CMD-04 spillover)</name>
  <files>sdk/src/vcs/__tests__/cmd-undo-jj.test.ts, sdk/src/vcs/__tests__/cmd-pr-branch-jj.test.ts, sdk/src/vcs/__tests__/cmd-hotfix-jj.test.ts, sdk/src/vcs/__tests__/cmd-ship-jj.test.ts, sdk/src/vcs/__tests__/cmd-complete-milestone-jj.test.ts, sdk/src/vcs/__tests__/cmd-verify-work-jj.test.ts</files>
  <read_first>
    - `sdk/src/vcs/__tests__/jj-hooks.test.ts` (skip-gate analog)
    - `sdk/src/vcs/__tests__/jj-octopus.test.ts` (multi-step setup analog)
    - `sdk/src/vcs/__tests__/jj-push-fetch.test.ts` (push-test analog for cmd-ship)
    - `sdk/src/vcs/__tests__/jj-refs.test.ts` (bookmark CRUD analog for cmd-hotfix)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"jj Idioms for New Command Translations (CMD-06..08)"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`sdk/test/integration/cmd-*-jj.test.ts`"
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md` (Plan 05-02's test-shape analog — five files already exist using this pattern)
  </read_first>
  <action>
    Create six new integration test files under `sdk/src/vcs/__tests__/`. Same patterns as plan 05-02 task 3 (skip-gate, per-suite mkdtemp, afterAll cleanup, unique-prefix tmpdirs like `gsd-cmd-undo-jj-`, `gsd-cmd-pr-branch-jj-`, etc.).

    **`cmd-undo-jj.test.ts` (CMD-06):**
    - Test 1: Seed two commits via `vcs.commit({...})`; call `gsd-sdk query revert <change_id_of_first>` programmatically (resolve handler from the static catalog); assert on jj, the change is abandoned (no longer in `vcs.log({ maxCount: 10 }).stdout`).
    - Test 2: Assert the SEMANTIC SHIFT: post-abandon, the change is recoverable via `jj op log` (verify by inspecting `execSync('jj op log --no-pager', { cwd: dir }).toString()`). This is the Pitfall 6 invariant.
    - Test 3: Multiple-commit revert: revert two commits in sequence; assert both are abandoned and the working-copy state is preserved (no data loss).

    **`cmd-pr-branch-jj.test.ts` (CMD-07):**
    - Test 1: Seed three commits — (a) `.planning/foo.md` only, (b) `src/code.ts` + `.planning/bar.md` mixed, (c) `src/code.ts` only. Apply the revset filter (planner picks the exact form per CMD-07 idiom; recommend `~ files(glob:".planning/**")`) via direct `execSync` of `jj log -r '<revset>' -T 'json(self)'` and verify which commits survive.
    - Test 2: Use `jj duplicate <revset> --destination <new_bookmark_head>` to materialize the filtered subset onto `gsd/pr/test`; assert `vcs.refs.bookmarks.exists('gsd/pr/test')` returns true and the bookmark points at a change with the expected content (only commits (b) and (c) should be reachable).
    - Test 3: **Empirical revset verification** — write the filtered-rev test result to a known string and assert against it. This is the verify-empirically-inline instruction from 05-CONTEXT.md (planner's call, locked in this test).

    **`cmd-hotfix-jj.test.ts` (CMD-08):**
    - Test 1: Seed a "release" baseline by creating `gsd/release/v1.0` bookmark at the second commit. Root a hotfix via `execSync('jj new <change_id_of_release>', ...)`; assert `vcs.refs.head` resolves to a new change descended from the release bookmark.
    - Test 2: Squash a hotfix commit; advance `gsd/hotfix/<id>` bookmark (use `YYYYMMDD-HHMM` format for `<id>` per the recommendation). Assert `vcs.refs.bookmarks.exists('gsd/hotfix/<id>')` returns true.
    - Test 3: `gsd-sdk query push --remote origin --bookmark gsd/hotfix/<id>` would push the new bookmark; since we don't have a remote in the tmpdir fixture, assert the verb's argv shape is correct via mock or via `vcs.push({ bookmark, dryRun: true })` if the adapter supports dryRun (verify; if not, assert the push verb's call signature via direct invocation of `pushQuery` from `sdk/src/query/push.js`).

    **`cmd-ship-jj.test.ts` (CMD-09):**
    - Test 1: On jj backend, create `gsd/release/v1.0` bookmark via `vcs.refs.bookmarks.create('gsd/release/v1.0', vcs.refs.head)`; assert it exists.
    - Test 2: Invoke `pushQuery(['--remote', 'origin', '--bookmark', 'gsd/release/v1.0'], dir)` from `sdk/src/query/push.js`; assert the verb returns `{ data: { ok: …, exitCode: …, stdout, stderr } }` (handle the no-remote case gracefully — the verb propagates the jj push error).
    - Test 3: **No auto-push invariant** — verify that `vcs.commit({...})` on its own does NOT trigger push (no remote-side state change); only explicit pushQuery does.

    **`cmd-complete-milestone-jj.test.ts` (CMD-04 spillover + CMD-11):**
    - Test 1: Seed a phase-merge scenario per Phase 4 04-05 octopus pattern; advance `gsd/phase-foo` bookmark to the merge change. Assert `branchListQuery([], dir)` returns the bookmark in `data.bookmarks`.
    - Test 2: After milestone close, archive bookmarks under `gsd/milestone/v1.0/...`; assert the rename/copy via `vcs.refs.bookmarks.create`. (If the workflow uses `git tag`, the jj equivalent uses bookmarks per REFS-06 — verify the test asserts on bookmarks, not tags.)
    - Test 3: `gsd-sdk query commit "chore: archive v1.0 milestone files" --files .planning/MILESTONES.md` lands a final commit; assert it appears in `vcs.log({ maxCount: 1 })`.

    **`cmd-verify-work-jj.test.ts` (CMD-04 spillover):**
    - Test 1: Create a synthetic in-tree conflict (jj-specific — write conflict markers to a file and `jj squash` it). Call `vcs.findConflicts({ scope: 'all' })`; assert it returns the conflicted change_id (proves CONFLICT-01 reaches verify-work).
    - Test 2: After conflict resolution (`jj resolve --list -r <rev>` followed by writing clean content + squash), `vcs.findConflicts({ scope: 'all' })` returns empty (proves the verify-work invariant clears).
    - Test 3: `statusQuery(['--porcelain'], dir)` runs against the cleaned-up tree and returns `{ data: { ok: true, stdout: '' } }` (no status entries).

    Every file mirrors `jj-hooks.test.ts:32-39` + `jj-octopus.test.ts:39-60` patterns. Unique tmpdir prefix per file (`gsd-cmd-undo-jj-`, etc.) — this preempts the Phase 4 LEARNINGS tmpdir-contention flake category that plan 05-05 will fix in the existing tests.
  </action>
  <acceptance_criteria>
    - `ls sdk/src/vcs/__tests__/cmd-undo-jj.test.ts sdk/src/vcs/__tests__/cmd-pr-branch-jj.test.ts sdk/src/vcs/__tests__/cmd-hotfix-jj.test.ts sdk/src/vcs/__tests__/cmd-ship-jj.test.ts sdk/src/vcs/__tests__/cmd-complete-milestone-jj.test.ts sdk/src/vcs/__tests__/cmd-verify-work-jj.test.ts | wc -l` returns 6.
    - `grep -cP "describe\\.skipIf\\(!jjAvailable\\)" sdk/src/vcs/__tests__/cmd-{undo,pr-branch,hotfix,ship,complete-milestone,verify-work}-jj.test.ts` returns at least 6.
    - `grep -lP "jj abandon|jj op log|destructive" sdk/src/vcs/__tests__/cmd-undo-jj.test.ts | wc -l` is 1 (CMD-06 Pitfall 6 invariant asserted).
    - `grep -lP "jj duplicate" sdk/src/vcs/__tests__/cmd-pr-branch-jj.test.ts | wc -l` is 1.
    - `grep -lP "gsd/hotfix/" sdk/src/vcs/__tests__/cmd-hotfix-jj.test.ts | wc -l` is 1.
    - `grep -lP "gsd/release/" sdk/src/vcs/__tests__/cmd-ship-jj.test.ts | wc -l` is 1.
    - `grep -lP "vcs\\.findConflicts" sdk/src/vcs/__tests__/cmd-verify-work-jj.test.ts | wc -l` is 1.
    - `grep -cP "mkdtempSync\\(join\\(tmpdir\\(\\), 'gsd-cmd-(undo|pr-branch|hotfix|ship|complete-milestone|verify-work)-jj-'" sdk/src/vcs/__tests__/cmd-{undo,pr-branch,hotfix,ship,complete-milestone,verify-work}-jj.test.ts` returns at least 6.
    - `cd sdk && pnpm tsc --noEmit` exits 0.
    - `cd sdk && pnpm vitest run sdk/src/vcs/__tests__/cmd-undo-jj.test.ts sdk/src/vcs/__tests__/cmd-pr-branch-jj.test.ts sdk/src/vcs/__tests__/cmd-hotfix-jj.test.ts sdk/src/vcs/__tests__/cmd-ship-jj.test.ts sdk/src/vcs/__tests__/cmd-complete-milestone-jj.test.ts sdk/src/vcs/__tests__/cmd-verify-work-jj.test.ts` exits 0 under jj 0.41.
  </acceptance_criteria>
  <verify>
    <automated>cd sdk && pnpm tsc --noEmit && pnpm vitest run sdk/src/vcs/__tests__/cmd-undo-jj.test.ts sdk/src/vcs/__tests__/cmd-pr-branch-jj.test.ts sdk/src/vcs/__tests__/cmd-hotfix-jj.test.ts sdk/src/vcs/__tests__/cmd-ship-jj.test.ts sdk/src/vcs/__tests__/cmd-complete-milestone-jj.test.ts sdk/src/vcs/__tests__/cmd-verify-work-jj.test.ts</automated>
  </verify>
  <done>
    Six lifecycle CMD-* integration tests exist, gate on `jj --version`, each exercises its translation surface, each uses a unique tmpdir prefix (preempting flake category from Phase 4). All six pass under jj 0.41. CMD-06 specifically asserts the Pitfall 6 destructive semantics invariant via `jj op log` recoverability check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown prompt → shell | LLM-generated `gsd-sdk query <verb> <args>` invocations with interpolated `${...}` strings |
| agent prompt → tool invocation | LLM-generated shell commands per the rewritten prompt |
| revset string → jj binary (CMD-07) | revset expression injected into `jj log -r '<revset>'` argv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.03-01 | Tampering | CMD-06 destructive jj abandon could surprise users → data perceived as lost | mitigate | Explicit Pitfall 6 prose paragraph in undo.md naming the semantic shift; `jj op restore` documented as recovery path. |
| T-05.03-02 | Tampering | CMD-08 hotfix bookmark name `gsd/hotfix/<id>` constructed from user-controlled `<id>` | mitigate | `pushQuery` validates `--bookmark` arg via `validateRefname` (added in plan 05-01); bookmark create routes through `vcs.refs.bookmarks.create` which also validates. Workflow markdown documents the YYYYMMDD-HHMM format recommendation. |
| T-05.03-03 | Tampering | CMD-07 revset expression `~ files(glob:".planning/**")` constructed inline; if user-controlled, could include unsafe revset operators | mitigate | The revset is HARDCODED in workflow markdown per the locked strategy; no user-controlled interpolation into the revset string. (If a future change parameterizes the revset, that change must add revset validation.) |
| T-05.03-04 | Information Disclosure | Test fixtures for cmd-pr-branch / cmd-ship create local-only tmpdir bookmarks; no remote interaction | accept | Tests run in isolated tmpdirs; no remote credentials reachable; `push` invocations against a non-existent remote return error cleanly without leaking. |
| T-05.03-05 | Repudiation | Agent prompts (gsd-code-fixer, gsd-executor) carry safety prose like "git clean -fd PROHIBITED" — rewrite could accidentally strip it | mitigate | Acceptance criterion: `grep -P "git clean -fd.*PROHIBITED|never.*git clean" agents/gsd-executor.md | wc -l` is at least 1. Spot-checked at task-3 done time. |
</threat_model>

<verification>
- Phase 5 success criterion #1: CMD-06..11 contribute the remaining 6 commands' integration tests (combined with 05-02's 5 = 11/11 CMD-* gates met, with CMD-10 brownfield owned by plan 05-04).
- Phase 5 success criterion #2: undo.md (15 hits), complete-milestone.md (36 hits), code-review.md (11 hits), gsd-code-fixer.md (37 hits), gsd-executor.md (24 hits) — combined 123/227 hot-file rewrites. Plus plan 05-02's 104/227 = full hot-file coverage.
- Local: `cd sdk && pnpm tsc --noEmit && pnpm vitest run --reporter=default sdk/src/vcs/__tests__/cmd-{undo,pr-branch,hotfix,ship,complete-milestone,verify-work}-jj.test.ts` passes.
- Lint guard: `node scripts/lint-vcs-no-raw-git.cjs` exits 0.
</verification>

<success_criteria>
- undo.md carries the Pitfall 6 destructive-jj-semantics prose paragraph.
- All three rewritten workflows + both agent prompts have at least 1 `gsd-sdk query <verb>` invocation per the rewrite inventory.
- Six new lifecycle CMD-* tests exist, are gated by jj availability, and pass under jj 0.41.
- Prohibition prose in `gsd-executor.md` is preserved.
- No backend conditionals introduced (D-33 anti-pattern guard).
- Any verb gaps appear in `# TODO(05-05 sweep): …` form AND in the plan SUMMARY's Deferred section.
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-03-SUMMARY.md` covering:
- Per-file rewrite stats (before-mention-count → after-mention-count for undo.md / complete-milestone.md / code-review.md / gsd-code-fixer.md / gsd-executor.md).
- Pitfall 6 prose insertion location (line numbers) in undo.md.
- A complete "Deferred to plan 05-05 sweep" list with file:line + missing verb name.
- Six CMD-* test descriptions (one paragraph per file: what it asserts, which jj idiom it exercises).
- A "Hand-off to plan 05-04" note: brownfield plan inherits the same rewrite patterns for `/gsd-resume-work` / `/gsd-pause-work` / `/gsd-import` / `/gsd-ingest-docs` / `/gsd-map-codebase`.
- Per-file enumeration of every `<!-- TODO: branch-create gap fill -->` HTML comment site (file:line) — hand-off to plan 05-05 Task 4. Path B locked: adapter shape unchanged in this plan.
</output>
