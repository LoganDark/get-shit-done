---
phase: 05-command-translations-brownfield-validation-ci-hardening
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/quick.md
  - sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts
  - sdk/src/vcs/__tests__/cmd-quick-jj.test.ts
autonomous: true
requirements:
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-04
  - CMD-05
  - PROMPT-01

must_haves:
  truths:
    - "`/gsd-new-project` (CMD-01): markdown invokes only `gsd-sdk query <verb>` calls for VCS mutations; no raw `git <verb>` or `jj <verb>` shells remain in the daily-driver portion of the workflow"
    - "`/gsd-plan-phase` (CMD-02): no raw git in workflow; the lazy octopus structure is invoked via the existing Phase 4 helpers, not direct jj shells"
    - "`/gsd-execute-phase` (CMD-03): the `git hook run pre-commit` invocation at execute-phase.md:682-690 is replaced with `gsd-sdk query hooks.fire pre-commit --cwd .`; the worktree-cleanup block (705-862) uses `gsd-sdk query worktree-list` / `vcs.workspace.*` patterns instead of raw `git worktree`"
    - "`/gsd-discuss-phase` (CMD-04 partial — verify-work and complete-milestone fall to plan 05-03): any raw-git invocations in the discuss workflow are dispatched through SDK"
    - "`/gsd-quick` (CMD-05): the commit cycle at quick.md:639-651 routes through `gsd-sdk query commit`; the worktree-cleanup block (774-864) follows the same pattern as execute-phase.md"
    - "Five new integration tests run end-to-end against jj-colocated fixtures, asserting each daily-driver command's translation correctness; tests skip cleanly when `jj --version` is unavailable"
    - "Each rewrite is mechanical (UPSTREAM-03): no surrounding bash-logic reshape, no prose-improvement edits, no section reorders in the same commit"
    - "Phase filename convention is padded form '05-NN-…' (per project memory); never unpadded '5.N-…'."
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "VCS-agnostic execute-phase workflow (PROMPT-01 — CMD-01/02/03 surface)"
      contains: "gsd-sdk query hooks.fire pre-commit"
    - path: "get-shit-done/workflows/quick.md"
      provides: "VCS-agnostic /gsd-quick workflow (PROMPT-01 — CMD-05 surface)"
      contains: "gsd-sdk query commit"
    - path: "sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts"
      provides: "CMD-01 integration test"
      contains: "/gsd-new-project"
    - path: "sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts"
      provides: "CMD-02 integration test"
      contains: "createPhaseStructure"
    - path: "sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts"
      provides: "CMD-03 integration test"
      contains: "createSubagentSlot"
    - path: "sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts"
      provides: "CMD-04 partial integration test (discuss only; complete-milestone+verify-work in plan 05-03)"
      contains: "discuss"
    - path: "sdk/src/vcs/__tests__/cmd-quick-jj.test.ts"
      provides: "CMD-05 integration test"
      contains: "single squash"
  key_links:
    - from: "get-shit-done/workflows/execute-phase.md (around line 682-690)"
      to: "sdk/src/query/hooks.ts fireHookQuery"
      via: "gsd-sdk query hooks.fire pre-commit --cwd ."
      pattern: "gsd-sdk query hooks\\.fire pre-commit"
    - from: "get-shit-done/workflows/quick.md (around line 639-651)"
      to: "sdk/src/query/commit.ts"
      via: "gsd-sdk query commit --message <s> --files <list>"
      pattern: "gsd-sdk query commit"
    - from: "sdk/src/vcs/__tests__/cmd-*-jj.test.ts (all 5)"
      to: "jj 0.41 binary + tmpdir fixture + Phase 4 octopus helpers"
      via: "execSync('jj …') in test setup; vcs.* in assertions"
      pattern: "describe\\.skipIf\\(!jjAvailable\\)"
---

<objective>
Rewrite the daily-driver workflow markdown to be VCS-agnostic (D-33) and add integration tests proving each command runs end-to-end on jj. The two hot files in scope: `execute-phase.md` (58 git mentions; primary A3-hand-off target at lines 682-690) and `quick.md` (46 git mentions; primary commit-cycle target at lines 639-651). Five new integration tests gate CMD-01..05.

Purpose: Plan 05-01 added the SDK verb substrate; this plan converts the markdown that calls it. The hand-off from Phase 4 LEARNINGS is concrete: `execute-phase.md:682-690` is the bridge target that proves the entire D-33 + D-32 hand-off lands clean.

Output: Two rewritten workflow markdown files (mechanical shape-for-shape per UPSTREAM-03), five new integration test files mirroring `jj-hooks.test.ts` / `jj-octopus.test.ts` shapes, no changes to surrounding bash logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-CONTEXT.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md
@.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-01-SUMMARY.md
@get-shit-done/workflows/execute-phase.md
@get-shit-done/workflows/quick.md
@get-shit-done/workflows/autonomous.md
@get-shit-done/workflows/complete-milestone.md
@sdk/src/vcs/__tests__/jj-hooks.test.ts
@sdk/src/vcs/__tests__/jj-octopus.test.ts
@sdk/src/vcs/__tests__/vcs-fixture.ts

<interfaces>
<!-- Canonical agnostic-prose-+-SDK-call pattern (in-repo analogs the rewrite mirrors). -->

From get-shit-done/workflows/autonomous.md:251-253 (cleanest in-repo example — Analog 2):
```bash
Commit the minimal context:

gsd-sdk query commit "docs(${PADDED_PHASE}): auto-generated context (discuss skipped)" --files "${phase_dir}/${padded_phase}-CONTEXT.md"
```

From get-shit-done/workflows/complete-milestone.md:497 (multi-path --files variant — Analog 3):
```bash
gsd-sdk query commit "chore: archive v[X.Y] milestone files" --files .planning/milestones/v[X.Y]-ROADMAP.md .planning/milestones/v[X.Y]-REQUIREMENTS.md
```

From get-shit-done/workflows/execute-phase.md:282 (in-file existing pattern — Analog 1):
```bash
gsd-sdk query state.begin-phase --phase "${PHASE_NUMBER}" --name "${PHASE_NAME}" --plans "${PLAN_COUNT}"
```

Rewrite rule (D-33) for every site in execute-phase.md + quick.md:
- `git hook run pre-commit` → `gsd-sdk query hooks.fire pre-commit --cwd .` (verb exists from Phase 4 plan 04-06)
- `git commit -m "<msg>" -- <files>` → `gsd-sdk query commit "<msg>" --files <files>` (verb exists in sdk/src/query/commit.ts)
- `git push origin <ref>` → `gsd-sdk query push --remote origin --bookmark <ref>` (verb added in plan 05-01)
- `git log --oneline --grep="<pat>"` → `gsd-sdk query log --grep "<pat>" --format oneline` (verb added in plan 05-01)
- `git status --porcelain` → `gsd-sdk query status --porcelain` (verb added in plan 05-01)
- `git diff --quiet HEAD` → `gsd-sdk query diff --quiet --range HEAD` (verb added in plan 05-01)
- `git worktree list --porcelain` → `gsd-sdk query worktree-list` (verb is `vcs.workspace.list()` exposed via existing workspace query; if a thin shim is needed, this plan must add it; otherwise use the existing `gsd-sdk query workspace` form)
- `git rev-parse HEAD` → `gsd-sdk query head-ref` (verb added in plan 05-01)
- `git rev-parse --abbrev-ref HEAD` / `git symbolic-ref --quiet HEAD` → `gsd-sdk query current-branch` (verb added in plan 05-01)
- `git reset --hard <rev>` → `gsd-sdk query reset --ref <rev> --mode hard` (verb added in plan 05-01; git-only escape — jj backend errors clearly)

Anti-pattern (D-33): never introduce `if vcs.adapter == 'jj'; then … else …; fi` blocks. Shape stays shell-form-for-shell-form.

From sdk/src/vcs/__tests__/jj-hooks.test.ts:32-39 (skip-gate analog) and jj-octopus.test.ts:39-60 (setup/teardown analog):
```typescript
let jjAvailable = false;
try { execSync('jj --version', { stdio: 'pipe' }); jjAvailable = true; } catch {}

describe.skipIf(!jjAvailable)('CMD-XX: …', () => {
  let dir: string;
  let vcs: ReturnType<typeof createVcsAdapter>;
  beforeAll(() => {
    dir = mkdtempSync(join(tmpdir(), 'gsd-cmd-<name>-'));
    execSync('jj git init --colocate', { cwd: dir, stdio: 'pipe' });
    execSync('jj config set --repo user.email "test@test.com"', { cwd: dir, stdio: 'pipe' });
    execSync('jj config set --repo user.name "Test"', { cwd: dir, stdio: 'pipe' });
    vcs = createVcsAdapter(dir, { kind: 'jj' });
  });
  afterAll(() => { if (dir) rmSync(dir, { recursive: true, force: true }); });
  // ... test cases
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite execute-phase.md to be VCS-agnostic (CMD-01/02/03 surface)</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <read_first>
    - `get-shit-done/workflows/execute-phase.md` (full file — 1772 LOC; rewrite scope is 58 raw-git invocations per 05-RESEARCH.md Consumer Call-Site Inventory table)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`get-shit-done/workflows/execute-phase.md` (58 mentions, 1772 LOC)" for the full per-line inventory with line numbers and SDK substitutes
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`get-shit-done/workflows/execute-phase.md`" for the mechanical-rewrite pattern and the four analog excerpts (autonomous.md:252, complete-milestone.md:497, code-review-fix.md:366, execute-phase.md:282)
    - `get-shit-done/workflows/autonomous.md:251-253` (the cleanest in-repo SDK-query commit example)
    - `.planning/phases/04-workspaces-octopus-structure-hooks/04-LEARNINGS.md` §"Hand-off to Phase 5" — replacement-target list including the literal A3 hand-off at execute-phase.md:682-728
  </read_first>
  <action>
    Apply the per-line rewrite table from 05-RESEARCH.md §"`get-shit-done/workflows/execute-phase.md` (58 mentions, 1772 LOC)" to every site listed. Mechanical shape-for-shape only — no prose improvements, no section reorders, no surrounding bash logic refactor (UPSTREAM-03; Pitfall 1).

    Key sites (line numbers approximate — verify by grep before edit):

    1. Line 154 / 668 / 675: `git log --oneline --grep="..."` → `gsd-sdk query log --format oneline --grep "..."`
    2. Lines 248-267: `git symbolic-ref` / `git switch` / `git fetch` / `git checkout -b` block — these are the orchestrator's bookmark-switch path. Replace each invocation with the corresponding new verb (`head-ref` / `current-branch` / there's no `switch` verb yet — if the markdown literally needs branch-switching, replace `git switch <name>` with a `gsd-sdk query workspace --set-bookmark <name>` if the adapter exposes it, otherwise document this as a P5 sweep item).

       **If a specific line requires a verb that doesn't exist in the catalog after 05-01:** Do NOT add the verb in this plan (out of scope). Instead, leave the original shell line in place, prepend a `# TODO(05-05 sweep): add SDK verb for <verb>` comment, and add the site to the plan SUMMARY's "Deferred to P5 sweep" list.
    3. Line 352, 377: `git diff --quiet HEAD` / `git status` / `git diff` → `gsd-sdk query diff --quiet --range HEAD` / `gsd-sdk query status` / `gsd-sdk query diff`.
    4. Lines 496, 549-552: `git rev-parse HEAD` / `git merge-base` / `git reset --hard` → `gsd-sdk query head-ref` / (no merge-base verb — defer) / `gsd-sdk query reset --ref <rev> --mode hard`.
    5. Lines 539-545: `git symbolic-ref --quiet HEAD` / `git rev-parse --abbrev-ref HEAD` → `gsd-sdk query current-branch`.
    6. **Lines 682-690 (A3 hand-off — the headline rewrite):**

       BEFORE:
       ```bash
       STASHED=false
       if (! git diff --quiet || ! git diff --cached --quiet) && git stash push -u -m "gsd-post-wave-hook-$$" >/dev/null 2>&1; then STASHED=true; fi
       git hook run pre-commit 2>&1 || echo "⚠ Pre-commit hooks failed — review before continuing"
       [ "$STASHED" = "true" ] && (git stash pop >/dev/null 2>&1 || echo "⚠ Could not pop gsd-post-wave-hook stash — recover manually")
       ```

       AFTER (per 05-PATTERNS.md):
       ```bash
       # Pre-commit hook (cross-backend; jj uses .githooks/pre-commit, git uses .git/hooks/pre-commit).
       # The adapter handles stashing on git side; on jj the working-copy auto-snapshot makes stashing unnecessary.
       gsd-sdk query hooks.fire pre-commit --cwd . 2>&1 \
         || echo "⚠ Pre-commit hooks failed — review before continuing"
       ```

       (The `git stash` lines may be removed if the surrounding prose accepts dropping them; otherwise prepend a `# TODO(05-05 sweep): add gsd-sdk query stash verb` comment and keep them. Planner discretion at execute time.)
    7. Lines 705-862 (worktree cleanup block, ~150 LOC): replace each `git worktree list --porcelain` with `gsd-sdk query worktree-list` (use `vcs.workspace.list` adapter; if no shim verb exists, add a `# TODO(05-05 sweep)` comment per the rule above). Replace `git worktree remove` / `git worktree prune` with the corresponding workspace verbs. The block is mechanical-only: do not reshape the surrounding bash loop structure.
    8. Line 907-963: `git diff --quiet` / `git log --oneline --all --grep=` → as above.

    For lines NOT in the inventory table that contain `git` strings (e.g., prose like "the git commit happens here"), leave them alone — those are docs, not invocations. Use grep `grep -nP "^\\s*git\\s+(commit|push|reset|revert|stash|status|diff|log|rev-parse|symbolic-ref|merge-base|reset|branch|switch|fetch|hook|worktree|add)" get-shit-done/workflows/execute-phase.md` to confirm zero raw invocations remain post-rewrite (modulo TODO-tagged sweep items).

    Do NOT touch quick.md in this task.
  </action>
  <acceptance_criteria>
    - `grep -P "gsd-sdk query hooks\\.fire pre-commit --cwd ." get-shit-done/workflows/execute-phase.md | wc -l` is at least 1.
    - `grep -P "git hook run pre-commit" get-shit-done/workflows/execute-phase.md | wc -l` is 0.
    - `grep -v '^#' get-shit-done/workflows/execute-phase.md | grep -v '^\s*
    - `grep -cP "# TODO\\(05-05 sweep\\):" get-shit-done/workflows/execute-phase.md` equals the count of deferred items (must match the SUMMARY's deferred list exactly).
    - `git diff --stat get-shit-done/workflows/execute-phase.md` shows only the rewrite-shape change set (no whole-section reorders — keep diff under ~200 modified lines for a clean reviewable rewrite).
    - From repo root: `bin/install.js --dry-run` (or equivalent installer dry-run command) does not error on the rewritten markdown (Pitfall 2; trust-installer doesn't break).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query hooks\.fire pre-commit --cwd \." get-shit-done/workflows/execute-phase.md && ! grep -P "^[^#]*\bgit hook run pre-commit" get-shit-done/workflows/execute-phase.md</automated>
  </verify>
  <done>
    execute-phase.md no longer contains `git hook run pre-commit` (replaced by `gsd-sdk query hooks.fire pre-commit --cwd .` per D-32 hand-off); the 58 raw-git-invocation surface from 05-RESEARCH.md inventory is rewritten to SDK queries OR explicitly TODO-tagged for plan 05-05 sweep; the rewrite is mechanical (UPSTREAM-03 preserved); no `if jj: …` backend conditionals appear (D-33 anti-pattern guard).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite quick.md to be VCS-agnostic (CMD-05 surface)</name>
  <files>get-shit-done/workflows/quick.md</files>
  <read_first>
    - `get-shit-done/workflows/quick.md` (full file — 1121 LOC; rewrite scope is 46 raw-git invocations per 05-RESEARCH.md inventory)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`get-shit-done/workflows/quick.md` (46 mentions, 1121 LOC)" for the per-line inventory
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` (same analogs as Task 1)
    - `get-shit-done/workflows/autonomous.md:251-253` (the commit-shape analog)
  </read_first>
  <action>
    Mirror the Task-1 rewrite approach for `quick.md`. Headline sites (per 05-RESEARCH.md Consumer Call-Site Inventory):

    1. Line 165: `git config --file .gitmodules` — read; keep inline (it's `.gitmodules` parsing, git-only by nature) OR add a sweep TODO.
    2. Lines 191-226: bookmark/switch block — same treatment as execute-phase.md Task-1 step 2.
    3. **Lines 639-651 (the CMD-05 single-squash headline target):**

       BEFORE (per 05-RESEARCH.md):
       ```bash
       git add ${PLAN}
       git diff --cached --quiet && echo "no changes" && exit 0
       git commit [--no-verify] -m "docs(${quick_id}): ${SUMMARY}" -- "${QUICK_DIR}/${quick_id}-PLAN.md"
       ```

       AFTER:
       ```bash
       # CMD-05: single squash on orchestrator @ (no phase setup, no workspace, no octopus).
       # Adapter handles git-add ↔ jj-squash translation; the SDK query commit verb
       # routes through vcs.commit({files, message, noVerify}).
       gsd-sdk query commit "docs(${quick_id}): ${SUMMARY}" --files "${QUICK_DIR}/${quick_id}-PLAN.md"
       # (--no-verify is a flag forwarded to vcs.commit when present.)
       ```

       Per 05-RESEARCH.md the `gsd-sdk query commit` argv shape is positional message + `--files <list>`. The existing `commit.ts` already accepts this shape (verified per CONTEXT D-35 / Open Q1 of 05-RESEARCH.md). If the executor finds at runtime that `commit.ts` lacks the `--no-verify` flag, the executor MUST add it to `commit.ts` argv-scan loop in the same task (~5-line edit) and add a regression test asserting `--no-verify` parses correctly.
    4. Lines 665, 681-699: `git rev-parse HEAD` / `git symbolic-ref` / `git merge-base` / `git reset --hard` → SDK substitutes as in Task 1.
    5. Lines 774-864: worktree cleanup block — same mechanical pattern as execute-phase.md Task-1 step 7.
    6. Lines 892-904, 1054-1063: `git log --oneline --grep` / `git rev-parse --short HEAD` / `git add ${file_list}` → `gsd-sdk query log` / `gsd-sdk query head-ref --short` / `gsd-sdk query commit --files <list>`.

    Same TODO(05-05 sweep) convention for any verb the catalog doesn't yet expose.
  </action>
  <acceptance_criteria>
    - `grep -P "gsd-sdk query commit " get-shit-done/workflows/quick.md | wc -l` is at least 1 (the CMD-05 headline rewrite).
    - `grep -v '^#' get-shit-done/workflows/quick.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+commit\\b"` is 0.
    - `grep -v '^#' get-shit-done/workflows/quick.md | grep -v '^\s*
    - `grep -cP "if vcs.adapter == 'jj'" get-shit-done/workflows/quick.md` is 0 (D-33 anti-pattern guard).
    - `grep -cP "if .* jj.*then" get-shit-done/workflows/quick.md` is 0 (no shell-form backend conditionals either).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query commit " get-shit-done/workflows/quick.md && ! grep -vE '^[#"]|gsd-sdk' get-shit-done/workflows/quick.md | grep -P "^\s*git\s+commit\b"</automated>
  </verify>
  <done>
    quick.md CMD-05 commit cycle routes through `gsd-sdk query commit`; the broader 46-site rewrite is mechanical-only (UPSTREAM-03); no backend conditionals (D-33); any verb gaps are TODO-tagged for plan 05-05 sweep and listed in the plan SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 5 CMD integration tests (CMD-01..05) against jj-colocated fixture</name>
  <files>sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts, sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</files>
  <read_first>
    - `sdk/src/vcs/__tests__/jj-hooks.test.ts` (skip-gate + setup pattern: lines 32-39, 60-81, 83-96 — the canonical CMD-* test analog per 05-PATTERNS.md)
    - `sdk/src/vcs/__tests__/jj-octopus.test.ts` (multi-step setup pattern: lines 1-60)
    - `sdk/src/vcs/__tests__/vcs-fixture.ts:31-77` (initJjRepo / makeBackendFixture factory)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`sdk/test/integration/cmd-*-jj.test.ts` (11 new files — CMD-01..11)" and §"Test Suite Skip-Gate" + §"Tmpdir Lifecycle"
  </read_first>
  <action>
    Create five new integration test files under `sdk/src/vcs/__tests__/`. Each file:

    1. Mirrors `jj-hooks.test.ts:32-39` skip-gate pattern (try `jj --version`; describe.skipIf(!jjAvailable)).
    2. Mirrors `jj-octopus.test.ts:39-60` setup pattern (mkdtempSync + `jj git init --colocate` + config user.email/name).
    3. Includes per-describe `afterAll` cleanup with `rmSync(dir, { recursive: true, force: true })`.

    Per-file scope:

    - **`cmd-new-project-jj.test.ts` (CMD-01):**
      - Test 1: `createVcsAdapter(emptyTmpDir, { kind: 'jj' })` constructs successfully when only `.jj` exists; assert `vcs` is defined and `vcs.commit` is a function.
      - Test 2: When `.git` is also present (colocated init via `jj git init --colocate`), the adapter still constructs in jj mode if the project's `vcs.adapter` config is explicitly set to `jj`; assert backend selection.
      - Test 3: `vcs.commit({ message: 'init', files: [] })` succeeds on the freshly-initialized repo (proves the CMD-01 init → first-commit path).

    - **`cmd-plan-phase-jj.test.ts` (CMD-02):**
      - Import `createPhaseStructure` from `sdk/src/vcs/jj/octopus.js` (Phase 4 plan 04-05).
      - Test 1: Calling `createPhaseStructure(repoCwd, { phaseName: 'foo' })` creates the lazy parent+merge change pair and returns valid change IDs.
      - Test 2: A single-plan phase WITHOUT subagent fan-out leaves the orchestrator's `@` linear (no parent+merge created — lazy-octopus invariant from WS-05).
      - Test 3: After plan-phase setup, `vcs.refs.bookmarks.exists('gsd/phase-foo')` (or equivalent — verify the namespace pattern from Phase 4 04-06) returns true.

    - **`cmd-execute-phase-jj.test.ts` (CMD-03):**
      - Import `createSubagentSlot` from `sdk/src/vcs/jj/octopus.js` and `performJjReap` from `sdk/src/vcs/jj/reap.js`.
      - Test 1: Three subagent slots created sequentially each get unique change IDs and unique workspace paths (sibling to main repo per WS-04).
      - Test 2: After a subagent's workspace is squashed via `vcs.commit({...}, { phaseMergeFor: undefined })`, the reap inspects the head (`jj show -r <head_id>`) and either abandons (empty) or surfaces (non-empty).
      - Test 3: Pre-commit hook fires after the squash (proves the A3 fix from 05-01 reaches the execute-phase orchestrator path). Use the same marker-file pattern as `jj-hooks.test.ts:83-96`.

    - **`cmd-discuss-phase-jj.test.ts` (CMD-04 — discuss only; verify-work + complete-milestone in plan 05-03):**
      - Test 1: `vcs.commit({ message: 'docs(05): discuss artifacts', files: ['.planning/phases/05-foo/05-CONTEXT.md', '.planning/phases/05-foo/05-DISCUSSION-LOG.md'] })` succeeds when both files exist; assert the commit appears in `vcs.log({ maxCount: 1 })`.
      - Test 2: `gsd-sdk query commit` end-to-end via the query-resolver (use the index entry point if accessible — otherwise direct `createVcsAdapter` + `vcs.commit` and document that the CLI surface is exercised in the smoke integration only).

    - **`cmd-quick-jj.test.ts` (CMD-05):**
      - Test 1: After `vcs.commit({ message: 'docs(quick-foo): summary', files: ['.planning/quick/quick-foo-PLAN.md'] })` on a colocated jj repo, exactly one change advances `@`'s parent (proves "single squash on orchestrator @").
      - Test 2: NO `parent + merge` octopus structure is created (assert `vcs.refs.bookmarks.list({ prefix: 'gsd/phase-' })` is empty — quick.md skips the octopus per CMD-05 spec).
      - Test 3: Pre-commit hook fires once per the commit (single fire — no double-fire from a non-existent octopus merge).

    Every test file MUST export at least 1 it() per Test 1 — failing the suite is preferable to silently skipping. Suite-level `describe.skipIf(!jjAvailable)` is the only skip surface.

    Do NOT add per-runtime smoke matrix (PROMPT-03 / D-37 — trust the installer).
  </action>
  <acceptance_criteria>
    - `ls sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts | wc -l` returns 5.
    - `grep -cP "describe\\.skipIf\\(!jjAvailable\\)" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (one per file).
    - `grep -cP "execSync\\('jj --version'" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5.
    - `grep -cP "mkdtempSync\\(join\\(tmpdir\\(\\), 'gsd-cmd-" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (per-file unique-prefix tmpdir; pre-empts the Phase 4 LEARNINGS tmpdir-contention flake category — see plan 05-05).
    - `grep -lP "createPhaseStructure" sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "createSubagentSlot" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "GSD_HOOK_SKIP_COLOCATED|hook.*fired|marker.*toBe\\(true\\)|markerPath" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1 (proves the test asserts A3-fix behavior).
    - `cd sdk && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts` exits 0 under jj 0.41 (skips cleanly if jj not present).
    - `cd sdk && pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd sdk && pnpm tsc --noEmit && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</automated>
  </verify>
  <done>
    Five new integration test files exist under `sdk/src/vcs/__tests__/`, each gated by `jj --version` availability, each exercising its target command's end-to-end translation against a fresh colocated jj fixture, each cleaning up its tmpdir on afterAll. All five pass locally under jj 0.41.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown prompt → shell (LLM execution) | LLM-generated `gsd-sdk query <verb>` shell invocations |
| test fixture → jj binary | tmpdir-scoped jj init/squash/log invocations |
| workflow string interpolation → SDK query | `${quick_id}` / `${SUMMARY}` / `${PHASE_NUMBER}` strings flow into commit messages and ref args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.02-01 | Tampering | `${variable}` interpolation into `gsd-sdk query commit "${SUMMARY}"` commit-message argv | mitigate | The SDK query commit handler routes the message through `sanitizeCommitMessage` (already in `sdk/src/query/commit.ts:44-66` — strips zero-width chars, prompt-injection markers). The argv form (positional message + `--files`) ensures shell expansion stays inside the message, not metadata. |
| T-05.02-02 | Tampering | Test fixtures run untrusted `jj` binary against tmpdirs | accept | The jj binary is pinned to 0.41 (CI-02; release-tarball install); tests are scoped to per-suite tmpdirs that are cleaned up afterAll. No data outside the tmpdir is at risk. |
| T-05.02-03 | Information Disclosure | Pre-commit hook fires now in colocated mode (D-32) — hooks have read access to the working tree | accept | Pre-existing trust boundary; the repo owner already trusts `.githooks/` content. Documented in plan 05-01 SUMMARY. |
| T-05.02-04 | Repudiation | Workflow markdown rewrites could introduce silent-skip patterns (e.g., `gsd-sdk query foo || true`) that mask failures | mitigate | Mechanical-only rewrite rule (UPSTREAM-03): preserve the original error-handling surface (`|| echo "⚠ …"` stays as-is, never weakened to `|| true`). Spot-checked via diff review. |
</threat_model>

<verification>
- Phase 5 success criterion #1 (every CMD-* command runs end-to-end on jj): CMD-01..05 contribute 5/11 commands' integration tests.
- Phase 5 success criterion #2 (workflow markdown rewritten VCS-agnostic): execute-phase.md + quick.md contribute 104/227 hot-file mentions rewritten.
- Local verification (the plan executor must run): `cd sdk && pnpm tsc --noEmit && pnpm vitest run --reporter=default sdk/src/vcs/__tests__/cmd-*-jj.test.ts` exits 0.
- No-raw-git lint guard: `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (proves the rewrite didn't sneak a raw-git source-side call site into the SDK or tests).
</verification>

<success_criteria>
- `execute-phase.md` has 0 raw `git hook run pre-commit` invocations and at least 1 `gsd-sdk query hooks.fire pre-commit --cwd .` invocation (proves D-32 hand-off lands).
- `quick.md` has 0 raw `git commit` invocations at the CMD-05 commit cycle and at least 1 `gsd-sdk query commit` invocation.
- Five new CMD-* integration test files exist and pass under jj 0.41.
- Any verb gaps (lines that need a verb the catalog doesn't expose) are TODO-tagged with `# TODO(05-05 sweep): …` AND enumerated in the plan SUMMARY's Deferred section.
- No backend conditional (`if jj: …`) introduced in either markdown file (D-33 anti-pattern guard).
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md` covering:
- For each rewritten file: before-mention-count → after-mention-count (the inventory shrink).
- A "Deferred to plan 05-05 sweep" list naming every site tagged `# TODO(05-05 sweep): …` with file:line and the missing verb.
- Each of the 5 CMD-* tests: a one-line description of what it asserts and the change_id / file count it exercises.
- The empirical confirmation that the A3 fix (plan 05-01) reaches the orchestrator path via the `cmd-execute-phase-jj.test.ts` hook-firing assertion.
- A "Hand-off to plan 05-03" note: lifecycle plan inherits the same rewrite patterns for `complete-milestone.md`, `undo.md`, `code-review.md`.
</output>
 | grep -cP "^\\s*git\\s+(commit|push|reset|revert|stash|hook|worktree|symbolic-ref|fetch)\\b"` is at most 12 (raw-git surface in execute-phase.md is 58 invocations per 05-RESEARCH.md inventory; after rewrite, residual ≤12 sweep-TODO sites accommodates worktree-block + symbolic-ref + stash gaps that depend on verbs not added in plan 05-01 — plan 05-05 Task 4 sweeps these to 0; any count >12 indicates incomplete rewrite — planner verifies SUMMARY list matches).
    - `grep -cP "# TODO\\(05-05 sweep\\):" get-shit-done/workflows/execute-phase.md` equals the count of deferred items (must match the SUMMARY's deferred list exactly).
    - `git diff --stat get-shit-done/workflows/execute-phase.md` shows only the rewrite-shape change set (no whole-section reorders — keep diff under ~200 modified lines for a clean reviewable rewrite).
    - From repo root: `bin/install.js --dry-run` (or equivalent installer dry-run command) does not error on the rewritten markdown (Pitfall 2; trust-installer doesn't break).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query hooks\.fire pre-commit --cwd \." get-shit-done/workflows/execute-phase.md && ! grep -P "^[^#]*\bgit hook run pre-commit" get-shit-done/workflows/execute-phase.md</automated>
  </verify>
  <done>
    execute-phase.md no longer contains `git hook run pre-commit` (replaced by `gsd-sdk query hooks.fire pre-commit --cwd .` per D-32 hand-off); the 58 raw-git-invocation surface from 05-RESEARCH.md inventory is rewritten to SDK queries OR explicitly TODO-tagged for plan 05-05 sweep; the rewrite is mechanical (UPSTREAM-03 preserved); no `if jj: …` backend conditionals appear (D-33 anti-pattern guard).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite quick.md to be VCS-agnostic (CMD-05 surface)</name>
  <files>get-shit-done/workflows/quick.md</files>
  <read_first>
    - `get-shit-done/workflows/quick.md` (full file — 1121 LOC; rewrite scope is 46 raw-git invocations per 05-RESEARCH.md inventory)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`get-shit-done/workflows/quick.md` (46 mentions, 1121 LOC)" for the per-line inventory
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` (same analogs as Task 1)
    - `get-shit-done/workflows/autonomous.md:251-253` (the commit-shape analog)
  </read_first>
  <action>
    Mirror the Task-1 rewrite approach for `quick.md`. Headline sites (per 05-RESEARCH.md Consumer Call-Site Inventory):

    1. Line 165: `git config --file .gitmodules` — read; keep inline (it's `.gitmodules` parsing, git-only by nature) OR add a sweep TODO.
    2. Lines 191-226: bookmark/switch block — same treatment as execute-phase.md Task-1 step 2.
    3. **Lines 639-651 (the CMD-05 single-squash headline target):**

       BEFORE (per 05-RESEARCH.md):
       ```bash
       git add ${PLAN}
       git diff --cached --quiet && echo "no changes" && exit 0
       git commit [--no-verify] -m "docs(${quick_id}): ${SUMMARY}" -- "${QUICK_DIR}/${quick_id}-PLAN.md"
       ```

       AFTER:
       ```bash
       # CMD-05: single squash on orchestrator @ (no phase setup, no workspace, no octopus).
       # Adapter handles git-add ↔ jj-squash translation; the SDK query commit verb
       # routes through vcs.commit({files, message, noVerify}).
       gsd-sdk query commit "docs(${quick_id}): ${SUMMARY}" --files "${QUICK_DIR}/${quick_id}-PLAN.md"
       # (--no-verify is a flag forwarded to vcs.commit when present.)
       ```

       Per 05-RESEARCH.md the `gsd-sdk query commit` argv shape is positional message + `--files <list>`. The existing `commit.ts` already accepts this shape (verified per CONTEXT D-35 / Open Q1 of 05-RESEARCH.md). If the executor finds at runtime that `commit.ts` lacks the `--no-verify` flag, the executor MUST add it to `commit.ts` argv-scan loop in the same task (~5-line edit) and add a regression test asserting `--no-verify` parses correctly.
    4. Lines 665, 681-699: `git rev-parse HEAD` / `git symbolic-ref` / `git merge-base` / `git reset --hard` → SDK substitutes as in Task 1.
    5. Lines 774-864: worktree cleanup block — same mechanical pattern as execute-phase.md Task-1 step 7.
    6. Lines 892-904, 1054-1063: `git log --oneline --grep` / `git rev-parse --short HEAD` / `git add ${file_list}` → `gsd-sdk query log` / `gsd-sdk query head-ref --short` / `gsd-sdk query commit --files <list>`.

    Same TODO(05-05 sweep) convention for any verb the catalog doesn't yet expose.
  </action>
  <acceptance_criteria>
    - `grep -P "gsd-sdk query commit " get-shit-done/workflows/quick.md | wc -l` is at least 1 (the CMD-05 headline rewrite).
    - `grep -v '^#' get-shit-done/workflows/quick.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+commit\\b"` is 0.
    - `grep -v '^#' get-shit-done/workflows/quick.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+(add|push|reset|revert|stash|hook|worktree|symbolic-ref|fetch)\\b"` is at most 5 (deferred-to-sweep allowance).
    - `grep -cP "if vcs.adapter == 'jj'" get-shit-done/workflows/quick.md` is 0 (D-33 anti-pattern guard).
    - `grep -cP "if .* jj.*then" get-shit-done/workflows/quick.md` is 0 (no shell-form backend conditionals either).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query commit " get-shit-done/workflows/quick.md && ! grep -vE '^[#"]|gsd-sdk' get-shit-done/workflows/quick.md | grep -P "^\s*git\s+commit\b"</automated>
  </verify>
  <done>
    quick.md CMD-05 commit cycle routes through `gsd-sdk query commit`; the broader 46-site rewrite is mechanical-only (UPSTREAM-03); no backend conditionals (D-33); any verb gaps are TODO-tagged for plan 05-05 sweep and listed in the plan SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 5 CMD integration tests (CMD-01..05) against jj-colocated fixture</name>
  <files>sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts, sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</files>
  <read_first>
    - `sdk/src/vcs/__tests__/jj-hooks.test.ts` (skip-gate + setup pattern: lines 32-39, 60-81, 83-96 — the canonical CMD-* test analog per 05-PATTERNS.md)
    - `sdk/src/vcs/__tests__/jj-octopus.test.ts` (multi-step setup pattern: lines 1-60)
    - `sdk/src/vcs/__tests__/vcs-fixture.ts:31-77` (initJjRepo / makeBackendFixture factory)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`sdk/test/integration/cmd-*-jj.test.ts` (11 new files — CMD-01..11)" and §"Test Suite Skip-Gate" + §"Tmpdir Lifecycle"
  </read_first>
  <action>
    Create five new integration test files under `sdk/src/vcs/__tests__/`. Each file:

    1. Mirrors `jj-hooks.test.ts:32-39` skip-gate pattern (try `jj --version`; describe.skipIf(!jjAvailable)).
    2. Mirrors `jj-octopus.test.ts:39-60` setup pattern (mkdtempSync + `jj git init --colocate` + config user.email/name).
    3. Includes per-describe `afterAll` cleanup with `rmSync(dir, { recursive: true, force: true })`.

    Per-file scope:

    - **`cmd-new-project-jj.test.ts` (CMD-01):**
      - Test 1: `createVcsAdapter(emptyTmpDir, { kind: 'jj' })` constructs successfully when only `.jj` exists; assert `vcs` is defined and `vcs.commit` is a function.
      - Test 2: When `.git` is also present (colocated init via `jj git init --colocate`), the adapter still constructs in jj mode if the project's `vcs.adapter` config is explicitly set to `jj`; assert backend selection.
      - Test 3: `vcs.commit({ message: 'init', files: [] })` succeeds on the freshly-initialized repo (proves the CMD-01 init → first-commit path).

    - **`cmd-plan-phase-jj.test.ts` (CMD-02):**
      - Import `createPhaseStructure` from `sdk/src/vcs/jj/octopus.js` (Phase 4 plan 04-05).
      - Test 1: Calling `createPhaseStructure(repoCwd, { phaseName: 'foo' })` creates the lazy parent+merge change pair and returns valid change IDs.
      - Test 2: A single-plan phase WITHOUT subagent fan-out leaves the orchestrator's `@` linear (no parent+merge created — lazy-octopus invariant from WS-05).
      - Test 3: After plan-phase setup, `vcs.refs.bookmarks.exists('gsd/phase-foo')` (or equivalent — verify the namespace pattern from Phase 4 04-06) returns true.

    - **`cmd-execute-phase-jj.test.ts` (CMD-03):**
      - Import `createSubagentSlot` from `sdk/src/vcs/jj/octopus.js` and `performJjReap` from `sdk/src/vcs/jj/reap.js`.
      - Test 1: Three subagent slots created sequentially each get unique change IDs and unique workspace paths (sibling to main repo per WS-04).
      - Test 2: After a subagent's workspace is squashed via `vcs.commit({...}, { phaseMergeFor: undefined })`, the reap inspects the head (`jj show -r <head_id>`) and either abandons (empty) or surfaces (non-empty).
      - Test 3: Pre-commit hook fires after the squash (proves the A3 fix from 05-01 reaches the execute-phase orchestrator path). Use the same marker-file pattern as `jj-hooks.test.ts:83-96`.

    - **`cmd-discuss-phase-jj.test.ts` (CMD-04 — discuss only; verify-work + complete-milestone in plan 05-03):**
      - Test 1: `vcs.commit({ message: 'docs(05): discuss artifacts', files: ['.planning/phases/05-foo/05-CONTEXT.md', '.planning/phases/05-foo/05-DISCUSSION-LOG.md'] })` succeeds when both files exist; assert the commit appears in `vcs.log({ maxCount: 1 })`.
      - Test 2: `gsd-sdk query commit` end-to-end via the query-resolver (use the index entry point if accessible — otherwise direct `createVcsAdapter` + `vcs.commit` and document that the CLI surface is exercised in the smoke integration only).

    - **`cmd-quick-jj.test.ts` (CMD-05):**
      - Test 1: After `vcs.commit({ message: 'docs(quick-foo): summary', files: ['.planning/quick/quick-foo-PLAN.md'] })` on a colocated jj repo, exactly one change advances `@`'s parent (proves "single squash on orchestrator @").
      - Test 2: NO `parent + merge` octopus structure is created (assert `vcs.refs.bookmarks.list({ prefix: 'gsd/phase-' })` is empty — quick.md skips the octopus per CMD-05 spec).
      - Test 3: Pre-commit hook fires once per the commit (single fire — no double-fire from a non-existent octopus merge).

    Every test file MUST export at least 1 it() per Test 1 — failing the suite is preferable to silently skipping. Suite-level `describe.skipIf(!jjAvailable)` is the only skip surface.

    Do NOT add per-runtime smoke matrix (PROMPT-03 / D-37 — trust the installer).
  </action>
  <acceptance_criteria>
    - `ls sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts | wc -l` returns 5.
    - `grep -cP "describe\\.skipIf\\(!jjAvailable\\)" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (one per file).
    - `grep -cP "execSync\\('jj --version'" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5.
    - `grep -cP "mkdtempSync\\(join\\(tmpdir\\(\\), 'gsd-cmd-" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (per-file unique-prefix tmpdir; pre-empts the Phase 4 LEARNINGS tmpdir-contention flake category — see plan 05-05).
    - `grep -lP "createPhaseStructure" sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "createSubagentSlot" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "GSD_HOOK_SKIP_COLOCATED|hook.*fired|marker.*toBe\\(true\\)|markerPath" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1 (proves the test asserts A3-fix behavior).
    - `cd sdk && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts` exits 0 under jj 0.41 (skips cleanly if jj not present).
    - `cd sdk && pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd sdk && pnpm tsc --noEmit && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</automated>
  </verify>
  <done>
    Five new integration test files exist under `sdk/src/vcs/__tests__/`, each gated by `jj --version` availability, each exercising its target command's end-to-end translation against a fresh colocated jj fixture, each cleaning up its tmpdir on afterAll. All five pass locally under jj 0.41.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown prompt → shell (LLM execution) | LLM-generated `gsd-sdk query <verb>` shell invocations |
| test fixture → jj binary | tmpdir-scoped jj init/squash/log invocations |
| workflow string interpolation → SDK query | `${quick_id}` / `${SUMMARY}` / `${PHASE_NUMBER}` strings flow into commit messages and ref args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.02-01 | Tampering | `${variable}` interpolation into `gsd-sdk query commit "${SUMMARY}"` commit-message argv | mitigate | The SDK query commit handler routes the message through `sanitizeCommitMessage` (already in `sdk/src/query/commit.ts:44-66` — strips zero-width chars, prompt-injection markers). The argv form (positional message + `--files`) ensures shell expansion stays inside the message, not metadata. |
| T-05.02-02 | Tampering | Test fixtures run untrusted `jj` binary against tmpdirs | accept | The jj binary is pinned to 0.41 (CI-02; release-tarball install); tests are scoped to per-suite tmpdirs that are cleaned up afterAll. No data outside the tmpdir is at risk. |
| T-05.02-03 | Information Disclosure | Pre-commit hook fires now in colocated mode (D-32) — hooks have read access to the working tree | accept | Pre-existing trust boundary; the repo owner already trusts `.githooks/` content. Documented in plan 05-01 SUMMARY. |
| T-05.02-04 | Repudiation | Workflow markdown rewrites could introduce silent-skip patterns (e.g., `gsd-sdk query foo || true`) that mask failures | mitigate | Mechanical-only rewrite rule (UPSTREAM-03): preserve the original error-handling surface (`|| echo "⚠ …"` stays as-is, never weakened to `|| true`). Spot-checked via diff review. |
</threat_model>

<verification>
- Phase 5 success criterion #1 (every CMD-* command runs end-to-end on jj): CMD-01..05 contribute 5/11 commands' integration tests.
- Phase 5 success criterion #2 (workflow markdown rewritten VCS-agnostic): execute-phase.md + quick.md contribute 104/227 hot-file mentions rewritten.
- Local verification (the plan executor must run): `cd sdk && pnpm tsc --noEmit && pnpm vitest run --reporter=default sdk/src/vcs/__tests__/cmd-*-jj.test.ts` exits 0.
- No-raw-git lint guard: `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (proves the rewrite didn't sneak a raw-git source-side call site into the SDK or tests).
</verification>

<success_criteria>
- `execute-phase.md` has 0 raw `git hook run pre-commit` invocations and at least 1 `gsd-sdk query hooks.fire pre-commit --cwd .` invocation (proves D-32 hand-off lands).
- `quick.md` has 0 raw `git commit` invocations at the CMD-05 commit cycle and at least 1 `gsd-sdk query commit` invocation.
- Five new CMD-* integration test files exist and pass under jj 0.41.
- Any verb gaps (lines that need a verb the catalog doesn't expose) are TODO-tagged with `# TODO(05-05 sweep): …` AND enumerated in the plan SUMMARY's Deferred section.
- No backend conditional (`if jj: …`) introduced in either markdown file (D-33 anti-pattern guard).
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md` covering:
- For each rewritten file: before-mention-count → after-mention-count (the inventory shrink).
- A "Deferred to plan 05-05 sweep" list naming every site tagged `# TODO(05-05 sweep): …` with file:line and the missing verb.
- Each of the 5 CMD-* tests: a one-line description of what it asserts and the change_id / file count it exercises.
- The empirical confirmation that the A3 fix (plan 05-01) reaches the orchestrator path via the `cmd-execute-phase-jj.test.ts` hook-firing assertion.
- A "Hand-off to plan 05-03" note: lifecycle plan inherits the same rewrite patterns for `complete-milestone.md`, `undo.md`, `code-review.md`.
</output>
 | grep -cP "^\\s*git\\s+(add|push|reset|revert|stash|hook|worktree|symbolic-ref|fetch)\\b"` is at most 10 (raw-git surface in quick.md is 46 invocations per inventory; residual ≤10 sweep-TODO sites — plan 05-05 Task 4 sweeps to 0).
    - `grep -cP "if vcs.adapter == 'jj'" get-shit-done/workflows/quick.md` is 0 (D-33 anti-pattern guard).
    - `grep -cP "if .* jj.*then" get-shit-done/workflows/quick.md` is 0 (no shell-form backend conditionals either).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query commit " get-shit-done/workflows/quick.md && ! grep -vE '^[#"]|gsd-sdk' get-shit-done/workflows/quick.md | grep -P "^\s*git\s+commit\b"</automated>
  </verify>
  <done>
    quick.md CMD-05 commit cycle routes through `gsd-sdk query commit`; the broader 46-site rewrite is mechanical-only (UPSTREAM-03); no backend conditionals (D-33); any verb gaps are TODO-tagged for plan 05-05 sweep and listed in the plan SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 5 CMD integration tests (CMD-01..05) against jj-colocated fixture</name>
  <files>sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts, sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</files>
  <read_first>
    - `sdk/src/vcs/__tests__/jj-hooks.test.ts` (skip-gate + setup pattern: lines 32-39, 60-81, 83-96 — the canonical CMD-* test analog per 05-PATTERNS.md)
    - `sdk/src/vcs/__tests__/jj-octopus.test.ts` (multi-step setup pattern: lines 1-60)
    - `sdk/src/vcs/__tests__/vcs-fixture.ts:31-77` (initJjRepo / makeBackendFixture factory)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`sdk/test/integration/cmd-*-jj.test.ts` (11 new files — CMD-01..11)" and §"Test Suite Skip-Gate" + §"Tmpdir Lifecycle"
  </read_first>
  <action>
    Create five new integration test files under `sdk/src/vcs/__tests__/`. Each file:

    1. Mirrors `jj-hooks.test.ts:32-39` skip-gate pattern (try `jj --version`; describe.skipIf(!jjAvailable)).
    2. Mirrors `jj-octopus.test.ts:39-60` setup pattern (mkdtempSync + `jj git init --colocate` + config user.email/name).
    3. Includes per-describe `afterAll` cleanup with `rmSync(dir, { recursive: true, force: true })`.

    Per-file scope:

    - **`cmd-new-project-jj.test.ts` (CMD-01):**
      - Test 1: `createVcsAdapter(emptyTmpDir, { kind: 'jj' })` constructs successfully when only `.jj` exists; assert `vcs` is defined and `vcs.commit` is a function.
      - Test 2: When `.git` is also present (colocated init via `jj git init --colocate`), the adapter still constructs in jj mode if the project's `vcs.adapter` config is explicitly set to `jj`; assert backend selection.
      - Test 3: `vcs.commit({ message: 'init', files: [] })` succeeds on the freshly-initialized repo (proves the CMD-01 init → first-commit path).

    - **`cmd-plan-phase-jj.test.ts` (CMD-02):**
      - Import `createPhaseStructure` from `sdk/src/vcs/jj/octopus.js` (Phase 4 plan 04-05).
      - Test 1: Calling `createPhaseStructure(repoCwd, { phaseName: 'foo' })` creates the lazy parent+merge change pair and returns valid change IDs.
      - Test 2: A single-plan phase WITHOUT subagent fan-out leaves the orchestrator's `@` linear (no parent+merge created — lazy-octopus invariant from WS-05).
      - Test 3: After plan-phase setup, `vcs.refs.bookmarks.exists('gsd/phase-foo')` (or equivalent — verify the namespace pattern from Phase 4 04-06) returns true.

    - **`cmd-execute-phase-jj.test.ts` (CMD-03):**
      - Import `createSubagentSlot` from `sdk/src/vcs/jj/octopus.js` and `performJjReap` from `sdk/src/vcs/jj/reap.js`.
      - Test 1: Three subagent slots created sequentially each get unique change IDs and unique workspace paths (sibling to main repo per WS-04).
      - Test 2: After a subagent's workspace is squashed via `vcs.commit({...}, { phaseMergeFor: undefined })`, the reap inspects the head (`jj show -r <head_id>`) and either abandons (empty) or surfaces (non-empty).
      - Test 3: Pre-commit hook fires after the squash (proves the A3 fix from 05-01 reaches the execute-phase orchestrator path). Use the same marker-file pattern as `jj-hooks.test.ts:83-96`.

    - **`cmd-discuss-phase-jj.test.ts` (CMD-04 — discuss only; verify-work + complete-milestone in plan 05-03):**
      - Test 1: `vcs.commit({ message: 'docs(05): discuss artifacts', files: ['.planning/phases/05-foo/05-CONTEXT.md', '.planning/phases/05-foo/05-DISCUSSION-LOG.md'] })` succeeds when both files exist; assert the commit appears in `vcs.log({ maxCount: 1 })`.
      - Test 2: `gsd-sdk query commit` end-to-end via the query-resolver (use the index entry point if accessible — otherwise direct `createVcsAdapter` + `vcs.commit` and document that the CLI surface is exercised in the smoke integration only).

    - **`cmd-quick-jj.test.ts` (CMD-05):**
      - Test 1: After `vcs.commit({ message: 'docs(quick-foo): summary', files: ['.planning/quick/quick-foo-PLAN.md'] })` on a colocated jj repo, exactly one change advances `@`'s parent (proves "single squash on orchestrator @").
      - Test 2: NO `parent + merge` octopus structure is created (assert `vcs.refs.bookmarks.list({ prefix: 'gsd/phase-' })` is empty — quick.md skips the octopus per CMD-05 spec).
      - Test 3: Pre-commit hook fires once per the commit (single fire — no double-fire from a non-existent octopus merge).

    Every test file MUST export at least 1 it() per Test 1 — failing the suite is preferable to silently skipping. Suite-level `describe.skipIf(!jjAvailable)` is the only skip surface.

    Do NOT add per-runtime smoke matrix (PROMPT-03 / D-37 — trust the installer).
  </action>
  <acceptance_criteria>
    - `ls sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts | wc -l` returns 5.
    - `grep -cP "describe\\.skipIf\\(!jjAvailable\\)" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (one per file).
    - `grep -cP "execSync\\('jj --version'" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5.
    - `grep -cP "mkdtempSync\\(join\\(tmpdir\\(\\), 'gsd-cmd-" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (per-file unique-prefix tmpdir; pre-empts the Phase 4 LEARNINGS tmpdir-contention flake category — see plan 05-05).
    - `grep -lP "createPhaseStructure" sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "createSubagentSlot" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "GSD_HOOK_SKIP_COLOCATED|hook.*fired|marker.*toBe\\(true\\)|markerPath" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1 (proves the test asserts A3-fix behavior).
    - `cd sdk && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts` exits 0 under jj 0.41 (skips cleanly if jj not present).
    - `cd sdk && pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd sdk && pnpm tsc --noEmit && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</automated>
  </verify>
  <done>
    Five new integration test files exist under `sdk/src/vcs/__tests__/`, each gated by `jj --version` availability, each exercising its target command's end-to-end translation against a fresh colocated jj fixture, each cleaning up its tmpdir on afterAll. All five pass locally under jj 0.41.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown prompt → shell (LLM execution) | LLM-generated `gsd-sdk query <verb>` shell invocations |
| test fixture → jj binary | tmpdir-scoped jj init/squash/log invocations |
| workflow string interpolation → SDK query | `${quick_id}` / `${SUMMARY}` / `${PHASE_NUMBER}` strings flow into commit messages and ref args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.02-01 | Tampering | `${variable}` interpolation into `gsd-sdk query commit "${SUMMARY}"` commit-message argv | mitigate | The SDK query commit handler routes the message through `sanitizeCommitMessage` (already in `sdk/src/query/commit.ts:44-66` — strips zero-width chars, prompt-injection markers). The argv form (positional message + `--files`) ensures shell expansion stays inside the message, not metadata. |
| T-05.02-02 | Tampering | Test fixtures run untrusted `jj` binary against tmpdirs | accept | The jj binary is pinned to 0.41 (CI-02; release-tarball install); tests are scoped to per-suite tmpdirs that are cleaned up afterAll. No data outside the tmpdir is at risk. |
| T-05.02-03 | Information Disclosure | Pre-commit hook fires now in colocated mode (D-32) — hooks have read access to the working tree | accept | Pre-existing trust boundary; the repo owner already trusts `.githooks/` content. Documented in plan 05-01 SUMMARY. |
| T-05.02-04 | Repudiation | Workflow markdown rewrites could introduce silent-skip patterns (e.g., `gsd-sdk query foo || true`) that mask failures | mitigate | Mechanical-only rewrite rule (UPSTREAM-03): preserve the original error-handling surface (`|| echo "⚠ …"` stays as-is, never weakened to `|| true`). Spot-checked via diff review. |
</threat_model>

<verification>
- Phase 5 success criterion #1 (every CMD-* command runs end-to-end on jj): CMD-01..05 contribute 5/11 commands' integration tests.
- Phase 5 success criterion #2 (workflow markdown rewritten VCS-agnostic): execute-phase.md + quick.md contribute 104/227 hot-file mentions rewritten.
- Local verification (the plan executor must run): `cd sdk && pnpm tsc --noEmit && pnpm vitest run --reporter=default sdk/src/vcs/__tests__/cmd-*-jj.test.ts` exits 0.
- No-raw-git lint guard: `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (proves the rewrite didn't sneak a raw-git source-side call site into the SDK or tests).
</verification>

<success_criteria>
- `execute-phase.md` has 0 raw `git hook run pre-commit` invocations and at least 1 `gsd-sdk query hooks.fire pre-commit --cwd .` invocation (proves D-32 hand-off lands).
- `quick.md` has 0 raw `git commit` invocations at the CMD-05 commit cycle and at least 1 `gsd-sdk query commit` invocation.
- Five new CMD-* integration test files exist and pass under jj 0.41.
- Any verb gaps (lines that need a verb the catalog doesn't expose) are TODO-tagged with `# TODO(05-05 sweep): …` AND enumerated in the plan SUMMARY's Deferred section.
- No backend conditional (`if jj: …`) introduced in either markdown file (D-33 anti-pattern guard).
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md` covering:
- For each rewritten file: before-mention-count → after-mention-count (the inventory shrink).
- A "Deferred to plan 05-05 sweep" list naming every site tagged `# TODO(05-05 sweep): …` with file:line and the missing verb.
- Each of the 5 CMD-* tests: a one-line description of what it asserts and the change_id / file count it exercises.
- The empirical confirmation that the A3 fix (plan 05-01) reaches the orchestrator path via the `cmd-execute-phase-jj.test.ts` hook-firing assertion.
- A "Hand-off to plan 05-03" note: lifecycle plan inherits the same rewrite patterns for `complete-milestone.md`, `undo.md`, `code-review.md`.
</output>
 | grep -cP "^\\s*git\\s+(commit|push|reset|revert|stash|hook|worktree|symbolic-ref|fetch)\\b"` is at most 12 (raw-git surface in execute-phase.md is 58 invocations per 05-RESEARCH.md inventory; after rewrite, residual ≤12 sweep-TODO sites accommodates worktree-block + symbolic-ref + stash gaps that depend on verbs not added in plan 05-01 — plan 05-05 Task 4 sweeps these to 0; any count >12 indicates incomplete rewrite — planner verifies SUMMARY list matches).
    - `grep -cP "# TODO\\(05-05 sweep\\):" get-shit-done/workflows/execute-phase.md` equals the count of deferred items (must match the SUMMARY's deferred list exactly).
    - `git diff --stat get-shit-done/workflows/execute-phase.md` shows only the rewrite-shape change set (no whole-section reorders — keep diff under ~200 modified lines for a clean reviewable rewrite).
    - From repo root: `bin/install.js --dry-run` (or equivalent installer dry-run command) does not error on the rewritten markdown (Pitfall 2; trust-installer doesn't break).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query hooks\.fire pre-commit --cwd \." get-shit-done/workflows/execute-phase.md && ! grep -P "^[^#]*\bgit hook run pre-commit" get-shit-done/workflows/execute-phase.md</automated>
  </verify>
  <done>
    execute-phase.md no longer contains `git hook run pre-commit` (replaced by `gsd-sdk query hooks.fire pre-commit --cwd .` per D-32 hand-off); the 58 raw-git-invocation surface from 05-RESEARCH.md inventory is rewritten to SDK queries OR explicitly TODO-tagged for plan 05-05 sweep; the rewrite is mechanical (UPSTREAM-03 preserved); no `if jj: …` backend conditionals appear (D-33 anti-pattern guard).
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite quick.md to be VCS-agnostic (CMD-05 surface)</name>
  <files>get-shit-done/workflows/quick.md</files>
  <read_first>
    - `get-shit-done/workflows/quick.md` (full file — 1121 LOC; rewrite scope is 46 raw-git invocations per 05-RESEARCH.md inventory)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-RESEARCH.md` §"`get-shit-done/workflows/quick.md` (46 mentions, 1121 LOC)" for the per-line inventory
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` (same analogs as Task 1)
    - `get-shit-done/workflows/autonomous.md:251-253` (the commit-shape analog)
  </read_first>
  <action>
    Mirror the Task-1 rewrite approach for `quick.md`. Headline sites (per 05-RESEARCH.md Consumer Call-Site Inventory):

    1. Line 165: `git config --file .gitmodules` — read; keep inline (it's `.gitmodules` parsing, git-only by nature) OR add a sweep TODO.
    2. Lines 191-226: bookmark/switch block — same treatment as execute-phase.md Task-1 step 2.
    3. **Lines 639-651 (the CMD-05 single-squash headline target):**

       BEFORE (per 05-RESEARCH.md):
       ```bash
       git add ${PLAN}
       git diff --cached --quiet && echo "no changes" && exit 0
       git commit [--no-verify] -m "docs(${quick_id}): ${SUMMARY}" -- "${QUICK_DIR}/${quick_id}-PLAN.md"
       ```

       AFTER:
       ```bash
       # CMD-05: single squash on orchestrator @ (no phase setup, no workspace, no octopus).
       # Adapter handles git-add ↔ jj-squash translation; the SDK query commit verb
       # routes through vcs.commit({files, message, noVerify}).
       gsd-sdk query commit "docs(${quick_id}): ${SUMMARY}" --files "${QUICK_DIR}/${quick_id}-PLAN.md"
       # (--no-verify is a flag forwarded to vcs.commit when present.)
       ```

       Per 05-RESEARCH.md the `gsd-sdk query commit` argv shape is positional message + `--files <list>`. The existing `commit.ts` already accepts this shape (verified per CONTEXT D-35 / Open Q1 of 05-RESEARCH.md). If the executor finds at runtime that `commit.ts` lacks the `--no-verify` flag, the executor MUST add it to `commit.ts` argv-scan loop in the same task (~5-line edit) and add a regression test asserting `--no-verify` parses correctly.
    4. Lines 665, 681-699: `git rev-parse HEAD` / `git symbolic-ref` / `git merge-base` / `git reset --hard` → SDK substitutes as in Task 1.
    5. Lines 774-864: worktree cleanup block — same mechanical pattern as execute-phase.md Task-1 step 7.
    6. Lines 892-904, 1054-1063: `git log --oneline --grep` / `git rev-parse --short HEAD` / `git add ${file_list}` → `gsd-sdk query log` / `gsd-sdk query head-ref --short` / `gsd-sdk query commit --files <list>`.

    Same TODO(05-05 sweep) convention for any verb the catalog doesn't yet expose.
  </action>
  <acceptance_criteria>
    - `grep -P "gsd-sdk query commit " get-shit-done/workflows/quick.md | wc -l` is at least 1 (the CMD-05 headline rewrite).
    - `grep -v '^#' get-shit-done/workflows/quick.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+commit\\b"` is 0.
    - `grep -v '^#' get-shit-done/workflows/quick.md | grep -v '^\s*$' | grep -cP "^\\s*git\\s+(add|push|reset|revert|stash|hook|worktree|symbolic-ref|fetch)\\b"` is at most 5 (deferred-to-sweep allowance).
    - `grep -cP "if vcs.adapter == 'jj'" get-shit-done/workflows/quick.md` is 0 (D-33 anti-pattern guard).
    - `grep -cP "if .* jj.*then" get-shit-done/workflows/quick.md` is 0 (no shell-form backend conditionals either).
  </acceptance_criteria>
  <verify>
    <automated>grep -P "gsd-sdk query commit " get-shit-done/workflows/quick.md && ! grep -vE '^[#"]|gsd-sdk' get-shit-done/workflows/quick.md | grep -P "^\s*git\s+commit\b"</automated>
  </verify>
  <done>
    quick.md CMD-05 commit cycle routes through `gsd-sdk query commit`; the broader 46-site rewrite is mechanical-only (UPSTREAM-03); no backend conditionals (D-33); any verb gaps are TODO-tagged for plan 05-05 sweep and listed in the plan SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 5 CMD integration tests (CMD-01..05) against jj-colocated fixture</name>
  <files>sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts, sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts, sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</files>
  <read_first>
    - `sdk/src/vcs/__tests__/jj-hooks.test.ts` (skip-gate + setup pattern: lines 32-39, 60-81, 83-96 — the canonical CMD-* test analog per 05-PATTERNS.md)
    - `sdk/src/vcs/__tests__/jj-octopus.test.ts` (multi-step setup pattern: lines 1-60)
    - `sdk/src/vcs/__tests__/vcs-fixture.ts:31-77` (initJjRepo / makeBackendFixture factory)
    - `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-PATTERNS.md` §"`sdk/test/integration/cmd-*-jj.test.ts` (11 new files — CMD-01..11)" and §"Test Suite Skip-Gate" + §"Tmpdir Lifecycle"
  </read_first>
  <action>
    Create five new integration test files under `sdk/src/vcs/__tests__/`. Each file:

    1. Mirrors `jj-hooks.test.ts:32-39` skip-gate pattern (try `jj --version`; describe.skipIf(!jjAvailable)).
    2. Mirrors `jj-octopus.test.ts:39-60` setup pattern (mkdtempSync + `jj git init --colocate` + config user.email/name).
    3. Includes per-describe `afterAll` cleanup with `rmSync(dir, { recursive: true, force: true })`.

    Per-file scope:

    - **`cmd-new-project-jj.test.ts` (CMD-01):**
      - Test 1: `createVcsAdapter(emptyTmpDir, { kind: 'jj' })` constructs successfully when only `.jj` exists; assert `vcs` is defined and `vcs.commit` is a function.
      - Test 2: When `.git` is also present (colocated init via `jj git init --colocate`), the adapter still constructs in jj mode if the project's `vcs.adapter` config is explicitly set to `jj`; assert backend selection.
      - Test 3: `vcs.commit({ message: 'init', files: [] })` succeeds on the freshly-initialized repo (proves the CMD-01 init → first-commit path).

    - **`cmd-plan-phase-jj.test.ts` (CMD-02):**
      - Import `createPhaseStructure` from `sdk/src/vcs/jj/octopus.js` (Phase 4 plan 04-05).
      - Test 1: Calling `createPhaseStructure(repoCwd, { phaseName: 'foo' })` creates the lazy parent+merge change pair and returns valid change IDs.
      - Test 2: A single-plan phase WITHOUT subagent fan-out leaves the orchestrator's `@` linear (no parent+merge created — lazy-octopus invariant from WS-05).
      - Test 3: After plan-phase setup, `vcs.refs.bookmarks.exists('gsd/phase-foo')` (or equivalent — verify the namespace pattern from Phase 4 04-06) returns true.

    - **`cmd-execute-phase-jj.test.ts` (CMD-03):**
      - Import `createSubagentSlot` from `sdk/src/vcs/jj/octopus.js` and `performJjReap` from `sdk/src/vcs/jj/reap.js`.
      - Test 1: Three subagent slots created sequentially each get unique change IDs and unique workspace paths (sibling to main repo per WS-04).
      - Test 2: After a subagent's workspace is squashed via `vcs.commit({...}, { phaseMergeFor: undefined })`, the reap inspects the head (`jj show -r <head_id>`) and either abandons (empty) or surfaces (non-empty).
      - Test 3: Pre-commit hook fires after the squash (proves the A3 fix from 05-01 reaches the execute-phase orchestrator path). Use the same marker-file pattern as `jj-hooks.test.ts:83-96`.

    - **`cmd-discuss-phase-jj.test.ts` (CMD-04 — discuss only; verify-work + complete-milestone in plan 05-03):**
      - Test 1: `vcs.commit({ message: 'docs(05): discuss artifacts', files: ['.planning/phases/05-foo/05-CONTEXT.md', '.planning/phases/05-foo/05-DISCUSSION-LOG.md'] })` succeeds when both files exist; assert the commit appears in `vcs.log({ maxCount: 1 })`.
      - Test 2: `gsd-sdk query commit` end-to-end via the query-resolver (use the index entry point if accessible — otherwise direct `createVcsAdapter` + `vcs.commit` and document that the CLI surface is exercised in the smoke integration only).

    - **`cmd-quick-jj.test.ts` (CMD-05):**
      - Test 1: After `vcs.commit({ message: 'docs(quick-foo): summary', files: ['.planning/quick/quick-foo-PLAN.md'] })` on a colocated jj repo, exactly one change advances `@`'s parent (proves "single squash on orchestrator @").
      - Test 2: NO `parent + merge` octopus structure is created (assert `vcs.refs.bookmarks.list({ prefix: 'gsd/phase-' })` is empty — quick.md skips the octopus per CMD-05 spec).
      - Test 3: Pre-commit hook fires once per the commit (single fire — no double-fire from a non-existent octopus merge).

    Every test file MUST export at least 1 it() per Test 1 — failing the suite is preferable to silently skipping. Suite-level `describe.skipIf(!jjAvailable)` is the only skip surface.

    Do NOT add per-runtime smoke matrix (PROMPT-03 / D-37 — trust the installer).
  </action>
  <acceptance_criteria>
    - `ls sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts | wc -l` returns 5.
    - `grep -cP "describe\\.skipIf\\(!jjAvailable\\)" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (one per file).
    - `grep -cP "execSync\\('jj --version'" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5.
    - `grep -cP "mkdtempSync\\(join\\(tmpdir\\(\\), 'gsd-cmd-" sdk/src/vcs/__tests__/cmd-*-jj.test.ts` returns at least 5 (per-file unique-prefix tmpdir; pre-empts the Phase 4 LEARNINGS tmpdir-contention flake category — see plan 05-05).
    - `grep -lP "createPhaseStructure" sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "createSubagentSlot" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1.
    - `grep -lP "GSD_HOOK_SKIP_COLOCATED|hook.*fired|marker.*toBe\\(true\\)|markerPath" sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts | wc -l` is 1 (proves the test asserts A3-fix behavior).
    - `cd sdk && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts` exits 0 under jj 0.41 (skips cleanly if jj not present).
    - `cd sdk && pnpm tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd sdk && pnpm tsc --noEmit && pnpm vitest run sdk/src/vcs/__tests__/cmd-new-project-jj.test.ts sdk/src/vcs/__tests__/cmd-plan-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-execute-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-discuss-phase-jj.test.ts sdk/src/vcs/__tests__/cmd-quick-jj.test.ts</automated>
  </verify>
  <done>
    Five new integration test files exist under `sdk/src/vcs/__tests__/`, each gated by `jj --version` availability, each exercising its target command's end-to-end translation against a fresh colocated jj fixture, each cleaning up its tmpdir on afterAll. All five pass locally under jj 0.41.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown prompt → shell (LLM execution) | LLM-generated `gsd-sdk query <verb>` shell invocations |
| test fixture → jj binary | tmpdir-scoped jj init/squash/log invocations |
| workflow string interpolation → SDK query | `${quick_id}` / `${SUMMARY}` / `${PHASE_NUMBER}` strings flow into commit messages and ref args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.02-01 | Tampering | `${variable}` interpolation into `gsd-sdk query commit "${SUMMARY}"` commit-message argv | mitigate | The SDK query commit handler routes the message through `sanitizeCommitMessage` (already in `sdk/src/query/commit.ts:44-66` — strips zero-width chars, prompt-injection markers). The argv form (positional message + `--files`) ensures shell expansion stays inside the message, not metadata. |
| T-05.02-02 | Tampering | Test fixtures run untrusted `jj` binary against tmpdirs | accept | The jj binary is pinned to 0.41 (CI-02; release-tarball install); tests are scoped to per-suite tmpdirs that are cleaned up afterAll. No data outside the tmpdir is at risk. |
| T-05.02-03 | Information Disclosure | Pre-commit hook fires now in colocated mode (D-32) — hooks have read access to the working tree | accept | Pre-existing trust boundary; the repo owner already trusts `.githooks/` content. Documented in plan 05-01 SUMMARY. |
| T-05.02-04 | Repudiation | Workflow markdown rewrites could introduce silent-skip patterns (e.g., `gsd-sdk query foo || true`) that mask failures | mitigate | Mechanical-only rewrite rule (UPSTREAM-03): preserve the original error-handling surface (`|| echo "⚠ …"` stays as-is, never weakened to `|| true`). Spot-checked via diff review. |
</threat_model>

<verification>
- Phase 5 success criterion #1 (every CMD-* command runs end-to-end on jj): CMD-01..05 contribute 5/11 commands' integration tests.
- Phase 5 success criterion #2 (workflow markdown rewritten VCS-agnostic): execute-phase.md + quick.md contribute 104/227 hot-file mentions rewritten.
- Local verification (the plan executor must run): `cd sdk && pnpm tsc --noEmit && pnpm vitest run --reporter=default sdk/src/vcs/__tests__/cmd-*-jj.test.ts` exits 0.
- No-raw-git lint guard: `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (proves the rewrite didn't sneak a raw-git source-side call site into the SDK or tests).
</verification>

<success_criteria>
- `execute-phase.md` has 0 raw `git hook run pre-commit` invocations and at least 1 `gsd-sdk query hooks.fire pre-commit --cwd .` invocation (proves D-32 hand-off lands).
- `quick.md` has 0 raw `git commit` invocations at the CMD-05 commit cycle and at least 1 `gsd-sdk query commit` invocation.
- Five new CMD-* integration test files exist and pass under jj 0.41.
- Any verb gaps (lines that need a verb the catalog doesn't expose) are TODO-tagged with `# TODO(05-05 sweep): …` AND enumerated in the plan SUMMARY's Deferred section.
- No backend conditional (`if jj: …`) introduced in either markdown file (D-33 anti-pattern guard).
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-translations-brownfield-validation-ci-hardening/05-02-SUMMARY.md` covering:
- For each rewritten file: before-mention-count → after-mention-count (the inventory shrink).
- A "Deferred to plan 05-05 sweep" list naming every site tagged `# TODO(05-05 sweep): …` with file:line and the missing verb.
- Each of the 5 CMD-* tests: a one-line description of what it asserts and the change_id / file count it exercises.
- The empirical confirmation that the A3 fix (plan 05-01) reaches the orchestrator path via the `cmd-execute-phase-jj.test.ts` hook-firing assertion.
- A "Hand-off to plan 05-03" note: lifecycle plan inherits the same rewrite patterns for `complete-milestone.md`, `undo.md`, `code-review.md`.
</output>
