---
phase: 06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - .planning/STATE.md
  - .planning/intel/rebase-log.md
  - .planning/intel/06-dogfood-log.md
autonomous: false
requirements:
  - BROWN-01
  - BROWN-02
  - PHASE6-MEMORY-RULE-LIFT

must_haves:
  truths:
    - "A sibling clone of this repo at `~/tmp/gsd-dogfood-$(date +%Y%m%d)` has run `/gsd-migrate-vcs --target jj` end-to-end with no errors (RESEARCH §BROWN-01 Dogfood Safety Strategy)"
    - "Every brownfield command from BROWN-01 ran inside the migrated sibling clone with observably-correct output (no degradation versus the git-side baseline): `/gsd-map-codebase`, `/gsd-import`, `/gsd-ingest-docs`, `/gsd-resume-work`, `/gsd-pause-work`, `/gsd-ship`, `/gsd-pr-branch`, `/gsd-undo`"
    - "A human checkpoint approved the sibling-clone go/no-go gate BEFORE any migration ran against THIS repo's `.planning/`"
    - "STATE.md's 'use git not jj' memory rule is LIFTED via the explicit edit per RESEARCH §Memory-rule lift trigger — the line `- **Pre-Phase-6:** Use git (not jj) until migration lands.` becomes `- **Post-BROWN-01 (Phase 6 plan 06-04):** Phase 6 migration validated on sibling clone. jj is now permitted in this repo.`"
    - "(OPTIONAL post-checkpoint) The real repo has been migrated to jj via `/gsd-migrate-vcs --target jj`; or the planner explicitly defers the in-place flip with a recorded reason in `.planning/intel/06-dogfood-log.md`"
    - "BROWN-02 retro file `.planning/intel/rebase-log.md` exists with the standard 3-column table header and (initially) a `_(no entries yet — first weekly rebase will be recorded here)_` placeholder OR (if the first rebase has occurred during plan execution) a single populated row per RESEARCH §BROWN-02 Retro File Shape"
    - "`.planning/intel/06-dogfood-log.md` records the full sibling-clone validation evidence: each brownfield command tested, observed behavior, deviation (if any) from the git-side baseline"
  artifacts:
    - path: ".planning/intel/06-dogfood-log.md"
      provides: "Sibling-clone validation evidence per brownfield command + final go/no-go verdict"
      min_lines: 40
    - path: ".planning/intel/rebase-log.md"
      provides: "BROWN-02 retro journal (3-col table — Date / Conflicts / Notes)"
      min_lines: 10
    - path: ".planning/STATE.md"
      provides: "Memory rule lifted via the documented STATE.md edit"
      contains: "Post-BROWN-01"
  key_links:
    - from: ".planning/intel/06-dogfood-log.md"
      to: "sibling clone validation outcomes"
      via: "Manual workflow execution evidence"
      pattern: "/gsd-map-codebase|/gsd-import|/gsd-ingest-docs|/gsd-resume-work|/gsd-pause-work|/gsd-ship|/gsd-pr-branch|/gsd-undo"
    - from: ".planning/STATE.md"
      to: "user memory at ~/.claude/projects/.../memory/feedback_use_git_until_migration.md"
      via: "The STATE.md edit lifts the in-repo rule; user memory update is the executor's manual hand-off"
      pattern: "Post-BROWN-01"
---

<objective>
Close out Phase 6 by validating the migration end-to-end and recording the BROWN-01 / BROWN-02 evidence:

1. **Sibling-clone safety net (RESEARCH §BROWN-01 Dogfood Safety Strategy, lines 800-816):** Before touching THIS repo's `.planning/`, run the full migration in an isolated `git clone . ~/tmp/gsd-dogfood-$(date +%Y%m%d)`. Inside the clone: invoke `/gsd-migrate-vcs --target jj`, inspect `.planning/intel/06-migration-report.md`, then exercise every BROWN-01 brownfield command (`/gsd-map-codebase`, `/gsd-import`, `/gsd-ingest-docs`, `/gsd-resume-work`, `/gsd-pause-work`, `/gsd-ship`, `/gsd-pr-branch`, `/gsd-undo`). Record observed behavior in `.planning/intel/06-dogfood-log.md` (in THIS repo, not the clone — the clone is throwaway).

2. **Human checkpoint (blocking):** Surface the dogfood-log to the user and ask: "Did every brownfield command behave correctly in the sibling clone? Proceed with in-place migration of THIS repo?" Wait for explicit approval or modification request.

3. **(POST-CHECKPOINT) Real-repo flip OR deferred-flip recording:** If the user approves, run `/gsd-migrate-vcs --target jj` against THIS repo. The flip is bidirectional (CONTEXT D-03) — `/gsd-migrate-vcs --target git` reverses it if any issue surfaces. If the user defers, record the reason in `.planning/intel/06-dogfood-log.md` and skip the flip (BROWN-01 evidence still requires the sibling-clone validation; the in-place flip is the secondary act).

4. **Memory rule lift (RESEARCH §Memory-rule lift trigger):** Edit `.planning/STATE.md` to replace the `- **Pre-Phase-6:** Use git (not jj) until migration lands.` memory-rule annotation with `- **Post-BROWN-01 (Phase 6 plan 06-04):** Phase 6 migration validated on sibling clone. jj is now permitted in this repo.` This is the canonical trigger for future agents to stop refusing `jj` operations.

5. **BROWN-02 retro seed:** Create `.planning/intel/rebase-log.md` with the standard 3-column table header (Date / Conflicts / Notes) and a placeholder row. The first weekly upstream rebase post-migration populates a real row. Per RESEARCH §"BROWN-02 Retro File Shape" and CONTEXT §"Specifics" — single-row to start; format becomes a per-week journal if useful.

**Mode constraint (Assumption A10):** This plan is the FIRST that exercises `jj` against THIS repo (and only after the checkpoint). Pre-checkpoint operations use raw `git commit`. Post-checkpoint, IF the in-place flip lands, jj becomes permitted for subsequent work. The STATE.md memory-rule edit MUST land in a commit, and the commit MUST use raw `git commit` (not `jj squash`) because at commit time the adapter has just flipped — the memory-rule edit IS the last work needing the pre-flip rule and the first work proving the post-flip rule.

The plan is `autonomous: false` because of the blocking checkpoint in Task 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-CONTEXT.md
@.planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-RESEARCH.md
@.planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-PATTERNS.md
@.planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-01-SUMMARY.md
@.planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-02-SUMMARY.md
@.planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-03-SUMMARY.md
@./CLAUDE.md

<!-- Hand-off references from plans 06-02 + 06-03 -->
@sdk/src/vcs/format-migration/index.ts
@get-shit-done/workflows/migrate-vcs.md
@get-shit-done/workflows/new-project.md

<dogfood_safety_recap>
**Recap from RESEARCH §BROWN-01 Dogfood Safety Strategy (Three viable strategies + recommended sequence):**

- **Sibling clone** — first-pass safety net. `git clone . ~/tmp/gsd-dogfood-$DATE`. Isolated. Failure can't corrupt main repo. Rollback = delete the clone.
- **Read-only dry-run** — OUT OF SCOPE (CONTEXT §Deferred ideas excludes `--dry-run`).
- **Branch + jj inside branch** — second-pass option AFTER sibling clone validates; Pitfall 1 (jj + git-worktree colocate incompatibility) makes branch-checkout-and-flip risky on colocated repos.
- **jj op-log undo** — recovery mechanism post-flip; works only when the flip succeeded.

**Recommended sequence (RESEARCH lines 810-817):**

1. Sibling clone validation (this plan's Task 1).
2. Human go/no-go gate (this plan's Task 2 checkpoint).
3. Real-repo flip (this plan's Task 3; optional based on user decision).
4. BROWN-02 retro file seed (this plan's Task 4).
</dogfood_safety_recap>

<brownfield_command_inventory>
**BROWN-01 brownfield commands** (from REQUIREMENTS.md BROWN-01 and ROADMAP Phase 6 SC #5):

| Command | Validation method in sibling clone | Expected observable behavior |
|---------|-----------------------------------|------------------------------|
| `/gsd-map-codebase` | Run against the migrated clone's codebase | Identical map structure to git-side baseline (modulo timestamps) |
| `/gsd-import` | Re-run with a test source path | Produces the same `.planning/imports/*` artifacts |
| `/gsd-ingest-docs` | Run with a known docs source | Same `.planning/docs/*` ingest output |
| `/gsd-resume-work` | Invoke with an existing position | Reads `.planning/STATE.md` and resumes at the recorded `last_activity` |
| `/gsd-pause-work` | Invoke during a phase to checkpoint | Writes `.planning/STATE.md` updates atomically |
| `/gsd-ship` | Invoke against a finished phase | Tags / pushes (or surfaces dry-run preview if `--dry-run`) |
| `/gsd-pr-branch` | Invoke to materialize a code-only branch | Filters `.planning/`-only commits; new bookmark/branch created |
| `/gsd-undo` | Invoke to discard the most recent commit | Surgical `jj abandon` per Phase 5 plan 05-03 CMD-06 semantics |

For each: capture the command's stdout/stderr in `.planning/intel/06-dogfood-log.md`, compare against a parallel run in a git-side sibling clone (`git clone . ~/tmp/gsd-baseline-git-$DATE` left as git), and flag any difference as a Phase 6 bug.
</brownfield_command_inventory>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sibling-clone validation + brownfield-command exercise log</name>
  <files>.planning/intel/06-dogfood-log.md</files>
  <read_first>
    - .planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-RESEARCH.md §"BROWN-01 Dogfood Safety Strategy" (full section, lines 796-826)
    - get-shit-done/workflows/migrate-vcs.md (plan 06-03 deliverable)
    - .planning/REQUIREMENTS.md BROWN-01 row (line 130)
    - CLAUDE.md (the .envrc / gh CLI rules — only applies if dogfood validation exercises `/gsd-ship` with a push step)
  </read_first>
  <action>
**Step 1 — Set up the sibling clones (both git-side baseline + jj-side migrated):**

```bash
DATE=$(date +%Y%m%d)
git clone . ~/tmp/gsd-baseline-git-$DATE         # untouched git-side baseline
git clone . ~/tmp/gsd-dogfood-jj-$DATE           # will be migrated to jj
```

The git-side baseline is the comparison reference. The jj-side clone is the migration target.

**Step 2 — Run migration on the jj-side clone:**

```bash
cd ~/tmp/gsd-dogfood-jj-$DATE
# Initialize jj colocation in the clone (the source repo is git-only).
jj git init --colocate
# Run the migration command (plan 06-03 deliverable).
node /Users/LoganDark/Documents/Projects/get-shit-done/bin/gsd-sdk.js query migrate-vcs --target jj --force --cwd .
# Or via the slash-command if you're driving from Claude: /gsd-migrate-vcs --target jj --force
```

Inspect the emitted `.planning/intel/06-migration-report.md` in the clone. Capture key data points:
- Files scanned count
- Files changed count
- Orphans: total / ancestor-resolved / unresolvable
- Migration commit change_id (`jj log -r '@-' -T 'change_id ++ "\\n"' --no-graph`)

**Step 3 — Exercise each BROWN-01 brownfield command in BOTH clones in parallel:**

For each command in the brownfield_command_inventory table above, run the command in both clones and capture stdout/stderr:

```bash
# Pattern (substitute the command name):
( cd ~/tmp/gsd-baseline-git-$DATE && <command> ) > /tmp/baseline-<cmd>.out 2>&1
( cd ~/tmp/gsd-dogfood-jj-$DATE && <command> ) > /tmp/dogfood-<cmd>.out 2>&1
diff /tmp/baseline-<cmd>.out /tmp/dogfood-<cmd>.out
```

For commands that take input (e.g., `/gsd-import <source>`), use a synthetic source the dogfood-log records.

**Step 4 — Write `.planning/intel/06-dogfood-log.md` capturing:**

```markdown
# Phase 6 Plan 06-04 — Sibling-Clone Dogfood Log (BROWN-01)

**Date:** $(date)
**Operator:** $(whoami)
**Sibling clone path (jj):** ~/tmp/gsd-dogfood-jj-$DATE
**Sibling clone path (git baseline):** ~/tmp/gsd-baseline-git-$DATE
**Migration command:** `node bin/gsd-sdk.js query migrate-vcs --target jj --force --cwd .`

## Migration outcome on sibling clone

- Files scanned: <N>
- Files changed: <K>
- Orphans (ancestor-resolved): <A>
- Orphans (unresolvable): <U>
- Migration commit change_id: <cid>
- `.planning/intel/06-migration-report.md` contents: (capture inline OR copy verbatim)

## Brownfield command validation

| Command | Baseline git stdout | Dogfood jj stdout | Match? | Notes |
|---------|---------------------|-------------------|--------|-------|
| /gsd-map-codebase | <link or excerpt> | <link or excerpt> | ✓ / ✗ | <notes> |
| /gsd-import | ... | ... | ✓ / ✗ | ... |
| /gsd-ingest-docs | ... | ... | ✓ / ✗ | ... |
| /gsd-resume-work | ... | ... | ✓ / ✗ | ... |
| /gsd-pause-work | ... | ... | ✓ / ✗ | ... |
| /gsd-ship | ... | ... | ✓ / ✗ | ... |
| /gsd-pr-branch | ... | ... | ✓ / ✗ | ... |
| /gsd-undo | ... | ... | ✓ / ✗ | ... |

## Bugs surfaced

(List any divergence between the baseline and dogfood that constitutes a Phase 6 bug.
For each: file a triage entry. If blocking, halt at the checkpoint with a description of what
needs fixing before the in-place flip.)

## Verdict

(One of: GREEN — proceed to in-place flip. YELLOW — flip approved with caveats listed.
RED — do not flip; bugs above must be fixed first.)
```

Commit message: `docs(06-04): record sibling-clone dogfood evidence (BROWN-01)`. Raw `git commit` per CLAUDE.md memory rule (the rule has NOT yet been lifted — that happens in Task 3 post-checkpoint).
  </action>
  <verify>
    <automated>test -f /Users/LoganDark/Documents/Projects/get-shit-done/.planning/intel/06-dogfood-log.md &amp;&amp; grep -E "Brownfield command validation|Verdict" /Users/LoganDark/Documents/Projects/get-shit-done/.planning/intel/06-dogfood-log.md &amp;&amp; grep -cE "^\\| /gsd-(map-codebase|import|ingest-docs|resume-work|pause-work|ship|pr-branch|undo) \\|" /Users/LoganDark/Documents/Projects/get-shit-done/.planning/intel/06-dogfood-log.md</automated>
  </verify>
  <done>
    - Both sibling clones existed at `~/tmp/gsd-baseline-git-$DATE` and `~/tmp/gsd-dogfood-jj-$DATE` during execution (may be cleaned up after the log is written; the LOG is the persistent evidence).
    - The jj-side clone successfully ran the migration (committed `gsd-migrate-vcs v1`-marker commit visible in `jj log -r '@-'`).
    - `.planning/intel/06-dogfood-log.md` exists with the 8-row brownfield validation table populated AND a Verdict (GREEN / YELLOW / RED).
    - The `06-migration-report.md` from the sibling clone is summarised inline (orphan counts, file counts).
    - `lint-vcs-no-raw-git.cjs` exits 0 (raw jj commands inside `~/tmp/` are outside the lint scope by definition — the lint runs on THIS repo's tree).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human go/no-go gate — proceed with in-place migration of THIS repo?</name>
  <what-built>
Task 1 produced `.planning/intel/06-dogfood-log.md` documenting the sibling-clone validation outcome.

The validation exercised the migration command + all 8 BROWN-01 brownfield commands against a fresh clone of this repo. Any divergence from the git-side baseline is captured in the log.

This checkpoint is the canonical "trust the migration" gate. The user reviews the dogfood-log + the migration report from the clone and decides whether to flip THIS repo or defer.
  </what-built>
  <how-to-verify>
1. Read `.planning/intel/06-dogfood-log.md` end-to-end. Pay particular attention to:
   - The "Brownfield command validation" table — every row should be ✓ (match) for a clean GREEN verdict.
   - The "Bugs surfaced" section — any entries here block the flip.
   - The "Verdict" line — GREEN, YELLOW, or RED.

2. Inspect the sibling clone's migration report:
   ```bash
   DATE=$(date +%Y%m%d)
   cat ~/tmp/gsd-dogfood-jj-$DATE/.planning/intel/06-migration-report.md
   ```
   Look for unresolvable orphans (`[orphan:...]` markers in the "Unresolvable orphans" section). If ancestor-resolved orphans dominate, that's fine (CONTEXT D-01 designed for this). If unresolvable orphans appear, decide whether each is acceptable.

3. (Optional) Re-run one or two brownfield commands manually in the sibling clone if any row in the validation table looks suspicious:
   ```bash
   cd ~/tmp/gsd-dogfood-jj-$DATE
   # Re-run a command of interest, compare against the baseline clone.
   ```

4. Decide:
   - **APPROVED — flip this repo.** Proceed to Task 3 which runs `/gsd-migrate-vcs --target jj` against the real repo.
   - **APPROVED — defer the flip.** Proceed to Task 3 which records the deferral rationale in `.planning/intel/06-dogfood-log.md` and skips the actual flip. BROWN-01 evidence is the sibling-clone validation; the in-place flip can wait.
   - **REJECTED — fix bugs first.** Halt the plan. File issues for each bug surfaced. Loop back to plan 06-02 / 06-03 with revisions, then re-run Task 1.

5. Independent of the flip decision, decide:
   - **Lift the memory rule now?** The "use git not jj" rule should lift as soon as the sibling clone validates GREEN, regardless of whether THIS repo flips. The rule's purpose was to prevent corrupted-state risk during Phase 6 development; once the migration is validated externally that risk is gone.
  </how-to-verify>
  <resume-signal>
Reply with one of:
- `APPROVED — flip and lift memory rule` (Task 3 runs the in-place migration AND lifts the STATE.md memory rule)
- `APPROVED — lift memory rule, defer in-place flip [reason]` (Task 3 lifts the memory rule only; records the deferral reason)
- `REJECTED — [list of bugs to fix first]` (halt the plan; spin up a gap-closure pass on plans 06-02 / 06-03)
- Or describe specific concerns and request modifications.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-checkpoint — STATE.md memory-rule lift + (optional) in-place migration + commit</name>
  <files>.planning/STATE.md, .planning/intel/06-dogfood-log.md</files>
  <read_first>
    - .planning/STATE.md (full file — locate the "use git not jj" memory rule line; it lives in the Decisions block or the inline-memory-summary section near the top of "Accumulated Context")
    - .planning/intel/06-dogfood-log.md (the document the user just approved)
    - .planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-RESEARCH.md §"Memory-rule lift trigger" lines 819-826
    - get-shit-done/workflows/migrate-vcs.md (plan 06-03 deliverable — the workflow for the optional in-place flip)
  </read_first>
  <action>
**Step 1 — Search for the canonical "use git not jj" line in STATE.md:**

```bash
grep -n "use git not jj\|use git (not jj)\|Use git not jj\|Use git (not jj)" /Users/LoganDark/Documents/Projects/get-shit-done/.planning/STATE.md
```

The line may not be in STATE.md verbatim — the memory rule lives in the user's session memory at `~/.claude/projects/-Users-LoganDark-Documents-Projects-get-shit-done/memory/feedback_use_git_until_migration.md`. If STATE.md does NOT contain a literal "use git not jj" line, add a NEW line in the "Accumulated Context > Decisions" block reading:

```markdown
- **Post-BROWN-01 (Phase 6 plan 06-04, 2026-05-14):** Phase 6 migration validated on sibling clone. The earlier "use git (not jj) until migration lands" memory rule is LIFTED. jj is now permitted in this repo. (User memory at `~/.claude/projects/.../memory/feedback_use_git_until_migration.md` should also be updated — manual hand-off.)
```

If STATE.md DOES contain a "use git not jj" line (search casts a wide net), edit it in place to:

```markdown
- **Post-BROWN-01 (Phase 6 plan 06-04, 2026-05-14):** Phase 6 migration validated on sibling clone. jj is now permitted in this repo.
```

The KEY tokens future agents will grep for: `Post-BROWN-01` AND `jj is now permitted`. Make sure both are present in whatever phrasing lands.

**Step 2 — (CONDITIONAL on Task 2 user decision) Run the in-place migration:**

If the user signalled `APPROVED — flip and lift memory rule`:

```bash
# At the REAL repo (not the sibling clone).
cd /Users/LoganDark/Documents/Projects/get-shit-done
# First: jj git init --colocate if not already (THIS repo is git-only at this point).
jj git init --colocate
# Then: drive the migration via the slash command or directly.
node bin/gsd-sdk.js query migrate-vcs --target jj --force --cwd .
```

The migration produces:
- A new commit on `@-` containing the rewritten `.planning/` prose + the flipped config.json + `06-migration-report.md`.
- A populated `.planning/intel/06-migration-report.md` (in THIS repo now).
- `vcs.adapter` = 'jj' in `.planning/config.json`.

After the flip, subsequent commits CAN use `jj squash` per the lifted memory rule. The STATE.md edit from Step 1 SHOULD have been committed BEFORE this flip via raw `git commit` — but if it's still pending, this migration commit picks it up alongside the rewrites.

If the user signalled `APPROVED — lift memory rule, defer in-place flip`:

Skip the migration. Append to `.planning/intel/06-dogfood-log.md`:

```markdown
## In-place flip deferred

**Date:** 2026-05-14
**Reason:** <user-provided rationale from Task 2 resume-signal>
**Recovery path:** Re-run `/gsd-migrate-vcs --target jj` at any time. The dogfood-log above
constitutes BROWN-01 evidence regardless; the in-place flip is the secondary act.
```

**Step 3 — Update REQUIREMENTS.md status:**

In `.planning/REQUIREMENTS.md` lines 277-278 (BROWN-01, BROWN-02 status table rows), update:
- BROWN-01: `Pending (re-bucketed from Phase 5 per Phase 5 CONTEXT D-31)` → `Complete (Phase 6 plan 06-04; sibling-clone dogfood validated 2026-05-14; in-place flip <executed | deferred per dogfood-log>)`
- BROWN-02: `Pending (re-bucketed from Phase 5 per Phase 5 CONTEXT D-31)` → `In progress (Phase 6 plan 06-04 — rebase-log.md seeded; first weekly rebase TBD)`

**Step 4 — Commit:**

Commit message: `docs(06-04): lift memory rule post-BROWN-01; <flip | defer> in-place migration`. Use raw `git commit` for the STATE.md edit (the rule is lifting AT this commit; the next commit is the first that may use jj). If the in-place flip ran in Step 2, the migration commit itself is the next commit and may use whatever backend it landed on.
  </action>
  <verify>
    <automated>grep -E "Post-BROWN-01|jj is now permitted" /Users/LoganDark/Documents/Projects/get-shit-done/.planning/STATE.md &amp;&amp; grep -E "BROWN-01.*Complete|BROWN-02.*progress" /Users/LoganDark/Documents/Projects/get-shit-done/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - STATE.md contains the `Post-BROWN-01` memory-rule-lift annotation; `grep 'Post-BROWN-01' STATE.md` returns at least 1 hit.
    - REQUIREMENTS.md BROWN-01 row shows `Complete (Phase 6 plan 06-04 ...)`; BROWN-02 row shows `In progress (Phase 6 plan 06-04 — rebase-log.md seeded ...)`.
    - If user approved flip: `.planning/config.json` contains `"vcs.adapter": "jj"` AND a new commit on HEAD~ contains both the rewritten prose AND the config flip AND `.planning/intel/06-migration-report.md`.
    - If user deferred flip: `.planning/intel/06-dogfood-log.md` contains the "In-place flip deferred" section with the user's rationale.
  </done>
</task>

<task type="auto">
  <name>Task 4: BROWN-02 retro file seed (.planning/intel/rebase-log.md)</name>
  <files>.planning/intel/rebase-log.md</files>
  <read_first>
    - .planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-RESEARCH.md §"BROWN-02 Retro File Shape" lines 828-842
    - .planning/REQUIREMENTS.md BROWN-02 (line 131 / status row line 278)
  </read_first>
  <action>
Create `.planning/intel/rebase-log.md` per RESEARCH §"BROWN-02 Retro File Shape" — a per-week-append journal with a 3-column markdown table:

```markdown
# Upstream Rebase Log

**Purpose:** BROWN-02 — record outcome of weekly upstream rebases post-Phase-6 migration.
First-row appender each week. Format may evolve into a richer per-week journal if useful long-term.

| Date | Conflicts | Notes |
|------|-----------|-------|
| _(no entries yet — first weekly rebase will be recorded here)_ | | |

## Conventions

- One row per weekly upstream rebase attempt (regardless of whether it landed cleanly).
- "Conflicts" = number of files with merge/rebase conflicts during the rebase.
- "Notes" = brief retro: what conflicted, why, mechanical resolution vs. logic-level resolution, any
  pattern that suggests an upstream-tracking concern.

## Recovery if a rebase corrupts state

Per CONTEXT D-03 the migration is bidirectional. If a weekly rebase produces an unrecoverable state:
- `jj op restore <pre-rebase-op>` rolls back the working state (jj-side).
- `/gsd-migrate-vcs --target git` rewrites planning prose back to git SHAs if the rebase was so
  damaging that the workflow needs to flip back temporarily.

---
*Initialized: 2026-05-14 (Phase 6 plan 06-04).*
```

The first real entry (a populated row) is appended at the time of the next weekly upstream rebase by whichever agent runs that workflow. This plan just seeds the file structure.

Commit message: `docs(06-04): seed .planning/intel/rebase-log.md for BROWN-02`. Backend depends on whether Task 3's optional in-place flip ran — if it did, this commit may use `jj squash`; if not, raw `git commit`.
  </action>
  <verify>
    <automated>test -f /Users/LoganDark/Documents/Projects/get-shit-done/.planning/intel/rebase-log.md &amp;&amp; grep -cE "^\\| Date \\| Conflicts \\| Notes \\|$" /Users/LoganDark/Documents/Projects/get-shit-done/.planning/intel/rebase-log.md &amp;&amp; grep -E "no entries yet|first weekly rebase" /Users/LoganDark/Documents/Projects/get-shit-done/.planning/intel/rebase-log.md</automated>
  </verify>
  <done>
    - `.planning/intel/rebase-log.md` exists with the 3-column table header.
    - Placeholder row exists (matches `no entries yet` OR a real populated row if a rebase happened during plan execution).
    - File is committed and visible in `git log`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sibling clone path → migration command | The clone is in user's tmpdir; failure is contained but stdout/stderr are captured into THIS repo's dogfood-log |
| Migration command on THIS repo → `.planning/` rewrites | The in-place flip mutates THIS repo's planning files |
| STATE.md memory-rule edit | Determines whether future agents use jj or git for commits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | Sibling clone corrupted by migration → leaked into dogfood-log | accept | The clone is throwaway; failure contained in `~/tmp/`. The dogfood-log captures evidence as text; even a corrupted clone produces readable failure logs. |
| T-06-04-02 | Repudiation | User approves flip without reading dogfood-log carefully → bugs slip in | mitigate | Task 2 checkpoint explicitly asks the user to read the log and verdict line. Task 3 is gated entirely on the checkpoint's resume-signal. The resume-signal options include explicit rejection paths. |
| T-06-04-03 | DoS | `jj git init --colocate` on THIS repo refuses or corrupts state (RESEARCH Pitfall 1) | mitigate | Sibling-clone validation in Task 1 catches this before THIS repo is ever touched. RESEARCH Pitfall 1 specifically warns about worktree+colocate interaction; THIS repo is NOT a worktree (it's the main clone), so the Pitfall 1 scenario does not apply, but the safety net is the sibling clone validation either way. |
| T-06-04-04 | Tampering | STATE.md memory-rule edit accidentally edits an unrelated line | mitigate | Task 3 Step 1 specifies the EXACT text to grep for + the EXACT replacement / addition; the executor is required to verify via `grep 'Post-BROWN-01' STATE.md` in Task 3's `<done>`. |
| T-06-04-05 | Repudiation | The in-place flip's migration commit lacks the stable marker → future tooling can't detect prior migration | mitigate | Plan 06-02 builds the marker `[gsd-migrate-vcs v1]` into every migration commit; this plan inherits that automatically. |
| T-06-04-06 | DoS | In-place flip lands while user has uncommitted edits to .planning/ | mitigate | Plan 06-02's pre-flight refuses if dirty (unless `--force`). This task uses `--force` only in the sibling-clone validation; for the in-place flip on the real repo, executor must NOT pass `--force` blindly — if `gsd-sdk query init.migrate-vcs` reports `dirty: true`, executor must surface that to the user BEFORE running. (Add this as an explicit halt-condition in Task 3.) |
| T-06-04-07 | Information disclosure | The dogfood-log captures stdout/stderr from brownfield commands which may include filesystem paths or secrets | accept | The brownfield commands run in a throwaway clone; secrets in the clone's `.envrc` would be from THIS repo's `.envrc` and are user-known. The log is committed to `.planning/intel/` which is repo-tracked but not externally published; risk is the same as any prose file in `.planning/`. |
| T-06-04-08 | Tampering | rebase-log.md seeded with the wrong column shape → future entries break parser | mitigate | The seed file uses the exact 3-column shape from RESEARCH §"BROWN-02 Retro File Shape" lines 832-837. Future appenders mirror this shape; verify via the automated `grep` check in Task 4. |
| T-06-04-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO new packages. RESEARCH §"Package Legitimacy Audit" attests Phase 6 is zero-dep. |
</threat_model>

<verification>
- `.planning/intel/06-dogfood-log.md` exists with all 8 brownfield-command validation rows + Verdict.
- The user explicitly approved the post-checkpoint course of action via Task 2's resume-signal.
- `.planning/STATE.md` contains the `Post-BROWN-01` memory-rule-lift annotation.
- `.planning/REQUIREMENTS.md` BROWN-01 row marked `Complete`; BROWN-02 row marked `In progress`.
- `.planning/intel/rebase-log.md` exists with the 3-column header + placeholder row.
- If the in-place flip ran: `.planning/config.json` has `vcs.adapter: "jj"`; HEAD~ commit message contains `[gsd-migrate-vcs v1]`.
- If the in-place flip deferred: `.planning/intel/06-dogfood-log.md` records the deferral rationale.
- `lint-vcs-no-raw-git.cjs` exits 0.
</verification>

<success_criteria>
- BROWN-01 evidence is concrete and reviewable: every brownfield command was exercised against the migrated sibling clone and the outcome recorded in `.planning/intel/06-dogfood-log.md`.
- A human checkpoint preceded any modification of THIS repo's `.planning/` files (Task 2 blocking gate).
- The "use git not jj" memory rule is lifted via the documented STATE.md edit (Task 3 Step 1).
- BROWN-02 retro is seeded — the rebase-log.md file is ready to receive the first weekly entry.
- Phase 6 closes with the migration mechanism validated end-to-end (rewriter + adapter flip + 8 brownfield commands all green in the sibling clone).
</success_criteria>

<output>
After completion, create `.planning/phases/06-brownfield-jj-migration-sticky-vcs-adapter-flip-planning-sha/06-04-SUMMARY.md` documenting:
- Final dogfood-log verdict (GREEN / YELLOW / RED).
- Whether the in-place flip executed (with the migration commit's change_id or commit hash) or was deferred (with rationale).
- The user's Task 2 resume-signal verbatim.
- BROWN-01 status: Complete.
- BROWN-02 status: In progress (file seeded; awaiting first weekly rebase).
- Format-migration tracker (CONTEXT D-19) net-new-surfaces note: this plan introduces `.planning/intel/06-dogfood-log.md` (one-time validation log, NOT a migration target — no SHAs that need rewriting since the log captures BOTH backends' outputs in their native shapes for comparison) and `.planning/intel/rebase-log.md` (per-week journal, may accumulate SHAs over time — future migration runs handle it normally).
- Phase 6 closure: ALL 7 ROADMAP success criteria evaluable as evidence — point each to its supporting file/test.

Also update `.planning/ROADMAP.md` Phase 6 entry status from "0 plans" → "4/4 plans complete" + "Completed: 2026-05-14" (or whatever date).
</output>
