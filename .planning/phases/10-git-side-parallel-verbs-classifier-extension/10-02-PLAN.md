---
phase: 10-git-side-parallel-verbs-classifier-extension
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/vcs/git/parallel.ts
  - sdk/src/vcs/jj/parallel.ts
autonomous: true
requirements:
  - PARALLEL-01
  - PARALLEL-02
  - VCS-18
tags:
  - sidecar
  - parallel-dispatch
  - adapter-internal
must_haves:
  truths:
    - "sdk/src/vcs/git/parallel.ts exists as a NEW adapter-internal sidecar with two exported pure functions: performGitParallelDispatch and performGitParallelFanIn"
    - "performGitParallelDispatch returns a frozen pure-JSON ParallelDispatchHandle matching the locked Phase 9 shape — no closures, methods, Symbols, or class instances on the return"
    - "performGitParallelDispatch composes the existing workspace.add primitive via the opts.vcs.workspace DI seam supplied by the wire-in (mirrors jj-side dispatch pattern)"
    - "performGitParallelFanIn iterates handle.workspaces in a 2-parent merge loop, halts on the first conflict, and is re-callable via stateless merge-base --is-ancestor probe"
    - "performGitParallelFanIn signature is `(mainRepoRoot: string, handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult` — NO `vcs` DI arg (mirrors jj/parallel.ts:308-315 verbatim); the fanIn body issues raw git verbs (`merge --no-ff`, `worktree remove`, `branch -D`, `merge-base --is-ancestor`, `diff --name-only --diff-filter=U`, `rev-parse`, `status --porcelain`, `for-each-ref`) via direct vcsExec(mainRepoRoot, 'git', [...]) calls, exactly mirroring jj-side fanIn's direct vcsExec(mainRepoRoot, 'jj', [...]) usage at jj/parallel.ts:366"
    - "The merge-in-tree-conflict producer fires inside the fanIn loop body (NOT inside reap) and writes via appendIncomplete from ../jj/incomplete-work.js"
    - "The crashed-agent classifier writes IncompleteWorkEntry with reason='crashed-with-uncommitted-work' and changeIdShort=<branchTipShortSha> per D-06"
    - "No `import { spawnSync } from 'node:child_process'` exists in the file; vcsExec is the sole subprocess primitive"
    - "No `import` line targets `../backends/git.js` or any path under `../backends/` — sidecar discipline preserved"
    - "The dispatch worktree-create for-loop has an inline comment citing PITFALLS Pitfall 5 and D-05 verbatim, naming `spawnSync` as the structural protection"
    - "sdk/src/vcs/jj/parallel.ts exports `derivePhaseRoot` (one-line `function` → `export function` change at jj/parallel.ts:144) so the git-side sidecar can import the cross-backend helper without inlining"
  artifacts:
    - path: "sdk/src/vcs/git/parallel.ts"
      provides: "performGitParallelDispatch + performGitParallelFanIn adapter-internal sidecar (VCS-18 deliverable)"
      contains: "export function performGitParallelDispatch"
      min_lines: 150
    - path: "sdk/src/vcs/git/parallel.ts"
      provides: "performGitParallelFanIn re-callable per-branch merge loop with halt-on-conflict + crash classifier"
      contains: "export function performGitParallelFanIn"
    - path: "sdk/src/vcs/jj/parallel.ts"
      provides: "derivePhaseRoot exported for cross-backend reuse by git-side sidecar"
      contains: "export function derivePhaseRoot"
  key_links:
    - from: "sdk/src/vcs/git/parallel.ts performGitParallelFanIn"
      to: "sdk/src/vcs/jj/incomplete-work.ts::appendIncomplete"
      via: "import { appendIncomplete } from '../jj/incomplete-work.js'"
      pattern: "appendIncomplete\\("
    - from: "sdk/src/vcs/git/parallel.ts performGitParallelDispatch"
      to: "opts.vcs.workspace.add (DI seam from backends/git.ts:570-582)"
      via: "DI closure injected by the wire-in in Plan 10.03"
      pattern: "opts\\.vcs\\.workspace\\.add"
    - from: "sdk/src/vcs/git/parallel.ts performGitParallelFanIn"
      to: "raw git verbs via vcsExec (merge --no-ff, worktree remove, branch -D, merge-base --is-ancestor, etc.)"
      via: "direct vcsExec(mainRepoRoot, 'git', [...]) calls (mirrors jj/parallel.ts:366 pattern)"
      pattern: "vcsExec\\([^,]+,\\s*'git',"
    - from: "sdk/src/vcs/git/parallel.ts (import block)"
      to: "sdk/src/vcs/jj/parallel.ts::derivePhaseRoot"
      via: "import { derivePhaseRoot } from '../jj/parallel.js' after the one-line export of derivePhaseRoot is added in Task 0"
      pattern: "import.*derivePhaseRoot.*from.*'\\.\\./jj/parallel\\.js'"
---

<objective>
Ship sdk/src/vcs/git/parallel.ts — the adapter-internal sidecar housing the
two pure functions performGitParallelDispatch and performGitParallelFanIn. This
is the primary VCS-18 deliverable and the bulk of PARALLEL-01 + PARALLEL-02
git-side substrate.

The sidecar mirrors sdk/src/vcs/jj/parallel.ts structurally with ONE deliberate
divergence: the fanIn loop body iterates per workspace with 2-parent merges
instead of doing a single N-parent merge. All other concerns (validators
inline, vcsExec only, frozen Object.freeze returns, appendIncomplete queue
writes) are verbatim mirrors of the jj-side sidecar.

DI seam shape: performGitParallelDispatch consumes the workspace.add primitive
via the opts.vcs.workspace closure (mirroring jj-side dispatch at
jj/parallel.ts:164-173). performGitParallelFanIn does NOT receive a vcs DI
seam — it calls vcsExec(mainRepoRoot, 'git', [...]) directly for merge,
worktree remove, branch -D, merge-base --is-ancestor, etc. This mirrors the
jj-side fanIn at jj/parallel.ts:308-315 + 366 verbatim, where the body
issues raw `jj new -r ...` / `jj bookmark delete --` / `jj bookmark list`
through direct vcsExec without composing workspace.* primitives.

Purpose: Replaces the Phase 9 throwing stub at backends/git.ts:723-734 (the
wire-in lives in Plan 10.03, separately, to keep this plan focused on the
algorithm). The cross-backend FanInResult shape (D-13 LOCKED at Phase 9 D-08)
ships uniform on git here.

Output: One new file (~150-250 LOC TypeScript) with two exports, plus a
one-line export-keyword addition to sdk/src/vcs/jj/parallel.ts (Task 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-RESEARCH.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md
@.planning/phases/09-jj-side-parallel-verbs/09-CONTEXT.md
@.planning/research/PITFALLS.md

# Phase 9 sidecar — the verbatim structural mirror target.
# Executor SHOULD read this file end-to-end before writing parallel.ts —
# 80% of the new file is a structural copy with `jj` → `git` literal swaps.
# CRITICAL — note that jj-side fanIn at lines 308-543 does NOT compose
# workspace.merge or workspace.remove via the vcs DI seam; it calls vcsExec
# directly (see line 366 for the canonical `vcsExec(mainRepoRoot, 'jj', [...])`
# pattern). Git-side fanIn MUST mirror this exactly — no `opts.vcs.workspace.merge`
# composition inside the fanIn body.
@sdk/src/vcs/jj/parallel.ts

# Existing git primitives. The sidecar DOES compose workspace.add inside
# performGitParallelDispatch via the DI seam (mirroring jj-side dispatch
# at jj/parallel.ts:164-173). The sidecar DOES NOT compose workspace.merge
# or workspace.remove inside performGitParallelFanIn — fanIn issues raw
# git verbs through direct vcsExec calls.
@sdk/src/vcs/backends/git.ts

# Cross-backend queue file write API — used by both reason='merge-in-tree-conflict'
# (inside fanIn loop) and reason='crashed-with-uncommitted-work' (inside fanIn
# crashed-agent reap branch).
@sdk/src/vcs/jj/incomplete-work.ts

# Type contracts (already shipped Phase 9; consume without extending):
@sdk/src/vcs/types.ts

# vcsExec — sole subprocess primitive per D-16.
@sdk/src/vcs/exec.ts

# The orchestrator-tier merge loop body being lifted into TS. Consult for
# loop ordering, error handling, and cleanup sequencing semantics. Phase 10
# does NOT delete this file — Phase 11 does.
@bin/lib/worktree-safety.cjs

<interfaces>
<!-- Key types the executor needs. Locked at Phase 9 — consume without extending. -->
<!-- Extracted from sdk/src/vcs/types.ts (Phase 9 plan 01 shipped these). -->

From sdk/src/vcs/types.ts (RevisionExpr is the cross-backend revision pointer):
  export type RevisionExpr = ... (opaque tagged template object; constructed via expr.rev / expr.bookmark / expr.head)

From sdk/src/vcs/types.ts (ParallelDispatchOpts — input to dispatch):
  ParallelDispatchOpts has these fields the sidecar consumes:
    - phaseNumber: number (used to derive phaseRoot + manifest path)
    - mainBookmark: string (used in merge commit message and queue context)
    - plan: ReadonlyArray<{ agentId: string; workspacePath?: string; planId?: string; baseRef?: RevisionExpr; }>
    - maxConcurrency?: number (NOT honored in Phase 10 — see PARALLEL-06 Phase 11; ignore in dispatch body but DO NOT strip from the type ref)

From sdk/src/vcs/types.ts (ParallelDispatchHandle — return of dispatch + input to fanIn):
  ParallelDispatchHandle = Readonly<{
    phaseRoot: string;          // absolute path; computed via derivePhaseRoot
    phaseNumber: number;
    mainBookmark: string;
    manifest: string;            // absolute path to wave-worktree-manifest.json
    workspaces: ReadonlyArray<Readonly<{
      name: string;              // e.g. `phase-${NN}-subagent-${idx}` mirroring jj naming
      path: string;              // absolute workspace path on disk
      baseRev: string;           // commit_id (SHA) on git; change_id on jj (D-14 carry)
      agentId: string;           // matches plan[i].agentId; validated by validateAgentId
      baselineOpId?: string;     // reserved; populated `undefined` per Phase 9 D-06
    }>>;
  }>;

From sdk/src/vcs/types.ts (ParallelAgentResult — fanIn results arg):
  ParallelAgentResult = Readonly<{
    agentId: string;
    exitCode: number;
    lastChangeId?: string;
    stderr?: string;
  }>;

From sdk/src/vcs/types.ts (FanInResult — return of fanIn):
  FanInResult = Readonly<{
    merged: readonly string[];           // per-call (not cumulative); SHAs on git
    conflicted: boolean;                  // true when this call halted on conflict
    conflictedPaths: readonly string[];   // populated only when conflicted=true
    incompleteQueued: number;             // count of appendIncomplete calls this invocation
    failedReaped: readonly string[];      // agent NAMES (not branches, not paths)
    surplusBookmarks: readonly string[];  // worktree-agent-* branches surviving cleanup
  }>;

From sdk/src/vcs/types.ts (IncompleteWorkEntry — closed-union reason):
  IncompleteWorkEntry = Readonly<{
    subagentName: string;
    changeIdShort: string;       // 12-char short SHA on git, 12-char short change_id on jj
    workspacePath: string;
    reason: 'crashed-with-uncommitted-work' | 'merge-in-tree-conflict';
  }>;

From sdk/src/vcs/jj/incomplete-work.ts (the write API; Phase 9 CR-02 made it mkdir-safe):
  export function appendIncomplete(phaseRoot: string, entry: IncompleteWorkEntry): void;
  // Internally does mkdirSync(dirname(p), { recursive: true }) before append.

From sdk/src/vcs/exec.ts (subprocess primitive — sole API per D-16):
  export function vcsExec(cwd: string, bin: 'git'|'jj', argv: readonly string[]): ExecResult;
  // ExecResult: { exitCode: number; stdout: string; stderr: string; timedOut: boolean; error?: Error }
  // Wraps spawnSync — synchronous, blocks event loop (THIS IS THE D-05 PROTECTION).

performGitParallelDispatch DI seam shape (matches jj-side at jj/parallel.ts:164-173):
  opts: ParallelDispatchOpts & {
    mainRepoRoot: string;
    vcs: {
      workspace: {
        add(input: { path: string; baseRef?: RevisionExpr; name?: string }): unknown;
      };
    };
  };
  // NOTE: dispatch ONLY consumes workspace.add via the DI seam. The opts type
  // does NOT include workspace.merge or workspace.remove — those would only
  // matter for fanIn, which does NOT take a DI seam (see below).

performGitParallelFanIn signature (matches jj/parallel.ts:308-315 VERBATIM):
  export function performGitParallelFanIn(
    mainRepoRoot: string,
    handle: ParallelDispatchHandle,
    results: readonly ParallelAgentResult[],
  ): FanInResult;
  // NORMATIVE: fanIn has NO `vcs` arg. Mirror jj-side at lines 308-315.
  // The body calls `vcsExec(mainRepoRoot, 'git', ['merge', '--no-ff', '-m', msg, agentBookmark])`
  // and the other raw verbs directly — NO `vcs.workspace.merge` / `vcs.workspace.remove`
  // composition. This is the verified ground truth: jj-side fanIn at jj/parallel.ts:366
  // calls `vcsExec(mainRepoRoot, 'jj', ['new', '-r', ...])` directly. Git-side mirrors.
  //
  // Why: the wire-in template at backends/jj.ts:1257-1264 passes ONLY (cwd, handle, results)
  // to fanIn. The git-side wire-in (Plan 10.03) MUST mirror that exact shape, so the
  // git-side fanIn signature cannot accept a 4th arg.

derivePhaseRoot helper:
  TASK 0 in this plan exports derivePhaseRoot from sdk/src/vcs/jj/parallel.ts
  (one-line change: `function derivePhaseRoot(` → `export function derivePhaseRoot(`
  at jj/parallel.ts:144). Task 1 then imports it:
    import { derivePhaseRoot } from '../jj/parallel.js';
  The jj/ path is the cross-backend home for these shared adapter-internal
  helpers per ARCHITECTURE.md Integration Point #3 — same pre-existing pattern
  as backends/git.ts:33 importing readIncomplete from '../jj/incomplete-work.js'.
</interfaces>

<existing_primitives>
<!-- The workspace.add primitive composed by dispatch via DI. -->
<!-- workspace.merge / workspace.remove are documented for reference only — -->
<!-- they are NOT composed by fanIn (fanIn calls vcsExec directly per the jj mirror). -->

backends/git.ts:570-582  workspace.add({ path, baseRef?, name? })
  → wraps `git worktree add <path> [-b <name>] [<baseRef>]`; returns workspace handle
  → COMPOSED INSIDE dispatch's for-loop via the opts.vcs.workspace.add DI seam

backends/git.ts:660-705  workspace.merge({ branch, message, ff, mainBookmark, agentBookmark })
  → wraps `git merge --no-ff -m <msg> <branch>` with conflict detection regex
    (`/CONFLICT|Automatic merge failed/i` at line 683)
  → NOT composed by fanIn. fanIn issues `git merge --no-ff` directly via
    vcsExec(mainRepoRoot, 'git', ['merge', '--no-ff', '-m', msg, agentBookmark])
    and inspects exit code / stderr regex inline (mirrors jj-side fanIn's
    direct vcsExec to `jj new` at jj/parallel.ts:366).

backends/git.ts:706-715  workspace.remove(path, { force? })
  → wraps `git worktree remove [--force] <path>`
  → NOT composed by fanIn. fanIn issues `git worktree remove` directly via
    vcsExec(ws.path or mainRepoRoot, 'git', ['worktree', 'remove', wsPath]).
    Non-zero exit on dirty trees (D-07 refusal) surfaces via exitCode check;
    DO NOT throw — push agentBookmark to surplusBookmarks instead.

backends/git.ts:723-734  workspace.parallel = Object.freeze({ dispatch, fanIn })
  → currently a throwing stub
  → REPLACED IN PLAN 10.03 (not this plan) with the wire-in to the new sidecar
</existing_primitives>

<algorithm_pseudocode>
<!-- Authoritative algorithm for both functions. Translates 10-RESEARCH.md §"System -->
<!-- Architecture Diagram" + 10-PATTERNS.md §"Pattern 2" into one place. -->

performGitParallelDispatch(opts):
  // D-05 + Pitfall 4 (Pitfall 5 in PITFALLS.md): plain sync for-loop. The
  // MANDATORY inline comment at this loop's header cites PITFALLS Pitfall 5
  // and notes spawnSync as the structural protection (verbatim per CONTEXT D-05).
  let phaseTag = String(phaseNumber).padStart(2, '0')
  let slots = []
  for (let i = 0; i < plan.length; i++):
    let item = plan[i]
    let idx = i + 1
    validateAgentId(item.agentId)                                      // inline regex
    let workspaceName = `phase-${phaseTag}-subagent-${idx}`              // mirror octopus.ts:30
    let agentBookmark = `worktree-agent-${item.agentId}`                 // matches refname regex
    let wsPath = item.workspacePath ?? <default path under .git/gsd-workspaces>
    opts.vcs.workspace.add({ path: wsPath, baseRef: item.baseRef, name: agentBookmark })
    // After workspace.add, the agent's branch tip = baseRef. Capture for handle.
    let headSha = (vcsExec(mainRepoRoot, 'git', ['rev-parse', agentBookmark])).stdout.trim()
    slots.push({ agentId, planId, workspacePath: wsPath, workspaceName, headSha })

  // Manifest write — VCS-19 schema (mirror jj/parallel.ts:252-265, change backend literal).
  let manifestDir = mkdtempSync(join(tmpdir(), 'gsd-wave-manifest-'))
  let manifestPath = join(manifestDir, 'wave-worktree-manifest.json')
  let manifestBody = { worktrees: slots.map(s => ({
    agent_id: s.agentId,
    plan_id: s.planId,
    backend: 'git' as const,                                            // ← CHANGE FROM 'jj'
    worktree_path: s.workspacePath,
    branch: `worktree-agent-${s.agentId}`,
    expected_base: s.headSha,
    main_bookmark: opts.mainBookmark,
  })) }
  writeFileSync(manifestPath, JSON.stringify(manifestBody, null, 2), 'utf-8')

  // Frozen pure-JSON return — verbatim mirror of jj/parallel.ts:270-289.
  return Object.freeze({
    phaseRoot: derivePhaseRoot(mainRepoRoot, opts.phaseNumber),
    phaseNumber: opts.phaseNumber,
    mainBookmark: opts.mainBookmark,
    manifest: manifestPath,
    workspaces: Object.freeze(slots.map(s => Object.freeze({
      name: s.workspaceName,
      path: s.workspacePath,
      baseRev: s.headSha,
      agentId: s.agentId,
      baselineOpId: undefined as string | undefined,
    }))),
  }) satisfies ParallelDispatchHandle

performGitParallelFanIn(mainRepoRoot, handle, results):
  // NORMATIVE: fanIn signature has NO `vcs` DI arg — mirrors jj/parallel.ts:308-315.
  // All raw git verbs are invoked via direct vcsExec(mainRepoRoot, 'git', [...]) calls,
  // mirroring jj-side's direct vcsExec(mainRepoRoot, 'jj', [...]) usage at line 366.
  let phaseTag = String(handle.phaseNumber).padStart(2, '0')                          // W-1: bind phaseTag at top of body
  let merged: string[] = []
  let conflicted = false
  let conflictedPaths: string[] = []
  let incompleteQueued = 0
  let failedReaped: string[] = []
  let surplusBookmarks: string[] = []

  // STEP 1 — fan-in loop (D-01..D-04, D-08). Each iteration issues raw git
  // verbs DIRECTLY via vcsExec — NO workspace.merge / workspace.remove
  // composition (mirrors jj-side fanIn's direct `vcsExec(... 'jj', ['new', '-r', ...])`
  // at jj/parallel.ts:366; here the verb is `git merge --no-ff` instead).
  for (let ws of handle.workspaces):
    let agentBookmark = `worktree-agent-${ws.agentId}`

    // D-03 stateless re-call probe. Pass branch NAME (not SHA). Per Open Q3 /
    // Pitfall 3: any non-zero exit is "not ancestor → process" except
    // exit=128 / "Not a valid object name" which means "branch missing → skip".
    let probe = vcsExec(mainRepoRoot, 'git', ['merge-base', '--is-ancestor', agentBookmark, 'HEAD'])
    if (probe.exitCode === 0):
      continue   // already merged on a previous call — skip per Pitfall 3 (do NOT push to merged[])
    if (probe.exitCode === 128 || probe.stderr.includes('Not a valid object name')):
      continue   // branch was deleted on a prior call; definitively skip

    // D-01 + D-02 + D-08: 2-parent merge via direct `git merge --no-ff` call.
    // Run from the MAIN repo root cwd (the worktree-checked-out main is the
    // merge target). Inspect mergeResult.exitCode and stderr regex inline.
    let mergeRes = vcsExec(mainRepoRoot, 'git', [
      'merge', '--no-ff',
      '-m', `phase ${phaseTag} merge: ${ws.agentId}`,
      agentBookmark,
    ])
    let mergeConflicted = mergeRes.exitCode !== 0 &&
      /CONFLICT|Automatic merge failed/i.test(mergeRes.stderr + mergeRes.stdout)

    if (mergeConflicted):
      // D-02 halt + D-08 producer.
      let diff = vcsExec(mainRepoRoot, 'git', ['diff', '--name-only', '--diff-filter=U'])
      conflictedPaths = diff.exitCode === 0
        ? diff.stdout.split('\n').map(s => s.trim()).filter(s => s.length > 0)
        : []
      conflicted = true
      let tipSha = vcsExec(mainRepoRoot, 'git', ['rev-parse', agentBookmark]).stdout.trim()
      appendIncomplete(handle.phaseRoot, {
        subagentName: ws.name,
        changeIdShort: tipSha.slice(0, 12),
        workspacePath: ws.path,
        reason: 'merge-in-tree-conflict',
      })
      incompleteQueued += 1
      break   // D-02: halt the loop, tree left wedged with MERGE_HEAD set

    if (mergeRes.exitCode === 0):
      // Successful merge. Capture the post-merge HEAD SHA for merged[].
      let headSha = vcsExec(mainRepoRoot, 'git', ['rev-parse', 'HEAD']).stdout.trim()
      merged.push(headSha.slice(0, 12))

      // D-04 per-success cleanup. Issue `git worktree remove` directly (non-force
      // per D-07). On non-zero exit (D-07 dirty-tree refusal), do NOT throw —
      // record agentBookmark as surplus and continue.
      let removeRes = vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', ws.path])
      if (removeRes.exitCode !== 0):
        surplusBookmarks.push(agentBookmark)
      else:
        // Worktree gone — try to delete the agent's branch. Failure (e.g. branch
        // still checked out elsewhere) just means surplus; do NOT throw.
        let delRes = vcsExec(mainRepoRoot, 'git', ['branch', '-D', agentBookmark])
        if (delRes.exitCode !== 0):
          surplusBookmarks.push(agentBookmark)

  // STEP 2 — crashed-agent classifier (D-06 + D-07). Only runs on results whose
  // exitCode !== 0. Skips workspaces that already merged successfully above.
  for (let result of results):
    if (result.exitCode === 0) continue
    let ws = handle.workspaces.find(w => w.agentId === result.agentId)
    if (!ws) continue   // unknown agentId — shouldn't happen but skip defensively
    let agentBookmark = `worktree-agent-${ws.agentId}`
    let tipResult = vcsExec(mainRepoRoot, 'git', ['rev-parse', agentBookmark])
    if (tipResult.exitCode !== 0):
      // Branch missing — bookmark already cleaned up (e.g., agent crashed AFTER successful merge).
      failedReaped.push(ws.name)
      continue
    let tipSha = tipResult.stdout.trim()
    // Probe the worktree directory's WC dirtiness with `git status --porcelain`.
    let statusResult = vcsExec(ws.path, 'git', ['status', '--porcelain'])
    let cleanTree = statusResult.exitCode === 0 && statusResult.stdout.trim() === ''

    // D-06: branch tip == baseRev AND clean → no queue write (abandoned-style).
    if (tipSha === ws.baseRev && cleanTree):
      failedReaped.push(ws.name)
      continue

    // Otherwise (committed work OR dirty tree OR both) → queue entry.
    appendIncomplete(handle.phaseRoot, {
      subagentName: ws.name,
      changeIdShort: tipSha.slice(0, 12),
      workspacePath: ws.path,
      reason: 'crashed-with-uncommitted-work',
    })
    incompleteQueued += 1
    // D-07: try non-force cleanup via direct vcsExec; surface refusal as surplus, do not throw.
    let removeRes = vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', ws.path])
    // Dirty-tree refusal expected — worktree survives on disk for human inspection.
    // The workspacePath in the queue entry is the inspection handle per D-07 + Pitfall 3.
    failedReaped.push(ws.name)

  // STEP 3 — surplus-bookmark audit (CONTEXT recommendation: incremental + final).
  let listResult = vcsExec(mainRepoRoot, 'git', ['for-each-ref', '--format=%(refname:short)', 'refs/heads/worktree-agent-*'])
  if (listResult.exitCode === 0):
    let alive = listResult.stdout.split('\n').map(s => s.trim()).filter(s => s.length > 0)
    for (let bm of alive):
      if (!surplusBookmarks.includes(bm)):
        surplusBookmarks.push(bm)

  // Frozen pure-JSON return — verbatim mirror of jj/parallel.ts:535-542.
  return Object.freeze({
    merged: Object.freeze(merged.slice()) as readonly string[],
    conflicted,
    conflictedPaths: Object.freeze(conflictedPaths.slice()) as readonly string[],
    incompleteQueued,
    failedReaped: Object.freeze(failedReaped.slice()) as readonly string[],
    surplusBookmarks: Object.freeze(surplusBookmarks.slice()) as readonly string[],
  }) satisfies FanInResult
</algorithm_pseudocode>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Export derivePhaseRoot from sdk/src/vcs/jj/parallel.ts (one-line change)</name>
  <files>sdk/src/vcs/jj/parallel.ts</files>
  <read_first>
    - sdk/src/vcs/jj/parallel.ts (lines 140-160 — the existing private `function derivePhaseRoot(mainRepoRoot: string, phaseNumber: number): string { ... }` declaration at line 144; verify the function body is the 5-line `padStart` + `existsSync` + `readdirSync` + fallback `join` form documented in the planner's verified ground truth)
  </read_first>
  <action>
    Edit sdk/src/vcs/jj/parallel.ts:144. Change the declaration line
    `function derivePhaseRoot(mainRepoRoot: string, phaseNumber: number): string {`
    to
    `export function derivePhaseRoot(mainRepoRoot: string, phaseNumber: number): string {`

    This is a literal one-token addition (`export `) at the start of the
    declaration line. Do NOT modify the function body, the surrounding JSDoc
    comment block (lines 140-143), any other declaration in the file, or any
    existing call site (the private call sites inside this same file continue
    to resolve the same identifier — the symbol is now exported as well as
    locally visible).

    No other edits to jj/parallel.ts. Build MUST remain green.
  </action>
  <verify>
    <automated>grep -q "^export function derivePhaseRoot(mainRepoRoot: string, phaseNumber: number): string {" sdk/src/vcs/jj/parallel.ts || (echo "FAIL: derivePhaseRoot is not exported (line 144 not updated to 'export function derivePhaseRoot(...)')"; exit 1)</automated>
    <automated>grep -c "^function derivePhaseRoot(" sdk/src/vcs/jj/parallel.ts | grep -q "^0$" || (echo "FAIL: the un-exported declaration still exists (duplicate?); should be 0 matches for '^function derivePhaseRoot('"; exit 1)</automated>
    <automated>pnpm -C sdk build 2>&1 | tee /tmp/p10-02-task0-build.log | tail -3; grep -q "error TS\|Error:" /tmp/p10-02-task0-build.log && (echo "FAIL: TypeScript build errors after exporting derivePhaseRoot"; exit 1) || echo "OK: build clean"</automated>
    <automated>node -e "const m = require('./sdk/dist-cjs/vcs/jj/parallel.js'); if (typeof m.derivePhaseRoot !== 'function') { console.error('FAIL: derivePhaseRoot not exported from CJS dist'); process.exit(1); } console.log('OK: derivePhaseRoot exported from CJS dist');"</automated>
  </verify>
  <acceptance_criteria>
    - Line 144 of sdk/src/vcs/jj/parallel.ts reads exactly `export function derivePhaseRoot(mainRepoRoot: string, phaseNumber: number): string {`
    - No `^function derivePhaseRoot(` (un-exported) declaration remains in the file
    - The function body is byte-identical to the pre-edit body (no logic change)
    - `pnpm -C sdk build` exits 0
    - The compiled CJS dist at sdk/dist-cjs/vcs/jj/parallel.js exports `derivePhaseRoot` as a function
    - All Phase 9 tests that exercise jj-side parallel still pass (no behavioral change; export-keyword addition only)
  </acceptance_criteria>
  <done>derivePhaseRoot is now an exported symbol of sdk/src/vcs/jj/parallel.ts, ready for cross-import by the git-side sidecar in Task 1; jj-side build and tests unaffected.</done>
</task>

<task type="auto">
  <name>Task 1: Ship performGitParallelDispatch + module scaffolding (header, imports, validators)</name>
  <files>sdk/src/vcs/git/parallel.ts</files>
  <read_first>
    - sdk/src/vcs/jj/parallel.ts (lines 1-300 — header docblock, imports, validators, performJjParallelDispatch body). 80% verbatim mirror target. Note line 144 now exports derivePhaseRoot (Task 0 just made it so).
    - sdk/src/vcs/types.ts (locate ParallelDispatchOpts, ParallelDispatchHandle, RevisionExpr type exports — confirm field names match the <interfaces> block above)
    - sdk/src/vcs/exec.ts (lines 80-130 region around vcsExec — confirm signature `vcsExec(cwd, bin, argv): ExecResult` matches what the algorithm pseudocode calls)
    - sdk/src/vcs/expr.ts (lines 80-100 region around expr.bookmark — confirm signature; NOTE the git-side fanIn does NOT need expr.* because the merge call passes the branch NAME directly to `git merge --no-ff <bookmark>` rather than going through workspace.merge's RevisionExpr-shaped `branch` field)
    - sdk/src/vcs/jj/incomplete-work.ts (lines 60-90 region around appendIncomplete; confirm derivePhaseRoot is NOT exported here — it lives at jj/parallel.ts:144 which Task 0 just made `export`)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md (the §"Pattern 1: Sidecar discipline" and §"Manifest write pattern" sections)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md (D-05, D-13, D-14, D-16 — locked decisions enforced in this task)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-RESEARCH.md §"Common Pitfalls" Pitfall 4 (the .git/config.lock race — the MANDATORY inline comment text at the dispatch for-loop header)
  </read_first>
  <action>
    Create sdk/src/vcs/git/parallel.ts as a new file. The directory sdk/src/vcs/git/
    does not yet exist; create it as part of writing the file (the Write tool
    handles parent-directory creation).

    Structure the file in this order, mirroring sdk/src/vcs/jj/parallel.ts:

    1. Header docblock (mirror jj/parallel.ts:1-32). Adapt the framing to
       "Phase 10 (VCS-18, PARALLEL-01/02 git-side)" and cite the locked
       decisions D-01 (loop, not octopus), D-02 (halt on conflict), D-03
       (re-callable via merge-base --is-ancestor), D-04 (per-success cleanup),
       D-05 (plain sync for-loop, Pitfall 5 protection from spawnSync), D-13
       (FanInResult shape), D-14 (commit_id on git, change_id on jj — baseRev
       JSDoc), D-15 (consumes the 2-value reason enum without extending), D-16
       (vcsExec only, pure-JSON frozen returns).

    2. Imports (mirror jj/parallel.ts:34-52 — adapt to git): keep
       `writeFileSync`, `mkdtempSync` from 'node:fs'; `tmpdir` from 'node:os';
       `join` from 'node:path'. Import `vcsExec` from '../exec.js'. Import
       TYPES (ParallelDispatchOpts, ParallelDispatchHandle, ParallelAgentResult,
       FanInResult, IncompleteWorkEntry, RevisionExpr) from '../types.js' —
       type-only import. Import `appendIncomplete` from '../jj/incomplete-work.js'
       (cross-backend queue home — same pre-existing cross-import pattern at
       backends/git.ts:33). Import `derivePhaseRoot` from '../jj/parallel.js'
       (Task 0 just exported it).

       DO NOT import from '../backends/git.js' or any path under '../backends/'
       — sidecar discipline (UPSTREAM-02 mirror).
       DO NOT import { spawnSync } from 'node:child_process' — D-16 forbids
       raw subprocess; vcsExec is the sole primitive.
       DO NOT import { validateRefname } from '../refs-validator.js' — use
       the inline regex per sidecar-discipline rule (mirror jj/parallel.ts:92-98).
       DO NOT import `expr` from '../expr.js' — the git-side fanIn invokes
       `git merge --no-ff <agentBookmark>` directly via vcsExec, passing the
       branch NAME as a literal string argument; no RevisionExpr construction
       is needed in this file.

    3. Inline validator (verbatim from jj/parallel.ts:92-98):
       validateAgentId(name: string): void that throws if !/^[A-Za-z0-9._/-]+$/.test(name).
       Error message cites the worktree-safety manifest character class
       (matches the jj-side error text — substitute "see get-shit-done/bin/lib/worktree-safety.cjs:334"
       reference).

    4. export function performGitParallelDispatch(opts): ParallelDispatchHandle
       with the DI seam signature shown in <interfaces> (opts &
       { mainRepoRoot: string; vcs: { workspace: { add(...) } } }). Body
       implements the dispatch pseudocode from <algorithm_pseudocode>:

       a. Build phaseTag = String(opts.phaseNumber).padStart(2, '0').
       b. Initialize empty slots array.
       c. Plain `for (let i = 0; i < plan.length; i++)` loop. Add the MANDATORY
          inline comment immediately ABOVE the `for` keyword. The comment MUST
          cite "PITFALLS Pitfall 5" by name AND mention "`.git/config.lock`
          race" AND mention "spawnSync" as the structural protection AND state
          "any future refactor to async vcsExec MUST revisit this decision"
          per D-05's verbatim mandate. Single-block comment, multi-line OK.
       d. In each iteration: validateAgentId(item.agentId); compute
          workspaceName = `phase-${phaseTag}-subagent-${idx}`; compute
          agentBookmark = `worktree-agent-${item.agentId}`; compute wsPath
          from item.workspacePath (or a default — recommend
          `join(opts.mainRepoRoot, '.gsd-workspaces', workspaceName)` mirroring
          the jj-side workspaces[].path semantics). Call
          opts.vcs.workspace.add({ path: wsPath, baseRef: item.baseRef, name: agentBookmark }).
          Capture headSha via vcsExec(opts.mainRepoRoot, 'git', ['rev-parse',
          agentBookmark]).stdout.trim(). Push to slots.
       e. Write the manifest (mirror jj/parallel.ts:252-265 — see <algorithm_pseudocode>
          STEP 1 manifest write block). Change `backend: 'jj' as const`
          to `backend: 'git' as const`.
       f. Return Object.freeze({...}) satisfies ParallelDispatchHandle. The
          workspaces array uses inner Object.freeze per element (mirror
          jj/parallel.ts:270-289). baselineOpId is `undefined as string |
          undefined`. The `phaseRoot` field is
          `derivePhaseRoot(opts.mainRepoRoot, opts.phaseNumber)` (the Task 0
          export).

    5. Stub out performGitParallelFanIn at the bottom of the file as:
       `export function performGitParallelFanIn(mainRepoRoot: string, handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult { throw new Error('NOT YET IMPLEMENTED — Task 2 of Plan 10.02'); }`
       SIGNATURE EXACTLY MATCHES jj/parallel.ts:308-315 — no `vcs` DI arg.
       This stub exists so the wire-in plan can land an import statement
       against a guaranteed-resolvable export; Task 2 replaces the body.

    Indentation MUST be tabs (project convention per CLAUDE.md global
    "always use tabs for indentation unless impossible" and confirmed by
    inspecting sdk/src/vcs/jj/parallel.ts).

    No fenced code blocks, no markdown. This is a .ts file.

    After writing, the file MUST type-check under the existing tsconfig
    (verify with `pnpm tsc -p sdk --noEmit` if available; if the SDK doesn't
    use a project tsc invocation, fall back to `pnpm -C sdk build` and
    confirm parallel.ts is in the emitted dist-cjs tree without TS errors).
  </action>
  <verify>
    <automated>test -f sdk/src/vcs/git/parallel.ts || (echo "FAIL: sdk/src/vcs/git/parallel.ts does not exist"; exit 1)</automated>
    <automated>grep -q "^export function performGitParallelDispatch" sdk/src/vcs/git/parallel.ts || (echo "FAIL: performGitParallelDispatch export missing"; exit 1)</automated>
    <automated>grep -q "^export function performGitParallelFanIn" sdk/src/vcs/git/parallel.ts || (echo "FAIL: performGitParallelFanIn stub/export missing"; exit 1)</automated>
    <automated>grep -q "import.*derivePhaseRoot.*from '\\.\\./jj/parallel\\.js'" sdk/src/vcs/git/parallel.ts || (echo "FAIL: derivePhaseRoot import from '../jj/parallel.js' missing — Task 0 export must be consumed here"; exit 1)</automated>
    <automated>grep -v '^\s*\*\|^\s*//' sdk/src/vcs/git/parallel.ts | grep -c "spawnSync" | grep -q "^0$" || (echo "FAIL: spawnSync appears outside comments — D-16 forbids raw child_process; vcsExec is the sole primitive"; exit 1)</automated>
    <automated>grep -q "import.*from.*'\\.\\./backends/" sdk/src/vcs/git/parallel.ts && (echo "FAIL: sidecar imports from backends/ — UPSTREAM-02 discipline violated"; exit 1) || true</automated>
    <automated>grep -qE "Pitfall 5|config\\.lock" sdk/src/vcs/git/parallel.ts || (echo "FAIL: mandatory D-05 inline comment referencing PITFALLS Pitfall 5 / .git/config.lock missing"; exit 1)</automated>
    <automated>grep -q "Object.freeze" sdk/src/vcs/git/parallel.ts || (echo "FAIL: no Object.freeze — SDK shape law violated"; exit 1)</automated>
    <automated>pnpm -C sdk build 2>&1 | tee /tmp/p10-02-task1-build.log | tail -3; grep -q "error TS\|Error:" /tmp/p10-02-task1-build.log && (echo "FAIL: TypeScript build errors"; exit 1) || echo "OK: build clean"</automated>
  </verify>
  <acceptance_criteria>
    - File sdk/src/vcs/git/parallel.ts exists and exports `performGitParallelDispatch` and `performGitParallelFanIn`
    - Header docblock cites D-01, D-02, D-03, D-04, D-05, D-13, D-14, D-15, D-16 by name
    - Dispatch for-loop header has a comment block (single comment or contiguous comments) containing the literal substrings "Pitfall 5" and ".git/config.lock" and "spawnSync" — the inline-comment-at-loop-header mandate from D-05
    - No `import` line targets `../backends/` anywhere in the file
    - No `import { spawnSync }` or `from 'node:child_process'` in the file (vcsExec is the sole subprocess primitive per D-16)
    - `import { derivePhaseRoot } from '../jj/parallel.js'` is present (cross-import of the Task 0 export)
    - The manifest body literal in the dispatch body contains the substring `backend: 'git' as const`
    - The dispatch return statement contains `Object.freeze(` and `satisfies ParallelDispatchHandle`
    - The workspaces array elements in the return are individually wrapped with `Object.freeze(`
    - The dispatch return `phaseRoot` field is computed via a call to `derivePhaseRoot(opts.mainRepoRoot, opts.phaseNumber)` (not inlined)
    - The performGitParallelFanIn stub signature is exactly `performGitParallelFanIn(mainRepoRoot: string, handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult` — NO `vcs` arg (mirrors jj/parallel.ts:308-315)
    - `pnpm -C sdk build` exits 0 with no TypeScript errors
    - The performGitParallelFanIn body throws "NOT YET IMPLEMENTED — Task 2 of Plan 10.02" (or equivalent placeholder string)
  </acceptance_criteria>
  <done>sdk/src/vcs/git/parallel.ts exists with a complete performGitParallelDispatch implementation matching the locked decisions, sidecar discipline preserved, build passing, and a placeholder performGitParallelFanIn export (with the correct vcs-free signature) ready for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Ship performGitParallelFanIn (per-branch merge loop via direct vcsExec + halt-on-conflict + crash classifier + surplus audit)</name>
  <files>sdk/src/vcs/git/parallel.ts</files>
  <read_first>
    - sdk/src/vcs/git/parallel.ts (the file you wrote in Task 1; the performGitParallelFanIn stub is at the bottom)
    - sdk/src/vcs/jj/parallel.ts (lines 308-543 — the jj-side fanIn body; structural reference for validator-inline, vcsExec discipline, frozen FanInResult return; line 366 is the canonical `vcsExec(mainRepoRoot, 'jj', ['new', '-r', ...])` direct-verb pattern that git-side mirrors with `vcsExec(mainRepoRoot, 'git', ['merge', '--no-ff', ...])`; DIVERGES at the loop shape per D-01 — per-branch 2-parent merges instead of single N-parent merge)
    - sdk/src/vcs/backends/git.ts (lines 660-705 around `workspace.merge` — read for the `/CONFLICT|Automatic merge failed/i` regex precedent at line 683; the sidecar RE-USES this regex inline against vcsExec's stdout+stderr to detect conflicts; it does NOT call workspace.merge)
    - sdk/src/vcs/backends/git.ts (lines 706-715 around `workspace.remove` — read for documented exit-code semantics; the sidecar does NOT call workspace.remove, it issues `git worktree remove` directly via vcsExec and inspects exitCode)
    - bin/lib/worktree-safety.cjs (lines 417-516 — the cjs body Phase 10 is lifting; consult for loop ordering, error handling, cleanup sequencing semantics; specifically lines 471-509 for the merge → worktree remove → bookmark-cleanup-audit ordering)
    - sdk/src/vcs/jj/incomplete-work.ts (lines 60-90 — appendIncomplete is mkdir-safe per Phase 9 CR-02; callers do NOT pre-mkdir phaseRoot)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md (D-01..D-08 — the locked decisions enforced in this task; especially the §"Specific Ideas" re-call walk-through example for re-call semantics)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-RESEARCH.md §"Common Pitfalls" (Pitfalls 1-7 all apply to the fanIn loop body); §"Code Examples" for the verbatim conflict-path enumeration and ancestor-probe snippets; §"Open Questions (RESOLVED)" Q3 (pass branch name not SHA; treat exit 128 as skip) and Q1 (incremental + final audit on surplusBookmarks)
  </read_first>
  <action>
    Replace the performGitParallelFanIn stub body in sdk/src/vcs/git/parallel.ts
    with a complete implementation per the <algorithm_pseudocode> "STEP 1
    fan-in loop", "STEP 2 crashed-agent classifier", and "STEP 3 surplus audit"
    sections.

    NORMATIVE: the signature is `(mainRepoRoot: string, handle: ParallelDispatchHandle,
    results: readonly ParallelAgentResult[]): FanInResult` — NO `vcs` DI arg.
    All raw git verbs are invoked via DIRECT `vcsExec(mainRepoRoot, 'git',
    [...])` calls (or `vcsExec(ws.path, 'git', [...])` where the worktree's
    own cwd is required, e.g. `git status --porcelain` in the crash classifier).
    This mirrors jj-side fanIn at jj/parallel.ts:308-315 + 366 verbatim. DO NOT
    call `opts.vcs.workspace.merge(...)` or `opts.vcs.workspace.remove(...)`
    anywhere in the fanIn body — those DI seams do not exist on this signature.

    The body must:

    (1) Bind `const phaseTag = String(handle.phaseNumber).padStart(2, '0')` at
        the TOP of the body (W-1: needed for the merge commit message template
        literal `phase ${phaseTag} merge: ${ws.agentId}`).

    (2) Initialize local accumulators: merged: string[] = [], conflicted =
        false, conflictedPaths: string[] = [], incompleteQueued = 0,
        failedReaped: string[] = [], surplusBookmarks: string[] = [].

    (3) STEP 1 fan-in loop — iterate `handle.workspaces` with a plain for-of.
        For each ws:
        - Compute agentBookmark = `worktree-agent-${ws.agentId}`.
        - D-03 stateless probe: `vcsExec(mainRepoRoot, 'git', ['merge-base',
          '--is-ancestor', agentBookmark, 'HEAD'])`. If `probe.exitCode === 0`,
          `continue` (ancestor — already merged on prior call; do NOT push to
          merged[]). If `probe.exitCode === 128` OR `probe.stderr` matches
          /Not a valid object name/, `continue` (branch deleted by prior call).
          Otherwise (exit=1) process the workspace.
        - Issue the merge DIRECTLY:
          `const mergeRes = vcsExec(mainRepoRoot, 'git', ['merge', '--no-ff', '-m', `phase ${phaseTag} merge: ${ws.agentId}`, agentBookmark])`
          (mirrors jj/parallel.ts:366 `vcsExec(mainRepoRoot, 'jj', ['new', '-r', ...])`).
        - Compute `const mergeConflicted = mergeRes.exitCode !== 0 && /CONFLICT|Automatic merge failed/i.test(mergeRes.stderr + mergeRes.stdout)`
          (the precedent regex at backends/git.ts:683).
        - On mergeConflicted === true: enumerate paths via
          `vcsExec(mainRepoRoot, 'git', ['diff', '--name-only', '--diff-filter=U'])`;
          set conflictedPaths = split.trim.filter; set conflicted = true;
          capture tipSha via `vcsExec(mainRepoRoot, 'git', ['rev-parse',
          agentBookmark]).stdout.trim()`; call appendIncomplete(handle.phaseRoot,
          { subagentName: ws.name, changeIdShort: tipSha.slice(0, 12),
          workspacePath: ws.path, reason: 'merge-in-tree-conflict' });
          incompleteQueued += 1; `break` the loop. D-02: tree left wedged
          with MERGE_HEAD set.
        - On mergeRes.exitCode === 0 (clean merge): capture
          `const headSha = vcsExec(mainRepoRoot, 'git', ['rev-parse', 'HEAD']).stdout.trim()`
          and push `headSha.slice(0, 12)` to merged[]. Then issue
          `const removeRes = vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', ws.path])`
          (non-force per D-07). If removeRes.exitCode !== 0, push agentBookmark
          to surplusBookmarks (D-07 dirty-tree refusal expected; do NOT throw).
          Otherwise issue `const delRes = vcsExec(mainRepoRoot, 'git', ['branch',
          '-D', agentBookmark])`; if delRes.exitCode !== 0, push agentBookmark
          to surplusBookmarks (defensive; branch may still be checked out
          elsewhere).
        - Edge case (mergeRes.exitCode !== 0 but not conflicted by regex): this
          would be an unexpected git failure (e.g., I/O error). Treat the same
          as a conflict path — capture stderr into conflictedPaths via the
          /CONFLICT/ regex check (which won't match, so paths stays []), set
          conflicted = true, write a 'merge-in-tree-conflict' queue entry with
          the same shape, break the loop. (Inspect mergeRes.stderr verbatim in
          a JSDoc comment for the executor's reference; the regex guard above
          captures both legitimate-conflict and I/O-failure shapes without
          re-throwing.)

    (4) STEP 2 crashed-agent classifier — iterate `results`. For each result
        with result.exitCode !== 0:
        - Look up ws = handle.workspaces.find(w => w.agentId === result.agentId).
          If undefined, continue.
        - Compute agentBookmark same as above.
        - Probe branch tip via `vcsExec(mainRepoRoot, 'git', ['rev-parse',
          agentBookmark])`. If exitCode !== 0 (branch missing), push ws.name
          to failedReaped and continue.
        - Probe WC dirtiness via `vcsExec(ws.path, 'git', ['status',
          '--porcelain'])` — note the cwd is the WORKSPACE dir, not mainRepoRoot.
          cleanTree = (statusResult.exitCode === 0 && statusResult.stdout.trim() === '').
        - D-06: if tipSha === ws.baseRev AND cleanTree → push ws.name to
          failedReaped, continue.
        - Otherwise → appendIncomplete with reason='crashed-with-uncommitted-work',
          changeIdShort=tipSha.slice(0,12), workspacePath=ws.path,
          subagentName=ws.name. incompleteQueued += 1. Issue
          `vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', ws.path])` —
          on non-zero exit (D-07 dirty-tree refusal), do NOT re-throw, do NOT
          push to surplusBookmarks here (the workspacePath in the queue entry
          is the human-inspection handle per D-07 + Pitfall 3 in research/PITFALLS.md).
          Push ws.name to failedReaped.

    (5) STEP 3 surplus-bookmark audit — `vcsExec(mainRepoRoot, 'git',
        ['for-each-ref', '--format=%(refname:short)', 'refs/heads/worktree-agent-*'])`.
        On exitCode === 0, split.trim.filter into a list. For each entry not
        already in surplusBookmarks, push it (incremental + final dedup per
        CONTEXT recommendation).

    (6) Return Object.freeze({ merged: Object.freeze(merged.slice()) as
        readonly string[], conflicted, conflictedPaths:
        Object.freeze(conflictedPaths.slice()) as readonly string[],
        incompleteQueued, failedReaped: Object.freeze(failedReaped.slice())
        as readonly string[], surplusBookmarks:
        Object.freeze(surplusBookmarks.slice()) as readonly string[] })
        satisfies FanInResult.

    Remove the "NOT YET IMPLEMENTED" placeholder from Task 1; the file ends
    with this implementation.

    NORMATIVE — forbidden patterns:
      - NO `opts.vcs.workspace.merge(`, `vcs.workspace.merge(`, `workspace.merge(`
        anywhere in the fanIn body.
      - NO `opts.vcs.workspace.remove(`, `vcs.workspace.remove(`, `workspace.remove(`
        anywhere in the fanIn body.
      - NO 4th argument added to performGitParallelFanIn's signature.

    Indentation MUST be tabs.

    No fenced code blocks. After writing, `pnpm -C sdk build` MUST exit 0.
  </action>
  <verify>
    <automated>grep -q "NOT YET IMPLEMENTED" sdk/src/vcs/git/parallel.ts && (echo "FAIL: Task 1 placeholder still present in performGitParallelFanIn body"; exit 1) || true</automated>
    <automated>grep -q "const phaseTag = String(handle.phaseNumber).padStart(2, '0')" sdk/src/vcs/git/parallel.ts || (echo "FAIL: W-1 phaseTag binding missing at top of fanIn body"; exit 1)</automated>
    <automated>grep -qE "vcsExec\\([^,]+,\\s*'git',\\s*\\['merge',\\s*'--no-ff'" sdk/src/vcs/git/parallel.ts || (echo "FAIL: direct vcsExec('git', ['merge', '--no-ff', ...]) call missing — fanIn must NOT compose workspace.merge"; exit 1)</automated>
    <automated>grep -qE "vcsExec\\([^,]+,\\s*'git',\\s*\\['worktree',\\s*'remove'" sdk/src/vcs/git/parallel.ts || (echo "FAIL: direct vcsExec('git', ['worktree', 'remove', ...]) call missing — fanIn must NOT compose workspace.remove"; exit 1)</automated>
    <automated>grep -qE "vcsExec\\([^,]+,\\s*'git',\\s*\\['branch',\\s*'-D'" sdk/src/vcs/git/parallel.ts || (echo "FAIL: direct vcsExec('git', ['branch', '-D', ...]) call missing — agent-branch cleanup must use direct vcsExec"; exit 1)</automated>
    <automated>grep -qE "vcsExec\\([^,]+,\\s*'git',\\s*\\['merge-base',\\s*'--is-ancestor'" sdk/src/vcs/git/parallel.ts || (echo "FAIL: D-03 ancestor probe via direct vcsExec missing"; exit 1)</automated>
    <automated>grep -v '^\s*\*\|^\s*//' sdk/src/vcs/git/parallel.ts | grep -qE "(opts\\.vcs|vcs)\\.workspace\\.merge\\(" && (echo "FAIL: fanIn body composes workspace.merge — forbidden per the planner's revised algorithm (mirror jj/parallel.ts:366 direct vcsExec instead)"; exit 1) || true    <automated>grep -v '^\s*\*\|^\s*//' sdk/src/vcs/git/parallel.ts | grep -qE "(opts\\.vcs|vcs)\\.workspace\\.merge\\(" && (echo "FAIL: fanIn body composes workspace.merge — forbidden per the planner's revised algorithm (mirror jj/parallel.ts:366 direct vcsExec instead)"; exit 1) || true</automated>
    <automated>grep -v '^\s*\*\|^\s*//' sdk/src/vcs/git/parallel.ts | grep -qE "(opts\\.vcs|vcs)\\.workspace\\.remove\\(" && (echo "FAIL: fanIn body composes workspace.remove — forbidden per the planner's revised algorithm; issue 'git worktree remove' via direct vcsExec instead"; exit 1) || true</automated>
    <automated>grep -q "diff-filter=U" sdk/src/vcs/git/parallel.ts || (echo "FAIL: D-08 conflicted-paths enumeration (diff --name-only --diff-filter=U) missing"; exit 1)</automated>
    <automated>grep -q "'merge-in-tree-conflict'" sdk/src/vcs/git/parallel.ts || (echo "FAIL: D-08 'merge-in-tree-conflict' producer literal missing"; exit 1)</automated>
    <automated>grep -q "'crashed-with-uncommitted-work'" sdk/src/vcs/git/parallel.ts || (echo "FAIL: D-06 'crashed-with-uncommitted-work' producer literal missing"; exit 1)</automated>
    <automated>grep -q "for-each-ref.*worktree-agent" sdk/src/vcs/git/parallel.ts || (echo "FAIL: STEP 3 surplus-bookmark audit missing"; exit 1)</automated>
    <automated>grep -q "satisfies FanInResult" sdk/src/vcs/git/parallel.ts || (echo "FAIL: fanIn return missing satisfies FanInResult"; exit 1)</automated>
    <automated>grep -q "appendIncomplete(" sdk/src/vcs/git/parallel.ts || (echo "FAIL: appendIncomplete call missing"; exit 1)</automated>
    <automated>pnpm -C sdk build 2>&1 | tee /tmp/p10-02-task2-build.log | tail -3; grep -q "error TS\|Error:" /tmp/p10-02-task2-build.log && (echo "FAIL: TypeScript build errors after Task 2"; exit 1) || echo "OK: build clean"</automated>
    <automated>node -e "const m = require('./sdk/dist-cjs/vcs/git/parallel.js'); if (typeof m.performGitParallelDispatch !== 'function') { console.error('FAIL: performGitParallelDispatch not exported'); process.exit(1); } if (typeof m.performGitParallelFanIn !== 'function') { console.error('FAIL: performGitParallelFanIn not exported'); process.exit(1); } console.log('OK: both exports present in CJS dist');"</automated>
  </verify>
  <acceptance_criteria>
    - performGitParallelFanIn signature is exactly `(mainRepoRoot: string, handle: ParallelDispatchHandle, results: readonly ParallelAgentResult[]): FanInResult` — NO `vcs` DI arg (mirrors jj/parallel.ts:308-315 verbatim)
    - The body binds `const phaseTag = String(handle.phaseNumber).padStart(2, '0')` at the top so the merge-message template literal is bound (W-1)
    - The body contains a direct `vcsExec(mainRepoRoot, 'git', ['merge', '--no-ff', ...])` call — NOT `opts.vcs.workspace.merge(...)` (B-1: mirrors jj/parallel.ts:366 pattern)
    - The body contains a direct `vcsExec(mainRepoRoot, 'git', ['worktree', 'remove', ws.path])` call — NOT `opts.vcs.workspace.remove(...)` (B-1)
    - The body contains a direct `vcsExec(mainRepoRoot, 'git', ['branch', '-D', agentBookmark])` call for agent-branch cleanup
    - The body contains a direct `vcsExec(mainRepoRoot, 'git', ['merge-base', '--is-ancestor', agentBookmark, 'HEAD'])` D-03 probe
    - No occurrence of `opts.vcs.workspace.merge(` or `vcs.workspace.merge(` outside comments (forbidden per the planner's revised algorithm)
    - No occurrence of `opts.vcs.workspace.remove(` or `vcs.workspace.remove(` outside comments (forbidden)
    - Halt-on-conflict logic: conflicted=true → appendIncomplete(..., reason: 'merge-in-tree-conflict') → incompleteQueued += 1 → `break`
    - STEP 2 crashed-agent classifier iterates `results` filtering exitCode !== 0, distinguishes baseRev-tip+clean (no queue write) from dirty/divergent (appendIncomplete with reason: 'crashed-with-uncommitted-work'), and always pushes ws.name to failedReaped
    - STEP 3 surplus-bookmark audit calls `for-each-ref refs/heads/worktree-agent-*` via direct vcsExec and dedups against in-loop accumulation
    - Return is Object.freeze with all six fields per D-13 LOCKED shape; readonly-string-array fields are wrapped with Object.freeze(arr.slice())
    - `pnpm -C sdk build` exits 0
    - The compiled output at sdk/dist-cjs/vcs/git/parallel.js exports both performGitParallelDispatch and performGitParallelFanIn as functions (node -e require check passes)
    - The D-02 halt-on-conflict break statement appears in the body
    - File total length is between 150 and 350 lines (sanity bound)
  </acceptance_criteria>
  <done>performGitParallelFanIn body fully implemented with direct vcsExec calls (no workspace.{merge,remove} composition); signature mirrors jj/parallel.ts:308-315 exactly; D-01..D-08 + carry-forward decisions D-13..D-17 all reflected in code; sidecar discipline preserved; build clean; CJS dist exports verified.</done>
</task>

</tasks>

<verification>
  - sdk/src/vcs/git/parallel.ts exists with both exports
  - sdk/src/vcs/jj/parallel.ts:144 has `export function derivePhaseRoot(...)` (Task 0 one-line export)
  - Sidecar discipline: no import from ../backends/ or node:child_process
  - D-05 Pitfall 5 inline comment present at the dispatch for-loop header
  - performGitParallelFanIn signature mirrors jj/parallel.ts:308-315 verbatim — `(mainRepoRoot, handle, results): FanInResult` with NO `vcs` arg
  - fanIn body uses direct `vcsExec(mainRepoRoot, 'git', [...])` calls for `merge --no-ff`, `worktree remove`, `branch -D`, `merge-base --is-ancestor`, `diff --name-only --diff-filter=U`, `rev-parse`, `status --porcelain`, `for-each-ref` — NO workspace.{merge,remove} composition
  - D-13 FanInResult shape satisfied (six fields, all frozen)
  - D-15 IncompleteWorkEntry.reason enum NOT extended (only two literals used: 'merge-in-tree-conflict' and 'crashed-with-uncommitted-work')
  - D-16 vcsExec is the sole subprocess primitive (verified via grep of file body excluding comments)
  - `pnpm -C sdk build` exits 0
  - CJS dist at sdk/dist-cjs/vcs/git/parallel.js exports both functions
  - CJS dist at sdk/dist-cjs/vcs/jj/parallel.js exports derivePhaseRoot
  - Plan 10.03 (wire-in) can now resolve `import { performGitParallelDispatch, performGitParallelFanIn } from '../git/parallel.js'` in backends/git.ts
</verification>

<success_criteria>
  - PARALLEL-01 git-side substrate complete (dispatch returns frozen pure-JSON ParallelDispatchHandle; serialized via plain sync for-loop; manifest write at VCS-19 schema with backend: 'git')
  - PARALLEL-02 git-side substrate complete (fanIn iterates 2-parent merges via direct vcsExec, halts on conflict, idempotent via merge-base --is-ancestor stateless probe, per-success cleanup non-force, crashed-agent classifier writes correctly-reasoned queue entries)
  - VCS-18 deliverable shipped (new sdk/src/vcs/git/parallel.ts; the allowlist entry lands in Plan 10.03)
  - File is consumable by Plan 10.03's wire-in via `import { performGitParallelDispatch, performGitParallelFanIn } from '../git/parallel.js'`
  - Both algorithm strands (dispatch + fanIn) are implementation-complete; only the backend wire-in (Plan 10.03) and contract tests (Plan 10.04) remain
</success_criteria>

<output>
Create `.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-02-SUMMARY.md` when done. Summary captures: (1) the exact import set in parallel.ts (confirming no backends/ or node:child_process, and confirming the `derivePhaseRoot` cross-import from '../jj/parallel.js'); (2) the exact location/text of the D-05 Pitfall 5 inline comment at the dispatch for-loop header; (3) line count of the final file; (4) confirmation that performGitParallelFanIn signature matches the locked `(mainRepoRoot, handle, results)` shape with NO `vcs` arg (mirrors jj/parallel.ts:308-315); (5) the one-line export change applied to sdk/src/vcs/jj/parallel.ts:144 in Task 0; (6) any deviations from the pseudocode and their rationale; (7) `pnpm -C sdk build` exit code 0 confirmation.
</output>
