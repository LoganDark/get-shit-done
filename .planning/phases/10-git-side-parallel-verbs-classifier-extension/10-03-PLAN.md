---
phase: 10-git-side-parallel-verbs-classifier-extension
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02
files_modified:
  - sdk/src/vcs/backends/git.ts
  - scripts/lint-vcs-no-raw-git.allow.json
autonomous: true
requirements:
  - PARALLEL-01
  - PARALLEL-02
  - VCS-18
tags:
  - wire-in
  - lint-allowlist
must_haves:
  truths:
    - "backends/git.ts:723-734 throwing stub is REPLACED with a real wire-in delegating to performGitParallelDispatch + performGitParallelFanIn from ../git/parallel.js"
    - "The wire-in shape mirrors backends/jj.ts:1257-1264 verbatim — Object.freeze({ dispatch, fanIn }) with dispatch curry passing { mainRepoRoot: cwd, vcs: { workspace }, ...opts } and fanIn passing (cwd, handle, results)"
    - "VcsNotImplementedError import is either preserved (still used by other stubs in the file) or deleted (if no other site references it)"
    - "scripts/lint-vcs-no-raw-git.allow.json gains one new entry for sdk/src/vcs/git/parallel.ts with {path, reason, owner} schema (NO `expires` field per solo-dev override)"
    - "`scripts/lint-vcs-no-raw-git.cjs` exits 0 (the new entry covers the raw-git invocations introduced by the sidecar)"
    - "`scripts/lint-vcs-no-commit-id.cjs` exits 0 (the sidecar honors the v1.2 unified revision model)"
    - "`pnpm -C sdk build` exits 0"
  artifacts:
    - path: "sdk/src/vcs/backends/git.ts"
      provides: "Real workspace.parallel.{dispatch,fanIn} bodies — the cross-backend FanInResult shape ships uniform on git"
      contains: "performGitParallelDispatch"
    - path: "scripts/lint-vcs-no-raw-git.allow.json"
      provides: "Single new entry for the sidecar substrate"
      contains: "sdk/src/vcs/git/parallel.ts"
  key_links:
    - from: "sdk/src/vcs/backends/git.ts (top imports)"
      to: "sdk/src/vcs/git/parallel.ts (the sidecar from Plan 10.02)"
      via: "import { performGitParallelDispatch, performGitParallelFanIn } from '../git/parallel.js'"
      pattern: "from '\\.\\./git/parallel\\.js'"
    - from: "sdk/src/vcs/backends/git.ts:723-734 region (replacement target)"
      to: "the new wire-in body"
      via: "structural mirror of backends/jj.ts:1257-1264"
      pattern: "performGitParallelDispatch\\("
    - from: "scripts/lint-vcs-no-raw-git.allow.json entries[]"
      to: "sdk/src/vcs/git/parallel.ts"
      via: "single new {path, reason, owner} object"
      pattern: "sdk/src/vcs/git/parallel.ts"
---

<objective>
Wire the Plan 10.02 sidecar into the GitVcsAdapter by replacing the Phase 9
throwing stub at sdk/src/vcs/backends/git.ts:723-734 with a real
Object.freeze({ dispatch, fanIn }) delegating to performGitParallelDispatch
and performGitParallelFanIn. Add the single allowlist entry for
sdk/src/vcs/git/parallel.ts to scripts/lint-vcs-no-raw-git.allow.json (the
only +1 net diff this phase makes to the allowlist).

Purpose: Plan 10.02 ships the algorithm. Plan 10.03 makes it reachable via
`vcs.workspace.parallel.dispatch(...)` / `vcs.workspace.parallel.fanIn(...)`
on the git backend. Without this wire-in, the sidecar exists but is
unreachable; the cross-backend FanInResult contract (Phase 9 + Phase 10
same-PR coupling on the contract shape) is not actually shipped.

Output: Two edits — one ≈8-LOC replacement in backends/git.ts (plus a top-of-file
import line) and one JSON entry in the allowlist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-RESEARCH.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-02-SUMMARY.md

# The wire-in template — verbatim mirror target (change jj → git literals only).
@sdk/src/vcs/backends/jj.ts

# The current throwing stub site that this plan replaces.
@sdk/src/vcs/backends/git.ts

# The sidecar whose exports the wire-in imports — shipped by Plan 10.02.
@sdk/src/vcs/git/parallel.ts

# Allowlist schema and existing entries.
@scripts/lint-vcs-no-raw-git.allow.json

<interfaces>
<!-- The verbatim wire-in shape, taken from sdk/src/vcs/backends/jj.ts:1255-1264. -->
<!-- Confirmed live during planning. -->

From sdk/src/vcs/backends/jj.ts:1255-1264 (the template to mirror):

    // Phase 9 (VCS-16, PARALLEL-01/02): cross-backend parallel namespace.
    // Delegates to UPSTREAM-02 sidecar in sdk/src/vcs/jj/parallel.ts.
    parallel: Object.freeze({
      dispatch: (opts: ParallelDispatchOpts): ParallelDispatchHandle =>
        performJjParallelDispatch({ mainRepoRoot: cwd, vcs: { workspace }, ...opts }),
      fanIn: (
        handle: ParallelDispatchHandle,
        results: readonly ParallelAgentResult[],
      ): FanInResult => performJjParallelFanIn(cwd, handle, results),
    }),

Translation to git-side (swap symbols, comment text):

    // Phase 10 (VCS-18, PARALLEL-01/02): cross-backend parallel namespace.
    // Delegates to adapter-internal sidecar in sdk/src/vcs/git/parallel.ts.
    parallel: Object.freeze({
      dispatch: (opts: ParallelDispatchOpts): ParallelDispatchHandle =>
        performGitParallelDispatch({ mainRepoRoot: cwd, vcs: { workspace }, ...opts }),
      fanIn: (
        handle: ParallelDispatchHandle,
        results: readonly ParallelAgentResult[],
      ): FanInResult => performGitParallelFanIn(cwd, handle, results),
    }),

Import line (parallel to the existing `readIncomplete` cross-import at backends/git.ts:33):

  import { performGitParallelDispatch, performGitParallelFanIn } from '../git/parallel.js';

Place the import immediately AFTER the existing
`import { readIncomplete } from '../jj/incomplete-work.js';` line for
visual grouping (both are sidecar cross-imports).
</interfaces>

<allowlist_entry>
<!-- The single +1 entry. Schema {path, reason, owner} — NO `expires` field. -->
<!-- Reason text follows ARCHITECTURE.md Integration Point #4 recommendation, -->
<!-- with planner-tweaked wording matching Phase 10's PATTERNS.md reference. -->

New entry to add to scripts/lint-vcs-no-raw-git.allow.json's `entries` array:

{
  "path": "sdk/src/vcs/git/parallel.ts",
  "reason": "VCS adapter internals — git-side parallel-dispatch substrate; raw `git worktree`/`merge`/`branch -D` are the substrate the cross-backend `workspace.parallel.*` surface wraps.",
  "owner": "@LoganDark"
}

Placement: insert AFTER the existing
  { "path": "sdk/src/vcs/backends/git.ts", ... }
entry (line 7-ish) for grouped readability. The lint scanner reads the
array linearly, so order is not load-bearing — group for human-readability
only.

NO `expires` field — solo-dev override of REQUIREMENTS-LINT-02 per the
`$migration_note` at line 3 of the allowlist file (and per the project
memory `feedback_solo_dev_no_expires`).
</allowlist_entry>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the sidecar into backends/git.ts (replace throwing stub + add import)</name>
  <files>sdk/src/vcs/backends/git.ts</files>
  <read_first>
    - sdk/src/vcs/backends/git.ts (lines 28-40 for the existing import block; lines 720-740 for the current throwing stub region — locate the `parallel: Object.freeze({ dispatch(): never { throw new VcsNotImplementedError(...); }, fanIn(): never {...} })` block at 723-734)
    - sdk/src/vcs/backends/jj.ts (line 33-34 region — confirm the import line shape for performJj* (`import { performJjParallelDispatch, performJjParallelFanIn } from '../jj/parallel.js';`); lines 1250-1270 — the wire-in template at 1255-1264 that git-side mirrors verbatim with symbol/comment swaps)
    - sdk/src/vcs/git/parallel.ts (the Plan 10.02 deliverable — confirm both `performGitParallelDispatch` and `performGitParallelFanIn` are exported)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md §"sdk/src/vcs/backends/git.ts (MODIFY — wire-in replaces throwing stub)" — the explicit before/after diff
  </read_first>
  <action>
    Two edits in sdk/src/vcs/backends/git.ts:

    (1) Add the import line. Insert
    `import { performGitParallelDispatch, performGitParallelFanIn } from '../git/parallel.js';`
    immediately AFTER the existing line
    `import { readIncomplete } from '../jj/incomplete-work.js';` (which sits at
    approximately backends/git.ts:33 per the imports block confirmed during
    planning — verify the actual line by reading the file).

    (2) Replace the throwing stub at backends/git.ts:723-734 with the wire-in
    body. The stub currently reads (verified during planning):

      parallel: Object.freeze({
        dispatch(): never {
          throw new VcsNotImplementedError(
            'workspace.parallel.dispatch is not yet implemented on the git backend; Phase 10 ships the body',
          );
        },
        fanIn(): never {
          throw new VcsNotImplementedError(
            'workspace.parallel.fanIn is not yet implemented on the git backend; Phase 10 ships the body',
          );
        },
      }),

    Replace with (verbatim from the <interfaces> block above, git-side
    translation):

      // Phase 10 (VCS-18, PARALLEL-01/02): cross-backend parallel namespace.
      // Delegates to adapter-internal sidecar in sdk/src/vcs/git/parallel.ts.
      parallel: Object.freeze({
        dispatch: (opts: ParallelDispatchOpts): ParallelDispatchHandle =>
          performGitParallelDispatch({ mainRepoRoot: cwd, vcs: { workspace }, ...opts }),
        fanIn: (
          handle: ParallelDispatchHandle,
          results: readonly ParallelAgentResult[],
        ): FanInResult => performGitParallelFanIn(cwd, handle, results),
      }),

    Preserve the surrounding `workspace = Object.freeze({ ... })` block
    bracketing and the multi-line comment above the stub (the comment at
    lines ~717-722 about "Idiom mirrors the 8 existing VcsNotImplementedError
    callsites" can be DELETED as it documents the stub which no longer
    exists). Trailing comma on the `parallel: ...` entry — preserve the
    original comma style.

    Indentation: tabs (project convention). The surrounding code uses tabs
    in the editor representation; preserve.

    Check whether `VcsNotImplementedError` is still used elsewhere in
    backends/git.ts after the stub removal. If `grep -c "VcsNotImplementedError"
    sdk/src/vcs/backends/git.ts` returns 1 (only the import line remains),
    DELETE the import. If it returns >= 2 (used elsewhere), preserve the
    import. The import line is at approximately backends/git.ts:30 region
    (verify with grep).

    If the type-only imports `ParallelDispatchOpts`, `ParallelDispatchHandle`,
    `ParallelAgentResult`, `FanInResult` are NOT already imported in the
    type-imports block at the top of backends/git.ts (they should be —
    they were added in Phase 9 for the throwing stub), inspect the imports
    region (lines 30-55 region) and add them to the appropriate type-import
    grouping. The Phase 9 stub used these types in its `(): never` signature
    via type-only access — likely already imported but verify.
  </action>
  <verify>
    <automated>grep -q "import { performGitParallelDispatch, performGitParallelFanIn } from '../git/parallel.js';" sdk/src/vcs/backends/git.ts || (echo "FAIL: import line for Git sidecar exports missing"; exit 1)</automated>
    <automated>grep -q "performGitParallelDispatch({ mainRepoRoot: cwd, vcs: { workspace }, ...opts })" sdk/src/vcs/backends/git.ts || (echo "FAIL: dispatch wire-in body missing or signature mismatch"; exit 1)</automated>
    <automated>grep -q "performGitParallelFanIn(cwd, handle, results)" sdk/src/vcs/backends/git.ts || (echo "FAIL: fanIn wire-in body missing or signature mismatch"; exit 1)</automated>
    <automated>grep -c "workspace.parallel.dispatch is not yet implemented on the git backend" sdk/src/vcs/backends/git.ts | grep -q "^0$" || (echo "FAIL: throwing-stub message text still present — replacement incomplete"; exit 1)</automated>
    <automated>grep -c "workspace.parallel.fanIn is not yet implemented on the git backend" sdk/src/vcs/backends/git.ts | grep -q "^0$" || (echo "FAIL: throwing-stub fanIn message text still present"; exit 1)</automated>
    <automated>pnpm -C sdk build 2>&1 | tee /tmp/p10-03-task1-build.log | tail -3; grep -q "error TS\|Error:" /tmp/p10-03-task1-build.log && (echo "FAIL: TypeScript build errors after wire-in"; exit 1) || echo "OK: build clean"</automated>
    <automated>node -e "const m = require('./sdk/dist-cjs/vcs/backends/git.js'); const c = m.createGitAdapter || m.default || m; console.log('OK: git backend module loads after wire-in');"</automated>
  </verify>
  <acceptance_criteria>
    - The import line `import { performGitParallelDispatch, performGitParallelFanIn } from '../git/parallel.js';` exists exactly once in backends/git.ts
    - The strings `workspace.parallel.dispatch is not yet implemented on the git backend` and `workspace.parallel.fanIn is not yet implemented on the git backend` are completely absent from backends/git.ts
    - The wire-in body contains both expressions: `performGitParallelDispatch({ mainRepoRoot: cwd, vcs: { workspace }, ...opts })` and `performGitParallelFanIn(cwd, handle, results)`
    - The wire-in comment line `// Phase 10 (VCS-18, PARALLEL-01/02): cross-backend parallel namespace.` is present
    - `pnpm -C sdk build` exits 0 with no TypeScript errors
    - The compiled sdk/dist-cjs/vcs/backends/git.js loads via `node -e require(...)` without error (no runtime resolution failures from the new import)
    - `VcsNotImplementedError` is either: (a) still imported AND still used by at least one other site in the same file, or (b) NOT imported AND NOT referenced anywhere in the file. The post-edit state of `grep -c "VcsNotImplementedError" sdk/src/vcs/backends/git.ts` is internally consistent (no orphan import; no missing import)
  </acceptance_criteria>
  <done>Throwing stub at backends/git.ts:723-734 replaced with real wire-in to the sidecar; import line added; type-imports consistent; build green; CJS dist loads.</done>
</task>

<task type="auto">
  <name>Task 2: Add the single allowlist entry for sdk/src/vcs/git/parallel.ts</name>
  <files>scripts/lint-vcs-no-raw-git.allow.json</files>
  <read_first>
    - scripts/lint-vcs-no-raw-git.allow.json (the entire file — short, well under 100 lines; locate the `entries` array and the existing entry for `sdk/src/vcs/backends/git.ts` at approximately line 7)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md §"scripts/lint-vcs-no-raw-git.allow.json (MODIFY — net +1 entry)" — the exact schema and recommended reason text
    - scripts/lint-vcs-no-raw-git.cjs (the lint script that reads this allowlist; verify it tolerates `{path, reason, owner}` entries without `expires` — confirmed by the `$migration_note` at line 3 of the JSON file)
  </read_first>
  <action>
    Add one new object to the `entries` array in scripts/lint-vcs-no-raw-git.allow.json.
    Insert it immediately AFTER the existing entry for `sdk/src/vcs/backends/git.ts`
    (at approximately line 7 of the file) for grouped readability.

    The new entry (exact verbatim — copy from the <allowlist_entry> block in
    <context>):

      {
        "path": "sdk/src/vcs/git/parallel.ts",
        "reason": "VCS adapter internals — git-side parallel-dispatch substrate; raw `git worktree`/`merge`/`branch -D` are the substrate the cross-backend `workspace.parallel.*` surface wraps.",
        "owner": "@LoganDark"
      }

    Schema: three fields exactly — `path`, `reason`, `owner`. NO `expires`
    field (solo-dev override per `$migration_note` at line 3; per project
    memory feedback_solo_dev_no_expires).

    Preserve JSON validity: trailing comma after the new entry (it's not the
    last array element; the surrounding entries each terminate with a
    comma); tab-indented per project convention; same indent depth as
    existing entries.

    Use `path` (not `glob`) — single-file entry per existing precedent at
    the other in-tree allowlist entries.

    Do NOT touch any of the existing 23 production entries. The lint
    framing-locked invariant from STATE.md ("Lint allowlist framing: net
    change is +0 or +1") — this plan executes the +1 case.
  </action>
  <verify>
    <automated>node -e "const a = require('./scripts/lint-vcs-no-raw-git.allow.json'); if (!Array.isArray(a.entries)) { console.error('FAIL: entries is not an array'); process.exit(1); } const e = a.entries.find(x => x.path === 'sdk/src/vcs/git/parallel.ts'); if (!e) { console.error('FAIL: new entry for sdk/src/vcs/git/parallel.ts missing'); process.exit(1); } if (!e.reason || !e.owner) { console.error('FAIL: new entry missing reason or owner field'); process.exit(1); } if ('expires' in e) { console.error('FAIL: new entry has forbidden expires field (solo-dev override)'); process.exit(1); } console.log('OK: new entry well-formed:', e);"</automated>
    <automated>node -e "const a = require('./scripts/lint-vcs-no-raw-git.allow.json'); const count = a.entries.filter(x => x.path === 'sdk/src/vcs/git/parallel.ts').length; if (count !== 1) { console.error('FAIL: expected exactly 1 entry for sdk/src/vcs/git/parallel.ts, got', count); process.exit(1); } console.log('OK: exactly one entry for the sidecar');"</automated>
    <automated>node scripts/lint-vcs-no-raw-git.cjs 2>&1 | tee /tmp/p10-03-task2-lint.log; grep -q "violations\|FAIL\|Error" /tmp/p10-03-task2-lint.log && (echo "FAIL: lint-vcs-no-raw-git.cjs found violations after wire-in + allowlist update"; exit 1) || echo "OK: lint clean"</automated>
    <automated>node scripts/lint-vcs-no-commit-id.cjs 2>&1 | tee /tmp/p10-03-task2-commitid.log; grep -q "violations\|FAIL" /tmp/p10-03-task2-commitid.log && (echo "FAIL: v1.2 unified revision model lint failed (commit_id leakage in the new sidecar)"; exit 1) || echo "OK: commit-id lint clean"</automated>
  </verify>
  <acceptance_criteria>
    - JSON parses successfully (`node -e "require('./scripts/lint-vcs-no-raw-git.allow.json')"` exits 0)
    - The entries array contains exactly one entry where `path === "sdk/src/vcs/git/parallel.ts"`
    - That entry has fields `path`, `reason`, `owner` and DOES NOT have `expires`
    - The reason field mentions "VCS adapter internals" and "git-side parallel-dispatch substrate"
    - `node scripts/lint-vcs-no-raw-git.cjs` exits 0 (no raw-git violations after the +1 allowlist entry)
    - `node scripts/lint-vcs-no-commit-id.cjs` exits 0 (the new sidecar does not leak commit_id from any cross-backend surface)
    - Total entry count in the file is N+1 where N was the pre-edit count (verify by recording pre-edit count first if needed — typically 23 → 24)
    - All 23 existing production entries are unchanged (`git diff` / `jj diff` of the allowlist file shows ONLY the new entry insertion; no edits to existing entries)
  </acceptance_criteria>
  <done>Allowlist has exactly one new entry for the sidecar; lint scripts exit 0; no other entries modified.</done>
</task>

</tasks>

<verification>
  - Plan 10.02 sidecar is now reachable via `vcs.workspace.parallel.dispatch(...)` and `vcs.workspace.parallel.fanIn(...)` on a GitVcsAdapter
  - All Phase 9 throwing-stub framing is removed; the cross-backend FanInResult contract ships uniform on git
  - Allowlist net diff is exactly +1 entry (the framing-locked +0/+1 invariant from STATE.md is honored at the +1 case)
  - `pnpm -C sdk build` exits 0
  - `node scripts/lint-vcs-no-raw-git.cjs` exits 0
  - `node scripts/lint-vcs-no-commit-id.cjs` exits 0
  - Existing test suite (whatever runs in the SDK pre-Plan-10.04) still passes (`pnpm -C sdk test` should be no-worse-than-baseline; full TEST-13 git coverage lands in Plan 10.04)
</verification>

<success_criteria>
  - The git backend's `parallel` namespace is real (not throwing) and routes correctly to the new sidecar
  - The single allowlist diff is in place
  - Both CI-blocking lints (`lint-vcs-no-raw-git.cjs` and `lint-vcs-no-commit-id.cjs`) remain green
  - The dist-cjs build emits the wire-in correctly (verified by `node -e require(...)` of the compiled output)
  - Plan 10.04 can now author contract tests that exercise the wired adapter end-to-end
</success_criteria>

<output>
Create `.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-03-SUMMARY.md` when done. Summary captures: (1) the exact line number where the import was inserted; (2) whether `VcsNotImplementedError` import was preserved or deleted (with grep count justification); (3) the pre-edit and post-edit `entries.length` from the allowlist JSON; (4) confirmation of all three exit-0 verifications (pnpm build, lint-no-raw-git, lint-no-commit-id).
</output>
