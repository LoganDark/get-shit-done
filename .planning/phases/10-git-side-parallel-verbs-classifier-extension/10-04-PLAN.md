---
phase: 10-git-side-parallel-verbs-classifier-extension
plan: 04
type: execute
wave: 3
depends_on:
  - 10-03
files_modified:
  - sdk/src/vcs/__tests__/cmd-parallel-git.test.ts
autonomous: true
requirements:
  - TEST-13
  - TEST-15
  - TEST-16
tags:
  - contract-tests
  - test-infrastructure
must_haves:
  truths:
    - "sdk/src/vcs/__tests__/cmd-parallel-git.test.ts exists with Pattern A `describe.sequential.skipIf(!gitAvailable)` lifecycle per scenario"
    - "Pattern B random-prefix `mkdtemp` is used for fixture isolation per Pitfall 9 / TEST-16"
    - "The N=2, N=3, N=4 clean-fan-in scenarios each assert `merged.length === N`, `conflicted === false`, `surplusBookmarks.length === 0`"
    - "The in-tree-conflict scenario asserts ALL THREE of: `conflicted === true`, `conflictedPaths.length > 0`, queue entry with `reason === 'merge-in-tree-conflict'` — in ONE `it` block (W3 joint-assertion lock-in per Pitfall 7 / D-17 carry)"
    - "The crashed-worker scenario asserts `failedReaped` contains the agent name AND a queue entry has `reason === 'crashed-with-uncommitted-work'`"
    - "The idempotency re-call scenario (git-side-unique, absorbs TEST-15 per D-11) calls fanIn twice with the same handle: first call returns merged with the pre-conflict successes + conflicted=true; user-side resolution via raw `git add` + `git commit`; second call returns merged with the post-conflict successes (per-call, not cumulative) + conflicted=false"
    - "scripts/check-skip-count.cjs exits 0 (vitest skip-count baseline unchanged — no `describe.skip` or `it.skip` introduced)"
    - "No `retry: N` option appears anywhere in vitest.config.ts (TEST-16 LOCKED)"
    - "`pnpm -C sdk vitest run sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` exits 0 on a machine with git available"
  artifacts:
    - path: "sdk/src/vcs/__tests__/cmd-parallel-git.test.ts"
      provides: "TEST-13 git contract tests (N=2/3/4 dispatch, clean fan-in, in-tree-conflict scenario, crashed-worker scenario, idempotency re-call) + TEST-15 happy-path subsumption + TEST-16 Pattern B mkdtemp + no-retry"
      contains: "describe.sequential.skipIf"
      min_lines: 250
  key_links:
    - from: "sdk/src/vcs/__tests__/cmd-parallel-git.test.ts setupGitRepo()"
      to: "the test scenarios"
      via: "fresh tmpdir per scenario via mkdtempSync with random prefix"
      pattern: "mkdtempSync"
    - from: "the conflict scenario assertions"
      to: "sdk/src/vcs/jj/incomplete-work.ts::readIncomplete"
      via: "import readIncomplete from incomplete-work module; call after fanIn to inspect the queue file"
      pattern: "readIncomplete\\("
    - from: "scenario fanIn calls"
      to: "the git backend's workspace.parallel namespace (wired in Plan 10.03)"
      via: "createGitAdapter() at fixture setup; call adapter.workspace.parallel.dispatch(...) and adapter.workspace.parallel.fanIn(...)"
      pattern: "\\.workspace\\.parallel\\.(dispatch|fanIn)"
---

<objective>
Ship sdk/src/vcs/__tests__/cmd-parallel-git.test.ts — the TEST-13 git contract
suite. Structural mirror of sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts
(430 lines, already shipped Phase 9) with: (a) `git` substituted for `jj` in
fixture setup; (b) the TEST-14 `divergent()` topology assertion DROPPED
(jj-specific, has no git equivalent per D-11); (c) a NEW idempotency re-call
scenario absorbing TEST-15 per D-11 reframing.

Purpose: Proves the end-to-end git-side parallel-dispatch contract. Without
TEST-13 coverage, the wire-in from Plan 10.03 is unverified — the cross-backend
FanInResult contract has no executable proof.

Output: One new file (~250-350 LOC) under sdk/src/vcs/__tests__/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-RESEARCH.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-02-SUMMARY.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-03-SUMMARY.md

# The verbatim structural template — 430 lines, every scenario mirrors here.
@sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts

# The wired adapter the tests exercise (Plan 10.03 wired the parallel namespace).
@sdk/src/vcs/backends/git.ts

# The sidecar the wire-in routes to (Plan 10.02).
@sdk/src/vcs/git/parallel.ts

# Read API for the queue file (assertions in conflict + crash scenarios).
@sdk/src/vcs/jj/incomplete-work.ts

@scripts/check-skip-count.cjs

<scenarios>
<!-- Authoritative scenario list. Five test scenarios. -->

Scenario 1: N=2 clean dispatch + clean fan-in
  - Setup: setupGitRepo(); createGitAdapter(dir); seed commit on main.
  - Action: dispatch with plan=[{agentId:'a',planId:'a',baseRef:expr.bookmark('main')}, {agentId:'b',planId:'b',baseRef:expr.bookmark('main')}, phaseNumber=10, mainBookmark:'main'].
    Each agent makes a non-conflicting change in its worktree (e.g. agent-a touches a.txt and commits; agent-b touches b.txt and commits) via raw git commands in the test (the test file IS allowlisted for raw git — verify after writing).
  - fanIn(handle, [{agentId:'a',exitCode:0},{agentId:'b',exitCode:0}]).
  - Assertions: result.merged.length === 2; result.conflicted === false;
    result.conflictedPaths.length === 0; result.incompleteQueued === 0;
    result.failedReaped.length === 0; result.surplusBookmarks.length === 0
    (incremental cleanup successful).
  - Lifecycle: own describe.sequential.skipIf(!gitAvailable) block; beforeAll
    setupGitRepo; afterAll rmSync the dir.

Scenario 2: N=3 clean dispatch + clean fan-in (TEST-15 reframed coverage)
  - Same shape as Scenario 1, N=3.
  - Critical assertion (TEST-15 per D-11): result.merged.length === 3 (three
    successful 2-parent merges produce three entries in merged[]). This is the
    REQ-ID-covering assertion that TEST-15's reframed wording calls out.

Scenario 3: N=4 clean dispatch + clean fan-in
  - Same shape as Scenario 1, N=4. result.merged.length === 4.

Scenario 4: N=2 in-tree-conflict on second agent (W3 joint-assertion lock-in)
  - Setup: setupGitRepo(); both agents modify the SAME line of the same file
    differently (forces conflict on the second merge — the first will succeed
    on main since it merges first).
  - fanIn with both agents reporting exitCode=0 (agent crashes are NOT the
    scenario being tested here — agents finished, but their merge into main
    conflicts).
  - Assertions in ONE `it` block (Pitfall 7 in 10-RESEARCH; D-17 carry):
      expect(result.merged.length).toBe(1); // first merge succeeded
      expect(result.conflicted).toBe(true);
      expect(result.conflictedPaths.length).toBeGreaterThan(0);
      const queue = readIncomplete(handle.phaseRoot);
      expect(queue.some(e => e.reason === 'merge-in-tree-conflict')).toBe(true);
      expect(queue.find(e => e.reason === 'merge-in-tree-conflict').workspacePath).toBeTruthy();
    DO NOT split across multiple `it` blocks — vitest test isolation would
    rebuild the fixture and the conflict-fixture state would not survive.

Scenario 5: N=2 with one crashed worker (D-06 producer)
  - Setup: setupGitRepo(); agent-a makes a clean change and exits 0; agent-b
    "crashes" — the test simulates by writing uncommitted dirty content in the
    agent-b worktree but reporting exitCode=1 in the results array.
  - fanIn with results=[{agentId:'a',exitCode:0},{agentId:'b',exitCode:1}].
  - Assertions:
      expect(result.merged.length).toBe(1); // agent-a merged
      expect(result.failedReaped).toContain('phase-10-subagent-2'); // ws.name for agent-b
      const queue = readIncomplete(handle.phaseRoot);
      const crashEntry = queue.find(e => e.reason === 'crashed-with-uncommitted-work');
      expect(crashEntry).toBeDefined();
      expect(crashEntry.subagentName).toBe('phase-10-subagent-2');
      expect(crashEntry.changeIdShort).toMatch(/^[0-9a-f]{12}$/);
      expect(crashEntry.workspacePath).toBeTruthy();
    The D-07 non-force-refuse case (dirty tree) means worktree survives
    on disk — assertion: existsSync(crashEntry.workspacePath) === true.

Scenario 6: N=3 idempotency re-call (git-side-unique; absorbs TEST-15 idempotency proof)
  - Setup: setupGitRepo(); 3 agents in PLAN ORDER — agent-a clean, agent-b will
    conflict, agent-c clean. plan=[{agentId:'agent-a',...}, {agentId:'agent-b',...},
    {agentId:'agent-c',...}].
  - Deterministic processing order (W-2 lock): performGitParallelFanIn iterates
    `for (const ws of handle.workspaces)` in slot index order (handle.workspaces
    is materialized in plan order by performGitParallelDispatch). Therefore:
    iteration 1 processes agent-a (clean → merges, success SHA captured),
    iteration 2 processes agent-b (conflicts → halts via `break`), iteration 3
    never runs (loop already broken).
  - Capture per-agent SHAs after each agent's commit (in the test, immediately
    after the agent's `git commit` in its workspace, run
    `execSync('git rev-parse HEAD', { cwd: workspaces[i].path }).toString().trim()`
    and store as `aSha`, `bSha`, `cSha`). The post-merge HEAD SHA captured by
    fanIn for agent-a is NOT the agent's branch tip — it's the merge commit's
    SHA. So `merged[]` entries are merge-commit SHAs, not branch-tip SHAs.
    Assert on length and the presence/absence of the agent branch SHA via
    the ancestor probe (`git merge-base --is-ancestor aSha HEAD`) rather than
    direct SHA membership in merged[] (since merged[] holds 12-char merge-commit
    short SHAs, not branch-tip SHAs).
  - First fanIn call: results=[{agent-a,0},{agent-b,0},{agent-c,0}]. LOCKED
    assertions (W-2):
      expect(firstResult.merged.length).toBe(1); // ONLY agent-a's merge commit
      expect(firstResult.conflicted).toBe(true);
      expect(firstResult.conflictedPaths.length).toBeGreaterThan(0);
      expect(firstResult.incompleteQueued).toBe(1);
      // Branch-membership proof — agent-a IS ancestor (merged), agent-b/c are NOT.
      expect(execSync(`git merge-base --is-ancestor ${aSha} HEAD`, { cwd: dir }).toString /* exit 0 */ || true).toBeTruthy();
      // For agent-b / agent-c: execSync throws on non-zero exit; wrap in try/catch
      // OR use `git merge-base --is-ancestor ${bSha} HEAD; echo $?` shell form.
      // Easier: spawn via `child_process.spawnSync('git', ['merge-base','--is-ancestor', bSha, 'HEAD'], {cwd:dir}).status`.
      expect(spawnSync('git', ['merge-base', '--is-ancestor', bSha, 'HEAD'], { cwd: dir }).status).not.toBe(0); // b absent
      expect(spawnSync('git', ['merge-base', '--is-ancestor', cSha, 'HEAD'], { cwd: dir }).status).not.toBe(0); // c absent
      const q1 = readIncomplete(handle.phaseRoot);
      expect(q1.filter(e => e.reason === 'merge-in-tree-conflict').length).toBe(1);
  - User-resolution simulation (raw git in the test): clean up the wedged
    merge by resolving + committing. Use shell-form so user-provided merge
    commit moves HEAD past agent-b's branch tip:

      execSync('git add seed.txt', { cwd: dir, stdio: 'pipe' });
      execSync('git commit -qm "user resolves agent-b conflict"', { cwd: dir, stdio: 'pipe' });

  - Second fanIn call with the SAME `handle` and the same results array (D-03
    stateless probe ignores `results` for the ancestor decision). LOCKED
    assertions (W-2):
      expect(secondResult.merged.length).toBe(1); // ONLY agent-c's merge commit
      expect(secondResult.conflicted).toBe(false);
      expect(secondResult.conflictedPaths.length).toBe(0);
      expect(secondResult.surplusBookmarks.length).toBe(0); // clean re-call
      // After the second call: agent-a (was already ancestor), agent-b (now
      // ancestor via user's manual commit), AND agent-c (just merged) are all
      // ancestors of HEAD.
      expect(spawnSync('git', ['merge-base', '--is-ancestor', aSha, 'HEAD'], { cwd: dir }).status).toBe(0);
      expect(spawnSync('git', ['merge-base', '--is-ancestor', bSha, 'HEAD'], { cwd: dir }).status).toBe(0);
      expect(spawnSync('git', ['merge-base', '--is-ancestor', cSha, 'HEAD'], { cwd: dir }).status).toBe(0);
    Critical (per Pitfall 3 in 10-RESEARCH "per-call not cumulative"): the
    second call's `merged[]` contains ONLY agent-c's merge-commit SHA. agent-a
    is NOT in secondResult.merged (already ancestor → skipped by D-03 probe).
    agent-b is NOT in secondResult.merged (user's manual commit advanced HEAD
    past it → also ancestor → also skipped).
  - Single `it` block per the same Pitfall 7 / D-17 joint-assertion logic.
</scenarios>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author sdk/src/vcs/__tests__/cmd-parallel-git.test.ts with all six scenarios</name>
  <files>sdk/src/vcs/__tests__/cmd-parallel-git.test.ts</files>
  <read_first>
    - sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts (entire file, ~430 lines — the verbatim structural template; the executor should treat 80% of this file as a copy-and-swap template: replace jj-specific commands with git equivalents)
    - sdk/src/vcs/git/parallel.ts (the sidecar from Plan 10.02 — confirm the exported function signatures match what the test calls)
    - sdk/src/vcs/backends/git.ts (lines 720-740 region — confirm the wired `parallel` namespace from Plan 10.03 is reachable via the standard `createGitAdapter` export)
    - sdk/src/vcs/jj/incomplete-work.ts (the readIncomplete signature — needed for assertions in Scenarios 4-6)
    - sdk/src/vcs/expr.ts (the expr factory — for expr.bookmark('main') used in dispatch input)
    - sdk/src/vcs/types.ts (locate ParallelDispatchOpts, ParallelDispatchHandle, FanInResult shape — for type-import in the test file)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md §"sdk/src/vcs/__tests__/cmd-parallel-git.test.ts (NEW — contract test)" — header, skipIf pattern, repo fixture setup, W2 lifecycle, W3 joint-assertion, idempotency re-call scenario notes
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-RESEARCH.md §"Code Examples" §"Test fixture (Pattern B mkdtemp + Pattern A skipIf)" — the verbatim setupGitRepo() body including the `commit.gpgsign false` line (empirically required during research)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-RESEARCH.md §"Common Pitfalls" Pitfall 7 — the W3 joint-assertion lock-in: ONE `it` block per Scenario 4 and 6; NEVER split across multiple `it`s
    - scripts/check-skip-count.cjs (verify the lint script's exact mechanism — the test file MUST NOT introduce `describe.skip` / `it.skip` / `test.skip`; the test count baseline at HEAD is what subsequent runs must match)
  </read_first>
  <action>
    Create sdk/src/vcs/__tests__/cmd-parallel-git.test.ts as a structural mirror
    of sdk/src/vcs/__tests__/cmd-parallel-jj.test.ts with the following
    deliberate divergences:

    (1) Header docblock — adapt to "Phase 10 plan 04 (TEST-13 + TEST-15 + TEST-16)".
        Cite D-15 (Pattern A skipIf), D-17 (Pattern B mkdtemp + no retry + no
        describe.skip), Pitfall 5 in 10-RESEARCH (Pitfall 7 — the W3
        joint-assertion lock-in for the conflict scenario), the D-11 reframed
        TEST-15 coverage path (absorbed into Scenarios 2 and 6 — happy-path
        N-merges and idempotency re-call).

    (2) Imports — adapt the jj-side import list. KEEP: vitest's
        `describe`, `it`, `expect`, `beforeAll`, `afterAll`; `mkdtempSync`,
        `rmSync`, `writeFileSync`, `mkdirSync`, `existsSync` from 'node:fs';
        `join` from 'node:path'; `tmpdir` from 'node:os'; `execSync` from
        'node:child_process' (test fixture is allowlisted for raw subprocess;
        confirmed by the existing entry for `sdk/src/vcs/__tests__/vcs-fixture.ts`
        and `sdk/src/vcs/__tests__/git-backend.test.ts` in the allowlist).
        SWAP: jj-specific imports → git equivalents. Import `createGitAdapter`
        from '../backends/git.js'. Import `readIncomplete` from
        '../jj/incomplete-work.js'. Import `expr` from '../expr.js'.
        Import types `ParallelDispatchHandle`, `FanInResult` from '../types.js'.
        DROP: any jj-specific helpers (createJjAdapter, octopus.ts helpers,
        anything jj-related).

    (3) `gitAvailable` probe (mirror jj-side `jjAvailable` at lines 44-50):

        let gitAvailable = false;
        try {
          execSync('git --version', { stdio: 'pipe' });
          gitAvailable = true;
        } catch {
          // git not on PATH; every describe in this file skips.
        }

    (4) `setupGitRepo()` helper (verbatim per 10-RESEARCH §"Test fixture
        (Pattern B mkdtemp + Pattern A skipIf)" — the empirically-verified
        body with `commit.gpgsign false`):

        function setupGitRepo(): string {
          const dir = mkdtempSync(
            join(
              tmpdir(),
              `gsd-git-parallel-${Math.random().toString(36).slice(2, 10)}-`,
            ),
          );
          execSync('git init -q -b main', { cwd: dir, stdio: 'pipe' });
          execSync('git config user.email "test@test.com"', { cwd: dir, stdio: 'pipe' });
          execSync('git config user.name "Test"', { cwd: dir, stdio: 'pipe' });
          execSync('git config commit.gpgsign false', { cwd: dir, stdio: 'pipe' });
          writeFileSync(join(dir, 'seed.txt'), 'seed\n');
          execSync('git add seed.txt', { cwd: dir, stdio: 'pipe' });
          execSync('git commit -qm seed', { cwd: dir, stdio: 'pipe' });
          // W-3 (revision): no explicit mkdir for the phase dir — appendIncomplete
          // is mkdir-safe per Phase 9 CR-02 (sdk/src/vcs/jj/incomplete-work.ts:71-85
          // does `mkdirSync(dirname(p), { recursive: true })` before append). The
          // dispatch's derivePhaseRoot may resolve to a name like `10-git-side-...`
          // anyway, so pre-creating `10-test` was dead-fixture noise.
          return dir;
        }

    (5) W2 lifecycle — Scenarios 1, 2, 3 share a structural pattern (clean
        fanIn at varying N). Use a top-level `for (const N of [2, 3, 4] as
        const)` wrapping a `describe.sequential.skipIf(!gitAvailable)` block
        (mirror cmd-parallel-jj.test.ts:96-110). Inside each describe:
          - beforeAll: dir = setupGitRepo(); vcs = createGitAdapter(dir).
          - afterAll: if (dir) rmSync(dir, { recursive: true, force: true }).
          - One `it` for clean fan-in: dispatch N agents; each agent makes a
            DIFFERENT non-conflicting file change (e.g., agent-i touches
            `agent-${i}.txt` in its workspace and commits via raw git in the
            workspace dir). Build the results array as N entries with
            exitCode=0. Call fanIn. Assert merged.length === N, conflicted
            === false, conflictedPaths.length === 0, incompleteQueued === 0,
            failedReaped.length === 0.

    (6) Scenario 4 — N=2 in-tree-conflict, ONE `it` block (Pitfall 7 + D-17):
        Own `describe.sequential.skipIf(!gitAvailable)`. beforeAll/afterAll
        same shape. Single `it('N=2 in-tree-conflict: conflicted===true AND
        conflictedPaths populated AND queue entry reason="merge-in-tree-conflict"
        — ALL THREE in ONE scenario', { timeout: 30000 }, () => { ... })`.
        Inside: dispatch N=2; in each workspace modify the SAME line of
        seed.txt differently (use the workspace dir as the raw-git cwd);
        commit in each workspace; fanIn with both exitCode=0. Then ALL three
        assertions sequentially.

    (7) Scenario 5 — N=2 crashed worker: own
        `describe.sequential.skipIf(!gitAvailable)`. Single `it` block (no
        joint-assertion mandate here, but for consistency keep all assertions
        in one). Dispatch N=2; agent-a makes a clean change and commits;
        agent-b writes uncommitted content to its workspace (e.g.,
        writeFileSync(join(workspaces[1].path, 'dirty.txt'), 'uncommitted\n'))
        and DOES NOT commit. fanIn with
        results=[{agentId:'a',exitCode:0},{agentId:'b',exitCode:1,stderr:'simulated crash'}].
        Assertions: merged.length === 1, failedReaped contains the
        agent-b workspace name, the queue has a 'crashed-with-uncommitted-work'
        entry with subagentName === <agent-b workspace name>, changeIdShort
        matches /^[0-9a-f]{12}$/, workspacePath truthy, existsSync(workspacePath)
        === true (D-07 dirty-tree refusal preserves the worktree on disk).

    (8) Scenario 6 — N=3 idempotency re-call: own
        `describe.sequential.skipIf(!gitAvailable)`. Single `it` block
        (joint-assertion of first-call halt + second-call clean).

        Fixture setup (DETERMINISTIC per W-2 revision): dispatch N=3 with plan
        order LOCKED as `[agent-a, agent-b, agent-c]`. The
        performGitParallelDispatch implementation populates `handle.workspaces`
        in slot-index order matching the input `plan` array; therefore
        performGitParallelFanIn's `for (const ws of handle.workspaces)` loop
        processes agent-a first, agent-b second, agent-c third.

        In each agent's workspace, make the agent's changes and commit:
          - agent-a: writes `a.txt` (non-conflicting); commits.
          - agent-b: edits line 1 of `seed.txt` to "agent-b override\n"; commits.
          - agent-c: writes `c.txt` (non-conflicting); commits.

        Then advance main: in `dir` itself (NOT a workspace), edit line 1 of
        `seed.txt` to "main override\n" and commit. This is the change that
        forces agent-b's merge to conflict (agent-b's branch tip and main's
        HEAD both modified line 1).

        Capture per-agent branch-tip SHAs IMMEDIATELY after each agent's
        commit (for the W-2 ancestor-probe assertions):
          const aSha = execSync('git rev-parse HEAD', { cwd: workspaces[0].path }).toString().trim();
          const bSha = execSync('git rev-parse HEAD', { cwd: workspaces[1].path }).toString().trim();
          const cSha = execSync('git rev-parse HEAD', { cwd: workspaces[2].path }).toString().trim();

        First fanIn call — LOCKED assertions per W-2 revision:
          const firstResult = vcs.workspace.parallel.fanIn(handle, [
            { agentId: 'agent-a', exitCode: 0 },
            { agentId: 'agent-b', exitCode: 0 },
            { agentId: 'agent-c', exitCode: 0 },
          ]);
          expect(firstResult.merged.length).toBe(1);          // agent-a ONLY
          expect(firstResult.conflicted).toBe(true);
          expect(firstResult.conflictedPaths.length).toBeGreaterThan(0);
          expect(firstResult.incompleteQueued).toBe(1);
          // Ancestor-probe proof (W-2): a IS ancestor, b/c NOT ancestor.
          expect(spawnSync('git', ['merge-base', '--is-ancestor', aSha, 'HEAD'], { cwd: dir }).status).toBe(0);
          expect(spawnSync('git', ['merge-base', '--is-ancestor', bSha, 'HEAD'], { cwd: dir }).status).not.toBe(0);
          expect(spawnSync('git', ['merge-base', '--is-ancestor', cSha, 'HEAD'], { cwd: dir }).status).not.toBe(0);
          const q1 = readIncomplete(handle.phaseRoot);
          expect(q1.filter(e => e.reason === 'merge-in-tree-conflict').length).toBe(1);

        User-resolution simulation (in `dir`, NOT a workspace; finishes the
        wedged merge):
          execSync('git add seed.txt', { cwd: dir, stdio: 'pipe' });
          execSync('git commit -qm "user resolves agent-b conflict"', { cwd: dir, stdio: 'pipe' });

        Second fanIn call with the SAME `handle` and the same results array —
        LOCKED assertions per W-2:
          const secondResult = vcs.workspace.parallel.fanIn(handle, [
            { agentId: 'agent-a', exitCode: 0 },
            { agentId: 'agent-b', exitCode: 0 },
            { agentId: 'agent-c', exitCode: 0 },
          ]);
          expect(secondResult.merged.length).toBe(1);          // agent-c ONLY
          expect(secondResult.conflicted).toBe(false);
          expect(secondResult.conflictedPaths.length).toBe(0);
          expect(secondResult.surplusBookmarks.length).toBe(0);
          // After the second call: a/b/c are ALL ancestors of HEAD.
          expect(spawnSync('git', ['merge-base', '--is-ancestor', aSha, 'HEAD'], { cwd: dir }).status).toBe(0);
          expect(spawnSync('git', ['merge-base', '--is-ancestor', bSha, 'HEAD'], { cwd: dir }).status).toBe(0);
          expect(spawnSync('git', ['merge-base', '--is-ancestor', cSha, 'HEAD'], { cwd: dir }).status).toBe(0);

        Per Pitfall 3 in 10-RESEARCH "per-call not cumulative":
        secondResult.merged contains ONLY agent-c's merge-commit SHA. agent-a
        is NOT in secondResult.merged (already ancestor → D-03 probe skips).
        agent-b is NOT in secondResult.merged (user's manual commit advanced
        HEAD past it → also ancestor → also skipped).

        Imports note: this scenario needs `spawnSync` for the ancestor-probe
        assertions (the test file is allowlisted for raw subprocess per the
        existing precedent for sdk/src/vcs/__tests__/git-backend.test.ts).
        Import: `import { execSync, spawnSync } from 'node:child_process';`.

    (9) DO NOT include the TEST-14 `divergent()` topology assertion — it's
        jj-specific and has no git equivalent (commit graphs don't have a
        divergence concept analogous to jj's anonymous-branch divergence
        revset). The D-11 reframed TEST-15 wording is covered by Scenarios
        2, 3 (the N=3 / N=4 merged.length === N assertions) and Scenario 6
        (idempotency re-call).

    (10) NEVER use `describe.skip`, `it.skip`, `test.skip`. NEVER add `retry:
         N` anywhere. NEVER use `.only` modifiers.

    (11) The Pattern A `describe.sequential.skipIf(!gitAvailable)` ensures the
         tests are SKIPPED ENTIRELY on machines without git on PATH. Pattern A
         is the sole skip mechanism — the test file MUST NOT introduce any
         other skip variants.

    Indentation: tabs. No fenced code blocks (this is a .ts file).

    After writing, run the test suite locally: `pnpm -C sdk vitest run sdk/src/vcs/__tests__/cmd-parallel-git.test.ts`. All scenarios MUST exit 0 on the host machine (git is available per Environment Availability table in 10-RESEARCH.md). Then run `node scripts/check-skip-count.cjs` — MUST exit 0 (no new skips introduced).
  </action>
  <verify>
    <automated>test -f sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: test file does not exist"; exit 1)</automated>
    <automated>grep -c "describe.sequential.skipIf" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts | awk '$1 < 4 { exit 1 } { exit 0 }' || (echo "FAIL: expected at least 4 describe.sequential.skipIf blocks (one per scenario group: N-loop, conflict, crash, idempotency)"; exit 1)</automated>
    <automated>grep -c "describe\\.skip\\|it\\.skip\\|test\\.skip\\|\\.only" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts | grep -q "^0$" || (echo "FAIL: forbidden skip or only modifier found (TEST-16 LOCKED — no skips)"; exit 1)</automated>
    <automated>grep -q "mkdtempSync" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: Pattern B random-prefix mkdtemp not used"; exit 1)</automated>
    <automated>grep -q "Math\\.random().toString(36).slice(2" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: random-prefix mkdtemp suffix not present"; exit 1)</automated>
    <automated>grep -q "gitAvailable" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: gitAvailable probe missing"; exit 1)</automated>
    <automated>grep -q "commit.gpgsign false" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: commit.gpgsign false config missing — fixture will fail in environments with global gpg-signing enabled"; exit 1)</automated>
    <automated>grep -q "readIncomplete" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: readIncomplete not imported/used — conflict and crash scenarios cannot assert queue state"; exit 1)</automated>
    <automated>grep -q "merge-in-tree-conflict" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: merge-in-tree-conflict assertion missing (Scenario 4 joint-assertion)"; exit 1)</automated>
    <automated>grep -q "crashed-with-uncommitted-work" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: crashed-with-uncommitted-work assertion missing (Scenario 5)"; exit 1)</automated>
    <automated>grep -q "for (const N of \\[2, 3, 4\\]" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: N=2/3/4 W2 lifecycle loop missing"; exit 1)</automated>
    <automated>! grep -q "divergent()" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: divergent() topology assertion present — jj-specific, must NOT appear in git-side test per D-11"; exit 1)</automated>
    <automated>! grep -qE "retry:\\s*[0-9]" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts || (echo "FAIL: vitest retry option found (TEST-16 LOCKED — no retry: N)"; exit 1)</automated>
    <automated>pnpm -C sdk vitest run sdk/src/vcs/__tests__/cmd-parallel-git.test.ts 2>&1 | tee /tmp/p10-04-vitest.log | tail -20; if grep -qE "failed|✗|FAIL" /tmp/p10-04-vitest.log && ! grep -q "0 failed\|Tests.*passed" /tmp/p10-04-vitest.log; then echo "FAIL: vitest reported failures"; exit 1; else echo "OK: vitest run clean"; fi</automated>
    <automated>node scripts/check-skip-count.cjs 2>&1 | tee /tmp/p10-04-skipcount.log; grep -qE "regression|FAIL|increase" /tmp/p10-04-skipcount.log && (echo "FAIL: skip-count regression"; exit 1) || echo "OK: skip count baseline preserved"</automated>
  </verify>
  <acceptance_criteria>
    - File sdk/src/vcs/__tests__/cmd-parallel-git.test.ts exists and contains exactly five non-overlapping describe groups: a top-level for-loop spawning N=2/3/4 clean-fan-in describes, plus one in-tree-conflict describe, one crashed-worker describe, one idempotency-re-call describe (total six scenarios across the file)
    - The W2 for-loop iterates over `[2, 3, 4] as const` — N=2 / N=3 / N=4 each get an own describe block
    - The in-tree-conflict scenario is ONE `it` block (not split). Verify: `grep -A 50 "in-tree-conflict" sdk/src/vcs/__tests__/cmd-parallel-git.test.ts | grep -c "^\\s*it("` returns 1 in that context (the joint assertion lives in a single `it`)
    - All three Scenario 4 assertions are inside that single `it`: `conflicted` boolean assertion, `conflictedPaths` length assertion, and a `readIncomplete` queue assertion checking `reason === 'merge-in-tree-conflict'`
    - Scenario 5 calls `readIncomplete` and asserts the entry's `reason === 'crashed-with-uncommitted-work'`, `subagentName`, `changeIdShort` regex, and `workspacePath` existence
    - Scenario 6 makes TWO sequential `fanIn` calls with the SAME handle, with user-side `git add` + `git commit` in between. The second call's `result.merged.length === 1` (NOT cumulative)
    - No `describe.skip`, `it.skip`, `test.skip`, `.only` modifiers anywhere
    - No `retry: N` option anywhere
    - `gitAvailable` probe and `setupGitRepo()` helper present with empirically-required `commit.gpgsign false` config line
    - `pnpm -C sdk vitest run sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` exits 0 (vitest reports "Tests passed" with no failures across all six scenarios)
    - `node scripts/check-skip-count.cjs` exits 0 (no skip-count regression)
    - The test file size is between 250 and 500 lines (sanity bound; jj-side analog is 430 lines)
  </acceptance_criteria>
  <done>cmd-parallel-git.test.ts ships with all six scenarios (3 clean-fan-in N-variants + conflict + crash + idempotency); vitest run is fully green; no skip-count regression; W3 joint-assertion lock-in preserved in Scenario 4.</done>
</task>

</tasks>

<verification>
  - TEST-13 git contract scenarios complete (N=2/3/4 dispatch, clean fan-in, in-tree-conflict, crashed-worker)
  - TEST-15 (D-11 reframed: per-branch loop happy-path proof for N ∈ {2, 3, 4}) covered by Scenarios 2 and 3
  - TEST-16 (Pattern B mkdtemp + no retry + no skip) honored — verified by grep gates above
  - Idempotency re-call scenario (git-side-unique) proves D-03 stateless probe semantics
  - `pnpm -C sdk vitest run sdk/src/vcs/__tests__/cmd-parallel-git.test.ts` is fully green
  - `node scripts/check-skip-count.cjs` exits 0
</verification>

<success_criteria>
  - All six scenarios execute green in CI / locally
  - The cross-backend FanInResult contract is now executable-proven on git
  - Phase 10's same-PR coupling with Phase 9 is complete: Phase 9 shipped jj contract tests + the contract surface; Phase 10 ships git contract tests + the git substrate
  - Skip-count baseline preserved (no `.skip` or `.only` modifiers; CI's `check-skip-count.cjs` continues to pass on origin/main parity)
  - W3 joint-assertion lock-in (single `it` for the conflict scenario) is enforced — the conflict test cannot false-pass via fixture-rebuild between sub-assertions
</success_criteria>

<output>
Create `.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-04-SUMMARY.md` when done. Summary captures: (1) total line count of the test file; (2) number of describe blocks and number of `it` blocks (confirm joint-assertion preserved); (3) full vitest output showing N passed / 0 failed for all six scenarios; (4) confirmation `node scripts/check-skip-count.cjs` exited 0; (5) any divergences from the jj-side analog beyond the documented ones (TEST-14 drop, idempotency add).
</output>
