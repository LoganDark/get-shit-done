---
phase: 10-git-side-parallel-verbs-classifier-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - PARALLEL-02
  - TEST-15
tags:
  - cascade-amendments
  - documentation
must_haves:
  truths:
    - "ROADMAP.md Phase 10 SC3 reads as 'per-branch 2-parent git merge --no-ff <agentBookmark> loop' (no octopus wording)"
    - "REQUIREMENTS.md PARALLEL-02 git-side clause reads as per-branch loop + halt-on-conflict + idempotent re-call"
    - "REQUIREMENTS.md TEST-15 is either deleted or reframed as the per-branch loop happy-path proof"
    - "No reference to octopus N-parent merge survives anywhere in Phase 10 success criteria or git-side requirement language"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Amended Phase 10 SC3 wording per D-09"
      contains: "Per-branch 2-parent"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended PARALLEL-02 git-side + amended/dropped TEST-15 per D-10 + D-11"
      contains: "merge-base --is-ancestor"
  key_links:
    - from: ".planning/ROADMAP.md Phase 10 §Success Criteria"
      to: "Phase 10 plans (02, 03, 04)"
      via: "executor reads SC for verification framing"
      pattern: "Per-branch 2-parent"
    - from: ".planning/REQUIREMENTS.md PARALLEL-02"
      to: "Plan 10.02 sidecar body decisions D-01..D-04"
      via: "requirement text must match locked decisions"
      pattern: "halts on first conflict"
---

<objective>
Cascade-amendment doc edits per CONTEXT.md D-09 / D-10 / D-11. Three line-level
amendments to ROADMAP.md and REQUIREMENTS.md so the authoritative Phase 10
success criteria and requirement text reflect the user-locked decisions (loop,
not octopus). Blocks downstream plans because Plan 10.02, 10.03, and 10.04
executors read these files for verification framing — the SC and REQ wording
MUST match the locked decisions before the sidecar lands.

Purpose: The ROADMAP and REQUIREMENTS files are the executor's source of truth
for "what must be TRUE" at phase completion. They currently encode the original
(now-rejected) octopus-form proposal. D-09 / D-10 / D-11 in CONTEXT.md mandate
amendments. This plan ships those amendments verbatim per the wording locked in
CONTEXT.md.

Output: Three line edits — one in ROADMAP.md (Phase 10 SC3), two in
REQUIREMENTS.md (PARALLEL-02 git-side clause + TEST-15 entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md
@.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-PATTERNS.md

<amendments>
<!-- Verbatim amendment text from 10-CONTEXT.md D-09 / D-10 / D-11. -->
<!-- Executor performs straight string replacement. No interpretation. -->

D-09 (ROADMAP.md Phase 10 SC3):

CURRENT (the line currently in ROADMAP.md Phase 10 Success Criteria item 3):
  "`git merge --no-ff <p1> <p2> <p3>` octopus form for N≥3 verified on
   test-fixture across CI git versions; `git worktree remove --force` is
   forbidden in the cross-backend path (non-force only)."

AMENDED TO (verbatim from 10-CONTEXT.md D-09):
  "Per-branch 2-parent `git merge --no-ff <agentBookmark>` loop verified on
   test-fixture across CI git versions; halt-on-conflict + re-call via
   `merge-base --is-ancestor` skip; `git worktree remove --force` is forbidden
   in the cross-backend path (non-force only)."

D-10 (REQUIREMENTS.md PARALLEL-02 git-side clause):

CURRENT (within the PARALLEL-02 bullet — the git-side sentence):
  "git uses N-parent `git merge --no-ff <p1>...<pN>` + batched `git update-ref -d`."

AMENDED TO (verbatim from 10-CONTEXT.md D-10):
  "git iterates per-branch 2-parent `git merge --no-ff <agentBookmark>` +
   per-success `git branch -D`; halts on first conflict; idempotent under
   re-call via `merge-base --is-ancestor` skip."

D-11 (REQUIREMENTS.md TEST-15 — REFRAME, do not drop):

CURRENT:
  "**TEST-15**: git N-parent octopus fixture. `git merge --no-ff <p1> <p2> <p3>`
   octopus form for N≥3 verified cross-version (ARCHITECTURE-researcher's open
   question)."

AMENDED TO (reframed per 10-CONTEXT.md D-11 alternative wording — planner choice
between delete vs. reframe; planner picks REFRAME to keep the REQ-ID alive and
explicitly covered by TEST-13's happy-path scenarios):
  "**TEST-15**: git per-branch loop happy-path: N successful 2-parent merges
   produce N entries in `merged[]` for N ∈ {2, 3, 4} verified cross-version."

Rationale for REFRAME over DELETE: 10-PATTERNS.md notes "CONTEXT.md D-11 leaves
the choice between delete/reframe to the planner. RESEARCH.md §State of the Art
leans drop; CONTEXT.md Recommendation is silent." Reframe preserves the REQ-ID
in the Traceability table (line 132 of REQUIREMENTS.md) without forcing a
recount or remap. The reframed wording is genuinely covered by the TEST-13
N=2/3/4 clean-fanIn scenarios in Plan 10.04, so the REQ remains testable.
</amendments>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ROADMAP.md Phase 10 SC3 (D-09)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (locate the "### Phase 10: git-side parallel verbs + classifier extension" section, then its "**Success Criteria**" subsection item 3)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md (the D-09 verbatim amendment text in the <amendments> block above is the source of truth)
  </read_first>
  <action>
    Replace SC item 3 under the Phase 10 heading. The current text contains the
    octopus-form clause `git merge --no-ff <p1> <p2> <p3>` octopus form for N≥3
    verified on test-fixture across CI git versions`. Replace with the D-09
    amended text exactly as written in the <amendments> block above (the
    "Per-branch 2-parent `git merge --no-ff <agentBookmark>` loop verified on
    test-fixture across CI git versions; halt-on-conflict + re-call via
    `merge-base --is-ancestor` skip; `git worktree remove --force` is forbidden
    in the cross-backend path (non-force only)." string).

    Preserve the surrounding bullet structure and SC numbering. Do not touch SC1,
    SC2, SC4, or SC5. Do not modify any other phase section. Do not modify the
    Phase 10 Goal, Depends on, Requirements, or Plans subsections.

    After the edit, the Phase 10 SC3 line MUST NOT contain the substring
    "octopus form" anywhere.
  </action>
  <verify>
    <automated>grep -c "octopus form for N" .planning/ROADMAP.md | grep -q "^0$" || (echo "FAIL: ROADMAP.md still contains the octopus-form octopus clause"; exit 1)</automated>
    <automated>grep -q "Per-branch 2-parent" .planning/ROADMAP.md || (echo "FAIL: ROADMAP.md does not contain the amended D-09 wording"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "octopus form for N" .planning/ROADMAP.md` returns 0
    - `grep -q "Per-branch 2-parent .git merge --no-ff <agentBookmark>. loop verified" .planning/ROADMAP.md` exits 0
    - `grep -q "merge-base --is-ancestor" .planning/ROADMAP.md` exits 0 (the amended SC3 introduces this phrase)
    - The line number of the Phase 10 section header `### Phase 10:` is preserved within ±5 lines of its pre-edit position (verify with `grep -n "^### Phase 10:" .planning/ROADMAP.md`)
  </acceptance_criteria>
  <done>SC3 text under Phase 10 is the D-09 amended wording verbatim; no octopus-form language survives anywhere in the Phase 10 section.</done>
</task>

<task type="auto">
  <name>Task 2: Amend REQUIREMENTS.md PARALLEL-02 git-side clause + TEST-15 (D-10 + D-11)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (locate the "**PARALLEL-02**" bullet and the "**TEST-15**" bullet)
    - .planning/phases/10-git-side-parallel-verbs-classifier-extension/10-CONTEXT.md (the D-10 + D-11 verbatim amendment text in the <amendments> block above is the source of truth)
  </read_first>
  <action>
    Two string replacements in .planning/REQUIREMENTS.md.

    (1) D-10 — inside the PARALLEL-02 bullet, replace the git-side sentence
    `git uses N-parent \`git merge --no-ff <p1>...<pN>\` + batched \`git update-ref -d\`.`
    with the D-10 verbatim wording from <amendments> above:
    `git iterates per-branch 2-parent \`git merge --no-ff <agentBookmark>\` + per-success \`git branch -D\`; halts on first conflict; idempotent under re-call via \`merge-base --is-ancestor\` skip.`

    Preserve everything else on the PARALLEL-02 bullet (jj-side clause, return
    shape, Pitfall 2 reference, results arg shape, D-07 carry note). Do not
    touch the bullet's `[x]` checkbox state.

    (2) D-11 — replace the TEST-15 bullet text with the reframed alternative
    from <amendments> above (the "git per-branch loop happy-path" wording).
    Preserve the bullet's checkbox state (`- [ ]`) and REQ-ID prefix
    (`**TEST-15**:`).

    Do not touch any other PARALLEL-* / TEST-* / VCS-* / PROMPT-* / LINT-* /
    HOOK-* / CI-* / CONFIG-* / DOGFOOD-* requirement. Do not modify the
    Traceability table at the bottom of the file — TEST-15 stays mapped to
    Phase 10, its row is unchanged.
  </action>
  <verify>
    <automated>grep -c "N-parent .git merge --no-ff <p1>" .planning/REQUIREMENTS.md | grep -q "^0$" || (echo "FAIL: REQUIREMENTS.md still contains the N-parent octopus PARALLEL-02 wording"; exit 1)</automated>
    <automated>grep -q "per-branch 2-parent" .planning/REQUIREMENTS.md || (echo "FAIL: REQUIREMENTS.md PARALLEL-02 was not amended to the D-10 wording"; exit 1)</automated>
    <automated>grep -q "git per-branch loop happy-path" .planning/REQUIREMENTS.md || (echo "FAIL: TEST-15 was not reframed per D-11"; exit 1)</automated>
    <automated>grep -c "N-parent octopus fixture" .planning/REQUIREMENTS.md | grep -q "^0$" || (echo "FAIL: TEST-15 still contains the dropped octopus-fixture wording"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "N-parent .git merge --no-ff <p1>" .planning/REQUIREMENTS.md` returns 0
    - `grep -q "per-branch 2-parent .git merge --no-ff <agentBookmark>." .planning/REQUIREMENTS.md` exits 0
    - `grep -q "git per-branch loop happy-path: N successful 2-parent merges produce N entries in .merged\[\]." .planning/REQUIREMENTS.md` exits 0
    - `grep -c "N-parent octopus fixture" .planning/REQUIREMENTS.md` returns 0
    - The "## Traceability" table is unchanged (`grep "| TEST-15 | Phase 10 |" .planning/REQUIREMENTS.md` still matches a row)
    - The PARALLEL-02 bullet still starts with `- [x] **PARALLEL-02**:` (checkbox state preserved)
    - The TEST-15 bullet still starts with `- [ ] **TEST-15**:` (checkbox state preserved)
  </acceptance_criteria>
  <done>PARALLEL-02 git-side clause uses the D-10 loop wording; TEST-15 uses the D-11 reframed loop happy-path wording; no octopus-form language survives in either bullet; Traceability table unchanged.</done>
</task>

</tasks>

<verification>
  - `grep -c "octopus form for N\|N-parent octopus fixture\|N-parent .git merge --no-ff <p1>" .planning/ROADMAP.md .planning/REQUIREMENTS.md` returns 0 across both files (no stale octopus-form language anywhere in the two amended files)
  - All three D-09 / D-10 / D-11 amended strings are present (grep exits 0 for each on its respective file)
  - No other requirements or success criteria modified
</verification>

<success_criteria>
  - ROADMAP.md Phase 10 SC3 reads verbatim per D-09
  - REQUIREMENTS.md PARALLEL-02 git-side clause reads verbatim per D-10
  - REQUIREMENTS.md TEST-15 reads verbatim per D-11 reframed alternative
  - No collateral edits — diff against pre-amendment state shows exactly three string replacements across two files
  - Documentation is internally consistent: the Phase 10 SC3 amended language matches the locked CONTEXT.md decision text, which matches the implementation Plan 10.02 will deliver
</success_criteria>

<output>
Create `.planning/phases/10-git-side-parallel-verbs-classifier-extension/10-01-SUMMARY.md` when done. Summary captures: which three strings were replaced (with old → new diff), which files were touched, and confirmation that no octopus-form language survives in either ROADMAP.md or REQUIREMENTS.md.
</output>
