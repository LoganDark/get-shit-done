---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 13
type: execute
wave: 13
depends_on: [19-12]
files_modified:
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md (finalized)
autonomous: true
requirements: [MERGE-01, MERGE-02, MERGE-03, MERGE-04, MERGE-05, PORT-01, PORT-02, AUDIT-01]
must_haves:
  truths:
    - "Every line of the comm sweep (fork files no longer in the tree) maps to a 19-MERGE-AUDIT.md ledger row — no silently-lost fork capability (MERGE-02 proof)"
    - "Every surviving `gsd-sdk` text reference is a ledgered historical mention, not a live call"
    - "The full ordered phase gate passes end-to-end in one run, in the VALIDATION.md order"
    - "The dispatch chain works end-to-end on both backends from a jj-only subdirectory cwd"
  artifacts:
    - path: ".planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md"
      provides: "Finalized ledger with completeness-proof transcript and phase-gate results — the precedent artifact for the next upstream pull"
  key_links:
    - from: "comm -23 fork-file sweep"
      to: "ledger rows"
      via: "every output line resolves to a ported-to/re-applied-at/dropped/substrate row"
---

<objective>
Run the phase-closing proofs: the MERGE-02 ledger-completeness sweep, the severed-chain sweep, the end-to-end dispatch smoke, and the full ordered verification gate from 19-VALIDATION.md. Finalize 19-MERGE-AUDIT.md as the precedent artifact for the next upstream pull.

Purpose: this is the phase gate — all eight requirement IDs are proven here in one place, in order.
Output: finalized ledger with proof transcripts; pass/fail verdict per gate step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-VALIDATION.md
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md

HARD CONSTRAINTS: same as 19-01. This plan makes NO tree changes except ledger finalization and small fixes to defects its own sweeps surface (a surfaced defect larger than a one-file fix gets recorded as a gap for /gsd-verify-work, not hot-patched).
</context>

<tasks>

<task type="auto">
  <name>Task 1: MERGE-02 completeness proof + severed-chain sweep + end-to-end dispatch smoke</name>
  <files>19-MERGE-AUDIT.md (proof transcripts)</files>
  <read_first>
    - 19-MERGE-AUDIT.md (entire ledger — the row set the proof checks against)
    - 19-VALIDATION.md "Per-Task Verification Map" rows MERGE-02 and PORT-02 (the exact proof commands)
  </read_first>
  <action>
    (1) Completeness proof: run `comm -23 <(jj file list -r c7bd6bee sdk get-shit-done tests | sort) <(jj file list -r @ sdk get-shit-done tests | sort)` — script a checker that, for EVERY output line, finds a ledger row whose path column matches (exact or covering-glob row); emit unmatched lines. Zero unmatched lines required; if any, add the missing rows with real dispositions before passing (a missing disposition that reveals an unported capability is a GAP — record it, do not invent a row). Paste the sweep size + checker result into the ledger. (2) Severed-chain sweep (Pitfall 4): `rg -n 'gsd-sdk' --glob '!.planning/**'` — classify every hit: live call (BUG — fix or record as gap) vs historical doc mention (add/confirm ledger row). Also `rg -n 'resolveGsdToolsPath|bin/gsd-sdk' src gsd-core tests` must return zero live spawn paths. (3) End-to-end dispatch smoke (PORT-02 final form): in a tmp jj-only (non-colocated) repo, from a SUBDIRECTORY, source the launcher snippet logic and assert gsd_run resolves this repo's gsd-tools and `gsd_run query status` returns a valid envelope; repeat in a tmp git repo (GSD_VCS=git). Paste both transcripts.
  </action>
  <verify>
    <automated>COMM_UNMATCHED=$(comm -23 <(jj file list -r c7bd6bee sdk get-shit-done tests | sort) <(jj file list -r @ sdk get-shit-done tests 2>/dev/null | sort) | wc -l); rg -c 'Completeness proof' .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md && [ $(rg -l 'resolveGsdToolsPath' src gsd-core 2>/dev/null | wc -l) -eq 0 ] && echo "comm lines: $COMM_UNMATCHED (checker transcript in ledger)"</automated>
  </verify>
  <acceptance_criteria>
    - Checker reports zero comm lines without a matching ledger row (transcript pasted in ledger)
    - Every gsd-sdk text hit outside .planning/ is classified historical with a ledger row; zero live spawn paths
    - Both tmp-repo dispatch smokes pass from subdirectory cwd (jj-only and git), transcripts in ledger
  </acceptance_criteria>
  <done>MERGE-02 and PORT-02 proven with machine transcripts in the precedent artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Full ordered phase gate + ledger finalization</name>
  <files>19-MERGE-AUDIT.md (final results section)</files>
  <read_first>
    - 19-VALIDATION.md "Sampling Rate → Before /gsd-verify-work" (the exact gate order)
    - 19-MERGE-AUDIT.md fixture-exclusion list (for the marker sweep) and hand-edited-.cjs inventory (accumulated across plans)
  </read_first>
  <action>
    Run the ordered gate exactly as specified in 19-VALIDATION.md, recording each step's result in a final "Phase gate results" section: (1) `jj st` — zero conflict warnings at @; materialize the ledger's fixture-exclusion list into /tmp/19-fixture-exclusions.txt (one repo-relative path per line, exactly as `rg -l` prints them, no `./` prefix), then the marker sweep `rg -l '^(<<<<<<<|%%%%%%%|>>>>>>>)' --glob '!.planning/**'` filtered through `grep -vxF -f /tmp/19-fixture-exclusions.txt` — zero surviving hits (every hit must be a line in the actual exclusion-list file, not a count bound); (2) MERGE-02 proof (Task 1 result); (3) `pnpm run build:lib` clean; (4) `node -c` loop over EVERY hand-edited .cjs accumulated in the ledger across all plans (gsd-tools.cjs, helpers.cjs, github-release-notes.cjs, ported tests, gate scripts, …); (5) full `GSD_TEST_BACKENDS=git,jj node scripts/run-tests.cjs` (env prefix so the ported node:test cross-backend cells run BOTH backends); (6) `GSD_TEST_BACKENDS=git,jj npx vitest run`; (7) `node scripts/check-skip-count.cjs`; (8) the four re-pointed lint gates (lint-vcs-no-commit-id, lint-vcs-no-raw-git, audit-workflow-raw-git, lint-vcs-parallel-call-presence); (9) upstream drift checks `pnpm run check:alias-drift` and `pnpm run check:identity-drift` (or the actual script names found in package.json — stale generated files were MERGE-REVIEW WARNING-8); (10) AUDIT-01 sweeps (`rg -n "execGit\(" src -g '*.cts'` only substrate; `rg "execSync|execFileSync" src -g '*.cts'` only the seam). Every step green (or its failure recorded as an explicit gap for /gsd-verify-work — do not hot-patch multi-file defects here). Finalize the ledger: completeness statement, gate-results table, pointer text telling the NEXT merge where to start (mirroring MERGE-REVIEW-upstream-2026-05-25.md's role for this one). Remind the operator in the SUMMARY: squash of the stack into vpzlrrlv is the operator's manual action; installed-GSD update is deferred.
  </action>
  <verify>
    <automated>pnpm run build:lib && GSD_TEST_BACKENDS=git,jj node scripts/run-tests.cjs && GSD_TEST_BACKENDS=git,jj npx vitest run --reporter=basic && node scripts/check-skip-count.cjs && node scripts/lint-vcs-no-commit-id.cjs && node scripts/lint-vcs-no-raw-git.cjs && node scripts/audit-workflow-raw-git.cjs && node scripts/lint-vcs-parallel-call-presence.cjs && { rg -l '^(<<<<<<<|%%%%%%%|>>>>>>>)' --glob '!.planning/**' 2>/dev/null | grep -vxF -f /tmp/19-fixture-exclusions.txt | tee /tmp/19-13-markers.txt; [ ! -s /tmp/19-13-markers.txt ]; }</automated>
  </verify>
  <acceptance_criteria>
    - All 10 gate steps recorded with green results in the ledger's "Phase gate results" table (or enumerated, justified gaps)
    - Marker sweep result matches the fixture-exclusion list exactly (every hit is on the list)
    - The node -c loop covers every .cjs file the ledger marks hand-edited, all passing
    - Ledger finalized with completeness statement + next-merge pointer text
    - vpzlrrlv and all ancestors untouched for the whole phase (`jj log -r 'vpzlrrlv | parents(vpzlrrlv)'` shows original change ids)
  </acceptance_criteria>
  <done>Phase 19 gate fully evidenced; 19-MERGE-AUDIT.md is the finished precedent artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger → operator squash decision | the operator squashes ~13 waves of work into the merge change based on this artifact's completeness claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-35 | Repudiation | completeness claimed without machine proof | mitigate | comm-checker transcript + per-step gate results pasted, not asserted |
| T-19-36 | Tampering | inventing ledger rows to make the proof pass | mitigate | rule: a missing row that reveals unported capability is recorded as a GAP, never back-filled with a fake disposition |
| T-19-37 | DoS | hot-patching multi-file defects inside the gate plan | accept→route | gate plan records gaps for /gsd-verify-work instead of expanding scope |
</threat_model>

<verification>
- The automated verify chain IS the phase gate; ledger carries the full evidence trail.
</verification>

<success_criteria>
- All eight requirement IDs (MERGE-01..05, PORT-01..02, AUDIT-01) evidenced green in one ordered run; ledger finalized as next-pull precedent.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-13-SUMMARY.md` when done.
</output>
