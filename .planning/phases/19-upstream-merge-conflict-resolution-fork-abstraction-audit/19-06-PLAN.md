---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 06
type: execute
wave: 6
depends_on: [19-05]
files_modified:
  - src/vcs-command-router.cts (new)
  - gsd-core/bin/gsd-tools.cjs
  - tests/vcs-cjs-smoke.test.cjs (ported)
  - tests/vcs-adapter-contract.test.cjs (ported)
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [PORT-02]
must_haves:
  truths:
    - "Every vcs verb the fork's workflows call resolves through `node gsd-core/bin/gsd-tools.cjs query <verb>` to the ported adapter and returns the fork's JSON envelope shape"
    - "gsd-tools.cjs still passes node -c after hand-wiring and dispatches all pre-existing upstream commands unchanged"
    - "No surviving code path spawns the retired gsd-sdk binary"
  artifacts:
    - path: "src/vcs-command-router.cts"
      provides: "routeVcsCommand: verb table re-expressing the fork's 23 vcs-facing query handler envelopes over gsd-core/bin/lib/vcs"
    - path: "tests/vcs-adapter-contract.test.cjs"
      provides: "Early regression net against the built vcs artifacts"
  key_links:
    - from: "gsd-core/bin/gsd-tools.cjs"
      to: "gsd-core/bin/lib/vcs-command-router.cjs"
      via: "require + switch-case family registration (mirrors routeStateCommand precedent)"
      pattern: "vcs-command-router"
    - from: "query meta-prefix (gsd-tools.cjs ~line 344)"
      to: "vcs verbs"
      via: "prefix-strip dispatch — `gsd_run query <verb>` calling convention"
---

<objective>
Build the CLI bridge (porting-map chunk 6): a net-new `src/vcs-command-router.cts` re-expressing the fork's 23 vcs-facing SDK query handlers as gsd-tools verbs, wired into `gsd-core/bin/gsd-tools.cjs`, with envelope-shape parity (workflows parse these JSON envelopes with jq). Port the two built-artifact regression tests early as the safety net.

Purpose: PORT-02 — workflows must be able to call vcs verbs through `gsd_run query <verb>` BEFORE any workflow re-wiring (Pitfall 1: never rewire a workflow before its verbs exist).
Output: vcs command router, wired dispatch, verb-family smoke, 2 ported regression tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
19-RESEARCH.md (same directory) — targeted sections ONLY, do NOT @-load the full file: "Porting Map" chunk 6 + "Common Pitfalls" (Pitfall 1)
19-MERGE-AUDIT.md (same directory) — append-target ledger; do NOT @-load the full file (read_first entries name the specific rows/sections needed)
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/harvest/query-handlers/ (envelope reference implementations)

HARD CONSTRAINTS: same as 19-01. `gsd-core/bin/gsd-tools.cjs` is hand-written checked-in CJS — every edit needs `node -c`. The `commit` verb is NOT bridged here: upstream's existing `commit` command keeps its name and gets its INTERNALS migrated to the adapter in 19-07 (locked decision: no parallel fork commit verb).
PHANTOM VERBS: do NOT invent `checkout`, `tag`, or `stash` handlers — fork-side these were TODO comments next to raw git fences; the fences stay raw and enter the 19-10 baseline.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write src/vcs-command-router.cts (verb table + envelope parity)</name>
  <files>src/vcs-command-router.cts</files>
  <read_first>
    - harvest/query-handlers/*.ts (ALL of them — the envelope contracts: exact JSON field names, null-for-absent-optional convention, error envelope shape; Phase 11 precedent: emit null, not undefined, for absent optional fields)
    - src/state-command-router.cts or another upstream `*-command-router.cts` (the router registration pattern to mirror)
    - src/vcs/index.cts (the adapter factory surface the router consumes)
  </read_first>
  <action>
    Create `src/vcs-command-router.cts` exporting `routeVcsCommand(command, args)` (mirror the upstream router signature exactly as found in the read_first router). Import the ported adapter via sibling specifier `./vcs/index.cjs`. Implement one handler per fork verb, re-expressing each harvested query handler's envelope logic over the adapter: status, log, diff, head-ref, current-branch, branch-list, branch-create, branch-delete, rev-parse, rm, push, merge, reset, restore, revert, commit-to-subrepo, hooks.fire (wraps hook-bridge fireHook), migrate-vcs (wraps format-migration run), workspace, workspace.assert-dispatched-cwd, workspace.parallel.dispatch, workspace.parallel.fan-in, workspace.parallel.cancel, cleanup-subagent-workspaces. Envelope rules (load-bearing for jq consumers): identical top-level field names to the fork handlers; null (never undefined/omitted) for absent optionals; same exit-code semantics. Do NOT implement commit/checkout/tag/stash (see context notes). Keep handler bodies thin — adapter calls + envelope shaping only, exactly like the fork handlers were thin wrappers.
  </action>
  <verify>
    <automated>pnpm run build:lib && test -f gsd-core/bin/lib/vcs-command-router.cjs && node -e "const r=require('./gsd-core/bin/lib/vcs-command-router.cjs'); if(typeof r.routeVcsCommand!=='function')throw new Error('routeVcsCommand missing')"</automated>
  </verify>
  <acceptance_criteria>
    - Build emits gsd-core/bin/lib/vcs-command-router.cjs; routeVcsCommand exported
    - Every verb listed in the action has a handler (rg for each verb string in the router source returns >=1)
    - `rg -n 'checkout|"tag"|"stash"' src/vcs-command-router.cts` returns no handler registrations
    - Envelope shaping uses null for absent optionals (spot-assert: rg ': null' present where harvested handlers used it)
  </acceptance_criteria>
  <done>Router compiles and exposes the full fork verb surface with envelope parity.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the router into gsd-core/bin/gsd-tools.cjs (hand-edited CJS, node -c gated)</name>
  <files>gsd-core/bin/gsd-tools.cjs, 19-MERGE-AUDIT.md</files>
  <read_first>
    - gsd-core/bin/gsd-tools.cjs (full file: the switch(command) dispatch, the family-router registrations like routeStateCommand, and the query meta-prefix at ~line 344)
  </read_first>
  <action>
    Register the vcs family in gsd-tools.cjs mirroring the existing family-router precedent: `require('./lib/vcs-command-router.cjs')` and dispatch the verbs from Task 1 (including dotted forms `workspace.parallel.dispatch` etc. — match however the fork's gsd-sdk dispatched dotted verbs and however upstream dispatches dotted commands like `state.load`; read both before choosing the case shape). The `query` meta-prefix must reach these verbs unchanged (`node gsd-core/bin/gsd-tools.cjs query status` ≡ `node gsd-core/bin/gsd-tools.cjs status`). Preserve every existing upstream case byte-identical. Run `node -c gsd-core/bin/gsd-tools.cjs` after editing. Don't-sever-the-chain check (BLOCKER-3 lesson): `rg -n 'gsd-sdk|resolveGsdToolsPath' src gsd-core/bin --glob '!*.md'` — any hit that would SPAWN the retired binary is a bug to fix now; ledger the sweep result. Ledger row: `ported-to:src/vcs-command-router.cts + gsd-tools wiring` covering the 23 harvested handlers (reference the 19-03 rows).
  </action>
  <verify>
    <automated>node -c gsd-core/bin/gsd-tools.cjs && node gsd-core/bin/gsd-tools.cjs query status >/dev/null 2>&1; test $? -ne 127 && node gsd-core/bin/gsd-tools.cjs query head-ref | node -e "let d='';process.stdin.on('data',c=>d+=c).on('end',()=>JSON.parse(d))"</automated>
  </verify>
  <acceptance_criteria>
    - `node -c gsd-core/bin/gsd-tools.cjs` passes
    - `node gsd-core/bin/gsd-tools.cjs query head-ref` returns parseable JSON on this jj repo (not "Unknown command")
    - `node gsd-core/bin/gsd-tools.cjs query state.load` (pre-existing upstream verb) still works — no upstream case severed
    - Zero surviving spawn-paths to the gsd-sdk binary (sweep result ledgered)
  </acceptance_criteria>
  <done>Full dispatch chain live: gsd_run query <verb> → gsd-tools → router → lib/vcs → adapter.</done>
</task>

<task type="auto">
  <name>Task 3: Port the early regression net + per-family dispatch smoke on both backends</name>
  <files>tests/vcs-cjs-smoke.test.cjs, tests/vcs-adapter-contract.test.cjs, 19-MERGE-AUDIT.md</files>
  <read_first>
    - `jj file show -r c7bd6bee tests/vcs-cjs-smoke.test.cjs` and `jj file show -r c7bd6bee tests/vcs-adapter-contract.test.cjs` (port sources)
    - tests/helpers.cjs (the merged _loadVcs now pointing at gsd-core/bin/lib/vcs/index.cjs)
  </read_first>
  <action>
    Port both test files from c7bd6bee with path re-pointing: any `sdk/dist-cjs` requires → `gsd-core/bin/lib/vcs`, build hints → `pnpm run build:lib`; `node -c` both. Run them: `node scripts/run-tests.cjs --files tests/vcs-cjs-smoke.test.cjs tests/vcs-adapter-contract.test.cjs` (use the runner's actual file-selection flag per scripts/run-tests.cjs --help) — both green. Then the per-family dispatch smoke: in a throwaway tmp dir, create one jj repo (`jj git init --colocate` is the lint-clean precedent from Phase 13 — quote-split the inner word if a raw-git lint would match) and one git-backend cell (GSD_VCS=git pin), and invoke at least one read-only verb per family via `node <abs-path>/gsd-core/bin/gsd-tools.cjs query ...`: status, log, head-ref, current-branch, workspace, hooks.fire --help-style probe, migrate-vcs probe, workspace.parallel.dispatch with a --help/dry probe (do NOT run a real dispatch). Record the per-verb-family results table in the ledger (PORT-02 evidence). Append every checked-in .cjs hand-edited in this plan (gsd-core/bin/gsd-tools.cjs from Task 2, plus both ported test files) to the ledger's "Hand-edited .cjs inventory" appendix (file path | plan | node -c confirmed).
  </action>
  <verify>
    <automated>node -c tests/vcs-cjs-smoke.test.cjs && node -c tests/vcs-adapter-contract.test.cjs && node scripts/run-tests.cjs --files tests/vcs-cjs-smoke.test.cjs tests/vcs-adapter-contract.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - Both ported tests pass against the BUILT artifacts
    - Smoke table in ledger shows one passing read-only invocation per verb family on the jj cell and on the git cell
    - No smoke invocation mutated this repo (read-only verbs only; dispatch probed, not executed)
  </acceptance_criteria>
  <done>Regression net live; PORT-02 dispatch chain demonstrated end-to-end on both backends.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow markdown → CLI dispatch | workflows blindly trust verb availability and envelope shape (jq parsing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-15 | DoS | severed dispatch chain (BLOCKER-3 recurrence) | mitigate | per-family smoke + gsd-sdk spawn sweep + node -c on gsd-tools.cjs |
| T-19-16 | Tampering | envelope drift breaking jq consumers silently | mitigate | harvested handlers as the envelope contract; null-not-undefined rule enforced; contract test ported in the same plan |
| T-19-17 | Elevation | dispatch smoke executing mutating verbs on this repo | mitigate | read-only verb set for smoke; parallel dispatch probed via help/dry path only, in tmp repos |
</threat_model>

<verification>
- Build green; gsd-tools parses and dispatches old + new verbs; both ported tests green; smoke table recorded.
</verification>

<success_criteria>
- PORT-02 bridge complete and evidenced; workflows can be rewired in 19-08 without runtime "Unknown command" risk.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-06-SUMMARY.md` when done.
</output>
