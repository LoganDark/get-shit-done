---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 09
type: execute
wave: 9
depends_on: [19-08]
files_modified:
  - agents/gsd-executor.md
  - agents/gsd-code-fixer.md
  - gsd-core/references/dispatch-cwd-safety.md (new, moved)
  - gsd-core/workflows/migrate-vcs.md (new, moved)
  - gsd-core/workflows/_runtime-launcher.snippet.sh
  - gsd-core/workflows/*.md (launcher snippet blocks, via sync script)
  - .githooks/pre-commit
  - hooks/lib/git-cmd.js (audit/annotations)
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [AUDIT-01, PORT-02]
must_haves:
  truths:
    - "The runtime launcher resolves the repo root correctly on a jj-only (non-colocated) repo invoked from a subdirectory"
    - "The launcher fix lives in the snippet SOURCE and was propagated by scripts/sync-runtime-launcher.cjs — not 108 hand edits"
    - "agents/gsd-executor.md carries the fork's assert-dispatched-cwd guard and destructive-git prohibition on top of upstream's text"
    - ".githooks/pre-commit references only paths/scripts that exist in the adopted tree and degrades safely on a jj-only repo"
  artifacts:
    - path: "gsd-core/workflows/_runtime-launcher.snippet.sh"
      provides: "jj-aware RUNTIME_ROOT resolution"
      contains: "jj workspace root"
    - path: "gsd-core/references/dispatch-cwd-safety.md"
      provides: "Fork-added reference, re-homed and re-pathed"
  key_links:
    - from: "_runtime-launcher.snippet.sh"
      to: "every gsd-core/workflows/*.md embed block"
      via: "scripts/sync-runtime-launcher.cjs (sync:launcher)"
      pattern: "jj workspace root"
---

<objective>
Finish the prompt-surface and hook re-wiring (porting-map chunk 9): re-apply fork deltas to the two agent prompts, re-home the fork-added reference/workflow files, fix the launcher's jj-hostile root resolution at the snippet source + re-sync, and rewrite `.githooks/pre-commit` for the adopted tree.

Purpose: every workflow invocation flows through the launcher (fork-critical Pitfall 9); the executor agent is the fork's destructive-git firewall; .githooks is the fork's hook-bridge firing surface.
Output: rewired agents, moved fork files, jj-aware launcher synced everywhere, live .githooks, hook-script audit rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
19-RESEARCH.md (same directory) — targeted sections ONLY, do NOT @-load the full file: "Porting Map" chunk 9 + "Common Pitfalls" (Pitfall 9)
19-MERGE-AUDIT.md (same directory) — append-target ledger; do NOT @-load the full file (read_first entries name the specific rows/sections needed)

HARD CONSTRAINTS: same as 19-01. The launcher snippet is EMBEDDED in workflow files via sync — fix the source, run `scripts/sync-runtime-launcher.cjs` (npm script `sync:launcher`), never hand-edit embeds (Pitfall 9).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent prompt re-wiring + fork-file re-homing</name>
  <files>agents/gsd-executor.md, agents/gsd-code-fixer.md, gsd-core/references/dispatch-cwd-safety.md, gsd-core/workflows/migrate-vcs.md, get-shit-done/references/dispatch-cwd-safety.md (delete), get-shit-done/workflows/migrate-vcs.md (delete), 19-MERGE-AUDIT.md</files>
  <read_first>
    - agents/gsd-executor.md and agents/gsd-code-fixer.md (upstream versions in tree — 23 and 31 git mentions respectively)
    - Fork deltas: `diff <(jj file show -r b533f71857ab agents/gsd-executor.md) <(jj file show -r c7bd6bee agents/gsd-executor.md)` and the same for gsd-code-fixer.md
    - get-shit-done/references/dispatch-cwd-safety.md and get-shit-done/workflows/migrate-vcs.md (fork-added files surviving in tree, to move)
  </read_first>
  <action>
    gsd-executor.md: re-apply the fork's Phase 11 rewiring onto upstream's text — the collapsed `workspace.assert-dispatched-cwd` guard (replacing the 4 worktree-aware guards: bugs #2924/#3097/#3099/namespace), the destructive_git_prohibition block (byte-identical to the fork version), and `gsd_run query` calling convention; upstream-new executor content stays. gsd-code-fixer.md: re-apply the fork's restore/revert verb usage (`gsd_run query restore` / `revert` instead of raw git) onto upstream text. Move the two fork-added files to their gsd-core homes with content re-pathing (`gsd-sdk query`→`gsd_run query`, `get-shit-done/`→`gsd-core/`): references/dispatch-cwd-safety.md and workflows/migrate-vcs.md; delete the get-shit-done originals. Probe every `gsd_run query <verb>` now referenced in both agent files (same dead-verb loop as 19-08). Ledger rows: 2 `re-applied-at:` (agents), 2 `re-applied-at:` (moved files).
  </action>
  <verify>
    <automated>rg -c 'workspace.assert-dispatched-cwd' agents/gsd-executor.md && [ $(rg -c 'gsd-sdk' agents/gsd-executor.md agents/gsd-code-fixer.md | rg -v ':0$' | wc -l) -eq 0 ] && test -f gsd-core/references/dispatch-cwd-safety.md && test -f gsd-core/workflows/migrate-vcs.md && [ ! -e get-shit-done/references/dispatch-cwd-safety.md ] && [ ! -e get-shit-done/workflows/migrate-vcs.md ]</automated>
  </verify>
  <acceptance_criteria>
    - gsd-executor.md contains the assert-dispatched-cwd guard and the destructive-git prohibition block; zero gsd-sdk references in either agent
    - Both moved files exist at gsd-core paths with zero `get-shit-done/` self-references (`rg -c 'get-shit-done/' <file>` = 0 hits)
    - Dead-verb probe over both agents reports zero dead verbs
    - 4 ledger rows appended
  </acceptance_criteria>
  <done>Agent prompts carry fork guards on upstream text; fork-added files live at canonical homes.</done>
</task>

<task type="auto">
  <name>Task 2: jj-aware launcher fix at the snippet source + repo-wide re-sync</name>
  <files>gsd-core/workflows/_runtime-launcher.snippet.sh, gsd-core/workflows/*.md (sync output), 19-MERGE-AUDIT.md</files>
  <read_first>
    - gsd-core/workflows/_runtime-launcher.snippet.sh (the `git rev-parse --show-toplevel 2>/dev/null || pwd` root resolution to fix)
    - scripts/sync-runtime-launcher.cjs (how the embed sync works; which files it touches)
  </read_first>
  <action>
    In the snippet SOURCE, change the repo-root resolution from `git rev-parse --show-toplevel 2>/dev/null || pwd` to `git rev-parse --show-toplevel 2>/dev/null || jj workspace root 2>/dev/null || pwd` (git first preserves upstream behavior on git/colocated repos; jj fallback fixes jj-only repos; pwd remains the last resort). Run the sync (`pnpm run sync:launcher` or `node scripts/sync-runtime-launcher.cjs`) to propagate into every workflow embed block. Functional check from a SUBDIRECTORY of this jj-only repo: source the snippet logic in a bash/zsh subshell with cwd set to e.g. `docs/` and assert the resolved root equals the repo root (`jj workspace root` output). Note: this fix removes the launcher's raw-git lint hit organically (relevant to 19-10 allowlist accounting). Ledger row: `merged` for the snippet with the jj-aware rationale.
  </action>
  <verify>
    <automated>rg -c 'jj workspace root' gsd-core/workflows/_runtime-launcher.snippet.sh && [ $(rg -l 'jj workspace root' gsd-core/workflows/*.md | wc -l) -ge 50 ] && (cd docs && bash -c 'ROOT=$(git rev-parse --show-toplevel 2>/dev/null || jj workspace root 2>/dev/null || pwd); test "$ROOT" = "$(jj workspace root)"')</automated>
  </verify>
  <acceptance_criteria>
    - Snippet source contains the three-stage resolution with `jj workspace root` second
    - The sync propagated: >=50 workflow files contain the new resolution line (actual count = sync script's full target set; record exact count)
    - Subdirectory resolution test passes on this jj-only repo
    - No workflow embed was hand-edited (sync script output only — `jj diff --git` shows uniform embed changes)
  </acceptance_criteria>
  <done>Every workflow invocation resolves the right runtime root on jj-only repos.</done>
</task>

<task type="auto">
  <name>Task 3: .githooks rewrite for the adopted tree + hook-script audit</name>
  <files>.githooks/pre-commit, hooks/lib/git-cmd.js (annotations only), 19-MERGE-AUDIT.md</files>
  <read_first>
    - .githooks/pre-commit (current content — upstream auto-won; references STALE sdk/src/query paths and npm run check:* scripts)
    - src/vcs/hook-bridge.cts and src/vcs/backends/jj.cts hook-firing section (the contract: adapter fires .githooks/<stage> on commit — content must be jj-tolerant)
    - hooks/gsd-validate-commit.sh, hooks/gsd-worktree-path-guard.js, hooks/gsd-graphify-update.sh, hooks/lib/git-cmd.js (raw-git audit targets)
  </read_first>
  <action>
    Rewrite `.githooks/pre-commit` for the adopted tree: remove every reference to deleted `sdk/src/query/...` paths and nonexistent `npm run check:*` scripts; keep/restore the checks that have live equivalents (the fork lint scripts under scripts/ that survive — pick from lint-vcs-no-commit-id.cjs, lint-vcs-no-raw-git.cjs as fast pre-commit checks, mirroring the Phase 12 idempotency-audited shape: read-only inspections feeding accept/reject only); guard any `git diff --cached` usage behind a git-availability check so the hook degrades to its non-git checks (not a crash) when fired by the jj adapter's hook-bridge on a non-colocated repo. Verify firing through the PORTED adapter: in a tmp jj repo with .githooks/pre-commit installed (marker-echo body per the Phase 12 test pattern), drive a `vcs.commit()` through `gsd-core/bin/lib/vcs/index.cjs` and assert the marker fired exactly once. Audit the hook scripts for raw git and append ledger dispositions: gsd-graphify-update.sh → `substrate:` (fork allowlist precedent); git-cmd.js token-walk classifier → ledger row noting jj-command-awareness parity is a deferred v1.5 item unless a PreToolUse guard in THIS tree depends on it today (check + record); gsd-validate-commit.sh / gsd-worktree-path-guard.js → per-file disposition rows.
  </action>
  <verify>
    <automated>bash -n .githooks/pre-commit && [ $(rg -c 'sdk/src/query' .githooks/pre-commit || true) -eq 0 ] && node -e "const fs=require('fs');const os=require('os');const cp=require('child_process');const d=fs.mkdtempSync(os.tmpdir()+'/gsd-hook-');process.exit(0)" && echo 'tmp-repo hook-fire test scripted in task (see SUMMARY for transcript)'</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n .githooks/pre-commit` passes; zero references to sdk/src/query or nonexistent scripts (every referenced path exists: scripted existence loop over each path mentioned in the hook)
    - tmp-jj-repo hook-fire test: marker line count === 1 after one vcs.commit() through the ported adapter (HOOK-07 exact-once precedent)
    - Hook degrades (exit 0 path exists) when git is unavailable — asserted by running the hook body with PATH stripped of git in the tmp repo
    - Ledger rows for all four hook scripts + git-cmd.js
  </acceptance_criteria>
  <done>The fork's .githooks firing contract works against the adopted tree through the ported adapter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| launcher root resolution → binary selection | wrong root silently executes a GLOBAL gsd-tools install instead of the repo's (Pitfall 9) |
| hook content → commit gating | a crashing pre-commit blocks all commits; a vacuous one gates nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-23 | Spoofing | wrong-binary execution via pwd fallback on jj-only repos | mitigate | jj workspace root fallback + subdirectory resolution test |
| T-19-24 | DoS | .githooks/pre-commit crash on jj-only (git-less) firing | mitigate | git-availability guard + degraded-path assertion in acceptance |
| T-19-25 | Repudiation | hand-edited embeds drifting from snippet source | mitigate | sync-script-only propagation; uniform-diff check |
</threat_model>

<verification>
- Launcher fix synced and functionally verified; agents rewired with zero dead verbs; hooks live and exactly-once; all dispositions ledgered.
</verification>

<success_criteria>
- Chunk-9 surfaces complete: launcher jj-aware everywhere, executor firewall restored, .githooks firing through the ported adapter.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-09-SUMMARY.md` when done.
</output>
