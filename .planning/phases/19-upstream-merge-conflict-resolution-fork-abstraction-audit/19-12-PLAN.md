---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 12
type: execute
wave: 12
depends_on: [19-11]
files_modified:
  - tests/** (fork-only test deletions, ~70 files)
  - sdk/** (residual tree deletion)
  - get-shit-done/** (residual tree deletion)
  - .github/workflows/parallel-e2e.yml
  - scripts/check-skip-count.cjs companion baseline (re-derived)
  - scripts/lint-vcs-no-raw-git.allow.json (legacy sdk/get-shit-done entry strip)
  - scripts/lint-vcs-no-commit-id.allow.json (legacy entry strip)
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [MERGE-04]
must_haves:
  truths:
    - "The full node:test suite + the ported vitest suite run green on both backends with zero crashed-at-require files"
    - "No sdk/ or get-shit-done/ directory remains in the working copy — every former file has a ledger row"
    - "Skip-count baseline re-derived with per-delta justification; check-skip-count green"
    - "Legacy sdk/** and get-shit-done/** allowlist entries stripped after the residue deletion; BOTH lint gates (lint-vcs-no-raw-git, lint-vcs-no-commit-id) exit 0"
    - "The fork CI lane (parallel-e2e.yml) invokes only commands that exist in the adopted tree"
  artifacts:
    - path: ".github/workflows/parallel-e2e.yml"
      provides: "Fork jj/git parallel CI lane re-pointed at pnpm + gsd-core build/test commands"
  key_links:
    - from: "scripts/run-tests.cjs full suite"
      to: "tests/**/*.test.cjs"
      via: "suite-by-suffix discovery — no test may require() a deleted sdk/ path"
---

<objective>
Triage the remaining fork-only tests (drop the SDK-infrastructure and doc-parity families per locked decisions), delete the residual sdk/ and get-shit-done/ trees, re-derive the skip-count baseline, re-point the fork CI lane, and bring the FULL suite green on both backends.

Purpose: completes MERGE-04; Pitfall 7 (fork-only tests crash the runner by requiring deleted sdk/ paths) is eliminated by explicit triage, never by silence.
Output: green full suite both backends, clean tree (no legacy dirs), justified skip baseline, working CI lane definition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
19-RESEARCH.md (same directory) — targeted sections ONLY, do NOT @-load the full file: "Test-set differences" table (under "Upstream Test Layout + Fork Test Porting") + "Common Pitfalls" (Pitfall 7)
19-MERGE-AUDIT.md (same directory) — append-target ledger; do NOT @-load the full file (read_first entries name the specific rows/sections needed)

HARD CONSTRAINTS: same as 19-01. Locked decisions: SDK-infrastructure tests (~60) DROP + ledger; doc-parity/drift-count tests (~10) DROP + ledger (re-derivation is a future-phase candidate, NOT this phase); release/cherry-pick tooling tests keep iff their target scripts survive. Pre-existing upstream test failures are TRIAGED (recorded, not fixed beyond merge scope) per Claude's-discretion grant.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fork-only test triage — drop the retired families with per-file ledger rows</name>
  <files>tests/** (deletions), 19-MERGE-AUDIT.md</files>
  <read_first>
    - 19-RESEARCH.md "Test-set differences" table (the 147-file fork-only inventory with per-class dispositions)
    - 19-MERGE-AUDIT.md "Pre-port test baseline" appendix (the 19-04 crash/failure set — the triage worklist)
    - Output of `rg -l "sdk/dist|sdk/src|gsd-sdk" tests --glob '*.test.cjs'` (live detector for tests still coupled to retired surfaces)
  </read_first>
  <action>
    Delete, with one ledger row each: (1) the SDK-infrastructure class (~60 files: sdk shim/path/install/bridge tests bug-2334/2439/2441/2519/…/3406, cjs-sdk-bridge-*, config-schema-sdk-parity, the 9 *-generator.test.cjs, gsd-sdk-query-registry-integration, sdk-no-sdk-guard, lint-shared-module-handsync, prompt-budget-sdk-parity-optimizer, runtime-bridge-sync-smoke, enh-3271-sdk-adr-structure) → `dropped:tests-target-retired-SDK`; (2) the doc-parity/drift-count class (~10: architecture-counts, command-count-sync, commands-doc-parity, agents-doc-parity, cli-modules-doc-parity, hooks-doc-parity, inventory-source-parity, config-schema-docs-parity, no-unconditional-win32-skip) → `dropped:doc-parity-invalid-against-upstream-docs (re-derivation = future-phase candidate)`; (3) release/cherry-pick tooling tests (bug-2964/2966/2968/2980/2982/2983/2987/3621): for each, check whether the script it tests exists in the adopted tree — keep and re-point if yes, else `dropped:target-script-retired`; every kept-and-re-pointed test file is a hand-edited checked-in .cjs, so append one row each to the ledger's "Hand-edited .cjs inventory" appendix (file path | plan | node -c confirmed) so the 19-13 node -c loop covers them. Do NOT delete anything outside these enumerated classes; any other failing test goes to the Task 3 triage record instead. After deletion, the retired-surface detector must return only files that were deliberately kept and re-pointed.
  </action>
  <verify>
    <automated>[ $(rg -l 'sdk/dist-cjs|require\(.*sdk/src' tests --glob '*.test.cjs' 2>/dev/null | wc -l) -eq 0 ] && [ ! -e tests/config-schema-sdk-parity.test.cjs ] && [ ! -e tests/architecture-counts.test.cjs ]</automated>
  </verify>
  <acceptance_criteria>
    - Zero test files require sdk/dist-cjs or sdk/src paths
    - Every deleted file has a ledger row whose disposition names its class
    - No file outside the three enumerated classes was deleted
  </acceptance_criteria>
  <done>Runner can discover and load every remaining test file without crashing at require time.</done>
</task>

<task type="auto">
  <name>Task 2: Residual tree deletion (sdk/, get-shit-done/) + skip-count re-baseline</name>
  <files>sdk/** (delete), get-shit-done/** (delete), scripts/check-skip-count.cjs companion baseline, scripts/lint-vcs-no-raw-git.allow.json, scripts/lint-vcs-no-commit-id.allow.json, 19-MERGE-AUDIT.md</files>
  <read_first>
    - Output of `jj file list -r @ sdk get-shit-done` (the exact residual file set to disposition)
    - 19-MERGE-AUDIT.md (which residuals already have rows from 19-03/19-05/19-09 — port sources, moved files)
    - scripts/check-skip-count.cjs (how the baseline is stored/derived)
  </read_first>
  <action>
    Enumerate every remaining file under `sdk/` and `get-shit-done/` in the working copy. For each: if it already has a ledger row (port source, moved file, harvested reference), delete it; if not, add the row first (`ported-to:`/`re-applied-at:`/`dropped:<reason>`) then delete. End state: neither directory exists. Then strip the legacy allowlist entries retained by 19-10 under the `pending-19-12-residue-deletion` ledger row: remove every entry whose `path`/`glob` field starts with `sdk/` or `get-shit-done/` (match on the path fields, NOT on prose in `reason` strings — several reasons mention sdk/ paths as historical citation and must survive) from scripts/lint-vcs-no-raw-git.allow.json AND scripts/lint-vcs-no-commit-id.allow.json, mark the pending ledger row resolved, and re-run BOTH lint gates (`node scripts/lint-vcs-no-raw-git.cjs`, `node scripts/lint-vcs-no-commit-id.cjs`) — both must exit 0 against the residue-free tree. Then re-derive the skip-count baseline: run `node scripts/check-skip-count.cjs`, and update its baseline to the post-triage count with a ledger justification decomposing the delta (fork-only deletions removed N skips; upstream-only tests added M; net change explained). Record in the ledger's "Skip-count / baseline re-derivations" appendix.
  </action>
  <verify>
    <automated>[ ! -d sdk ] && [ ! -d get-shit-done ] && [ $(jq '[.entries[]? | select((.path // .glob) | test("^(sdk/|get-shit-done/)"))] | length' scripts/lint-vcs-no-raw-git.allow.json) -eq 0 ] && [ $(jq '[.entries[]? | select((.path // .glob) | test("^(sdk/|get-shit-done/)"))] | length' scripts/lint-vcs-no-commit-id.allow.json) -eq 0 ] && node scripts/lint-vcs-no-raw-git.cjs && node scripts/lint-vcs-no-commit-id.cjs && node scripts/check-skip-count.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `sdk/` and `get-shit-done/` directories do not exist
    - `jj file list -r @ sdk get-shit-done` returns empty
    - check-skip-count exits 0 against the re-derived baseline; the delta decomposition is in the ledger appendix
    - Zero sdk/** or get-shit-done/** entries remain in either lint allowlist; the `pending-19-12-residue-deletion` ledger row is marked resolved
    - BOTH lint gates (lint-vcs-no-raw-git, lint-vcs-no-commit-id) exit 0 after the strip
  </acceptance_criteria>
  <done>Tree contains only the adopted layout; the 19-13 comm proof has a closed universe to verify.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point parallel-e2e.yml + full suite green on both backends</name>
  <files>.github/workflows/parallel-e2e.yml, 19-MERGE-AUDIT.md</files>
  <read_first>
    - .github/workflows/parallel-e2e.yml (survived untouched — its sdk/pnpm-workspace invocations are now stale)
    - .github/workflows/test.yml (the 19-04 de-org'd pnpm install pattern to mirror)
  </read_first>
  <action>
    Re-point parallel-e2e.yml: replace `pnpm -F sdk ...` workspace invocations with root-level equivalents (`pnpm run build:lib`, `npx vitest run`, `node scripts/run-tests.cjs ...`); install steps mirror test.yml's pnpm pattern; keep the inverted-polarity matrix (git allow-fail / jj-colocated required) and the parallel-e2e-gate aggregate job byte-identical in structure; the CI-06 audit step now invokes the re-pointed scripts/audit-workflow-raw-git.cjs. Every command in the file must exist locally — dry-run each `run:` command's executable form locally where feasible (the harness scripts themselves). Then the FULL suite gate: `GSD_TEST_BACKENDS=git,jj node scripts/run-tests.cjs` (all suites — the env prefix makes the ported node:test cross-backend cells actually run BOTH backends) green, `GSD_TEST_BACKENDS=git,jj npx vitest run` green. Pre-existing upstream failures unrelated to the merge (assumption A5): record each in a "Pre-existing upstream failures (triaged, not fixed)" ledger appendix with file + one-line cause + why-unrelated rationale — the suite is green MODULO exactly that enumerated, justified set, and the set must be empty or every entry traceable to upstream-side behavior reproducible at 03764dbc.
  </action>
  <verify>
    <automated>[ $(rg -c 'pnpm -F sdk' .github/workflows/parallel-e2e.yml || true) -eq 0 ] && GSD_TEST_BACKENDS=git,jj node scripts/run-tests.cjs && GSD_TEST_BACKENDS=git,jj npx vitest run --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - Zero stale `-F sdk` invocations in parallel-e2e.yml; every run: command's binary/script exists in the tree
    - `GSD_TEST_BACKENDS=git,jj node scripts/run-tests.cjs` exits 0 (or fails only on entries in the triaged pre-existing appendix, each justified and reproducible at 03764dbc)
    - Both-backend vitest run exits 0
    - check-skip-count still green after the full run
  </acceptance_criteria>
  <done>MERGE-04 complete: both suites green on both backends with a justified skip baseline and a working CI lane definition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test triage → coverage truth | over-deletion silently removes jj coverage; under-deletion crashes the runner |
| residual trees → ledger completeness | any undispositioned residual file is a MERGE-02 hole |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-32 | Repudiation | test deleted outside the enumerated classes | mitigate | class-scoped deletion rule; non-class failures go to the triage record, not the bin |
| T-19-33 | Tampering | skip-count baseline moved without decomposition | mitigate | delta decomposition required in ledger appendix |
| T-19-34 | DoS | CI lane invoking dead commands | mitigate | per-command existence check; mirror of vetted test.yml install pattern |
</threat_model>

<verification>
- Full node:test + vitest green both backends; trees gone; skip baseline justified; CI lane re-pointed.
</verification>

<success_criteria>
- MERGE-04 closed; tree reduced to the adopted layout with total ledger coverage, ready for the 19-13 completeness proof.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-12-SUMMARY.md` when done.
</output>
