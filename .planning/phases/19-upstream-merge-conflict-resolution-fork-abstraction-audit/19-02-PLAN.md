---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - pnpm-lock.yaml (generated)
  - node_modules/ (generated)
  - gsd-core/bin/lib/*.cjs (build output, gitignored)
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: false
requirements: [MERGE-03]
must_haves:
  truths:
    - "Every dependency in the resolved manifest was registry-vetted and human-approved BEFORE the single pnpm install"
    - "Upstream src/*.cts compiles in-tree (pnpm run build:lib green) before any port work begins, so later port errors are attributable"
    - "gsd-core/bin/gsd-tools.cjs loads and dispatches against the built lib"
  artifacts:
    - path: "pnpm-lock.yaml"
      provides: "Fresh lockfile generated from the fork-controlled manifest (never hand-merged)"
    - path: "gsd-core/bin/lib/semver-compare.cjs"
      provides: "Build sentinel proving tsc -p tsconfig.build.json emitted"
  key_links:
    - from: "tsconfig.build.json"
      to: "gsd-core/bin/lib/"
      via: "pnpm run build:lib (tsc rootDir src, outDir gsd-core/bin/lib, noEmitOnError)"
      pattern: "build:lib"
---

<objective>
Vet every dependency in the resolved manifest against the npm registry, get human sign-off (mandatory legitimacy gate — all upstream devDeps are [ASSUMED] per RESEARCH.md Package Legitimacy Audit), then perform the single pnpm install and prove upstream's build chain is green in-tree before any port work.

Purpose: MERGE-03 build bring-up; supply-chain gate before the only install of the phase.
Output: vetting table, fresh pnpm-lock.yaml, built gsd-core/bin/lib/, gsd-tools dispatch smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-RESEARCH.md
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md

HARD CONSTRAINTS: same as 19-01 (working copy on top of vpzlrrlv; jj only; no rewrite of vpzlrrlv or below).
NOTE: conflicted files still exist elsewhere in the tree (buckets A–G resolve in 19-03/19-04). That is expected — `src/` and `tsconfig.build.json` are clean upstream additions, so the build gate is meaningful now. Do NOT run the node:test suite yet (tests/helpers.cjs still carries conflict markers; baseline run happens in 19-04).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry-vet every manifest dependency (automated evidence gathering)</name>
  <files>.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md (append vetting appendix)</files>
  <read_first>
    - package.json (resolved manifest from 19-01 — the authoritative dependency list to vet)
    - 19-RESEARCH.md section "Package Legitimacy Audit (REVISED)" (the [ASSUMED] classification table)
  </read_first>
  <action>
    For EVERY entry in dependencies + devDependencies + optionalDependencies of the resolved package.json (expected set: @anthropic-ai/claude-agent-sdk, ws, typescript, eslint, @eslint/js, typescript-eslint, eslint-plugin-n, eslint-plugin-no-only-tests, globals, @stryker-mutator/core, fast-check, c8, js-yaml, @types/node, vitest — plus anything else present), run `npm view <pkg> name version time.modified maintainers --json` (read-only registry query, NOT an install) and record per package: resolved latest version, whether the manifest range is satisfiable, last-publish date, maintainer count. Confirm `fallow` is ABSENT from the manifest. Write the results as a "Dependency vetting (pre-install)" appendix table in 19-MERGE-AUDIT.md, flagging any package whose registry data looks anomalous (publish date in the last 30 days for a supposedly-mature package, single anonymous maintainer, name within edit-distance 1 of a popular package). This evidence table is what the human reviews in Task 2.
  </action>
  <verify>
    <automated>rg -c 'Dependency vetting \(pre-install\)' .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md && node -e "const p=require('./package.json');const all={...p.dependencies,...p.devDependencies,...(p.optionalDependencies||{})};if(all.fallow)throw new Error('fallow present');console.log(Object.keys(all).length+' deps to vet')"</automated>
  </verify>
  <acceptance_criteria>
    - The vetting appendix has one row per manifest dependency (row count equals the dep count printed by the verify command)
    - Each row carries version, satisfiability, last-publish date, and an explicit OK/FLAGGED verdict
    - No install command was executed (node_modules/ still absent)
  </acceptance_criteria>
  <done>Complete registry evidence table exists for human review; zero installs performed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Human legitimacy sign-off before install</name>
  <what-built>Automated registry vetting of every dependency in the resolved manifest (Task 1 appendix table in 19-MERGE-AUDIT.md). All upstream devDeps are classified [ASSUMED] in RESEARCH.md — human verification is mandatory and not auto-approvable.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md`, section "Dependency vetting (pre-install)".
    2. Spot-check at least the FLAGGED rows (if any) and the less-famous names (eslint-plugin-no-only-tests, globals, fast-check, c8) on https://www.npmjs.com/package/<name> — confirm each is the well-known package (download counts, repo link, maintainers).
    3. Confirm `fallow` does not appear in package.json.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the single pnpm install, or name the package(s) to remove/replace.</resume-signal>
  <files>none (human gate — no tree changes)</files>
  <action>PAUSE for human review of the Task 1 vetting evidence. Do NOT run any install until the operator responds. If the operator names packages to remove/replace, amend package.json accordingly, re-run the Task 1 vetting for the changed entries, and re-present this checkpoint.</action>
  <verify>
    <automated>NONE — blocking human gate (never auto-approvable); the resume signal is the verification</automated>
  </verify>
  <done>Operator typed "approved" (or the manifest was amended per operator instruction and then approved).</done>
</task>

<task type="auto">
  <name>Task 3: Single pnpm install + upstream build bring-up + dispatch smoke</name>
  <files>pnpm-lock.yaml (generated), gsd-core/bin/lib/ (build output), 19-MERGE-AUDIT.md (append build-bring-up note)</files>
  <read_first>
    - tsconfig.build.json (build contract: rootDir src, outDir gsd-core/bin/lib, noEmitOnError)
    - scripts/run-tests.cjs (ensureBuiltArtifacts sentinel logic — context only, do not run the suite)
    - gsd-core/bin/gsd-tools.cjs (lines around 344: the `query` meta-prefix dispatch — the surface 19-06 extends)
  </read_first>
  <action>
    Run `pnpm install` (plain install; manifest is fork-controlled and human-approved). Commit/record the freshly generated `pnpm-lock.yaml`. Then run `pnpm run build:lib` and require a clean exit (tsc -p tsconfig.build.json, noEmitOnError — upstream src must compile in-tree BEFORE the vcs port so later errors are attributable to the port). Verify the build sentinel `gsd-core/bin/lib/semver-compare.cjs` exists. Smoke the dispatch chain: `node gsd-core/bin/gsd-tools.cjs --help` exits 0, and `node gsd-core/bin/gsd-tools.cjs query state.load` (or another existing read-only verb from the gsd-tools case list) returns a JSON envelope rather than "Unknown command". Append a ledger note recording: install timestamp, lockfile generated, build green, dispatch smoke result. Do NOT run the test suite (tests/helpers.cjs is still conflicted).
  </action>
  <verify>
    <automated>pnpm run build:lib && test -f gsd-core/bin/lib/semver-compare.cjs && node gsd-core/bin/gsd-tools.cjs --help >/dev/null && test -f pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build:lib` exits 0 with no TypeScript errors
    - `gsd-core/bin/lib/semver-compare.cjs` exists (sentinel)
    - `node gsd-core/bin/gsd-tools.cjs --help` exits 0
    - `pnpm-lock.yaml` exists and `package-lock.json` does not
    - exactly one `pnpm install` was run in this phase so far
  </acceptance_criteria>
  <done>Upstream build chain proven green in-tree; dispatch surface alive; lockfile fresh.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | unvetted third-party code enters the executable environment at install |
| lifecycle scripts → local machine | install-time scripts execute arbitrary code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-SC | Tampering | pnpm install of [ASSUMED] upstream deps | mitigate | Task 1 automated registry evidence + Task 2 blocking human checkpoint BEFORE the single install; `fallow` already dropped in 19-01 |
| T-19-04 | Spoofing | typosquat within the dep set | mitigate | edit-distance flagging rule in Task 1; human spot-check in Task 2 |
| T-19-05 | Tampering | hand-merged lockfile smuggling resolved URLs | mitigate | both old lockfiles deleted in 19-01; pnpm-lock.yaml regenerated from scratch here |
</threat_model>

<verification>
- Build green, sentinel present, dispatch smoke passes, lockfile fresh, ledger updated.
</verification>

<success_criteria>
- Human-approved dependency set installed exactly once; `pnpm run build:lib` green pre-port; gsd-tools dispatch alive.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-02-SUMMARY.md` when done.
</output>
