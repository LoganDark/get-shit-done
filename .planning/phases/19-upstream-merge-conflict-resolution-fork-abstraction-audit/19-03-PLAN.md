---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/harvest/** (new reference copies)
  - sdk/src/query/*.ts (delete, bucket A)
  - sdk/src/*.ts, sdk/tsconfig.json, sdk/vitest.config.ts, sdk/README.md, sdk/shared/config-schema.manifest.json (delete, bucket A)
  - get-shit-done/bin/gsd-tools.cjs + get-shit-done/bin/lib/*.cjs (delete, bucket B)
  - get-shit-done/workflows/{code-review,complete-milestone,execute-phase,help,new-milestone,new-project,plan-phase,quick,undo}.md (delete, bucket C)
  - get-shit-done/templates/config.json (delete, bucket C)
  - gsd-core/templates/config.json (fork content applied)
  - gsd-core/bin/shared/config-schema.manifest.json (fork delta grafted)
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [MERGE-01, MERGE-02]
must_haves:
  truths:
    - "Every fork file deleted in this plan was harvested first (reference copy from c7bd6bee) or explicitly ledgered as not-needed — no blind deletions"
    - "All bucket A/B/C conflicts are resolved; jj resolve --list shows only bucket D/E/F/G paths (~20)"
    - "The fork's config-schema additions (e.g. parallelization, vcs.adapter) and the Phase-14 templates/config.json flat-boolean parallelization flip survive at their gsd-core counterpart paths"
  artifacts:
    - path: ".planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/harvest/"
      provides: "c7bd6bee-sourced reference copies: 23 vcs-facing query handlers, 10 bin/lib CJS reference implementations, 9 workflow base→fork delta diffs, sdk/vitest.config.ts, config-schema fork delta, templates/config.json"
    - path: "gsd-core/templates/config.json"
      provides: "Fork template content (upstream made zero changes vs base)"
      contains: "parallelization"
  key_links:
    - from: "harvest/workflow-deltas/*.diff"
      to: "gsd-core/workflows/*.md (19-08)"
      via: "base→fork diff is the exact delta to re-apply"
    - from: "harvest/bin-lib/*.cjs"
      to: "src/{commands,verify,init,core}.cts (19-07)"
      via: "line-annotated '// (was: <git cmd>)' reference implementations"
---

<objective>
Harvest all fork reference content needed by later waves from revision c7bd6bee, then mechanically clear conflict buckets A (sdk TS + packaging, ~33 remaining), B (get-shit-done bin CJS, 10), and C (workflows + templates/config.json, 10) by accepting upstream deletion — with the two fork JSON payloads (templates/config.json, config-schema fork delta) re-applied at their gsd-core counterparts immediately so they cannot be forgotten.

Purpose: MERGE-01 bulk conflict clearing under the locked upstream-side-wins-at-tree-level direction; Pitfall 3 (silently lost fork capability) countered by harvest-before-delete + per-file ledger rows.
Output: harvest/ reference tree, ~53 conflicts resolved, 2 fork payloads re-homed, ledger rows for every path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-RESEARCH.md
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md

HARD CONSTRAINTS: same as 19-01. Recovery reads are `jj file show -r c7bd6bee <path>`; NEVER recover content from the conflicted working-copy text (jj materializes delete/modify conflicts as a diff format, not both-sides text — hand-stripping markers reproduces MERGE-REVIEW BLOCKER-1).
Resolving a delete/modify conflict as "deleted" = removing the file from the working copy. The authoritative remaining-conflict list is always `jj resolve --list`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harvest fork reference content from c7bd6bee into the phase harvest/ directory</name>
  <files>.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/harvest/** (new)</files>
  <read_first>
    - 19-RESEARCH.md sections "Conflict Buckets" (bucket A/B/C harvest lists) and "Counterpart Map"
    - Output of `jj resolve --list` (authoritative current conflict set)
    - Output of `jj file list -r c7bd6bee sdk/src/query` (enumerate the vcs-facing handler files)
  </read_first>
  <action>
    Create `harvest/` inside the phase directory with four subdirectories, populated via scripted `jj file show -r c7bd6bee` loops: (1) `query-handlers/` — the vcs-facing sdk/src/query handler sources that define the CLI envelope contract for 19-06: commit, commit-to-subrepo, log, diff, status, head-ref, current-branch, branch-list/create/delete (whatever the actual filenames are — enumerate via `jj file list -r c7bd6bee sdk/src/query` and grep each for createVcsAdapter/vcs. usage), merge, push, reset, restore, revert, rev-parse, rm, hooks, migrate-vcs, workspace*, worktree, cleanup-subagent-workspaces; record the exact harvested filename list in the ledger (target ~23 files). (2) `bin-lib/` — the 10 fork CJS reference implementations: get-shit-done/bin/gsd-tools.cjs and bin/lib/{command-aliases.generated,commands,config-schema,core,graphify,init,shell-command-projection,verify,worktree-safety}.cjs. (3) `workflow-deltas/` — for each of the 9 conflicted workflows (code-review, complete-milestone, execute-phase, help, new-milestone, new-project, plan-phase, quick, undo): `diff <(jj file show -r b533f71857ab get-shit-done/workflows/X.md) <(jj file show -r c7bd6bee get-shit-done/workflows/X.md) > X.md.diff` (a missing-at-base file means the whole fork file is the delta — note that in the diff header). (4) `misc/` — fork sdk/vitest.config.ts, fork sdk/shared/config-schema.manifest.json, fork get-shit-done/templates/config.json, plus a `config-schema-delta.diff` of fork manifest vs gsd-core/bin/shared/config-schema.manifest.json (working copy). Add a ledger row noting harvest/ is phase-local reference material (not shipped tree content).
  </action>
  <verify>
    <automated>H=.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/harvest; test -d $H/query-handlers && test -d $H/bin-lib && test -d $H/workflow-deltas && test -d $H/misc && [ $(ls $H/bin-lib | wc -l) -eq 10 ] && [ $(ls $H/workflow-deltas/*.diff | wc -l) -eq 9 ] && test -f $H/misc/config.json && test -f $H/misc/vitest.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - harvest/bin-lib has exactly 10 files; harvest/workflow-deltas has exactly 9 .diff files; misc has vitest.config.ts, config-schema.manifest.json, config.json, config-schema-delta.diff
    - harvest/query-handlers contains every sdk/src/query file that references createVcsAdapter or vcs.* verbs at c7bd6bee (enumerated list recorded in the ledger)
    - All harvested content byte-matches `jj file show -r c7bd6bee <path>` (sourced from the revision, not the conflicted working copy)
  </acceptance_criteria>
  <done>Every piece of fork content later waves need exists as a c7bd6bee-sourced reference copy before any deletion happens.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve buckets A + B as upstream deletions (ledger row per file)</name>
  <files>sdk/src/query/*.ts (delete), sdk/src/*.ts (delete), sdk/{tsconfig.json,vitest.config.ts,README.md} (delete), sdk/shared/config-schema.manifest.json (delete), get-shit-done/bin/gsd-tools.cjs (delete), get-shit-done/bin/lib/*.cjs (delete), 19-MERGE-AUDIT.md (append ~43 rows)</files>
  <read_first>
    - Output of `jj resolve --list` filtered to `sdk/` and `get-shit-done/bin/` (the exact path set to resolve — do not work from the research's approximate counts)
    - 19-RESEARCH.md "Conflict Buckets" rows A and B (per-file dispositions)
  </read_first>
  <action>
    For every remaining conflicted path under `sdk/` (bucket A; sdk/package.json was already resolved in 19-01) and under `get-shit-done/bin/` (bucket B): delete the file from the working copy (this resolves the delete/modify conflict as upstream-deletion). Then append one ledger row per path. Disposition values: harvested reference files → `dropped:superseded-by-upstream-restructure; reference copy at harvest/<subdir>/<name>` ; vcs-facing query handlers → `ported-to:src/vcs-command-router.cts (19-06, envelope reference at harvest/query-handlers/)` ; bin-lib reference CJS → `ported-to:src/<module>.cts call-site migration (19-07, reference at harvest/bin-lib/)` ; everything else (non-vcs query handlers, sdk infrastructure, tsconfig, README) → `dropped:<one-line reason>` (typical reason: SDK retired per upstream ADR-0174, upstream counterpart at gsd-core/ or src/). Verified pre-condition from research: the fork gsd-tools case list is a strict subset of upstream's, so deleting fork gsd-tools.cjs loses no dispatch case.
  </action>
  <verify>
    <automated>[ $(jj resolve --list | grep -c -E '^(sdk/|get-shit-done/bin/)' || true) -eq 0 ] && [ ! -e get-shit-done/bin/gsd-tools.cjs ] && [ ! -e sdk/src/query ]</automated>
  </verify>
  <acceptance_criteria>
    - `jj resolve --list` contains zero paths under sdk/ or get-shit-done/bin/
    - 19-MERGE-AUDIT.md gained exactly one row per path resolved in this task (row count matches the resolved-path count recorded at task start)
    - No file content was reconstructed from conflicted working-copy text
  </acceptance_criteria>
  <done>Buckets A and B fully cleared; every deletion traceable to a ledger row with a port/drop disposition.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve bucket C as upstream deletions + immediately re-home the two fork JSON payloads</name>
  <files>get-shit-done/workflows/*.md (delete 9), get-shit-done/templates/config.json (delete), gsd-core/templates/config.json (overwrite with fork content), gsd-core/bin/shared/config-schema.manifest.json (graft fork delta), 19-MERGE-AUDIT.md (append rows)</files>
  <read_first>
    - harvest/misc/config.json (fork template content to apply)
    - harvest/misc/config-schema-delta.diff (the fork schema additions to graft)
    - gsd-core/templates/config.json (current upstream content in working copy)
    - gsd-core/bin/shared/config-schema.manifest.json (current upstream content)
  </read_first>
  <action>
    Delete the 9 conflicted `get-shit-done/workflows/*.md` files and `get-shit-done/templates/config.json` (their fork deltas live in harvest/ and are re-applied at gsd-core counterparts in 19-08). Then re-home the two JSON payloads NOW (small, mechanical, high forget-risk): (1) verify upstream made no changes to templates/config.json vs base (`jj file show -r b533f71857ab get-shit-done/templates/config.json` vs `jj file show -r 03764dbc gsd-core/templates/config.json` — research says delta is zero; if NOT zero, stop and do a field-level merge instead of overwrite, recording the divergence in the ledger), then write harvest/misc/config.json content onto `gsd-core/templates/config.json` — this carries the Phase-14 flat-boolean `"parallelization": true` flip and the fork `vcs` block. (2) Apply the fork's schema additions from config-schema-delta.diff onto `gsd-core/bin/shared/config-schema.manifest.json` as a field-level JSON graft (add fork-only keys such as `parallelization` and `vcs.adapter`; do not remove upstream-added keys). Validate both files with `node -e "JSON.parse(...)"`. Ledger rows: 9 workflows → `re-applied-at:gsd-core/workflows/<name>.md (19-08, delta at harvest/workflow-deltas/)`; templates/config.json → `re-applied-at:gsd-core/templates/config.json (done this plan)`; manifest delta → `re-applied-at:gsd-core/bin/shared/config-schema.manifest.json (done this plan)`.
  </action>
  <verify>
    <automated>[ $(jj resolve --list | grep -c -E '^get-shit-done/' || true) -eq 0 ] && node -e "const c=require('./gsd-core/templates/config.json'); if(c.parallelization!==true)throw new Error('parallelization flip lost')" && node -e "JSON.parse(require('fs').readFileSync('gsd-core/bin/shared/config-schema.manifest.json','utf8'))"</automated>
  </verify>
  <acceptance_criteria>
    - `jj resolve --list` contains zero get-shit-done/ paths; total remaining conflicts ≈ 20, all in buckets D/E/F/G (docs, .github, tests, package surfaces, agents)
    - `gsd-core/templates/config.json` parses and has `"parallelization": true` plus the fork `vcs` block
    - `gsd-core/bin/shared/config-schema.manifest.json` parses and contains every fork-only key from config-schema-delta.diff while retaining all upstream keys
    - One ledger row per path resolved in this task
  </acceptance_criteria>
  <done>Bucket C cleared; the two easily-forgotten fork JSON payloads already live at their upstream-canonical homes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mass deletion → fork capability | ~53 fork files deleted in one wave; silent capability loss is the dominant failure mode (Pitfall 3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | Repudiation | bucket A/B/C deletions | mitigate | harvest-before-delete; one ledger row per path with parseable disposition; 19-13 comm proof closes the loop |
| T-19-07 | Tampering | reconstructing content from conflicted marker text (BLOCKER-1 pattern) | mitigate | all recovery reads pinned to `jj file show -r c7bd6bee`; working-copy conflicted text never used as a source |
| T-19-08 | DoS | losing the parallelization/vcs config schema keys breaks every fork workflow | mitigate | payloads re-homed in the SAME plan as the deletion, with node JSON assertions in verify |
</threat_model>

<verification>
- `jj resolve --list` is down to the D/E/F/G set; marker scan of files touched this plan (with 19-01 exclusion list) is clean; `vpzlrrlv` untouched.
</verification>

<success_criteria>
- ~53 conflicts cleared with full harvest + ledger coverage; fork config payloads live at gsd-core paths; only genuine-merge conflicts remain.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-03-SUMMARY.md` when done.
</output>
