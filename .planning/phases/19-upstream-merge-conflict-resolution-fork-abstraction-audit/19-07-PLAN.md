---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 07
type: execute
wave: 7
depends_on: [19-06]
files_modified:
  - src/commands.cts
  - src/verify.cts
  - src/init.cts
  - src/core.cts
  - src/graphify.cts
  - src/check-command-router.cts
  - src/roadmap-upgrade.cts
  - src/drift.cts
  - src/research-store.cts
  - src/learnings.cts
  - src/intel.cts
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [AUDIT-01]
must_haves:
  truths:
    - "No execSync/execFileSync git outlier remains anywhere in src/ — including the destructive `git reset --hard` rollback in roadmap-upgrade.cts, now routed through adapter restore semantics"
    - "Every remaining execGit call site in src/ lives in an explicitly-ledgered substrate module (shell-command-projection, worktree-safety, worktree-base-ref, graphify substrate reads)"
    - "upstream cmdCommit internals route through the ported VcsAdapter (single commit path, no parallel verb)"
    - "jj backend never volunteers commit_id through any migrated path (unified revision model: LogEntry.id)"
  artifacts:
    - path: "src/commands.cts"
      provides: "Adapter-routed command internals (replayed from fork commands.cjs annotations)"
    - path: "src/roadmap-upgrade.cts"
      provides: "Non-destructive, adapter-routed rollback path"
  key_links:
    - from: "src/commands.cts"
      to: "src/vcs/index.cts"
      via: "sibling import './vcs/index.cjs' + createVcsAdapter"
      pattern: "\\./vcs/index\\.cjs"
    - from: "src/roadmap-upgrade.cts rollback"
      to: "adapter restore verb"
      via: "vcs restore semantics replacing execSync('git reset --hard')"
---

<objective>
Migrate upstream's raw-git call sites through the ported VcsAdapter (porting-map chunk 7): replay the fork's line-annotated reference migrations onto src/commands.cts and src/verify.cts, migrate the smaller modules and the five seam-bypassing outliers (including the destructive `git reset --hard` rollback), and formally disposition the substrate modules with ledger rows.

Purpose: AUDIT-01 — every upstream raw-git/SHA site migrated or justified; the jj-support invariants (unified revision model, no commit_id from jj) extended over upstream's command surface.
Output: 8 migrated modules, substrate dispositions, clean seam sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
19-RESEARCH.md (same directory) — targeted sections ONLY, do NOT @-load the full file: "Raw-Git Seam Inventory" + "Porting Map" chunk 7 + "Common Pitfalls" (Pitfall 10)
19-MERGE-AUDIT.md (same directory) — append-target ledger; do NOT @-load the full file (read_first entries name the specific rows/sections needed)
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/harvest/bin-lib/ (the fork's line-annotated reference migrations)

HARD CONSTRAINTS: same as 19-01. All edits in src/*.cts (never the emitted gsd-core/bin/lib artifacts). `pnpm run build:lib` after each module batch. Pitfall 10: do not touch upstream's execGit/SpawnResultOutput shape in shell-command-projection.cts.
OPEN-Q6 DECISION (made at plan time, Claude's discretion): `worktree-base-ref.cts` stays git-only substrate; jj needs no base-mismatch degrade because jj workspaces do not share git's worktree base problem — the jj path is served by `workspace.parallel.*`. Documented as a ledger row + code comment, revisited only if chunk-8 finds a shared workflow path calling it under jj (assumption A6).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate commands.cts (14 sites, incl. cmdCommit internals) + verify.cts (6 sites)</name>
  <files>src/commands.cts, src/verify.cts</files>
  <read_first>
    - src/commands.cts and src/verify.cts (current upstream raw-git state — find every `execGit(` site first: `rg -n 'execGit\(' src/commands.cts src/verify.cts`)
    - harvest/bin-lib/commands.cjs and harvest/bin-lib/verify.cjs (the fork's reference migrations with `// (was: <git command>)` line annotations — the exact verb mapping to replay)
    - src/vcs/types.cts (adapter verb signatures: vcs.commit, vcs.status, vcs.diff, vcs.log, vcs.refs.*)
  </read_first>
  <action>
    Replay the fork's annotated migration onto src/commands.cts: each execGit site replaced by its fork-reference adapter call — `rev-parse --abbrev-ref HEAD` → `vcs.refs.currentBookmarks()`; `checkout [-b]` → `vcs.refs.bookmarks.switch(name, {create})`; `commit [-m/--amend]` + `add` + `rm --cached` → `vcs.commit({message, files, noVerify, respectStaged})` (cmdCommit INTERNALS route through the adapter per the locked decision — the CLI command name and flag surface stay byte-compatible, including `--files`); `rev-parse --short HEAD` → `vcs.refs.resolveShort(vcs.refs.head)`; `status --porcelain` → `vcs.status({porcelain})`; `diff --cached --name-only` → `vcs.diff({staged, nameOnly})`; `rev-list --count/--max-parents=0` and `show -s --format=%as` → the fork reference's equivalents (read the annotation; if the fork ledgered one as substrate, mirror that annotation instead of inventing a verb). src/verify.cts: `log --oneline --all -50` → `vcs.log()`; `diff --name-status <base> HEAD` → `vcs.diff()`; `cat-file -t <hash>` probes → unified-revision verification via `LogEntry.id` per the fork verify.cjs reference (legitimate hex fixtures keep the fork's allowlist annotation style). Adapter import: sibling `./vcs/index.cjs`, instantiated per the fork reference pattern (per-invocation factory with cwd). Unified-model invariant: JSON envelopes expose `LogEntry.id`; no migrated path may surface a git-only commit_id field when the backend is jj. Build, then run the explicit known-good regression set (the two 19-06-ported tests; see verify). Baseline comparison (acceptance criterion, checked here in the action): run `node scripts/run-tests.cjs --suite unit` and diff its failure set against the "Pre-port test baseline" appendix recorded by 19-04 in 19-MERGE-AUDIT.md — NO new failures attributable to this migration; fork-leftover SDK-infrastructure crashes already in the baseline are expected to persist until 19-12 and do not count (which is why this comparison lives in the action, not in the automated verify exit-code gate).
  </action>
  <verify>
    <automated>pnpm run build:lib && [ $(rg -c 'execGit\(' src/commands.cts src/verify.cts | rg -v ':0$' | wc -l) -eq 0 ] && node scripts/run-tests.cjs --files tests/vcs-cjs-smoke.test.cjs tests/vcs-adapter-contract.test.cjs && ROOT=$PWD && D=$(mktemp -d) && (cd "$D" && jj git init --colocate >/dev/null 2>&1 && echo x > f.txt && node "$ROOT/gsd-core/bin/gsd-tools.cjs" query commit "smoke" --files f.txt >/dev/null && node "$ROOT/gsd-core/bin/gsd-tools.cjs" query log | node -e "let s='';process.stdin.on('data',c=>s+=c).on('end',()=>{const j=JSON.parse(s);if(JSON.stringify(j).includes('commit_id'))throw new Error('commit_id leaked from jj backend')})")</automated>
  </verify>
  <acceptance_criteria>
    - Zero `execGit(` call sites remain in src/commands.cts and src/verify.cts
    - Both files import `./vcs/index.cjs`; build green
    - cmdCommit's CLI contract unchanged (same command name, --files and existing flags accepted) — assert by invoking `node gsd-core/bin/gsd-tools.cjs query commit --help`-style probe or a tmp-repo commit
    - A tmp jj-repo invocation of the migrated commit/log path emits envelopes whose revision field is the unified id (no commit_id key from the jj backend)
    - Unit-suite failure set is not worse than the 19-04 "Pre-port test baseline" appendix (no NEW failures attributable to this migration; pre-existing fork-leftover SDK crashes excluded — they persist until 19-12)
  </acceptance_criteria>
  <done>The two reference-backed migrations replayed completely; single adapter-routed commit path live.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the small modules + the 5 seam-bypassing outliers (incl. destructive rollback)</name>
  <files>src/init.cts, src/core.cts, src/graphify.cts, src/check-command-router.cts, src/roadmap-upgrade.cts, src/drift.cts, src/research-store.cts, src/learnings.cts, src/intel.cts</files>
  <read_first>
    - `rg -n "execGit\(|execSync|execFileSync" src/{init,core,graphify,check-command-router,roadmap-upgrade,drift,research-store,learnings,intel}.cts` (exact current site list)
    - harvest/bin-lib/{init,core,graphify}.cjs (fork reference migrations/annotations for these modules)
    - src/vcs/index.cts (restore/status/log/refs surface)
  </read_first>
  <action>
    init.cts: `status --porcelain` ×2 → `vcs.status()`; `--version` probe and `rev-parse --show-prefix` → mirror the fork init.cjs reference (backend capability probe / adapter root, or fork's substrate annotation if that is what the reference did). core.cts: `rev-parse --is-inside-work-tree` + `--show-toplevel` → backend-aware root detection per fork core.cjs; `check-ignore -q --no-index` is git-only — keep on the seam with the fork's substrate annotation style + ledger row. graphify.cts: 2 reads stay raw per fork precedent ("jj commit_ids ARE git commit_ids in colocated repos") — add the same inline annotation the fork allowlist used + ledger row `substrate:graphify-colocated-read`. check-command-router.cts:252 `execFileSync('git',['log','-n','200',...])` → `vcs.log({limit:200})` with format shaping in JS. roadmap-upgrade.cts (SECURITY-CRITICAL, T-19-DG): :484 `execSync('git status --porcelain')` → `vcs.status()`; :495 headSha capture → `vcs.refs.head` resolved id (unified model); :579–580 `execSync('git reset --hard <sha>')` + `git clean -fd .planning/phases/` → adapter restore semantics (the fork `restore` verb surface used by undo.md/gsd-code-fixer) restoring the captured revision and the .planning/phases paths — the raw destructive pair must NOT survive in any form; if exact behavioral parity is impossible through restore, the rollback must fail loudly (error + instructions) rather than fall back to raw git, and the deviation is ledgered. drift.cts / research-store.cts / learnings.cts / intel.cts SHA sites (1–2 each): migrate to unified `LogEntry.id` reads or annotate substrate per the same rules. Build after each module.
  </action>
  <verify>
    <automated>pnpm run build:lib && [ $(rg -c "execSync\(|execFileSync\(" src -g '*.cts' | rg -v ':0$' | rg -v 'shell-command-projection' | wc -l) -eq 0 ] && [ $(rg -c "git reset --hard" src -g '*.cts' | rg -v ':0$' | wc -l) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Zero execSync/execFileSync sites outside shell-command-projection.cts in src/
    - The string `reset --hard` appears nowhere in src/*.cts
    - graphify/core substrate sites carry inline annotations mirroring the fork allowlist reasons, each with a ledger row
    - Build green; unit suite not worse than the 19-04 baseline for these modules
  </acceptance_criteria>
  <done>All five outliers gone (destructive path adapter-routed or loud-fail), small modules migrated or substrate-annotated.</done>
</task>

<task type="auto">
  <name>Task 3: Substrate dispositions + AUDIT-01 seam sweep</name>
  <files>src/worktree-safety.cts (annotations only), src/worktree-base-ref.cts (annotations only), 19-MERGE-AUDIT.md</files>
  <read_first>
    - src/worktree-safety.cts and src/worktree-base-ref.cts (confirm they are pure git-worktree machinery; note any export consumed by a path that could run under jj — assumption A6 check)
    - 19-RESEARCH.md "Raw-Git Seam Inventory" (disposition rationale per module)
  </read_first>
  <action>
    Formally disposition the substrate set with ledger rows + a header comment in each file: (1) `src/shell-command-projection.cts` — `substrate:adapter-internal exec seam (mirrors fork sdk/src/vcs/exec.ts precedent)`; (2) `src/worktree-safety.cts` (27 sites) — `substrate:git-backend worktree machinery; jj path served by vcs.workspace.parallel.* (analogue of fork src/vcs/git/parallel.cts)`; (3) `src/worktree-base-ref.cts` (4 sites) — `substrate:git-only base-ref degrade; jj workspaces do not share the base-mismatch failure mode (Open-Q6 decision)`. A6 check: `rg -n "worktree-safety|worktree-base-ref" src gsd-core/bin/gsd-tools.cjs --glob '!*.cjs.map'` — confirm no consumer reaches these modules on a jj-only code path without a git-availability guard; record findings. Then run the AUDIT-01 sweep and paste results into the ledger: `rg -n "execGit\(" src -g '*.cts'` (every hit must be in shell-command-projection.cts, worktree-safety.cts, worktree-base-ref.cts, or a ledgered annotated site); `rg -n "execSync|execFileSync" src -g '*.cts'` (only shell-command-projection.cts); commit-id spot check `rg -n "rev-parse --short|cat-file -t" src -g '*.cts'` (zero outside substrate). Also ledger `gsd-core/bin/verify-reapply-patches.cjs` (checked-in CJS, 2 SHA sites) with a disposition (migrate if trivial via built vcs lib require, else `substrate:` row with reason).
  </action>
  <verify>
    <automated>[ $(rg -l "execGit\(" src -g '*.cts' | rg -v 'shell-command-projection|worktree-safety|worktree-base-ref|vcs/' | wc -l) -eq 0 ] && rg -c 'substrate:' .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Every execGit-importing module outside the three substrate files has zero remaining call sites
    - Three substrate header comments + matching ledger rows exist (plus verify-reapply-patches.cjs disposition)
    - A6 check result recorded (no unguarded jj-path consumer of worktree modules, or the guard added)
    - Sweep output pasted into the ledger as AUDIT-01 evidence
  </acceptance_criteria>
  <done>AUDIT-01 seam state: migrated or justified, with machine-checkable sweeps recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream destructive code path → user repo | roadmap-upgrade rollback ran `git reset --hard` + `git clean -fd` on user data |
| migrated envelopes → workflow consumers | commit_id leakage from jj backend breaks the unified-revision invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-DG | Tampering/DoS | roadmap-upgrade.cts:579 destructive rollback | mitigate | adapter restore semantics or loud-fail; `reset --hard` string asserted absent from src/*.cts in verify |
| T-19-18 | Information Disclosure | jj backend volunteering commit_id through migrated envelopes | mitigate | unified LogEntry.id rule enforced per migrated module; tmp-repo envelope assertion in Task 1 |
| T-19-19 | Repudiation | substrate exceptions without justification | mitigate | inline annotation + ledger row per substrate module; sweep output recorded |
</threat_model>

<verification>
- Build green; seam sweeps clean; destructive path gone; substrate ledgered; unit suite not regressed vs 19-04 baseline.
</verification>

<success_criteria>
- AUDIT-01 (src/ surface) complete: 60 execGit sites migrated or substrate-justified, 5 outliers eliminated, jj invariants hold through upstream commands.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-07-SUMMARY.md` when done.
</output>
