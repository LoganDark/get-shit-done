---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 05
type: execute
wave: 5
depends_on: [19-04]
files_modified:
  - src/vcs/** (new, 36 .cts modules in subdirs backends/, jj/, git/, parse/, format-migration/)
  - .gitignore
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [PORT-01]
must_haves:
  truths:
    - "All 36 fork vcs production modules exist at src/vcs/**.cts, byte-for-behavior identical to c7bd6bee apart from the mechanical .ts→.cts rename, .js→.cjs specifier rewrite, and the execGit→execGitVcs export rename"
    - "pnpm run build:lib emits gsd-core/bin/lib/vcs/**.cjs with zero TypeScript errors"
    - "The emitted vcs index loads under require() and createVcsAdapter detects jj on this repo"
  artifacts:
    - path: "src/vcs/index.cts"
      provides: "Ported VcsAdapter entry point"
    - path: "src/vcs/types.cts"
      provides: "VcsAdapter contract incl. unified revision model types"
    - path: ".gitignore"
      provides: "Ignore coverage for emitted gsd-core/bin/lib/vcs/ artifacts"
      contains: "gsd-core/bin/lib/vcs/"
  key_links:
    - from: "src/vcs/backends/jj.cts"
      to: "src/vcs/exec.cts"
      via: "execGitVcs / vcsExec imports with .cjs specifiers"
      pattern: "from '\\./(\\.\\./)?exec\\.cjs'"
    - from: "gsd-core/bin/lib/vcs/index.cjs"
      to: "node require()"
      via: "build emit + smoke require"
---

<objective>
Port the fork's VCS layer (36 production modules, ~7,750 lines: core contract, jj+git backends, parallel verbs, parsers, format-migration) from c7bd6bee `sdk/src/vcs/` into upstream's architecture as `src/vcs/**.cts`, with the build emitting `gsd-core/bin/lib/vcs/**.cjs`.

Purpose: PORT-01 — the fork's only durable divergence (jj support) re-homed in upstream's tree. Mechanical translation, verified by research: no import.meta, no default exports, no top-level await anywhere in the 36 modules.
Output: src/vcs/ tree, gitignore coverage, green build including the new modules, require + adapter smoke.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
19-RESEARCH.md (same directory) — targeted sections ONLY, do NOT @-load the full file: "Porting Map" chunks 1–5 + "Translation rules" (as cited in read_first)
19-MERGE-AUDIT.md (same directory) — append-target ledger; do NOT @-load the full file (read_first entries name the specific rows/sections needed)

HARD CONSTRAINTS: same as 19-01. Port source is `jj file show -r c7bd6bee sdk/src/vcs/<path>` — NOT the on-disk sdk/src/vcs survivors (identical today, but the revision is the contract).
PITFALL 10 (exec-shape): keep the fork's `ExecResult { exitCode, stdout, stderr, timedOut, error }` shape EXACTLY (WR-06 sentinel exitCode -1 on signal-kill). Do NOT unify with upstream's `SpawnResultOutput { ...signal... }`. Upstream's `src/shell-command-projection.cts` stays untouched.
PITFALL 5 (emitted artifacts): never hand-edit anything under `gsd-core/bin/lib/` — those are build outputs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scripted relocation + mechanical translation of the 36 vcs modules</name>
  <files>src/vcs/** (new .cts files)</files>
  <read_first>
    - Output of `jj file list -r c7bd6bee sdk/src/vcs` (authoritative module inventory; EXCLUDE `__tests__/` paths — tests port in 19-11)
    - `jj file show -r c7bd6bee sdk/src/vcs/exec.ts` (the execGit convenience export to rename)
    - 19-RESEARCH.md "Porting Map" chunks 1–5 + "Translation rules" (the exact rule set)
    - tsconfig.build.json (confirm include covers src/**/*.cts; rootDir/outDir contract)
  </read_first>
  <action>
    Scripted loop: for each production module from `jj file list -r c7bd6bee sdk/src/vcs` (excluding `__tests__/`), write `jj file show -r c7bd6bee <path>` content to `src/vcs/<relative-path>` with the `.ts` extension renamed to `.cts`, then rewrite every RELATIVE import specifier ending in `.js` to `.cjs` (e.g. `from './types.js'` → `from './types.cjs'`; `import x = require('./y.js')` likewise; leave bare package specifiers like `node:child_process` untouched). Expected layout: src/vcs/{types,index,backends,exec,expr,refs-validator,hook-bridge}.cts, src/vcs/backends/{jj,git}.cts, src/vcs/jj/{parallel,octopus,reap,workspace-cleanup,incomplete-work,pre-push,lock,conflict-paths}.cts, src/vcs/git/parallel.cts, src/vcs/parse/*.cts (8), src/vcs/format-migration/*.cts (8). Naming-hazard mitigation (research-mandated): in src/vcs/exec.cts rename the exported convenience function `execGit` → `execGitVcs` and update its fork-internal call sites (3 sites, in the backends modules — find with `rg -n 'execGit' src/vcs`); this prevents any future misimport against upstream's differently-shaped `execGit` in shell-command-projection.cts. Make NO other semantic edits — byte-for-behavior port (Pitfall 10: ExecResult shape untouched). Append scripted ledger rows: one `ported-to:src/vcs/<path>.cts` row per source module (36 rows).
  </action>
  <verify>
    <automated>[ $(find src/vcs -name '*.cts' -not -path '*__tests__*' | wc -l) -ge 36 ] && [ $(rg -c "from '.*\.js'" src/vcs -g '*.cts' | rg -v ':0$' | wc -l) -eq 0 ] && [ $(rg -n 'export function execGit\b' src/vcs/exec.cts | wc -l) -eq 0 ] && rg -c 'execGitVcs' src/vcs/exec.cts</automated>
  </verify>
  <acceptance_criteria>
    - File count at src/vcs/**/*.cts (excluding __tests__) >= 36 and matches the c7bd6bee inventory 1:1 (recorded in ledger)
    - Zero relative `.js` specifiers remain under src/vcs
    - `execGit` is no longer exported from src/vcs/exec.cts; `execGitVcs` is; all fork-internal call sites updated (rg 'execGitVcs' hits in backends)
    - `rg -n 'timedOut' src/vcs/exec.cts` still matches (ExecResult shape preserved)
    - 36 `ported-to:` ledger rows appended
  </acceptance_criteria>
  <done>Complete, mechanically-translated vcs tree at src/vcs with the seam-collision hazard removed.</done>
</task>

<task type="auto">
  <name>Task 2: Gitignore coverage + build green + emitted-artifact smoke</name>
  <files>.gitignore, 19-MERGE-AUDIT.md</files>
  <read_first>
    - .gitignore (current per-file enumeration style for /gsd-core/bin/lib/*.cjs)
    - src/vcs/index.cts (the entry point whose emitted form is smoked)
  </read_first>
  <action>
    Add a single directory ignore line `/gsd-core/bin/lib/vcs/` to .gitignore (deliberate deviation from upstream's per-file enumeration style: a directory line auto-covers future vcs modules and makes drift impossible; record this convention deviation as a ledger row with that rationale). Run `pnpm run build:lib`; it must exit 0 and emit `gsd-core/bin/lib/vcs/index.cjs` plus nested subdirectories (backends/, jj/, git/, parse/, format-migration/). Smoke the emitted artifact: `node -e "const v=require('./gsd-core/bin/lib/vcs/index.cjs'); if(typeof v.createVcsAdapter!=='function') throw new Error('createVcsAdapter missing')"` and an adapter-detection smoke on this jj repo: create the adapter for process.cwd() and assert its backend kind is `jj` (use the actual factory/probe API found in index.cts — read it first; if detection requires explicit config, assert via the backends detection helper instead and record the exact call used). Confirm `jj st` shows NO tracked changes under gsd-core/bin/lib/ (artifacts ignored, not snapshotted).
  </action>
  <verify>
    <automated>pnpm run build:lib && test -f gsd-core/bin/lib/vcs/index.cjs && node -e "const v=require('./gsd-core/bin/lib/vcs/index.cjs'); if(typeof v.createVcsAdapter!=='function'&&typeof v.default!=='object'){throw new Error('vcs entry missing exports')}" && [ $(jj st | grep -c 'gsd-core/bin/lib/' || true) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run build:lib` exits 0 with the ported modules included
    - `gsd-core/bin/lib/vcs/index.cjs` and at least backends/jj.cjs, backends/git.cjs, git/parallel.cjs, jj/parallel.cjs exist post-build
    - The require smoke passes and the jj-detection smoke (exact call recorded in the SUMMARY) passes on this repo
    - `jj st` shows no gsd-core/bin/lib/ paths (ignore line effective)
    - Ledger row for the gitignore convention deviation exists
  </acceptance_criteria>
  <done>Ported vcs layer compiles and loads via upstream's build chain; jj backend detectable on this repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork revision content → upstream build | port must not introduce semantic drift in security-relevant paths (commit gating, hook firing, destructive-op guards) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Tampering | semantic drift during translation | mitigate | scripted byte-for-behavior port from pinned revision; only three sanctioned edits (extension, specifiers, execGitVcs rename); tsc strict + 19-11 ported test suite re-verify behavior |
| T-19-13 | Spoofing | execGit name collision between fork exec and upstream seam | mitigate | execGitVcs rename in this plan, before any consumer exists |
| T-19-14 | Repudiation | modules silently skipped during the loop | mitigate | 1:1 inventory check against `jj file list -r c7bd6bee` + 36 ledger rows |
</threat_model>

<verification>
- Build green, emit present, smoke passes, no .js specifiers, ExecResult shape untouched, ledger complete for all 36 modules.
</verification>

<success_criteria>
- src/vcs/**.cts complete and compiling; emitted gsd-core/bin/lib/vcs/**.cjs loads; jj invariants structurally intact pending test-suite proof in 19-11.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-05-SUMMARY.md` when done.
</output>
