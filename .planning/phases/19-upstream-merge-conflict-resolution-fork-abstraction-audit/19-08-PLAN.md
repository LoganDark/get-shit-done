---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 08
type: execute
wave: 8
depends_on: [19-07]
files_modified:
  - gsd-core/workflows/execute-phase.md
  - gsd-core/workflows/quick.md
  - gsd-core/workflows/code-review.md
  - gsd-core/workflows/complete-milestone.md
  - gsd-core/workflows/help.md
  - gsd-core/workflows/new-milestone.md
  - gsd-core/workflows/new-project.md
  - gsd-core/workflows/plan-phase.md
  - gsd-core/workflows/undo.md
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [PORT-02, AUDIT-01]
must_haves:
  truths:
    - "The fork's VCS rewiring (cross-backend verb calls replacing raw-git fences) is re-applied on upstream's workflow text WITHOUT deleting upstream's 1.4.x guards (#48/#683/#630 worktree_branch_check, base-ref degrade, manifest pinning)"
    - "Every verb a rewired workflow calls dispatches successfully through `node gsd-core/bin/gsd-tools.cjs query <verb>` (Pitfall 1: bridge landed first)"
    - "Zero `gsd-sdk` references remain in gsd-core/workflows/"
  artifacts:
    - path: "gsd-core/workflows/execute-phase.md"
      provides: "Upstream text + fork workspace.parallel.{dispatch,fan-in} dispatch, upstream worktree semantics re-expressed through fork verbs"
      contains: "workspace.parallel.dispatch"
    - path: "gsd-core/workflows/quick.md"
      provides: "Upstream text + fork parallel dispatch rewiring (Phase 11 delta re-applied)"
  key_links:
    - from: "gsd-core/workflows/*.md verb fences"
      to: "gsd-core/bin/gsd-tools.cjs query dispatch"
      via: "gsd_run query <verb> calling convention"
      pattern: "gsd_run query "
---

<objective>
Re-apply the fork's workflow VCS rewiring as a minimal, well-marked delta on top of upstream's `gsd-core/workflows/*.md` (porting-map chunk 8): the 9 harvested base→fork deltas, re-pathed (`get-shit-done`→`gsd-core`, `gsd-sdk query`→`gsd_run query`), three-way-aware so upstream's 1.4.x fixes survive.

Purpose: workflow markdown converges to upstream, fork verb dispatch restored (locked decision); raw-git fences with no fork verb (checkout/branch/tag/stash) stay raw and enter the 19-10 baseline — do not invent phantom verbs.
Output: 9 rewired workflows, per-file verb smoke, ledger rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
19-RESEARCH.md (same directory) — targeted sections ONLY, do NOT @-load the full file: "Porting Map" chunk 8 + "Upstream Delta Semantics" + "Common Pitfalls" (Pitfall 11)
19-MERGE-AUDIT.md (same directory) — append-target ledger; do NOT @-load the full file (read_first entries name the specific rows/sections needed)
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/harvest/workflow-deltas/ (the 9 base→fork diffs)

HARD CONSTRAINTS: same as 19-01. Markdown has no compiler — the verb smoke per file is the only mechanical guard. Do NOT edit the `_runtime-launcher.snippet.sh` embed blocks inside workflow files (that is 19-09's sync-script territory).
PITFALL 11: the fork deltas were written against 05-21 base text; upstream's execute-phase/quick carry NEW logic the fork never saw. Where both sides touched the same section, re-express upstream's new semantics THROUGH fork verbs — never resolve by choosing a side.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three-way re-wiring of execute-phase.md and quick.md (the hard pair)</name>
  <files>gsd-core/workflows/execute-phase.md, gsd-core/workflows/quick.md</files>
  <read_first>
    - gsd-core/workflows/execute-phase.md and gsd-core/workflows/quick.md (upstream text in tree — including the #48/#683 worktree_branch_check fragment embed, #630 manifest pinning, base-SHA capture)
    - harvest/workflow-deltas/execute-phase.md.diff and quick.md.diff (the fork's Phase 11 dispatch rewiring: workspace.parallel.{dispatch,fan-in} blocks, WAVE_WORKTREE_PLANS_JSON construction, HANDLE_OK guard, EXPECTED_BRANCH pre-check)
    - gsd-core/references/worktree-branch-check.md (upstream's NEW fragment the dispatch section must keep honoring or supersede explicitly)
  </read_first>
  <action>
    Apply the fork delta hunk-by-hunk onto upstream's text. Dispatch sections: replace upstream's raw `git worktree` dispatch/fan-in with the fork's `gsd_run query workspace.parallel.dispatch` / `workspace.parallel.fan-in` blocks (carrying the Phase 11 semantics: plan-array JSON shape, numeric --phase, HANDLE_OK guard, sequential Agent() pattern, no WAVE_WORKTREE_MANIFEST). Upstream-new semantics that the fork never saw must be RE-EXPRESSED through fork verbs, not deleted: base-mismatch degrade (#683) becomes a documented condition around dispatch (jj note: workspaces don't share the base problem — degrade check applies on the git backend via the dispatch envelope, or is explicitly noted as git-backend-only); manifest pinning (#630) and worktree_branch_check (#48) either ride the fork's assert-dispatched-cwd / dispatch envelope or are kept verbatim where they don't conflict. Re-path everything: `gsd-sdk query` → `gsd_run query`, `get-shit-done/` → `gsd-core/`. Commit fences → `gsd_run query commit`. Raw fences with no fork verb stay raw. After each file: extract every `gsd_run query <verb>` it now contains and probe each verb (`node gsd-core/bin/gsd-tools.cjs query <verb>` with a help/read-only form) — zero "Unknown command". Ledger rows: `re-applied-at:` per file with a note listing which upstream-new guards were re-expressed and how.
  </action>
  <verify>
    <automated>[ $(rg -c 'gsd-sdk' gsd-core/workflows/execute-phase.md gsd-core/workflows/quick.md | rg -v ':0$' | wc -l) -eq 0 ] && rg -c 'workspace\.parallel\.dispatch' gsd-core/workflows/execute-phase.md && rg -c 'workspace\.parallel\.dispatch' gsd-core/workflows/quick.md && for v in $(rg -o 'gsd_run query [a-z.-]+' gsd-core/workflows/execute-phase.md gsd-core/workflows/quick.md | sed 's/.*query //' | sort -u); do node gsd-core/bin/gsd-tools.cjs query "$v" --help >/dev/null 2>&1 || node gsd-core/bin/gsd-tools.cjs query "$v" >/dev/null 2>&1 || echo "DEAD VERB: $v"; done | tee /tmp/19-08-task1-deadverbs.txt && [ ! -s /tmp/19-08-task1-deadverbs.txt ]</automated>
  </verify>
  <acceptance_criteria>
    - Both files call workspace.parallel.{dispatch,fan-in} via gsd_run query; zero gsd-sdk references
    - Upstream's #48/#683/#630 semantics are present (rg finds worktree_branch_check / base-ref / manifest-pin language or its documented fork-verb re-expression) — not silently deleted
    - Verb probe loop reports zero dead verbs across both files
    - Ledger rows with re-expression notes for both files
  </acceptance_criteria>
  <done>The two dispatch-bearing workflows run fork parallel verbs while preserving upstream's 1.4.x guard semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Re-apply the remaining 7 workflow deltas</name>
  <files>gsd-core/workflows/{code-review,complete-milestone,help,new-milestone,new-project,plan-phase,undo}.md, 19-MERGE-AUDIT.md</files>
  <read_first>
    - The 7 corresponding harvest/workflow-deltas/*.md.diff files (what to re-apply)
    - Each target gsd-core/workflows file (upstream text being modified)
  </read_first>
  <action>
    For each of the 7 files: apply the fork delta hunk-by-hunk onto upstream text with re-pathing (`gsd-sdk query`→`gsd_run query`, `get-shit-done/`→`gsd-core/`). Typical delta content per research: commit fences → `gsd_run query commit`; undo.md → query status/log/diff/revert/restore verbs; complete-milestone.md → query merge/reset verbs where the fork rewired them, while its checkout/tag fences STAY RAW (no fork verb exists — they enter the 19-10 audit baseline); help/new-milestone/new-project/plan-phase → whatever the fork delta actually contains (the diff is authoritative, not this list). Where a fork hunk has no anchor in upstream text (upstream rewrote the section), re-express the fork's intent in the new structure and note it in the ledger row. After each file, run the same verb-probe loop as Task 1. Ledger row per file.
  </action>
  <verify>
    <automated>[ $(rg -c 'gsd-sdk' gsd-core/workflows/ | rg -v ':0$' | wc -l) -eq 0 ] && [ $(rg -c 'gsd_run query commit' gsd-core/workflows/complete-milestone.md gsd-core/workflows/undo.md | rg -v ':0$' | wc -l) -ge 1 ] && for v in $(rg -o 'gsd_run query [a-z.-]+' gsd-core/workflows/*.md | sed 's/.*query //' | sort -u); do node gsd-core/bin/gsd-tools.cjs query "$v" --help >/dev/null 2>&1 || node gsd-core/bin/gsd-tools.cjs query "$v" >/dev/null 2>&1 || echo "DEAD: $v"; done | tee /tmp/19-08-deadverbs.txt && [ ! -s /tmp/19-08-deadverbs.txt ]</automated>
  </verify>
  <acceptance_criteria>
    - Zero gsd-sdk references anywhere under gsd-core/workflows/
    - All 7 deltas applied with ledger rows (anchorless hunks noted)
    - Verb probe across ALL gsd-core/workflows/*.md reports zero dead verbs
    - checkout/tag/stash fences remain raw (rg confirms no invented gsd_run query checkout/tag/stash)
  </acceptance_criteria>
  <done>All 9 fork workflow deltas live at gsd-core counterparts; full workflow verb surface dispatchable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow prose → runtime dispatch | markdown has no compiler; a dead verb fails only at runtime in a user session |
| fork delta → upstream guards | blind delta application can delete upstream's 1.4.x safety logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-20 | DoS | rewired workflow calling unbridged verb (Pitfall 1) | mitigate | bridge shipped in 19-06; per-file verb probe loop in verify |
| T-19-21 | Tampering | fork delta clobbering #48/#683/#630 guards (Pitfall 11) | mitigate | three-way hunk application; guard-presence assertion in Task 1 acceptance |
| T-19-22 | Repudiation | anchorless hunks silently dropped | mitigate | per-file ledger rows note every re-expressed/anchorless hunk |
</threat_model>

<verification>
- Zero gsd-sdk in workflows; zero dead verbs; upstream guards present; 9 ledger rows.
</verification>

<success_criteria>
- Workflow markdown converged to upstream with the fork's VCS rewiring as a verified, minimal, dispatchable delta.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-08-SUMMARY.md` when done.
</output>
