---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
  - package.json
  - package-lock.json
  - pnpm-workspace.yaml
  - pnpm-lock.yaml
  - sdk/package.json
  - next-branch-files.tar.gz
  - rollout-next-phase1.sh
  - rollout-next-phase2.sh
  - .github/workflows/ (org-automation files, enumerated at execution)
autonomous: true
requirements: [MERGE-01, MERGE-02]
must_haves:
  truths:
    - "19-MERGE-AUDIT.md exists with the ledger schema and already carries rows for every disposition made in this plan"
    - "Root package.json is conflict-free, upstream-shaped, named @opengsd/gsd-core@1.4.3, pnpm-pinned, with fallow dropped and vitest added"
    - "No lockfile, workspace manifest, rollout artifact, or org-automation CI file from either side remains undispositioned"
  artifacts:
    - path: ".planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md"
      provides: "Disposition ledger (path | origin side | disposition | justification) + conflict-marker fixture-exclusion list"
    - path: "package.json"
      provides: "Resolved root manifest: upstream shape + operator decisions overlaid"
      contains: "\"name\": \"@opengsd/gsd-core\""
  key_links:
    - from: "package.json"
      to: "19-02 install task"
      via: "packageManager pnpm pin; manifest resolved BEFORE any install (Pitfall 6)"
      pattern: "\"packageManager\": \"pnpm@"
---

<objective>
Open the Phase 19 disposition ledger and resolve the packaging/identity surface of the merge: root package.json to upstream shape with operator overlays (mirror upstream identity, pnpm, drop fallow, add vitest), kill all stale lockfiles/workspace manifests, drop upstream rollout artifacts and org-automation CI, and record every disposition in the ledger.

Purpose: Packaging MUST resolve before any dependency install (Pitfall 6); the ledger MUST exist before any other plan makes a tree-level disposition (MERGE-02 accumulates from Wave 0, never retrofitted).
Output: 19-MERGE-AUDIT.md (live ledger), resolved package.json, deleted lockfiles/artifacts, ledgered CI drops.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-CONTEXT.md
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-RESEARCH.md
@.planning/MERGE-REVIEW-upstream-2026-05-25.md

HARD CONSTRAINTS (apply to this plan and every later plan in Phase 19):
- All edits in the working copy on top of merge change `vpzlrrlv`, workspace `get-shit-done-2`. Per-task commits on top are fine. NEVER rewrite/squash/abandon/rebase `vpzlrrlv` or anything below it. No bookmark moves. No `jj op restore`/`jj undo`.
- jj only, never raw `git`, for VCS operations. `jj diff --git` for all diff inspection. Fork content recovery is read-only: `jj file show -r c7bd6bee <path>` / `jj file list -r <rev>`.
- Do NOT run any package install in this plan. Install happens in 19-02 AFTER the human legitimacy checkpoint.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Open 19-MERGE-AUDIT.md ledger + build the conflict-marker fixture-exclusion list</name>
  <files>.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md</files>
  <read_first>
    - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-CONTEXT.md (audit policy section)
    - .planning/MERGE-REVIEW-upstream-2026-05-25.md (the precedent artifact this ledger replaces for the next pull)
  </read_first>
  <action>
    Create 19-MERGE-AUDIT.md in the phase directory with: (1) a header naming the merge topology (merge change vpzlrrlv = upstream 03764dbc into fork c7bd6bee, base b533f718) and the locked strategy (upstream layout canonical, vcs layer ported); (2) the ledger table with columns: path | origin side | disposition | justification. Disposition vocabulary (use exactly these prefixes so the 19-13 completeness proof can parse them): `ported-to:<path>`, `re-applied-at:<path>`, `dropped:<reason>`, `adopted-upstream`, `substrate:<reason>`, `merged`. (3) A "Fixture exclusions (conflict-marker sweep)" section: run `rg -l '^(<<<<<<<|%%%%%%%|>>>>>>>)' --no-ignore-vcs` over the tree, then classify each hit as either a genuine unresolved conflict (expected at this point — do NOT list those) or a file that legitimately contains marker-like text (test fixtures, docs about merge conflicts); record only the legitimate-marker files as the exclusion list with one-line reasons. This list is the `--glob '!<path>'` exclusion set used by every later marker sweep. (4) An empty "Skip-count / baseline re-derivations" appendix section (filled by 19-10/19-12). (5) An empty "Hand-edited .cjs inventory" appendix section (columns: file path | plan | node -c confirmed) — 19-04/19-06/19-10/19-11/19-12 append one row per hand-edited checked-in .cjs file; 19-13's phase-gate node -c loop iterates exactly this list.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md && rg -c '\| *path *\| *origin side *\| *disposition *\| *justification *\|' .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 19-MERGE-AUDIT.md exists and contains the four-column ledger table header exactly: path | origin side | disposition | justification
    - The disposition vocabulary section lists all six prefixes verbatim
    - The fixture-exclusion section exists; every listed file has a one-line reason; no currently-conflicted path appears in it
    - The "Hand-edited .cjs inventory" appendix section exists (empty, with the three-column header)
  </acceptance_criteria>
  <done>Ledger artifact open with parseable schema and a frozen fixture-exclusion list usable by all later marker sweeps.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve packaging/identity surface + drop rollout artifacts and org-automation CI (all ledgered)</name>
  <files>package.json, package-lock.json (delete), pnpm-workspace.yaml (delete), pnpm-lock.yaml (delete), sdk/package.json (delete), next-branch-files.tar.gz (delete), rollout-next-phase1.sh (delete), rollout-next-phase2.sh (delete), .github/workflows/* (org-automation files, delete), 19-MERGE-AUDIT.md (append rows)</files>
  <read_first>
    - package.json (current conflicted state in working copy)
    - Upstream manifest: `jj file show -r 03764dbc package.json` (the wholesale starting point)
    - Fork sdk manifest: `jj file show -r c7bd6bee sdk/package.json` (harvest the vitest devDep version pin before deleting)
    - 19-RESEARCH.md sections "Entry points / packaging" and "Package Legitimacy Audit"
  </read_first>
  <action>
    Resolve `package.json` by writing upstream's content (from `jj file show -r 03764dbc package.json`) and overlaying exactly these operator decisions: (a) keep `name: @opengsd/gsd-core`, `version: 1.4.3`, and upstream's `bin` map (`gsd-core: bin/install.js`, `gsd-tools: gsd-core/bin/gsd-tools.cjs`) — mirror upstream identity, and ensure NO `gsd-sdk` bin entry survives; (b) add `"packageManager": "pnpm@<X>"` pinned to the locally installed pnpm version (`pnpm --version`); (c) remove the `fallow` optionalDependency entirely (src/fallow-runner.cts degrades to null when absent — verified by research); (d) add `vitest` to devDependencies at the version harvested from fork `sdk/package.json` at c7bd6bee (vitest revival decision); (e) leave upstream's scripts/engines untouched otherwise (upstream parity = cheap future pulls). Then delete: `package-lock.json` (conflict resolves as fork-deletion; lockfiles are never hand-merged — fresh pnpm-lock.yaml is generated in 19-02), stale `pnpm-lock.yaml` (workspace-era, invalid for the new manifest), `pnpm-workspace.yaml` (no workspaces remain post-SDK-retirement), `sdk/package.json` (Wave-0 requirement: gone BEFORE first install so no phantom package), `next-branch-files.tar.gz` + `rollout-next-phase1.sh` + `rollout-next-phase2.sh` (upstream next-branch repo-ops staging, verified not load-bearing). Enumerate `.github/workflows/`: keep `test.yml` (conflicted — resolved in 19-04) and the fork's `parallel-e2e.yml` (re-pointed in 19-12); for every OTHER upstream-added workflow file, inspect for org-automation signals (references to `secrets.` tokens for OpenGSD org, bot accounts, Discord/changelog automation, duplicate-issue sweeps, PR-policy enforcement, `next` branch release train) and delete it (locked decision: drop org-automation, 9 files expected). Append one ledger row per file touched in this task (origin side + disposition + justification), including a `dropped:org-automation-references-org-secrets` row per CI file and a permanent-divergence row for pnpm. Also append a single `adopted-upstream` ledger row for the `.changeset/` tree (465 archived upstream changeset fragments, research Open Q7) — retained as-is with upstream history, no per-file rows needed.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.name!=='@opengsd/gsd-core'||p.version!=='1.4.3') throw new Error('identity');if(!/^pnpm@/.test(p.packageManager||''))throw new Error('packageManager');if(p.optionalDependencies&&p.optionalDependencies.fallow)throw new Error('fallow survives');if(!p.devDependencies||!p.devDependencies.vitest)throw new Error('vitest missing');if(p.bin&&p.bin['gsd-sdk'])throw new Error('gsd-sdk bin survives')" && test ! -f package-lock.json && test ! -f pnpm-workspace.yaml && test ! -f sdk/package.json && test ! -f next-branch-files.tar.gz && test ! -f rollout-next-phase1.sh && test ! -f rollout-next-phase2.sh</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('./package.json')"` parses; name/version/bin/packageManager/devDeps assertions above all pass
    - `jj resolve --list` no longer lists package.json, package-lock.json, or sdk/package.json
    - `ls .github/workflows/` contains test.yml and parallel-e2e.yml; every deleted workflow file has a ledger row whose justification names the org-automation signal found in it
    - 19-MERGE-AUDIT.md gained one row per file deleted or resolved in this task (count of new rows >= count of deleted files + 1 for package.json)
    - A `.changeset/` row with disposition `adopted-upstream` exists in the ledger
    - No package install was executed (node_modules/ still absent)
  </acceptance_criteria>
  <done>Packaging surface fully resolved to upstream shape + operator overlays; every disposition ledgered; tree ready for the vetted single install in 19-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream manifest → fork install surface | upstream dependency declarations cross into the fork's executable environment at install time |
| upstream CI files → fork repo automation | upstream workflow files reference OpenGSD org secrets/bots |
| merge resolution → ledger | every silent tree-level disposition is a potential lost fork capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `fallow` optionalDependency (unknown provenance) | mitigate | removed from manifest in this plan, before any install; ledger row records the drop |
| T-19-02 | Elevation | org-automation `.github/workflows/*` referencing org secrets | mitigate | deleted in this plan; each file's secret/bot reference named in its ledger row |
| T-19-03 | Repudiation | silent tree-level dispositions | mitigate | ledger opens here with parseable disposition vocabulary; 19-13 proves completeness |
| T-19-SC | Tampering | pnpm installs | mitigate | NO install in this plan; install gated behind the 19-02 blocking human legitimacy checkpoint |
</threat_model>

<verification>
- `jj resolve --list` count decreased by exactly the packaging paths resolved here; no path at or below `vpzlrrlv` was rewritten (`jj log -r 'vpzlrrlv'` unchanged).
- 19-MERGE-AUDIT.md parses: every row's disposition starts with one of the six vocabulary prefixes.
</verification>

<success_criteria>
- Ledger live with schema + fixture-exclusion list; packaging/identity/lockfile/rollout/org-CI surface fully dispositioned and ledgered; zero installs performed.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-01-SUMMARY.md` when done.
</output>
