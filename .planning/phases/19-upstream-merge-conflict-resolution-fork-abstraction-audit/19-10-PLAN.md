---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 10
type: execute
wave: 10
depends_on: [19-09]
files_modified:
  - scripts/lint-vcs-no-raw-git.cjs
  - scripts/lint-vcs-no-commit-id.cjs
  - scripts/audit-workflow-raw-git.cjs (+ companion baseline JSON)
  - scripts/lint-vcs-parallel-call-presence.cjs
  - scripts/lint-vcs-no-raw-git.allow.json
  - tests/scripts/lint-vcs-no-raw-git-fixture.test.cjs (ported + extended)
  - scripts/audit-id-namespace.cjs
  - scripts/audit-root-commits-rename.cjs
  - scripts/migr-06-close-gate.cjs
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [MERGE-05]
must_haves:
  truths:
    - "Both lint scanners scan .cts files — a planted .cts violation is CAUGHT (the gates are not vacuously green)"
    - "All four fork gate scripts run green against the re-pointed tree"
    - "Every allowlist +/- line and every baseline row carries a ledger justification — nothing re-derived silently"
  artifacts:
    - path: "scripts/lint-vcs-no-raw-git.cjs"
      provides: "SCAN_EXT including cts; whole-repo raw-git gate over the adopted tree"
      contains: "cts"
    - path: "scripts/lint-vcs-no-raw-git.allow.json"
      provides: "Re-pointed allowlist (src/vcs/* + substrate entries ADDED; legacy sdk/** and get-shit-done/** entries RETAINED pending 19-12 residue deletion)"
    - path: "tests/scripts/lint-vcs-no-raw-git-fixture.test.cjs"
      provides: "Fixture test proving .cts violations are detected"
  key_links:
    - from: "scripts/lint-vcs-no-raw-git.allow.json entries"
      to: "src/vcs/exec.cts, src/vcs/backends/git.cts, src/vcs/git/parallel.cts, src/shell-command-projection.cts, src/worktree-safety.cts, src/worktree-base-ref.cts"
      via: "path-exact allowlist matching"
---

<objective>
Re-point all fork invariant gates at the adopted tree and close the verified `.cts` blindness booby trap: extend both SCAN_EXT regexes, re-point allowlists and scan roots, re-derive the audit-workflow-raw-git baseline from the finished workflow text, and prove detection with a fixture test.

Purpose: MERGE-05 — the fork's invariant enforcement survives the restructure with line-by-line ledgered deltas; without the SCAN_EXT fix every gate passes vacuously green over the new .cts production tree (Pitfall 8).
Output: four green re-pointed gates, justified allowlist/baseline deltas, .cts detection proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
19-RESEARCH.md (same directory) — targeted sections ONLY, do NOT @-load the full file: "Lint-Gate Re-pointing" + "Common Pitfalls" (Pitfall 8)
19-MERGE-AUDIT.md (same directory) — append-target ledger; do NOT @-load the full file (read_first entries name the specific rows/sections needed)

HARD CONSTRAINTS: same as 19-01. Pre-re-pointing allowlist baseline is 26 entries (pass-2 verified; the "24" in older docs is stale). Baselines are re-derived by running the scripts' OWN counters against the finished tree — never hand-counted (Don't-Hand-Roll rule).
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCAN_EXT +cts in both scanners + allowlist re-point + .cts detection fixture proof</name>
  <files>scripts/lint-vcs-no-raw-git.cjs, scripts/lint-vcs-no-commit-id.cjs, scripts/lint-vcs-no-raw-git.allow.json, tests/scripts/lint-vcs-no-raw-git-fixture.test.cjs, 19-MERGE-AUDIT.md</files>
  <read_first>
    - scripts/lint-vcs-no-raw-git.cjs (SCAN_EXT at ~line 63; allowlist loading; inline `vcs-lint:allow-git-here` escapes)
    - scripts/lint-vcs-no-commit-id.cjs (SCAN_EXT at ~line 54; walk behavior — confirm whether it walks gitignored gsd-core/bin/lib emitted files and add a walk-ignore if it does)
    - scripts/lint-vcs-no-raw-git.allow.json (current 26 entries to re-point)
    - `jj file show -r c7bd6bee tests/scripts/lint-vcs-no-raw-git-fixture.test.cjs` (port source to extend)
    - 19-MERGE-AUDIT.md rows from 19-07 (the substrate dispositions the new allowlist entries cite)
  </read_first>
  <action>
    Extend SCAN_EXT in both scanners to include `cts` (lint-vcs-no-raw-git.cjs ~line 63: `/\.(cjs|cts|js|mjs|ts|yml|yaml|sh|bash)$/`; lint-vcs-no-commit-id.cjs ~line 54: `/\.(cjs|cts|js|mjs|ts)$/`). Ensure neither scanner reports the gitignored emitted artifacts under gsd-core/bin/lib/ (add a walk-ignore for that directory if the walker reaches it; the checked-in exceptions gsd-tools.cjs, lib/legacy-cleanup.cjs, lib/package-identity.cjs stay scanned). Re-point scripts/lint-vcs-no-raw-git.allow.json: `sdk/src/vcs/exec.ts`→`src/vcs/exec.cts`, `sdk/src/vcs/backends/git.ts`→`src/vcs/backends/git.cts`, `sdk/src/vcs/git/parallel.ts`→`src/vcs/git/parallel.cts`, `sdk/src/vcs/__tests__/**` globs→`src/vcs/__tests__/**`; ADD substrate entries: `src/shell-command-projection.cts`, `src/worktree-safety.cts`, `src/worktree-base-ref.cts` (justifications cite the 19-07 ledger dispositions); add hook-script entries only for files ledgered as substrate in 19-09 (gsd-graphify-update.sh precedent); KEEP every legacy sdk/** and get-shit-done/** entry in BOTH allowlists for now ("re-point" above therefore means ADD the new src/ entries alongside the legacy lines): the residual sdk/ tree (~234 raw-git/commit_id-laden files) survives until 19-12 deletes it, and stripping the legacy entries here would make both lint gates exit 1 (unpassable verify). Append one ledger justification row `pending-19-12-residue-deletion` covering the retained legacy entries; 19-12 Task 2 strips them after the residue deletion and re-runs both gates. The launcher snippet should need NO new entry if the scanner tolerates the 19-09 three-stage resolution; if it still flags, allowlist the snippet with that justification rather than weakening the scan pattern. Re-point lint-vcs-no-commit-id allowlist entries the same way: `sdk/src/vcs/*` → `src/vcs/*.cts`; `get-shit-done/bin/lib/{graphify,verify}.cjs` → `src/{graphify,verify}.cts`; `sdk/src/query/{commit,log}.ts` + `sdk/src/types.ts` JSDoc entries → `src/vcs-command-router.cts` / `src/vcs/types.cts` equivalents; any NEW .cts violation surfaced by the widened scan is preferentially already-migrated (19-07) — remaining hits get reasoned allowlist entries. Port the fixture test from c7bd6bee and EXTEND it: a fixture `.cts` file containing a raw `spawnSync('git', ...)` must be REPORTED by the scanner (positive-detection proof of the SCAN_EXT fix). Ledger: one row per allowlist line added/removed/re-pointed, both allowlists (line-by-line, per the locked decision).
  </action>
  <verify>
    <automated>node scripts/lint-vcs-no-raw-git.cjs && node scripts/lint-vcs-no-commit-id.cjs && node scripts/run-tests.cjs --files tests/scripts/lint-vcs-no-raw-git-fixture.test.cjs && rg -c 'cts' scripts/lint-vcs-no-raw-git.cjs && rg -c 'cts' scripts/lint-vcs-no-commit-id.cjs && rg -q 'pending-19-12-residue-deletion' .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Both gates exit 0 on the current tree AND the extended fixture test proves a planted .cts violation is reported
    - Allowlist contains the three substrate src entries; legacy sdk/** and get-shit-done/** entries are RETAINED (residual trees survive until 19-12) under a `pending-19-12-residue-deletion` ledger justification row
    - Every allowlist delta (both files) has a ledger row with justification
    - Emitted gsd-core/bin/lib artifacts are not scanned/reported
  </acceptance_criteria>
  <done>The .cts booby trap is closed with mechanical proof; raw-git and commit-id gates green over the adopted tree.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point scan roots + re-derive the audit-workflow-raw-git baseline (ledgered row-by-row)</name>
  <files>scripts/audit-workflow-raw-git.cjs (+ companion baseline JSON), scripts/lint-vcs-parallel-call-presence.cjs, 19-MERGE-AUDIT.md</files>
  <read_first>
    - scripts/audit-workflow-raw-git.cjs (SCAN_ROOTS ~line 50; the embedded Object.freeze baseline map keyed by fork paths; the fence-aware counter)
    - scripts/lint-vcs-parallel-call-presence.cjs (SCAN_ROOTS ~line 68; --scan-root parametric support; empty allowlist)
  </read_first>
  <action>
    audit-workflow-raw-git.cjs: change SCAN_ROOTS from `['get-shit-done/workflows','get-shit-done/references','agents']` to `['gsd-core/workflows','gsd-core/references','agents']`. Re-derive the baseline: run the script's own fence-aware counter against the finished tree (post 19-08/19-09 rewiring), freeze the resulting per-file hit map as the new baseline (replacing the obsolete 127-hit fork-path map), and write one ledger row PER baseline file explaining its count (expected shape per research: fork-rewired files near zero; never-rewired files — pr-branch, ship, forensics, complete-milestone checkout/tag fences — carry counts; upstream-new files like worktree-branch-check.md enter with justification). The regression rule (current > baseline = fail) stays byte-identical. lint-vcs-parallel-call-presence.cjs: SCAN_ROOTS → `['gsd-core/workflows']`; allowlist stays empty; it must pass given 19-08 wired workspace.parallel.dispatch into execute-phase/quick. Run both gates green.
  </action>
  <verify>
    <automated>node scripts/audit-workflow-raw-git.cjs && node scripts/lint-vcs-parallel-call-presence.cjs && [ $(rg -c 'get-shit-done/workflows' scripts/audit-workflow-raw-git.cjs scripts/lint-vcs-parallel-call-presence.cjs | rg -v ':0$' | wc -l) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Both gates exit 0; zero get-shit-done/ scan-root references remain in either script
    - The new baseline map's keys are all gsd-core/ or agents/ paths; every key has a ledger row with a count rationale
    - Baseline derivation transcript (script output) pasted into the ledger appendix — provably machine-derived
  </acceptance_criteria>
  <done>Workflow raw-git regression guard and parallel-call-presence guard live on the new tree with a justified frozen baseline.</done>
</task>

<task type="auto">
  <name>Task 3: One-shot audit scripts — re-point or mark historical</name>
  <files>scripts/audit-id-namespace.cjs, scripts/audit-root-commits-rename.cjs, scripts/migr-06-close-gate.cjs, 19-MERGE-AUDIT.md</files>
  <read_first>
    - All three scripts (which paths each targets; whether those paths exist in the adopted tree)
  </read_first>
  <action>
    For each of the three Phase 8/15/16 one-shot/audit artifacts: check every path it targets against the adopted tree. If the script's invariant still applies (target paths exist or have direct counterparts), re-point the paths and run it green. If the invariant is historical (its targets retired with the SDK / fork layout), do NOT delete — add a header comment marking it historical with the Phase 19 merge as the cutoff, and a ledger row (`merged` if re-pointed, `dropped:historical-gate (script retained, marked inactive)` if not). Record the per-script decision in the ledger. Append every checked-in .cjs file hand-edited in this plan (both scanners, audit-workflow-raw-git.cjs, lint-vcs-parallel-call-presence.cjs, the extended fixture test, any re-pointed one-shot script) to the ledger's "Hand-edited .cjs inventory" appendix (file path | plan | node -c confirmed).
  </action>
  <verify>
    <automated>node -c scripts/audit-id-namespace.cjs && node -c scripts/audit-root-commits-rename.cjs && node -c scripts/migr-06-close-gate.cjs && rg -c 'audit-id-namespace' .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - All three scripts parse (node -c); re-pointed ones exit 0 when run
    - Each has a ledger row recording re-point vs historical decision with the path evidence
  </acceptance_criteria>
  <done>No gate script silently targets dead paths; every survivor's status is explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint gates → invariant enforcement | a vacuously-green gate is worse than no gate: violations accumulate invisibly (.cts blindness, Pitfall 8) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-26 | Tampering | SCAN_EXT excluding .cts → gates pass vacuously | mitigate | regex extension + positive-detection fixture test in the same task |
| T-19-27 | Repudiation | silent allowlist/baseline re-derivation hiding new raw-git | mitigate | line-by-line ledger rows; baseline derived by the script's own counter with transcript pasted |
| T-19-28 | DoS | scanners walking emitted artifacts producing phantom violations | mitigate | walk-ignore for gsd-core/bin/lib generated files; checked-in exceptions still scanned |
</threat_model>

<verification>
- All four gates exit 0; fixture test green including the .cts positive case; ledger carries every delta.
</verification>

<success_criteria>
- MERGE-05: fork invariant gates re-pointed, non-vacuous, green, with fully justified deltas.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-10-SUMMARY.md` when done.
</output>
