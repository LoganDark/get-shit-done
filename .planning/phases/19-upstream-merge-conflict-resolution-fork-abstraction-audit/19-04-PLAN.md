---
phase: 19-upstream-merge-conflict-resolution-fork-abstraction-audit
plan: 04
type: execute
wave: 4
depends_on: [19-03]
files_modified:
  - README.md
  - CONTRIBUTING.md
  - SECURITY.md
  - CHANGELOG.md
  - docs/FEATURES.md
  - docs/USER-GUIDE.md
  - docs/INVENTORY.md
  - docs/INVENTORY-MANIFEST.json
  - docs/ja-JP/ARCHITECTURE.md
  - docs/ko-KR/ARCHITECTURE.md
  - .github/workflows/test.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - scripts/changeset/github-release-notes.cjs
  - tests/helpers.cjs
  - tests/bug-3097-3099-executor-worktree-path-safety.test.cjs
  - tests/bug-2767-gsd-sdk-commit-files-flag.test.cjs (delete)
  - agents/gsd-research-synthesizer.md
  - .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md
autonomous: true
requirements: [MERGE-01]
must_haves:
  truths:
    - "jj resolve --list reports ZERO remaining conflicted paths (MERGE-01 tree-level done)"
    - "Marker sweep over the whole tree is clean modulo the 19-01 fixture-exclusion list"
    - "Every hand-edited .cjs from this plan passes node -c (BLOCKER-1 guard)"
    - "No upstream CI secret/org token survives in retained workflow files"
  artifacts:
    - path: "tests/helpers.cjs"
      provides: "Merged helper: upstream base + fork _loadVcs/gitOnly block re-pointed at gsd-core/bin/lib/vcs/index.cjs"
      contains: "gsd-core/bin/lib/vcs/index.cjs"
    - path: ".github/workflows/test.yml"
      provides: "Upstream CI shape, de-org'd, pnpm install steps"
  key_links:
    - from: "tests/helpers.cjs _loadVcs"
      to: "gsd-core/bin/lib/vcs/index.cjs"
      via: "require path (target exists after 19-05 build)"
      pattern: "gsd-core/bin/lib/vcs/index\\.cjs"
---

<objective>
Resolve the remaining ~20 genuine-merge conflicts: bucket D (11 docs/translations, upstream-preferred + fork-essential grafts), bucket E remainder (test.yml, ISSUE_TEMPLATE config, release-notes script), bucket F (tests/helpers.cjs and two test files), bucket G (gsd-research-synthesizer.md). End state: zero conflicts in the tree, marker sweep clean, pre-port baseline test run recorded.

Purpose: completes MERGE-01 at tree level; records the pre-port test baseline so port regressions are attributable.
Output: 0-conflict tree, merged docs/CI/tests/agent files, baseline failure-set record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-RESEARCH.md
@.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md

HARD CONSTRAINTS: same as 19-01. Side-content reads: upstream side = `jj file show -r 03764dbc <path>`, fork side = `jj file show -r c7bd6bee <path>`, base = `jj file show -r b533f71857ab <path>`. Never salvage from conflicted working-copy text.
IDENTITY NOTE (operator decision, supersedes the stale bucket-D text in RESEARCH.md): fork MIRRORS upstream identity (@opengsd/gsd-core, 1.4.3, upstream bin/command names). Doc naming converges to upstream; the old @opengsd/get-shit-done-redux naming does NOT survive in docs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bucket D — docs/translations, upstream-preferred with fork-essential grafts</name>
  <files>README.md, CONTRIBUTING.md, SECURITY.md, CHANGELOG.md, docs/FEATURES.md, docs/USER-GUIDE.md, docs/INVENTORY.md, docs/INVENTORY-MANIFEST.json, docs/ja-JP/ARCHITECTURE.md, docs/ko-KR/ARCHITECTURE.md, 19-MERGE-AUDIT.md</files>
  <read_first>
    - Output of `jj resolve --list` (confirm the exact bucket-D path set)
    - For each file: `jj file show -r 03764dbc <path>` (upstream side — the new base text) and `diff <(jj file show -r b533f71857ab <path>) <(jj file show -r c7bd6bee <path>)` (the fork delta to triage)
  </read_first>
  <action>
    For each bucket-D file: write the upstream side as the resolution, then triage the fork delta hunk-by-hunk into three classes: (a) jj-support / VCS-abstraction documentation (jj backend, VcsAdapter, .githooks bridge, workspace.parallel, migrate-vcs) → re-apply, re-pathed to upstream layout (`gsd-core/`, `gsd_run query`); (b) redux-identity naming deltas → DROP (identity converges to upstream per operator decision); (c) drift-count fixes in INVENTORY/ARCHITECTURE translations → DROP (doc-parity tests are dropped per locked decision; upstream counts stand as-is). CHANGELOG.md: take upstream's file (1.2.0→1.4.3 history), then append a clearly-marked `## Fork history (jj support line, pre-merge)` section containing the fork-side entries from c7bd6bee. docs/INVENTORY-MANIFEST.json must parse after resolution. One ledger row per file: `merged` with a one-line note of which fork hunk classes were re-applied vs dropped.
  </action>
  <verify>
    <automated>[ $(jj resolve --list | grep -c -E '^(README|CONTRIBUTING|SECURITY|CHANGELOG|docs/)' || true) -eq 0 ] && node -e "JSON.parse(require('fs').readFileSync('docs/INVENTORY-MANIFEST.json','utf8'))" && rg -c 'Fork history' CHANGELOG.md && [ $(rg -c 'get-shit-done-redux' README.md docs/USER-GUIDE.md docs/FEATURES.md 2>/dev/null | rg -v ':0$' | wc -l) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - Zero bucket-D paths remain in `jj resolve --list`
    - CHANGELOG.md contains both upstream 1.4.3 history and the marked fork-history section
    - No `get-shit-done-redux` naming survives in README/FEATURES/USER-GUIDE
    - jj-support documentation content (jj backend, .githooks, workspace.parallel) is present in the resolved docs (rg finds 'jj' sections in README or FEATURES)
    - One `merged` ledger row per file with hunk-class notes
  </acceptance_criteria>
  <done>All docs/translation conflicts resolved upstream-preferred with fork jj-content preserved and identity converged.</done>
</task>

<task type="auto">
  <name>Task 2: Bucket E remainder — CI + release-notes script, de-org'd for fork reality</name>
  <files>.github/workflows/test.yml, .github/ISSUE_TEMPLATE/config.yml, scripts/changeset/github-release-notes.cjs, 19-MERGE-AUDIT.md</files>
  <read_first>
    - `jj file show -r 03764dbc .github/workflows/test.yml` (upstream shape to adopt)
    - `jj file show -r c7bd6bee .github/ISSUE_TEMPLATE/config.yml` (fork URLs to retain)
    - `jj diff --git -r @ scripts/changeset/github-release-notes.cjs` is not usable (conflicted) — read both sides via `jj file show`
  </read_first>
  <action>
    test.yml: adopt upstream's scope-detection job shape, then strip everything org-specific: any `secrets.*` references beyond the default GITHUB_TOKEN, bot account names, `next`-branch triggers/refs; convert install steps from `npm ci`/npm cache to pnpm (use pnpm/action-setup or corepack + `pnpm install --frozen-lockfile`; cache key on pnpm-lock.yaml) per the locked pnpm decision. Leave the fork's parallel-e2e.yml untouched (re-pointed in 19-12). ISSUE_TEMPLATE/config.yml: upstream side, with contact/docs links replaced by the fork-side URLs from c7bd6bee. github-release-notes.cjs: take the upstream side, re-apply any small fork naming delta found in the two-sided diff, then `node -c` it (hand-edited checked-in CJS). Ledger rows for all three (`merged`, with the org-strip note for test.yml).
  </action>
  <verify>
    <automated>[ $(jj resolve --list | grep -c -E '^(\.github/|scripts/changeset/)' || true) -eq 0 ] && node -c scripts/changeset/github-release-notes.cjs && [ $(rg -c 'pnpm' .github/workflows/test.yml) -ge 1 ] && [ $(rg -c -i 'discord|opengsd_bot|ORG_' .github/workflows/test.yml || true) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - test.yml uses pnpm install steps, has no non-default secrets references, no `next`-branch triggers
    - github-release-notes.cjs passes `node -c`
    - ISSUE_TEMPLATE/config.yml links point at fork URLs, not OpenGSD org URLs
    - Ledger rows present for all three files
  </acceptance_criteria>
  <done>Retained CI tests the code under fork reality; no org coupling survives.</done>
</task>

<task type="auto">
  <name>Task 3: Buckets F + G — test helper merges, agent reconciliation, zero-conflict gate + pre-port baseline</name>
  <files>tests/helpers.cjs, tests/bug-3097-3099-executor-worktree-path-safety.test.cjs, tests/bug-2767-gsd-sdk-commit-files-flag.test.cjs (delete), agents/gsd-research-synthesizer.md, 19-MERGE-AUDIT.md</files>
  <read_first>
    - Both sides of tests/helpers.cjs (`jj file show -r 03764dbc` / `-r c7bd6bee`) — the fork side contains the `_loadVcs` + `vcs.gitOnly` temp-project block to carry over
    - Both sides of tests/bug-3097-3099-executor-worktree-path-safety.test.cjs — fork side carries `workspace.assert-dispatched-cwd` assertions (Phase 11 rewiring)
    - Both sides of agents/gsd-research-synthesizer.md — same bug fixed two ways (fork: edit pre-seeded SUMMARY.md; upstream #222: write-only summary contract)
  </read_first>
  <action>
    tests/helpers.cjs: genuine merge — upstream base text plus the fork's `_loadVcs`/`vcs.gitOnly`/backend-selection block, with the require re-pointed `'../sdk/dist-cjs/vcs/index.js'` → `'../gsd-core/bin/lib/vcs/index.cjs'` and the build-hint error message changed from `pnpm -F sdk build:cjs` to `pnpm run build:lib` (target exists after 19-05; helpers must not crash at load for tests that never call _loadVcs — keep the require lazy inside the function, as the fork version does). bug-3097-3099: upstream text + fork's assert-dispatched-cwd assertions grafted. bug-2767: delete; ledger row `ported-to:19-11 bridge commit --files test` (the `gsd_run query commit --files` semantics get a new bridge test in 19-11). gsd-research-synthesizer.md: take upstream side; diff against fork side and verify the fork's behavioral guarantee (synthesizer edits the pre-seeded SUMMARY.md rather than Write-ing a new one) is covered by upstream's #222 contract; if not covered, graft the missing clause; ledger the reconciliation either way. GATE: run `jj resolve --list` (must be EMPTY); run the full marker sweep `rg -l '^(<<<<<<<|%%%%%%%|>>>>>>>)'` with the 19-01 exclusion globs (must be empty); `node -c` both touched test .cjs files; append every checked-in .cjs hand-edited in this plan (scripts/changeset/github-release-notes.cjs, tests/helpers.cjs, tests/bug-3097-3099-executor-worktree-path-safety.test.cjs) to the ledger's "Hand-edited .cjs inventory" appendix (file path | plan | node -c confirmed). Then record the pre-port baseline: run `node scripts/run-tests.cjs --suite unit`; do NOT fix failures — write the failure/crash set (file names + one-line causes; fork-leftover requires of deleted sdk/ paths are expected) into a "Pre-port test baseline" appendix in 19-MERGE-AUDIT.md.
  </action>
  <verify>
    <automated>[ $(jj resolve --list | wc -l) -eq 0 ] && node -c tests/helpers.cjs && node -c tests/bug-3097-3099-executor-worktree-path-safety.test.cjs && [ ! -e tests/bug-2767-gsd-sdk-commit-files-flag.test.cjs ] && rg -c 'Pre-port test baseline' .planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-MERGE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `jj resolve --list` output is empty (zero conflicted paths in the tree)
    - Marker sweep with exclusion list returns zero files
    - helpers.cjs contains the string `gsd-core/bin/lib/vcs/index.cjs` and passes node -c
    - The pre-port baseline appendix lists the unit-suite outcome (pass/fail/crash per failing file)
    - Ledger rows for all four bucket F/G paths
  </acceptance_criteria>
  <done>MERGE-01 tree-level complete: zero conflicts, clean marker sweep, parse-valid CJS, baseline recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream CI definitions → fork repo automation | retained workflow files may smuggle org secrets/bot triggers |
| hand-merged CJS → runtime | parse-broken CJS takes down the whole toolchain (BLOCKER-1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Information Disclosure / Elevation | test.yml org secrets/bot refs | mitigate | explicit strip step + rg assertion for org tokens in verify |
| T-19-10 | DoS | hand-merged tests/helpers.cjs, github-release-notes.cjs parse breakage | mitigate | `node -c` on every hand-edited .cjs in the same task |
| T-19-11 | Repudiation | silently dropping fork behavioral guarantee in gsd-research-synthesizer.md | mitigate | explicit coverage diff + ledger row (BLOCKER-2 lesson) |
</threat_model>

<verification>
- `jj resolve --list` empty; tree-wide marker sweep clean modulo exclusions; all hand-edited .cjs parse; baseline appendix present; `vpzlrrlv` untouched.
</verification>

<success_criteria>
- Zero conflicts; bucket D/E/F/G content correct per locked decisions; pre-port baseline frozen for attribution.
</success_criteria>

<output>
Create `.planning/phases/19-upstream-merge-conflict-resolution-fork-abstraction-audit/19-04-SUMMARY.md` when done.
</output>
