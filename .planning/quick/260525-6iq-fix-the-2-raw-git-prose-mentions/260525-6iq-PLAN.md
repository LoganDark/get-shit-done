---
phase: 260525-6iq-fix-the-2-raw-git-prose-mentions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/workflows/execute-phase.md
  - get-shit-done/workflows/plan-phase.md
autonomous: true
requirements:
  - B1-AUDIT-CLOSE
must_haves:
  truths:
    - "node scripts/audit-workflow-raw-git.cjs exits 0 (Result: OK, not FAIL)"
    - "execute-phase.md no longer contains the literal substring 'git ' on line 558 (the PARALLEL-08 comment)"
    - "plan-phase.md no longer contains the literal substring 'git ' on line 1696 (the WIP-stash hint echo)"
    - "Both edited lines preserve their original semantic meaning — readers still understand the jj-@/git-detached-HEAD dispatch note and the WIP-resolution hint"
  artifacts:
    - path: "get-shit-done/workflows/execute-phase.md"
      provides: "Phase 14.1 PARALLEL-08 comment block reworded to avoid 'git ' substring"
      contains: "Bookmark-less jj"
    - path: "get-shit-done/workflows/plan-phase.md"
      provides: "Clean-WC error-message hint reworded to avoid 'git ' substring"
      contains: "if unrelated WIP:"
  key_links:
    - from: "scripts/audit-workflow-raw-git.cjs (baseline map line 76, 'execute-phase.md': 11)"
      to: "get-shit-done/workflows/execute-phase.md line 558"
      via: "raw-git regex scan over bash/sh/zsh fences"
      pattern: "execute-phase.md.*: 11"
    - from: "scripts/audit-workflow-raw-git.cjs (baseline map, 'plan-phase.md': 0)"
      to: "get-shit-done/workflows/plan-phase.md line 1696"
      via: "raw-git regex scan over bash/sh/zsh fences"
      pattern: "plan-phase.md.*: 0"
---

<objective>
Close BLOCKER B1 from `.planning/v1.4-MILESTONE-AUDIT.md` by rewording two prose mentions of the literal substring `git ` that sit inside bash fences in workflow markdown. The audit script `scripts/audit-workflow-raw-git.cjs` currently reports FAIL with 2 regressions (execute-phase.md: 12 actual vs 11 baseline; plan-phase.md: 1 actual vs 0 baseline). Both regressions were introduced by Phase 14.1's PARALLEL-08 refactor and never assigned to a phase.

Purpose: Audit script returns OK so v1.4 milestone gate can close. User explicitly rejected baseline-bump (option #2) and Phase 18 fold (option #1); this is option #3 — reword to non-raw-git phrasing.

Output: Two file edits + one audit-script verification. No `.planning/` mutations (quick mode owns STATE/ROADMAP updates in its own steps).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.4-MILESTONE-AUDIT.md
@.planning/phases/17-drift-control-reconciliation/17-CONTEXT.md
@scripts/audit-workflow-raw-git.cjs
@get-shit-done/workflows/execute-phase.md
@get-shit-done/workflows/plan-phase.md

<interfaces>
<!-- Audit script behavior the executor must align with. -->

The audit script `scripts/audit-workflow-raw-git.cjs`:
- Scans bash/sh/zsh fenced code blocks in every `get-shit-done/workflows/*.md` file
- Counts occurrences of a regex that matches the literal substring `git ` (the word `git` followed by a space and another character) — comments, echo strings, and real invocations all count equally
- Baseline map at line 76 freezes per-file expected counts
- Exits 0 only when every file's actual count matches its baseline (or is absent and 0)
- Reports `**Result:** OK` on success, `**Result:** FAIL` on regression with a per-file delta table

Current state from `.planning/v1.4-MILESTONE-AUDIT.md` B1:
- `execute-phase.md`: baseline 11, actual 12 (+1 from PARALLEL-08 comment at line 558)
- `plan-phase.md`: baseline 0, actual 1 (+1 from PARALLEL-08 echo at line 1696)

After this plan lands, both deltas must be zero.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword execute-phase.md PARALLEL-08 comment to avoid the literal 'git ' substring</name>
  <files>get-shit-done/workflows/execute-phase.md</files>
  <action>
    Edit line 558 of `get-shit-done/workflows/execute-phase.md`. The current line is a bash-fence comment introduced by Phase 14.1 PARALLEL-08:

      # Bookmark-less jj `@` and detached-HEAD git working copies pass cleanly.

    Replace it with this reworded form, which preserves the same meaning (both backends are first-class dispatch states even without bookmarks) while breaking the `git ` substring by reordering so `git` no longer precedes a space-letter pair inside the audited regex:

      # Bookmark-less jj `@` and detached-HEAD (on git) working copies pass cleanly.

    The parenthetical `(on git)` ends with `)`, not a space, so the audit regex (which matches `git ` — `git` + space + char) no longer fires on this line. The surrounding two comment lines (556–557 explaining the PARALLEL-08/D-02 zero-flags rationale) MUST remain untouched. Do not touch the dispatch command on lines 559–561 or any other part of the file.

    Use Edit with enough surrounding context to make the match unique — the natural anchor is lines 556–558 together (the full three-line comment block), since 557 ("yields empty mainBookmarks → fan-in skips the bookmark/ref advance step.") appears only once in the file.
  </action>
  <verify>
    <automated>grep -nE 'git [a-zA-Z]' get-shit-done/workflows/execute-phase.md | grep -c ':558:' | grep -q '^0$'</automated>
  </verify>
  <done>Line 558 no longer matches the `git [a-zA-Z]` pattern; the comment still reads as a coherent English sentence explaining that jj `@` and git detached-HEAD both pass cleanly through bookmark-less dispatch.</done>
</task>

<task type="auto">
  <name>Task 2: Reword plan-phase.md WIP-stash echo hint to avoid the literal 'git ' substring</name>
  <files>get-shit-done/workflows/plan-phase.md</files>
  <action>
    Edit line 1696 of `get-shit-done/workflows/plan-phase.md`. The current line is an `echo` inside a bash fence emitting a clean-WC-violation hint:

      echo "  - if unrelated WIP: jj abandon @ / git stash before re-running plan-phase" >&2

    Replace it with this reworded form, which keeps both backend mentions (jj abandon and git stash) explicit so the hint remains actionable for users on either VCS, but reorders the `git stash` mention so the literal substring `git ` (git + space + letter) no longer appears:

      echo "  - if unrelated WIP: jj abandon @ — or 'git stash' on the git side — before re-running plan-phase" >&2

    The single-quotes around `git stash` make the command name a quoted literal in the message, and the trailing `'` after `stash` (not a space) means the audit regex no longer matches that span. The `git side` later in the same line ends `git` with a space-then-`s`, so check that too: `git s` would match `git [a-zA-Z]`. Therefore prefer this alternative which avoids the issue entirely by dropping the second `git`:

      echo "  - if unrelated WIP: jj abandon @ — or stash via 'git'-side tooling — before re-running plan-phase" >&2

    Actually the cleanest form that preserves both backend mentions and definitely passes the audit regex `git [a-zA-Z]`:

      echo "  - if unrelated WIP: jj abandon @ (or stash via git, then re-run plan-phase)" >&2

    Here `via git,` has `git` followed by `,` (a comma, not a space-letter), so the regex does not match. Use this final form. The semantic content — telling the user to clean their working copy either via jj or via stash before retrying — is fully preserved.

    The surrounding echo lines (1691–1695) and the trailing `exit 1` / `fi` on 1697–1698 MUST remain untouched. Use Edit with enough surrounding context — anchor on lines 1695–1697 together (the previous echo about "planning artifacts dirty" through the `exit 1`) for a unique match.
  </action>
  <verify>
    <automated>grep -nE 'git [a-zA-Z]' get-shit-done/workflows/plan-phase.md | grep -c ':1696:' | grep -q '^0$'</automated>
  </verify>
  <done>Line 1696 no longer matches the `git [a-zA-Z]` pattern; the echo still conveys to the user that they should clean unrelated WIP via `jj abandon` or a git stash before re-running plan-phase.</done>
</task>

<task type="auto">
  <name>Task 3: Run the raw-git audit script and confirm it exits OK</name>
  <files>scripts/audit-workflow-raw-git.cjs</files>
  <action>
    Run `node scripts/audit-workflow-raw-git.cjs` from the repo root. This is the close gate for BLOCKER B1 — the script MUST exit 0 AND its stdout MUST contain `**Result:** OK` (not `**Result:** FAIL`).

    Do NOT edit the baseline map at line 76 (user explicitly rejected option #2 — "baseline bump"). The script should pass because Tasks 1 and 2 brought `execute-phase.md` back to 11 hits and `plan-phase.md` back to 0 hits.

    If the script still reports FAIL, do not paper over by editing baselines. Re-read the FAIL output's per-file delta table, locate the still-failing line(s) via `grep -nE 'git [a-zA-Z]'` on the named file inside bash/sh/zsh fences, and apply the same reword-don't-bump pattern as Tasks 1 and 2.
  </action>
  <verify>
    <automated>node scripts/audit-workflow-raw-git.cjs; test $? -eq 0</automated>
  </verify>
  <done>`node scripts/audit-workflow-raw-git.cjs` exits 0; stdout contains `**Result:** OK`; no FAIL deltas listed for `execute-phase.md` or `plan-phase.md`. BLOCKER B1 from v1.4-MILESTONE-AUDIT.md is now closeable.</done>
</task>

</tasks>

<verification>
End-to-end close gate:

```bash
node scripts/audit-workflow-raw-git.cjs
echo "audit exit: $?"
grep -nE 'git [a-zA-Z]' get-shit-done/workflows/execute-phase.md get-shit-done/workflows/plan-phase.md | grep -vE '^[^:]+:[0-9]+:.*```' || true
```

- First command MUST exit 0 and print `**Result:** OK`.
- Second grep is informational — it shows every remaining `git ` substring across the two files; the count for each file must still match the baseline (execute-phase.md: 11, plan-phase.md: 0) when filtered to bash/sh/zsh fences (the audit script handles that filtering; this raw grep is only for human spot-checking).
</verification>

<success_criteria>
- [ ] `get-shit-done/workflows/execute-phase.md` line 558 reworded; no longer contains `git ` (git + space + letter).
- [ ] `get-shit-done/workflows/plan-phase.md` line 1696 reworded; no longer contains `git ` (git + space + letter).
- [ ] Both reworded lines preserve the original semantic intent (PARALLEL-08 dispatch note; clean-WC WIP hint).
- [ ] `node scripts/audit-workflow-raw-git.cjs` exits 0 with `**Result:** OK`.
- [ ] No edits to `scripts/audit-workflow-raw-git.cjs` (baseline map untouched per user's rejection of option #2).
- [ ] No edits to `.planning/` (quick mode owns STATE/ROADMAP updates in its own steps).
- [ ] No other workflow markdown files modified.
</success_criteria>

<output>
Create `.planning/quick/260525-6iq-fix-the-2-raw-git-prose-mentions/260525-6iq-SUMMARY.md` when done.

Suggested commit message (single squash for the two file edits — the audit-script run in Task 3 is a verification step, not a commit):

  fix(workflows): reword PARALLEL-08 prose to clear raw-git audit (B1 closure)

  Closes BLOCKER B1 from .planning/v1.4-MILESTONE-AUDIT.md. Two prose
  mentions of the literal `git ` substring (a comment in execute-phase.md
  and an echo-hint in plan-phase.md, both introduced by Phase 14.1
  PARALLEL-08) were tripping scripts/audit-workflow-raw-git.cjs without
  representing actual raw-git invocations. Reworded both to preserve
  semantic meaning while breaking the audit regex. Baseline map and audit
  logic unchanged.
</output>
